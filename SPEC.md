# SPEC — Karttamaster SyöteMTB 2026

See VISION.md for product context and UX principles.

## §G Goal

SyöteMTB 2026 merkintätyökalu — suunnittelu, kenttätyö, purku yhdessä sovelluksessa. Korvaa paperinen kartta. Kaksi roolia: järjestäjä (suunnittelu + tilannekuva) ja talkoolainen (kenttätyö). Kaikki näkevät kaikkien statuksen.

Talkoolais-hub (T267–T276): talkoolainen kirjautuu yhdellä admin-asettamalla yleissalasanalla `/patkat`-sivulle → näkee kaikki pätkät (nimi/assignee/status/linkki) + FAQ → avaa pätkän kartalle. Järjestäjä pääsee samalle sivulle ja voi vaihtaa talkoo↔järjestäjä-näkymän valikosta tehdäkseen itse pätkiään (järjestäjä = usein itse reitintekijä).

## §C Constraints

- Stack: Vite + TypeScript + Leaflet + Bun. Ei frameworkkia.
- Scope: kaksi Syöte-tapahtumaa yhdellä kartalla: SyöteMTB (3 reittiä: 30/55/110-siirtymä) + Syötekylä Gravel Fest (3 reittiä) = 6 reittiä. Tapahtumat erotetaan värisävyperheellä (MTB=viileä/sininen, Gravel=lämmin oranssi-puna). Multi-event OK värein, ei erillisiä näkymiä. (amend 2026-07-24 T288: 5→6 reittiä, SyöteMTB sai 110km-siirtymän; oli aiemmin "5 reittiä" / "yksi tapahtuma, 4 reittiä, ei multi-event")
- Deploy: Fly.io, Docker, nginx. Basic auth tällä hetkellä.
- Offline: valinnainen, PWA myöhemmin.
- Ei PDF-tulostusta.
- Testit: Vitest (unit) + Playwright (e2e).
- Model B (T267/V188): yleissalasana = AINOA portti talkoolaiselle. Mikään pätkä ei aukea ilman talkoo-sessiota (tai järjestäjä-sessiota). Deep-link `/s/<slug>` vaatii session ensin; slug = ihmisluettava pätkävalitsin (reitin nimi), EI salaisuus.
- `/patkat` = uusi Vite-entrypoint (`patkat.html`), vanilla, sama pattern kuin `admin.html`/`inventory.html`.
- Reitistö on GRAAFI, ⊥ lineaarinen jana: reitit haarautuvat, jakavat osuuksia (3 SMTB-reittiä samalla polulla) & palaavat itsensä lähelle (lenkit, edestakaiset osuudet). Nykymalli = 1 polyline/reitti + 1 skalaari-km/merkki + pätkä = `[startDist,endDist]`. **Päätös 2026-07-25 (käyttäjä): A-linja** — km-väli SÄILYY pätkän tunnisteena, merkille lisätään km per reitti (V212). Klikkipolku-työkalu (T304) tulee PÄÄLLE, ⊥ tilalle.
- `?` PARKISSA (käyttäjä 2026-07-25 "annetaan olla, seuraa reittiä pääsääntöisesti ok"): tuotannon vanhan merkkidatan uudelleenlaskenta. Migraatio ajettiin 5/6 reitillä & eri primary-säännöllä (B117) ∴ osa km-arvoista väärin. ⊥ ajeta korjausmigraatiota nyt. Avoin: automaatti-recompute vs. epävarmat käsin (päätös siirtyy siihen asti kun oire häiritsee). Koodikorjaus (T299–T302) estää UUDEN väärän datan synnyn ilman että vanhaan kosketaan.

## §I Interfaces

- `public/route-*.gpx` — 4 GPX-tiedostoa (35km, 55km + 2 puuttuu?)
- `localStorage['karttamaster-layer']` — tile-layer preference
- ~~`localStorage['karttamaster-markers']`~~ POISTETTU T64/T65 — merkit kirjoitetaan suoraan backendiin (V18). Ainoa sallittu localStorage-kirjoituspolku on durable write-outbox (`karttamaster-outbox`, V116).
- `GET /api/markers?event=<id>` — hae kaikki merkit tapahtumalle
- `PUT /api/markers/:id` — luo tai päivitä merkki (upsert)
- `DELETE /api/markers/:id` — poista merkki
- `GET /api/events/:id` — tapahtuman metatiedot (GPX-polut jne.)
- `POST /api/segments` — luo/upsert segmentti (järjestäjä+)
- `PUT /api/segments/:id` — päivitä segmentti (järjestäjä+)
- `DELETE /api/segments/:id` — poista segmentti (järjestäjä+)
- `GET /api/segments/by-code/:code` — talkoolaisen oma pätkä (auth vaaditaan)
- Leaflet tile APIs: MML Taustakartta, MML Maastokartta, OSM
- `VISION.md` — product vision, luetaan suunnittelun ja testauksen referenssinä

## §R Research

| id | kysymys | löydös / status |
|----|---------|-----------------|
| R(faq-editor) | WYSIWYG-markdown-editori adminin FAQ:lle (T270) — "Word-tuntuma", inline-formatointi (boldit renderöityvät, ei raakoja `*tähtiä`), tulostaa markdownia, vanilla-JS (ei Reactia), Vite-buildattava, CSP-yhteensopiva | **RATKAISTU 2026-07-22: Milkdown Crepe (`@milkdown/crepe`).** Ainoa joka yhdistää aidon inline-WYSIWYG:n (Typora-tyyli, ei raakoja tähtiä) + markdown-natiivin (remark round-trip) + vanilla-init + aktiivisen ylläpidon (v7.21.3, MIT, commit 2026-07-22). Init: `new Crepe({root, defaultValue}).create()` / `.getMarkdown()` / `.destroy()`. CSS: `@milkdown/crepe/theme/common/style.css` + `theme/frame.css`. CSP-ok (DOMPurify, ei eval). **RISKI: iso bundle** (niputtaa ProseMirror+CodeMirror+katex+sisäinen vue; tree-shake-bugi #1533) → **mitigointi: admin-only + dynaaminen `import()`** → oma chunk, EI osu talkoolaisen bundleen. Varavaihtoehto: Toast UI Editor (kevyempi 546/162KB mutta STALE 2023). EasyMDE HYLÄTTY (ei aito WYSIWYG, tähdet näkyvät). T270: testaa md round-trip säilyy siistinä edit→save→render. Lähteet: npm/@milkdown/crepe, bundlephobia, GitHub #1533. |

## §V Invariants

| id | invariant |
|----|-----------|
| V1 | ~~Merkki snappaa lähimpään reittipistettä (kaikista reiteistä) bearing-laskentaa varten~~ POISTETTU T129 — bearing-feature poistettu, routeIds-snap säilyy V2:ssa |
| V2 | Merkki saa routeIds kaikista reiteistä joiden lähin piste on ≤100m |
| V3 | Merkit järjestetään distanceFromStart-arvon mukaan nousevasti |
| V4 | Drive mode step = lähimpään piste 50m eteenpäin/taaksepäin, clampaa [0, last] |
| V5 | Tile-layer valinta persistoi localStorageen, palautuu latauksessa |
| V6 | Vähintään yksi reitti on aina visible; viimeistä ei voi piilottaa |
| V7 | GPX-parsinta hylkää pisteet joissa lat tai lon ei ole numero |
| V8 | Bearing-laskenta: atan2(dx, -dy) merkistä kursoriin → asteet [0, 360) |
| V9 | Merkillä on elinkaari-status: suunniteltu → asetettu → tarkistettu → kerätty \| ei_tarpeen |
| V10 | Merkkikirjaston malli (SignTemplate) pitää sisältää: iconId (valinnainen Lucide-ikoninimi), teksti, kuvaus |
| V11 | Pätkä on jatkuva väli reitillä pisteestä Y pisteeseen X; ei voi olla epäjatkuva |
| V12 | Merkit persistoidaan sessioiden yli — ei kadota sivun päivityksellä |
| V13 | Järjestäjä ja talkoolainen ovat erilliset näkymät ja flow — eri layout, eri autentikaatio. Järjestäjä ei näe talkoolaisen työkaluja talkoo-layoutissa ja päinvastoin. **AMEND T274:** ~~Ei rooli-togglea kummallekaan~~ kumottu järjestäjän osalta — järjestäjä (rooli-hierarkia järjestäjä ⊃ talkoolainen, täydet oikeudet) VOI vaihtaa talkoo-layoutiin ja takaisin valikosta client-view-flipillä (sama järjestäjä-sessio, EI uutta autentikaatiota, V189). Talkoolaisen oma flow pysyy kapeana. Talkoolainen autentikoituu erikseen yleissalasanalla (V188). Perustelu: järjestäjä on usein itse reitintekijä, tekee pätkät itselleen. |
| V14 | Korruptoitunut tai tuntematon versio localStorage-datassa → silent reset: console.warn + removeItem + tyhjä tila; ei kaatumista |
| V15 | Merkki on drag-siirrettävissä kartalla — siirto laskee routeIds uudelleen uudesta sijainnista (sama logiikka kuin add). ~~+ bearing~~ POISTETTU T129 |
| V16 | Rotation arm ei häviä tahattomalla karttaklikillä — arm pitää poistaa vain explicit dismiss (Esc, toinen merkki, uusi lisäys) |
| V17 | Merkin tyyppi on vaihdettavissa jälkikäteen ilman delete+redo — kontekstivalikko tai lista |
| V18 | Backend on ainoa totuus. Merkit kirjoitetaan suoraan `POST/PUT/DELETE /api/markers` — ei localStorage-välivaihetta lukupolulle. `GET /api/markers` palauttaa kaikki merkit kaikille autentikoituneille käyttäjille. **Poikkeus (T183):** durable write-outbox (localStorage) epäonnistuneille kirjoituksille on SALLITTU resilienssimekanismi — se EI ole rinnakkainen totuus vaan retry-jono. Backend pysyy ainoana totuutena luettaessa; outbox vain takaa että jokainen kirjoitus lopulta osuu backendiin. Onnistuneen 2xx:n jälkeen outbox-entry poistetaan. |
| V21 | Merkki jonka routeIds on tyhjä ei saa tallentua hiljaa — `MarkerManager.add()` pakottaa vähintään lähimmän reitin routeIds:ään riippumatta etäisyydestä. Näytetään varoitus jos klikki >500m lähimmältä reitiltä. Ei koskaan hiljainen katoaminen. |
| V22 | Poistettu — kartta-tila-gate poistettu. Kaikki autentikoidut käyttäjät näkevät merkit aina. Snapshots ovat itsenäinen backup-mekanismi ilman hyväksymiskytkentää. |
| V24 | Snapshot restore on atominen (SQLite transaction) — kaikki merkit korvataan tai operaatio peruuntuu kokonaan. Restore luo uuden snapshottia ("Palautus: [alkuperäinen label]"). |
| V25 | Segment kattaa kaikki reitit (routeIds[]) jotka kulkevat sen fyysisen osuuden läpi. Talkoolainen käsittelee merkin kerran — kuittaus koskee koko merkkiä, ei per-route. Merkillä on yksi status riippumatta routeIds-määrästä. |
| V26 | Pätkällä on `phase: 'asettaminen' \| 'purku'`. Asettaminen ja purku ovat erillisiä pätkäjakoja — sama fyysinen osuus voi olla eri talkoolaisella eri vaiheissa. |
| V27 | `/s/<koodi>` URL ohjaa talkoolaisen suoraan omaan pätkänäkymään auto-autentikoinnilla — koodi pre-täyttää T51:n kirjautumislomakkeen ja skippaavat manuaalisen syötön. |
| V28 | Purku bulk-kuittaus on atominen: "merkitse kaikki kerätyksi" siirtää pätkän kaikki ei-terminal-merkit kerätty-tilaan yhdessä operaatiossa tai ei yhtään. |
| V29 | Auth-screen ei saa kutsua `onAuthenticated` ilman onnistunutta `/api/auth/me`- tai `/api/auth/login`-vastausta. Verkkovirhe tai non-401 → näytä kirjautumislomake. Ei silent skip. Dev-ympäristö ilman backendiä → lomake näkyy, ei ohita. |
| V30 | Segmentit persistoidaan backendiin. localStorage ei enää käytetä segmenteille — V36 on koko totuus. |
| V31 | Pätkän luontivaiheessa ensimmäinen klikattu piste visualisoidaan kartalla (väliaikainen markkeri) kunnes toinen piste klikataan tai luonti peruutetaan (Esc). |
| V32 | Pätkän startDist ja endDist ovat muokattavissa luonnin jälkeen — piste on siirrettävissä kartalla tai numeerisesti panelissa. Poista+redo ei ole ainoa vaihtoehto. |
| V33 | Kun rooli=talkoolainen JA talkoolainenCode on asetettu: merkki-modaali (☰ Lista) näyttää vain `getMarkersForSegment(seg, markers)` — ei globaalia listaa. Järjestäjälle näytetään aina kaikki. |
| V34 | Kun järjestäjä tallentaa `assignedCode` segmentille → `POST /api/admin/codes {code, display_name, segment_id}` kutsutaan välittömästi. Kun `assignedCode` poistetaan → `DELETE /api/admin/codes/:code`. `Segment.assignedCode` ja `talkoolainen_codes`-taulu pysyvät synkronissa — koodi löytyy backendistä ennen kuin URL jaetaan. API-virhe → älä päivitä localStoragea, näytä virheviesti. |
| V35 | `saveSegments` tallennusvirhe (quota exceeded tai write error) → älä ignoroi hiljaa. Kutsu virhe-callback tai heitä error; UI näyttää varoitusbannerin. Segment-data ei saa hävitä käyttäjältä näkymättömästi. |
| V36 | Segmentit persistoidaan backendiin (`segments`-taulu). Järjestäjä hallinnoi `/api/admin/segments` CRUD. Talkoolainen hakee oman pätkänsä `GET /api/segments/by-code/:code` auth-cookiella — ei tarvitse clientin localStoragea pätkädatan saamiseen. localStorage on vain cache. |
| V37 | Poistettu — inline expand/collapse korvattu modaaliksi (T69). Modal-pattern ei kärsi render()-nollausongelmasta. |
| V38 | Kaikki segment-kentän muutokset (description, equipment, assignedCode, startDist, endDist) tallentuvat sekä localStorage:en (`saveSegments`) että backendiin (`updateSegmentRemote`) — ei osittaisia tallennuksia. |
| V39 | Backend-sessio on roolin lähde. `RoleSelector`-nappi ei saa näkyä kummallekaan roolille kun `lockedRole` on asetettu — ei järjestäjälle eikä talkoolaiselle (V13). `setRole()` ei saa ylikirjoittaa backend-roolia. |
| V40 | `AuthScreen.start()` kutsuu `this.show()` ennen ensimmäistä `fetch`-kutsua — karttanäkymä ei saa olla näkyvissä ennen kuin auth on ratkaistu. Onnistunut `/api/auth/me` (200) → `this.hide()` ennen `onAuthenticated`-kutsua. |
| V41 | GPX-tiedostot voivat päivittyä milloin tahansa — reittimerkki (SignMarker) ei ole sidottu GPX-pisteisiin. Merkit säilyvät GPX-päivityksessä. `distanceFromStart` lasketaan uudelleen uudesta GPX:stä (snap lähimpään pisteeseen). ~~+ bearing~~ POISTETTU T129 |
| V42 | ~~Kaikki talkoolaisten URL:t hash-pohjaisia, ei arvattavissa~~ **AMEND T272 (Model B):** talkoolais-URL `/s/<slug>` EI enää tarvitse olla arvaamaton — yleissalasana (V188) on portti, mikään pätkä ei aukea ilman voimassa olevaa talkoo- tai järjestäjä-sessiota. Slug on ihmisluettava (reitin nimestä johdettu, uniikki V191). Turva tulee salasanasta, ei URL:n arvaamattomuudesta. Deep-link säilyy (jaettavissa WhatsAppilla/QR), mutta vaatii session ensin (V27 auto-login laukeaa vasta session jälkeen). |
| V43 | Talkoolainen voi muokata hänelle assignatun pätkän `startDist`/`endDist` kentällä — laajentaa tai lyhentää. Järjestäjä voi yliajaa muutoksen milloin tahansa. Muutos tallentuu backendiin (`PUT /api/segments/:id`). |
| V44 | Käyttäjäsyöte (templatejen label, shortLabel, description) ei interpoloida suoraan `innerHTML`-stringeihin — käytetään aina `escapeHtml()`-sanitointia tai DOM-assign (`textContent`/`.value`). Koskee kaikkia UI-komponentteja jotka renderöivät käyttäjän syöttämää tekstiä. |
| V45 | `GpsDrivePanel` instantioituu vain kun `talkoolainenCode` on asetettu. Järjestäjällä `gpsDrivePanel = null` → `gpsDrivePanel?.update()` on no-op → `#gps-drive-panel` pysyy `hidden`. |
| V46 | SignLibrary custom-mallit persistoidaan localStorage:een — `saveLibrary()/loadLibrary()` `src/logic/sign-library.ts`:ssä. Format `{version:1,templates:SignTemplate[]}`. Korruptoitunut data → silent reset (V14-pattern). `karttamaster-sign-library` avain. |
| V47 | Custom SignTemplate-tyypin merkki kartalla näyttää ympyrässä `shortLabel`-lyhenteen (ei `→`-nuolta). `SignMarker` tallentaa `color?: string` + `shortLabel?: string` templatestä — välitetään `createSignIcon`-kutsussa. |
| V48 | `MarkerManager.updateType(id, newType, color?, shortLabel?)` päivittää `m.color` ja `m.shortLabel` ennen `createSignIcon`-kutsua. Jos `color`/`shortLabel` puuttuu (default-4 tyyppi), ne tyhjennetään markerilta. Caller välittää template-metadata kun vaihdetaan custom-tyyppiin. |
| V49 | Pätkät eivät voi mennä päällekkäin samalla reitillä — overlap = startDist2 < endDist1 && startDist1 < endDist2. Uuden pätkän luonti tai muokkaus hylätään jos overlap syntyy. UI näyttää virheviestin. `validateNoOverlap(store, routeId, startDist, endDist): boolean` — `src/logic/segments.ts`. |
| V50 | SignTemplate.iconId on valinnainen Lucide-ikoninimi (string). Jos asetettu, kartalla merkin ympyrässä näytetään Lucide SVG-ikoni shortLabel-tekstin sijaan. Jos ei asetettu, shortLabel fallback (V47 ennallaan). |
| V51 | `createSignIcon` suunniteltu-status → `opacity:1.0`, `fill-opacity:0.55`, `stroke-dasharray:"4 2"`. Muut statusit → `opacity:1.0`, `fill-opacity:1.0`, ei dasharray. Ei opacity-eroa statuksien välillä — näkyvyys sama, erottelu dasharray+fill-opacity kautta. |
| V52 | ~~Jos `SignMarker.bearingManual === true`, `dragend` ei laske `bearingAtIndex` uudelleen...~~ POISTETTU T129 — bearing-feature poistettu kokonaan |
| V53 | Merkin voi poistaa vain modaalin kautta. ~~Context-menu (`showContextMenu`) tarjoaa vain "↻ Käännä" -napin — ei `deleteBtn`:ää.~~ Context-menu poistettu T130:ssa (oli olemassa vain käännölle) — poisto edelleen vain modaalin kautta, ei enää context-menu-huomiota tarvita |
| V54 | Pätkä-rivi sisältää max 3 elementtiä: nimi (truncated), km-väli, ···-nappi. Kaikki muokkaus- ja poistotoiminnot vain `openDetailsModal()`-modaalissa. `buildSegmentRow()` ei luo assign-, delete- eikä edit-pts-elementtejä. |
| V55 | `SegmentOverlay.update(store)` kutsutaan heti `new SegmentOverlay(...)` -konstruktorin jälkeen `src/main.ts`:ssä — pätkät näkyvät kartalla myös sivun refreshin jälkeen ilman käyttäjäinteraktiota. |
| V56 | `openDetailsModal()` save-nappi kutsuu `this.onUpdate()` tallennuksen jälkeen — `segmentOverlay.update(segmentStore)` triggeröityy ja kartta päivittyy välittömästi. |
| V57 | Pätkän poisto modaalissa vaatii `window.confirm()`-varmistuksen ennen `deleteSegment()`-kutsua. |
| V58 | Merkin poisto vaatii `window.confirm('Poistetaanko merkki?')` -varmistuksen ennen `manager.remove()`-kutsua. Koskee kaikkia delete-nappeja: `marker-list.ts` (nyt) ja T105 `MarkerDetailModal` (myöhemmin). |
| V59 | `MarkerDetailModal` talkoolainen-osio (status-napit) renderöidään kun `role === 'talkoolainen'` — ei `getTalkoolainenCode() !== null`. Segmenttikoodi ei ole ehto status-toiminnoille. Talkoolainen ilman pätkäkoodia saa silti kuitata merkit. |
| V61 | Jokainen `#left-panel`-osio noudattaa yhtenäistä section-patternia: klikattava `.left-panel-section-header` (`[▼/▶] [Nimi] [(count)]`, 11px uppercase text-muted) + item-lista + section-footer add-nappi. Uudet osiot lisätään samalla patternilla. |
| V62 | `#left-panel` item-riveillä ei koskaan inline delete-nappia. Kaikki tuhoavat toiminnot (`modal-btn-destructive`) asuvat ko. osion edit/details-modaalissa (DESIGN.md modal-footer-pattern). |
| V60 | ~~`bearing_manual` persistoidaan kolmessa kohdassa...~~ POISTETTU T129/T132 — bearing-feature poistettu kokonaan, sarake poistuu kannasta |
| V63 | Area-osion UI-tekstit käyttävät "Alue"/"Alueet" — ei "Huoltopiste"/"Huoltopisteet". Koskee section-header, add-footer, dots-nappi aria-label, prompt-teksti, modal-otsikko. |
| V64 | Area CRUD (lisäys/muokkaus/poisto/siirto) kutsuu välittömästi vastaavaa `/api/areas`-endpointia. `area-sync.ts` exporttaa `createArea`, `updateArea`, `deleteArea`. `setOnAreaMove` on wired `main.ts`:ssä → `updateArea` + `areaPanel.updateAreas()`. In-memory-only muutokset eivät riitä. |
| V65 | Area details -modaali näyttää `widthM`, `heightM`, `rotation`-kentät — alueen koko ja kierto muokattavissa ilman poista+redo. Tallenna-nappi kutsuu `updateArea`. |
| V66 | `MapRectEditor` tekee kaikki kulmalaskennat metrikoordinaateissa — ei suoraan lat/lng-deltoista. Rotaatiokulma: muunna (dlat,dlng) → (dNorthM, dEastM) aspect-ratio-kertoimella ennen `atan2`. Nurkkavetäminen: vastakkainen kulma pysyy paikallaan → uusi center = midpoint(vedetty, vastakkainen) → uusi widthM/heightM metriavaruudessa → takaisin lat/lng. |
| V67 | `AreaOverlay.updateOne(area)` korvaa vain ko. alueen layerit — ei full `clear()`. Käytetään drag-callbackeissa (corner, rotation, move) flikkauksen eliminoimiseksi. |
| V68 | `AreaFeature` sijoitetaan piirtämällä kartalle (`MapRectEditor.startDraw`) — ei auto-sijoitusta parent-alueen centeriin. Featuret ovat kartalla itsenäisiä: voi olla missä tahansa, ei vaadi yhteyttä toisiinsa. |
| V69 | `AreaOverlay` polygon-click kutsuu `onAreaClick` vain jos `mapRectEditor.isEditing() === false`. Edit-tilassa polygon-klikki ei avaa modaalia. Edit-tilasta poistutaan: Esc-keydown TAI karttaklikki polygon-ulkopuolella → `stopEdit()`. `MapRectEditor.isEditing(): boolean` palauttaa `this.editId !== null`. |
| V70 | `rotationArmTip` käyttää CW-rotaatiota (bearing-konventio, North=0°, East=90°): `rU = uM*cos(rad) + vM*sin(rad)`, `rV = -uM*sin(rad) + vM*cos(rad)`. Rotation=0 → arm osoittaa pohjoiseen (vM=+OFFSET), rotation=90 → itään (uM=+OFFSET). |
| V71 | `MapRectEditor.onCornerDrag` laskee uuden koon rotatoidussa kehyksessä: center = midpoint(vedetty, vastakkainen) world-koord, vedetty_local = latLngToLocal suhteessa uuteen centeriin, frame = rotate(vedetty_local, -rotation), widthM = 2×|frame.uM|, heightM = 2×|frame.vM|. Akseli-aligned `cornersToRect` ei riitä rotation≠0-tapauksessa. |
| V72 | Area details -modaalin ohjeteksti-kenttä on plain textarea (ei markdown-labellia, ei erillistä tallenna-nappia). Auto-save blur/Enter — sama pattern kuin T69 SegmentDetailsModal. `AreaMarker.markdownDescription` kentän nimi säilyy tietomallissa, mutta UI ei lupaa markdown-renderöintiä ennen T111. |
| V73 | Area details -modaali noudattaa modal-footer-patternia (V62): kaikki tekstikentät (nimi, koko/kierto, ohjeteksti) auto-save blur/Enter — ei erillisiä tallenna-nappeja. Footer sisältää "Poista alue" (modal-btn-destructive, window.confirm()) + sulkeminen ✕/Esc/backdrop. |
| V74 | Admin-rooli sisältää kaikki järjestäjän oikeudet + käyttäjähallinta. `requireRole('admin')` endpointit: `GET /api/admin/users`, `PATCH /api/admin/users/:id`, `POST /api/admin/users/:id/reset-password`. Admin pääsee myös kaikkiin `requireRole('admin','järjestäjä')` endpointteihin. |
| V75 | `users.is_active = 0` → `requireAuth()`-middleware palauttaa 401 `{error:'account_disabled'}` myös voimassa olevan session aikana. Tarkistus session validoinnin jälkeen — ei vain loginissa. Deaktivoitu käyttäjä ei pääse sisään. |
| V76 | Invite-token on kertakäyttöinen. `POST /api/auth/register` nollaa `invite_token = NULL` onnistuneen rekisteröinnin jälkeen. Sama token-mekaniikka toimii sekä uuden järjestäjän kutsumiseen että salasanan resetointiin — ei erillistä reset-endpointtia tarvita. |
| V77 | GPKG-import ei koskaan saa vaatia tunnettua/kovakoodattua layer-nimeä lukiessa ulkopuolista tiedostoa — GIS-työkalut (QGIS) nimeävät layerin uudelleen tallennuksen yhteydessä. Vain oman exportin kirjoittaessa saa nimetä layerin itse. |
| V78 | V21:n periaate koskee myös GPKG-tuontia: uusi merkki jonka `routeIds` on tyhjä ei saa jäädä hiljaa näkymättömäksi kartalla. `MarkerManager.fixOrphanRouteIds()` korjaa lähimmän reitin fallbackilla (`nearestRouteAssignment`, sama logiikka kuin `add()`) heti importin jälkeen — `GpkgControls`-onImported-callback kutsuu tätä `main.ts`:ssä. |
| V79 | Poistettu/stubattu ominaisuus (esim. `MapStateBadge` approval-konseptin poiston jälkeen) vaatii samassa committissa e2e-testin poiston tai päivityksen — kuollutta DOM-elementtiä testaava testi ei saa jäädä sviittiin merkiksi väärästä regressiosta. |
| V81 | Uusi tai muutettu tekstiväri-token pitää tarkistaa WCAG AA (4.5:1) sitä taustaa vasten jossa sitä oikeasti käytetään (esim. `--surface-card`, ei vain `--surface-app`) — pelkkä "näyttää OK:lta desktop-monitorilla" ei riitä, laske kontrastisuhde ennen käyttöönottoa.
| V86 | ~~`createSignIcon`/`circleSvg` täyttää päämerkin ympyrän statuksen mukaisella värillä...~~ KORVATTU V87:llä T140:ssä — käyttäjä halusi tyyppikuvan pysyvän muuttumattomana (myös kerätty-tilassa) ja statusvärin siirtyvän ulkoreunaan |
| V87 | `createSignIcon`/`circleSvg`: päämerkin ympyrän **täyttö on aina tyyppiväri/-kuva** (myös kerätty/ei_tarpeen — ei glyfikorvausta). Statusväri (`STATUS_RING`) näkyy ympyrän **ulkoreunassa** (`stroke`, leveys 4px) statuksissa asetettu/tarkistettu/kerätty/ei_tarpeen — suunniteltu/upcoming säilyttävät valkoisen/dashed-reunan ennallaan. Neljä keskenään selkeästi erottuvaa reunusväriä (vihreä/sininen/violetti/harmaa), ei saman perheen pastelleja. |
| V88 | ~~Pätkän edistyminen näytetään kartan alla omana pienenä elementtinä, ei sivupalkin segmenttirivin perässä~~ KUMOTTU V89:llä T142:ssa — live-testissä käyttäjä totesi kartan-alle-sijoittelun huonoksi, halusi takaisin sivupalkkiin. |
| V89 | ~~Pätkän status-lukumäärä (kaikki 4 statusta) näytetään sivupalkin pätkäjako-listan rivillä~~ TÄYDENNETTY V90:llä T143:ssa — täysi breakdown ei mahtunut ahtaaseen riviin, tiivistetty yhdeksi phase-tietoiseksi luvuksi. |
| V90 | Pätkäjako-listan rivi (`.segment-km`) näyttää **yhden** phase-tietoisen luvun täyden 4-statuksen breakdownin sijaan: `phase: 'asettaminen'` → `"<done>/<total> asetettu"` (done = asetettu+tarkistettu+kerätty), `phase: 'purku'` → `"<done>/<total> kerätty"` (done = vain kerätty). Tyhjä pätkä: "ei merkkejä". Täysi breakdown + km-alue säilyvät `title`-attribuutissa (hover). `getPhaseProgress(segment, markers)` `src/logic/segments.ts` — lookup phase→target-status, ei if-ketjua, jotta tuleva `tarkastus`-phase on helppo lisätä. Rivi päivittyy sekä segmentin mutaatiosta että merkin status-muutoksesta (sama kaksi kutsupaikkaa kuin V89: `SegmentPanel.render()` + `refreshCounts()`). |
| V91 | `Segment.phase` on kolmijakoinen: `'asettaminen' \| 'tarkastus' \| 'purku'` (laajennus V26:sta). Sama fyysinen osuus voi olla samanaikaisesti eri pätkäjaossa eri vaiheessa (V26-periaate säilyy) — järjestäjä kloonaa asetettu-pätkän uuteen phase-arvoon (T146), vanha jako säilyy muuttumattomana. Ei migraatiota olemassa olevalle datalle: kaikilla nykyisillä segmenteillä on jo `'asettaminen'` tai `'purku'`, uusi arvo on vain lisäys enum-tyyppiin. |
| V92 | Talkoolaisen ohjattu status-flow (`validActions()`, `src/logic/marker-status.ts`) EI enää pakota `asetettu→tarkistettu→kerätty`-ketjua — `asetettu→kerätty` on suora, kelvollinen siirtymä (`kerää`-action lisätään `asetettu`-tilan transitioihin). `tarkistettu`-status-arvo EI poistu tyypistä eikä vaadi migraatiota: se pysyy järjestäjän manuaalisesti valittavana (V84, `MarkerDetailModal`) ja talkoolainen voi yhä käydä sen kautta halutessaan (`tarkista`-action säilyy `asetettu`-tilassa vaihtoehtona, ei pakkona). Tarkastus siirtyy ensisijaisesti segmentin `phase: 'tarkastus'` -tasolle (V91) — ei enää per-merkki-pakkokuittaus. |
| V80 | `#btn-role`-rooli-toggle on vain silloin interaktiivinen kun `RoleSelector` saa `lockedRole===undefined`. Tili-per-rooli-authin myötä `main.ts` antaa aina backendin roolin → toggle pysyy piilossa kaikilla oikeilla tileillä. E2e-testit jotka tarvitsevat toisen roolin näkymän kirjautuvat suoraan sillä roolilla (`mockAuthAsJarjestaja`/`mockAuthAsTalkoolainen`) — eivät koskaan klikkaa `#btn-role`. |
| V82 | Merkin klikki-kuuntelija käyttää Leafletin omaa event-systeemiä (`marker.on('click', ...)`), ei raakaa `element.addEventListener('click', ...)`:ia — vain Leaflet-taso suppressoi synteettisen clickin dragendin jälkeen. Drag-siirto ei koskaan avaa `MarkerDetailModal`ia sivuvaikutuksena. |
| V83 | Merkkikirjaston (`SignLibraryPanel`) jokainen rivi — myös custom-mallit — on suoraan sijoitettavissa kartalle klikkaamalla riviä sivupalkissa (arm + seuraava karttaklikki sijoittaa), ei vaadi ennakkoon suosikiksi merkitsemistä. `favorite`-kenttä vaikuttaa vain floating-pickerin (dblclick-kartalla) listaan, ei sivupalkin sijoitettavuuteen. |
| V84 | Järjestäjä voi vaihtaa minkä tahansa merkin `status`-arvon suoraan `MarkerDetailModal`ista, kaikki `MarkerStatus`-arvot valittavissa milloin tahansa — ei rajattu `validActions()`-osajoukkoon (se koskee vain talkoolaisen ohjattua flow'ta). |
| V85 | Merkki-ikonissa (`createSignIcon`/`circleSvg`) ei toisteta samaa tyyppi-/lyhennetekstiä kahdesti samassa ikonissa. Statusta (`MarkerStatus`) osoittaa yksi visuaalisesti selkeä, riittävän kontrastinen elementti (ei pelkkä 8px piste) — järjestäjä erottaa asetettu/ei-asetettu-tilan kartalta yhdellä silmäyksellä (VISION.md järjestäjän UX-testi). |
| V93 | `Segment.inspected` + `inspectionNote` persistoituvat backendiin: `segments`-taulussa sarakkeet, `rowToSegment` palauttaa, POST/PUT tallentavat. Talkoolainen jonka `session.talkoolainen_code` = segmentin `assigned_code` saa PUT:lla päivittää oman pätkänsä kentät `inspected/inspectionNote/startDist/endDist` (V43-periaate) — muut kentät järjestäjä+. `updateSegmentRemote`-epäonnistuminen (false-paluu tai reject) ⊥ hiljainen — UI näyttää virheen (V35-pattern). |
| V94 | Uusi pätkä syntyy järjestäjän aktiiviseen phase-näkymään (`getActivePhase()`), ⊥ hardcoded `'asettaminen'`. Luotu pätkä näkyy heti siinä näkymässä jossa se luotiin. |
| V95 | V49-overlap pätee vain saman `phase`-arvon sisällä — eri phasejen pätkät saavat olla päällekkäin samalla reitillä (V91). `validateNoOverlap` saa phase-parametrin. `cloneSegmentToNextPhase` validoi kohde-phasen overlapin ennen luontia — duplikaattiklooni hylätään + UI-virheviesti. |
| V96 | Pätkän karttaväri on stabiili per segment-id (⊥ lista-indeksi — pätkän poisto ei saa vaihtaa muiden värejä) & phase-progress luettavissa kartalta visuaalisesti ilman sivupalkkia (VISION järjestäjä-UX-testi: "näenkö kartalta yhdellä silmäyksellä missä vaiheessa mikäkin pätkä on"). **Poikkeus värikanavaan (T348, 2026-07-26):** `valmis`-tila ohittaa tunnistevärin kiinteällä `--confirm`-vihreällä (`SEGMENT_DONE_COLOR`) — valmiin pätkän identiteetti ⊥ enää kanna tietoa (kukaan ⊥ etsi kartalta "kuka teki tämän loppuun"), status kantaa. Stabiilius pätee muuttumattomana `kesken`/`ei_alkanut`-tiloissa & `colorForSegment` säilyy sivupalkin/listan tunnistevärinä ilman status-kontekstia (rivi & kartta ⊥ saa olla eri mieltä identiteetistä). Vihreä sävyperhe on VARATTU status-kanavalle: `SEGMENT_COLORS` ⊥ saa sisältää vihreää (T304-paletti, V244:n `SEGMENT_COLORS ∩ ROUTE-värit = ∅` -ehdon rinnalle). |
| V97 | `SignTemplate.id` on käsin annettu, uniikki kirjastossa & muotoa `/^[A-Za-z0-9_-]+$/` (filename-safe — id toimii sekä GPKG-export-`type`-koodina että kuva-tiedoston avaimena). EI `crypto.randomUUID()`. `createTemplate` hylkää duplikaatin/virheellisen id:n (ei kirjoita kirjastoon). id on muuttumaton luonnin jälkeen (avain). |
| V98 | GPKG-export `properties.name` = merkin `label` (denormalisoitu templatelta markers-tauluun); round-trip import (export→QGIS→import) säilyttää `label`in. `type` = template-id (V97), ei UUID. |
| V99 | Sign-visualin valintajärjestys on **kuva > ikoni > johdettu compactLabel(label)**: template-kuva jos on, muuten Lucide-ikoni (`iconId`), muuten `compactLabel(label)` = `label.slice(0,3).toUpperCase()` (esim. "Oikealle"→"OIK"). EI erillistä `shortLabel`-kenttää (poistettu T160 — oli koodillinen kenttä ilman käyttäjäarvoa; `label` denormalisoitu merkkiin T157:ssä). Puuttuva kuvatiedosto → fallback ikoniin/compactLabeliin, ei rikkoudu (`<img onerror>`). Kuva täyttää karttamerkin ympyrän tyyppi-identiteettinä (V87-yhdenmukainen: täyttö = tyyppi, status = ulkoreuna). |
| V100 | Snapshot (auto + manuaali) & restore kattaa **koko datasetin**: markers + segments + areas + area_features, yhtenä versioituna JSON-blobina (`{version, markers, segments, areas, areaFeatures}`). Restore korvaa nämä taulut atomisesti yhdessä SQLite-transaktiossa (V24 laajenee koko datasettiin — kaikki tai ei mitään). Snapshotin serialisointi on yksi kanoninen funktio (⊥ copy-paste scheduler vs route). Backward-compat: legacy markers-only-snapshot (ei `version`/segments/areas-avaimia) restore korvaa VAIN markers-taulun — ei tyhjennä segments/areas (⊥ hiljainen data-häviö olemassa olevalle pätkäjaolle). |
| V101 | Automaattinen yövarmuuskopio ⊥ riipu siitä onko fly-koneella aktiivista käyttäjää. In-process `setInterval` ei riitä (`auto_stop_machines=stop` → ajastin kuolee koneen mukana). Automaattikopio laukaistaan ulkoisesti (ajastettu ping autentikoituun snapshot-endpointtiin, joka `auto_start_machines`-koneen herättää) — riippumaton user-trafiikista. Endpoint suojattu jaetulla salaisuudella (env), ⊥ session-auth. |
| V102 | Järjestäjä voi ladata koko datasetin (V100-blob) tiedostona omalle koneelle (off-site-turva fly-volyymin 5 pv snapshot-ikkunan yli) & palauttaa saman formaatin tiedostosta. Export/import käyttää **samaa** kanonista V100-serialisointia — ⊥ kahta rinnakkaista formaattia. Tiedostosta-palautus on tuhoava → `window.confirm()` ennen. |
| V103 | Fly deploy sisältää HTTP health checkin (`[[http_service.checks]]`) joka pollaa `/api/health`. nginx (:80) palauttaa 200 staattiselle indexille vaikka taustan bun-serveri (`/api/*`) olisi kuollut → pelkkä TCP/root-check ⊥ riitä. Kaatunut serveri → fly restart. |
| V104 | `.dockerignore` excludeaa dev-datan & salaisuudet build-kontekstista: `.env*`, `*.db*` (dev.db + WAL), `node_modules`, `.git`, `dist`, `test-results`, `tests`, `e2e`, `.htpasswd`. Tuotanto-image ⊥ sisällä kehitysdataa/kredentiaaleja (`Dockerfile COPY . .` kopioi muuten kaiken). |
| V105 | Secretit (`ADMIN_PASSWORD`, `ADMIN_USERNAME`, `SNAPSHOT_CRON_TOKEN`, MML-avain) luetaan vain runtime-envistä (fly secrets → `process.env`). ⊥ committoidu gittiin (`.gitignore` kattaa `.env`) eikä bundlaudu Docker-imageen (V104). Fly secret = ainoa lähde tuotannossa; `.env` on paikallinen dev-override. |
| V106 | `SignMarker.iconId` denormalisoidaan markerille luontihetkellä (T89-pattern, sama kuin `color`/`label`) — kartalle asetetun merkin ikoni-tier (V99 kuva>ikoni>label) toimii aina riippumatta siitä onko template myöhemmin muuttunut. `createSignIcon()`-kutsu välittää aina markerin oman `iconId`-kentän, ei koskaan `undefined`. |
| V107 | `SignTemplate.parts` (yhdistelmämerkki) pinoutuu **pystysuunnassa** kuten oikea liikennemerkkikeppi — ylin osa `parts[0]`, sen alla `parts[1]` jne, järjestys merkitsee. Max 4 osaa V1:ssä (talkoolaistesti: metsässä auringossa pienellä puhelimella pidempi pino vaikeuttaa tunnistusta). Jokainen osa resolvoituu itsenäisesti kuva>ikoni-precedencellä (EI label-fallbackia per osa — combo-osa on aina tarkoituksella valittu). Fyysinen ankkuripiste (kepin kärki) on yksi ja sama koko pinolle, alimman osan alla — merkki on yksi kartalla oleva objekti. Status näkyy yhteisenä koko pinon ulkoreunana (V87), ei per-osa. |
| V108 | Kartan zoom-hyppy on 0.25 (`zoomSnap`/`zoomDelta`) — ei kokonaislukuzoomia. Zoomista riippuva logiikka (esim. `area-overlay.ts` label-opacity, `tile-layers.ts` `maxNativeZoom`) tarkistaa aina raja-arvoa numerovertailuna, ei oleta kokonaislukuzoomia. |
| V109 | Kartan merkki-ikonit skaalautuvat zoomin mukaan CSS `transform: scale()` -tyylillä marker-elementtiin, `transform-origin: center bottom` (ankkuripiste = tip). Skaalaus ⊥ koskaan piilota statusrenkaan väriä (ei clusterointia/dot-only-tilaa joka menettäisi statustiedon) — pienikin skaalattu ikoni säilyttää saman SVG:n kuin täysikokoinen, vain CSS-transform pienentää. |
| V110 | `/auth/invite/:token`-polku (kutsu uudelle järjestäjälle TAI salasanan resetointi, V76-sama-token-mekaniikka) pitää route:ata client-puolella omaan lomakkeeseen (tunnus + salasana + vahvista salasana) — EI koskaan pelkkää normaalia login-lomaketta. `auth-screen.ts` tunnistaa polun samalla tavalla kuin `/s/<code>` (`extractSegmentCode`-pattern), validoi tokenin `GET /api/auth/invite/:token`illa, submit → `POST /api/auth/register`. Invalid/expired token → virheviesti, ei silent fallback loginiin. |
| V111 | SignLibraryPanel edit-modaalin pää-visuali (yksittäinen ikoni/kuva-valinta) ja "Osat"-lista (yhdistelmämerkki, `parts[]`) EIVÄT saa olla käyttäjälle kaksi eri asiaa jotka pitää täyttää erikseen (B78). Kun combo on aktiivinen (`parts.length > 0`), pää-visuali edustaa aina `parts[0]`:aa kahteen suuntaan synkassa: top-valinta kirjoittaa `parts[0]`:n, osalistan poisto/järjestys index 0:ssa kirjoittaa top-valinnan takaisin. "+ Lisää osa" -ensiklikkaus siirtää senhetkisen top-valinnan (jos on) listan ekaksi riviksi ennen pickerin avaamista — käyttäjä ei koskaan valitse samaa merkkiä kahdesti. |
| V114 | Mobiilissa (≤480px) `#left-panel` EI saa varastaa kartan leveyttä flex-siblinginä — 260px kiinteä paneeli (`flex-shrink:0`, `src/style.css:2257`) jättää 375px-viewportissa `#map-area`:lle vain ~115px = kartta on sliver, VISION "kartta on pääasia myös mobiilissa" rikkoutuu. Mobiilissa paneeli overlay/drawer (`position:absolute` `#map-area`:n päällä, oma z-index + varjo) SIL­LOIN kun auki, ja/tai default-collapsed pienellä näytöllä → kartta saa aina täyden leveyden alleen. Paneelin toggle (32px collapsed-strip) pysyy saavutettavana. Yhdistyy V112:een: paneelin avaus/sulku mobiilissa kutsuu `map.invalidateSize()` jotta kartta repaintaa täyteen leveyteen. |
| V113 | ~~Yläkulman toolbar-napit touch-turvallisia~~ **KORJATTU (T180-suunnitteluvaihe):** alkuperäinen analyysi virheellinen — `#btn-role` on AINA `hidden=true` ajossa (`role-selector.ts:13-17`, V80: tili-per-rooli-auth antaa aina `lockedRole`:n) → ei vahinko-moodivaihtoriskiä siinä, kuollutta UI:ta. `#btn-gps` piilossa järjestäjältä (`data-role-hide="järjestäjä"`) → järjestäjän toolbarissa ajonaikaisesti vain 1 nappi (`#btn-menu`), ei 3 napin rykelmää. Oikea invariantti: overflow-valikon (`#toolbar-menu`) sisäinen interaktiivinen kontrolli (esim. `PhaseSwitcher`-`<select>`, `src/ui/phase-switcher.ts`) EI saa keskeytyä dokumentti-tason "klikkaa ulkopuolelle sulkee menun" -listenerin takia (`map-init.ts` `document.addEventListener('click', ...)` ilman poikkeusta select-elementin omalle interaktiolle) — natiivin pudotusvalikon avaus/valinta ei saa katketa kesken. VISION §Järjestäjä-mobiili "moodia ei saa vaihtaa vahingossa" täyttyy pitämällä vaihe-valinta keskeytymättömänä, ei toolbar-nappien koolla (ne olivat jo 44px, DESIGN.md §R). |
| V112 | Karttakontainerin koon muuttuessa on AINA kutsuttava `map.invalidateSize()` — Leaflet ei havaitse kontainerin resizea itse, ja ilman kutsua laatat renderöityvät vanhaan viewport-kokoon → puoli karttaa jää lataamatta. Laukaisijat: vasemman paneelin toggle (`left-panel.ts`), `window resize`, `orientationchange`, `visualViewport` resize (mobiilin osoitepalkin piilo/näyttö scrollatessa). Resize-tulville debouncattu (~150ms). Karttakontainerin korkeus `100dvh`/`100svh`, EI `100vh` (mobiilin dynaaminen osoitepalkki muuttaisi korkeutta ja jättäisi harmaan raidan). |
| V115 | Kaikki client→server-kirjoitukset (POST/PUT/DELETE fetch merkeille, pätkille, muulle mutatoivalle datalle) TARKISTAVAT `response.ok`/status-koodin, EIVÄT vain verkkovirhettä (`.catch`). Epäonnistunut kirjoitus (401/403/400/500) näytetään käyttäjälle näkyvästi (esim. toast) — merkki/muutos ei saa jäädä hiljaiseksi "haamuksi" joka näkyy lokaalisti mutta katoaa refreshillä (B82, sama antipattern jo B62:ssa segmenteille). |
| V116 | Jokainen merkki/pätkä/alue-kirjoitus (POST/PUT/DELETE) joka ei saa 2xx-vastausta persistoidaan durable-outboxiin (localStorage) ja uudelleenyritetään kunnes onnistuu. Sivun päivitys EI saa kadottaa vahvistamatonta kirjoitusta — outbox ladataan käynnistyksessä ja tyhjennetään retryllä. Ei koskaan hiljainen kato. Menee V115:tä pidemmälle: V115 näyttää virheen, V116 takaa ettei data katoa. (B82, B85) |
| V117 | Vahvistamaton kirjoitus (outboxissa) näkyy käyttäjälle persistentisti — merkki visuaalisesti erottuva "tallentamatta"-tilassa kunnes 2xx vahvistaa. EI transientti banner ainoana signaalina. Käyttäjä näkee yhdellä silmäyksellä mikä ei ole tallessa. |
| V118 | `fetchMarkers`/`fetchAllSegments`/`fetchAreas` erottaa "0 tulosta" ja "lataus epäonnistui". Verkko-/HTTP-virhe latauksessa EI palauta hiljaa tyhjää → näytetään latausvirhe eikä tyhjää karttaa, jottei käyttäjä luule dataa hävinneeksi ja luo duplikaatteja. Epäonnistunut lataus ei myöskään saa ylikirjoittaa aiempaa tilaa tyhjällä. |
| V119 | Kirjoituksen 401-vastaus (session vanhentunut kesken session) laukaisee re-auth-kehotteen — työtä ei jatketa hiljaa tilassa jossa mikään ei tallennu. Erityisesti talkoolaisen 24h-session vanhenee day-2 kenttätyössä; outbox säilyy re-authin yli ja retrytään kirjautumisen jälkeen. |
| V120 | Prod-SQLite-kirjoitukset kestävät fly-koneen auto-stopin. `server/db.ts`: `PRAGMA synchronous=FULL` WAL-moodissa (commit fsync per-transaktio, ei vain checkpointissa). `server/index.ts`: SIGINT+SIGTERM → `PRAGMA wal_checkpoint(TRUNCATE)` + `db.close()` ennen exitiä (WAL foldataan main-tiedostoon ennen VM-stopia). Hiljainen datan katoaminen palvelinpuolella kielletty — jatkaa V100/V101-datansuojalinjaa server-kirjoituksiin. |
| V121 | Poistetun featuren sarake ei saa jäädä NOT NULL -ansaksi olemassa oleviin kantoihin. Kun sarake poistetaan koodin INSERT/UPDATE-lauseista (esim. `bearing` T129/T132), on kirjoitettava idempotentti DROP-migraatio `server/db.ts`:ään — muuten ennen muutosta luotu taulu (tuotanto) säilyttää NOT NULL -sarakkeen ilman defaultia ja jokainen INSERT kaatuu `SQLITE_CONSTRAINT_NOTNULL`. `CREATE TABLE IF NOT EXISTS` EI muuta olemassa olevaa taulua → skeema ajautuu erilleen dev (tuore DB) ja prod (vanha DB) välillä. |
| V122 | Kartan Leaflet-kontrollit (zoom +/-) EIVÄT saa jakaa vasenta yläkulmaa (`topleft`) sivupalkin `▶`-toggle-napin kanssa — mobiililla ne peittäisivät toisensa ja sivupalkki jäisi avaamattomaksi. Zoom-kontrolli ankkuroidaan `topright`iin (`L.control.zoom({ position:'topright' })`), panel-toggle säilyy vasemmalla. Kumpikaan interaktiivinen kontrolli ei saa olla toisen touch-targetin (≥44px) päällä millään näyttökoolla. |
| V123 | Merkkikirjaston mallit (SignTemplate) tallennetaan backendiin (`templates`-taulu). `GET /api/templates` palauttaa kaikki mallit kaikille autentikoiduille käyttäjille (kuten V18 merkeille). localStorage on vain cache, ei rinnakkainen totuus lukupolulle. |
| V124 | Template-kirjoitukset (POST/PUT/DELETE) kulkevat durable write-outboxin kautta (V116-pattern), `resourceKey: 'template:<id>'`. 2xx poistaa entryn. `fetchTemplates` erottaa lataus-epäonnistumisen tyhjästä tuloksesta (V118 diskriminoitu union) — UI näyttää latausvirheen eikä hiljaa tyhjää kirjastoa. |
| V125 | Merkkikirjasto seedaa TYHJÄNÄ — ei oletusmalleja. `seedDefaults`/`signCatalog`/`DEFAULT_IDS` palauttavat tyhjän. Järjestäjä rakentaa jokaisen mallin itse. |
| V126 | SignLibraryPanel ryhmittää mallit: "Suosikit"-osio ensin (favorite:true, aina auki), sitten "Muut"-väliotsikko + loput label-aakkosjärjestyksessä (`localeCompare 'fi'`). Ei collapsible-accordionia. Rivi näyttää koko `description`-kentän (escapeHtml, V44) — ei vain label/shortLabel. |
| V127 | Mobiilissa (≤480px, V114-overlay-left-panel) merkin valinta sivupalkin listasta (marker-list) sulkee `#left-panel`in (`.collapsed`) — koska paneeli overlayaa kartan, valitun merkin ja detail-modaalin on jäätävä näkyviin. Desktopilla (>480px) paneeli säilyy auki (tilaa riittää, ei overlayta). Sulkeminen tehdään aina merkin valinnassa, ei kartan `panTo`-only-polussa. |
| V128 | MarkerDetailModal EI auto-focusoi mitään textarea/input-kenttää `open()`-vaiheessa — virtuaalinäppäimistö ei saa avautua ennen kuin käyttäjä eksplisiittisesti tapittaa kenttää. Mobiilissa auto-näppäimistö vie ruututilaa ennen kuin käyttäjä ehtii lukea merkin tietoja. Fokus siirtyy kenttään vain käyttäjän omasta interaktiosta. |
| V129 | Pienten zoom/lightbox-badgien (esim. `.marker-visual-row-zoom`, `.sign-image-zoom-btn`) klikattava hit-area on AINA ≥44×44px (§A) riippumatta näkyvän glyfin koosta — näkyvä badge saa olla visuaalisesti pieni, mutta klikattava alue laajenee paddingilla/positioinnilla täyteen 44px:ään. Näkyvä koko ja klikattava koko ovat eri asia; testissä tarkistetaan CSS width/height, ei vain elementin olemassaolo. |
| V130 | Merkkikirjaston uusi/muokkaa-modaalin ("Kuva"-välilehti) kuvagalleria renderöi valittavat thumbnailit KAIKILLA näyttöleveyksillä, myös ≤480px mobiililla. Sisäkkäiset scroll-containerit eivät saa piilottaa galleriasisältöä: modaali (`.sign-lib-modal`, `sign-library-panel.ts:249` `max-height:80vh;overflow-y:auto`) sisältää gallerian jolla oma `max-height:min(50vh,420px);overflow-y:auto` (rivi 379) flex-column-lapsena — matalalla/kapealla mobiiliviewportilla lapsi ei saa collapsata 0-korkeuteen. `style.css` `@media (max-width:480px)` (rivi 2369) käsittelee vain `#left-panel`in → modaalin/gallerian mobiililayout on katettava erikseen. Testataan Playwright 375×667: "Kuva"-välilehti → ≥1 näkyvä `.sign-image-thumb` `<img>` (boundingBox height>0). |
| V131 | Template-kuvan lataus backendiin (T196): `template_images`-taulu (blob) + `POST /api/templates/:id/images` (multipart field `image`, requireRole admin/järjestäjä, max 8MB, `image/*`) → `{url}`; `GET /api/templates/:id/images/:imageId` (requireAuth) serveroi blobin. POST EI vaadi template-rivin olemassaoloa — id on käsin annettu (V97) ja kuva ladataan usein luonti-modaalissa ennen mallin tallennusta. Ladatun kuvan `imageId` = backend-URL. `signImageSrc(imageId)` palauttaa absoluuttisen polun (`/`- tai `http`-alkuinen) sellaisenaan, muuten bundle-webp-lookup — bundled-templaten `imageId==id`-konventio säilyy (V99 ennallaan). `SignMarker.imageId` denormalisoidaan (kuten `iconId` V106): kartan kuva-tier `signImageSrc(m.imageId ?? m.type)` näyttää ladatun kuvan sijoitetussa merkissä myös kun template muuttuu myöhemmin; persistoidaan `markers.image_id` (POST/PUT/fetchMarkers, idempotentti ALTER-migraatio). Puuttuva kuva → `<img onerror>` fallback ikoniin/labeliin (V99). |
| V132 | UI-teema (T202) on **käyttäjän vapaasti valittavissa, EI roolisidottu** — kaksi teemaa: **Reittimerkki-vaalea** (`:root`, oletus) ja **Kaamos-tumma** (`[data-theme="dark"]`). Valinta persistoidaan localStorageen (V5-pattern, esim. `karttamaster-theme`) ja palautuu latauksessa; toggle tilivalikossa (T203). Korvaa vanhan roolisidotun `daylight`/`dark`-jaon. Reittimerkki-vaalea chrome: `--surface-app #EDF1EC` (birkkupaperi), `--surface-card #F6F9F5`, `--surface-raised #fff`, `--text-body #17221D` (kuusenmusta), `--text-muted #5B6A61`; aksentti `--accent #F2542D` (huomionauha, korvaa amber `#f59e0b`) hover `#D8441F`; `--confirm #1F8A50`, `--danger #C4384A`, `--gps #2F6FB0`. Status-värit (§C, näkyvät myös kuvamerkin kortin reunuksena V87): suunniteltu `#8A968D`, asetettu `#2FA35B`, tarkistettu `#3B82C4`, kerätty `#8A5CD1`, ei_tarpeen `#C9922E` — eri sävyperheistä (B58). SEGMENT_COLORS/route-värit valkoiselle kartalle: `#2F6FB0 #7A4E9C #0E9594 #B5476B`. Kaamos määrittelee samat tokenit lämpimänä hiilenmustana. Kaikki värit CSS-tokeneista (`:root`/`[data-theme]`), EI hardcoded hex (§C-sääntö). **Kuvamerkin (V99 kuva-tier) rakenne EI muutu palettivaihdossa** — valkoinen kortti + `object-fit:contain` + kärki säilyvät, vain status-reunaväri tulee tokenista. |
| V133 | Järjestäjän yläpalkki näyttää käyttäjän identiteetin (`/api/auth/me` `display_name`) ja tarjoaa "Kirjaudu ulos" -toiminnon (`POST /api/auth/logout` → `AuthScreen`). Yläpalkki = yksi toimiva palkki (ei `⋯`-only), ei rooli-togglea (`#btn-role` poistettu, V80/B48). Sekundaaritoiminnot (Varmuuskopiot, Vie/Tuo kartta-aineisto) tilivalikon alla. |
| V134 | Järjestäjän näkymässä EI drive-mode-kontrolleja. `RouteBar` on jaettu: reittivalitsin (näytä/piilota reitti, järjestäjä) erillään drive-kontrolleista (`DriveMode`/`gps-drive-panel`/`route-track`, vain talkoolainen). Vahvistaa V13 (erilliset flow't) ja V6 (vähintään yksi reitti aina näkyvissä). |
| V135 | Kaikki napit rakentuvat jaetusta `.btn` + variantti-järjestelmästä (`--primary`/`--secondary`/`--confirm`/`--ghost`/`--danger` + koot). Ei uusia kertakäyttö-nappiluokkia — uusi nappi = olemassa oleva variantti. Modal-footer-pattern (primary/secondary/destructive) ja touch-target 44px (§R) säilyvät. |
| V136 | Kaikki karttamerkit ovat **neliö/suorakaide-kortteja** yhtenäisellä tyylillä ("kyltti kepissä") — EI teardrop/pyöreä pisara. Ikoni/label-merkki = tyyppivärinen kortti + valkoinen glyfi/label keskitettynä; kuvamerkki = valkoinen kortti `object-fit:contain` (koko kyltti croppaamatta); yhdistelmä = kortit pystypinossa (V107). Kaikilla: status = kortin reuna (V87), kärki-kolmio alareunassa = ankkuri tarkkaan sijaintiin (`iconAnchor` kärjen kärkeen). Sama muoto renderöityy kartalla, sivupalkissa, modaalissa ja listoissa (`icons.ts` + `buildMarkerVisual`). Korvaa V87:n teardrop-oletuksen muodon osalta; täyttö=tyyppi / reuna=status -periaate säilyy. Suuntamerkki = selkeä nuoli-glyfi kortin sisällä (ei bearing-rotaatiota, T129). |
| V137 | Kartan alanurkan reittivalitsin renderöityy **idempotentisti** — `RouteVisibilityControl.buildPills()` tyhjentää `#route-selector`-containerin (`replaceChildren()`) ENNEN pillereiden lisäystä, ettei init/onAuthenticated-tuplakutsu tuota tuplapillereitä. Yleisperiaate: init:ssä rakennetut DOM-listat (route-pillerit, route-tabit) rakennetaan aina tyhjästä, ei append-only olemassa olevan päälle. |
| V138 | `markerScaleForZoom(zoom)` (`src/logic/marker-scale.ts`) on **ei-vähenevä koko kartan zoom-välillä** ja saavuttaa maksiminsa kartan todellisessa `maxZoom`issa (tile-layers.ts = **19**), EI kovakoodatussa 17:ssä. Merkki ei saa lakata kasvamasta ennen loppuzoomia — muuten se näyttää kutistuvan suhteessa karttaan viimeisillä zoom-tasoilla. `MARKER_SCALE_ZOOM_MAX` seuraa kartan maxZoomia (tai ≥19); loppuzoomissa merkki on suurimmillaan (scale saa olla ≥1). Pure-funktio → Vitest: `markerScaleForZoom(z+Δ) >= markerScaleForZoom(z)` kaikilla z∈[11,19]. |
| V139 | Segment on yleistetty **tehtävä-olio**: `routeIds`/`startDist`/`endDist` ovat VALINNAISIA. Reitillinen tehtävä (kaikki kolme annettu) = lineaarinen reittipätkä; reititön tehtävä (kentät puuttuvat) = aluetehtävä (maalialue, keräysalue). V11 (startDist<endDist) + V25 (routeIds non-empty) validoidaan `createSegment`/`updateSegment`:ssä VAIN kun reitilliset kentät annettu — reititön tehtävä ohittaa ne laillisesti. `validateNoOverlap` skippaa reitittömät (overlap merkitsee vain reitillisille); `cloneSegmentToNextPhase` undefined-safe (ei kopioi olematonta reittiä eikä laske overlappia). Olemassa olevat reitilliset pätkät (kaikilla kentät) → validointi ennallaan. `src/logic/segments.ts`. (T212) |
| V140 | Tehtävän merkkijoukko resolvoidaan yhdellä kanonisella pure-funktiolla `resolveTaskMarkers(source: TaskMarkerSource, markers): SignMarker[]` (`src/logic/task-markers.ts`) = reittifiltteri (jos reitillinen: `routeIds`-leikkaus ∩ `distanceFromStart∈[startDist,endDist]`, V25) ∪ eksplisiittiset `linkedMarkerIds` ∪ dynaamiset `markerTypeFilter`-osumat. `TaskMarkerSource` on strukturaalinen — sekä `Segment` että `AreaMarker` toteuttavat sen, ei olio-spesifiä koodia. `getMarkersForSegment` delegoi tähän (ei duplikoitua route+dist-filtteriä); `getPhaseProgress`/`getSegmentStatusCounts` kutsuvat samaa. Merkin id uniikki → unioni deduplikoi implisiittisesti. (T214) |
| V141 | Segments-taulun `route_ids`/`start_dist`/`end_dist` ovat NULLABLE (reititön tehtävä persistoituu, V139). Migraatio idempotentti — EI DROP/recreate ilman datan kopiointia transaktiossa (⚠️ B84-oppi: skeemaristiriita vanhaan prod-tauluun = hiljainen INSERT-500 + datakato). Serialisointi (`rowToSegment` + POST/PUT, `server/routes/segments.ts`) käsittelee null/undefined molempiin suuntiin; `GET /api/segments/by-code/:code` palauttaa reitittömän tehtävän ilman route-kenttiä. (T213) |
| V142 | Talkoolaisen tehtävänäkymässä oma tehtävä (assignattu pätkä/alue) renderöityy kirkkaana ja klikattavana; muut tehtävät näkyvät himmeinä ja read-onlyna taustalla — konteksti näkyy mutta interaktio kohdistuu vain omaan (VISION §Talkoolainen 2026-07-08, **amendaa V33**). V33 (talkoolaiselle vain oman pätkän merkit modaalissa) pysyy oman tehtävän merkkilistan osalta; muutos koskee kartan taustanäkyvyyttä. (T218) |
| V143 | Dynaaminen `markerTypeFilter` (V140) nojaa vakaaseen merkki→template-viitteeseen: `SignMarker.templateId` denormalisoidaan (kuten `iconId` V106 / `imageId` V131) ja persistoidaan (`markers.template_id`, idempotentti ALTER). Suodatin täsmää `templateId`:llä, EI muuttuvalla `label`illa. Skenaario 2 (keräys/autoporukka): kuka tahansa autentikoitu voi lisätä `keräyskasa`-tyyppisen merkin ja kuitata sen "haetuksi"; autoporukan reititön tehtävä = `markerTypeFilter:<keräyskasa-templateId>`, elävä lista. (T215) |
| V144 | Talkoolaisen merkkilista (marker-modal + segment-view) näyttää VAIN oman pätkän merkit myös re-renderissä. Kaikki `renderMarkerList`-kutsupolut (MarkerManager `onUpdate`, outbox-change, modal-refresh) välittävät `segmentMarkerIds`:n (talkoolaisella `getMarkersForSegment`, järjestäjällä `undefined`=kaikki). Suodatus EI saa kadota ensimmäisen status-muutoksen jälkeen. (B94) |
| V145 | Talkoolaisen "Aseta"-ohjaus valitsee pätkän ENSIMMÄISEN asettamattoman merkin: pienin `distanceFromStart` jolla `status==='suunniteltu'` (`firstUnsetMarker`), rajattuna oman pätkän merkkeihin (`getMarkersForSegment`). EI lähin kursoriin/GPS-sijaintiin (nearestUnsetMarker) eikä koko reitin merkkeihin. GPS-drive-panelin navigointi rajataan myös pätkän merkkeihin. (B95) |
| V146 | Purku bulk-kuittaus (`bulkCollect`) palauttaa merkit valmiiksi `kerätty`-tilassa → kutsuja soveltaa `bulkSetStatus(ids, 'kerätty')`, EI `updateStatus(id, 'kerää')`-actionia. `kerää`-action heittää `suunniteltu`/`ei_tarpeen`-merkeille (ei siirtymää) → uncaught throw + osittainen päivitys. (B96) |
| V147 | Write-outbox erottaa pysyvät ja ohimenevät virheet: pysyvä 4xx (400/403/404/409/422) → entry POISTETAAN jonosta (dead-letter) + kertaluontoinen selkeä virheviesti; retry EI koskaan korjaisi → muuten jono myrkyttyy ja banneri toistuu joka kierroksella. 401 (uudelleenauth), 408/425/429 (rate-limit/timeout), 5xx ja verkkovirhe (null) EIVÄT ole pysyviä → jäävät jonoon retryä varten. (B97) |
| V148 | Talkoolaisen seuraava-merkki-ohjaus (Aseta/Näytä kartalla/Ei tarpeen+⋯) renderöityy TASAN YHDESSÄ paikassa: `SegmentView`-hero. ⊥ duplikoituna. `#gps-drive-panel` (komponentti `gps-drive-panel.ts`) POISTETTU. Talkoolaisen alapalkki `#route-bar` PIILOTETTU kokonaan (`hidden`) — ei reittivalitsinta, ei km-scrubberia, ei ◀▶ (ne ohjasivat koko reittiä ei pätkää). Navigointi = hero (T232: hero sis. GPS-toggle + ◀▶ asettamattomien välillä; GPS EI enää yläpalkissa) + kartta (seuraava merkki korostettu `NextMarkerHighlight`) + yläpalkin "Kaikki merkit"/"Varustelista" (toolbar-koostumus V155). Järjestäjän RouteVisibilityControl ennallaan (V134). (T224, amend T232) |
| V149 | Talkoolaisen `POST /api/markers` ownership-tarkistus on JOHDONMUKAISUUSVAHTI (estää vahinkokirjauksen väärään pätkään), EI turvaraja: server ei säilytä reittigeometriaa (ei routes-taulua, `db.ts`) → `distance_from_start`+`route_ids` luetaan clientin payloadista, joka on väärennettävissä. Väärinkäyttö (autentikoitu talkoolainen) EI esty tässä — havaitaan JÄLKIKÄTEEN audit-lokista (T226) + massaperuutuksesta (T227), joka on todellinen kontrolli. Tarkistus: `route_ids ∩ seg.route_ids ≠ ∅` JA `distance_from_start ∈ [start_dist−ε, end_dist+ε]` (ε-toleranssi GPS-driftille — pätkän reunan laillinen sijoitus EI saa 403:ta → V147 dead-letter → V21 hiljainen katoaminen). Reititön seg (V139): ei geo-rangea → salli jos assignattu seg olemassa (käytännössä rajoittamaton sijoitus; audit+undo kontrolli). Vieras/pätkätön → 403 selkeällä viestillä. EI tyyppirajausta (jaettu kirjasto). (T219, harden ck:review 2026-07-09) |
| V150 | Talkoolaisen `PUT /api/markers/:id`: (a) auktorisointi = OLEMASSA OLEVA merkkirivi kuuluu hänen pätkäänsä (`segments.ts:125`-pattern), EI payloadin uusi sijainti; (b) siirto sallii uuden sijainnin vain jos uusi `distance_from_start ∈` oman pätkän range (ε, V149) — ei saa raahata merkkiä ulos omasta pätkästä. KOSKEE MYÖS `status`+`location_note`-kenttiä: nyk. `markers.ts:135` positionFields-gate EI kata niitä → talkoolainen muuttaa nyt status/location_note MISSÄ TAHANSA merkissä ilman ownershipiä (pre-existing aukko) → T222/T225 sulkevat: talkoolaisen jokainen PUT vaatii oman pätkän merkin. Vieras → 403. Frontend: vain oman pätkän merkit `draggable`, muut read-only. Vahvistaa V142. (T222, harden ck:review 2026-07-09) |
| V151 | Talkoolainen EI kovapoista suunniteltua (järjestäjän luomaa) merkkiä — soft `ei_tarpeen`+syy (T10-status). Kova `DELETE /api/markers/:id` VAIN oman itse-luoman merkin (`created_by`=oma) omalla pätkällä. `created_by` tallentaa luojan tunnisteen (talkoolainen_code ensisijainen; pelkkä display_name törmää nimikaimoilla). Olemassa olevat merkit `created_by=NULL` → ei talkoolais-kovapoistettavissa (oikein: suunniteltuja). (T225, harden ck:review 2026-07-09) |
| V152 | Jokainen merkkimutaatio (POST/PUT/DELETE `markers.ts`) kirjaa `marker_audit`-rivin SAMASSA `db.transaction()`-lohkossa mutaation kanssa (`db.ts:225` osoittaa APIn) — kirjaus+mutaatio committautuvat atomisesti tai ei kumpikaan. `payload_json` tallentaa PERUUTETTAVILLE actioneille ENNEN-tilan: `move`→vanha lat/lon/distance/route_ids, `status`→vanha status, `add`→(peruutus=DELETE, ei ennen-tilaa). Ilman ennen-tilaa V153 ei voi palauttaa siirtoa. Hiljainen kirjaamatta-jättö kielletty (V120-linja). (T226, harden ck:review 2026-07-09) |
| V153 | Massaperuutus (`POST /api/audit/undo`) on atominen `db.transaction()` (kaikki/ei mitään, V100/V24-linja): `add`→DELETE merkki, `move`/`status`→restore V152-payloadin ENNEN-tilasta. Tuhoava → `window.confirm` ennen. `requireRole('admin','järjestäjä')`. Undo peruuttaa VAIN kirjatun mutaation — jos merkkiä muokattu undo-ikkunan jälkeen (interleaved), restore voi olla vanhentunut. (T227, harden ck:review 2026-07-09) |
| V154 | Talkoolaisen ownership-tarkistus jakaa SAMAN kanonisen unionin backendissä (`markerInOwnSegment`, `server/marker-audit.ts`) ja frontendissä (`resolveTaskMarkers`, `task-markers.ts`): reittifiltteri (route∩ ∧ dist∈[start−ε,end+ε]) ∪ `linked_marker_ids` ∪ `marker_type_filter`. Yksi kerros EI saa tuntea merkkiä pätkän jäseneksi jos toinen ei — muuten frontend näyttää merkin (esim. link/typeFilter-osuma) mutta backend torjuu mutaation 403:lla (B100). ε=50m (`RANGE_EPS_M`) GPS-driftille. (T222/T226) |
| V155 | Talkoolaisen `#toolbar-actions` = TASAN {Kaikki merkit `#btn-list`, Varustelista (avaa `EquipmentModal`), ⋯ `#btn-menu`}. GPS (`#btn-gps`) + Merkin-lisäys (`#btn-add-marker`) EIVÄT ole yläpalkissa — asuvat `SegmentView`-herossa (T232). Karttatyyli (`#btn-layer`) siirretty ⋯-valikkoon (`#toolbar-menu`) MOLEMMILLA rooleilla (jaettu nappi, ei `data-role-hide` — myös järjestäjän toolbarista poistuu näkyvä layer-nappi). Talkoolais-toolbar ⊥ rivinylitä/hajoa ≤360px (mobiili). Järjestäjän toolbar muuten ennallaan (V133/V134): reittivalitsin + phase-switcher-valikko säilyy; ainoa muutos = layer-nappi valikkoon. Amendaa V148:n toolbar-kuvausta. (T233) |
| V156 | Talkoolaisen `#segment-view` AINA näkyvissä (ei-kutistettuna) vain: otsikko (pätkän pituus km) + progress + seuraava-merkki-hero {GPS-toggle, ◀▶ (selaa VAIN asettamattomia, T231), ✓ Aseta, Näytä kartalla, ⋯}. Varustelista (→ yläpalkki V155), "Merkitse pätkä valmiiksi" (`completeSection`) + "Muokkaa pätkän rajoja" (`boundsSection`) (→ panel-tason sekundäärivalikko V158) EIVÄT ole hero-primaryssä aina-näkyviä. Vahvistaa V148 (hero = ainoa ohjaus). **AMEND T262/R3b:** seuraava-merkki-hero on VAIN kartta-moodin ohjaus (V177/V182) — KOTI-moodi EI näytä heroa (`.segment-view-next` `display:none`), koti näyttää varustelistan+varustarkastuksen inline (V182). "hero = koti-primary aina-näkyvissä" kumottu koti-moodille; pätee vain `data-view-mode=kartta`. (T232, T262) |
| V157 | Talkoolaiselle EI orpoa alapalkki-tilaa: koska `#route-bar` on `hidden` (V148), talkoolaisen `#app`/`#app-main` EI varaa `padding-bottom`-tilaa route-barille → ei kuollutta alagappia kartan alla (VISION r131 kartta=päänavigointi). (T233, B101) |
| V158 | Talkoolaisen "Merkitse pätkä valmiiksi" (`onComplete`, näkyy `phase` asettaminen+purku) ja "Muokkaa pätkän rajoja" (`onEditBounds`, aina) tavoitettavissa PANEL-TASON sekundäärivalikosta ("Lisää ⋯"), EI marker-hero-overflowsta. Peruste (ck:review 2026-07-09): hero `renderNext` (segment-view.ts:180) renderöityy VAIN `phase==='asettaminen'` JA kun asettamaton merkki jäljellä (`!next` r189 → done-slim, ei overflowia) → hero-overflow olisi tavoittamaton purku-phasessa JA done-tilassa. Sekundäärivalikko näkyy phase-riippumatta. Varustelista EI tässä — se on yläpalkissa (V155). **AMEND T260/R4:** "Lisää ⋯" on nyt AINA näkyvissä (ei enää piiloudu ilman complete/bounds), koska se pitää sisällään myös pätkän kommentit (siirretty koti-etusivulta valikkoon). Complete/bounds-osiot gaten silti itsenäisesti phase/rooli-mukaan. (T232, T260) |
| V159 | Talkoolaisen hero näyttää yhtä "valittua" merkkiä (oletus `firstUnsetMarker`; ◀▶ `stepUnset` T231 selaa asettamattomia). ✓ Aseta, Näytä kartalla, ⋯-overflow JA `NextMarkerHighlight` kohdistuvat SAMAAN valittuun id:hen. `update()`-re-render (markers-wiring.ts:130) reconciloi: jos valittu id ei enää ole asettamattomien listalla (asetettu/poistettu) → palaa `firstUnsetMarker`iin — hero EI KOSKAAN osoita asetettuun/poistettuun merkkiin. `updateNextHighlight` (r90-95) seuraa valittua id:tä, EI suoraan `firstUnsetMarker`ia → SegmentView antaa navigointi-callbackin (`onNavigate?(id)`) markers-wiringille. Estää B95-tyyppisen "Aseta osui randomiin" -regression selailun kanssa. (T232) |
| V160 | src/-koodissa ⊥ guardaamatonta crypto.randomUUID() — kaikki id-generointi src/logic/uid.ts-turva-apurin kautta (guard `c?.randomUUID` + kryptaton fallback), toimii insecure contextissa (http/ei-localhost). write-outbox.ts:54-56 oli jo oikein; uid.ts yleistää. (T238, B103) |
| V161 | inventory_items.name pakollinen KUN template_id puuttuu (tarvike). Merkkirivi (template_id asetettu) → nimi elävästi templates-taulusta, name saa olla null. Tyhjä/whitespace name ilman template_id → POST/PUT `/api/inventory` 400. Sama sääntö src/logic/inventory.ts `validateInventoryItem` (client-pre) + backend (auktoritatiivinen). (T240, T241, T243, T244) |
| V162 | inventory_items.qty = äärellinen numero >= 0 — `validateInventoryItem` (src/logic/inventory.ts) + backend hylkää: ei-numero, NaN, Infinity, negatiivinen (`typeof x==='number' && Number.isFinite(x) && x>=0`, EI pelkkä `x>=0` joka päästää coercion "5">=0 läpi) → POST/PUT `/api/inventory` 400. Numero (ei vapaateksti) jotta v2-dekrementointi ilman migraatiota. (T240, T241) |
| V163 | `/api/inventory` ∀ route (GET/POST/PUT/DELETE) vaatii `requireRole('admin','järjestäjä')` — talkoolais-sessio → 403. Talkoolainen ⊥ näe /inventaario-sivua (LeftPanel-linkki piilotettu `body[data-role=talkoolainen]`-CSS). Inventaario = tarkoituksella avoin järjestäjien kesken, EI per-user-lukitusta. (T240) |
| V164 | src/ui/inventory-page.ts renderöi käyttäjän syöttämän tekstin (name/unit/location/note) VAIN `textContent`/`createElement`-kautta — ⊥ `innerHTML`-interpolaatiota user-datalla. Stored-XSS-esto. Malli: admin-page.ts:47-50 (textContent); precedent B19/V44 (template-XSS-escape). (T242) |
| V165 | Inventaario-merkkirivi (template_id asetettu) resolvoi elävän template.label + ikonin templates-taulusta (T193/template-sync); template puuttuu → fallback `item.name ?? '(poistettu merkki)'`. Inventaariorivin poisto ⊥ poista SignTemplatea (unlink, EI kaskadi). `resolveItemName` src/logic/inventory.ts. (T243, T244, T246) |
| V166 | inventory_locations poisto → sen itemien `location_id = NULL` (itemeja EI kadoteta; näkyvät "Ei paikkaa"-ryhmässä); ⊥ kaskadipoistoa itemeille. (T243) |
| V167 | Inventaario-merkkirivi (templateId) näyttää template-visuaalin (ikoni/kuva/combo) nimen vieressä — uusiokäyttää `buildMarkerVisual` (T198, src/ui/marker-visual-row.ts), EI duplikoi renderöintiä. `zoomable` → lightbox isolla kuvalla (kyltin avaus). Template puuttuu → ei visuaalia, fallback-nimi (V165). Tarvike-rivi (ei templateId) → ei visuaalia. (T247) |
| V168 | ⚠️ **SUPERSEDED V177** (2026-07-21: keppi siirretty templatesta inventaarioriville — sama kylttipinta yhdellä tunnuksella voi olla kepillä TAI irto). Historiallinen: Merkin kiinnitystapa = **SignTemplate-kenttä `keppi`** (boolean; DB `templates.keppi INTEGER NOT NULL DEFAULT 1`), EI inventaariorivin kenttä. `keppi=true` (yleisin) → label näytetään sellaisenaan; `keppi=false` → näyttönimeen liitetään `' - irto'` **KAIKKIALLA** missä template-label renderöityy käyttäjälle (inventaario-rivi, merkkikirjasto, picker, karttamerkin label/tooltip). Yksi totuus: `signDisplayLabel(tpl)` (src/logic/sign-library.ts, puhdas, ei DOM) — EI compactLabel/kartan lyhennettä (se johdetaan raakalabelista ennallaan). DEFAULT: `keppi=true` AINA (yleisin — kartta, kirjasto, inventaarion "Muuta merkiksi" -convert); irto = käsin poistettu täppä. Inventaario-convert lisää vain `favorite=false` (ei suosikkipickeriin), keppi jää default trueksi (käyttäjän tarkennus 2026-07-21: "keppi aina default paikoillaan"). Migraatio: `ALTER TABLE templates ADD COLUMN keppi INTEGER NOT NULL DEFAULT 1` → kaikki olemassa olevat merkit saavat keppi=true. keppi tallentuu templates-tauluun (favorite-kentän malli). (T249, T250) |
| V169 | Inventaario-sivu avautuu **read-modessa** (`viewMode='read'`, oletus); KAIKKI mutaatiotoiminnot piilossa: add-form (uusi rivi), per-item −/+ stepperi, ✎ Tiedot, Poista, paikkojen ✎ Muokkaa, "lisää paikka". Näkyvät vasta kun `viewMode='edit'`. Read-modessa säilyy katselu: paikkatabit, Kaikki-tabi, zoom-lightbox, kyltin nimi→SignTemplateModal (tietoinen editori-avaus, EI rivi-mutaatio). (T251) |
| V170 | `viewMode` **sessiokohtainen** (state muistissa, EI localStorage); reload → aina `'read'` (turvallinen oletus). Tabin vaihto sivun sisällä säilyttää moodin. (T251) |
| V171 | "Kaikki"-koontitabi `viewMode='edit'` → per-rivi-muokkaukset sallittu (−/+, Poista, ✎ Tiedot, siirto) MUTTA add-form EI (uuden lisäys vaatii paikkakontekstin → vain paikkatabeissa). (T251) |
| V172 | Inventaarion "Kumoa"-toast on CLIENT-ONLY: muistaa VAIN viimeisimmän mutaation (yksi undo-slotti, ei historiaa). Uusi mutaatio korvaa edellisen undo-tilan. Reload/navigaatio → undo-tila katoaa (ei persistointia, ei backend-audit-taulua — pysyvä `inventory_audit`-versio jää eksplisiittisesti PARKKIIN). Toast auto-dismiss ~5s; yksi toast kerrallaan (uusi korvaa vanhan + resetoi timerin). Toast näkyy vain `viewMode='edit'` (mutaatiot tapahtuvat vain edit-moodissa, T251/V169). Peilaa merkkien T226/T227 undo-henkeä mutta KEVENNETTY (ei V152/V153-backend-tauluja). (T253) |
| V173 | Undo palauttaa ENNEN-tilan olemassa olevien `/api/inventory`-reittien kautta, EI uutta undo-endpointia: (a) item-poisto → POST uudelleenluonti ENNEN-tilan kentistä (uusi id — alkuperäinen id menetetään, rivi funktionaalisesti sama); (b) qty-muutos (−/+/tarkka) ja siirto → PUT vanha arvo takaisin; (c) paikan poisto → POST paikka uudelleen (uusi id) + PUT jokainen affektoitu item uuteen location_id:hen. Undo-payload tallentaa affektoidut item_id:t + vanha location_id ENNEN DELETEä, koska V166 nullaa location_id:t peruuttamattomasti. Interleaved-varaus (V153-linja): jos taustatila muuttunut undo-ikkunassa, undo voi olla vanhentunut. (T253) |
| V174 | Talkoolaisen näkymällä on kaksi moodia: `koti` (pätkänäkymä, EI karttaa) ja `kartta` (kartta näkyvä). Oletusmoodi kun `/s/<koodi>` avataan = `koti` (kartta piilossa kunnes "Kartalle →"). Moodi koskee VAIN talkoolaista (`data-role=talkoolainen`); järjestäjän layout ennallaan (V155). (T254) — amend 2026-07-25: "oletus koti" koskee TUORETTA avausta; saman session refresh palauttaa viimeisen moodin (V225/T313). |
| V175 | Moodi = `#app` `data-view-mode="koti"\|"kartta"`, CSS ohjaa näkyvyyttä. `koti` → `#segment-view-container` täysi + `#map` piilossa; `kartta` → `#map` näkyvä (≥~80% pystytilaa) + segment-chrome minimaali. "🏠 koti" -paluunappi näkyy vain `data-role=talkoolainen` + `data-view-mode=kartta`. (T254) |
| V176 | Moodivaihto (koti↔kartta) on pelkkä näkyvyyskytkin: EI nollaa segment-/marker-tilaa, EI tee verkkokutsuja, EI muuta karttainstanssia. Karttamoodiin siirryttäessä Leaflet `invalidateSize()` (näkyvyyden palautus) sallittu. (T254) |
| V181 | ⚠️ **SUPERSEDED V186** (2026-07-21: keppi poistettu KOKONAAN — kiinnitystapa ei ole kenttä, oletus keppi, poikkeus merkin kommenttiin). Historiallinen: Merkin kiinnitystapa = **inventaariorivin kenttä `keppi`** (EI SignTemplate-kenttä, supersedes V168). DB `inventory_items.keppi INTEGER` (nullable): `0`=irto, `1`/`NULL`=keppi. SignTemplate = pelkkä kylttipinta (yksi tunnus) — sama malli voi olla inventaariossa kepillä JA irtona eri riveillä. `' - irto'` -suffix näkyy VAIN inventaarion rivinäytössä (`resolveItemName` → `signDisplayLabel(tpl.label, item.keppi)`), EI kirjastossa/pickerissä/kartalla (ne = pelkkä `tpl.label`). `signDisplayLabel(label, keppi)` puhdas: `keppi===false ? label+' - irto' : label`. keppi merkityksellinen vain merkkirivillä (template_id); tarvike aina NULL. server/routes/inventory.ts validate: `keppi` false→0/true→1/muu→NULL, vain jos template_id. templates.ts EI enää lue/kirjoita keppiä (DB-sarake `templates.keppi` jää orvoksi, SQLite-DROP vältetään). Migraatio: `ALTER TABLE inventory_items ADD COLUMN keppi INTEGER` + kertaluontoinen backfill `UPDATE inventory_items SET keppi=0 WHERE template_id IN (SELECT id FROM templates WHERE keppi=0)` (näyttönimi säilyy). VANHAT duplikaattimallit (esim 'oikealle' + 'oikealle-irto') EIVÄT automaattisesti yhdisty — käsin siivottavissa. (T261, renum V177→V181: main-redesign otti V177) |
| V180 | Talkoolaisen varustarkastus ("otin nämä" checkoff) on CLIENT-ONLY: localStorage per pätkä (`karttamaster-varustarkastus-<segId>`), henkilökohtainen valmisteluapu — EI backend, EI jaettu järjestäjälle/muille. Peilaa inventaario-undon client-only-linjaa (V172). Checkoff-avaimet: auto-merkki `sign:<type>`, manuaali `item:<nimi>`. Reload säilyttää (localStorage); rikkinäinen JSON → tyhjä (turvallinen oletus). Edistymä EquipmentModalin headerissa "Varustarkastus: N/M otettu". (T258) |
| V182 | Talkoolaisen KOTI-moodi (`data-view-mode=koti`) näyttää varustelistan+varustarkastuksen INLINE (`.segment-view-equipment`, `src/ui/segment-equipment.ts`) JA piilottaa seuraava-merkki-heron (`.segment-view-next` `display:none` koti-moodissa); KARTTA-moodi päinvastoin (V177: hero näkyy alapalkkina, `.segment-view-equipment` piilossa). Koti-varustarkastus jakaa SAMAN client-only-tilan EquipmentModalin kanssa: sama label-avainmuoto (`sign:<type>`/`item:<nimi>`) + `varustarkastus.ts` (V180) → checkoff-tila täsmää inline↔modaali. Inline = readonly auto-laskuri merkkityypeittäin + checkoff (auto-persistoi, EI Tallenna-nappia); manuaalilistan editointi "✎ Muokkaa varusteita" → olemassa oleva `EquipmentModal` (Tallenna V38/V93). `SegmentHero.render()`-side-effectit (onNavigate → kartan korostus) jäävät ajoon koti-moodissa (kartta piilossa → ei visuaalivaikutusta) — pelkkä CSS-piilo, EI `segment-hero.ts`-logiikkamuutosta. (T262) |
| V183 | Talkoolaisen KOTI-moodi näyttää oman pätkän "Kaikki merkit" -listan INLINE (`.segment-view-markers`, `src/ui/segment-marker-list.ts`) — brief Näkymä 1; koti = ei karttaa → tilaa listalle (T228:n tilankäyttö-peruste ei päde koti-moodissa). Lista = oman pätkän merkit (`currentMarkers`, V33/V142-suodatus jo tehty) distanceFromStart-järjestyksessä, rivi = `buildMarkerVisual` + nimi (`markerLabel`) + status + km; rivi-klikkaus → `onFocusMarker` (= `onOpenMarkerDetail` → MarkerDetailModal, jaettu yläpalkin modaalin kanssa, EI uusi mutaatiopolku). KARTTA-moodissa CSS piilottaa (`#app[data-view-mode=kartta] .segment-view-markers{display:none}`) — kartalla merkit näkyvät kartalla + yläpalkin modaali/tap (T228-linja säilyy kartta-moodille). Elävä: `SegmentView.update()` re-renderöi (status-muutos). (T263) |
| V184 | Talkoolaisen KOTI-moodi = 3-tab-shell (`SegmentKotiTabs`, `.segment-koti-tabs`): **Varustelista** (inline-varuste V182) · **Kaikki merkit** (ryhmitelty lista V183 + "Merkitse pätkä valmiiksi" `completeSection` + "Muokkaa pätkän rajoja" `boundsSection`) · **Kommentit** (koko-pätkän CommentThread). "Lisää ⋯" -accordion (`.segment-view-more`) POISTETTU — valmis+rajat+kommentit EIVÄT haitarin alla vaan tabeissa (käyttäjäpalaute 2026-07-21: "ilman että ne on jossain haitarin alla"). Tabit VAIN koti-moodissa (kartta-moodi: hero näkyy, `.segment-koti-tabs` piilossa CSS:llä, V177). Talkoolaisen yläpalkissa EI Varustelista- (`#btn-varuste` poistettu) EIKÄ Kaikki merkit -nappia (`#btn-list` piilotettu talkoolaiselta) — molemmat ovat koti-tabeja; järjestäjän `#btn-list` ennallaan (V134). "Kaikki merkit" -tab ryhmittää: Asettamatta (suunniteltu) / Asetetut (asetettu·tarkistettu·kerätty) / Ei tarpeen. (T264, supersedes V158-accordion) |
| V185 | Talkoolaisen pätkälataus (`/s/<koodi>`) fittaa AINA KOKO pätkän [startDist,endDist] karttaan — `planSegmentZoom` palauttaa aina `mode:'fit'` koko välille (EI anchor-clamppia pitkille pätkille). Käyttäjäpäätös 2026-07-21 (B107): pätkän oikea pituus > merkkien koko. `fitMapToSegment` slice = koko pätkän reittipisteet → fitBounds koko pätkään (maxZoom 16 säilyy). maxFitLengthM/anchor-haara poistettu. Supersedes T224/D-anchor. (T265, B107) |
| V186 | Merkin kiinnitystapa EI ole strukturoitu kenttä (supersedes V181). Oletus AINA keppi; poikkeus ("sido puuhun") ilmaistaan merkin kommentissa/paikkaohjeessa kartalla (`marker-detail-modal` note, placeholder "kiinnitä puuhun" — mekanismi jo olemassa). `signDisplayLabel(label)` palauttaa AINA `label` (EI keppi-parametria, EI `' - irto'`-suffixia). `resolveItemName` = `signDisplayLabel(tpl.label)` ilman rivin keppiä. `SignTemplate` JA `InventoryItem`/`Fields` ilman keppi-kenttää. `server/routes/inventory.ts` EI validoi/lue/kirjoita keppiä (INSERT/UPDATE ilman keppi-saraketta). DB-sarake `inventory_items.keppi` jää ORVOKSI (EI DROP, SQLite-turva — kuten `templates.keppi` V181:ssä). Perustelu (käyttäjä 2026-07-21): keppi-boolean oli vain järjestäjä-kosmeettinen `' - irto'`-suffix inventaariorivissä, näkymätön talkoolaiselle, EI ohjaa materiaalilaskuria (erillinen manuaali-"keppiä"-tarvikerivi). VISION §Merkkijärjestelmä: kiinnitystapa = paikkaohje, ei kenttä. Tukee tulevaa "kartat ja merkit kohtaamaan" -convergenssia (poistaa inventaario-only attribuutin ilman kartta-vastinetta). (T266) |
| V187 | Mobiili-scroll-hygienia (§R). (a) Jokainen sisäinen scroll-alue (modaalit, valikot, paneelit, listat) määrittää `overscroll-behavior:contain` → reunaan asti scrollattu liike EI valu taustan karttaan tai toiseen paneeliin (ei scroll chainingia fixed-elementtien takana). (b) Karttasivun kuori (`html:has(#app),body:has(#app)`) on `overflow:hidden` → sivu ei koskaan scrollaa itse (ei pull-to-refresh, ei 100dvh-address-bar-hyppyä). EI koske `#inventory-app`/`#admin-app`-normaali-flow-sivuihin (ne scrollaavat vapaasti — `overflow:hidden` VAIN `:has(#app)`-kuorelle, muuten inventaario-lista leikkautuisi). (c) Kaikki modaali-backdropit `touch-action:none;overscroll-behavior:none` → drag backdropin päällä ei panoroi alla olevaa karttaa. (d) `#toolbar-menu` `max-height:calc(100dvh-68px);overflow-y:auto` → valikko ei koskaan kasva yli ruudun (alimmat kohteet aina tavoitettavissa). (e) `.bulk-status-toolbar` `flex-wrap:wrap` → rivittyy 320px-modaalissa, ei vaakaleikkausta. (T266→B108) |
| V179 | Talkoolaisen yläpalkin ⋯-valikko (`#toolbar-menu` → `#tk-menu-actions`, `data-role-hide=järjestäjä`) sisältää pätkän core-toiminnot: GPS-toggle (`#btn-tk-gps`, label ↔ "📍 GPS"/"📍 GPS päällä"), Lisää merkki (`#btn-tk-add-marker`), Merkitse pätkä valmiiksi (`#btn-tk-complete`, label ↔ valmis/keskeneräinen). GPS-nappi POISTETTU talkoolaisen SegmentView-herosta (CSS-piilo) — GPS asuu VAIN ⋯-valikossa (VISION "GPS ym ylävalikkoon"). Toiminnot jakavat saman tallennuspolun kuin hero (gpsNavigator/placeMode/applyComplete). (T257) |
| V178 | Pätkän seuraava asettamaton merkki korostuu kartalla korostamalla ITSE IKONIA huomiovärillä (`--accent` glow, `.marker-next-highlight`), EI erillistä accent-rengasta (`NextMarkerHighlight` poistettu T256). Korostus = `MarkerManager.setNextHighlight(id|null)`, seuraa hero:n valintaa (V159); uudelleensovelletaan elementin uudelleenpiirrossa (setIcon/reload). `filter:drop-shadow` ei kosketa Leafletin position-transformia. (T256) |
| V177 | Karttamoodissa (`data-view-mode=kartta`) `#map` täyttää `#map-area`:n (kartta ≥~80% ruudusta, VISION "yksinkertainen kartta"); `#segment-view` kelluu alareunassa alapalkkina (`position:absolute;bottom`) ja näyttää VAIN seuraava-merkki-heron (+ keräyslista/done). Ei-hero-osat (header/progress/gps/varuste/lisää/kommentit/inspect) piilossa karttamoodissa. Koti-moodi näyttää koko pätkänäkymän ennallaan. (T255) |
| V188 | Model B -portti: talkoolainen tunnistautuu yhdellä admin-asettamalla yleissalasanalla. **PLAINTEXT `settings`-taulussa** (käyttäjäpäätös 2026-07-22: admin voi katsoa "mikäs se salasana oli" `GET /api/admin/settings`; jaettu vapaaehtoissalasana, ei henkilökohtainen credentiaali, näkyy vain admin-sivulla). `POST /api/auth/talkoo-login {password}` → plaintext-vertailu → httpOnly-sessio role=talkoolainen **7pv** (V119-oppi: 24h vanhenisi kenttätyön day-2 kesken), EI sidottu yksittäiseen pätkään. Talkoolainen näkee kaikki pätkät hubissa: `GET /api/segments` = ∀ autentikoitu (T271; mutaatiot POST/PUT/DELETE pysyvät järjestäjä+). Rate-limit talkoo-loginiin (10 väärää → 429). Ilman voimassa olevaa sessiota (talkoolainen TAI järjestäjä) `/patkat` ja `/s/<slug>` ohjaavat yleissalasana-loginiin. Kevyt rate-limit talkoo-loginiin (viive/lukitus n väärän jälkeen). (T267) |
| V189 | Crossover ei luo sessiota: järjestäjän talkoo-näkymä on client-view-flip. **Mekaniikka (T274-toteutus): `/s/<koodi>`-deep-linkin koodin LÄSNÄOLO → `viewRole='talkoolainen'` riippumatta tilin roolista** (`role-view.ts` `wireAuth`: `const viewRole = code ? 'talkoolainen' : role`). Järjestäjä-sessio ja -oikeudet säilyvät (cookie), EI uutta talkoo-sessiota, ei logoutia. Ilman koodia (esim. tili-login juureen) → tilin oma rooli. Tilivalikossa "Pätkät-sivu"-linkki (`/patkat`) järjestäjälle. Talkoolaisen rooli-piilotukset (V148/V150 ym.) EIVÄT koske järjestäjää flipissä (täydet oikeudet). Paluu järjestäjä-näkymään sama flip. (T274) |
| V190 | FAQ-XSS: FAQ-markdown renderöidään sanitoituna (marked + sanitointi/escape) — admin-syöte ei saa injektoida skriptiä `/patkat`-hub-sivulle. Sama linja kuin V44. (T269,T271) |
| V191 | Slug-uniikkius: auto-generoitu pätkä-slug on uniikki (`talkoolainen_codes.code`-avaruus); törmäys → suffiksoi (`patka-1`, `patka-1-2`). Slug filename/URL-safe `[a-z0-9-]`, ääkköset normalisoidaan (ä→a, ö→o). `generateSegmentSlug(name, existing)` `src/logic/segments.ts`. (T273) |
| V192 | Linkin luonti/muutos pätkä-modaalissa EI sulje modalia — `SegmentDetailsModal` re-renderöi assign-osion näyttämään juuri luodun `/s/<slug>`-linkin + "Kopioi"-nappi samassa näkymässä. Sama "Muuta"-haaralle. Ei `this.close()` linkin luonnin jälkeen. (T276, B109) |
| V193 | (latentti hardening, EI B110:n syy — löytyi triage-vaiheessa) `renderMarkerList` segmentMarkerIds-filtteri saa aktivoitua VAIN ei-tyhjälle joukolle: tyhjä `Set` on truthy, joten `marker-list.ts:99 segmentMarkerIds ? filter : all` filtteröisi kaikki merkit pois. Järjestäjä (`talkoolainenCode=undefined` → `currentSegmentMarkerIds()=undefined`) näkee AINA kaikki managerin merkit — undefined ohittaa filtterin oikein, mutta jos joku kutsuu tyhjällä Setillä, guardin pitää olla `segmentMarkerIds?.size ? filter : all`. Ei tunnettua callsitea joka rikkoisi tämän nyt; invariantti estää regression. |
| V194 | Ei ulkoisia render-blocking CSS-riippuvuuksia — kaikki tyylit (ml. Leaflet) bundlataan paikallisesti Viteen. Ei CDN-`<link>`ejä `<head>`issä. Ensimmäinen paint pitää olla tyylitelty myös offline/hitaalla yhteydellä (talkoolainen metsässä). Leaflet CSS `@import 'leaflet/dist/leaflet.css'` `style.css`:n alussa (tai per-entry JS-import), EI unpkg. App-CSS-import JS-moduulista sallittu (Vite extractaa prodissa `<link>`:iksi), mutta ei ainoa tyylitelty-first-paint-tae — kriittinen ehto: ei ulkoista CSS:ää. (T278, B111) |
| V195 | Content-hashatut assetit (`/assets/*` — Viten hash-nimet) tarjoillaan `Cache-Control: public, max-age=31536000, immutable` (nginx). HTML-sivut (`index.html`, `/admin`, `/patkat`, `/inventaario`) ja `robots.txt` EIVÄT saa immutable-cachea (`no-cache`/`must-revalidate`) — muuten uusi deploy ei näy. Lighthouse `cache-insight` ≈354 KiB → 0 toistolataus. (T279) |
| V196 | SEO-perusta: jokaisella HTML-entryllä (`index.html`, `admin.html`, `patkat.html`, `inventory.html`) `<meta name="description">` (fi). Validi `/robots.txt` olemassa (ei fallback-HTML `try_files`-kautta → Lighthouse `robots-txt` 104 virhettä nollaan). (T280) |
| V197 | A11y-nimet: (a) dokumentilla `<main>`-landmark (index.html — muilla jo on). (b) `role="progressbar"` (`.status-panel-bar`) `aria-label`. (c) näkyvä-teksti-napin `aria-label` SISÄLTÄÄ näkyvän tekstin (WCAG 2.5.3, `label-content-name-mismatch`) — `sign-lib-place-btn` ym. (d) karttamerkeillä (Leaflet `role=button` divIcon) saavutettava nimi `alt`/`title`/`aria-label` (`aria-command-name`). (T281) |
| V198 | Ensimmäisen näytön CLS < 0.1 — route-bar ja sivupaneelit varaavat tilansa ennen dataa (ei layout-hyppyä latauksen jälkeen). Lighthouse `layout-shifts`-pääsyy `#route-bar` 0.17. (T282) |
| V199 | Main-sivun (`index.html`) initial-JS ⊥ sisällä moduuleja joita ei käytetä ensimaalauksessa, JOS ne voidaan dynaamisesti importata (`import()`) ilman UX-regressiota. `@milkdown/crepe` (admin-page.ts:235) ja feedback-widget (main.ts:136) JO lazy. **Poikkeus: Leaflet** = main-kartan ydinriippuvuus, saa olla staattinen. Verifioidaan bundle-mittauksella: Lighthouse `unused-javascript` ⊥ kasva regressiona. (T283) |
| V200 | Karttamarkkerien ikonit (`icons.ts`/`markers.ts` Leaflet divIcon / `L.icon` webp-signit) ⊥ saa `loading="lazy"` — ne näkyvät heti kartalla, lazy rikkoisi ensinäkymän. `loading="lazy"` VAIN offscreen-`<img>`:lle (merkkikirjaston galleria-thumbnailit `.sign-image-thumb`, ei-heti-näkyvät modaalikuvat). Testataan: markkeri-ikoni renderöityy ilman lazy-attr, galleria-thumb saa lazy-attr. (T284) |
| V201 | Pohjakartan näkyvyys-slider = `tilePanen` (`map.getPane('tilePane')`) opacity SUORAAN (slider% / 100). Slider 0–100 %, DEFAULT 100 (koko kartta). 0 % = tiilet opacity 0 → pohja häviää, `.leaflet-container` valkoinen tausta jää (paperikarttamaster); 50 % = 50 % kuultaa läpi. Vaikuttaa VAIN pohjatiiliin; overlayPane-reitit (z400) & markerPane-merkit (z600) opaque ∴ merkit/reitit/merkki-opacity (V51) koskemattomia. ⊥ kutsu `tileLayer.setOpacity` → tilePane persistoituu ∴ säilyy karttatyyli-vaihdossa (`map-init.ts:40-41` recreate lisää tiilet SAMAAN tilePaneen). Persistoituu localStorageen (`'karttamaster-basemap-opacity'`), clamp 0–1, palautuu reloadissa. HUOM: erillinen viewport-verho-pane hylätty — Leaflet-pane transformoituu kartan mukana ∴ ei peitä viewportia; tilePane-opacity on oikea mekaniikka. (T287) |
| V208 | Talkoo-sessio ilman deep-link-koodia → landing `/patkat` (V188/T271-hubi). `POST /api/auth/talkoo-login` ok & ⊥ pendingCode → navigoi `/patkat`, ⊥ jää `/`-karttaan. Koodin kanssa → deep-select ennallaan (T272). Järjestäjän tili-login juureen → oma rooli + kartta (V189 koskematon). Re-auth kesken session ⊥ redirectiä. (T296) |
| V209 | ∀ segment ∃ slug @ luonti — ⊥ vaadi "jaa linkki". Slug = `slugifyName(displayName)`, uniikki (V191). Nimenmuutos → slug regeneroituu & URL päivittyy; vanha slug KUOLEE (käyttäjäpäätös 2026-07-25: ⊥ alias-taulu, ⊥ takaisinpäin-yhteensopivuus — jaettu vanha linkki lakkaa toimimasta, järjestäjä jakaa uuden). Pätkä avattavissa & linkitettävissä heti luonnista. (T297,T298) |
| V210 | Slug ⊥ credentiaali. Slug elää `segments.slug`-sarakkeessa, ⊥ `talkoolainen_codes`-rivinä. Ihmisluettava slug (V191) + `POST /api/auth/code-login` = arvattava sessio ∴ auto-slug ∀ segmentille ⊥ saa luoda code-riviä. Portti = yleissalasana (V188). `/s/<slug>` resolvoituu client-side (`getSegmentForCode` synkatusta storesta) voimassa olevan session takaa. (T297) |
| V211 | Pätkän km-väli mitataan AINA YHTÄ kanonista reittiä vasten: `Segment.primaryRouteId` (uusi kenttä) määrää mitä geometriaa `[startDist,endDist]` tarkoittaa. `routeIds` = jaettu osuus (V25, näkyvyys+jäsenyys), ⊥ km-lähde. Pätkänluonnin 2 klikkiä (`segment-panel.ts receivePoint`): klikki 1 lukitsee `primaryRouteId`; klikki 2 km luetaan SAMASTA reitistä (⊥ oman snap-reittinsä km:stä) → `min/max` vertaa aina saman reitin lukuja. Klikki 2 > `SHARED_THRESHOLD_M` primaryReitistä → virhe "Toinen piste ei ole samalla reitillä", ⊥ hiljainen sekoitus. Legacy-pätkä ilman `primaryRouteId` → `routeIds[0]`. (B114) |
| V212 | Merkin km per reitti: `SignMarker.distanceByRoute: Record<routeId, meters[]>` ∀ `routeIds`-jäsen — arvo on LISTA ⊥ yksi luku. Syy (toteutus 2026-07-25): lenkillä/edestakaisella osuudella SAMA reitti ohittaa merkin useasti & jokainen km on yhtä tosi; yksi luku olisi ollut sama arpa jota B116 korjaa. `distanceFromStart` säilyy (primary/legacy, V3-järjestys & drive-UI). Pätkäsuodatus (V140 `resolveTaskMarkers`) hyväksyy merkin jos YKSI ehdokas `distanceByRoute[seg.primaryRouteId]`-listasta osuu `[startDist,endDist]` väliin; lista puuttuu → fallback `distanceFromStart`. ⊥ skalaaria sokeasti. LASKETTAVISSA geometriasta (`lat/lon` = totuus, km = johdettu) ∴ ⊥ VAADI kantamigraatiota: puuttuva `distanceByRoute` backfillataan lazysti latauksessa clientissä (`MarkerManager` GPX:n valmistuttua) & serverin validoinnissa. Vanhaan tuotantodataan ⊥ kosketa (§C-parkki). (B115) |
| V213 | Backend & frontend ! käyttävät SAMAA km-lähdettä pätkäjäsenyyteen. `markerInOwnSegment` (`server/marker-audit.ts`) peilaa `resolveTaskMarkers`ia (V154, B100-oppi) ∴ V212:n `distanceByRoute` ! kulkea payloadissa (`distance_by_route`) & DB-sarakkeessa, muuten talkoolainen näkee listan mutta saa 403:n samasta merkistä (tai päinvastoin). Server ⊥ säilytä reittigeometriaa (V149) ∴ server ⊥ voi laskea km:ää itse — se LUKEE clientin `distance_by_route`n samalla luottamustasolla kuin `distance_from_start`in (V149: johdonmukaisuusvahti, ⊥ turvaraja; kontrolli = audit T226 + undo T227). (B115,B114) |
| V214 | `nearestPointIndex` (`bearing.ts:26`) globaali argmin ⊥ yksikäsitteinen kun polyline palaa itsensä lähelle (lenkki, edestakainen osuus, haara): 2 fyysisesti vierekkäistä pistettä voivat olla km 12 & km 47, valinta ratkeaa metrin erolla = kohina. ∴ ∀ km-päätös joka voi osua tällaiseen kohtaan ! käyttää `nearestPointCandidates(points, lat, lon, threshold)` → KAIKKI lokaalit minimit ≤ threshold, & valitsija päättää KONTEKSTILLA (pätkän `[startDist,endDist]` sisään osuva ehdokas voittaa; drive-tilassa edelliseen sijaintiin jatkuva voittaa). 1 ehdokas → nykykäytös ennallaan. `nearestPointIndex` säilyy ohuena wrapperinä (⊥ rikkoa kutsujia). (B116) |
| V215 | Migraatio-/huoltoskriptit lukevat reittilistan SAMASTA lähteestä kuin sovellus (`ROUTE_DEFS`). Kanoninen koti = `src/logic/route-defs.ts` (siirretty sinne T303:ssa, `src/main.ts` re-exportaa yhteensopivuuden vuoksi): main vetää Leafletin+DOM:in ∴ skriptit ⊥ voineet importata sitä & tekivät oman kopion — juuri se kopio jäi jälkeen (B117). Puhtaassa `src/logic/`-moduulissa kopiolle ⊥ enää syytä. ⊥ omaa kovakoodattua kopiota (`scripts/migrate-marker-routes.ts:22` ROUTE_FILES = 5/6 → 110km-siirtymä puuttui). Primary-reitti = LÄHIN (sama sääntö kuin `nearestRouteAssignment`, `markers.ts:181`), ⊥ listajärjestyksen ensimmäinen osuva. NEAREST-fallback (orpo merkki, ⊥ reittiä ≤`SHARED_THRESHOLD_M`) ! etäisyyskatto `FAR_FROM_ROUTE_M`=500 m: yli katon → merkkiä ⊥ tägätä lainkaan, raportoi listalle käsin katsottavaksi (kilometrien päässä oleva merkki ⊥ saa pudota jonkun pätkän km-rangeen). (B117,B118) |
| V216 | Reittivärit erottuvat KAHDELLA riippumattomalla kanavalla, ⊥ pelkällä vaaleudella. Nyk. 3 SMTB-reittiä = sama sininen sävy 3 vaaleudella (`#4C97D6`/`#2F6FB0`/`#1E5A8F`) → katoaa auringossa, mobiilin autokirkkaudessa & päällekkäisillä osuuksilla (yksi väri peittää toisen täysin). Vaadi: sävyero myös tapahtuman sisällä + 2. kanava (viivakuvio/leveys) joka paljastaa alla kulkevan reitin jaetulla osuudella. Toimii akromaattisena (värisokeus + kirkas aurinko = sama vaatimus). DESIGN.md §Reitti-/pätkävärit = totuus. (T303) |
| V217 | Talkoo-sessio (yleissalasana V188, `session.talkoolainen_code = null`) = kenttätyöoikeus ∀ pätkään & merkkiin — EI segmenttisidosta (käyttäjäpäätös 2026-07-25: ⊥ hierarkiaa, kaverit auttavat toistensa pätkillä). `segments` PUT: kentät `inspected/inspectionNote/completed/startDist/endDist/equipment` ∀ segmentille (muut kentät järjestäjä+, ennallaan). `markers` POST ∀; PUT `status/location_note/sijainti` ∀ merkille — V150b range-tarkistus EI päde koodittomaan sessioon (ei pätkää mihin verrata), identiteettikentät (type/description/icon/image/template/parts) pysyvät järjestäjä+ (V150 ennallaan). SEURAUS jonka käyttäjä hyväksyi: `created_by` = `display_name` = "Talkoolainen" ∀ yleissalasana-käyttäjälle ∴ V151:n "oma itse-luoma" -poisto-oikeus kattaa kaikki talkoolaisten luomat merkit. Koodillinen legacy-sessio (`code-login`) pitää nykyiset rajansa. (T306) |
| V218 | Kartan muokkaustila ∀ roolille (järjestäjä & talkoolainen). `document.body.dataset.mapMode ∈ {'katselu','muokkaus'}`, DEFAULT `katselu` JOKAISELLA sivunlatauksella (⊥ persistoi localStorageen — vahinkomuokkaus reloadin jälkeen = juuri se mitä estetään). KATSELU: merkit ⊥ draggable (`MarkerManager.setDraggableFn` → false ∀), kartan klikki/tuplaklikki ⊥ luo merkkiä (`PlaceMode` disarmattu & dblclick-haara no-op), pätkän rajakahvat (`segment-overlay.enterEditMode`) ⊥ raahattavissa. MUOKKAUS: nykyinen käytös (V150-rajat voimassa). Siirtymä vaatii eksplisiittisen käyttäjätoimen (toggle) — ⊥ automaattista siirtymää merkin/pätkän valinnasta. Yhtenäinen tila: `PlaceMode.armFromSidebar` ja rajojen raahaus kuuluvat muokkaustilan ALLE, ⊥ rinnakkaisia mekanismeja. (T307) |
| V219 | Muokkaustila erottuu katselusta pysyvästi ja perifeerisesti — talkoolainen metsässä (aurinko, hanskat) ⊥ saa joutua arvaamaan tilaa. ! vähintään: (a) kartta-alueen kehys accent-värillä (`--accent`), (b) pysyvä tilapilleri/banneri tekstillä "Muokkaustila" (⊥ pelkkä ikoni, ⊥ pelkkä väri — V197-linja), (c) toggle-nappi ≥44px kummassakin tilassa & sen label kertoo KOHDETILAN. Kontrasti ≥4.5:1 molemmissa teemoissa. §K-sopimus DESIGN.md:hen. (T308) |
| V220 | Merkin siirto kartalla on optimistinen VAIN kunnes serveri vahvistaa. `MarkerManager` dragend (`src/map/markers.ts:410`) ! ottaa talteen edellisen tilan (`lat/lon/distanceFromStart/distanceByRoute/routeIds`) ja PALAUTTAA sen + Leaflet-markerin `setLatLng`:n + re-renderin jos `PUT /api/markers/:id` vastaa 403/muu virhe (server hylkää oman pätkän ulkopuolelle siirron, V150 + `server/routes/markers.ts:217`). Nyk. mutatoi paikallisen `m`:n ENNEN `apiPut`:ia & kutsuu `onUpdate()` ehdoitta → hylätty siirto jää client-tilaan = divergenssi serveristä. Hylkäys ! näkyä käyttäjälle (`showWarning`), ⊥ hiljaisesti. Yleistys: mikä tahansa optimistinen merkkikirjoitus jonka server voi 403:lla hylätä ! rollbackata (⊥ vain siirto). (B121→T309) |
| V221 | Merkin pätkäjäsenyys siirron jälkeen ratkaistaan pätkän PRIMARY-reitin km:llä (`distanceByRoute[segment.routeId]`, V212/V215), ⊥ globaalisti lähimmän reitin km:llä. Nyk. dragend valitsee `bestRouteIdx` = lähin piste yli KAIKKIEN reittien ja kirjoittaa sen `distanceFromStart`iin ∴ jaetulla osuudella (3 SMTB-reittiä samalla polulla, §C graafi) merkki voi pudota oman pätkän `[startDist,endDist]`-rangesta liikkumatta käytännössä minnekään. Sama sääntö client & server (`markerInOwnSegment`) — kerrokset ⊥ saa olla erimielisiä jäsenyydestä (B100-oppi). (B121→T309) |
| V222 | Talkoolaisella on AINA tavoitettava polku takaisin `/patkat`-hubiin — molemmissa moodeissa (`data-view-mode=koti` JA `kartta`), ilman URL:n käsin kirjoittamista. Nyk. `src/ui/account-menu.ts:26` gatettaa `/patkat`-linkin ehdolla `role !== 'talkoolainen'` (T274/V189 järjestäjä-crossover) ∴ talkoolainen on umpikujassa. Model B (V188) tekee hubista talkoolaisen KODIN — pätkä valitaan sieltä ∴ paluu ! olla peruspolku. Sessio säilyy (cookie), ⊥ uutta kirjautumista. 🏠 (`#btn-home-view`) ⊥ ole tämä: se vaihtaa moodin saman pätkän sisällä, ei sivua — eri asia, molemmat ! olla olemassa & erottua labelista. (B122→T310) |
| V223 | `/patkat`-hubin primary-toiminto ("Kartalle →", `src/ui/patkat-page.ts:87`) on aina viewportissa ⊥ vaadi skrollausta pätkälistan ohi. Sticky/kiinteä alapalkki + vastaava `padding-bottom` sisältöön ettei viimeinen listarivi jää palkin alle (B101/V157-oppi: kiinnitetty palkki ⊥ saa jättää orpoa gappia eikä peittää sisältöä). Kosketuskohde ≥44px (DESIGN.md §R). (T311) |
| V224 | Hero-alapalkin ◀▶-selailunuolten x-sijainti on VAKIO — ankkuroitu palkin vasempaan & oikeaan reunaan, riippumaton valitun merkin nimen pituudesta (label joustaa keskellä: `flex:1` + `min-width:0` + ellipsis). Nyk. `src/ui/segment-hero.ts:100–144` renderöi `◀ label ▶` inline-rivinä ∴ nuoli hyppää merkistä toiseen → hanskakäsi/hiiri ! hakea kohde joka kerta. Kosketuskohde ≥44×44, disabled-clamp päihin säilyy (V159). (T312) |
| V225 | Talkoolaisen view-moodi (V174) säilyy saman session refreshissä: `sessionStorage`-avain per pätkä-slug tallettaa viimeisen moodin; palautus vain samalle slugille. TUORE `/s/<slug>`-deep-link (ei tallennettua moodia) → `koti` (V174 ennallaan). ⊥ `localStorage` — uusi päivä/uusi pätkä alkaa kodista. Syy: vahinkorefresh metsässä ⊥ saa heittää karttatyöstä varustelistalle. (T313) |
| V226 | Talkoolaisen sisältöpaneelit ovat RAJATTUJA scroll-alueita, ⊥ vapaasti kasvavia. Sivukuori ⊥ scrollaa (`html:has(#app),body:has(#app){overflow:hidden}`, V187b) & `#map-area{overflow:hidden}` leikkaa ylijäämän ∴ jokainen pystysuunnassa DATAN MUKANA kasvava alue ! omistaa OMAN scrollerinsa jonka korkeus on sidottu viewportiin (`flex:1;min-height:0;overflow-y:auto`), ⊥ `max-height:none` (= korkeus karkaa sisällön mukaan → oma `overflow-y` ⊥ koskaan aktivoidu). Koskee ∀ moodia (koti/kartta) & ∀ koti-tabia (Varustelista/Kaikki merkit/Kommentit): lista jonka pituus riippuu datasta ⊥ saa määrätä kuorensa korkeutta. Viimeinen rivi ! tavoitettavissa 375×667 JA 1280×720. (B123→T315) |
| V227 | Audit-rivin `segment_code` johdetaan MUTATOIDUSTA MERKISTÄ, ⊥ sessiosta. `logMarkerAudit` (`server/marker-audit.ts`) ratkaisee pätkän SAMALLA kanonisella unionilla kuin `markerInOwnSegment` (V154: route∩ ∧ dist∈[start−ε,end+ε] ∪ `linked_marker_ids` ∪ `marker_type_filter`) ∴ kooditon talkoo-sessio (V217/T296) & järjestäjä tuottavat MOLEMMAT pätkäkoodillisen rivin. Sessio ⊥ ole luotettava lähde: yleissalasana ⊥ kanna koodia (`auth.ts:105-110` `talkoolainen_code=NULL`). Merkki joka ⊥ osu yhteenkään pätkään → NULL sallittu (reititön alue, V139). ⊥ uutta jäsenyyslogiikkaa — kopio hajoaisi kuten B100. Ilman tätä T227-undo ⊥ löydä rivejä & pätkämodaalin loki on tyhjä (B124). (T316) |
| V228 | Jokainen kirjautuminen tuottaa TUNNISTETTAVAN tekijän: hub-login (`POST /api/auth/talkoo-login`) vaatii nimen (trim, 2–40 merkkiä) & tallentaa sen `sessions.display_name`iin — jaettu salasana ⊥ riitä identiteetiksi. Nimi ⊥ OLE TURVARAJA (kuka tahansa kirjoittaa mitä tahansa; V149-linja: johdonmukaisuusvahti ⊥ pääsynvalvonta) vaan JÄLJITETTÄVYYSVÄLINE: audit-loki ⊥ voi vastata kysymykseen "kuka" jos ∀ sessio on 'Talkoolainen' (B124: 11 kpl tuotannossa). Tyhjä/liian lyhyt → 400, ⊥ hiljaista fallbackia geneeriseen nimeen (V21). SIIRTYMÄ (T322): kenttätyö oli jo käynnissä kun nimi tuli pakolliseksi ∴ ennen sitä kirjautuneet sessiot elävät 7 vrk nimettöminä — uudelleenkirjautumista ⊥ voi vaatia kesken maastotyön (talkoolainen ⊥ tiedä yleissalasanaa ulkoa metsässä) ∴ nimi ! olla asetettavissa KESKEN session (`POST /api/auth/name`), ohitettavissa (⊥ estä kenttätyötä — nimi on jäljitettävyysväline ⊥ pääsynvalvonta). (T317,T322) |
| V229 | `action='remove'` kirjaa `payload_json`iin KOKO merkkirivin (∀ `markers`-sarake), ⊥ pelkkää viitettä ∴ poisto on peruttavissa INSERTillä. Muut actionit säilyttävät V152-semantiikan (osittainen ennen-tila). Undo valitsee restore-strategian actionin mukaan: `add`→DELETE, `move`/`status`→UPDATE, `remove`→INSERT ALKUPERÄISELLÄ `id`llä ∴ `linked_marker_ids`-viittaukset & `marker_images`-rivit ⊥ jää orvoiksi. Payload ilman täyttä riviä (legacy, ennen T318) → `not_undoable`, ⊥ osittaista restorea (puolittainen merkki on pahempi kuin puuttuva). (T318) |
| V230 | Per-rivi-undo (`POST /api/audit/undo/:auditId`) on atominen `db.transaction()` (V100/V153-linja) & kirjaa PALAUTUKSEN ITSENSÄ audit-riviksi (`actor`=peruuttaja, `payload_json`=tila ENNEN palautusta) ∴ peruutus on peruttavissa samalla työkalulla — sama sopimus jota `scripts/restore-marker-moves.ts` noudattaa. `requireRole('admin','järjestäjä')`. Jo peruttu rivi → 409 (⊥ tuplarestorea). Merkki poistettu välissä & action≠`remove` → 404 selkeällä viestillä, ⊥ hiljaista no-oppia (V21). (T319,T321) |
| V231 | `/loki` on OMA entry (`loki.html`+`src/loki.ts`), ⊥ osa `admin.html`ia: audit-API sallii `admin`+`järjestäjä`, mutta `src/admin.ts:99` gettoi KOKO sivun `role==='admin'`iin ∴ järjestäjä (näkymän pääkäyttäjä) ⊥ pääsisi sisään. Rooligate `admin|järjestäjä`, muu → `renderForbidden`. Tyylit OMAAN `src/audit-log.css`:ään, ⊥ `src/style.css`:ään — tokenit peritään `:root`ista, ⊥ kopioida arvoja (V81-linja). (T321) |
| V232 | Merkin siirto on KUMOTTAVISSA TEKIJÄN ITSENSÄ toimesta heti siirron jälkeen — ⊥ blokkaavaa vahvistusta ennen siirtoa. Vahvistusdialogi verottaisi JOKAISTA laillista siirtoa (hanskat, aurinko, kiire; VISION "max 2 nappia"), undo vain virheellistä ∴ kitka kohdistuu oikein. Toteutus peilaa V172:ta (inventaarion "Kumoa"): CLIENT-ONLY, YKSI undo-slotti (uusi siirto korvaa edellisen), reload/navigaatio → slotti katoaa, ⊥ omaa backend-taulua (`marker_audit` kantaa jo pysyvän jäljen T226/V152 & järjestäjän per-rivi-undon T319/V230 — tämä on nopea itsekorjaus, ⊥ toinen totuuslähde). Kumoaminen kulkee SAMAA durable-polkua kuin siirto (V116-outbox) ∴ offline-kumoaminen jonottuu kuten offline-siirto. Undo palauttaa V220-snapshotin (`snapshotMarkerPosition`) — sama ennen-tila jonka rollback käyttää, ⊥ toista kopiota. Toast näkyy VAIN kun serveri vahvisti siirron: hylätty siirto (403) on jo rollbackattu V220:ssa ∴ kumottavaa ⊥ ole. (T323) |

| V233 | e2e-baseline ! olla vihreä TAI jokainen punainen testi kirjattu §B:hen juurisyyllä. Punainen baseline = sokea piste: 2026-07-25 T307–T315-erässä 12 failausta joista 9 oli punaisia jo HEADissa ∴ aidot 3 regressiota erottuivat vasta erillisellä baseline-worktree-ajolla (`git worktree add <tmp> HEAD` + `bunx playwright test`). Sääntö: ⊥ hyväksytä "nuo failaa muutenkin" ilman §B-riviä. Deploy sallittu punaisella baselinella VAIN jos punaiset ∈ §B (tietoinen velka), ⊥ tuntemattomia. (B128→T324,T325) |
| V234 | Aktiivi-tilan kytkinnappi (`.map-mode-toggle.active`, `.inv-mode-toggle.active`, ∀ tuleva) ! kantaa aktiivisuuden REUNALLA/tintillä & tekstillä, ⊥ accent-täytöllä valkoisella tekstillä: `--accent` (#F2542D) + #fff = 3.46:1 & accent-teksti vaalealla pinnalla ~3.2:1 ∴ molemmat < 4.5:1 vaalealla teemalla (T308-mittaus). Teksti `--text-body` accent-tintin/`--surface-card`in päällä = ≥12:1 molemmissa teemoissa. Nykyinen rikkoja: `.inv-mode-toggle.active` (T251, inventaario). (B125→T326) |
| V235 | Kulkusuunnan navigaatio ("Seuraava merkki" -hero, ◀▶-selailu, drive-hyppy) järjestää merkit PÄTKÄN/AKTIIVISEN REITIN km:llä — `distanceForRoute(m, routeId)` (V212/V214), ⊥ skalaarilla `m.distanceFromStart`. Skalaari on mitattu SIJOITUSHETKEN lähimmältä reitiltä (`types.ts:18`) ∴ eri merkeillä eri akseli → `sort()` tuottaa mielivaltaisen järjestyksen. Akseli = `segmentPrimaryRouteId(segment)` (`segments.ts:71`, fallback `routeIds[0]` — prod-pätkillä `primary_route_id` on NULL 9/9 ∴ fallback on TODELLINEN polku ⊥ reunatapaus) herossa, `activeRouteProvider().id` drivessä. Sama sukulinja kuin V211 (piirto), V212 (jäsenyys), V221 (siirto): km-akseli ! olla eksplisiittinen ∀ km-vertailussa. Semantiikka: seuraava = pienin akseli-km > nykyinen ∴ lähin lähdöstä ensin, sitten eteenpäin. YKSI järjestys kaikkialla — hero & drive ⊥ saa olla eri mieltä "seuraavasta". Mitattu prod-datalla 2026-07-25: 3/9 pätkää rikki, Kendall tau nyt-vs-oikea 0.282 (Pätkä 4). (B126→T327) |
| V236 | Pätkän merkkimäärä & -jäsenyys on SAMA riippumatta sisääntulosta (`/patkat`-hub, `/s/<slug>`-näkymä, kartta, server `markerInOwnSegment`). ⊥ saa riippua siitä onko client ehtinyt ladata GPX:t: `distanceByRoute` on JOHDETTU kenttä (client-backfill `main.ts:66`, ⊥ persistoitu) ∴ jokainen näkymä joka lukee jäsenyyttä ILMAN sitä laskee eri tuloksen (`distancesForRoute` → skalaari-fallback). Mitattu 2026-07-25: hub vs. näkymä eroaa 7/9 pätkällä (Pätkä 4: 8 vs 13). Sääntö: kerrokset ⊥ saa olla erimielisiä jäsenyydestä (B100/V221-oppi toistuu kolmatta kertaa) ∴ km-akselin ! olla saatavilla KAIKISSA jäsenyyttä lukevissa poluissa tai jäsenyys ! tulla yhdestä lähteestä (server). Uusi näkymä joka listaa pätkän merkkejä ! todistaa tämä testillä, ⊥ olettaa backfillin ajetun. (B127→§T avoin) |
| V237 | PÄTKÄ OMISTAA KM-AKSELIN — merkin paikka pätkässä on JOHDETTU SUHDE, ⊥ merkin kenttä & ⊥ tallennettava. `segmentKm(marker, segment)` = se `distancesForRoute(m, segmentPrimaryRouteId(seg))`-ehdokas joka osuu `[startDist−ε, endDist+ε]`. ⊥ saa tallentaa merkkiin: merkki kuuluu USEAAN pätkään (jaettu osuus V25, `linkedMarkerIds` V140, `markerTypeFilter` V143 = V154-unioni) ∴ "järjestysluku" ⊥ ole olemassa globaalisti, vain pätkän suhteen. Tämä KUMOAA akselin kuljettamisen erillisenä `routeId`-parametrina (V235:n toteutustapa): parametri oli vapausaste jonka kutsuja saattoi unohtaa → hiljainen fallback skalaariin → B126 (järjestys) & B129 (näyttö) samasta juuresta kahdesti. Konteksti joka näyttää pätkän merkkejä TUNTEE pätkän jo ∴ akselia ⊥ voi unohtaa. Sivutuote: LENKKI (V214/B116) ratkeaa oikein — nyk. `orderKm` ottaa PIENIMMÄN ehdokkaan = arvaus, `[start,end]` VALITSEE ehdokkaan = ⊥ arvausta. JÄRJESTYS & NÄYTTÖ ovat yksi sopimus: kumpikaan ⊥ lue `m.distanceFromStart`ia (V238). (B129→T328) |
| V238 | Järjestyksen SUUNTA seuraa pätkän VAIHETTA, ⊥ ole vakio: `phase='asettaminen'\|'tarkastus'` → nouseva `segmentKm` (alusta loppuun, myötäsuuntaan reittiä — merkit asetetaan ajosuunnassa); `phase='purku'` → LASKEVA (lopusta alkuun — purku ajetaan vastasuuntaan, käyttäjäpäätös 2026-07-25). `phase` on jo `segments.phase` ∴ ⊥ uutta kenttää, suunta on phasen funktio (lookup ⊥ if-ketju, V91-kuvio). Merkki joka ⊥ osu `[start−ε,end+ε]`-välille ⊥ katoa hiljaa & ⊥ mene sekaan järjestykseen: oma ryhmä listan ALKUUN otsikolla "ei reitillä", järjestettynä OMALLA skalaarillaan (se on niille ainoa olemassa oleva km & säilyttää reitittömän keräyskasa-tehtävän T218/V139 entisen järjestyksen) (käyttäjäpäätös 2026-07-25; hiljainen katoaminen olisi B127:n uusi versio). Prod 2026-07-25: Pätkä 4:llä 2 merkkiä 25.69 km > `end_dist` 25.653 ∴ ε=50 m kantaa ne juuri & juuri — ryhmä ⊥ ole teoreettinen. (T328) |
| V239 | Audit-lokin rivi kertoo ONKO se palautus vai alkuperäinen toimenpide. Peruutus (`POST /api/audit/undo/:auditId`, V230) kirjaa oman rivinsä käänteisaktiolla (`add`↔`remove`, `move`/`status` säilyttävät actionin, `audit.ts:131`) ∴ palautettu siirto näyttää lokissa TÄYSIN samalta kuin ihmisen tekemä siirto — 2026-07-25 incidentin jälkiselvittelyssä tämä on juuri se ero joka ! näkyä ("siirsikö joku merkin vai palautettiinko se?"). Rivi ! kantaa viittaus alkuperäiseen (`undo_of_id`) & verbi ! erottua ("palautti siirron" ⊥ "siirsi merkkiä"). Peruutuksen peruutus (⊥ estetty) näkyy ketjuna, ⊥ silmukkana. Jäljitettävyys ⊥ ole pääsynvalvonta (V228-linja) vaan kysymykseen "mitä oikeasti tapahtui" vastaaminen. (T331) |
| V240 | ⊥ YKSIKÄÄN audit-rivi ole nimetön. Jokainen sessio kantaa tunnistettavan tekijän — jos `users.display_name` puuttuu, `username` on fallback (`auth.ts:44`), ⊥ NULL. Todiste 2026-07-25: `admin`-käyttäjän `display_name` on NULL kannassa ∴ 12 admin-sessiota & 4 audit-riviä ovat `actor=NULL` → `/loki` näyttää "tuntematon" juuri sille roolille jolla on eniten valtaa (poisto, massaperuutus). V228 vaati nimen TALKOOLAISELTA; sama vaatimus koskee ∀ roolia — muuten jäljitettävyysketju katkeaa ylhäältä. Fallback-ketju (käyttäjäpäätös 2026-07-25 "pistetään että se on role tai username"): `display_name` → `username` → ROOLI ('admin'/'järjestäjä'/'talkoolainen'). 'tuntematon' jää VAIN legacy-riveille joilla `actor` on jo NULL kannassa — uusi rivi ⊥ voi koskaan olla nimetön, koska rooli on aina tiedossa. Rooli ⊥ yksilöi henkilöä mutta kertoo VALTATASON ∴ jälkiselvittelyssä "admin poisti" on käyttökelpoinen tieto, "tuntematon poisti" ⊥ ole. Olemassa olevat NULL-rivit jäävät NULLiksi (B124-oppi: ⊥ arvata takautuvasti), mutta `users`-rivi korjataan ∴ UUSIA nimettömiä ⊥ synny. (T331) |

| V241 | Onnistuneen toiminnon vahvistus ! SELVIYTYÄ siitä uudelleenrenderöinnistä jonka toiminto itse laukaisee. Viesti joka asetetaan DOM-solmuun jonka `innerHTML=''` pyyhkii seuraavalla tikillä ⊥ ole palautetta vaan kilpajuoksu (B130: `/loki`-undo). Kuvio: tila (viesti) elää renderin ULKOPUOLELLA & render lukee sen — ⊥ render tuhoa tilaa. Koskee ∀ näkymää joka yhdistää "aseta status" + "lataa lista uudelleen" (`/loki`, inventaarion undo-toast T252, outbox-reauth). Testivaatimus: assert ⊥ saa nojata siihen että uudelleenlataus on hidas — vahvistuksen ! olla luettavissa myös kun fetch palaa välittömästi (mockattu). aria-live-ilmoitus katoaa hiljaa ruudunlukijalta jos solmu poistetaan ∴ tämä on MYÖS saavutettavuusvaatimus, ⊥ vain kosmetiikkaa. (B130→T332) |
| V242 | Talkoolaisen hero-ohjaus (`.segment-view-next`) piiloutuu VAIN käyttäjän eksplisiittisestä kutistuskomennosta (chevron `.segment-view-collapse`), ⊥ minkään muun toiminnon sivuvaikutuksena. Panorointi/fokus ("Näytä kartalla" `segment-hero.ts:178`, keräyslistan rivi `segment-view.ts:319`) siirtää KARTTAA — ⊥ kosketa kutistustilaan. Yleissääntö: toiminto ⊥ saa poistaa sitä ohjainta jolla käyttäjä sen käynnisti, ellei tilalle tule TEKSTILLISTÄ paluukontrollia samassa näkymässä — pelkkä ikoni (18px chevron, vain aria-label) ⊥ riitä kenttäkäytössä (hanskat, aurinko, stressi). Sukua V220 (hero ⊥ katoa siirrosta) & V222 (paluupolku ! ∃). (B131→T333) |
| V243 | Kartan fokus-tila HIMMENTÄÄ, ⊥ PIILOTA — olemassa oleva merkki näkyy AINA kartalla riippumatta fokuksesta (DESIGN.md §K MarkerFocus). Kartta joka jättää merkin renderöimättä valehtelee: talkoolainen ⊥ tiedä onko naapuripätkän merkki jo asetettu vai puuttuuko se, & järjestäjä laskee väärän kokonaismäärän. Himmennysarvo ⊥ ole sama kuin viivojen `CONTEXT_DIM_OPACITY 0.22` (`segment-overlay.ts:27`): viiva on satoja px pitkä, merkki 40×48 ∴ sama alfa katoaa ilmakuvasta auringossa — kaksi kanavaa (alfa+kylläisyys) korvaa syvemmän alfan. Himmennys ⊥ saa kytkeytyä pohjakartan slideriin (T287/V201, eri pane, ⊥ kertolaskua). Fokusoitua merkkiä ⊥ korosteta ylöspäin: `.marker-next-highlight` (T256/V178) omistaa "seuraava merkki" -tason, kolmas taso syö molemmat. LISÄKSI: jokainen EI-OLETUSTILA jonka käyttäjä kytkee päälle ! kantaa näkyvä poistumis-affordanssi samassa näkymässä (`.map-mode-pill`-kuvio, T308/V219) — modaalista kytketty tila joka jää päälle modaalin sulkeuduttua on sama umpikuja-luokka kuin B131. **AMEND 2026-07-28 (PM-päätös):** himmennystaso = KOLME PORRASTA — kevyt (merkki 0.4 / viiva 0.22 = alkuperäiset kalibroidut arvot) · vahva (merkki 0.15 / viiva 0.08) · piilota (⊥ renderöidä). Porras on JÄRJESTÄJÄN asetus (`MapFilter.dimLevel`, oletus **vahva**). TALKOOLAISEN automaattifokus (V142) pysyy VAKIONA kevyt-portaassa ∴ kenttänäkymä ⊥ muutu tapahtuman aikana kenenkään säädöstä — jaettu oletus olisi tummentanut jokaisen talkoolaisen kartan ilman että kukaan pyysi. "Piilota" sallittu VAIN eksplisiittisestä järjestäjän valinnasta — ⊥ oletuksena, ⊥ minkään toiminnon sivuvaikutuksena ∴ valehtelu on käyttäjän tietoinen valinta ⊥ järjestelmän oletus, & alkuperäinen perustelu pätee oletuksessa. Kaksikanavaisuus (alfa + `grayscale(1)`) säilyy ∀ portaalla; merkin & viivan arvot pysyvät ERI (peruste yllä pätee ∀ portaalla). DESIGN.md §K MarkerFocus "Himmennysarvo" = kolmiportainen taulukko ⊥ yksi arvo. (T334,T335,T376,T377) |
| V244 | Pätkä & reitti ovat ERI visuaalisia kanavia jotka ! olla luettavissa YHTÄ AIKAA samasta pikselistä. Pätkä piirtyy reitin päälle ∴ yksi viiva = yksi kanava = toinen tieto katoaa. Sopimus (DESIGN.md §K SegmentCasing): casing — pätkäväri `weight 15` alla, reitin väri `weight 9` päällä ⇒ sisus=reitti, reuna=pätkä; statuskieli (`LINE_STATE_STYLE`) elää CASINGISSA, sisus pysyy ehjänä. Ehto jonka ilman kuvio ⊥ toimi: `SEGMENT_COLORS ∩ ROUTE-värit = ∅` & pätkäväri ≥3:1 sisusväriään vasten. RIKKI NYT: `SEGMENT_COLORS[0]='#2F6FB0'` (`segments.ts:274`) = `smtb-55` (`route-defs.ts:12`) pikselilleen ∴ DESIGN.md §K:n oma sääntö "paletti ⊥ saa sisältää route-värejä" on ollut voimassa mutta valvomatta. Paletti kalibroidaan KERRAN T304:ssä (V216), ⊥ kahdesti. **AMEND 2026-07-27 (B137):** ehto `pätkäväri ≥3:1 sisusväriään vasten` on YLIMÄÄRITELTY (reittiväri on lukittu kaistaan 0.142–0.304 pillerin & taustakartan väliin ∴ mikään pätkäpaletti ⊥ täytä sitä ∀ parille — mitattu huonoin 2.19). Korvataan GEOMETRIALLA: casing = **kolme viivaa** — pätkäväri `weight 15`, valkoinen erotin `weight 11`, reitin väri `weight 9` ⇒ reunus & sisus ⊥ ole koskaan vierekkäin & kumpikin kontrastoi VALKOISTA vasten (mitattu: reitit 3.40–5.23, pätkät 11.49–11.64). Ehto kuuluu siis muotoon: pätkä- & reittivärin ! kumpikin saada ≥3:1 valkoista (`#FFFFFF`) vasten. `SEGMENT_COLORS ∩ ROUTE-värit = ∅` säilyy (T304: eri vaaleusperheet ∴ leikkaus tyhjä myös silmällä). (T336) |
| V245 | Huomio (`Comment`, `targetType:'point'`) ⊥ OLE MERKKI eikä saa vuotaa merkkien joukkoon missään kerroksessa: ⊥ `comments`-rivi päädy `SignMarker`-listaan, merkkilaskureihin (`getSegmentStatusCounts`, `getPhaseProgress`, `/patkat`-hubin määrä V236), pätkäjäsenyyteen (`resolveTaskMarkers` V140), navigaatioon (`firstUnsetMarker`/`stepUnset` V235) eikä varustelistaan. Erillinen taulu (`comments`, T221) tekee tästä TOSI nyt — invariantti suojaa sen: huomiolla ⊥ ole statusta, elinkaarta (V9) eikä km-akselia (V237) ∴ mikä tahansa polku joka "yhdistää kaikki karttapisteet" rikkoo talkoolaisen edistymälukeman ("12/13 asetettu" muuttuisi 12/14:ksi huomiosta jota kukaan ⊥ voi asettaa). Visuaalinen ero ! olla MUODOSSA ⊥ värissä (DESIGN.md §K CommentPin: pyöreä 32px kupla vs. merkin 40px neliökortti+kärki) — auringossa & värisokealle väri ⊥ kanna eroa. (T221,T237) |
| V246 | Kentällä otettu VALOKUVA ! olla katsottavissa täysikokoisena siinä näkymässä johon se on liitetty. Thumbnail ⊥ ole kuvan esitys vaan sen KAHVA: `object-fit:cover` rajaa (`.marker-detail-image-thumb` 72×72, `style.css:3688`) ∴ pelkkä thumb HÄVITTÄÄ juuri sen mitä kuvaan tallennettiin — kuvan pointti on todiste jota sanat ⊥ kanna ("tältä se näyttää"). Sääntö: jokainen kuvan thumb on klikattava (≥44px hit-area §R) & avaa lightboxin jossa `object-fit:contain` + Esc/backdrop-sulku (escape-chain). Lightbox ! olla YKSI jaettu komponentti — merkkien kuvat, huomioiden kuvat & kylttivisuaali (`marker-visual-row.ts:126-160`) ⊥ saa saada kolme eri sulkemis-käytöstä. LISÄKSI: kuvan LIITTÄMINEN huomioon on ∀ autentikoidun oikeus (V13), ⊥ järjestäjä+ kuten merkkikuvissa (`markers.ts:366`) — talkoolainen on se joka NÄKEE ongelman maastossa; jos hän ⊥ voi liittää kuvaa, huomio kutistuu tekstiksi jonka järjestäjä joutuu arvaamaan. (B132→T337,T338) |
| V247 | Oma sijainti (GPS) on LAITTEEN kyky ⊥ pätkän ominaisuus ∴ sen kytkentä ⊥ saa elää segment-datan ehdoilla: `#btn-tk-gps`-kuuntelija kytketään talkoolaiselle AINA kun rooli on talkoolainen, riippumatta siitä löytyykö `getSegmentForCode`-pätkää (⊥ `if (seg)`-lohkon sisällä, `markers-wiring.ts:328`). LISÄKSI paikannus ⊥ saa epäonnistua hiljaa: jokainen `GeolocationPositionError` ! tuottaa näkyvän syyn talkoolaiselle (lupa evätty / sijainti ei saatavilla / aikakatkaisu) & napin tila ! kertoa totuus — "📍 GPS päällä" VASTA ensimmäisen fixin jälkeen, sitä ennen "📍 Haetaan…", virheessä takaisin pois-tilaan. `console.warn` (`gps-navigator.ts:18`) ⊥ ole käyttäjäpalaute — metsässä ⊥ ole devtoolsia ∴ hiljainen virhe = korjaamaton vika. (B133→T341) |
| V248 | Testisuiten AJONOPEUS on osa sen käyttökelpoisuutta — suite jota ⊥ jaksa ajaa ⊥ suojaa mitään. (1) jsdom annetaan VAIN tiedostolle joka koskee DOM:ia. Globaali `environment: 'jsdom'` (`vite.config.ts:41`) maksaa mitatusti ~0.74s/tiedosto & 144:stä vain 87 tarvitsee sen ∴ 57 tiedostoa maksaa jsdom-veroa turhaan (otos 20 puhdasta: jsdom 18.5s → node 3.7s = 5×). (2) Testitiedosto ⊥ saa vuotaa tilaa toiseen tiedostoon: eristys on turvaverkko ⊥ lupa jättää siivoamatta. Vuotava tiedosto on RIKKI vaikka se on vihreä eristettynä — se piilottaa moduulitason tilan jota tuotantokoodikin kantaa (mitattu: `--no-isolate` paljastaa 51 vuotavaa testiä 5 tiedostossa). Nopeutus ⊥ saa KOSKAAN ostaa testiä poistamalla, skippaamalla | odotusarvoa löysentämällä: mittari on sama testimäärä vihreänä lyhyemmässä ajassa. (T342) |
| V249 | Pre-deploy-backup suojaa yhtä asiaa: tuotanto-DB:stä on palautuskelpoinen kopio ENNEN kuin uusi koodi koskee siihen. Sen ! olla NOPEA muuten se rapautuu — hidas pakollinen vaihe joko ohitetaan (`SKIP_BACKUP=1` tulee tavaksi) | deployta lykätään, & molemmat ovat huonompia kuin kevyempi backup. ∴ deployn kriittisellä polulla saa olla vain se osa joka suojaa julkaisuhetkeä (volume-snapshot, ⊥ vaadi käynnissä olevaa konetta); kaikki mikä vaatii ssh:ta nukkuvaan koneeseen (`min_machines_running = 0`) kuuluu deployn JÄLKEEN. Turvallisuutta ⊥ poisteta vaan SIIRRETÄÄN: jokainen backup-vaihe ajetaan edelleen & jokaisen epäonnistuminen näkyy — taustalle siirretty vaihe joka epäonnistuu hiljaa = ei backupia lainkaan. (T343) |
| V250 | Yhdellä dialogilla saa olla VAIN YKSI tallennusmalli. `SegmentDetailsModal` tallentaa 6/8 osiota heti (varusteet, assign, rajat, klooni, korostus, poisto) & 2 (nimi, kuvaus) vasta footerin `Tallenna muutokset`-napista ∴ footer lupaa enemmän kuin tekee: ✕:stä sulkeva käyttäjä luulee menettäneensä varusteet (⊥ menettänyt) & säilyttäneensä nimenmuutoksen (⊥ säilyttänyt). Kaksi mallia samassa dialogissa = arvaus jokaisella sulkemisella. Sopimus: kenttä tallentuu blurilla, footer = `Sulje` (`.modal-btn-secondary`), ⊥ primarya — dialogissa jossa ⊥ ole vahvistettavaa ⊥ ole `Tallenna`a eikä `confirm`-täytteistä nappia (kumpikin lupaisi tallennuksen jonka kenttä on jo tehnyt). LISÄKSI destruktiivinen toiminto elää footerin `.modal-footer-destructive`-rivillä pienenä tekstinappina (DESIGN.md §Modal footer -pattern), ⊥ koskaan `danger-soft`-blokkina rungossa — alkuperäinen huoli (footerin ALLE jäävä vaaravyöhyke luetaan toiseksi dialogiksi) ratkeaa TYYLILLÄ ⊥ sijainnilla (T352 korjaa). LISÄKSI listarivin nimi ! olla klikattava sisääntulo (DESIGN §K "Item-label: klikkaus = toiminto") — 44px `···` yksin jättää ~80 % rivistä kuolleeksi alueeksi. (T344,T345,T346,T352) |
| V251 | Karttalayeria ⊥ saa renderöidä ennen kuin sen kuuntelijat on kytketty. `SegmentOverlay.update()` päättää klikkikuuntelijan kytkennän KERTALUONTOISESTI piirtohetkellä (`if (this.onSegmentClick …)`, `segment-overlay.ts:116`) ∴ ennen `setOnSegmentClick`ia ajettu render tuottaa layerin joka NÄYTTÄÄ oikealta mutta ⊥ reagoi — & vika on ajoittainen koska myöhempi uudelleenrender (merkin muutos, phase-vaihto) korjaa sen huomaamatta. Sopimus: init-järjestys on `luo layer → kytke kaikki kuuntelijat → renderöi kerran`; jos render on pakko ajaa aiemmin, kuuntelijan kytkennän ! laukaista uudelleenrender itse. Sama koskee jokaista layeria joka lukee kuuntelijansa piirtohetkellä. LISÄKSI: pätkän nimilappu on sen ainoa LUETTAVA kohde kartalla ∴ lappu ! olla klikattava sisääntulo täsmälleen silloin kun pätkä itse on (sama `interactive`-lippu, ⊥ omaa totuutta) — nimi joka näyttää luettavalta mutta ⊥ reagoi jättää käyttäjälle vain ~8px viivan lapun vierestä. (B134→T347) |
| V252 | Kartan statuskieli ! olla luettavissa TAUSTAKARTAN PÄÄLTÄ & akromaattisesti — invariantti ⊥ täyty sillä että kanava on olemassa koodissa. Konkreettisesti: `LINE_STATE_STYLE`n kolme tilaa (`valmis`/`kesken`/`ei_alkanut`, `src/map/segment-overlay.ts`) ! erottua toisistaan ILMAN väriä (ehjä / karkea katko / haalea harva katko) & katkokuvion viivanpätkä ! olla ≥ aukon kokoluokkaa — `'1 9'` (1px viiva, 9px aukko) hajoaa pistesarjaksi joka katoaa MML-maastokartan tekstuuriin. Sama vaatimus kuin V216 reittiväreille: värisokeus + aurinko + mobiilin autokirkkaus = sama testi. LISÄKSI positiivinen tila ! saada positiivinen signaali: "valmis" ⊥ saa olla pääteltävissä VAIN jonkin puuttumisesta (katkon puuttuminen) — sillä ⊥ ole omaa merkkiä jota etsiä. Playwright ! tarkistaa dashArrayn ARVON, ⊥ pelkkää olemassaoloa (`toBeTruthy` katkoviivalle = testi joka ⊥ voi failata). LISÄKSI luettavuus ⊥ ole vain KUVIO- vaan myös ALFAKYSYMYS: viivan **efektiivinen** kontrasti (alfa mukaan laskettuna) vaaleaa taustakarttaa (`#F2F0EA`) vasten ! olla ≥3:1 (WCAG non-text). Pelkkä dashArray-korjaus jätti tämän vuodon auki — `ei_alkanut` @ `opacity .4` oli 1.79:1 eli näkymätön kirkkaassa vaikka kuvio oli oikea (UX-audit 2026-07-27 → T348-toteutuma, alfa 0.7). (B135→T348) |
| V253 | **Karttapinta on oma väriavaruutensa** — tile/overlay/karttalappu ⊥ saa lukea teemariippuvaisia CHROME-tokeneita (`--confirm`, `--accent`, `--surface-*`, `--text-*`). Perustelu: pohjakartta ⊥ vaihdu teeman mukana ∴ sen päällä olevan grafiikan kontrastisuhteet on kalibroitu KARTTAA vasten, ⊥ sovelluksen chromea vasten — & Leaflet-vektorit (polyline `color`, `L.divIcon`-HTML) saavat värinsä JS:stä joka ⊥ näe CSS-muuttujaa ∴ jokainen chrome-token karttapinnalla luo parin joka hajoaa teemanvaihdossa (B136). Sopimus: karttapinnan värit tulevat tokeneista jotka määritellään VAIN `:root`issa & joita `[data-theme]` ⊥ ylikirjoita (esim. `--segment-done`), ja JS-vakio on niiden kommentoitu peili. Sama perhe kuin B106 (lapun kiinteä navy-tausta) & V132 (`SEGMENT_COLORS`/`ROUTE_DEFS` kiinteät hexit). Testattavuus: teemariippumattomuus ! olla mitattavissa — E2E ajaa saman näkymän molemmilla teemoilla & vertaa karttapinnan värit identtisiksi (⊥ pelkkä silmämääräinen tarkistus vaaleassa, jossa vika on näkymätön). (B136→T349) |
| V254 | Talkoolaisen "pätkä valmiiksi" ! olla saatavilla SIINÄ näkymässä joka kertoo pätkän valmiudesta: hero:n done-rivi (`segment-hero.ts:80-88`, "✓ Kaikki asetettu 🎉") ! sisältää valmis-toggle (`SegmentViewActions.onComplete`, jo olemassa & kytketty `markers-wiring.ts`:ssa). ⊥ riitä että toiminto on olemassa jossain (Kaikki merkit -tabin pohja TAI yläpalkin ⋯-valikko) — valmiussignaali & sen kuittaus erillään = umpikuja metsässä (max 2 nappia). Valmis-merkinnällä ! olla YKSI sisääntulo per rooli, ⊥ rinnakkaisia duplikaatteja omine label-synkkoineen (kaksi synkkaa = B133-tyylinen elinkaaririski: toinen jää jälkeen). (B140→T351) |
| V255 | Pätkän valmis-toggle ! olla myös järjestäjällä. `#tk-menu-actions` on `data-role-hide="järjestäjä"` (`index.html`) & `SegmentView` (∴ sen `onComplete`) kytketään VAIN talkoolaispolussa (`markers-wiring.ts`, vaatii `talkoolainenCode` → `getSegmentForCode`) ∴ järjestäjällä ⊥ ole yhtään reittiä kuitata pätkää valmiiksi eikä perua virhekuittausta. Järjestäjän sisääntulo = `SegmentDetailsModal`; se ! käyttää SAMAA `updateSegment` + `updateSegmentRemote({completed})` + `renderSegmentOverlay()` -polkua kuin talkoolainen (⊥ oma kirjoituspolku, ⊥ oma optimistinen tila — kaksi polkua ajautuu erilleen). HUOM: `completed` ⊥ vaikuta kartan viivaväriin — `segmentLineState` johtaa `valmis`-tilan MERKKIEN statuksista (`segments.ts:254-262`); lippu on siitä ERILLINEN eksplisiittinen signaali (B142/V256). (B141→T352) |
| V256 | `Segment.completed` ! olla LUETTAVISSA järjestäjän tilannekuvasta, ⊥ pelkkä napin label (B142: kirjoitus ilman lukijaa). **Kartta:** `segmentLineState(progress, completed)` — kuittaus VOITTAA merkkilaskurin (kuitattu + 0/5 asetettu = `valmis`), koska talkoolainen on paikan päällä & laskuri on vain johdettu arvio suunnitelmasta. **Lista:** kuittaus omana merkintänään (`.segment-kuitattu`) laskurin VIERESSÄ, ⊥ sen tilalla — ne voivat olla eri mieltä molempiin suuntiin & ristiriita on TIETOA järjestäjälle ("kuitattu vaikka merkkejä kesken" = soita talkoolaiselle), ⊥ virhe jota pitäisi siivota näkyvistä. `segmentLineState`:n 2. parametri on valinnainen (oletus `false`) ∴ vanhat kutsupaikat & testit säilyvät ennallaan. (B142→T353) |
| V257 | Tabikuvio on YKSI toteutus koko sovelluksessa. `SegmentKotiTabs` (`src/ui/segment-koti-tabs.ts`) palvelee sekä talkoolaisen kotinäkymää että järjestäjän pätkämodaalia ∴ toinen tabipalkki, oma `is-active`-kirjanpito | oma `hidden`-togglaus on rikkomus: kaksi toteutusta ajautuvat erilleen näppäimistökäytössä, `aria-selected`issä & scroll-nollauksessa (T315/V226 on jo kerran korjattu — sitä ⊥ korjata kahdesti). Yleistys tehdään PARAMETRILLA (scroller-valitsin), ⊥ haarautuvalla roolilogiikalla komponentin sisällä. LISÄKSI: tabien taakse piiloutuva sisältö rikkoo jokaisen valitsimeen luottavan testin joka ⊥ aktivoi tabia ensin — se on odotettu seuraus ⊥ regressio, & testin ! klikata tabi, ⊥ kaivaa piilotettua panelia `hidden`in ohi. (T354,T264,T315) |
| V258 | **`SegmentTrack` on pätkän geometrian TOTUUS**: järjestetty pistejono `{lat,lon,d}` (`d` = matka jäljen alusta metreinä), leikattuna alla olevan reitin pisteistä. Pätkän pituus = jäljen viimeinen `d`, ⊥ `endDist−startDist`. `startDist`/`endDist`/`primaryRouteId` SÄILYVÄT kenttinä mutta ovat tästä lähtien JOHDETTUJA yhteensopivuusarvoja (serverin omistajuustarkistus, snapshotit, kartan piirto) — ⊥ enää totuus. Perustelu: km→paikka ⊥ ole injektio (reitti käy samassa kohdassa kahdesti, 3 SMTB-reittiä kulkee ≤100 m toisistaan) ∴ (reitti, kmAlku, kmLoppu) ⊥ yksilöi maastoa & jokainen sen varaan rakennettu vertailu on arvaus (B143,B144 — & sama juuri kuin B114/B115/B117/B120/B121). Jälki on aina reitin YHTENÄINEN SIIVU ⊥ vapaa polku: `distanceFromStart` on kumulatiivinen ∴ monotoninen indeksissä ∴ `(primaryRouteId, startIdx, endIdx)` määrää jäljen täysin & siivu km:llä = siivu indeksillä. Välipisteet (T362) ⊥ lisää ilmaisuvoimaa TALLENNETTUUN geometriaan — ne ratkaisevat vain MINKÄ indeksin käyttäjä tarkoitti (B144). ∴ ⊥ yleistä jälkeä vapaaksi poluksi joka ⊥ enää vastaa mitään reittiä: pisteiden tallennuksen syy on serveri (⊥ lataa GPX:iä), ⊥ ilmaisuvoima. Jälki on ITSEKANTAVA: se on kannassa pisteinä ∴ GPX:n vaihtuminen ⊥ siirrä olemassa olevaa pätkää ITSESTÄÄN — eksplisiittinen rajamuokkaus (T363) johtaa jäljen uudelleen NYKYISESTÄ GPX:stä ∴ se on käyttäjän teko ⊥ hiljainen siirto (ck:review N-9) (VISION §Järjestäjä: "GPX voi päivittyä — merkit säilyvät") & serveri (joka ⊥ lataa GPX:iä) voi laskea siitä samaa matematiikkaa kuin selain. YKSI johtofunktio palvelee kolmea kutsujaa — legacy-backfill (T361), rajojen muokkaus kentällä (T78/V43→T363), klik-klik-luonti (T362) — ⊥ kolmea kopiota: kolme johtotapaa ajautuisi erilleen & pätkän maasto riippuisi siitä kuka sen viimeksi tallensi. (B143,B144→T358) |
| V259 | **Merkin jäsenyys pätkässä ratkeaa KOHTISUORALLA etäisyydellä pätkän JÄLKEEN (⊥ km-lukemalla) & LÄHIN JÄLKI VOITTAA AINA — kynnystä ⊥ OLE** (PM-päätös 2026-07-27, ck:review B-1). Ehdokasjoukko = saman phasen pätkät joiden `routeIds` leikkaa merkin `routeIds`ia (V25:n nykyinen 100 m korridorijäsenyys säilyy MUUTTUMATTOMANA — se on scope, ⊥ jäsenyyssääntö); ehdokkaista lähin jälki OMISTAA merkin, tasapeli ratkeaa pätkän id:llä (determinismi ⊥ lista-indeksi, V96-kuvio). Jäsenyys on siis EKSKLUSIIVINEN rakenteeltaan ⊥ tarkistuksen varassa. **AMEND (B148):** kynnyksettömyys tekee KILPAILIJAJOUKOSTA pakollisen syötteen — yksin arvioitu pätkä voittaisi jokaisen merkin reitillään (mitattu 122 vs 12). ∴ tyhjä kilpailijajoukko tarkoittaa "kilpailijoita ⊥ TIEDETÄ" & tulos putoaa legacy-km-sääntöön (V260-haara), ⊥ "kilpailijoita ⊥ ole". Jokainen jäsenyyttä lukeva pinta (lukemat, kartan viivatyyli, korostusjoukko) ! antaa joukon. **Miksi ⊥ kynnystä:** kynnys tuottaa orpoja & merkki ilman pätkää on NÄKYMÄTÖN talkoolaiselle (`markers-wiring.ts:131` rajaa näkymän jäsenyyteen) ∴ orpous ! olla rakenteellisesti mahdotonta ⊥ vain epätodennäköistä — mitattu: 40 m kynnyksellä 2 nykyistä jäsentä putoaisi JOKAISESTA pätkästä ("Alueella pyöräkilpailu" 83 m, "55 km 110 km only" 46 m, molemmat `suunniteltu`). Mitattu seuraus tuotantodatassa (194 merkkiä / 10 pätkää): omistaja löytyy 133:lle (nyt 117), nykyjäsenistä 0 menettää omistajansa, duplikaatit 10→0, merkki joka ⊥ ole minkään pätkän reitillä pysyy orpona ennallaan (61 kpl). **16 merkkiä jotka ovat reitillä mutta pätkien km-AUKOSSA saavat nyt lähimmän pätkän** — tietoinen valinta ⊥ sivuvaikutus (pahin etäisyys 989 m, "Alueella pyöräkilpailu" → Pätkä 1, sama omistaja kuin tänään); järjestäjän ulospääsy on `excludedMarkerIds` ⊥ kynnyksen viritys. Perustelu koko säännölle: yksi fyysinen kyltti = yksi tekijä; kaksi omistajaa on turhaa ajoa metsässä & kuittausten kilpajuoksu (B143). Eksplisiittinen tahto VOITTAA geometrian: `linkedMarkerIds` lisää & uusi `excludedMarkerIds` poistaa, molemmat ohittavat etäisyyslaskun ∴ järjestäjällä on aina viimeinen sana ⊥ tarvitse taistella heuristiikkaa vastaan. Merkin km pätkässä = matka pätkän OMAA jälkeä pitkin (0…pituus) ⊥ reitin kokonaiskilometri — talkoolainen ⊥ tarvitse tietää mistä reitin alku on; tällä lauseella ! olla TOTEUTTAJA (`segment-order.ts`:n `segmentKm`/`displayKm` saavat jälkihaaran, T359) — lupaus ilman toteuttajaa on B142:n peilikuva (siellä kirjoitus ilman lukijaa). Reititön tehtävä (V139) ENNALLAAN: ⊥ jälkeä, ⊥ geometriaa, vain `linkedMarkerIds`/`markerTypeFilter`. (B143→T359) |
| V260 | **Migraatio on asteittainen & ⊥ kaadu puuttuvaan jälkeen**: `track` puuttuu → käytös on TÄSMÄLLEEN NYKYINEN (km-väli + V214-ehdokkaat), sama kuvio kuin V139 (reitittömät kentät) / V211 (`primaryRouteId`) / V212 (`distanceByRoute`). Legacy-pätkä saa jäljen johtamalla KERRAN nykyisistä `startDist`/`endDist`-arvoista — sama siivu jonka kartta jo piirtää (`segment-overlay.ts:290 sliceRoutePoints`) ∴ maasto ⊥ muutu, vain esitys; jos rajat olivat maastossa väärässä paikassa, ne pysyvät siellä kunnes pätkä vedetään uudelleen (T362) — korjattavissa käsin per pätkä, ⊥ estä migraatiota. Serveri (`marker-audit.ts:100 markerInOwnSegment`, `segmentCodeForMarker`) lukee `track`in kun se on kannassa, muuten vanhan km-haaran ∴ kerrokset ⊥ voi olla eri mieltä jäsenyydestä missään välitilassa (B100/V213-oppi: 403-portti & frontin näkymä samasta unionista). Slugit (V209), `assignedCode`, jaetut linkit & pätkien nimet ovat KOSKEMATTOMIA — tämä muutos ⊥ kosketa pätkän identiteettiin. **AMEND (B145):** backfillin PUSH menee OUTBOXIN OHI (`pushSegmentTrack`, hiljainen fetch) & vain järjestäjältä. Johdettu arvo ⊥ tarvitse outboxin durabiliteettia — se lasketaan uudelleen joka latauksella ∴ epäonnistuminen korjautuu itsestään; outboxin 401-polku sen sijaan nostaa reauth-overlayn. Push on lisäksi VAIN järjestäjän oikeus — pätkän geometria on järjestäjän dataa (V13/V93) ∴ talkoolaisen sivulataus johtaa jäljen MUISTIIN mutta ⊥ kirjoita sitä. Yleisemmin: **sivulataus ⊥ saa olla kirjoitus.** Taustakirjoitus jota käyttäjä ⊥ pyytänyt voi 401:llä laukaista `promptReauth`-overlayn (`main.ts:180`) & lukita koko näkymän — talkoolaiselle metsässä se on V18-luokan vika, ⊥ haitta. Tämä on turvallista koska V260:n välitila on jo laillinen: jäljetön pätkä toimii km-haarassa ∴ push saa odottaa siihen asti kun joku jolla on oikeus avaa näkymän. (B143,B144,B145→T360,T361) **AMEND 2 (B146):** välitila on laillinen VAIN jos se ⊥ tuota 403:a — kerrosten erimielisyys saa siirtää omistajuutta pätkältä toiselle (haitta), mutta ⊥ pudottaa merkkiä NOLLALLE omistajalle (kenttätyön este). Mitattu: 5/26 merkkiä sai serveriltä "ei kenenkään" koska km-haara putosi eri reitiltä mitattuun skalaariin. ∴ V260-ikkunan ajan serverin km-haaran ! saada oikea akseli: client lähettää `distance_by_route`n kirjoitusten mukana (B146). |
| V261 | **Piste→jälki-etäisyys lasketaan KOHTISUORANA projektiona janalle**, ⊥ etäisyytenä lähimpään kärkipisteeseen. Perustelu on MITATTU ⊥ esteettinen (ck:review H-4, tuotantodata + GPX): suurin ero kärkipiste−kohtisuora on **41.9 m**, & pisteväli ⊥ ole tasainen — smtb-55 mediaani 23.6 m mutta p95 **73.8 m**, sgf-125 mediaani 41.1 m / p95 207.6 m / max **767 m**. ∴ kärkipiste-etäisyys erehtyy kymmeniä metrejä juuri siellä missä GPX on harva (pitkä suora, huono taltiointi) & V259:n lähin-voittaa-vertailu valitsisi VÄÄRÄN pätkän kahden rinnakkaisen jäljen välillä. Sama virhe on jo nykykoodissa: `nearestPointIndex`/`nearestPointCandidates` (`src/logic/bearing.ts`) mittaavat kärkipisteisiin — ne JÄÄVÄT reitti-/km-käyttöön, jälki saa oman funktionsa ⊥ niitä muuteta. Funktio on käytössä MOLEMMILLA kerroksilla & serverin kopio on TARKOITUKSELLINEN duplikaatti (server ⊥ importtaa `src/`, sama precedent kuin `server/db.ts:305` slugify) ∴ sillä ! olla oma testi joka vertaa SAMAAN odotusarvoon kuin frontin testi — kaksi toteutusta ilman yhteistä odotusarvoa on B100-suvun seuraava jäsen. (B143,B144→T358,T360) |
| V262 | Kuvaputki ! olla rajattu SERVERISSÄ, ⊥ clientissä. Client pienentää kuvan ennen lähetystä (`src/ui/image-downscale.ts`: max 1600px, JPEG q0.8) mutta se on NOPEUS & KAISTA (talkoolainen metsässä, heikko yhteys, akku) — ⊥ suoja: API:a voi kutsua curlilla ohi clientin ∴ jokainen raja tarkistetaan `server/routes/comments.ts`:ssä. Kolme rajaa (käyttäjäpäätös 2026-07-25): koko ≤2 MB (413), mime ∈ {jpeg,png,webp} (400), taajuus ≤2 kuvaa/60 s (429). Taajuusavain ! olla KESTÄVIN saatavilla oleva identiteetti — `user_id > talkoolainen_code > IP > sessio`, ⊥ pelkkä sessio: uusi kirjautuminen antaa uuden session ∴ kiintiö nollautuisi kolmella klikkauksella (löytyi T338-testissä, `authHeaders` luo session per kutsu ⇒ raja ⊥ lauennut). Yleissalasana-sessiolla `user_id` & `talkoolainen_code` ovat NULL (auth.ts:105, B124) ∴ IP kantaa. Pienennyksen epäonnistuminen palauttaa ALKUPERÄISEN tiedoston — hiljainen pudotus hävittäisi kentällä otetun todisteen; serverin koko-portti hoitaa loput. Virheilmoitus ! kertoa MIKÄ raja tuli vastaan: "yritä uudelleen" on väärä ohje kun kiintiö on täynnä (silloin oikea ohje on odottaa). (T338) |
| V263 | Huomion KUITTAUS on eri asia kuin merkin status (V9/V245): `Comment.resolvedAt`/`resolvedBy` puuttuu ⇒ työ on AVOIN. Kuittaus on JÄRJESTÄJÄN koordinointipäätös (`canResolve`, backend-gate) — talkoolainen ILMOITTAA mitä maastossa on, järjestäjä päättää onko asia hoidettu; jos kentällä oleva voisi kuitata oman havaintonsa, lista lakkaisi olemasta työjono & muuttuisi lokiksi. Kuitattu huomio HIMMENEE kartalla (`opacity .45` + `grayscale(1)`) ⊥ katoa — sama sääntö kuin V243: kartta joka jättää renderöimättä valehtelee siitä mitä alueella on. Kuitattu tila kerrotaan SANOIN (kuka + milloin) ⊥ pelkällä värillä (V197-linja) — väri ⊥ kanna "kuka" -tietoa eikä selviä auringosta. `CommentPanel`in laskuri kertoo AVOIMET ⊥ kaikkia: järjestäjä kysyy "montako työtä on tekemättä", ⊥ "montako havaintoa on koskaan kirjattu" — kokonaismäärä kasvaa ikuisesti ∴ se ⊥ ole tilannekuva. KATEGORIA valitaan YHDELLÄ painalluksella (rivi nappeja, ⊥ dropdown): hanskat kädessä avautuva valikko + osuma pieneen riviin on kaksi virhemahdollisuutta siinä missä nappi on yksi. (T364) |
| V264 | Huomion LUONTI on LUONNOS kunnes käyttäjä painaa `Lähetä` — kartalle pudotettu pinni on ehdotus sijainnista ⊥ tallennettu tosiasia. Kolme ehtoa: (a) pinni on RAAHATTAVA luonnosvaiheessa (kartan keskipiste ⊥ ole sijainti jonka käyttäjä valitsi, se on sijainti jonka kartta arvasi — havainto koskee KOHTAA maastossa, metrit merkitsevät); (b) kuvat kertyvät PAIKALLISESTI (objectURL-esikatselu + ✕ per kuva) ⊥ lähde palvelimelle ennen `Lähetä`ä ∴ peruttu luonnos ⊥ jätä orpoja BLOBeja kantaan eikä kuluta kuvakiintiötä (V262: 2 kuvaa/60 s — kolme peruttua luonnosta olisi syönyt sen); (c) `Lähetä` tekee KOKO tallennuksen (POST huomio → kuvat perässä) & sulkee modaalin ∴ käyttäjälle on YKSI hetki jolloin työ on valmis, ⊥ kolmea erillistä tallennusta joista jokainen voi jäädä kesken. Esc/peruutus luonnosvaiheessa ⊥ jätä jälkeäkään: pinni katoaa kartalta. (B150,B152→T366) |
| V265 | Huomiolla on OMISTAJA & omistaja saa korjata sen: `comments.created_by` = `user_id ?? talkoolainen_code` kirjoitushetkellä. Siirto (`PUT /api/comments/:id` lat/lon) & poisto ovat omistajan + järjestäjän oikeuksia, ⊥ pelkän järjestäjän (⊥ myöskään kenen tahansa: huomio on todiste jota toinen tarvitsee). Perustelu: sijaintivirhe syntyy JUURI silloin kun huomio jätetään kiireessä maastossa & korjaaja on sama ihminen joka seisoo paikalla — jos hän ⊥ voi siirtää, väärä sijainti jää kantaan & järjestäjä ajaa väärään kohtaan. Gate ! olla SERVERISSÄ (`created_by`-vertailu sessiosta) & sen TULOS kerrotaan clientille rivikohtaisena `canEdit`-lippuna — client ⊥ toisinna sääntöä (kaksi toteutusta ajautuu erilleen & UI lupaisi mitä API kieltää). Avainketju `user:<id>` > `code:<pätkäkoodi>` > `session:<id>`: jaetulla koodilla "tekijä" on koko pätkän porukka (TIETOINEN valinta — sama pätkä = sama työ), & yleissalasana-sessiolla kumpaakaan ⊥ ole (B124) ∴ sessio kantaa — ilman sitä hub-käyttäjä ⊥ omistaisi omaa havaintoaan lainkaan. Sessio katkeaa uloskirjautumisesta ∴ oikeus katoaa silloin & rivi jää järjestäjälle: karkeampi ankkuri antaa VÄHEMMÄN oikeuksia ⊥ enempää. `created_by` puuttuu (ennen tätä kirjattu huomio) ⇒ vain järjestäjä ∴ vanha data ⊥ muutu kenenkään omaisuudeksi. (B151→T367) |
| V266 | Fokus-tila (V243) himmentää sen mikä on KONTEKSTIA, ⊥ sitä mikä on käyttäjän oma työ. Talkoolaisen näkymässä fokus on automaatti (T335) ∴ jos huomiot himmennetään binäärisesti "fokus päällä ⇒ ∀ huomio harmaaksi", talkoolaisen OMA tuore havainto syntyy suoraan harmaana & luetaan poissuljetuksi — se katoaa juuri siltä jolle se kirjoitettiin. Sääntö: huomio himmenee VAIN järjestäjän eksplisiittisestä pätkäkorostuksesta (lukemisen apu, hän vertailee pätkiä), ⊥ talkoolaisen automaattifokuksesta. Kuittaus-himmennys (V263) on eri kanava & pysyy: se kertoo tilan (tehty), ⊥ kontekstin. Yleisoppi: automaattinen tila ⊥ saa periä eksplisiittisen tilan sivuvaikutuksia — sama kytkin, kaksi eri merkitystä = kaksi eri sääntöä. (B154→T370) |
| V267 | Left-panelin section-header renderöidään VAIN `src/ui/section-header.ts`:n `createSectionHeader`illa — ⊥ paneelin oma `createElement`/`innerHTML`-kopio. Pattern oli kirjattu (V61) mutta ⊥ kapseloitu ∴ rapautui kolmeksi eri toteutukseksi (`sign-library-panel.ts:76-86` innerHTML, `segment-panel.ts:290-307` createElement, `area-panel.ts:49-71` createElement+inline) & neljäs osio (Huomiot, `comment-panel.ts:33-41`) jäi kokonaan ilman. Kapseloimaton pattern maksaa kahdesti: `area-panel.ts` keksi oman `.section-toggle-icon`-luokan jota ⊥ ole `style.css`:ssä ⇒ tyylit oli pakko upottaa `style.cssText`-inlineksi (§C-rikkomus — teemanvaihto ⊥ tavoita elementin `style`-attribuuttia) & `role="button"`-header jäi ilman näppäimistökuuntelijaa kolmessa paikassa kolmesti. Sopimus: uusi osio saa headerinsa apurilta tai sitä ⊥ ole. Luokat (`.left-panel-section-header`, `.section-header-toggle`, `.section-header-name`) elävät `style.css`:ssä, ⊥ koskaan elementin `style`-attribuutissa. (T371,T372) |
| V268 | Kosketuskohde-vahti (`e2e/critical-paths.spec.ts` "Touch targets — T45") valitsee kohteet ROOLIN ⊥ TAGIN perusteella: `button, [role="button"]` (⊥ pelkkä `button`). Perustelu: vahti assertoi `violations.toHaveLength(0)` ∴ se LUPAA että §A:n 44×44px pitää — mutta `page.locator('button')` ⊥ näe `div[role="button"]`ia ∴ neljä left-panelin section-headeria (~28px) eli KAIKKI sivupalkin haitarikontrollit olivat vahdin katveessa koko sen elinkaaren (T106→T371). Vahti joka mittaa tagia mittaa toteutusta; §A koskee sitä mihin käyttäjä osuu sormella ∴ mittari ! seurata roolia. Yleisoppi: vihreä vahti jonka valitsin ⊥ kata koko sopimusta on pahempi kuin puuttuva vahti — se tuottaa perusteettoman luottamuksen. Jos valitsimen laajennus paljastaa lisää rikkomuksia, jokainen saa oman §B-rivinsä & korjauksensa — valitsinta ⊥ kapeuteta takaisin vihreän saamiseksi. Interaktiivinen elementti ! olla `<button>` kun se käyttäytyy nappina (natiivi fokus, Enter/Space, vahdin kattavuus); `role="button"` on poikkeus jolle ! olla syy. (T373) |
| V269 | Reittinäkyvyys = YKSI kytkin joka koskee ∀ siihen reittiin ankkuroitua kerrosta: reitti-polyline + merkit + pätkäviivat (casing+erotin+sisus, T336) + pätkien nimilaput. Jäsenyys `segmentPrimaryRouteId`illa (V211); reititön tehtävä (V139) ⊥ katoa reittisuodatuksesta — sillä ⊥ ole reittiä jonka mukana kadota. Predikaatti asuu `src/logic/`ssa (`segmentVisibleOnRoutes`), ⊥ map-kerroksessa. Kerros joka ⊥ kuuntele `visibleRouteIds`ia jää leijumaan ilman ankkuriaan ∴ kartta väittää pätkän olevan siellä missä ⊥ ole reittiä. (T374) |
| V270 | Fokus-HIMMENNYS & interaktioLUKKO = ERI kanavat. Himmennys = "tämä ⊥ ole nyt huomiosi kohde"; lukko (`interactive:false`, `pointer-events:none`) = "et omista tätä". Talkoolainen saa MOLEMMAT (V142/V93/V150 — oma pätkä on ainoa jota hän saa muokata), järjestäjä VAIN himmennyksen: himmennetty merkki & pätkäviiva pysyvät klikattavina, hän omistaa kaiken. Yhteen booliin kiedottuna toinen rooli jää aina ilman — juuri se solmu esti järjestäjän pätkäviivojen himmennyksen (B158). `contextSegmentStyle` ! ottaa `locked` erillisenä parametrina opacity-portaasta. (T375) |
| V271 | Kartan näkyvyys-/himmennyspäätös ratkeaa TASAN YHDESSÄ moduulissa `src/logic/map-filter.ts`. Paluu KOLMIARVOINEN `full\|dim\|hidden` ⊥ boolean: "piilota" ⊥ ole himmennyksen ääriarvo vaan kolmas tila, & tyyppi pakottaa kutsupaikan käsittelemään sen. `src/map/` SOVELTAA palautetun tilan, ⊥ päätä sitä. `map-filter` KOOSTAA olemassa olevat predikaatit (`focusState` V243, `segmentVisibleOnRoutes` V269, `segmentLineState`, `markersForSegment` V259) — oma jäsenyyssääntö tänne = B114–B129-perheen 10. esiintymä. Sääntö joka elää kahdessa paikassa on kaksi eri mieltä olevaa karttaa. (T376) |
| V272 | Suodatettu kartta ! kertoa olevansa suodatettu SAMASSA näkymässä: suodatinbar aktiivilaskurilla + banneri kun tila ≠ oletus + "Nollaa" yhdellä klikillä. Tila persistoituu (`localStorage`, V5-kuvio) ∴ banneri on PAKOLLINEN — eilinen suodatin jonka syytä ⊥ näy luetaan kadonneena datana (B131-luokka: käyttäjä ⊥ tiedä onko vika datassa vai näkymässä). Monivalinta ⊥ saa tyhjentyä nollaan (V6-kuvio reiteiltä): viimeistä valintaa ⊥ voi poistaa ∴ tyhjä kartta ⊥ ole saavutettavissa vahingossa. Vioittunut/vanha persistoitu JSON → oletusarvot, ⊥ kaadu. (T377) |
| V274 | Kartan kontrolli ! olla kokonaan VIEWPORTIN SISÄLLÄ jokaisella tuetulla leveydellä (≥320px) — `overflow-x:auto` ⊥ ole affordanssi: vaakaskrolli ilman näkyvää vihjettä = kontrolli jota ⊥ ole olemassa (B160). Kun kontrolleja on enemmän kuin riviin mahtuu, kapea leveys saa OMAN valikkonsa (yksi trigger → sheet jossa kaikki osiot), ⊥ typistettyä riviä. E2E-vahti ! mitata `boundingBox().x + width ≤ viewport` — pelkkä `click()` menee läpi vaikka kohde on ruudun ulkopuolella (Playwright skrollaa) ∴ vihreä klikkitesti ⊥ todista tavoitettavuutta. **AMEND 2026-07-28 (B161):** sama koskee PÄÄLLIMMÄISYYTTÄ — kelluva paneeli ! olla `elementFromPoint`illa mitattuna ylin osumapiste keskellään. `z-index` ⊥ riitä: esi-isän `overflow:auto|hidden` LEIKKAA absoluuttisen lapsen riippumatta kerroksesta, & `toBeVisible`/`boundingBox` ovat silti vihreitä (B161: paneeli piirtyi kartan alle vaikka z-index oli 1000 vs 400). Kontrollirivi joka ⊥ mahdu ⊥ saa saada skrollia ⊥ leikkausta — se saa oman valikon (V274 yllä). (T377,B160,B161) |
| V273 | Aukko (reitin osuus jolla ⊥ ole pätkää) luetaan POISSAOLONA pätkän väristä — paljas reittiviiva ON aukon merkki. ⊥ omaa aukkovisualisointia (väri, kuvio, oma kerros): kolmas visuaalinen kanava reitin & pätkän päällä on kohinaa, jonka `#94a3b8 @ 0.3` jo todisti näkymättömäksi (B159). Aukon havaittavuus ratkeaa PÄTKÄN kontrastilla (§K SegmentOverlay alfa-alaraja ≥3:1), ⊥ aukon omalla tyylillä ∴ yksi säädin ⊥ kaksi. Seuraus: `computeGapRanges` & `GAP_COLOR` poistuvat, ⊥ siirry `src/logic/`-kerrokseen. (T378) |
| V275 | Karttakohteen lisätieto on YKSISUUNTAINEN OHJE ⊥ keskustelu. Merkillä TASAN yksi vapaa tekstikenttä (`markers.locationNote`), pätkällä TASAN yksi (`segments.description`) — kirjoittaja ⊥ saa joutua valitsemaan kahden samaa tarkoittavan kentän välillä. Tuotanto todisti: 2026-07-26 PATKA-7 kirjoitti saman vaarahavainnon MOLEMPIIN (`comments.text` "Kelo vaarassa kaatua polulle" + saman merkin `location_note` "Tässä on kelo, joka oli osittain tippunut polulle…") ∴ kaksi kenttää ⊥ tuota kahta tietoa vaan yhden tiedon kahdesti & epävarmuuden siitä kumpi luetaan. ⊥ kommenttilankaa, ⊥ vastauksia, ⊥ author-per-viesti -listaa, ⊥ resolved-tilaa kartan kohteissa. Keskustelu käydään WhatsAppissa: sovelluksen sisäinen rinnakkaiskanava on aina huonompi kuin se johon porukka jo vastaa (koko kauden käyttö: 1 kommentti / 194 merkkiä, 0 pätkä-, 0 pistehuomiota, 0 kuvaa — vrt. `location_note` 6/194). Uusi "kommentoi"-UI ⊥ saa palata ilman että tämä invariantti kumotaan ensin. SUOJAA: `locationNote` & `segments.description` SÄILYVÄT & renderöityvät talkoolaiselle (`segment-view.ts:319`, `segment-hero.ts:161`, `marker-list.ts:199`) — invariantti kieltää KETJUN, ⊥ ohjetta. (T380,T381,T382) |
| V276 | Inventaarion & kartan yhdistäminen tapahtuu AINA `templates.id`-liitosavaimella (`inventory_items.template_id` ↔ `markers.template_id`) — molemmat kentät ovat jo olemassa (T243/V165, T215/V143), vain täyttämättä. Ajonaikainen nimivertailu (`item.name === marker.label`) ⊥ ole liitos vaan ehdotusheuristiikka: se saa elää TASAN `src/logic/template-match.ts`:ssä & tuottaa vain ehdotuksia ihmisen kuitattavaksi. Laskurit (kartalla/varastossa) lukevat VAIN template_id:tä. Perustelu: nimi on muuttuva näyttöarvo (V165 elävä label — templaten uudelleennimeäminen katkaisisi nimipohjaisen liitoksen hiljaa), id on pysyvä. Tuotantotila 2026-07-29: inventory 23/124 linkitetty, markers 138/205. (T383,T384,T386,T387) |
| V277 | Kone ⊥ kirjoita inventaarion template_id-linkitystä automaattisesti. `rankTemplates` tuottaa ehdotuksia; kirjoitus vaatii järjestäjän eksplisiittisen kuittauksen PER RIVI — ⊥ "linkitä kaikki varmat" -massanappia, ⊥ esivalintaa. AINOA poikkeus: T383:n markers-backfill, jonka ehto on SANATARKKA label-täsmäys (⊥ fuzzy, ⊥ normalisointi) ∴ deterministinen, ⊥ arvaus. Perustelu: väärä linkki on pahempi kuin puuttuva, koska puuttuva näkyy yhdistämislistalla & väärä ⊥ näy missään. Kirjanpito ⊥ ole vedenpitävä (grill 2026-07-21) & 2026-07-25 incidentti (14 merkkiä siirtyi 1–7 km, havaittu vasta kentällä) osoitti hiljaisen datamuutoksen hinnan. (T384,T386) |
| V278 | "irtokyltti"/"lisäkilpi"-suffiksi & muut kiinnitystapaan viittaavat nimivariantit ovat SAMA nimike kuin kantamuoto — normalisointi poistaa ne ennen vertailua. Jatkaa V186:ta: kiinnitystapa ⊥ ole strukturoitu kenttä missään, ⊥ palaa keppi/irto-erotteluun. Tuotantodata: ~25 riviä muotoa "X irtokyltti" elää rinnakkain "X":n kanssa (esim. "Ennakko oikea, 90 irtokyltti" qty 3 vs "Ennakko, oikea 90" qty 3 → `ennakko-oikea-90`) ∴ ilman tätä sääntöä sama kyltti näkyy kahtena nimikkeenä eikä kumpikaan määrä ole oikein. (T384) |
| V279 | Tarvikkeeksi merkitty inventaariorivi (`not_sign=1`) ⊥ saa koskaan template_id:tä; ristiriita = 400 backendissä. Tarvike ⊥ ole "vielä linkittämätön merkki" vaan LOPULLINEN tila (Taittopöytä, Fenix mainoslippu, Syöte MTB roll-up, Shuttle-bus aikataulu) — ilman tätä erottelua yhdistämislista ⊥ tyhjene koskaan & järjestäjä käy samat rivit läpi joka kerta ∴ työkalu ⊥ kerro milloin on valmis. (T385,T386) |
| V280 | Luonnin ankkuriklikki ! tuottaa VÄLITTÖMÄN merkin KARTALLE — jokaisesta ankkurista oma, ⊥ vain ensimmäisestä, & kertyvä jälki esikatseluviivana. Modaalin ankkurilista ⊥ ole palaute: järjestäjä katsoo klikatessaan karttaa ⊥ modaalia ∴ lista joka kasvaa näkymättömissä on sama kuin ei palautetta (B162 — sama juuri kuin B6 2026-06-10 "ensimmäinen klikattu piste ⊥ näy" & B147 "interaktiivinen palautemarkeri söi klikin"; kolmas kerta ∴ sääntö ⊥ ohje). Luonnin virhe ("⊥ osumaa reitillä eteenpäin") ! näkyä samassa katseessa kuin klikki — modaalin alalaidan `setError` on ankkurilistan (`max-height:132px`) alla ∴ mobiilissa taitteen takana & järjestäjä lukee vaikenemisen rikkinäisyytenä (B163). Palautemarkeri pysyy `interactive:false` (B147). E2E-vahti ! assertoida `.segment-creation-marker`-määrä = ankkurimäärä — modaalin `<li>`-määrä ⊥ todista karttapalautetta, juuri se ero oli B162:n katve. (T388) |
| V281 | Luonnin ensiklikki ⊥ saa lukita reittiä jota käyttäjä ⊥ voi vaihtaa. Reitin valinta on KAKSI ulospääsyä samaan ongelmaan: (a) `resolveClick` katsoo vain NÄKYVIÄ reittejä (`visibleRouteIds`, V271/V272) ∴ olemassa oleva suodatin ON "valitse tämä reitti" -työkalu ⊥ erillinen ominaisuus; (b) luontimodaali tarjoaa reittipillerit kun ensiklikin ympärillä on ≥2 ehdokasta (≤`SHARED_THRESHOLD_M`) & vaihto re-resolvoi ankkurin AlKUPERÄISESTÄ klikkikoordinaatista. Perustelu: `resolveClick` valitsee globaalisti lähimmän `Infinity`-kynnyksellä & SMTB-reitit kulkevat ≤100 m toisistaan ∴ GPX-kärkipisteiden metrien ero ratkaisee reitin — arvaus jota T362 teki NÄKYVÄKSI muttei VAIHDETTAVAKSI (B164). Vaihto NOLLAA ankkurit yhteen: ankkuri on reitin pisteindeksi ∴ toisen reitin indeksi ⊥ tarkoita samaa maastoa & hiljainen siirto olisi juuri se arvaus jonka B144 kielsi. Kaikki reitit piilotettu → ehdokkaiksi palautuvat KAIKKI reitit: suodatin ⊥ saa tuottaa tilaa jossa luonti ⊥ ole mahdollinen. **V258 pysyy:** jälki on yhden reitin yhtenäinen siivu — pätkä joka JATKUU reitiltä toiselle vaatii oman speciinsä, ⊥ ratkea tällä. (T389) |
| V282 | **Pätkä on MERKKIJOUKON omistaja; geometria on väline ⊥ päämäärä.** Järjestäjän lause 2026-07-29: "tärkeintä on vain että pätkän alla on joku merkki". Seuraus: jäljen tarkkuusvaatimus on TASAN se mitä `markersForSegment` (V259, lähin jälki voittaa) tarvitsee erottaakseen naapuripätkät — ⊥ enempää. Sääntö joka torjuu LAILLISEN pätkän geometrisen puhtauden nimissä on väärä sääntö: talkoolainen jää ilman tehtävää & merkit ilman omistajaa (pahempi tila kuin epätarkka jälki). ∴ luonti ⊥ saa umpikujaa vain siksi että maasto ei noudata yhtä GPX-tiedostoa (B164-perhe). Tämä ⊥ kumoa V258:n MONOTONISUUTTA saman reitin sisällä (B144: takaisinpäin-arvaus valitsi väärän kierroksen) — se rajaa sen koskemaan yhtä reittisiivua kerrallaan. (T390) |
| V283 | **V259-AMEND: lähin jälki voittaa VAIN kynnyksen sisällä.** Alkuperäinen V259 jätti kynnyksen pois ("kynnys tuottaisi orpoja & orpo merkki on näkymätön talkoolaiselle") & luotti siihen että ehdokkuutta rajaa reittijäsenyys (V25, 100 m korridori). Se päättely pitää VAIN kun reitti on kokonaan jaettu pätkiin: reitin ENSIMMÄINEN pätkä ⊥ ole ketään vastaan ∴ se voittaa jokaisen merkin koko reitiltä. Mitattu tuotannossa 2026-07-29 (B165): G62 (sgf-62:n ainoa pätkä, 14.3 km) omisti 38 merkkiä joista 25 oli >100 m & 21 >1 km päässä sen jäljestä, kauimmainen 10.5 km; myös Pätkä 8 (smtb-55) omisti 4 liikaa. ∴ omistus vaatii kynnyksen (`MEMBERSHIP_THRESHOLD_M`, oletus 200 m — mitattu: 200 m & 300 m antavat G62:lle saman 14, 100 m veisi ⊥ vain oikeita pois naapuripätkiltä). Orpo merkki on LAILLINEN & VÄLTTÄMÄTÖN tila rakennusvaiheessa — se ⊥ ole virhe vaan tieto siitä että reitti ⊥ ole vielä jaettu. Orpous ! olla järjestäjälle NÄKYVÄ (laskuri), muuten se on hiljainen tietokato (B131-luokka) — juuri se pelko sai jättämään kynnyksen pois & vaihtoi näkyvän puutteen näkymättömään väärään omistukseen. Talkoolaisen näkymä ⊥ muutu: hän näkee OMAN pätkänsä merkit, & niitä on nyt vähemmän ⊥ enemmän. (T391) |
| V284 | **Pätkä ⊥ omista merkkiä joka on selvästi lähempänä TOISTA reittiä.** Jäsenyys mittasi vain merkin & pätkän JÄLJEN välin (V259/V283) — reittien keskinäinen etäisyys jäi kokonaan lukematta ∴ merkki 30 m päässä sgf-125:stä & 150 m päässä sgf-62:sta oli sgf-62-pätkän laillinen jäsen (molemmat ≤100 m korridori, V25). Sääntö: pätkä saa merkin vain jos `dJälki ≤ merkin_lähin_reitti + ROUTE_TOLERANCE_M` (25 m). Toleranssi ⊥ ole nolla: jaetulla osuudella reitit kulkevat samaa tietä & senttimetrien ero ratkaisisi omistuksen arvalla — 25 m erottaa "sama tie" & "toinen tie". Mitattu tuotannosta 2026-07-29: sääntö siirtää 9 merkkiä orvoiksi (G62 −3, Pätkä 1 −2, Pätkä 4 −2, Pätkä 8 −1, Pätkä 5a −1), muut ennallaan ∴ se poistaa väärät ⊥ riko oikeita. **Mittaustapa ! olla sama molemmilla puolilla vertailua: KOHTISUORA etäisyys** (`distanceToPathM`), ⊥ kärkipiste-etäisyys — V261 mittasi eron jopa 41.9 m & se on suurempi kuin koko toleranssi ∴ sekamittaus tekisi säännöstä satunnaisen. Merkin lähin reitti lasketaan KERRAN & tallennetaan merkille (`nearestRouteId`,`nearestRouteDistM`): jäsenyyslogiikka ⊥ tunne reittigeometriaa & sen läpivienti 29 kutsupaikkaan tekisi säännöstä hiljaa pettävän (kutsupaikka joka unohtaa reitit palauttaisi vanhan käytöksen). Kentät puuttuvat (vanha merkki, backfill kesken) → sääntöä ⊥ sovelleta ∴ välitila on entinen käytös, ⊥ tyhjä kartta. (T392) |
| V285 | **Varustelistan luku vastaa "paljonko PAKKAAN", ⊥ "paljonko pätkällä on".** Iso luku on aina JÄLJELLÄ OLEVA (`take`), kokonaismäärä elää metassa (`N/M`) — talkoolainen lukee ensimmäisen luvun & alkaa laskea kylttejä reppuun ∴ sen ! olla se joka menee reppuun. `ei_tarpeen` ⊥ ole "tehty" vaan "ei koskaan" ∴ se poistuu myös NIMITTÄJÄSTÄ: muuten M kasvaa merkeistä joita kukaan ⊥ koskaan aseta & laskuri ⊥ täyty. `phase` ratkaisee suunnan (asettaminen: done=asetettu+tarkistettu+kerätty | purku: done=kerätty) LOGIIKASSA ⊥ UI:ssa — sama sääntö kuin V90/`getPhaseProgress` (DESIGN.md:438: "ei branch-logiikkaa UI-tasolle"). Täysin tehty tyyppi poistuu varustarkastuksen nimittäjästä (V182/V180): rasti tavaralle jota ⊥ oteta on kuollut pinta (V250-luokka) & estäisi "lähtövalmis"-tilan pysyvästi. Kolme kutsupaikkaa (`segment-equipment.ts`, `equipment-modal.ts`, `segment-details-modal.ts`) jakavat YHDEN pure-funktion — kolme laskentaa samalle luvulle on kolme mahdollisuutta erota. VISION.md:285 lupasi tämän laskurin ("tällä pätkällä 30 merkkiä, otettu 12") & lupaus oli lunastamatta. (T393, T394) |
| V286 | **Ulkoinen navigointi on YKSI ankkuri yhteen kohteeseen:** `navUrl` (Google Maps universal URL) + `navTarget`, molemmat `src/logic/nav-link.ts`. ⊥ Waze-/Apple-haaraa, ⊥ platform-sniffausta, ⊥ nav-appivalitsinta — talkoolaisella on hanskat & yksi käsi (VISION §UX "max 2 nappia"); appivalinta olisi kolmas napautus ennen kuin auto liikkuu. ⊥ tieverkkolaskentaa eikä routing-palvelua: se vaatisi verkkoyhteyden juuri metsässä missä sitä ⊥ ole, & lopputulos olisi silti arvaus (nav-appi snappaa metsämerkin usein väärälle puolelle). Lähimmän autolla saavutettavan pisteen tuo IHMISTIETO — järjestäjän asettama pysäköintipiste-POI (VISION §Avoimet 8) — & se vaihdetaan `navTarget`in SISÄLLÄ ⊥ kutsupaikoissa: kutsupaikka ⊥ rakenna URLia itse eikä lue `marker.lat/lon` navigointia varten. Yksi kohdefunktio = yhden rivin vaihto kun POI tulee; hajautettu kohde = N kutsupaikkaa joista osa unohtuu. (T395, T396) |
| V287 | **GPS-sijaintipiste on kartan YLIN kerros & sen paikka tulee PANESTA ⊥ piirtojärjestyksestä.** Oma Leaflet-pane (`'gps'`, `zIndex 675`) — yli `overlayPane`(400, reitit+pätkäviivat+aluepolygonit), `markerPane`(600, merkki-ikonit) & `tooltipPane`(650, pätkälaput) mutta alle `popupPane`(700). Piirtojärjestys ⊥ kelpaa takeeksi: jokainen `SegmentOverlay.update()` / `AreaOverlay`-render / `MarkerManager.reload()` lisää layerit UUDELLEEN samaan `overlayPaneen` ∴ mikä tahansa myöhempi uudelleenpiirto hautaa GPS-pisteen — vika palaisi joka kerta kun uusi overlay lisätään, & sitä ⊥ näe kehityskoneella missä pätkiä on yksi. Pane luodaan idempotentisti (`getPane('gps') ?? createPane('gps')`) GPS-koodin SISÄLLÄ ⊥ `map-init.ts`:ssä: pane on GPS-pisteen toteutusyksityiskohta & kartan alustus ⊥ tunne sitä. Leaflet luo pane-kohtaisen SVG-rendererin automaattisesti (`Map._getPaneRenderer`) ∴ pelkkä `pane`-optio `circleMarker`ille riittää — ⊥ omaa `L.svg()`-instanssia. Talkoolainen metsässä lukee kartalta KAKSI asiaa: missä minä olen & missä merkki on; jos ensimmäinen katoaa pätkäviivan alle, GPS-toiminto on olemassa mutta hyödytön. (T397) |
| V288 | Merkkipohjan luonnin yhteydessä valittu inventaariolinkki kirjoitetaan VASTA kun template on TALLENNETTU — templatea ⊥ ole olemassa ennen sitä ∴ `template_id`:llä ⊥ ole kohdetta johon osoittaa. Peruttu / Esc-suljettu luonti ⊥ jätä linkkiä eikä orpoa `template_id`:tä. Jatkaa V277:ää: valinta on ihmisen EKSPLISIITTINEN teko (⊥ konearvaus), mutta teon VAIKUTUS ajoittuu tallennukseen ∴ "valitsin väärin, suljin ikkunan" ⊥ jätä jälkeä kantaan. Tallennus onnistui & linkitys epäonnistui = template JÄÄ (⊥ rollbackia): merkkipohja on itsenäisesti arvokas & sen poistaminen linkkivirheen takia hukkaisi juuri tehdyn työn — käyttäjälle kerrotaan & ohjataan inventaariosivun 🔗 Yhdistä -työkaluun (V21). |
| V289 | Varastolinkki-osio on VALINNAINEN laajennus `SignTemplateModal`iin: puuttuvat callbackit TAI epäonnistunut/estetty inventaariohaku (V163 → 403 talkoolaiselle, verkkovirhe) → osio jää renderöimättä HILJAA. Merkkipohjan luonti ⊥ saa kaatua eikä näyttää virhettä siksi että VARASTOA ⊥ saatu — luonti on itsenäinen toiminto joka on toiminut ilman inventaariota T193:sta asti & ! toimia jatkossakin. Virheilmoitus tässä kohtaa syyttäisi käyttäjää asiasta jota hän ⊥ ole pyytänyt. |
| V290 | **Merkkijono-paneeli KOOSTAA, ⊥ omista sääntöjä.** Suodatus ← `src/logic/map-filter.ts` (V271), jäsenyys ← `segment-membership.ts` (V259), km & järjestys ← `segment-order.ts` (V237/V238). Oma status-suodatin- \| jäsenyyssääntö tänne = B114–B129-perheen 11. esiintymä. Lista rajautuu AKTIIVISEEN VAIHEESEEN (V91): sama fyysinen merkki elää eri pätkäjaossa eri vaiheessa ∴ vaiheiden yli koottu lista näyttäisi saman merkin monta kertaa eri omistajilla. Kartan suodattimen rajaamat merkit ⊥ KATOA listalta hiljaa vaan menevät viimeiseksi, oletuksena suljetuksi ryhmäksi "Suodattimen ulkopuolella (N)" — lista jonka luku ⊥ täsmää karttaan luetaan rikkinäisenä datana (B131-luokka, sama peruste kuin V272:n banneri). Merkki joka ⊥ kuulu yhteenkään pätkään → oma "ei pätkää" -ryhmä, ⊥ pudota pois (V238-kuvio). (T400,T402) |
| V291 | **Merkkien poiminta reitittömään tehtävään SIIRTÄÄ omistajuuden, ⊥ kopioi:** `linkedMarkerIds` on eksplisiittinen tahto joka V259:n mukaan ohittaa geometrian JA estää saman merkin geometrisen adoption muualle. ∴ tallennus ! vahvistusaskel joka LISTAA menettävät pätkät nimeltä ("N merkkiä siirtyy pois pätkiltä: <nimet>"). `window.confirm` ⊥ riitä — se ⊥ voi listata pätkiä luettavasti eikä osoittaa mitkä rivit siirtyvät. Siirtyviä 0 → vahvistusaskelta ⊥ ole: turha porras opettaa ohittamaan sen joka merkitsee. Hiljainen siirto = talkoolaisen merkki katoaa kesken kenttätyön (V18-luokan haitta). (T401,T403) |
| V292 | **Talkoolaisen VALIKOIVA bulk-kuittaus ⊥ saa kadota siivouksessa.** `.bulk-action-bar` (T17: "✓ Aseta valituille" / "Ei tarpeen") elää TASAN `src/ui/marker-list.ts`:ssä — `segment-marker-list.ts` ⊥ tunne checkboxeja & `segment-view.ts`:n `bulkCollect` on vain purkuvaiheen "merkitse KAIKKI kerätyksi" (⊥ valikoiva). VISION §Kenttätyö vaatii bulkin eksplisiittisesti ("ei halua olla koko ajan puhelimella") ∴ `marker-list.ts` ⊥ saa poistua ennen kuin valikoiva bulk on `SegmentMarkerList`issä. Poisto ilman siirtoa on talkoolaisen regressio ⊥ siivous. **Amendaa V148/V155:** `#btn-list` on VAIN järjestäjän toolbarissa; talkoolaisen `#toolbar-actions` = TASAN {Varustelista, ⋯ `#btn-menu`} & "Kaikki merkit" asuu koti-tabissa (V183/V184). (T404) |

## §T Tasks

| id | status | task | cites |
|----|--------|------|-------|
| T1 | ✓ | GPX lataus, reittiviivat kartalla, fitBounds | V7 |
| T2 | ✓ | Perusmerkit 4 tyyppiä: asetus dblclick/toolbar, poisto, bearing-snap | V1,V2,V3,V8 |
| T3 | ✓ | Multi-route: merkki kuuluu useammalle reitille (100m threshold) | V2 |
| T4 | ✓ | Drive mode: 50m step, keyboard nav, progress bar drag, polyline click | V4 |
| T5 | ✓ | Tile-layer vaihto MML/OSM, persistoi | V5 |
| T6 | ✓ | Route tabs: näkyvyys toggle, drive-reitti vaihto, eye-icon | V6 |
| T7  | ~ | ~~Lisää puuttuvat 2 GPX-reittiä (4 reittiä yhteensä)~~ SUPERSEDED T285:llä (2 tapahtumaa, 5 reittiä) | §C |
| T8  | ✓ | SignTemplate data model + CRUD logiikka — `src/logic/sign-library.ts`. Tyyppi: `SignTemplate{id,label,shortLabel,color,description}`. Funktiot: createTemplate/updateTemplate/deleteTemplate/listTemplates, in-memory (ei localStorage). SignTemplate on superset SignTypeInfo:lle (sign-picker.ts) — korvaa sen T22-vaiheessa. Testattavuus: Vitest-pure. Käyttäjä: järjestäjä rakentaa, talkoolainen käyttää. Avaa: T9,T22,T27 | V10 |
| T9  | ✓ | Ikonilähde-research: päätös Lucide — TypeScript-natiivi, tree-shakeable, MIT. `lucide` npm-paketti. SignTemplate.iconId = Lucide icon name string. T22 voi edetä. | V10 |
| T10 | ✓ | MarkerStatus type + tila-siirtymälogiikka (Vitest-pure): suunniteltu→asetettu→tarkistettu→kerätty\|ei_tarpeen. Tyyppi `src/logic/types.ts`, logiikka `src/logic/marker-status.ts`. `transitionStatus/canTransition/validActions/isTerminal`. Normalisointi persistence.ts:ssä vanhalle datalle. | V9 |
| T11 | ✓ | Paikkaohjeet: `locationNote?: string` SignMarker-tyyppiin, `updateNote(id, text)` MarkerManageriin, inline-input marker-listassa, tallentuu automaattisesti (blur/Enter), XSS-safe (DOM-assign) | §G |
| T12 | ✓ | Rooli-state + toggle UI. Logiikka: `src/logic/role.ts` — `type Role = 'järjestäjä' \| 'talkoolainen'`, `getRole(): Role` (localStorage, default `'järjestäjä'`), `setRole(r: Role): void` (persist). Toggle-nappi toolbariin: `src/ui/role-selector.ts`, vaihtaa roolia yhdellä klikillä, active = accent-highlight (DESIGN.md §K role-toggle). Ei piilota/näytä mitään vielä — se on T32. localStorage: vi.stubGlobal-mock (CLAUDE.md). Testattavuus: Vitest-pure (logic), Vitest-jsdom (toggle). Käyttäjä: molemmat. Avaa: T24, T32. | V13 |
| T13 | ✓ | Segment type + logiikka: `{id, routeIds: string[], startDist: number, endDist: number, assignedCode?: string, displayName?: string, description?: string, equipment: EquipmentItem[], phase: 'asettaminen'\|'purku'}`. `EquipmentItem: {name: string, count: number}`. Funktiot `src/logic/segments.ts`: `createSegment / updateSegment / deleteSegment / getSegmentsForPhase(segments, phase) / getSegmentForCode(segments, code) / getMarkersForSegment(segment, markers)` — filtteröi routeIds-leikkauksella + distanceFromStart-rangella. Jatkuvuus-validointi (V11). Vitest-pure. Käyttäjä: järjestäjä luo, talkoolainen kuluttaa. Avaa: T14,T25,T26,T27,T52. | V11,V25,V26 |
| T14 | ✓ | Talkoolaisen pätkänäkymä: filtteröity kartta + merkkilista vain oman pätkän merkeistä (`getMarkersForSegment` T13). Näyttää varustelistan (T27) + järjestäjän description-kentän. URL `/s/<koodi>` → auto-auth T51:n code-login → suoraan näkymään (V27, T53). Yhteinen osuus: kaikkien routeIds-reittien merkit yhdessä listassa — yksi merkki kerran (V25). `src/ui/segment-view.ts` (uusi komponentti). Vitest-jsdom. Käyttäjä: talkoolainen. §K-sopimus lisätään DESIGN.md:hen ennen rakennusta. | V11,V25,T12,T13 |
| T15 | ✓ | Tilannekuva-logiikka: laske % per status per reitti (Vitest-pure) | V9 |
| T16 | ✓ | Navigointilogiikka: uusi `src/logic/navigation.ts`. `nearestUnsetMarker(markers: SignMarker[], currentDist: number, routeId: string): SignMarker | null` — pienin |distanceFromStart−currentDist| jolla status==='suunniteltu' JA routeIds sisältää routeId. `distanceToNext(markers: SignMarker[], currentDist: number, routeId: string): number | null` — etäisyys metreinä lähimpään 'suunniteltu'-merkkiin, null jos ei löydy. Testattavuus: Vitest-pure (pakollinen). Käyttäjä: talkoolainen. Avaa: T31. | V9 |
| T17 | ✓ | Talkoolaisen jälkikäteinen kuittaus (bulk check-in): checkbox per marker-rivi talkoolaisen marker-listassa (pätkän merkit, V33). BulkActionBar sticky bottom: [☐ Valitse kaikki] + [✓ Aseta valituille] + [Ei tarpeen valituille]. "Aseta valituille": `manager.updateStatus(id, 'asetettu')` kaikille valituille ei-terminal-merkeille. "Ei tarpeen": `manager.updateStatus(id, 'ei_tarpeen')`. Terminal-merkit (kerätty, ei_tarpeen) skip silently. DESIGN.md §K BulkActionBar-sopimus kirjoitettu. Vitest-jsdom: select all + bulk aseta → kaikki ei-terminal saavat asetettu, terminal skip. Käyttäjä: talkoolainen. | V9,T10,T14,T58 |
| T18 | . | Offline-tuki: oma pätkä ladattavissa (PWA + service worker) | §C |
| T19 | . | Purkupätkä: lähtösuunnan valinta, merkit kerätään käänteisessä järjestyksessä | V11,T10 |
| T20 | . | Kasauspisteet: erikoismerkki "kasa tässä", merkitään haetuksi kerralla | V9,T10 |
| T21 | . | Live tracking v1: laitteen GPS-sijainti näkyvissä itselle kartalla | §C,T30 |
| T22 | ✓ | SignLibrary UI paneeli: järjestäjä luo/muokkaa/poistaa SignTemplateja | T8,T9 |
| T23 | ✓ | Merkin status-kuvake kartalla: `createSignIcon(type, bearing, status)` — opacity 0.45 suunniteltu / 1.0 muut + 8px status-piste oik. alakulmaan (`#4ade80` asetettu, `#93c5fd` tarkistettu, `#fbbf24` ei_tarpeen, piilotettu suunniteltu). Muoto on aina teardrop (ympyrä + kiinteä kärki). Ei status-perusteista muotomuutosta, ei tekstiä merkillä. | T10 |
| T24 | ✓ | Talkoolaisen kuittaus-UI: 1 iso nappi (asetettu / ei_tarpeen) — max 2 toimintoa | T10,T12 |
| T25 | ✓ | SegmentPanel + karttavisualisointi: (a) järjestäjä klikkaa kaksi pistettä reittiviivalta → luo pätkän, pisteitä voi siirtää jälkikäteen; (b) kaikki pätkät värillisinä kaistoina kartalla + talkoolaisen displayName-overlay; (c) aukot (ei-jaetut osuudet) harmaana. Leaflet polyline overlay `src/map/segment-overlay.ts`. Panel `src/ui/segment-panel.ts`. Playwright (karttainteraktio). Käyttäjä: järjestäjä. §K-sopimus lisätään DESIGN.md:hen ennen rakennusta. | T13,V11 |
| T26 | ✓ | Assign + URL-generointi: järjestäjä syöttää assignedCode + displayName → syntyy URL `/s/<koodi>` + kopiointinappi. Segment.assignedCode + displayName tallennetaan (T13). Assign-flow `src/ui/segment-panel.ts`:ssä (sama paneeli kuin T25). Vitest-jsdom. Käyttäjä: järjestäjä. | T12,T25,V27 |
| T27 | ✓ | Varustelista: auto-laskuri merkkityypeittäin per pätkä + manuaali-rivit (EquipmentItem[]) + ohjeteksti. Järjestäjä muokkaa `segment-panel.ts` "Lisätiedot & varusteet" -osiossa. Talkoolainen näkee readonly `segment-view.ts`:ssä. Vitest-pure (segment-sync), Vitest-jsdom (segment-persistence). | T8,T14 |
| T28 | ✓ | Tilannekuva-kartta UI: merkit värikoodattu + % per reitti näkyvissä | T15,T23 |
| T29 | ✓ | Merkkien persistointi: `src/logic/persistence.ts` — saveMarkers/loadMarkers, tallennus joka add/remove/updateBearing, lataus init:ssä ennen karttaa, format `{version:1,markers:[]}`, routeIds säilyy vaikka reitti puuttuisi | V12,V14,§I |
| T30 | ✓ | Geolocation API: laitteen sijainti pisteenä kartalla (itselle, ei muille) | §C |
| T31 | ✓ | GPS-drive UI: navigointi lähimpään asettamattomaan merkkiin + kuittaus | T16,T24,T30 |
| T32 | ✓ | Rooli-näkymävalinta UI: toolbar + paneelit eriytyvät roolin mukaan | T12,V13 |
| T33 | ✓ | Arkkitehtuurirefaktorointi: src/logic/, src/map/, src/ui/ hakemistot — siirrä tiedostot | §C |
| T34 | . | GPX-korvaus: varoitus + merge/discard-valinta olemassa oleville merkeille | V2 |
| T35 | . | Yhteinen osuus -visualisointi: korostus kartalla missä reitit jakavat merkin | V2 |
| T36 | ✓ | Auth-reittit + käyttäjähallinta: `POST /api/auth/login` (username+password → httpOnly cookie sessio, bcrypt), `POST /api/auth/code-login` (talkoolainen-koodi → 24h sessio, uusittavissa), `POST /api/auth/logout`, `GET /api/auth/me`. Invite-flow järjestäjälle: `POST /api/admin/invites` (admin luo token), `GET /api/auth/invite/:token`, `POST /api/auth/register` (one-time). Talkoolainen-koodit: `POST /api/admin/codes` (bulk, `{code,display_name,segment_id?}`), `GET /api/admin/codes`, `DELETE /api/admin/codes/:code`. `GET /api/admin/users` (admin). `server/routes/auth.ts`. Bun integraatiotestit. Vaatii T41. Käyttäjä: molemmat. Avaa: T51. | T41,V13 |
| T37 | ✓ | Drag-to-move merkki: `marker.options.draggable = true` MarkerManager.add()-vaiheessa. `dragend` → uusi LatLng → `assignRoutesToMarker` (V21: pakota lähimmän reitin id jos [] palautuu) → päivitä routeIds + laske bearing uudelleen → päivitä Leaflet-ikoni → `saveMarkers`. Varoitusbanneri jos >500m reitistä (sama kuin T44). `src/map/markers.ts`. Käyttäjä: järjestäjä (desktop/hiiri). Testattavuus: Playwright — drag-testi `e2e/critical-paths.spec.ts`. | V15,V1,V2,V21 |
| T38 | ✓ | Vaihda merkin tyyppi: kontekstivalikosta tai listasta — ei delete+redo. Päivittää ikonia kartalla + listaa | V17 |
| T39 | x | Drive mode "hyppää seuraavaan merkkiin": nappi joka siirtyy seuraavan merkin distanceFromStart-kohtaan aktiivisella reitillä — ei GPS-riippuvainen, toimii jo nyt ilman T30. ✓ `nextMarkerAhead(markers,currentDist,routeId)` (`src/logic/navigation.ts`, strict `>` ∴ toistopainallus etenee ⊥ juutu) + `#btn-route-next-marker` (⇥) drive-kontrolleissa (piiloon järjestäjältä V134) + `driveMode.jumpToDistance`-glue `markers-wiring.ts`. 8 Vitest-pure + E2E critical-paths 21/21. | T4 |
| T40 | ✓ | Rotation arm sticky: poista `handleArmOutsideClick` document-mousedown/touchstart kokonaan. Lisää `handleEscKey` (keydown Escape → `disarm()`). `arm(id)` kutsuu `this.disarm()` alussa → "toinen merkki" + "uusi lisäys" hoidettu automaattisesti. Arm poistuu vain: Esc / arm() uudelleen. Ei poistu karttaklikistä. `src/map/marker-interaction.ts`. Käyttäjä: järjestäjä (desktop). Testattavuus: Playwright — arm-sticky-testi `e2e/critical-paths.spec.ts` (karttaklikki ei poista armia, Esc poistaa). | V16 |
| T41 | ✓ | Backend server-perusta: Hono + Bun + SQLite. `server/index.ts` (Hono app + routes wiring), `server/db.ts` (SQLite yhteys + schema init, idempotent). Schema: `users, sessions, talkoolainen_codes, markers, map_state, snapshots` — katso `docs/components/backend.md`. Seed admin: luo 'admin'-käyttäjä env:stä (`ADMIN_USERNAME` + `ADMIN_PASSWORD_HASH`) jos ei ole — idempotent deploy. Auth-middleware: `server/middleware/auth.ts` (session cookie check + role gate). `GET /api/health` → 200. Bun integraatiotestit, in-memory SQLite. Avaa: T36, T47, T48. | V18,V19 |
| T42 | ✓ | Sync-logiikka: `src/logic/sync.ts` — online-first, localStorage cache. `syncMarkers()`: fetch serveriltä → päivitä localStorage → palauta. `pushPending()`: lähetä pendingSync-merkit serverille. Vitest-pure (mock fetch). | V18,T41 |
| T43 | x | POISTETTU (PM 2026-07-08): suunniteltu offline-first `pendingSync`-mallille jonka V18 (backend ainoa totuus) + V116 durable-outbox korvasi — kirjoitukset retryataan 2xx:ään, luku aina backendistä → per-marker merge-konfliktia ei synny. ~~Merge-konflikti UI: `pendingSync > 0` → dialog "vaihda kaikki / pidä omat".~~ cite V20 on jo poistettu invariantti. | T42 |
| T44 | ✓ | Ghost marker fix: `MarkerManager.add()` pakottaa lähimmän reitin id routeIds:ään jos assignRoutesToMarker palauttaa []. Näytä varoitusbanneri kartalla jos merkki >500m reitistä. Vitest-pure (logiikka), Playwright (banneri). | V21,V1 |
| T45 | ✓ | Touch targets mobile: route-tab-napit (35km/55km) ja eye-icon-toggle ≥44px korkeus mobiililla. Playwright (375px viewport, kaikki napit ≥44px). | §C |
| T46 | ✓ | Playwright E2E -perusta: `playwright.config.ts` webServer-config, `e2e/`-hakemisto, 3 kriittistä polkua: (1) merkki asetetaan kartalle → näkyy listassa, (2) drive mode käynnistyy + navigoi, (3) rooli-toggle muuttaa toolbaria. | §C |
| T47 | ✓ | Markers REST API role-gatella: `GET /api/markers` (403 jos luonnos+talkoolainen, V22), `POST /api/markers` (järjestäjä+), `PUT /api/markers/:id` (status: kaikki autentikoidut; lat/lon/bearing/type: järjestäjä+), `DELETE /api/markers/:id` (järjestäjä+). `updated_by = session.display_name`. `server/routes/markers.ts`. Bun integraatiotestit (role gate per endpoint). Vaatii T41, T36. Käyttäjä: molemmat. Avaa: T42. | V18,V19,V22 |
| T48 | ✓ | Kartta-tila API: `GET /api/admin/map-state` → `{status,approved_at?,approved_by?}`. `POST /api/admin/map-state {status}` (järjestäjä+) → jos 'hyväksytty': luo automaattinen snapshot (V23) + tallentaa `approved_at` + `approved_by`. `server/routes/admin.ts`. Bun integraatiotestit (approve luo snapshot). Vaatii T41, T36, T47. Käyttäjä: järjestäjä. Avaa: T49, T50. | V22,V23 |
| T49 | ✓ | Kartta-tila UI: 'LUONNOS'/'HYVÄKSYTTY' -badge toolbariin (järjestäjä näkee tilan). Järjestäjälle 'Hyväksy kartta' -nappi (confirm dialog → `POST /api/admin/map-state`). `PersistenceLayer.syncMarkers()` saa 403 → näyttää 'Kartta valmisteilla' -bannerin talkoolaiselle (sama tyyli kuin distance-warning). `src/ui/map-state-badge.ts`. Vitest-jsdom. Vaatii T48, T32. Käyttäjä: järjestäjä (badge + approve), talkoolainen (banneri). | V22,V13 |
| T50 | ✓ | Snapshot API + UI: `GET /api/admin/snapshots` (max 20, vanhimmat poistetaan automaattisesti), `POST /api/admin/snapshots` (manual, `{label?}`), `POST /api/admin/snapshots/:id/restore` (atominen transaction: korvaa kaikki merkit + luo "Palautus: ..."-snapshot, V24). Auto-snapshot: yöllinen (Bun setInterval klo 03:00 UTC). UI: `src/ui/snapshot-panel.ts` — lista + 'Palauta tämä versio' -nappi. Vitest-jsdom. Vaatii T41, T36, T48. Käyttäjä: järjestäjä. | V23,V24 |
| T51 | ✓ | Auth screen ennen karttaa: `src/ui/auth-screen.ts`. App käynnistyy → `GET /api/auth/me` → 401 → näytä login-lomake (toggle: 'Järjestäjä' username+password / 'Talkoolainen' koodi-kenttä). Onnistunut login → piilota lomake, näytä kartta + set role. Virheviesti väärästä tunnuksesta/koodista. Vitest-jsdom (lomake + toggle + virheviesti). Playwright: login-flow kriittinen polku (`e2e/critical-paths.spec.ts`). Vaatii T36. Käyttäjä: molemmat. | V13,T36 |
| T52 | ✓ | Purku-pätkäjako + bulk-kuittaus: järjestäjä luo purku-vaiheen pätkäjaon (`phase: 'purku'`, T13) — eri jako kuin asettaminen. Talkoolainen voi kuitata merkit yksitellen (V9) TAI bulk: "Merkitse kaikki kerätyksi" -nappi → atominen `bulkCollect(segment, markers)` → kaikki ei-terminal-merkit → kerätty (V28). Retroaktiivinen: toimii jälkikäteen ilman per-merkki-kuittailua. `src/logic/segment-actions.ts` (`bulkCollect`). Bulk-UI: iso nappi `src/ui/segment-view.ts`:ssä (T14). Vitest-pure (logiikka) + Vitest-jsdom (UI). Käyttäjä: talkoolainen purku-vaiheessa. | V9,V26,V28,T13,T14 |
| T53 | ✓ | URL-reititys: `/s/<koodi>` deep-link → `src/ui/auth-screen.ts` lukee `window.location.pathname` käynnistyksessä, poimi koodi → pre-täytä koodi-kenttä + automaattinen submit → suoraan talkoolaisen pätkänäkymään (T14). Ei erillistä backend-reittiä — SPA client-side URL-handling, nginx fallback `try_files $uri /index.html`. Vitest-jsdom (URL-parsinta + auto-fill). Käyttäjä: talkoolainen. | V27,T51,T14 |
| T54 | ✓ | Auth fallback -korjaus: `auth-screen.ts` — poista silent skip (rivit 48–53). Verkkovirhe tai non-401 → `this.show()` (kirjautumislomake näkyviin), ei `onAuthenticated`. Dev-ympäristö ilman backendiä: käyttäjä näkee lomakkeen. Vitest-jsdom: testi jossa fetch heittää NetworkError → lomake näkyy, `onAuthenticated` ei kutsuta. | V29,T51 |
| T55 | ✓ | Segmenttien persistointi: `src/logic/segment-persistence.ts` — `saveSegments(store)/loadSegments(): SegmentStore`. Format `{version:1,segments:Segment[]}`. localStorage-avain `karttamaster-segments`. Korruptoitunut data → silent reset (V14). Kutsu `saveSegments` jokaisen `createSegment/updateSegment/deleteSegment`-kutsun jälkeen. Lataa `loadSegments()` `init()`-vaiheessa ennen karttaa. Vitest-pure (localStorage-mock). | V30,V14,T13 |
| T56 | ✓ | Pätkän luonti-UX: (a) ensimmäisen klikkauksen jälkeen näytä väliaikainen markkeri kartalla (L.circleMarker, punainen) — häviää kun toinen piste klikataan tai Esc. (b) Pätkärivillä "Muokkaa pisteitä" -nappi → siirrä overlay-pisteet drag-to-reposition -moodiin. `src/ui/segment-panel.ts` + `src/map/segment-overlay.ts`. Playwright (luonti-flow). | V31,V32,T25 |
| T57 | ✓ | Snapshot-paneeli collapsible: oletuksena supistettu (vain "Varmuuskopiot (N)" -otsikko + expand-nappi). Laajenee klikillä. Tieto-elementit eivät vie tilaa oletuksena. `src/ui/snapshot-panel.ts`. Vitest-jsdom. | T50 |
| T58 | ✓ | Talkoolaisen marker-modaali filtteröity: `openMarkerModal` `src/main.ts`:ssä — jos `talkoolainenCode` asetettu JA pätkä löytyy, kutsu `renderMarkerList(markerManager, highlightId, segmentMarkerIds)` jossa `segmentMarkerIds = Set<string>` pätkän merkki-id:t. `renderMarkerList` suodattaa: talkoolaiselle näytetään vain pätkän merkit. Vitest-jsdom (renderMarkerList suodatus). | V33,T13,T14,T25 |
| T59 | ✓ | Assign-sync fix: `segment-panel.ts` `saveBtn`-klikissä kutsu `POST /api/admin/codes {code, display_name, segment_id}` — vasta onnistuneen vastauksen jälkeen kutsu `updateSegment` + `saveSegments`. `Muuta`-napissa kutsu `DELETE /api/admin/codes/:code` — onnistumisen jälkeen `updateSegment(..., {assignedCode: undefined})`. API-virhe → näytä virheviesti assign-osiossa, älä päivitä localStoragea. Vitest-jsdom (fetch-mock: onnistunut save, save epäonnistuu → ei localStorage-muutosta, delete). Käyttäjä: järjestäjä. | V34,T26,T36 |
| T60 | ✓ | `saveSegments` error handling: `onError?: (err: unknown) => void` callback `saveSegments`-funktioon. Virhe catch-blokissa kutsuu `onError` eikä ignoroi hiljaa. `SegmentPanel` välittää `onSaveError`-callback `main.ts`:stä — virhe näyttää varoitusbannerin. Vitest-pure (saveSegments + onError-mock). | V35,T55 |
| T61 | ✓ | Segments backend: `segments`-taulu `server/db.ts`:ään. `POST /api/segments` (upsert, järjestäjä+), `PUT /api/segments/:id`, `DELETE /api/segments/:id`. `GET /api/segments/by-code/:code` (talkoolainen). `server/routes/segments.ts`. Bun integraatiotestit (9 testiä). | V36,T41,T36 |
| T62 | ✓ | Segment sync client: `src/logic/segment-sync.ts` — `fetchSegmentByCode/pushSegment/updateSegmentRemote/deleteSegmentRemote`. `segment-panel.ts` create/update/delete pushes to backend (best-effort). `init(code)` fetchaa segmentin serveriltä, päivittää localStorage-cachen. Vitest-pure (mock fetch, 9 testiä). | V36,T61,T55 |
| T63 | ✓ | Poista map-state gate: `GET /api/markers` palauttaa merkit kaikille autentikoituneille ilman map-state-tarkistusta. Poistettu: V22 (403-logiikka), `POST /api/admin/map-state`, `GET /api/admin/map-state`, `showMapNotReadyBanner`, `MapStateBadge` approve-nappi. Snapshots säilyy itsenäisenä. | V18,V22 |
| T64 | ✓ | Markers direct-write: `MarkerManager` kirjoittaa suoraan `POST/PUT/DELETE /api/markers` — ei localStorage-välivaihetta. `add()→POST`, `remove()→DELETE`, `updateStatus/Type/Note/dragend→PUT`. Bearing-tallennus: `MarkerInteraction.onSave(id)` → `PUT {bearing}`. | V18,T63 |
| T65 | ✓ | Poista localStorage marker/segment-cache: `persistence.ts` + `segment-persistence.ts` ovat no-op stubs. `init()` hakee merkit suoraan `fetchMarkers()`. Järjestäjä hakee kaikki segmentit `GET /api/segments` init:ssä (B16-korjaus). Talkoolainen hakee oman pätkän `fetchSegmentByCode`. | V18,V30,V36,T63 |
| T66 | ✓ | `fetchAllSegments()` lisätty `segment-sync.ts`:ään. `pendingSync`-kenttä poistettu `SignMarker`-tyypistä. `sync.ts` yksinkertaistettu: vain `fetchMarkers()`. | T65 |
| T67 | ✓ | Lukitse rooli-toggle backend-rooliin: `RoleSelector` ottaa `lockedRole?: Role`-parametrin — jos `'talkoolainen'`, nappi on `hidden`. `main.ts` välittää `onAuthenticated`-vastauksesta saadun roolin. `setRole()` ei kutsuta talkoolaiselle (localStorage ei ylikirjoita backend-roolia). Vitest-jsdom (nappi piilossa talkoolaisella, näkyy järjestäjällä). Käyttäjä: molemmat. | V39,V13,T12,T32,T36 |
| T68 | ✓ | Auth-screen ensin: `AuthScreen.start()` kutsuu `this.show()` heti alussa ennen `fetch('/api/auth/me')`. Onnistunut vastaus (200) → `this.hide()` ennen `onAuthenticated`. Muut haarat (401, virhe, polkukoodi) pysyvät ennallaan. Vitest-jsdom: testi jossa `start()` kutsutaan — overlay on näkyvissä ennen kuin fetch resolvoituu. | V40,V29,T51,T54 |
| T70 | ✓ | Teardrop-ikoni: terävä kärki ankkuripisteeksi. `src/map/icons.ts` — `H: 50→52`, `CY: 38→26` (ympyrä ylös), kiinteä tip-SVG (EI pyöri bearing-transform mukana, `position:absolute;bottom:0`) `path M8,0 L16,12 L24,0 fill=color stroke=white 1.5px`. `iconAnchor: [CX,52]`, `popupAnchor: [0,-56]`. Kärki osoittaa tarkan sijainnin kartalla — erityisesti talkoolaiselle metsässä. DESIGN.md §K SignIcon päivitetään. Testattavuus: Playwright — `iconAnchor` vastaa kärjen pikseliä (vertaa klikattuun karttakoordinaattiin). | T23 |
| T71 | ✓ | CSS token-migraatio: korvaa kaikki hardkoodatut hex-arvot `src/style.css`:ssä semanttisilla custom propertiesilla. Tokenit: `--surface-app/card/raised`, `--text-body/muted/meta`, `--accent/accent-text/accent-hover`, `--confirm/confirm-hover/confirm-text`, `--danger/danger-text/danger-soft`, `--border-subtle/card/default/strong`, `--hover/hover-strong`, `--overlay`, `--gps-active`, `--field-tint`, `--warn-highlight`. Radius konsolidoitu kolmeen: `--radius-sm:6px`, `--radius-md:10px`, `--radius-lg:14px` (poistaa U5). `prefers-reduced-motion` media query motion-muuttujille. Kaksi teemaa `:root/[data-theme="dark"]` (nykyinen dark) + `[data-theme="daylight"]` (vaalea, korkea kontrasti). DESIGN.md §C päivitetään uusilla token-nimillä. Vitest-jsdom: getComputedStyle tarkistaa key-tokenit molemmilla `data-theme`-arvoilla. Käyttäjä: molemmat. | - |
| T72 | . | Teema-toggle käyttäjäpreferenssinä: `prefers-color-scheme` OS-default (`dark`→dark, `light`→daylight). Manuaalinen override: `localStorage('karttamaster-theme')` tallentaa valinnan. `<html data-theme>` päivitetään käynnistyksessä + togglesta. Toggle-nappi overflow-valikossa (`⋯`) — kuvake sun/moon (Lucide). `src/ui/theme-toggle.ts` (uusi komponentti). Vitest-jsdom: OS-default dark → `data-theme="dark"`, localStorage override daylight → `data-theme="daylight"`. Käyttäjä: molemmat. | T71 |
| T73 | ✓ | Järjestäjän collapsible left panel: merkkikirjasto + pätkäjako sivupaneelissa (desktop, `#left-panel`). Rakenne: `#left-panel-header` (aina näkyvissä: "MERKKIKIRJASTO"-otsikko + `#left-panel-toggle` `◀/▶` oikealla) + `#left-panel-content` (piilotetaan kun suljettu). Suljettu → paneeli kutistuu `32px`-leveäksi, kartta saa lähes koko leveyden. Paneelin sisältö: §K-mukainen merkkikirjasto-grid + pätkäjako-lista (siirtää content `#segment-panel`:sta + `#sign-type-dropdown`:sta). `src/ui/left-panel.ts` (uusi komponentti). `.collapsed`-luokka `#left-panel`-elementillä suljetussa tilassa. Vitest-jsdom: toggle avaa/sulkee, paneeli `hidden` kun suljettu. Playwright (1280px viewport): paneeli auki → kartta-alue pienempi, paneeli kiinni → kartta täyttää leveyden. Käyttäjä: järjestäjä. | T71 |
| T74 | . | CheckIn bottom sheet: talkoolaisen kuittaus-modaali bottom sheet -patterilla — nousee alhaalta, `border-radius:14px 14px 0 0`, `width:100%`. Sisältö: merkintunnus + nimi + km + `StatusBadge`, iso `CheckInButton` (asetettu/kerätty), sekundäärit "Ei tarpeen" + "Lisäsin toisen". Backdrop-klikki sulkee. `src/ui/checkin-sheet.ts` (uusi komponentti). KORVAA MarkerDetailModalin (T105) talkoolaishaaran — talkoolainen saa bottom sheetin (peukalo-UX yhdellä kädellä metsässä), T105-modaali jää järjestäjälle. Ei rinnakkaista talkoolais-modaalia. Vitest-jsdom: avaus merkkiklikistä, kuittaus päivittää statuksen, backdrop sulkee. Käyttäjä: talkoolainen. | T71,T24 |
| T75 | . | Kommentti-systeemi design + tietomalli: yleiskäyttöinen kommentti/huomio joka voidaan kiinnittää merkkiin, pätkään tai vapaaseen karttapisteeseen. Kenttä: `{id, targetType: 'marker'\|'segment'\|'point', targetId?: string, lat?: number, lon?: number, text: string, iconId?: string, authorName?: string, createdAt: string}`. Backend: `comments`-taulu + `GET/POST/DELETE /api/comments`. UI: kommentti-nappi merkin/pätkän modaalissa → textarea + ikoni-valinta. Kartalla: kommentti-ikoni vapailla pisteillä. Vitest-pure (logiikka) + Vitest-jsdom (UI) + Bun integraatiotestit (API). Käyttäjä: molemmat. | V13,T41,T36 |
| T76 | x | AreaMarker + AreaFeature tyypit `src/logic/area-types.ts`. Huoltopiste = nimetty geo-suorakaide kartalla (AreaMarker) joka sisältää sisäisiä suorakaiteita (AreaFeature: tarjoilupöytä, jakaja jne.). `AreaMarker{id,name,centerLat,centerLng,widthM,heightM,rotation,markdownDescription,status:'suunniteltu'\|'valmis',hashCode,features:AreaFeature[]}`. `AreaFeature{id,name?,centerLat,centerLng,widthM,heightM,rotation,color}`. Koko alue merkitään valmis-tilaan yhtenä yksikkönä — ei per-feature kuittausta. `createAreaMarker/addFeature/removeFeature/setAreaStatus`. Vitest-pure (roundtrip, status-siirtymä, feature-lisäys/poisto). Järjestäjä luo, jakaa read-only hash-URL:lla huoltopisteryhmälle. Avaa: T107,T108,T109,T110. | V13,T41,T36 |
| T107 | x | area-geometry `src/logic/area-geometry.ts`. `cornersFromRect(centerLat,centerLng,widthM,heightM,rotationDeg):[LatLng,LatLng,LatLng,LatLng]` — laskee 4 nurkkaa geo-koordinaateina offsetLatLng-apufunktiolla (m→astelaskenta, approx ok alle 10km). `rotatePoint(lat,lng,centerLat,centerLng,angleDeg)`. Vitest-pure: (1) 100×50m 0° → pohjoinen pari 50m pohjoiseen; (2) 90° rotaatio vaihtaa leveys/korkeus-suunnat; (3) nurkkien etäisyys centeristä vakio rotaatiosta riippumatta. Käyttäjä: logiikkakerros. | T76 |
| T108 | x | AreaOverlay `src/map/area-overlay.ts`. Renderöi AreaMarker-listan L.Polygon-layereina kartalle. Pääalue: L.Polygon (4 nurkkaa area-geometry.ts:stä, sininen fillColor, klikattava → onAreaClick(area) → map.flyTo(center,18)). AreaFeature-suorakaiteet: oma L.Polygon feature.color-värillä + valinnainen nimi L.Tooltip:na. Drag-to-move: koko alue + kaikki featuret liikkuvat yhdessä (dragend → päivitä centerLat/centerLng). Rotation: MarkerInteraction-pattern (vivutus hiirellä + touchilla). `update(areas:AreaMarker[])` — poista vanhat layerit, renderöi uudet. E2E Playwright `e2e/critical-paths.spec.ts`: area piirtyy kartalle + klikkaus triggeroi flyTo. Käyttäjä: molemmat (järjestäjä muokkaa, huoltopisteryhmä näkee). | T76,T107 |
| T109 | x | AreaPanel `src/ui/area-panel.ts`. Järjestäjän editor-paneeli sivupalkissa (V61 section-pattern: "Huoltopisteet" + count + footer add-nappi). Add-nappi → nimi-input + klikkaa karttaa sijainniksi (crosshair-cursor, 60×40m defaultkoko) + tallenna. Details-modaali (V62): nimi-input, markdownDescription-textarea, AreaFeature-lista (lisää suorakaide / väri-picker DESIGN.md §C-tokeneista / valinnainen nimi / poista), "Merkitse valmiiksi" -nappi (confirm-väri, window.confirm()). AreaFeature add: uusi 10×5m feature centerin päällä, käyttäjä siirtää kartalla. §K-sopimus lisätään DESIGN.md:hen. Vitest-jsdom: modaali avaus, feature-lisäys, status-vaihto, sulkeminen ✕/Esc/backdrop. Käyttäjä: järjestäjä. | V61,V62,T76,T107,T108 |
| T110 | x | AreasAPI `server/routes/areas.ts`. SQLite: `areas`-taulu (id TEXT PK, name, center_lat, center_lng, width_m, height_m, rotation REAL DEFAULT 0, markdown_description TEXT, status TEXT DEFAULT 'suunniteltu', hash_code TEXT UNIQUE NOT NULL) + `area_features`-taulu (id TEXT PK, area_id TEXT FK, name, center_lat, center_lng, width_m, height_m, rotation REAL DEFAULT 0, color TEXT). Routes: `GET /api/areas` (auth), `POST /api/areas` (järjestäjä+), `PUT /api/areas/:id` (järjestäjä+, sisältää features[]), `DELETE /api/areas/:id` cascade features, `GET /api/areas/by-hash/:hash` (auth). Bun integraatiotestit: CRUD roundtrip + hash-haku + cascade delete. Käyttäjä: järjestäjä hallinnoi, huoltopisteryhmä lukee. | T76,T41,T36,V18 |
| T111 | x | HuoltoPiste jako + tulostusnäkymä. `/a/<hash>` URL-reitti → koko kartta + map.flyTo(area.center,18) + readonly-modaali (ohjeteksti + feature-lista nimillä + "Tulosta"-nappi). `window.print()` → `@media print` CSS: piilottaa #toolbar,#left-panel,#route-bar,#btn-layer; näyttää `.area-print-content` (area.name h1, markdown ohjeistus, feature-taulukko nimillä+väreillä). Markdown→HTML: `marked`-kirjasto (lisätään jos ei vielä), tai vanilla regex minimilistan kera. Vitest-jsdom: tulosta-nappi olemassa, markdown renderöityy HTML:ksi. Playwright: `/a/<hash>` avaa oikean alueen + flyTo kutsutaan. Käyttäjä: huoltopisteryhmä (tulostus), järjestäjä (jako). | T76,T108,T109,T110,V42 |
| T77 | ✓ | Pätkä klikattavissa kartalta: `segment-overlay.ts` — klikkaus pätkän polylineen → avaa `SegmentDetailsModal` (T69) suoraan kartalta. Ei tarvitse etsiä listasta. Vitest ei riitä — Playwright: klikkaa pätkää kartalla → modaali avautuu. Käyttäjä: järjestäjä. | T25,T69 |
| T78 | ✓ | Talkoolaisen pätkän pituuden muokkaus kentällä: `segment-view.ts` — kokoontaitettava "✎ Muokkaa pätkän rajoja (X–Y km)" -osio → numeeriset km-inputit (alku/loppu, V32 sallii numeerisen tavan drag-repositioinnin sijaan, siistimpi mobiilissa) + validointi (loppu>alku) + Tallenna. `onEditBounds` → `updateSegment` + `updateSegmentRemote` (`PUT /api/segments/:id`) + kartta-overlay-päivitys. Server sallii (V93: talkoolainen_code===assigned_code). Järjestäjä voi yliajaa (V43). Vitest-jsdom (6 testiä) + Playwright (PUT-verifiointi). Käyttäjä: talkoolainen. | V43,V32,V93,T62 |
| T79 | . | Reaaliaikainen sync: talkoolaisen marker/segment-muutokset näkyvät järjestäjälle kartalla ilman sivun latausta. Toteutus: SSE (Server-Sent Events) tai polling (10s). `GET /api/events` stream tai `GET /api/markers?since=<timestamp>`. Client: `src/logic/realtime.ts` — subscribe, päivitä MarkerManager + SegmentOverlay. Playwright: talkoolainen kuittaa → järjestäjä näkee värimuutoksen <15s. Käyttäjä: järjestäjä (vastaanottaa), talkoolainen (lähettää). | V18,T41,T47 |
| T80 | . | Materiaalien kirjaus purkuvaiheessa: talkoolainen kirjaa paljonko materiaalia otettu mukaan ("10 keppiä", "5 rullaa nauhaa"). Kasa-toiminto: merkitsee kasapaikan kartalle (T20). Toinen henkilö merkkaa kasan kerätyksi. `segment-view.ts` purku-moodissa: "Kirjaa materiaalit" -osio. Vitest-jsdom. Käyttäjä: talkoolainen purku-vaiheessa. | V26,V28,T52,T20 |
| T81 | ✓ | Järjestäjän toolbar yksinkertaistaminen: toolbar = vain `[+ Merkki]` + `[⋯]`. (a) `index.html`: `#btn-gps` saa `data-role-hide="järjestäjä"` — piilossa järjestäjällä, näkyy talkoolaisella. (b) `src/ui/role-selector.ts`: `lockedRole`-parametri laajennetaan — järjestäjälläkin nappi `hidden` (V13: ei rooli-togglea kummallekaan; T67 teki saman talkoolaiselle). `main.ts` välittää `sessionRole` molemmille. (c) `src/main.ts` wiring päivittyy. Vitest-jsdom: järjestäjä-sessiolla toolbar näyttää vain 2 nappia; GPS ja role-toggle `hidden`. Käyttäjä: järjestäjä. | V13,V39,T67,T32 |
| T82 | ✓ | SnapshotPanel overflow-modaaliksi: `#snapshot-panel-container` poistetaan kartan yläpuolelta kiinteänä elementtinä — ei enää vie karttatilaa. `#btn-snapshot-panel` (overflow-valikossa) avaa `SnapshotModal`-modaalin (`position:fixed`, `bg-card`, `border-radius:14px`, `min(480px,92vw)`, `max-height:80vh` scrollable, backdrop-klikki + Esc sulkee). `src/ui/snapshot-panel.ts`: muokkaa renderöimään modal-containerin eikä inline-containerin. `index.html`: poista `#snapshot-panel-container` toolbarin alta. `main.ts` wiring päivittyy. §K-sopimus päivitetään DESIGN.md:hen (SnapshotModal korvaa SnapshotPanel inline). Vitest-jsdom: overflow → Varmuuskopiot-klikki → modal näkyy; backdrop-klikki → sulkee; kartan yläpuolella ei kiinteää paneelia. Käyttäjä: järjestäjä. | T50,T57,T73 |
| T83 | ✓ | `SignTemplate.favorite: boolean` + suosikki-toggle UI. `src/logic/sign-library.ts`: lisää `favorite` kenttä `SignTemplate`-interfaceen, `listFavorites(library): SignTemplate[]` (filter `favorite:true`), `createTemplate` hyväksyy `favorite` osana `Omit<SignTemplate,'id'>`. `src/ui/sign-library-panel.ts`: `seedDefaults` asettaa default-4 `favorite:true`, uudet custom `favorite:false`. Per-template ⭐-toggle-nappi (`min-height:44px`) — klikki kutsuu `updateTemplate(lib, id, {favorite:!t.favorite})` + re-render + onChange. ⭐ filled = suosikki, ⭐ outline = ei. Vitest-pure (listFavorites, createTemplate favorite-kenttä) + Vitest-jsdom (toggle muuttaa favorite, onChange kutsutaan). Käyttäjä: järjestäjä. Avaa: T84. | V10,T22 |
| T84 | ✓ | Floating picker käyttää kirjastoa (suosikit) + MarkerType laajenee string:ksi. (a) `src/logic/types.ts`: `MarkerType = string` — broadening, vanhat 4 string-literaalia toimivat edelleen. (b) `src/map/icons.ts`: `createSignIcon(type: string, bearing, status, color?: string, shortLabel?: string)` — default-4 switch kuten ennen; tuntematon type käyttää `color ?? '#94a3b8'` ja `shortLabel ?? '?'`; ei upcoming-dashia custom-tyypeille. (c) `src/ui/place-mode.ts`: konstruktori saa `library: SignLibrary`-parametrin, `openPicker()` rakentaa `floatingPicker.innerHTML` lazy `listFavorites(library)`:stä joka kutsukerralla — ei staattinen SIGN_TYPES. Napit: `class="sign-type-btn" data-type="${t.id}" data-color="${t.color}" data-short="${t.shortLabel}"`. (d) `src/main.ts`: välittää `signLibrary` PlaceMode-konstruktoriin. Playwright: dblclick kartalla → picker näyttää kirjaston suosikit (päivitä e2e/critical-paths.spec.ts "dblclick kartalla" -testi). Käyttäjä: järjestäjä. | V10,T83 |
| T85 | ✓ | Poista `+ Merkki` toolbar-nappi + PlaceMode siivous. (a) `index.html`: poista `#btn-add-sign`-elementti ja `#place-mode-label`-span. (b) `src/ui/place-mode.ts`: poista `btnAddSign`, `placeModeLabel`, `openDropdown()`, `closeDropdown()`, `isDropdownOpen()` ja `#sign-type-dropdown`-klikki-handler — PlaceMode hallinnoi enää vain `#floating-picker`-logiikan. (c) `src/style.css`: poista `.place-mode-label` ja `.btn-add-sign.place-mode` säännöt. (d) `src/main.ts`: poista PlaceMode-wiring joka viittasi `onOpenPanel`-callbackiin. Playwright: varmista "Merkki kartalle" E2E-testi toimii ilman btn-add-sign:iä. Käyttäjä: järjestäjä. | T84,V10 |
| T87 | ✓ | XSS-korjaus place-mode.ts: `openPicker()` — lisää `escapeHtml()` `t.id`, `t.color`, `t.shortLabel`, `t.label` -arvoille `innerHTML`-templateen. Importtaa tai kopioi `escapeHtml`-funktio `sign-library-panel.ts`:stä (tai siirrä yhteiseen apumoduuliin). Vitest-jsdom: XSS-payload-label ei luo DOM-elementtiä pickerin sisällä. Käyttäjä: järjestäjä. | V44,B21 |
| T86 | ✓ | Poista GpsDrivePanel järjestäjältä: `main.ts` — alusta `GpsDrivePanel` vain kun `talkoolainenCode` truthy; muuten `gpsDrivePanel` pysyy `null`. Paneeli pysyy `hidden`, `?.update()` no-op. Vitest-jsdom: järjestäjä-sessiolla `#gps-drive-panel` pysyy `hidden` vaikka unset-merkkejä on. Käyttäjä: järjestäjä. | V45,B20,T81 |
| T89 | ✓ | Custom-template ikoni-korjaus: (a) `icons.ts circleSvg()` default-case: vaihda `arrow='→'` → `arrow = shortLabelOverride ?? '?'` — ympyrä näyttää lyhennettä. (b) `src/logic/types.ts`: lisää `SignMarker`-tyyppiin `color?: string` ja `shortLabel?: string`. (c) `MarkerManager.add()` saa `color?: string, shortLabel?: string` parametrit → asettaa `marker.color` + `marker.shortLabel`. `addLeafletMarker()`, `updateBearing()`, `updateType()`, `updateStatus()`, `dragend` kutsuvat `createSignIcon(m.type, m.bearing, m.status, m.color, m.shortLabel)`. (d) `place-mode.ts` välittää `btn.dataset.color` + `btn.dataset.short` `markerManager.add()`-kutsussa. (e) `apiPost()` lähettää `color` + `short_label` — backend tallentaa (API-muutos). (f) `fetchMarkers()` palauttaa `color`/`short_label` — mappataan `SignMarker`:iin `init()`:ssä. Vitest-pure (add tallentaa color+shortLabel), Playwright (dblclick custom → oikea lyhenne kartalla). Käyttäjä: järjestäjä. | V47,T8,T84,T64 |
| T90 | ✓ | Väri-picker layout-korjaus: `sign-library-panel.ts buildForm()` — väri-picker rivi: lisää `min-width:0` lyhenne-inputtiin (`flex:1;min-width:0`) ja poista `<span>Väri</span>` teksti (color-input on selkeä ilman labelia). Vitest-jsdom: form renderöityy ilman leveysvirheitä. Käyttäjä: järjestäjä. | B24 |
| T92 | ✓ | updateType color/shortLabel: `MarkerManager.updateType(id, newType, color?, shortLabel?)` — lisää parametrit, aseta `m.color = color ?? undefined`, `m.shortLabel = shortLabel ?? undefined` ennen `createSignIcon`-kutsua. Päivitä `marker-list.ts` `marker-type-select` käyttämään `SignLibrary`-templateja SIGN_TYPES:n sijaan (vaatii library-ref `renderMarkerList`-parametriksi). Vitest-jsdom (type-change custom-tyypillä → oikea color+shortLabel). Käyttäjä: järjestäjä. | V47,V48,T89,T38 |
| T91 | ✓ | Uusi malli suosiksi automaattisesti: `sign-library-panel.ts saveForm()` — `createTemplate(..., {favorite: false})` → `{favorite: true}`. Uusi malli näkyy heti floating pickerissä. Vitest-jsdom: createTemplate kutsutaan `favorite:true`. Käyttäjä: järjestäjä. | T83,T84 |
| T88 | ✓ | SignLibrary persistointi: `saveLibrary(library): void` + `loadLibrary(): SignLibrary` `src/logic/sign-library.ts`:ään. Format `{version:1,templates:SignTemplate[]}`. `createSignLibrary()` kutsuu `loadLibrary()` ensin — seedaa defaultit vain jos tyhjä. `SignLibraryPanel.onChange` kutsuu `saveLibrary()` joka mutaation jälkeen (fav-toggle, save-form, delete). Korruptoitunut data → silent reset + `createSignLibrary()` (V14, V46). Vitest-pure (save/load roundtrip, korruptoitunut → reset). Käyttäjä: järjestäjä. | V46,V14,T8,T22,T83 |
| T93 | ✓ | Merkkikirjasto create/edit -modaali + iconId + ikoni-picker. (a) `src/logic/sign-library.ts`: lisää `iconId?: string` SignTemplate-interfaceen; saveLibrary/loadLibrary käsittelee kentän. (b) `src/ui/sign-library-panel.ts`: "Uusi malli"- ja ✎-nappi avaavat modaalin inline-formin sijaan. Modal: `position:fixed`, `min(480px,92vw)`, backdrop, Esc+✕ sulkee. (c) Modal-lomake: ikoni-grid (~25 kuratoitua Lucide-ikonia — nuolet, varoitukset, POI-symbolit), label, shortLabel, color, description. Valittu ikoni korostuu gridissä. (d) `src/map/icons.ts createSignIcon()`: jos `iconId` annettu, renderöi Lucide SVG ympyrässä (shortLabel fallback). (e) Vitest-pure (iconId tallentuu roundtrip, V50). Vitest-jsdom (modal avaus ✎-klikistä, sulkeminen ✕/Esc/backdrop, tallentaa iconId). Käyttäjä: järjestäjä. | V10,V44,V47,V50,T8,T22,T88 |
| T94 | ✓ | Pätkäluonti-modal + cursor-palaute + endpoint-snap + overlap-esto. (a) `src/ui/segment-panel.ts`: "Luo uusi pätkä" -nappi avaa modaalin heti (ei vain statusEl-teksti). Modal-tilakone: `idle → vaihe1 (klikkaa aloituspiste) → vaihe2 (klikkaa lopetuspiste) → tiedot`. Vaihe1/2-tilassa modal näyttää ohjetekstin + progress-indikaattorin, "Peruuta" sulkee. (b) Cursor: luontimoden aikana `map._container.style.cursor = 'crosshair'`, palautetaan `''` luonnin päättyessä tai peruutuksessa. (c) `src/map/segment-overlay.ts`: luontimoden aikana näytä olemassa olevien pätkien aloitus- ja lopetuspisteet `L.circleMarker` (r=8, korostettu väri) — klikattavissa, snappaa kyseisen pätkän startDist/endDist arvoon. (d) `src/logic/segments.ts`: `validateNoOverlap(store: SegmentStore, routeId: string, startDist: number, endDist: number): boolean` — false jos overlap olemassa olevaan pätkään. (e) Kun molemmat pisteet klikattu ja validoitu: modal siirtyy tiedot-vaiheeseen (nimi, kuvaus, varusteet — sama sisältö kuin T69 SegmentDetailsModal). Tallenna luo segmentin. (f) Vitest-pure (validateNoOverlap: ei overlapia, täysi overlap, osittainen overlap, vierekkäiset OK). Vitest-jsdom (modal tilakone, peruuta palauttaa idle). Playwright (luontiflow: cursor muuttuu, endpoint-snap, overlap-virhe). Käyttäjä: järjestäjä. | V11,V31,V32,V49,T25,T56,T69 |
| T95 | x | Pätkäprogress % sivupalkkilistaan — korvattu T141:llä, ks. B60. `getSegmentProgress()` jäi kutsumatta mistään (T99-rivinuudistus pudotti sen), ei koskaan näkynyt järjestäjälle. | T13,T25,T15 |
| T96 | ✓ | Suunniteltu-status: täysi opacity + dasharray-erottelu. `src/map/icons.ts`: poista `opacity = status === 'suunniteltu' ? 0.45 : 1.0` — korvaa: kaikki statusit `opacity:1.0`. Suunniteltu: `fill-opacity:0.55` + `stroke-dasharray:"4 2"`. Muut: `fill-opacity:1.0`, ei dasharray. Status-piste: suunniteltu piilotettu (ennallaan). Testit: Vitest-pure (SVG-string sisältää dasharray suunniteltu-statuksella, fill-opacity:0.55; asetettu-statuksella ei dasharray, fill-opacity:1.0). Käyttäjä: kaikki. | V51,T10,T23 |
| T97 | ✓ | Drag ei autorotate jos bearing manuaalisesti asetettu. (a) `src/logic/types.ts`: lisää `bearingManual?: boolean` `SignMarker`-tyyppiin. (b) `MarkerManager.add()`: `marker.bearingManual = false`. (c) `MarkerManager.updateBearing()`: aseta `m.bearingManual = true`. (d) `dragend` `src/map/markers.ts`: `if (m.bearingManual) { /* säilytä m.bearing */ } else { bearing = bearingAtIndex(...) }` — laske kuitenkin uusi lat/lon + routeIds. (e) `apiPut` sisältää `bearing_manual` kentän (best-effort). Testit: Vitest-pure (dragend bearingManual=true → bearing muuttumaton; bearingManual=false → bearing päivittyy). Käyttäjä: järjestäjä. | V52,V15,T37,T40 |
| T99 | ✓ | Segment-panel UX-parannus: rivi → `[nimi][km][···]`. `src/ui/segment-panel.ts buildSegmentRow()`: poista editPtsBtn/deleteBtn/buildAssignSection-kutsu — jätä vain `segment-info` (flex:1 truncated) + `segment-km` (flex-shrink:0) + `btn-segment-details-open` (···, 44×44px). `render()`: poista markers/getMarkers-kutsu. `openDetailsModal()`: lisää (5) assign-osio (`buildAssignSectionModal()`), (6) "Muokkaa pisteitä kartalla" -nappi + (7) "Tallenna muutokset" -footer (confirm-tausta, koko leveys, kutsuu `onUpdate()` + `closeModal()`), (8) danger-zone "Poista pätkä" (pieni, `window.confirm()` ennen poistoa). Poista `buildAssignSection()`. CSS: `.segment-item` flex-row + `min-width:0`; `.segment-km` oma flex-item; `.btn-segment-details-open` transparent. Vitest-jsdom: päivitetty `t26-segment-assign.test.ts` — avaa modaali ennen assign-query. Käyttäjä: järjestäjä. | V54,V57,B27,B28 |
| T100 | ✓ | Persistence-bugi fix: `src/main.ts` kutsu `segmentOverlay.update(segmentStore)` heti `new SegmentOverlay(...)` jälkeen. `openDetailsModal()` save-nappi kutsuu `this.onUpdate()` tallennuksen jälkeen. | V55,V56,B27,B28 |
| T101 | ✓ | Järjestäjän bulk status override: marker-modal levenee `min(560px,92vw)` `max-height:82vh`. `renderMarkerList` lisää checkbox per rivi (järjestäjä). BulkStatusToolbar sticky header-alla: [☐ Valitse kaikki] + [status `<select>` kaikki 5 tilaa] + [Aseta valituille (N)] — confirm-tausta N>0, field-tint disabled N=0. `bulkSetStatus(ids: string[], status: MarkerStatus, manager: MarkerManager): Promise<void>` `src/map/markers.ts`. CSS: `.bulk-status-toolbar`, `.marker-item-checkbox`, `.btn-bulk-apply` `src/style.css`. DESIGN.md §K BulkStatusToolbar-sopimus kirjoitettu. Vitest-jsdom: select all → kaikki valittu; "Aseta valituille" kutsuu manager.updateStatus oikeilla id:illä ja oikealla statuksella. Käyttäjä: järjestäjä. | V9,T10,T47 |
| T102 | x | Merkkilista-haku ja tyyppifilttri: haku-inputkenttä modaalin yläosassa (placeholder "Hae merkki..."), filtteröi reaaliajassa — match merkin label TAI km-väli. Tyyppifilttri: dropdown per merkityyppi. Filtterit yhdisteltävissä: haku + tyyppi. Nollaus: ✕-nappi → tyhjentää haku + tyyppi + resetoi checkboksivalinnat. **Bulk kohdistuu vain näkyviin (filtteröityihin) riveihin** — "Valitse kaikki" valitsee vain listatut. `src/ui/marker-list.ts`. ✓ `.marker-filter-bar` (`.marker-search` + `.marker-type-filter` + ✕-nollaus); bulk-invariantti `visibleCheckboxes`-helperin kautta; 12 Vitest-jsdom (3 kriittistä bulk-invariantille), tyhjä→"Ei tuloksia". Käyttäjä: järjestäjä. | V9,T10,T101 |
| T103 | x | Kuva ja lisäkuvaus per merkki: `SignMarker`-tyyppiin `images?: string[]` + `description?: string`. Backend: `PUT /api/markers/:id` hyväksyy `description`. `POST /api/markers/:id/images` (multipart) → tallentaa kuvan, palauttaa URL. Näytetään MarkerDetailModal:ssa (T105) — ei suoraan listarivillä. Järjestäjä: textarea kuvaukelle + "Lisää kuva" -nappi. Talkoolainen: readonly kuva + kuvaus. Kuvat lazy-load — jos offline, näytä `[kuva ei saatavilla]`-placeholder eikä spinner. Vitest-pure (SignMarker-tyyppi hyväksyy kentät), Bun integraatiotesti (PUT description + image upload URL). Käyttäjä: molemmat. | V9,T10,T47,T105 |
| T104 | ✓ | Merkkilista kompakti yksirivinen layout + poista locationNote-input listasta + nimeä modaalin otsikko "Merkkilista". Merkkirivi: yksi rivi `[checkbox?][icon][km][status-badge][type-label]` — ei note-inputia eikä toimintopainikkeita. Rivin klikkaus → avaa MarkerDetailModal (T105). LocationNote editointi siirtyy T105:een. `src/ui/marker-list.ts` + `src/style.css`. Vitest-jsdom: rivi yksirivinen, note-input puuttuu, otsikko "Merkkilista", rivin klikkaus kutsuu openDetail-callback. Käyttäjä: molemmat. | V9,T10,T105 |
| T105 | ✓ | MarkerDetailModal: klikkaamalla merkkilistariviä avautuu modaali (järjestäjä + talkoolainen). Rakenne: otsikko (type + km), status-badge, locationNote-input (auto-save blur/Enter, järjestäjä editoi / talkoolainen editoi omalla pätkällä), description (T103-valmis placeholder textarea), kuvat (T103-valmis placeholder). Järjestäjälle: type-select + delete-nappi. Talkoolaiselle: status-napit (HUOM: talkoolaishaara siirretty T74 CheckIn-bottom-sheetiin — T105-modaali jää järjestäjälle). DOM: `document.body`-lapsi, `position:fixed`, `bg-card`, `border-radius:14px`, `width:min(480px,92vw)`, `max-height:80vh`, backdrop sulkee. Sulkeminen: ✕ + Esc + backdrop. `src/ui/marker-detail-modal.ts` (uusi). Vitest-jsdom: avautuu oikealla merkillä, locationNote auto-save, sulkeminen kolmella tavalla. Käyttäjä: molemmat. | V9,T10,T11 |
| T98 | ✓ | Merkin poisto vain modaalin kautta. `src/map/marker-interaction.ts showContextMenu()`: poista `deleteBtn` elementti + sen addEventListener — context-menu sisältää enää vain "↻ Käännä" -napin. Modaalin (marker-list.ts) delete-nappi säilyy ennallaan. Testit: Playwright (context-menu ei sisällä ✕-nappia, `marker-ctx-delete` ei löydy); Vitest-jsdom (marker-list delete toimii). Käyttäjä: järjestäjä. | V53,V15,T37 |
| T69 | ✓ | SegmentDetailsModal: "Lisätiedot & varusteet" -nappi pätkärivillä avaa modaalin järjestäjälle. Korvaa `buildDetailsSection()`-inline-toggle `src/ui/segment-panel.ts`:ssä. Modaali sisältää: (1) pätkän nimi-kenttä (`displayName`, `<input>`, auto-save blur/Enter → `updateSegment` + `updateSegmentRemote`), (2) järjestäjän kuvaus-textarea (auto-save `change` → `updateSegment` + `updateSegmentRemote`), (3) merkit pätkällä readonly-lista statuksilla (`getMarkersForSegment`-tulos, status-värit §C DESIGN.md), (4) manuaali-varusteet add/remove/edit (`EquipmentItem[]`, sama logiikka kuin poistuva `buildEquipmentManualSection`). Modal DOM: `document.body`-lapsi, `position:fixed`, `bg-card`, `border-radius:14px`, `box-shadow:0 16px 48px rgba(0,0,0,0.5)`, backdrop `overlay`+`blur(2px)`, `width:min(480px,92vw)`, `max-height:80vh` scrollable. Sulje: ✕-nappi (`aria-label:"Sulje"`, `min-height:44px`) + Escape + backdrop-klikki — auto-save on change, ei hylkäysdialogi. §K-sopimus lisätty DESIGN.md §K SegmentPanel-osioon. Vitest-jsdom: avaus, sulkeminen (✕/Esc/backdrop), displayName-save, description-save, equipment-lisäys, equipment-poisto, merkkilista statuksilla. Käyttäjä: järjestäjä. | V38,T25,T27 |

| T112 | x | `src/logic/area-geometry.ts` — lisää geometria-apufunktiot: (a) `cornersToRect(cornerA: LatLng, cornerB: LatLng): {centerLat, centerLng, widthM, heightM}` — draw-by-drag kahdesta kulmasta suorakulmion parametreiksi (widthM = haversine EW-etäisyys, heightM = haversine NS-etäisyys, center = midpoint). (b) `latLngToLocal(lat, lng, centerLat, centerLng): {uM: number, vM: number}` — konvertoi lat/lng → metrikoordinaatit (North=+v, East=+u) aspektikorjauksella. (c) `localToLatLng(uM, vM, centerLat, centerLng): LatLng` — käänteinen. Tarvitaan nurkkavetämisen inversiolle. Vitest-pure: `cornersToRect` roundtrip `cornersFromRect`-kanssa (±1m), `latLngToLocal` ja takaisin (±1m). Käyttäjä: järjestäjä. | V66,T76,T107 |
| T113 | x | `src/map/area-overlay.ts` — lisää `updateOne(area: AreaMarker): void`. Poistaa vain ko. `area.id`:n layerit + dragHandles, kutsuu `renderArea(area)`. Ei `clear()` koko kartalle. Kutsutaan drag-callbackeista (corner-drag, rotation-drag, move-drag). Olemassa oleva `update(areas[])` ennallaan bulk-päivityksille. Ei pakollinen E2E-testi — `updateOne` katetaan yksikkötestaamisella mockaamalla `areaLayers`-map. Käyttäjä: järjestäjä. | V67,T108 |
| T114 | x | `src/map/map-rect-editor.ts` (uusi) — `MapRectEditor`-luokka jaetuksi karttainteraktioksi. (a) `startDraw(onDone: (rect: {centerLat,centerLng,widthM,heightM}) => void)`: `map.dragging.disable()`, `mousedown` tallentaa aloituspisteen, `mousemove` päivittää esikatselupolygonin (`L.polygon`), `mouseup` kutsuu `cornersToRect(start,end)` → `onDone(rect)` → poistaa esikatselu, `map.dragging.enable()`. Esc peruuttaa. (b) `startEdit(id: string, rect: EditableRect, onUpdate: (r: EditableRect) => void, onDone: () => void)`: luo 4 nurkka-L.Marker (`draggable:true`, ympyräikoni `.area-corner-handle`) + 1 rotaatio-L.Marker (armin kärki, `.area-rotation-handle`). Corner-drag: `latLngToLocal` kaikille kulmille → päivitä vedetty → `localToLatLng` → `cornersToRect` → `onUpdate`. Rotation-drag: `latLngToLocal(armiPiste, center)` → `atan2(uM, vM)` → `onUpdate({...rect, rotation})`. Kaikki handleit poistetaan `stopEdit()`:ssä. (c) `stopEdit(): void`. Tyypit: `EditableRect = {centerLat,centerLng,widthM,heightM,rotation}`. Riippuu: `cornersFromRect`, `cornersToRect`, `latLngToLocal`, `localToLatLng` (T112). E2E: `e2e/area-interaction.spec.ts` — draw-by-drag luo polygonin, edit-tila näyttää handlet, corner-drag muuttaa kokoa, rotation-drag muuttaa rotaatiota. CSS: `.area-corner-handle` (sininen ympyrä), `.area-rotation-handle` (amber). DESIGN.md §K päivitetään. Käyttäjä: järjestäjä. | V66,V67,T112,T113 |
| T115 | x | Wire `MapRectEditor` → `AreaMarker`: draw + edit. (a) `src/ui/area-panel.ts` "Lisää alue": korvaa `window.prompt`+click-flow → `onEnterDrawMode`-callback (uusi) → `mapRectEditor.startDraw(rect => createAreaMarker(rect))`. Nimi kysytään dragin JÄLKEEN floating-inputilla. (b) `src/map/area-overlay.ts renderArea()`: polygon dblclick → `mapRectEditor.startEdit(area.id, area, onUpdate, onDone)` → `onUpdate` kutsuu `updateOne(area)` + `onAreaMove?.(area)`. Esc / ulkoklikki → `stopEdit()`. (c) `src/main.ts`: luo `MapRectEditor(map)`, välitä `AreaPanel`- + `AreaOverlay`-konstruktoreille. Vitest-jsdom: `onEnterDrawMode` kutsutaan "Lisää alue" -klikistä (ei `window.prompt`). Käyttäjä: järjestäjä. | V64,V65,V66,V67,T114 |
| T116 | x | Wire `MapRectEditor` → `AreaFeature`: itsenäinen sijoitus + edit. (a) `src/ui/area-panel.ts` "Lisää sisäinen alue" -nappi modaalissa: kutsuu `onEnterDrawMode` → `mapRectEditor.startDraw(rect => addFeature(area, newFeature(rect)))`. Feature sijoittuu piirrettyyn kohtaan — ei parent-alueen centeriin (V68). (b) `src/map/area-overlay.ts renderFeature()`: feature-polygon dblclick → `mapRectEditor.startEdit(feature.id, feature, onUpdate, onDone)` → `onUpdate` päivittää parent-alueen `features`-arrayta → `updateOne(parentArea)` + backend PUT. (c) Feature-polygonilla myös move-handle (⊕). Vitest-pure: `addFeature(area, feat)` missä `feat.centerLat` ≠ `area.centerLat` (itsenäinen sijoitus). Käyttäjä: järjestäjä. | V68,T114,T115 |
| T119 | x | AreaFeature inline edit sivupalkissa. `src/ui/area-panel.ts` feature-sub-list rivi: lisää `✎`-nappi (`.btn-feat-inline-edit`, `min-width:44px;min-height:44px`, `color:text-muted`) + dblclick feature-nimelle = sama. Inline-edit tila: rivi saa `background:var(--hover);border-left:2px solid var(--accent)`. Elementit: `[swatch] [name-input autofocus field-tint flex:1] [color-select]`. Enter/blur → tallentaa (`onAreaUpdate(area)`). Escape → palauttaa alkuperäisen nimen. `src/style.css`: `.area-feature-sublist .feat-row.editing` tyyli. Vitest-jsdom: ✎-klikki vaihtaa rivin editing-tilaan, name-input saa fokuksen, Enter tallentaa kutsuen onAreaUpdate, Escape peruu. Käyttäjä: järjestäjä. | T109,T118 |
| T120 | x | AreaFeature karttanimet zoom-riippuvaisesti. `src/map/area-overlay.ts`: `renderFeature()` lisää `L.Tooltip` jokaiselle nimelliselle featurelle (`feature.name` olemassa ja ei-tyhjä): `layer.bindTooltip(feature.name, {permanent:true, direction:'center', className:'area-feature-label'})`. `updateFeatureLabels(zoom: number)`: iteroi kaikki feature-tooltip-layerit, aseta `opacity: zoom >= 16 ? 1 : 0`. `map.on('zoomend', () => updateFeatureLabels(map.getZoom()))` + kutsu heti `update()`-lopussa. `src/style.css`: `.area-feature-label { font-size:11px; font-weight:600; color:#fff; text-shadow:0 1px 3px rgba(0,0,0,0.9); background:transparent; border:none; padding:0; white-space:nowrap; pointer-events:none }` — ei CSS custom propertya (Leaflet-DOM ei peri `:root`-tokeneja). Vitest-jsdom: feature jolla name → tooltip luodaan; feature ilman name → ei tooltippia. Playwright `e2e/area-interaction.spec.ts`: zoom ≥16 → label näkyvissä; zoom <16 → label piilotettu. Käyttäjä: järjestäjä + huoltopisteryhmä. | T108,T109 |
| T118 | ✓ | Area details -modaali UX-yhtenäistys — B41+B42. `src/ui/area-details-modal.ts`: (a) poista `buildNameSection` "Tallenna"-nappi → auto-save blur/Enter. (b) poista `buildSizeSection` "Tallenna"-nappi → auto-save blur/change. (c) poista `buildDescSection` "Tallenna kuvaus" -nappi + vaihda label "Ohjeteksti (Markdown)" → "Ohjeteksti" → auto-save blur. (d) lisää modal-footer: "Poista alue" (`modal-btn-destructive`, `window.confirm()` → `callbacks.onAreaDelete(area.id)` → `this.close()`) + "Sulje"-nappi (valinnainen, ✕ headerissa riittää). `AreaDetailsCallbacks` laajenee: `onAreaDelete: (id: string) => void`. `area-panel.ts` välittää `onAreaDelete`-callback. Vitest-jsdom: blur nimi-inputissa → onAreaUpdate kutsutaan; "Poista alue" → confirm → onAreaDelete kutsutaan; cancel confirm → ei kutsuta. Käyttäjä: järjestäjä. | V62,V72,V73,B41,B42 |
| T117 | x | MapRectEditor korjaukset — B38/B39/B40. (a) `map-rect-editor.ts`: lisää `isEditing(): boolean` → `return this.editId !== null`. `startEdit` lisää `map.on('click', onMapClickOutside)` — klikki polygon ulkopuolella → `stopEdit()` + `editOnDone?.()`. `stopEdit()` poistaa map-klikki-handlerin. (b) `rotationArmTip`: vaihda CCW → CW: `rU = uM*cos(rad) + vM*sin(rad)`, `rV = -uM*sin(rad) + vM*cos(rad)`. (c) `onCornerDrag`: poista `cornersToRect`-kutsu, laske rotatoidussa kehyksessä: center = midpoint world-koord → `latLngToLocal(dragged, center)` + `latLngToLocal(opposite, center)` → rotate by `-rect.rotation` (CCW 2D matriisi) → `widthM = 2×|frame.uM|`, `heightM = 2×|frame.vM|`. Lisää apufunktio `rotateLocal(u, v, deg)`. (d) `area-overlay.ts` polygon-click: tarkista `mapRectEditor?.isEditing()` ennen `onAreaClick`-kutsua (V69). Vitest-pure: rotation=90° → arm tip osoittaa itään; corner-drag rotatoidulle neliölle pitää vastakkaisen kulman paikallaan ±1m. Playwright `e2e/area-interaction.spec.ts`: edit → ulkoklikki karttaan → handleit katoavat; polygon-klikki edit-tilassa ei avaa modaalia. Käyttäjä: järjestäjä. | V69,V70,V71,T114 |
| T106 | x | Sivupalkki-uudistus: yhtenäinen section-pattern merkkikirjastolle + pätkäjakolle. (a) `index.html` `#left-panel-header`: otsikko-span → `"Työkalut"` (11px uppercase text-muted), toggle-nappi `◀/▶` pysyy oikeassa reunassa. Poista "Merkkikirjasto"-teksti headerista — se siirtyy oman osionsa section-headeriksi. (b) `src/ui/sign-library-panel.ts`: item-rivi muodoksi `[swatch+label btn flex:1] [···]` — poista ★-nappi + ×-nappi riviltä. `✎` → `···` (`.sign-lib-dots-btn`, `min-width:44px;min-height:44px`). Section-header lisätään containerin yläosaan: `[▼/▶ Merkkikirjasto]` klikattava collapse-toggle. Section-footer: `[+ Uusi merkki]` `field-tint` tausta `min-height:44px` koko leveys. Edit-modaaliin lisätään: (i) Suosikki-toggle (`<label><input type=checkbox> Näytä suosikit-pickissä</label>`, 44px touch), (ii) footer-destructive "Poista malli" (`.modal-btn-destructive`, `window.confirm()` ennen poistoa, vain ei-default-malleille). (c) `src/ui/segment-panel.ts`: poista `createBtn` (`#btn-segment-create`) header-riviltä. Header → vain `[▼/▶ Pätkäjako (N)]`. Section-footer add-nappi luodaan `render()`-metodissa listan jälkeen: `[+ Luo uusi pätkä]` `field-tint` `min-height:44px` koko leveys, klikki kutsuu `enterCreationMode()`. (d) `src/style.css`: `.left-panel-section-header` yhtenäinen tyyli (cursor:pointer, flex, align-center, 11px uppercase text-muted, border-bottom border-subtle). DESIGN.md §K LeftPanel päivitetään section-patternilla. Invariantit: V61, V62. Vitest-jsdom: sign-library-rivi ei sisällä ★- tai ×-nappia; modaali sisältää poisto-napin; pätkä-header ei sisällä create-nappia; footer-nappi löytyy. Käyttäjä: järjestäjä. | V61,V62,T73,T99,T83 |

| T121 | x | Backend — is_active + käyttäjähallinta API. (a) `server/db.ts`: lisää `is_active INTEGER NOT NULL DEFAULT 1` users-tauluun (migraatio idempotent). (b) `server/middleware/auth.ts` `requireAuth()`: session validoinnin jälkeen hae `users.is_active` → 0 = palauta 401 `{error:'account_disabled'}`. (c) `PATCH /api/admin/users/:id {is_active: 0\|1}` (admin) → päivittää users-taulun. (d) `POST /api/admin/users/:id/reset-password` (admin) → generoi uusi UUID-token → `UPDATE users SET invite_token=? WHERE id=?` → palauttaa `{inviteUrl: '/auth/invite/<token>'}`. (e) `GET /api/admin/users` palauttaa myös `is_active`-kentän. Bun integraatiotestit: deaktivoitu käyttäjä saa 401; reset luo uuden invite-linkin; PATCH aktivoi uudelleen. Käyttäjä: admin. Avaa: T122, T123. | V74,V75,V76,T36,T41 |
| T122 | x | Admin UI — /admin-sivu. Erillinen sivu karttanäkymästä. Pääsy: admin-rooli (server redirectaa `/admin` → 403 jos ei admin). Rakenne: (a) Header: "Karttamaster — Käyttäjähallinta" + logout-linkki. (b) Käyttäjälista-taulukko: display_name, username, rooli, created_at, tila (aktiivinen/deaktivoitu) + toimintopainikkeet per rivi. (c) Per käyttäjä: "Deaktivoi"/"Aktivoi"-nappi (`PATCH /api/admin/users/:id`) + kopiointinappi invite-linkille. (d) "Kutsu uusi järjestäjä" -nappi yläosassa: `POST /api/admin/invites` → näyttää invite-URL kopiointinapilla. Yksinkertainen tyyli — ei karttaa, ei left-panel, toimii mobiililla. `src/admin.ts` + `admin.html` (erillinen entrypoint). Vitest-jsdom: taulukon renderöinti, deaktivointi-napin klikki kutsuu PATCH, kutsu-napin klikki kutsuu POST invites. Käyttäjä: admin. | V74,V75,T121,T36 |
| T123 | x | Salasanan reset admin-sivulla. Per käyttäjärivi (aktiiviset käyttäjät): "Resetoi salasana" -nappi. Klikki → `POST /api/admin/users/:id/reset-password` → palauttaa invite-URL → näytetään kopiointinapilla "Lähetä WhatsAppilla". Käyttäjä avaa linkin → sama rekisteröintilomake kuin invite-flowssa (`/auth/invite/<token>`, T36 valmis). Vitest-jsdom: napin klikki kutsuu reset-endpointtia, URL näytetään UI:ssa. Käyttäjä: admin. | V76,T121,T122,T36 |
| T124 | x | GPKG-export/import geojson-muunnin: `server/gpkg/geojson.ts`. `markersToGeoJSON(rows): FeatureCollection` — Point-geometria (lat/lon), properties `{id, type, bearing, description}` (ei color/status/routeIds/locationNote/images). `geoJSONToMarkers(fc): Array<{id,type,bearing,description,lat,lon}>` käänteinen parsinta. EI src/-importteja (server/-sääntö) — omat tyypit tässä tiedostossa. Bun-testit: round-trip marker→geojson→marker säilyttää kentät. Käyttäjä: järjestäjä. Avaa: T125,T126. | §C |
| T125 | x | GPKG export-endpoint: `server/gpkg/convert.ts` — ogr2ogr subprocess wrapper, GeoJSON-välitiedosto → `.gpkg` (1 layer "kyltit"). `GET /api/gpkg/export` (`server/routes/gpkg.ts`): hakee kaikki markerit DB:stä, ajaa `markersToGeoJSON`, konvertoi, palauttaa tiedoston `Content-Disposition: attachment`. Bun-testi: `Bun.which('ogr2ogr')` puuttuu → skip; löytyy → export tuottaa validin .gpkg:n (tarkistus `ogrinfo`). Käyttäjä: järjestäjä. | T124 |
| T126 | x | GPKG import-endpoint: `POST /api/gpkg/import` (`server/routes/gpkg.ts`) — multipart upload `.gpkg`, ogr2ogr → GeoJSON, `geoJSONToMarkers`, upsert id:n perusteella: id löytyy DB:stä → päivitä `type/bearing/lat/lon/description` (status/routeIds/muut koskematta — lat/lon lisätty alkuperäiseen spec-tekstiin nähden, muuten kaverin QGIS:ssä siirtämä merkki ei liikkuisi); id ei löydy → uusi rivi, `status='suunniteltu'`, `routeIds=[]`. Bun-testi (skip jos ei ogr2ogr): upsert olemassa olevalle id:lle säilyttää statuksen+routeIds mutta päivittää sijainnin, uusi id luo suunniteltu-statuksen. Käyttäjä: järjestäjä. | T124 |
| T127 | x | GPKG UI-napit: järjestäjän näkymään (`src/ui/left-panel.ts` tms.) "Vie GPKG" (linkki `/api/gpkg/export`) + "Tuo GPKG" (file input → POST `/api/gpkg/import`, onnistumis/virheilmoitus). Vitest-jsdom: nappien klikki kutsuu oikeat endpointit. Käyttäjä: järjestäjä. | T125,T126 |
| T128 | x | Dockerfile: `apk add --no-cache gdal-tools` production-stageen (paketti `gdal` yksin ei sisällä ogr2ogr-CLI:tä Alpinessa, `gdal-tools` sisältää). Verifioitu `docker build` + `docker run` täydellä round-tripillä (markersToGeoJSON→geoJSONToGpkg→gpkgToGeoJSON) samassa Alpine-imagessa. Paikallinen dev ei vaadi gdal-asennusta — testit skippaa jos `Bun.which('ogr2ogr')` puuttuu (T125,T126 -testeissä jo). | T125,T126 |
| T129 | x | Poista bearing logiikkakerroksesta: `calcBearing`+`bearingAtIndex` pois `src/logic/bearing.ts`:sta (SÄILYTÄ `haversineDistance`/`nearestPointIndex`/`buildRoutePoints`/`routePositionPct` — laajasti käytetty reittigeometria, ei bearing-spesifistä, käyttäjät: route-bar.ts, segment-overlay.ts, segment-panel.ts, marker-list.ts, multi-route.ts). Poista `SignMarker.bearing`+`bearingManual` `src/logic/types.ts`:sta. Vitest-pure. | V1,V15,V41,V52,V53,V60 |
| T130 | x | Poista merkin kääntö kartalta: poista `src/map/marker-interaction.ts` KOKONAAN (koko tiedosto olemassa vain rotaatiota varten — arm/disarm/context-menu/drag-rotate, ei muuta säilytettävää). `src/map/markers.ts`: poista interaction-kenttä + käyttö (saveBearing, updateBearing, armMarker, add():n auto-arm, dragendin bearing-uudelleenlaskenta — säilytä lat/lon/routeIds-päivitys), nearestRouteAssignment():n bearing-paluuarvo, apiPost/apiPut bearing-kentät. `src/map/icons.ts`: poista rotate-transform + bearing-parametri kaikista signatuureista ja kutsupaikoista. Playwright: varmista e2e "Merkki kartalle"/"Drag-to-move" ei regressoi, poista rotate-spesifiset. Riippuu T129. | T129 |
| T131 | x | Poista kääntö-UI: `src/ui/marker-detail-modal.ts` "↻ Käännä" -nappi + click-handler (`MarkerManager.armMarker()`-kutsu). Vitest-jsdom. Riippuu T130. | T130 |
| T132 | x | Poista bearing server-kerroksesta: `server/db.ts` markers-taulun `bearing`/`bearing_manual`-sarakkeet CREATE TABLE:sta (kanta nollataan puhtaasti, ei migraatiota — vain testidataa). `server/routes/markers.ts`: poista bearing-kentät GET/POST/PUT:sta. `server/gpkg/geojson.ts`: poista bearing `GpkgMarkerRow`/`GpkgFeature`/`markersToGeoJSON`/`geoJSONToMarkers`:sta. `server/routes/gpkg.ts`: poista bearing SELECT/INSERT/UPDATE-lauseista. Päivitä testit: geojson.test.ts, convert.test.ts, gpkg-import.test.ts, gpkg-export.test.ts, markers-API-testit. Bun-integraatiotestit. Riippuu T129. | T129 |
| T133 | x | Dokumentaatiosiivous: VISION.md rivi 101 poista "suunta (bearing)" merkin ydinmääritelmästä. COMPONENTS.md: poista MarkerInteraction-rivi taulukosta+pilkkohälytyksistä, nimeä BearingMath → RouteGeoMath (vastuu on reittigeometria) + päivitä docs/components/logic.md, päivitä MarkerManager/SignIcon/MarkersAPI/GpkgGeoJSON/GpkgRoutes-kuvaukset docs/components/map.md+backend.md:ssä (bearing pois — GPKG upsert/mäppäys-dokumentaatio T124-T126:sta säilyy muuten sellaisenaan, jo kattava). Riippuu T129,T130,T131,T132. | T129,T130,T131,T132 |
| T134 | x | UX-audit-korjaukset (talkoolainen metsässä -kriittisiä: kontrasti+touch): (1) ✓ `--text-meta` WCAG AA `--surface-card`illa (B51/V81): light `#7C8A80`→`#647268` (3.41→4.77:1), dark `#857B72`→`#9A8F85` (3.97→5.20:1); meta<muted-hierarkia säilyy, (2) ✓ U18/U19 36px→44px (B52): `.btn-segment-details-toggle` + equipment-inputit/napit `min-height:44px`, (3) ✓ `.marker-item-checkbox`+`.marker-checkin-cb` 44×44 kosketuskohde (padding-box, näkyvä ruutu 22px) — talkoolaisen bulk check-in hanskoilla. `src/style.css`, ∀ väri tokeneista. E2E touch ≥44px ok (critical-paths). | B51,B52,B53,V21,V81 |
| T135 | x | `src/map/markers.ts` `addLeafletMarker()`: korvaa raaka `el.addEventListener('click', ...)` Leafletin omalla `lm.on('click', ...)`-kuuntelijalla (tai `wasDragged`-flag dragstart→dragend→click) — dragend ei enää laukaise `onMarkerClick`/modaalia sivuvaikutuksena. Vitest tai Playwright: simuloi drag, varmista modaali EI aukea; erillinen click ilman dragia AVAA modaalin edelleen. | V82 |
| T136 | x | `src/ui/sign-library-panel.ts` `buildRow()`: poista `DEFAULT_IDS`-rajaus place-napista, kaikki mallit (myös custom) saavat klikattavan `sign-lib-place-btn`. `bindEvents()`: sido click-kuuntelija `.sign-lib-place-btn`:lle joka "arm:aa" `PlaceMode`n valitulla template-id:llä (uusi metodi esim. `PlaceMode.armFromSidebar(templateId)`); seuraava kartan klikki sijoittaa merkin samalla tavalla kuin floating pickerin valinta. Ei suosikkivaatimusta. Vitest-jsdom (panel) + Playwright (koko flow: klikkaa sivupalkin custom-mallia → klikkaa karttaa → merkki ilmestyy). | V83 |
| T137 | x | `src/ui/marker-detail-modal.ts` `buildJarjestajaFooter()`: lisää status-nappirivi (kaikki `MarkerStatus`-arvot valittavissa, ei `validActions()`-rajausta) `Tallenna`-napin viereen/yläpuolelle — klikki kutsuu `manager.setStatus(marker.id, newStatus)`-tyyppistä metodia (tarkista/lisää `MarkerManager`iin jos puuttuu) + `this.onUpdate()`. Vitest-jsdom: jokainen status valittavissa järjestäjälle riippumatta nykytilasta. | V84 |
| T138 | x | `src/map/icons.ts` `circleSvg()`: poista nurkkabadge joka toistaa `shortLabel`-tekstin päällekkäin `circleContent`in kanssa (tai käytä sitä vain kun `iconEntry` on custom-ikoni, disambiguointiin). Korvaa/yhdistä statusindikaattori selkeämmäksi — isompi, kontrastisempi merkki `STATUS_DOT_COLOR`in tilalle (esim. ✓-glyph asetettu/tarkistettu/kerätty, tyhjä/dashed suunniteltu, ✕/harmaa ei_tarpeen). Päivitä `karttamaster-ux`-DESIGN.md §K jos komponenttisopimus muuttuu. | V85 |
| T139 | x | `src/map/icons.ts` `circleSvg()`: pääympyrän täyttöväri statuksen mukaan (`STATUS_FILL`), ei tyyppivärillä, statuksissa asetettu/tarkistettu/kerätty/ei_tarpeen — neljä keskenään selvästi erottuvaa sävyä (ei saman perheen pastelleja, entinen #4ade80/#93c5fd/#6ee7b7 -kolmikko liian samankaltainen). Suunniteltu/upcoming säilyy ennallaan (tyyppiväri, faded/dashed). Kerätty+ei_tarpeen: iso ✓/✕ tyyppi-ikonin tilalle. Vitest: jokainen statusvärimuunnos + terminaaliglyfit. | V86 (korvattu) |
| T140 | x | `src/map/icons.ts` `circleSvg()`: käännä T139:n väritys ympäri — täyttö takaisin tyyppiväriksi/-kuvaksi aina (myös kerätty, ei enää ✓-glyfikorvausta), statusväri (`STATUS_RING`, entinen `STATUS_FILL`) siirtyy `stroke`-attribuuttiin leveydellä 4px. Suunniteltu/upcoming ennallaan (valkoinen/dashed stroke). Vitest: fill on aina tyyppiväri, stroke vaihtelee statuksen mukaan, icon/nuoli näkyy jokaisessa statuksessa. | V87 |
| T141 | x | Korvaa T95 (ks. B60) — kartan-alle-toteutus, ks. B61: käyttäjä hylkäsi tämän live-testissä, korvattu T142:lla samassa sessiossa. `SegmentStatusBar`-komponentti poistettu kokonaan. | V88(kumottu),T13,T15,B60 |
| T142 | x | Korjaa T141 (ks. B61): status-lukumäärä sivupalkin pätkäjako-listan riville, ei kartan alle. `src/ui/segment-panel.ts buildRow()`: `.segment-km`-elementin teksti = `formatStatusCounts(getSegmentStatusCounts(seg, markers))` (uusi pure-funktio `src/logic/segments.ts`), vanha km-alue siirretty `title`-attribuutiksi (hover). `SegmentPanel.refreshCounts()` (public, kutsuu `render()`) — wired `main.ts`:n `MarkerManager`-onUpdate-hookiin niin että rivi päivittyy merkin status-muutoksesta, ei vain segmentin omasta mutaatiosta. Testit: Vitest-pure (`formatStatusCounts`, tests/segments.test.ts) + Playwright (`e2e/segments.spec.ts` ".segment-km näyttää status-lukumäärän") — regressiosuoja samalla periaatteella kuin B60/B61 vaativat. Käyttäjä: järjestäjä. | V89,T13,T15,B61 |
| T143 | x | Korjaa T142 (ks. UX-palaute, ahdas tila): `.segment-km` näyttää yhden phase-tietoisen luvun täyden breakdownin sijaan. `getPhaseProgress(segment: Segment, markers: SignMarker[]): {done: number, total: number, label: string}` `src/logic/segments.ts` — `asettaminen`→asetettu-progressi, `purku`→kerätty-progressi, lookup-taulu (ei if-ketju, tuleva `tarkastus`-phase helppo lisätä). `buildRow()`: teksti = `"<done>/<total> <label>"` tai "ei merkkejä". `title`-attribuutti: km-alue + täysi `formatStatusCounts`-breakdown. DESIGN.md §K päivitetty. Vitest-pure (asettaminen/purku, tyhjä pätkä, kaikki done, ei yhtään done) + päivitä e2e/segments.spec.ts vastaamaan uutta formaattia. Käyttäjä: järjestäjä. | V90,T13,T15 |
| T144 | x | Kolmivaiheinen elinkaari, osa 1/4 — data model: `Segment.phase` laajennus `'asettaminen'\|'tarkastus'\|'purku'` (`src/logic/segments.ts`). `PHASE_TARGET`-lookupiin (T143) rivi `tarkastus`: EI per-merkki-statuslaskentaa (ei ole erillistä marker-tason "tarkastettu"-statusta, ks. V92) — sen sijaan segmentin oma boolean-kenttä `inspected?: boolean` + vapaateksti `inspectionNote?: string` (VISION §4: "huomiot vapaakenttänä"). `getPhaseProgress()` palauttaa `tarkastus`-phaselle eri muotoisen tuloksen kuin asettaminen/purku (`{done: boolean, label: 'tarkastettu'}` -tyyppinen, EI `{done:number,total:number}`) — käytä discriminated unionia `PhaseProgress` paluutyyppinä, ei valehdella numeroa. Ei migraatiota (additive enum + optional-kentät). Vitest-pure: uusi tarkastus-segmentti default `inspected:false`, `getPhaseProgress` palauttaa oikean muodon per phase. | V91 |
| T145 | x | Kolmivaiheinen elinkaari, osa 2/4 — tarkistettu ei enää mandaatti: `src/logic/marker-status.ts` `TRANSITIONS`: lisää `asetettu: { kerää: 'kerätty', tarkista: 'tarkistettu', peru: 'suunniteltu' }` (uusi suora `kerää`-action `asetettu`-tilaan, `tarkista` säilyy valinnaisena vaihtoehtona samasta tilasta). `tarkistettu`-tila pysyy tyypissä muuttumattomana (V92) — `validActions('tarkistettu')` säilyy ennallaan (`kerää`,`peru`). Talkoolaisen UI (`marker-list.ts`, `segment-view.ts` bulk-napit) saa nyt kaksi vaihtoehtoa `asetettu`-tilasta: "Kerätty" JA "Tarkistettu" — ei enää yksi pakotettu polku. Vitest-pure: `asetettu`→`kerää` toimii suoraan (uusi), `asetettu`→`tarkista`→`kerää` toimii yhä (vanha, ei regressoi), olemassa oleva data `status:'tarkistettu'` pysyy validina ilman muutosta. | V92,T10 |
| T146 | x | Kolmivaiheinen elinkaari, osa 3/4 — UI-aukko: **ei ole olemassa mitään tapaa luoda `purku`- tai `tarkastus`-vaiheen pätkäjakoa käytännössä** — `segment-creation-modal.ts:177` hardcodettu `phase: 'asettaminen'`, eikä `segment-details-modal.ts`:ssä ole phase-editointia. T52:n speksi olettaa tämän olemassa olevaksi, mutta koodi ei tue sitä — tämä task korjaa puuttuvan mekanismin kaikille kolmelle vaiheelle. `SegmentDetailsModal`: uusi nappi "Kloonaa seuraavaan vaiheeseen" — kopioi `routeIds/startDist/endDist/displayName`, luo UUDEN segmentin `phase: seuraava(nykyinen.phase)` (`asettaminen→tarkastus→purku`, lookup-taulu ei if-ketju), TYHJÄ `assignedCode`/`equipment`/`description` (uusi jako, ei peri vanhan koodia — V26-periaate: eri talkoolainen eri vaiheessa). Vanha segmentti säilyy koskemattomana. Vitest-jsdom: klikkaus luo uuden segmentin oikealla phase-arvolla, vanha segmentti ei muutu, `assignedCode` ei periydy. | V91,T13,T52 |
| T147 | x | Kolmivaiheinen elinkaari, osa 4/4 — tarkastus-UI talkoolaiselle/tarkastajalle: `segment-view.ts` (talkoolaisen pätkänäkymä, T14): kun `segment.phase === 'tarkastus'`, näytä bulk-napin (T52-mallin mukaan) sijaan "Merkitse tarkastetuksi" -nappi + vapaatekstikenttä (`inspectionNote`) — VISION §4: "ajetaan pyörällä, huomiot vapaakenttänä", ei per-merkki-kuittausta. Klikkaus asettaa `segment.inspected = true` + tallentaa `inspectionNote`. `SegmentPanel`-rivi (T143): `tarkastus`-phase näyttää "Tarkastettu ✓" tai "Ei vielä tarkastettu" (boolean-muotoinen `getPhaseProgress`-tulos, T144) numeerisen `X/N`:n sijaan. Pätkäkoodin jako: sama `assignedCode`/`/api/admin/codes`/`/s/<koodi>`-mekanismi kuin T52:ssa (ei backend-muutosta — jo geneerinen per-segmentti, ei phase-sidonnainen). Vitest-jsdom + Playwright (segments.spec.ts: tarkastus-segmentin luonti T146:lla → segment-view näyttää oikean UI:n). Käyttäjä: talkoolainen/tarkastaja + järjestäjä (tilannekuva). | V91,T14,T52,T144,T146 |
| T148 | x | Globaali phase-näkymän vaihdin järjestäjälle (VISION.md §Vaiheistus, rivit 218-230 — event lifecycle jo kuvattu): `activePhase: SegmentPhase` ('asettaminen'\|'tarkastus'\|'purku', sama tyyppi V91), persistoitu localStorageen — uusi `src/logic/phase-view.ts`: `getActivePhase()/setActivePhase(phase)`, sama pattern kuin `role.ts` (T12). Vapaa siirtymä mihin arvoon tahansa, ei rajoitettu ketju (eri asia kuin marker-status V92). Vitest-pure, vi.stubGlobal-mock (CLAUDE.md). UI: uusi `src/ui/phase-switcher.ts` — pieni kontrolli toolbariin (dropdown tai 3-vaihtoehtoinen segmented control), näkyy vain järjestäjä-roolille (talkoolainen ei tarvitse, menee aina suoraan omaan pätkäänsä `/s/<koodi>`). Valinta kutsuu `setActivePhase()` + triggeröi `SegmentPanel.render()`/kartan pätkälistauksen uudelleenrenderin jo olemassa olevalla `getSegmentsForPhase(segments, activePhase)`-suotimella (T13, tähän asti käyttämätön). Vitest-jsdom. DESIGN.md §K-sopimus lisättävä ennen rakennusta (`/karttamaster-ux komponentti phase-switcher`). Riippuu T144:sta, ei T146:sta (mutta hyötyy siitä käytännössä). Ei vaikuta talkoolaisen näkymään. | V91,T13,T144 |
| T149 | x | Tarkastuskuittauksen persistointi-fix (B62): (a) `server/db.ts` `segments`-tauluun `inspected INTEGER NOT NULL DEFAULT 0` + `inspection_note TEXT` (idempotent migraatio). (b) `server/routes/segments.ts`: `rowToSegment` palauttaa `inspected`/`inspectionNote`; POST/PUT lukevat & tallentavat kentät; `phase`-cast korjataan `'asettaminen'\|'tarkastus'\|'purku'`. (c) PUT role-gate: salli myös talkoolainen jonka `session.talkoolainen_code` = segmentin `assigned_code` — sallitut kentät rajattu `inspected,inspectionNote,startDist,endDist` (V43), muut kentät järjestäjä+. (d) `main.ts` onInspect-callback: `updateSegmentRemote` false → virhebanneri, ei hiljaista häviötä. Bun-testit: inspected roundtrip, talkoolainen oma pätkä PUT 200, vieras pätkä 403, kenttärajaus. Vitest-jsdom: virhebanneri. Käyttäjä: tarkastaja + järjestäjä (tilannekuva). | V93,V43,T147,T144 |
| T150 | x | Phase-tietoinen pätkäluonti (B63): `segment-creation-modal.ts` `phase: 'asettaminen'` → aktiivinen phase (`getActivePhase()`, main.ts/panel välittää). Snap-markerit: `main.ts` `showCreationSnapMarkers` saa phase-suodatetun storen (sama suodin kuin overlay) — ei snap-pisteitä piilotettujen vaiheiden pätkistä. Vitest-jsdom: purku-näkymässä luotu pätkä saa `phase:'purku'` + näkyy listassa heti. Käyttäjä: järjestäjä. | V94,T148,T146 |
| T151 | x | Phase-scoped overlap (B64): `validateNoOverlap(store, routeId, startDist, endDist, phase, excludeId?)` — filtteröi `s.phase === phase`. Kutsupaikka `segment-panel.ts` välittää aktiivisen phasen. `cloneSegmentToNextPhase`: validoi kohde-phasen overlap ennen `createSegment` — overlap → ei luontia + virheviesti modaalissa (estää tuplakloonin). Vitest-pure: eri phase overlap sallittu, sama phase hylätään, tuplaklooni hylätään. Käyttäjä: järjestäjä. | V95,V49,V91,T146 |
| T152 | x | Pätkän status kartalle (B65): kaksi kanavaa — **väri = tunniste** (`colorForSegment(id)` hash, stabiili; poisto ei siirrä muita), **viivatyyli = status** (`segmentLineState(getPhaseProgress)`: valmis=ehjä opacity 0.9 / kesken=katko '1 9' 0.85 / ei_alkanut=haalea katko 0.4 w9). `SegmentOverlay.update(store, markers)`; `main.ts` MarkerManager onUpdate kutsuu renderSegmentOverlay (status päivittyy merkin muutoksesta). tarkastus valmis → tooltip ✓. Logiikka `src/logic/segments.ts` (Vitest-pure: colorForSegment determinismi+stabiilius, segmentLineState 3 ämpäriä). DESIGN.md §K päivitetty. Playwright: tyhjä pätkä = katkoviiva. StatusPanel (per-reitti %) säilyy — eri aggregaatio, ei ristiriita. | V96,V90,T143 |
| T153 | x | Siivous: poista kuollut `getSegmentProgress` (`src/logic/segments.ts` — B60 totesi orvoksi, T142/T143 korvasi). Vitest-ajo varmistaa ettei mikään importtaa. | B60 |
| T154 | x | Testihygienia: (a) `src/logic/area-sync.ts` ainoa logic-moduuli ilman Taso 1 -testiä → `tests/area-sync.test.ts` (`segment-sync.test.ts` malli). (b) Poista kuollut `src/logic/tile-layers.test.ts` — vitest include `tests/**` vain → ei aja koskaan; duplikoi `tests/tile-layers.spec.ts`. (c) Stale todo-siivous (55 todoa yht): `sign-picker.spec.ts` 16 (dblclick-picker katettu e2e/critical-paths.spec.ts, "Merkit dropdown" kuvaa T85:ssä poistettua UI:ta → poista), `route-progress.spec.ts` 15, `multi-route.spec.ts` 7, `t38-type-change.test.ts` 7, `tile-layers.spec.ts` 6 — poista jo-katetut/vanhentuneet, konvertoi aidosti puuttuvat oikeiksi testeiksi. Todo-massa hämärtää "↓ = roadmap, ei regressio" -signaalin. | T85,T46 |
| T155 | x | main.ts pilkko: 472 riv → ~80 (VISION-raja "vain init + wiring"). Vastuut: (1) kartta+tile-layer-init+localStorage r.46–58, (2) toolbar-wiring (btn-layer/menu/left-panel/gps) r.59–104, (3) jätti-`init()` r.105–438 ~330 riv — markers/segments/areas/panels-wiring yhdessä funktiossa, (4) auth+rooli-view r.439–472. Pilkko: `src/app/map-init.ts`, `src/app/markers-wiring.ts`, `src/app/segments-wiring.ts`, `src/app/areas-wiring.ts`, `src/app/role-view.ts` — main.ts jää orkestroijaksi. Ei uutta logiikkaa, vain siirto: kaikki testit pysyvät vihreinä, Playwright critical-paths pakollinen validointi (src/map-rajapinta ei muutu). Valmis → COMPONENTS.md AppController-rivi + ⚠️-lippu päivitys. | §C |
| T156 | x | Merkkikirjaston template-id käsin annettavaksi (V97, GPKG-tuki). `src/logic/sign-library.ts`: `createTemplate` vaatii `id`:n (poista `crypto.randomUUID()`-default), validoi (a) uniikkius `library`ssa (b) formaatti `/^[A-Za-z0-9_-]+$/` — hylkää duplikaatti/virheellinen palauttamalla virheen (esim. `{ ok:false, reason }`), EI kirjoita kirjastoon. Perustelu: id = export-`type`-koodi (T157) + kuva-avain (T158), siksi filename-safe. Greenfield: bumppaa `StoredLibrary.version` 1→2, `loadLibrary` hylkää v1:n → reset (ei UUID-migraatiota, ei tuotantodataa). UI `src/ui/sign-library-panel.ts`: id-input `.sign-lib-form`:iin (min-height 44px, field-tint, border-default — §K), virheviesti duplikaatista/virheellisestä; id vain luonnissa (editissä lukittu, on avain) → DESIGN.md §K SignLibraryPanel-lomake päivitys. localStorage → `vi.stubGlobal`-mock (CLAUDE.md). Testit: Vitest-pure (uniikkius, regex-hylkäys, v2-reset), Vitest-jsdom (validointiviesti näkyy lomakkeessa). Käyttäjä: järjestäjä. | V97 |
| T157 | x | GPKG-export käyttää `label`ia luettavana nimenä (V98). Vaatii T156. Denormalisoi `label` markers-tauluun (kuten `short_label`/`color`/`description` jo): `server/db.ts` CREATE TABLE + `ALTER TABLE markers ADD COLUMN label` -migraatio; `server/routes/markers.ts` POST + `server/routes/gpkg.ts` import INSERT tallentaa `label`; export SELECT + `label`. `server/gpkg/geojson.ts`: `GpkgMarkerRow` + `label`, `GpkgFeature.properties` + `name`; `markersToGeoJSON` kirjoittaa `name = label`, `geoJSONToMarkers` lukee `label = properties.name` (round-trip). Testit: Bun integraatio `server/` (`test-fixtures.ts` `seedTestUsers`+`authHeaders`): export `properties.name === label`, round-trip import säilyttää `label`in. Käyttäjä: järjestäjä. | V98,T156 |
| T158 | ✓ | Sign-kuvat template-tasolla — rakenne + fallback (V99). Vaatii T156. **Kuvia ei vielä ole** → tässä vain rakenne + fallback-polku, ei oikeita kuvia. `SignTemplate.imageId?: string` (konventio = `template.id`). Uusi puhdas valintafunktio `src/logic/` (esim. `signVisual(template): {kind:'image',src}|{kind:'icon',id}|{kind:'label',text}`) toteuttaa precedence kuva>ikoni>shortLabel — testattava ilman DOM:ia. Asset-konventio `src/assets/signs/<id>.webp` (Vite glob-import). Render: `src/map/icons.ts` `circleSvg` (kuva ympyrän täyttönä — V87: täyttö=tyyppi, status=ulkoreuna), `sign-library-panel.ts` swatch, `sign-picker` — kaikki `<img onerror>`-fallback ikoniin/shortLabeliin (T103-pattern). Resize/resoluutio-käsittely EI tässä → erillinen myöhempi §T. DESIGN.md §K SignIcon + SignLibraryPanel: kuva-precedence lisättävä. Testit: Vitest-pure (precedence: image|icon|label-valinta 3 ämpäriä), Vitest-jsdom (render + onerror-fallback). Käyttäjä: molemmat (järjestäjä kirjastossa, talkoolainen kartalla). | V99,V87,T103 |
| T159 | ✓ | U-käännös-ikonit merkkikirjastoon. `src/logic/icon-set.ts` `CURATED_ICONS` + `u-turn-left` + `u-turn-right` (Lucide-tyyli: viewBox 0 0 24 24, stroke-path, ei täyttöä; Lucidessa ei valmista u-turnia → custom path nuoli+180°-kaari). Suomenkieliset labelit ("U-käännös vasen/oikea"). Testi: Vitest-pure `getIconById('u-turn-left')`/`('u-turn-right')` löytää molemmat, `renderIconSvg` tuottaa validin SVG:n. Käyttäjä: järjestäjä. | T9 |
| T160 | x | Poista `shortLabel` erillisenä kenttänä — johda kompakti kartta-teksti `label`ista (V99-muutos). Vaatii T157. Uusi puhdas `compactLabel(label): string` = `label.slice(0,3).toUpperCase()` `src/logic/`:ssä (esim. `sign-visual.ts` tai oma moduuli). Poista `SignTemplate.shortLabel` (`src/logic/sign-library.ts`) + `SignMarker.shortLabel` (`src/logic/types.ts`); LISÄÄ `SignMarker.label` + sync-mappaus (`src/logic/sync.ts` `row.label` → `label`; GET /api/markers palauttaa jo `label`in `SELECT *`:llä). Lomake `src/ui/sign-library-panel.ts`: poista shortLabel-input + pakollisuus (`if (!label || !shortLabel)` → `if (!label)`); default-templatet (left/right/upcoming/upcoming) `createTemplate` ilman `shortLabel`ia. Render johdettuna: `src/map/icons.ts` circleSvg `shortLabelOverride` → `compactLabel(label)`; `src/logic/sign-visual.ts` `signVisual` label-tier; `src/ui/marker-list.ts` (rivit + pisteet, entinen `m.shortLabel ?? info.shortLabel`); `place-mode` picker + panel swatch. Server minimimuutos: `short_label`-sarake jää kuolleeksi (greenfield, EI DROP), lopeta kirjoitus+luku POST (`server/routes/markers.ts` body + INSERT) + GPKG import INSERT (`server/routes/gpkg.ts`) — poista `short_label` INSERT-listoista; kompakti johdetaan clientissä `label`ista. `sign-picker.ts SIGN_TYPES.shortLabel` → poista tai johda `compactLabel(label)`illa. Testit: Vitest-pure (`compactLabel`: 3 ekaa isoin, <3 merkin nimi, tyhjä string), päivitä olemassa olevat shortLabel-testit (`sign-icon-status` T84 custom-override, `sign-library`, `sync`, marker-list). Käyttäjä: järjestäjä (vähemmän lomakekenttiä) + talkoolainen (sama kartta-identiteetti). | V99,T157 |
| T161 | ✓ | Sign-kuvien konversio-pipeline (dev-työkalu) — PNG-kyltit → `src/assets/signs/<id>.webp`. Kaksi osaa. **(A) Puhdas slug-fn** `src/logic/sign-id-slug.ts` `signIdFromFilename(fn): string`: pudota printtiprefix `/^(A3\|A4)\s+(vaaka\|pysty\|neliö\|leikattu\|rajattu\|\s)+\d+\s*kpl\s+/i`, slugifioi loppu (ä→a, ö→o, väli→`-`, lower, poista muut ID_RE-kelvottomat) → tulos matchaa `sign-library.ts` `ID_RE` `/^[A-Za-z0-9_-]+$/`. Prefiksitön nimi slugifioidaan suoraan. Esim `"A3 vaaka leikattu 1 kpl iso-syöte 430.png"`→`iso-syote-430`, `"park and ride.png"`→`park-and-ride`, `"wc.png"`→`wc`. EI Leafletia/DOM:ia/ImageMagickia tässä moduulissa. **(B) CLI** `scripts/convert-signs.ts` (Bun, `bun scripts/convert-signs.ts <lähdekansio>`), importtaa vain (A):n puhtaan fn:n — ei muuta src/-runtimea. Per PNG: (1) whitelist `.png` (skip `pspbrwse.jbf`/`.zip`/muut), (2) `id = signIdFromFilename(basename)`, (3) VÄRIPOHJA ENSIN: indexed/colormap→truecolor RGBA (`magick`, koneella) ENNEN pienennystä — muuten reunat rikki, (4) trim tyhjä reuna, (5) resize Lanczos max ~256px pisin sivu, (6) → `src/assets/signs/<id>.webp`. Tulostaa mapping-raportin (lähde→id→dimensiot→polku) + FLÄGÄÄ id-törmäykset virheenä. Idempotentti (ylikirjoittaa). Assetit poimitaan automaattisesti `sign-images.ts` glob-importilla (T158). Vaatii T156 (ID_RE), T158 (asset-konventio). Testit: Vitest-pure `src/logic/sign-id-slug.test.ts` (prefix-strip, ä/ö-translit, väli→viiva, ID_RE-validius, prefiksitön, törmäyshavainto); (B) ei unit-testiä (subprocess+I/O). Seuraava erillinen task (ei tässä): raportista kuratointi kuva/ikoni/skip. Käyttäjä: kehittäjä + järjestäjä (sisällöntuonti). | V99,T156,T158 |
| T162 | x | Snapshot kattaa koko datasetin (B66/V100) — kriittisin data-turva. Nyt snapshot+restore vain markers-taulu → restore hävittää pätkäjaon + alueet. (a) `server/db.ts` `snapshots`-tauluun `dataset_json TEXT` -sarake (idempotent migraatio; `markers_json` jää nullableksi legacy-lukua varten, EI DROP). (b) Uusi kanoninen serialisointi `server/` — esim. `serializeDataset(db): {version:1, markers, segments, areas, areaFeatures}` + `restoreDataset(db, parsed)` — molemmat käyttävät SAMAA koodia (poista `snapshot-scheduler.ts`:n oma duplikoitu INSERT, kutsu jaettua `createSnapshot`-helperiä `admin.ts`:stä). (c) `createSnapshot` (`admin.ts:106`) kirjoittaa `dataset_json` = `serializeDataset(...)`. (d) restore (`admin.ts:146`): jos `dataset_json` → korvaa markers+segments+areas+area_features atomisesti yhdessä `db.transaction`issa; jos vain legacy `markers_json` → korvaa VAIN markers (V100 backward-compat, ei tyhjennä segments/areas). Restore luo edelleen "Palautus:"-snapshotin (V24). Bun-integraatio (`server/test-fixtures.ts`): full snapshot→restore roundtrip palauttaa kaikki 4 taulua, legacy markers-only-restore jättää segments/areas koskemattomiksi, transaktio-atomisuus (viallinen segment-rivi → mikään ei muutu). Käyttäjä: järjestäjä. | V100,V24 |
| T163 | x | Automaattikopio laukeaa vaikka kone nukkuu (B67/V101). Nyt `snapshot-scheduler.ts` `setInterval` kuolee kun fly auto-stoppaa koneen → yökopio ei laukea luotettavasti. (a) Uusi endpoint `POST /api/cron/snapshot` (`server/routes/`), suojattu jaetulla salaisuudella: header `X-Cron-Token` === `process.env.SNAPSHOT_CRON_TOKEN` (⊥ session-auth — cron ei kirjaudu); väärä/puuttuva token → 401. Kutsuu jaettua `createSnapshot(..., trigger:'auto-yö')` (T162:n helper). (b) Ulkoinen ajastin: `.github/workflows/nightly-snapshot.yml` — `schedule: cron '0 3 * * *'`, `curl -X POST https://karttamaster-web.fly.dev/api/cron/snapshot -H "X-Cron-Token: ${{secrets.SNAPSHOT_CRON_TOKEN}}"` (ping herättää `auto_start_machines`-koneen). (c) `snapshot-scheduler.ts` in-process `setInterval` → poista TAI jätä best-effort-toisekopiona (dokumentoi ettei se ole ensisijainen). (d) `SNAPSHOT_CRON_TOKEN` fly secretsiin (dokumentoi entrypoint/README). Bun-testi: endpoint oikea token → 201 + snapshot syntyy, väärä token → 401, ei snapshottia. Käyttäjä: järjestäjä (näkymätön turvaverkko). | V101,V100,T162 |
| T164 | x | Järjestäjän "Lataa varmuuskopio" + "Palauta tiedostosta" (V102, off-site-turva). Vaatii T162 (jaettu serialisointi). (a) `GET /api/admin/backup` (`requireRole('admin','järjestäjä')`): body = `serializeDataset(db)`, header `Content-Disposition: attachment; filename="karttamaster-backup-<pvm>.json"`. (b) `POST /api/admin/backup/restore`: vastaanottaa V100-blobin, validoi `version`, kutsuu `restoreDataset` (sama atominen korvaus kuin snapshot-restore) + luo "Palautus:"-snapshotin. Virheellinen/versioimaton JSON → 400, ei osittaista kirjoitusta. (c) UI `src/ui/snapshot-panel.ts`: "⬇ Lataa varmuuskopio" -nappi (GET → `<a download>`-trigger) + "⬆ Palauta tiedostosta" (`<input type=file>` → POST). Palautus tuhoava → `window.confirm()` ennen (V102). Napit ≥44px (§R), modal-footer-pattern jos destructive-osio. DESIGN.md §K SnapshotPanel päivitys (`/karttamaster-ux komponentti snapshot-panel`). Bun-integraatio: backup export -muoto = `serializeDataset`, restore roundtrip; Vitest-jsdom: napit olemassa, confirm ennen restorea, versioimaton tiedosto → virheviesti. Käyttäjä: järjestäjä desktopilla. | V102,V100,T162 |
| T165 | x | fly.toml HTTP health check `/api/health`:lle (B68/V103). Lisää `[[http_service.checks]]`: `type="http"`, `path="/api/health"`, `interval`+`timeout`+`grace_period` (bun+nginx ehtii nousta `docker-entrypoint.sh`:ssa). Check osuu nginx→proxy `/api/`→bun:3001 → valvoo koko ketjun (⊥ pelkkä `/` jonka nginx palauttaa staattisesti vaikka serveri kuollut). Verifiointi: `fly deploy` + `fly status` CHECKS=passing; tapa bun-prosessi koneella → check fail → fly restart. Ei src-muutosta (`/api/health` jo olemassa `server/index.ts`). Käyttäjä: järjestäjä (näkymätön luotettavuus). | V103 |
| T166 | x | `.dockerignore` (B69/B71/V104). Luo tiedosto joka excludeaa: `.env`, `.env.*`, `*.db`, `*.db-shm`, `*.db-wal`, `node_modules`, `.git`, `.github`, `dist`, `test-results`, `tests`, `e2e`, `.playwright-mcp`, `.htpasswd`, `claude-design-assets`, `Kyltit Syöte MTB`. Poista myös `.htpasswd` repostä (kuollut, B71). ÄLÄ excludea mitään mitä `bun run build`/server tarvitsee (src/, server/, package.json, bun.lock, index.html, public/, nginx.conf, docker-entrypoint.sh, vite/tsconfig). Verifiointi: `fly deploy` + `fly ssh console` → `ls /app` ei sisällä `.env`/`*.db`; build-konteksti pienempi. Käyttäjä: kehittäjä (turvallisuus + build-nopeus). | V104 |
| T167 | x | Siirrä `VITE_MML_API_KEY` pois `fly.toml`:ista (B70). (a) `fly secrets set` / CI-`--build-arg` deploy-ympäristöstä; poista literaali arvo `fly.toml [build.args]`:ista. Matala prio — avoin MML-avain näkyy client-bundlessa joka tapauksessa, tämä on hygienia ⊥ vuotokorjaus. Käyttäjä: kehittäjä. | B70 |
| T168 | x | `.gitignore` + secret-vuodon esto (B72/V105). (a) Lisää `.env` `.gitignore`:en (nyt vain `.env.local`+`.env*.local` → `.env` jossa `ADMIN_PASSWORD` ei suojattu). (b) T166:n `.dockerignore` estää `.env`:n bundlauksen imageen — varmista `.env*` mukana excludeissa. (c) Tarkista `git log --all -- .env` ettei `.env` ole koskaan committautunut historiaan (jos on → rotate `ADMIN_PASSWORD` fly-secret + T169). Fly secretit ovat jo asetettu (ADMIN_PASSWORD/USERNAME/CRON deployed) → prod ok, tämä estää vuodon git+image-reittiä pitkin. Käyttäjä: kehittäjä (turvallisuus). | V105,T166 |
| T169 | x | Admin-salasanan rotaatio (B73). `seedAdmin` (`server/db.ts:157`) `if (exists) return` → fly `ADMIN_PASSWORD`-secretin vaihto ei päivitä olemassaolevaa admin-käyttäjää, vanha salasana jää voimaan. PÄÄTÖS AUKI: (a) `seedAdmin` upsert — jos user olemassa & env-salasana eri hashista → päivitä `password_hash`; TAI (b) jätä seed-once, dokumentoi että rotaatio tehdään `POST /api/admin/users/:id/reset-password`-reitin (admin.ts:33) kautta / manuaalisella DB-updatella. Kysy käyttäjältä kumpi. Testi (jos a): olemassaoleva admin + muuttunut ADMIN_PASSWORD → hash päivittyy, vanha ei enää validoi. Käyttäjä: järjestäjä/ylläpito. | V105 |
| T170 | ✓ | iconId-denormalisointi markerille (B74/V106) — korjaa V99-precedencen ikoni-tierin joka ei koskaan renderöidy. (a) `src/logic/types.ts`: `SignMarker.iconId?: string`. (b) `src/map/markers.ts`: `add(lat,lon,type,color?,label?,iconId?)` tallentaa `...(iconId ? {iconId} : {})`; `updateType(id,newType,color?,label?,iconId?)` asettaa `m.iconId = iconId ?? undefined`; KAIKKI `createSignIcon(...)`-kutsut (`updateStatus`,`bulkSetStatus`,`updateType`,`addLeafletMarker`, dragend) välittävät `m.iconId` viidentenä argumenttina `undefined`:n sijaan (kuudes arg `signImageSrc(m.type)` pysyy). (c) `src/ui/place-mode.ts`: `openPicker()`/kutsu `markerManager.add(...)` välittää `btn.dataset.iconId` (lisää dataset-attribuutti `data-icon="${t.iconId ?? ''}"` floating-picker-nappiin, kuten `data-color`/`data-short` T89:ssä). (d) `src/ui/marker-list.ts` `marker-type-select`-vaihto kutsuu `updateType(...)` template-kohtaisella `iconId`illa. (e) Server: `server/db.ts` migraatio lisää `icon_id TEXT` `markers`-tauluun (idempotent, kuten `short_label`-sarake T89:ssä — EI poisteta mitään); `server/routes/markers.ts` POST-body + INSERT-lista + PUT-tyyppimuutos lukee/kirjoittaa `icon_id`↔`iconId` (`SELECT *` poimii automaattisesti). (f) `src/logic/sync.ts`: `row.icon_id` → `iconId` mappaus GET-vastauksessa (sama pattern kuin `label`). Testit: Vitest-pure (`markers.test.ts`-tyylinen: `add` tallentaa iconId, `updateType` päivittää sen), Vitest-jsdom (place-mode picker välittää `data-icon`), Bun-integraatio (POST/GET roundtrip `icon_id`). Playwright: dblclick-kartalla ikoni-templatella (ei kuvaa) → SVG-ikoni näkyy DOM:issa, ei fallback-teksti (päivitä `e2e/critical-paths.spec.ts`). Käyttäjä: molemmat (järjestäjä asettaa, talkoolainen näkee). | V106,V50,V99,T93,T89 |
| T171 | ✓ | Yhdistelmämerkki-datamalli: `SignTemplate.parts?: {iconId?: string; imageId?: string}[]` (`src/logic/sign-library.ts`) — **järjestys = pino ylhäältä alas** (kepissä kiinni, `parts[0]` ylin), katto 4 osaa (V107). Uusi puhdas `signVisualParts(template): SignVisual[]` (`src/logic/sign-visual.ts`) — jos `parts` asetettu, palauttaa taulukon **samassa järjestyksessä** per-osan `signVisual`-tuloksia (kuva>ikoni, EI label-fallbackia per osa — combo-osa on aina tarkoituksella valittu kuva/ikoni); jos `parts` tyhjä/puuttuu, sama 1-elementin taulukko kuin nykyinen `signVisual` (backward-compat, olemassa olevat templatet toimivat muuttumatta). `createTemplate`/`updateTemplate` hyväksyvät `parts`-kentän osana patchia. Testit: Vitest-pure — 1 osa (nykyinen käytös säilyy), 2-4 osaa (järjestys säilyy, kaikki resolvoituvat), yli 4 osaa hylätään/typistetään 4:ään. Käyttäjä: järjestäjä (kirjastossa). | V50,V99,V107,T8,T158 |
| T172 | ✓ | Yhdistelmämerkki-render + kirjasto-UI — **pystypino, kuten oikea kyltti-keppi**. Vaatii T170 (iconId kartalle), T171 (parts-data). (a) `src/map/icons.ts`: uusi `comboMarkerSvg(parts: ResolvedPart[], status, ...)` — osat pinottu **pystysuunnassa** järjestyksessä `parts[0]` ylimpänä, sama leveys (`40px`) kaikilla, ohut `1px border-default`-jakoviiva osien välissä (kuten kyltit samassa kepissä). **Yksi ankkuripiste/tip koko pinolle** — kärki-SVG vain pinon alimman osan alla (ei per-osa tippiä), koko pino on yksi kartta-objekti. Yhteinen status-reunus koko pinon ympäri (V87-pattern, ei per-osa). `createSignIcon()` haarautuu combo-caseen kun `parts.length > 1`; `iconAnchor` = `[40/2, totalHeight]` (pinon korkeus = osien summa + tip). (b) `src/ui/sign-library-panel.ts` modaali: nykyinen yksi-ikoni-grid → multi-select (max 4) **järjestettävällä listalla** (lisää/poista osa + ylös/alas-nuolet järjestykselle — järjestys = pinojärjestys). Valitut osat näkyvät pinottuna esikatselussa (`signPreviewHtml` laajenee `modal-helpers.ts`:ssä lukemaan `parts` pystysuunnassa). (c) DESIGN.md §K: ComboSignIcon-sopimus kirjataan (pino-kaava, yksi ankkuri, jakoviiva, status-yhtenäisyys — ks. UX-analyysi 2026-07-07, korjattu pystypinoksi). Testit: Vitest-jsdom (multi-select + järjestys-nuolet toimivat, max 4 -rajoitus UI:ssa), Vitest-pure (comboMarkerSvg-kaavan korkeuslaskenta 1-4 osalla, järjestys säilyy). Playwright: combo-template kartalla näyttää kaikki osat pinossa oikeassa järjestyksessä, yksi ankkuripiste (`e2e/critical-paths.spec.ts`). Käyttäjä: järjestäjä (rakentaa), talkoolainen (näkee kartalla). | T170,T171,V87,V107 |
| T173 | ✓ | Korjaa stale e2e-testi (B75) [korvattu T195:llä]: `e2e/critical-paths.spec.ts:56` `toHaveCount(4)` → laske oikea nykyinen suosikkimäärä (`seedDefaults` + `signCatalog().filter(favorite)`) ja päivitä odotusarvo — TAI löysää assertioon `toBeGreaterThanOrEqual(4)` jos tarkka määrä ei ole testin ydin (testi tarkistaa pickerin avautumisen ja sisällön, ei tarkkaa lukumäärää). Verifiointi: `bunx playwright test e2e/critical-paths.spec.ts -g "kirjaston suosikeilla"` vihreä. Käyttäjä: kehittäjä (testihygienia). | B75 |
| T174 | x | Zoom-step-granulariteetti — `src/app/map-init.ts:14` `L.map('map')` → `L.map('map', { zoomSnap: 0.25, zoomDelta: 0.25 })`. 4x hienompi zoom-jako +/- -napeille ja hiiriscrollille, käyttäjä hyväksyy enemmän scrollausta pienempien hyppyjen sijaan. Tarkistettu ei rikkoudu: `area-overlay.ts:30` (`zoom >= 16`) ja `tile-layers.ts` `maxNativeZoom`-kynnykset toimivat fraktionaalisilla zoom-arvoilla numerovertailuna ilman koodimuutosta. Testit: manuaalinen selainverifiointi (zoom-käyttäytymisen tuntuma ⊥ yksikkötestattavissa mielekkäästi) + varmista ⊥ `e2e/`-testeissä ole zoom-tason tarkkaa oletusarvoa joka rikkoutuisi. Käyttäjä: molemmat (järjestäjä hiiri, talkoolainen pinch). | V108 |
| T175 | x | Merkkien zoom-skaalaus (kaukaa piste, lähellä täysi koko) — `src/map/icons.ts` kaikki 3 icon-tyyppiä (circleSvg/imageMarkerSvg/comboMarkerSvg) käyttävät `L.divIcon` kiinteällä px-koolla, Leaflet ei skaalaa niitä zoomilla. Ratkaisu: CSS `transform: scale()` marker-DOM-elementtiin (`transform-origin: center bottom`, ankkuroituu tip-pisteeseen jolloin sijainti ei driftaa skaalatessa) — EI regeneroida divIcon-HTML:ää. `src/map/markers.ts` (`MarkerManager`) lisää `map.on('zoomend', ...)`-kuuntelijan (sama pattern kuin `area-overlay.ts:26-36`) joka iteroi aktiiviset `L.Marker`-instanssit ja päivittää scalen. Uudet markerit saavat oikean scalen heti luonnissa kartan senhetkisen zoomin mukaan (ei odoteta seuraavaa zoomend-eventtiä). Kaava: `ZOOM_MIN=11 → scale 0.3`, `ZOOM_MAX=17 → scale 1.0`, lineaarinen interpolaatio välissä, clamp rajojen ulkopuolella. EI klusterointia (Leaflet.markercluster) — klusteri-bubla piilottaisi status-renkaan/värin, rikkoisi VISION §UX "tilannekuva ilman numeropaneeleja, kartalta näkee" -periaatteen; pieni CSS-skaalattu ikoni säilyttää statusvärin näkyvissä pienenäkin. Koskee T172-yhdistelmämerkin (pystypino, max 4 osaa) korkeaa korttia erityisesti — testattava että pino skaalautuu yhtenä objektina siististi eikä katkea. Kerros: `src/map/` (Leaflet-glue), ei odotettua `src/logic/`-muutosta. Testit: Playwright (`e2e/critical-paths.spec.ts` — zoomaa ulos/sisään, tarkista marker-elementin `transform`-tyyli muuttuu; erikseen combo-markerille). Käyttäjä: järjestäjä (tilannekuva paljon merkkejä kartalla), talkoolainen (kartta ei tuki näkymää kaukaa zoomattuna). | T172,V107,V109 |
| T177 | x | Korjaa B77: `/auth/invite/:token`-route puuttuu frontendistä. `src/ui/auth-screen.ts`: lisää tokenin tunnistus polusta (pattern kuten `extractSegmentCode`, esim. `extractInviteToken(pathname)` regex `^/auth/invite/([a-z0-9-]+)$`). Tunnistettu → näytä lomake (tunnus, salasana, vahvista salasana) `login-lomakkeen` sijaan. Validoi token heti `GET /api/auth/invite/:token`illa — invalid/expired → virheviesti, ei fallback-loginia. Submit → `POST /api/auth/register` `{token, username, password}`; onnistunut → sama post-login-polku kuin normaali kirjautuminen (piilota lomake, näytä kartta). Salasana+vahvistus ei täsmää → clientin virheviesti ennen POSTia. Testit: Vitest-jsdom (token-tunnistus, lomake näkyy, salasanat eivät täsmää → virhe, onnistunut submit → register-kutsu + post-login-polku, invalid token → virheviesti). Playwright: kutsulinkin avaus näyttää oikean lomakkeen (ei login-lomaketta) — voi laajentaa `e2e/critical-paths.spec.ts`. Käyttäjä: järjestäjä (kutsutaan/resetoidaan). | V110,V76,T51 |
| T176 | ✓ | ImageGalleryPicker — kuva-galleria + zoom-lightbox merkkikirjaston kuvavalintaan. Laajenna SignLibraryPanel edit-modaalin visual-valintaa (`src/ui/sign-library-panel.ts`) kaksi-tabiseksi `[Ikoni]/[Kuva]` (§K DESIGN.md ImageGalleryPicker-sopimus). Kuva-tabi renderöi gridin `src/assets/signs/*.webp`-tiedostoista (Vite glob-import), thumbnail 64×64px valkoisella taustalla (`.sign-image-thumb`), klikkaus valitsee kuvan templatelle (`SignTemplate.imageId = template.id`-konventio, V99 kuva>ikoni>label). Jokaisessa thumbnailissa zoom-nappi (`.sign-image-zoom-btn`) avaa lightboxin (`.sign-image-lightbox`, sama pattern kuin SnapshotModal, z-index 5000 yli edit-modaalin) — suurempi esikatselu koska pienet thumbnailit eivät riitä erottamaan samankaltaisia kylttejä (esim. useita "ylämäki"-variaatioita, "huolto ohituskaista 1-4"). Lightbox sulkeutuu Esc/backdrop/✕, sisältää "[Valitse tämä kuva]"-napin suoravalintaan. Kerros `src/ui/` (Vitest-jsdom: grid render, valinta, lightbox avautuu/sulkeutuu). Vaatii T161 (`convert-signs.ts` ajettava manuaalisesti 91 PNG:lle kansiosta "Kyltit Syöte MTB/" → `src/assets/signs/*.webp`) ja T158 (SignTemplate.imageId + signVisual-precedence, tällä hetkellä rakentamatta) valmiiksi ensin. Käyttäjä: järjestäjä. | V99,V97,T158,T161,T93 |
| T181 | ✓ | Left-panel mobiili-drawer, ei kartan kutistus (B81) — `src/style.css` lisää `@media (max-width:480px)` (ensimmäinen leveys-breakpoint koko projektissa): `#left-panel { position:absolute; top:0; left:0; height:100%; z-index:500; box-shadow: var(--shadow-lg) tai vastaava }` → paneeli overlayaa `#map-area`:n kutistamatta sitä, `#map` saa täyden leveyden. Collapsed-tila (32px strip) pysyy kartan reunassa saavutettavana. Harkitse `src/ui/left-panel.ts` konstruktori: `if (window.innerWidth <= 480) this.collapsed = true` → mobiilissa kartta näkyy heti auettaessa (ensivaikutelma = koko kartta, ei sliver). Toggle kutsuu `map.invalidateSize()` (T179/V112-yhteys) jotta kartta repaintaa oikeaan leveyteen overlayn auettua/sulkeuduttua. Kerros: `src/style.css` (+ pieni `src/ui/left-panel.ts`-default). Testi: Playwright 375px — kartta täysleveä paneelin ollessa auki (overlay, ei sliver); Vitest-jsdom: mobiili-leveydellä konstruktori aloittaa collapsed. Käyttäjä: järjestäjä mobiili-selailu. | V114,V112,T179 |
| T180 | ✓ | PhaseSwitcher-select ei keskeydy overflow-menun ulkoklikki-sulkijasta (B80/V113, korjattu juurisyy) — `src/ui/phase-switcher.ts`: lisää `select.addEventListener('mousedown', e => e.stopPropagation())` ja vastaava `click`-listener select- ja wrapper-elementille, jotta `map-init.ts`:n dokumentti-tason `document.addEventListener('click', () => toolbarMenu.classList.remove('open'))` ei sulje `#toolbar-menu`:a kesken natiivin pudotusvalikon avausta/valintaa. Menu saa silti sulkeutua normaalisti muista overflow-itemeistä (btn-list, btn-layer, gpkg-napit — ennallaan, ei stopPropagationia niille, koska ne ovat "valitse ja sulkeudu" -toimintoja). Kerros: `src/ui/phase-switcher.ts` (ei toolbar-CSS-muutosta — napit olivat jo 44px). Testi: Playwright — overflow-menu auki, klikkaa phase-switcher-select, valitse "Tarkastus" → menu pysyy auki interaktion ajan JA vaihe vaihtuu oikein (`getActivePhase()`); ei regressiota muiden overflow-itemien sulkeutumiskäyttäytymiseen. Käyttäjä: järjestäjä (ainoa joka näkee phase-switcherin, `data-role-hide="talkoolainen"`). | V113 |
| T179 | ✓ | `map.invalidateSize()` kartan resize-tilanteisiin (B79) — (a) `src/ui/left-panel.ts` `toggle()` triggeröi karttapäivityksen paneelin auki/kiinni-vaihdossa: LeftPanel saa konstruktoriin optional `onToggle`-callbackin jonka `main.ts`/`map-init.ts` kytkee `() => map.invalidateSize()`. (b) `src/app/map-init.ts` rekisteröi `window.addEventListener('resize'|'orientationchange', …)` + `window.visualViewport?.addEventListener('resize', …)` → debouncattu (~150ms) `map.invalidateSize()`. (c) karttakontainerin CSS-korkeus `100vh` → `100dvh`/`100svh` (`src/style.css`, `#map`-kontaineri). Verifiointi: Playwright mobiili-viewport 375px — avaa paneeli auki, sulje, scrollaa → kartta täyttää kontainerin (ei harmaata raitaa, laatat kattavat koko alueen). Käyttäjä: järjestäjä nyt, talkoolainen mobiili-kriittisesti myöhemmin (sama pohja). | V112 |
| T178 | ✓ | Yhdistelmämerkin osat (parts) tukevat kuvaa — `SignPart{iconId?,imageId?}` tuki oli jo datamallissa (V107/T171) ja `signVisualParts` resolvoi kuva>ikoni per-osa, mutta osan lisäys-UI (`sign-library-panel.ts` "+ Lisää osa") näytti vain ikoni-gridin. Lisätty osan-lisäys-flow'hun sama [Ikoni]/[Kuva]-tabipari kuin T176:ssa (`.sign-part-visual-tab`, eri luokka kuin päävisualin `.sign-visual-tab` ettei testilaskenta törmää), kuva-tabissa 44×44px thumbnail-grid `signImageIds()`:stä (`.sign-lib-part-image-btn`). Osalistan rivi (`renderPartsList`) näyttää kuva-osalle thumbnailin tekstilabelin sijaan. Mahdollistaa kuva+kuva, kuva+ikoni, ikoni+kuva -yhdistelmät (aiemmin vain ikoni+ikoni oli UI:sta saavutettavissa). Kerros `src/ui/`. Käyttäjä: järjestäjä. | V107,V99,T171,T172,T176 |
| T182 | ✓ | Korjaa B82 — merkkien API-kirjoitukset hiljaa hukkuvat: `src/map/markers.ts` `apiPost()`/`apiPut()`/`apiDelete()` tarkistavat vain `.catch()` (verkkovirhe), eivät `response.ok`:ia. (a) lisää status-tarkistus kaikkiin kolmeen — non-2xx käsitellään virheenä. (b) näytä käyttäjälle näkyvä virheilmoitus epäonnistuneesta tallennuksesta (toast tms.) — merkki EI saa jäädä vain lokaaliksi ilman merkkiä käyttäjälle. (c) selvitä juurisyy MIKSI kirjoitukset ovat tuotannossa oikeasti epäonnistuneet tälle käyttäjälle (sessio vanhentunut / rooli ei admin/järjestäjä / muu 4xx) — kirjaa löydös. (d) jos käyttäjän tekemät merkit ovat vielä talteen saatavissa selaimen puolella (esim. sivu ei ole suljettu, `MarkerManager.markers` muistissa) — tarjoa keino viedä ne uudelleen palvelimelle nyt kun kirjoitus toimii. Testit: Vitest-jsdom (apiPost/Put/Delete non-2xx → virhe-callback laukeaa, ei vain networkerroria). Käyttäjä: järjestäjä (kylttien piirto, data-turva). Kohta (c) RATKAISTU T189-diagnostiikkalokilla → juurisyy = B84 (bearing NOT NULL). | V115,B82,B84 |
| T183 | ✓ | **[Kriittinen]** Durable write-outbox merkeille ja pätkille — T182/V115 näyttää virheen mutta data katoaa yhä refreshillä. Uusi `src/logic/write-outbox.ts` (puhdas, Vitest-pure): jono `{op:POST\|PUT\|DELETE, resource:marker\|segment, id, url, method, body, attempts, createdAt}`, persistoi localStorageen (avain `karttamaster-outbox`), lataa käynnistyksessä. `MarkerManager` (`apiPost/apiPut/apiDelete`) + segment-sync reitittävät KAIKKI kirjoitukset outboxin kautta: enqueue → yritä heti → 2xx poistaa entryn, muu/verkkovirhe jättää jonoon. Retry-triggerit: käynnistys, `window 'online'`, periodinen backoff. V14-pattern korruptoituneelle outbox-datalle (console.warn + removeItem + tyhjä). Testit: enqueue persistoi, 2xx poistaa, epäonnistuminen säilyttää, käynnistyslataus+retry tyhjentää, korruptoitunut→reset. Käyttäjä: molemmat (data-turva, talkoolainen huonolla netillä). | V12,V18,V116,B82 |
| T184 | x | **[Suuri]** `fetchMarkers`/`fetchAllSegments` virhe-erottelu (`src/logic/sync.ts`, `segment-sync.ts`). Palauta erottelu verkko/HTTP-virhe vs. onnistunut tyhjä (esim. `{ok:true, markers}` / `{ok:false}` tai heitä). `main.ts` init näyttää latausvirheen ("Merkkien lataus epäonnistui — päivitä") eikä renderöi tyhjää karttaa onnistuneena; ei ylikirjoita olemassa olevaa tilaa tyhjällä epäonnistuessa. Itsenäinen T183:sta — rinnakkain rakennettavissa. Testit: Vitest 200+[]→tyhjä ok, 500/verkkovirhe→virhetila ei tyhjä. Käyttäjä: molemmat (estää duplikaatit). | V118,B82 |
| T185 | x | **[Suuri]** Pending/unsaved visuaalinen tila. Outboxissa (T183) oleva merkki renderöidään erottuvasti (esim. himmeä/pulssaava reuna tai badge) kartalla (`src/map/markers.ts`) + merkkilistassa (`src/ui/marker-list.ts`) kunnes 2xx vahvistaa → tila poistuu kun outbox-entry poistuu. `/karttamaster-ux` + DESIGN.md tarkistus (kontrasti, ei sekaannu status-väreihin). Testit: Vitest-jsdom pending-luokka listalla; Playwright kartalla jos kriittinen. Käyttäjä: molemmat. | V117,T183 |
| T186 | x | **[Suuri]** Mid-session 401 → re-auth. Keskitetty vastauskäsittely (fetch-wrapper tai outbox-retry-kohta): kirjoituksen 401 → näytä re-login-modaali (`AuthScreen` uudelleen) hukkaamatta outboxia; onnistuneen re-authin jälkeen retry outbox (T183). Talkoolaisen 24h-session erityishuomio. Harkitse erillisenä palvelinmuutoksena talkoolaisen session-TTL 24h→7pv (`server/routes/auth.ts:48,55`). Testit: Vitest-jsdom 401→modaali; re-auth→outbox retry. Käyttäjä: talkoolainen (day-2 kenttätyö). | V119,T183 |
| T187 | x | **[Pieni]** `MarkerManager.add()` reconcile server-vastaus — käytä POST 201 -bodyn palauttamaa kanonista markeria (id, route_ids, distance_from_start) muistitilan vahvistukseen outbox-2xx:n yhteydessä, ettei client-laskettu tila ja backend eroa. Testit: Vitest-jsdom 201-body korvaa muistitilan kentät. Käyttäjä: järjestäjä. | V18,T183 |
| T188 | ✓ | **[Suuri]** Server-SQLite durability-hardening (V120, B83): (a) `server/db.ts` `createDb`: lisää `PRAGMA synchronous=FULL` heti `journal_mode=WAL`-pragman jälkeen — jokainen commit fsync (tuotannon durability VM-stopissa; in-memory-testeille yhdentekevä). (b) `server/index.ts`: rekisteröi SIGINT+SIGTERM-käsittelijä → checkpoint + `db.close()` + `process.exit(0)`, jotta WAL foldautuu main-tiedostoon ennen `auto_stop_machines=stop`-pysäytystä. Erota checkpoint+close erilliseen funktioon (esim. `gracefulClose(db)` `server/db.ts`:ssä) testattavuutta varten. Testit: Bun-integraatio — `createDb` jälkeen `PRAGMA synchronous` palauttaa 2 (FULL); `gracefulClose(db)` kirjoitetulla rivillä tiedostopohjaisessa temp-DB:ssä → uusi `createDb` samaan tiedostoon näkee rivin (ei `:memory:`). Käyttäjä: molemmat (data-turva). | V120 |
| T189 | ✓ | Diagnostiikkalokitus: `server/index.ts` logaa mutatoivat `/api`-pyynnöt (POST/PUT/DELETE) muodossa `[api] <method> <path> -> <status> (<rooli\|ei-cookiea\|sessio-invalid>)`. Ei bodyja eikä salaisuuksia. Paljasti B84:n tuotannosta (`fly logs`); jää pysyväksi, jotta kirjoituksen torjuntasyy (401 sessio / 403 rooli / 500 muu) on aina näkyvissä. | B84 |
| T190 | x | **[Suuri]** Alue-kirjoitukset durable-outboxin kautta (B85, sama korjaus kuin merkit/pätkät T183/T184). `src/logic/area-sync.ts`: reititä `createArea`/`updateArea`/`deleteArea` jaetun outbox-singletonin (`outbox-instance.ts`) kautta `resourceKey: 'area:<id>'` (alueen id on client-generoitu → toimii kuten segment-sync). `updateArea`/`deleteArea` eivät saa enää olla hiljaisia fire-and-forgeteja (poista "silent" `.catch`). `fetchAreas`: erottele lataus-epäonnistuminen tyhjästä (V118) → diskriminoitu union kuten fetchMarkers/fetchAllSegments; `areas-wiring.ts` näyttää latausvirheen eikä hiljaa tyhjää. Testit: Vitest — area-sync outbox-reititys + fetchAreas ok/virhe-erottelu. Käyttäjä: järjestäjä (huoltoalueet). | V115,V116,V118,B85 |
| T191 | x | **[Pieni]** Siirrä zoom-kontrolli oikeaan yläkulmaan (B86 — peittää sivupalkin `▶`-toggle-napin mobiililla). `src/app/map-init.ts:14`: `L.map('map', { zoomSnap: 0.25, zoomDelta: 0.25 })` → lisää `zoomControl: false`, sitten `L.control.zoom({ position: 'topright' }).addTo(map)` map-luonnin jälkeen. Ei logiikkamuutosta, säilyttää T174:n `zoomSnap`/`zoomDelta`. Testit: Playwright `e2e/` — mobiiliviewportilla zoom-kontrolli topright, `▶`-toggle klikattavissa (ei peity). Käyttäjä: talkoolainen mobiili. | V122,B86 |
| T192 | ✓ | Merkkikirjasto backendiin — `templates`-taulu + `/api/templates` CRUD. Korjaa V46-aukon (kirjasto localStorage-only → ei jaeta muille järjestäjille). `server/db.ts`: `CREATE TABLE IF NOT EXISTS templates (id TEXT PRIMARY KEY, label TEXT NOT NULL, color TEXT NOT NULL, description TEXT, favorite INTEGER NOT NULL DEFAULT 0, icon_id TEXT, image_id TEXT, parts_json TEXT, updated_at TEXT NOT NULL, updated_by TEXT)` (idempotent migraatio). `server/routes/templates.ts`: `GET /` (requireAuth, palauttaa kaikki mallit kaikille auth-käyttäjille — kuten `markers.ts` V18), `POST/PUT/DELETE /:id` (requireRole 'admin','järjestäjä'), `rowToTemplate` serialisointi (parts_json parse/stringify). Rekisteröi `server/index.ts`:ssä. Malli 1:1: `server/routes/markers.ts` + `server/db.ts` markers-taulu. Testattavuus: Bun-integraatio `server/` (`test-fixtures.ts` seedTestUsers+authHeaders): GET palauttaa kaikille auth, POST järjestäjä 2xx, talkoolainen 403, parts_json roundtrip. Käyttäjä: järjestäjä. | V123,V18 |
| T193 | ✓ | Template-sync clientissä outboxin kautta (vaatii T192). Uusi `src/logic/template-sync.ts`: `fetchTemplates(): Promise<{ok:true,templates}|{ok:false}>` (V118-erottelu kuten `fetchMarkers`/`fetchAllSegments`), `createTemplate`/`updateTemplate`/`deleteTemplate` reitittävät `outbox-instance.ts`-singletonin kautta `resourceKey: 'template:<id>'`. `sign-library.ts`: localStorage → vain cache (`loadLibrary` offline-fallback, backend on totuus latauksessa). `markers-wiring.ts`: lataa templatet backendistä initissä. Testattavuus: Vitest-pure (outbox-reititys `template:<id>`, `fetchTemplates` ok/virhe-erottelu). localStorage-testit `vi.stubGlobal`-mock. Käyttäjä: järjestäjä. | V123,V124,V116,V118,T192 |
| T194 | ✓ | SignLibraryPanel ryhmittely + kuvaus-render (`src/ui/sign-library-panel.ts`). "Suosikit"-väliotsikko + favorite:true-mallit ensin, sitten "Muut"-väliotsikko + loput label-aakkosjärjestyksessä (`localeCompare 'fi'`). Ei accordionia. `buildRow` näyttää koko `description`-kentän (escapeHtml, V44). Päivitä DESIGN.md §K SignLibraryPanel-sopimus (~rivi 290) ryhmittelyllä — auto-ux buildissa (src/ui-muutos). Testattavuus: Vitest-jsdom (suosikit ennen muita, muut aakkosjärjestyksessä, kuvaus renderöityy, XSS-safe). Käyttäjä: järjestäjä. | V126,V44 |
| T195 | ✓ | Tyhjä seed (vaatii T193). `sign-library.ts`: `seedDefaults`/`signCatalog`/`DEFAULT_IDS` → tyhjä. Kirjasto käynnistyy tyhjänä, järjestäjä rakentaa mallit itse. Päivitä stale e2e (B75/T173): `e2e/critical-paths.spec.ts:56` suosikkimäärä 4/10 → 0 (tai poista count-assertio, testaa vain pickerin avautuminen). EI poista `src/assets/signs/*.webp`-tiedostoja (89 kpl) — odottaa T196:ttä, muuten järjestäjällä ei ole kuvia lainkaan. Testattavuus: Vitest-pure (seed tyhjä) + Playwright (picker avautuu tyhjänä). Korvaa avoimen T173:n. Käyttäjä: järjestäjä. | V125,B75 |
| T197 | ✓ | **[Pieni]** Mobiili merkki-detail -tilankäyttö (B87+B88). (a) `src/ui/marker-detail-modal.ts:60` — poista pakotettu `.marker-detail-note`.focus() (V128): näppäimistö ei saa auota ennen kuin käyttäjä tapittaa kenttää. (b) merkin valinta sivupalkin listasta sulkee overlay-paneelin mobiilissa (V127): collapse `#left-panel` (`.collapsed`) kun ≤480px (`matchMedia('(max-width:480px)')`) — toteuta `onOpenMarkerDetail`-wiringissä (`src/app/markers-wiring.ts`) tai `marker-list.ts:156` select-handlerissa, EI `panTo`-only-polussa. Testattavuus: Vitest-jsdom (`open()` ei aseta document.activeElementia kenttään; select lisää `.collapsed` kun matchMedia≤480px matchaa, ei lisää kun ei) + Playwright mobiiliviewport (merkin valinta → paneeli piiloon, näppis ei aukea). Käyttäjä: järjestäjä mobiili. | V127,V128,V114 |
| T196 | ✓ | (Follow-up, ei MVP-blokkeri) Template-kuvan lataus backendiin — ratkaisee "tuplakuvat"/huonot kuvanimet (käyttäjän alkuperäinen kipupiste). Nyt `imageId` → build-time bundled `src/assets/signs/<id>.webp`, järjestäjä ei voi lisätä omia kuvia ajossa. Uusi backend-blob-storage templateille (malli: `server/routes/markers.ts` `/:id/images` POST). Järjestäjä lataa oman kuvan edit-modaalissa → tallennetaan backendiin → jaetaan kaikille (imageId osoittaa backend-URL:iin, ei bundle-assettiin). VASTA tämä mahdollistaa `src/assets/signs`-dirin + tuplakuvien siivoamisen. Testattavuus: Bun-integraatio (upload+haku) + Vitest-jsdom (modaali-upload-UI). Käyttäjä: järjestäjä. | V123,V131 |
| T198 | ✓ | MarkerVisualRow — jaettu render-helper merkin visuaalille listariveihin (Vitest-jsdom, ei Leafletia). Uusi `src/ui/marker-visual-row.ts`: pure-DOM-funktio `buildMarkerVisual(marker: Pick<SignMarker,'iconId'\|'label'\|'parts'>, opts: {size: number, zoomable: boolean}): HTMLElement` — käyttää `signVisualParts()` (src/logic/sign-visual.ts:33) + `signImageSrc()` kuva>ikoni>label-precedence (V99). Tuplamerkki (parts.length>1): pystypino, max 4 lohkoa, EI kulmabadgea (käyttäjäpäätös: kaksi näkyvää lohkoa riittää kertomaan tuplauden). `opts.zoomable=true`: 44×44px zoom-hit-area (§A) oikeassa alakulmassa, pienempi näkyvä suurennuslasi-glyfi, klikkaus avaa lightbox-modaalin (`stopPropagation`, sama pattern kuin ImageGalleryPicker `.sign-image-zoom-btn`/`.sign-image-lightbox`, DESIGN.md §K rivit 461-470) — EI valitse mitään, pelkkä esikatselu. Syy erilliseen tiedostoon: `segment-details-modal.ts` on jo COMPONENTS.md:ssä liputettu ⚠️ pilkko (monta vastuuta) — visuaali-render ei saa kasvattaa sitä lisää, ja sama helper on uudelleenkäytettävä myöhemmin talkoolaisen SegmentView-näkymässä. Testattavuus: Vitest-jsdom (kuva>ikoni>label-precedence per osa, tupla renderöi kaikki osat max 4 ilman badgea, zoomable=true lisää zoom-napin joka avaa lightboxin, zoomable=false ei lisää nappia). Käyttäjä: molemmat (järjestäjä nyt, talkoolainen tuleva). Lisää DESIGN.md §K uusi MarkerVisualRow-sopimus. | V99,V107 |
| T199 | ✓ | SegmentDetailsModal: yhtenäinen "Merkit & varusteet" -lista (vaatii T198). Korvaa nykyiset kolme erillistä osiota (`buildMarkersSection` rivit 202-229 tekstilista, `buildEquipmentSection`:n automaattinen laskuri rivit 240-251 `"6× left"`-teksti, manuaalinen lista rivit 259-345) yhdellä loogisella listalla `src/ui/segment-details-modal.ts`:ssä — käyttäjäpäätös: "kaikki samassa listassa, yhteinen lista -fiilis". Rakenne: (1) per-merkki-rivit — `buildMarkerVisual(marker, {size:34, zoomable:true})` (T198) + nimi + km + status-pilli, korvaa nykyisen `TYPE_LABELS[m.type]`-tekstin oikealla merkkivisuaalilla; (2) yhteenveto-chip-rivit samassa listassa — groupoi merkit nimen mukaan (`counts`-logiikka säilyy, korvaa vain rivin visuaalin), iso tabular-nums-luku (esim. "7×") + `buildMarkerVisual(marker, {size:28, zoomable:false})` (ei zoomia yhteenvetorivillä) + nimi, korvaa `${count}× ${type}`-tekstin; (3) manuaaliset lisävarusteet — nykyinen count/name-input/poisto-logiikka (rivit 259-345) säilyy täysin ennallaan, vain visuaalinen yhtenäistäminen samaan listarivikorkeuteen/typografiaan kuin (1) ja (2). Ei uutta dataa, ei backend-muutosta — `SignMarker` kantaa jo `label`/`iconId`/`parts`/`color` (src/logic/types.ts:13-28). Testattavuus: Vitest-jsdom (per-merkki-rivi näyttää oikean visuaalin, tupla renderöi ilman badgea, zoom avaa lightboxin oikealla kuvalla, yhteenveto-chip näyttää oikean lukumäärän groupitettuna nimen mukaan, manuaalinen add/edit/poisto toimii ennallaan). Päivitä DESIGN.md §K SegmentDetailsModal-sopimus (rivit 356-370) kuvaamaan yhtenäistetty lista. Käyttäjä: järjestäjä (nyt), sama komponentti uudelleenkäytettävä talkoolaisen SegmentView:ssä myöhemmin. | V99,V107,T198 |
| T200 | ✓ | SignLibraryPanel: `.sign-lib-row` swatch (`src/ui/sign-library-panel.ts:144-169` `buildRow()`) korvataan T198:n `buildMarkerVisual`-helperillä (`src/ui/marker-visual-row.ts`) jotta tuplamerkki-mallit (`template.parts.length>1`) näyttävät sivupalkissa saman pystypino-visuaalin kuin SegmentDetailsModal (T199) — nyt sivupalkki näyttää tuplamerkeistä vain yhden ikonin, epäjohdonmukainen kartan/modaalin kanssa (sama teema kuin B90). Kutsu `buildMarkerVisual(adapted, {size:22, zoomable:false})` (sama koko kuin nykyinen swatch, ei zoomia riveillä — sama perustelu kuin T199 yhteenveto-chipit). Tyyppiadapteri pakollinen: `buildMarkerVisual` vaatii `MarkerVisualInput` jonka `type`-kenttää käytetään sekä värin fallback-hakuun (`SIGN_TYPES`) että top-level-kuvan avaimena (`signImageSrc(marker.type)`) — mutta `SignTemplate`:llä ei ole `type`-kenttää, ja sen kuva-avainkonventio on `t.imageId ?? t.id` (ei type-pohjainen). Adapteri: `{ type: t.imageId ?? t.id, iconId: t.iconId, label: t.label, parts: t.parts, color: t.color }` — `color` on SignTemplatella aina pakollinen kenttä joten `resolveColor()` käyttää sitä suoraan, type-fallback ei koskaan laukea värille, ainoastaan kuva-avaimena. Per-part-kuvat (`parts[].imageId`) resolvoituvat jo yhteensopivasti (`signVisualParts` käyttää `SignPart.imageId` suoraan, sama tyyppi molemmilla). Testattavuus: Vitest-jsdom (rivi renderöi oikean visuaalin: kuva>ikoni>label-precedence per template, tupla renderöi kaikki parts max 4 ilman badgea, zoomable=false ei lisää zoom-nappia, väri täsmää `template.color`:iin). Päivitä DESIGN.md §K SignLibraryPanel-osio (rivi 455 swatch-kuvaus) ja MarkerVisualRow-osio (rivi 478) mainitsemaan sivupalkki-uudelleenkäyttö. Ei uutta dataa, ei backend-muutosta. Käyttäjä: molemmat (järjestäjä sijoittaa sivupalkista nyt, sama komponentti tuleva talkoolaisen SegmentView:ssä). | V87,V99,V107,T198 |
| T201 | ✓ | **[Suuri]** Korjaa B91 — merkkikirjaston uusi/muokkaa-modaalin "Kuva"-gallerian mobiilinäkyvyys (≤480px). `src/ui/sign-library-panel.ts`: modaali (rivi 249 `max-height:80vh;overflow-y:auto`) + galleria (rivi 379 `.sign-image-gallery` `max-height:min(50vh,420px);overflow-y:auto`) = sisäkkäinen scroll-lukko joka collapsaa matalalla mobiiliviewportilla. Korjaus: poista/keventä gallerian sisäkkäinen `overflow-y:auto`+`max-height` mobiililla (anna modaalin oman scrollin hoitaa) TAI mitoita gallerian `max-height` responsiiviseksi niin että ≥1 thumbnail-rivi näkyy. Lisää `src/style.css` `@media (max-width:480px)` -sääntö modaalille/gallerialle (nyt vain `#left-panel` katettu, rivi 2369). EI-server: webp bundlaus + nginx + fly-logit vahvistettu puhtaiksi (ei asset-404/500). Auto-ux (src/ui + CSS) → /karttamaster-ux. Testattavuus: Playwright 375×667 — kirjasto → "+ Uusi malli" → "Kuva"-välilehti → ≥1 `.sign-image-thumb img` näkyvissä (boundingBox height>0, viewportissa). Käyttäjä: järjestäjä mobiili. | V130,B91 |
| T202 | ✓ | **[Suuri]** UX-uudistus "Reittimerkki" — väripaletti. `DESIGN.md §C` + `src/style.css` `:root`/`[data-theme]`-tokenit uusiksi (arvot: V132). Vaihda tumma slate-chrome (`#0f172a`/amber) vaaleaan birkkupaperiin (`--surface-app #EDF1EC`, `--text-body #17221D`, aksentti huomionauha `--accent #F2542D`). Päivitä status-värit + SEGMENT_COLORS/route-värit (`src/logic/segments.ts` SEGMENT_COLORS, `src/logic/sign-picker.ts` SIGN_TYPES-tyyppivärit) uuteen palettiin — säilytä B58-periaate (eri sävyperheet) ja V96 (väri=tunniste, viivatyyli=status). **Kaksi käyttäjän valittavaa teemaa, EI roolisidottu:** Reittimerkki-vaalea (`:root`, oletus) + Kaamos-tumma (`[data-theme="dark"]`, lämmin hiilenmusta). Käyttäjä valitsee teeman itse (toggle tilivalikossa, T203); valinta persistoidaan localStorageen (V5-pattern, esim. `karttamaster-theme`) ja palautuu latauksessa. Korvaa vanhan roolisidotun `daylight`(talkoolainen)/`dark`(järjestäjä)-jaon — teema on käyttäjäpreferenssi, ei rooli; molemmat teemat molempien roolien käytettävissä. **KRIITTINEN — kuvamerkit säilyvät:** `src/map/icons.ts` `circleSvg`/`comboMarkerSvg` + `src/ui/marker-visual-row.ts` `buildMarkerVisual` väri-precedence (`marker.color ?? SIGN_TYPES ?? neutraali`) ja rakenne (valkoinen kuvakortti, `object-fit:contain`, status=kortin reuna V87, combo-pystypino) EIVÄT muutu — vain reunaväri tulee päivitetystä tokenista/SIGN_TYPES-taulusta. Ei hardcoded hex CSS:ään (§C-sääntö). Testattavuus: olemassa olevat Vitest-jsdom väri-/precedence-testit (T198–T200, marker-visual-row) vihreinä ilman struktuurimuutosta; visuaalinen tarkistus kartta↔lista↔modaali. Auto-ux → /karttamaster-ux. Käyttäjä: molemmat (teema kummankin roolin vapaasti valittavissa). | V132,V87,V99,V96,V5,B58 |
| T203 | ✓ | **[Suuri]** Yläpalkki + tilivalikko + Kirjaudu ulos. Yhdistä `index.html` `#toolbar` + `#toolbar-menu` yhdeksi järjestäjän yläpalkiksi: tuotenimi/logo vasemmalle; oikealle Karttatyyli (`#btn-layer`) + Kaikki merkit (`#btn-list`) + **tilivalikko** (avaa: `display_name` `/api/auth/me`:stä + **teemavalitsin** (Reittimerkki-vaalea / Kaamos-tumma, käyttäjän valinta, persistoituu — V132) + "Kirjaudu ulos" → `POST /api/auth/logout` (jo olemassa `server/routes/auth.ts:61`) → `AuthScreen`; sekundaarit Varmuuskopiot `#btn-snapshot-panel` + Vie/Tuo kartta-aineisto `#btn-gpkg-*` valikon alle). Poista `#btn-role` + `RoleSelector` dead code (B48/V80). Wiring `src/app/role-view.ts` + `src/main.ts`. Testattavuus: Vitest-jsdom (tilivalikko renderöi `display_name`; "Kirjaudu ulos" -klikki kutsuu `POST /api/auth/logout` ja avaa `AuthScreen`-DOM:n) + Playwright kriittinen polku (logout → auth-screen); teemavalitsin vaihtaa `data-theme` ja persistoi. Auto-ux → /karttamaster-ux. Käyttäjä: molemmat (tilivalikko järjestäjä; teemavalinta koskee kaikkia). | V133,V132,B48,V80 |
| T204 | ✓ | **[Suuri]** RouteBar-jako → RouteVisibilityControl (järjestäjä) + DriveController (talkoolainen). `src/map/route-bar.ts` niputtaa nyt reittivalinnan + drive-kontrollit (`btn-route-prev/next`, `route-tab-drive`) + km-scrubberin (`#route-track`). Erota: järjestäjälle jää kevyt **reittivalitsin** (näytä/piilota reitti per route, pyöreä pill-kontrolli kartan alakulmassa — ks. Artifact-mockup), EI mato/feedback-widgetin ylimenoa, EI `◀▶`-drive-nuolia, EI scrubberia. Drive-osat (`src/map/drive.ts` DriveMode, `#gps-drive-panel`, `#route-track`) renderöityvät VAIN talkoolaisen näkymässä (V13/V134). V6 säilyy (viimeistä reittiä ei voi piilottaa). Testattavuus: Vitest-jsdom (järjestäjä-DOM:issa ei drive-kontrolleja; reittivalitsin togglaa polyline-näkyvyyden ja estää viimeisen piilotuksen) + Playwright molemmat roolit. Auto-ux → /karttamaster-ux. Käyttäjä: molemmat (eri näkymät). | V134,V13,V6 |
| T205 | ✓ | **[Keski]** StatusPanel-redesign (EI poisto). `src/ui/status-panel.ts` per-reitti-rollup (35/55km %) SÄILYY — 2026-07-04 PM-päätös: ei VISION:n kieltämä oletusnäkymän numeropaneeli, eri aggregaatio kuin T152 per-pätkä-kartta. Muutos vain visuaalinen: poista "päälle liimattu" tumma leijuva palkki, sovita uuteen chrome-kieleen (T202) ja kevennä (esim. reittivalitsimen T204 yhteyteen tai hillitty rivistö). Ei logiikkamuutosta. Testattavuus: V88-regressiosuoja (jsdom `#segment-status-bar`-teksti mutaation jälkeen) pysyy vihreänä; visuaalinen tarkistus. Auto-ux → /karttamaster-ux. Käyttäjä: järjestäjä. | V88,V132 |
| T206 | ✓ | **[Suuri]** Nappijärjestelmä — jaettu `.btn` + variantit korvaa kertakäyttöluokat. `src/style.css`: yksi `.btn` base (44px touch-target §R) + `.btn--primary` (accent), `--secondary`, `--confirm`, `--ghost`, `--danger` (+ koko `--sm`). Korvaa hajanaiset: `.btn-bulk-apply`, `.btn-status-primary/secondary`, `.btn-snapshot-*`, `.btn-segment-*`, `.btn-copy-url`, `.btn-assign-*`, `.btn-approve`, `.btn-mark-inspected`, `.btn-bulk-collect` ym. Säilytä modal-footer-pattern (primary/secondary/destructive, DESIGN.md §K) 1:1 semantiikaltaan. Päivitä DESIGN.md §K nappisopimus yhdeksi totuudeksi. Testattavuus: olemassa olevat Vitest-jsdom modaalitestit vihreinä (napit löytyvät roolinsa mukaan); visuaalinen regressio modaaleissa/paneeleissa. Auto-ux → /karttamaster-ux. Käyttäjä: molemmat. | V135 |
| T207 | ✓ | **[Pieni]** Nimeäminen — käyttäjänäkyvät tekstit käyttäjän kielelle (ei DOM-id:eitä). "MML Taustakartta"→"Karttatyyli" (`#btn-layer`), "Lista"→"Kaikki merkit" (`#btn-list`), "Vie GPKG"→"Vie kartta-aineisto" + "Tuo GPKG"→"Tuo kartta-aineisto" (`#btn-gpkg-*`), "Pätkäjako"→"Reittipätkät" (left-panel segment-osio), "TYÖKALUT"→"Suunnittelu" (`.left-panel-section-title`). Päivitä vastaavat E2E-tekstiselektorit (`e2e/`). Testattavuus: Playwright/jsdom label-tekstit uusilla nimillä. Käyttäjä: molemmat. | V133 |
| T208 | ✓ | **[Suuri]** Karttamerkit yhtenäisiksi neliökorteiksi (V136 — käyttäjätoive: "kaikki samantyylisiä, ei pyöreitä pisaroita, nuoli ei osoita väärään"). `src/map/icons.ts`: `circleSvg` (teardrop-ympyrä `r=14 cy=28` + kärki) → **kortti**-render — pyöristetty neliö (esim. 34–40px, radius ~8). Ikoni/label-merkki: tyyppiväri kortin täyttönä + valkoinen Lucide-ikoni tai `compactLabel` keskitettynä. Kuvamerkki: valkoinen kortti (jo suorakaide T158/`imageSrc`-tier) — yhtenäistä koko + rounding samaksi. `comboMarkerSvg`: jo pystypino (V107) — yhtenäistä leveys/rounding korttitrityylin kanssa. Status = **kortin reuna** kaikilla (V87, `stroke` 3–4px, `suunniteltu`=katkoviiva). Kärki-kolmio (16×8) alareunassa tyyppivärillä, `iconAnchor` kärjen kärkeen. `src/ui/marker-visual-row.ts` `buildMarkerVisual`: yksittäinen ikoni/label `border-radius:999px` → neliökortti (`radius:8`, `.marker-visual-row-single`) samalla väri-precedencellä (V87), jotta sivupalkki/modaali/lista täsmäävät kartan uuteen muotoon. Suuntamerkin nuoli = selkeä glyfi kortissa (ei bearing-rotaatio, T129 poisti bearingin). Päivitä `DESIGN.md §K` SignIcon (rivit ~225-242) + MarkerVisualRow: teardrop→kortti. Testattavuus: Vitest-jsdom (`buildMarkerVisual` yksittäinen renderöi neliön, EI `border-radius:999px`; status=reuna säilyy; combo pino ennallaan; väri=tyyppiväri V87/B90-regressiosuoja) + Playwright kartta (merkki näkyy korttina). Riippuu T202:sta väri-tokenien osalta (voi tehdä rinnakkain, eri tiedostot). Auto-ux → /karttamaster-ux. Käyttäjä: molemmat. | V136,V87,V99,V107,T198 |
| T209 | ✓ | **[Pieni]** Korjaa route-vis-pillereiden tuplautuminen loginissa (B92). `src/map/route-visibility-control.ts buildPills()`: tyhjennä `#route-selector` (`replaceChildren()`) ennen pillereiden luontia → idempotentti render (V137). Tutki + korjaa onAuthenticated/init-tuplakutsu tuoreessa loginissa (`main.ts:122`, `auth-screen.ts finishAuth`) — jos `init()` ajetaan kahdesti, myös muut init-sivuvaikutukset tuplautuvat (ei vain pillerit). Testattavuus: Vitest-jsdom — `new RouteVisibilityControl(...)` kahdesti samaan containeriin → N pilleriä, ei 2N. Käyttäjä: järjestäjä/admin. | B92,V137 |
| T210 | ✓ | **[Pieni]** Karttamerkki lakkaa kasvamasta loppuzoomissa (B93). `src/logic/marker-scale.ts`: `MARKER_SCALE_ZOOM_MAX=17` → nosta kartan maxZoomiin (19, `tile-layers.ts`) tai laske dynaamisesti; salli loppuzoomissa suurin koko (scale ≥1) niin ettei merkki näytä kutistuvan viimeisillä tasoilla. `markerScaleForZoom` pysyy ei-vähenevänä (V138). Testattavuus: Vitest-pure — monotonisuus z∈[11,19], `scale(19) >= scale(18) >= scale(17)`, scale(19)=max. Käyttäjä: molemmat (kartta). | B93,V138 |
| T211 | x | Dead code -siivous (PM 2026-07-08 -interventio): (a) poista map-state approval -endpoint `server/routes/admin.ts` `GET/POST /api/admin/map-state` — approval-konsepti poistettu (V22,B46,V79), endpoint on T48:n jäänne. (b) poista kuollut `pendingSync`/`pushPending`-koodi `src/logic/sync.ts`:stä jos V18+V116-outbox on korvannut sen (T42:n jäänne) — ÄLÄ riko `sync.ts`:n live-osaa (`fetchMarkers`, T172). (c) poista poistettuja endpointteja/funktioita testaavat testit (Bun + Vitest). Käyttäjä: infra/tekninen. | V22,V116,T48,T42 |
| T212 | x | **[Suuri]** Segment route-optional — tehtävämallin ydinrefaktori (VISION §Tehtävämalli). `src/logic/segments.ts`: `Segment.routeIds?`/`startDist?`/`endDist?` → valinnaisia. `createSegment`/`updateSegment`: V11 (startDist<endDist) + V25 (routeIds non-empty) validoidaan VAIN kun reitilliset kentät annettu — reititön tehtävä (kaikki kolme puuttuu) sallittu. `validateNoOverlap` skippaa reitittömät; `cloneSegmentToNextPhase` undefined-safe (ei kopioi olematonta reittiä). Ei riko olemassa olevia reitillisiä pätkiä (kaikilla kentät → validointi ennallaan). Testattavuus: Vitest-pure (reititön luonti onnistuu ilman route-kenttiä; reitillinen validointi ennallaan; overlap vain reitillisille; klooni undefined-safe). Käyttäjä: molemmat. Blokkaa T213–T219. | V139,V11,V25 |
| T213 | x | **[Suuri]** Segments-taulu nullable-migraatio (reititön tehtävä persistoituu). `server/db.ts`: `route_ids`/`start_dist`/`end_dist` NOT NULL → nullable. KRIITTINEN B84-oppi: skeemaristiriita = hiljainen INSERT-500 + datakato → migraatio idempotentti, EI DROP/recreate ilman datan kopiointia transaktiossa. `server/routes/segments.ts` serialisointi (`rowToSegment` + POST/PUT) null/undefined-safe molempiin suuntiin. Testattavuus: Bun-integraatio (reitittömän tehtävän POST→GET roundtrip ilman route-kenttiä; reitillinen ennallaan; migraatio idempotentti tyhjällä + datalla). Käyttäjä: molemmat. Vaatii T212. Blokkaa T214. | V141,V139,B84 |
| T214 | x | **[Suuri]** `resolveTaskMarkers` — tehtävän merkkijoukon kanoninen resolvointi. Uusi `src/logic/task-markers.ts`: `TaskMarkerSource`-interface (strukturaalinen: `routeIds?`/`startDist?`/`endDist?`/`linkedMarkerIds?`/`markerTypeFilter?`) + `resolveTaskMarkers(source, markers): SignMarker[]` = reittifiltteri (jos reitillinen) ∪ `linkedMarkerIds` ∪ `markerTypeFilter`-osumat. `getMarkersForSegment` delegoi tähän (poista duplikoitu route+dist-filtteri); `getPhaseProgress`/`getSegmentStatusCounts` kutsuvat `resolveTaskMarkers`. Sama funktio palvelee `AreaMarker`ia (lisää `linkedMarkerIds?`/`markerTypeFilter?` `area-types.ts`:ään). Testattavuus: Vitest-pure (reitillinen = sama tulos kuin vanha getMarkersForSegment; reititön+linkedIds; reititön+typeFilter; unioni deduplikoi; tyhjä source = tyhjä). Käyttäjä: molemmat. Vaatii T212,T213. Blokkaa T216,T218. | V140 |
| T215 | x | **[Keski]** `SignMarker.templateId` denormalisointi — dynaamisen tyyppisuodattimen prerekvisiitta. `src/logic/types.ts`: lisää `templateId?: string` (denormalisoitu kuten `iconId` V106 / `imageId` V131). Persistointi `markers.template_id` (POST/PUT/fetchMarkers, `server/routes/markers.ts` + `server/db.ts` idempotentti ALTER). Sijoitettaessa merkki templateista → `templateId = template.id`. Testattavuus: Vitest-pure (resolveTaskMarkers typeFilter täsmää templateId:llä) + Bun (template_id roundtrip). Käyttäjä: molemmat. Vaatii T214. Vain jos dynaaminen suodatin v1:ssä; muuten lykättävissä. Blokkaa T218:n dynaaminen keräyslista. | V143,V140,V106 |
| T216 | x | **[Suuri]** Järjestäjän tehtävänluonti + merkkien liitos-UX. `src/ui/segment-creation-modal.ts`: uusi haara reitittömälle tehtävälle — ei route-klikki-flowta, vaan nimi + kuvaus + valinnainen merkkijoukko. `src/ui/segment-panel.ts`: merkkien liitos — (a) poimi merkit kartalta → `linkedMarkerIds` (eksplisiittinen), (b) tyyppisuodatin-picker (SignTemplate-dropdown) → `markerTypeFilter` (dynaaminen). Reitillisen tehtävän luonti ennallaan. Auto-ux → /karttamaster-ux (§K-sopimus reititön-luonnille + liitos-UI). Testattavuus: Vitest-jsdom (reititön luonti tallentaa ilman route-kenttiä; linkedMarkerIds tallentuu; typeFilter-valinta tallentuu). Käyttäjä: järjestäjä. Vaatii T214. | V139,V140 |
| T217 | x | **[Keski]** `SegmentOverlay` reititön-haara. `src/map/segment-overlay.ts`: reititön tehtävä EI piirrä reitti-polylinea (nyt `sliceRoutePoints` olettaa reitin) — skip polyline + edit-handles reitittömälle. Valinnainen rajauslaatikko: jos reitittömällä on geo-alue, renderöi uusiokäyttäen `area-overlay.ts`-geometriaa. Reitillinen render ennallaan. Testattavuus: Playwright (reitillinen piirtää polylinen; reititön ei kaadu; rajauslaatikko renderöityy jos annettu). Päivitä e2e/segments.spec.ts + COMPONENTS.md E2E-sarake. Käyttäjä: molemmat. Vaatii T212. | V139,V96 |
| T218 | x | **[Suuri]** Talkoolaisen tehtävänäkymä — resolvoidut merkit + navigointi + kuittaus + dynaaminen keräyslista. `src/ui/segment-view.ts`: näytä `resolveTaskMarkers`-merkit (V140) korostettuna, navigointi uusiokäyttää T16/T31, kuittaus per merkki + bulk (V9/T17). Dynaaminen keräyslista (skenaario 2): `markerTypeFilter`-tehtävä listaa tyyppiosumat elävästi, "haettu"-kuittaus (kuka tahansa autentikoitu, V143). Näkymä-amend (V142): oma tehtävä kirkas+klikattava, muut himmeä read-only — kytkeytyy `segment-overlay` opacity/interaktio-gateen. Auto-ux → /karttamaster-ux. Testattavuus: Vitest-jsdom (resolvoidut merkit; typeFilter-lista elävä; haettu-kuittaus) + Playwright (oma kirkas / muut himmeä; navigointi). Käyttäjä: talkoolainen. Vaatii T214; dynaaminen osa vaatii T215. | V142,V140,V33,V9 |
| T219 | ✓ | Talkoolainen lisää merkin kentällä — segment-scoped, EI tyyppirajausta (jaettu kirjasto, VISION r254). Poistaa 2-kohtaisen blokin. (backend) `POST /api/markers` (`server/routes/markers.ts:48`, nyt requireRole järjestäjä+) sallii talkoolaiselle kun uusi merkki osuu omaan pätkään: hae seg `session.talkoolainen_code`→assigned_code (uusiokäytä `segments.ts:125-131` ownership-pattern), validoi `route_ids ∩ seg.route_ids ≠ ∅` JA `distance_from_start ∈ [start_dist,end_dist]`; muu → 403. Reititön pätkä (V139): ei distance-rangea → salli jos assignattu seg olemassa. (frontend) place-UI koska `left-panel` piilossa (`style.css:2709`) — suosikki-picker (`listFavorites`, jaettu SignLibrary, samat ← → ↑ ↓/kepit kuin järjestäjä), max 2 nappia (VISION r135). Kirjaa audit (T226). Testattavuus: Bun (oma pätkä ok / vieras 403), Vitest-jsdom (picker), Playwright (sijoitus). Päivitä docs/components/backend.md+ui.md. Käyttäjä: talkoolainen. | V149,V25,V139,V9 |
| T220 | ✓ | Talkoolaisen pätkänäkymän yhtenäistys + seuraava-merkki-ohjaus. `SegmentView` käyttää jaettuja komponentteja (buildMarkerVisual T198, `.btn`-napit, surface/border/status-tokenit, phase-tietoinen edistymispalkki `getPhaseProgress`). Merkkirivit klikattavia → `MarkerDetailModal` (ohjeet/kommentti/tyyppi). Uusi "Seuraava merkki" -hero (asettaminen-phase): ohjaa pätkän ensimmäiseen asettamattomaan merkkiin (`firstUnsetMarker`), iso Aseta + Näytä kartalla + Ei tarpeen; kaikki asetettu → valmis-tila. Mobiili-first (max-height 48vh, 44px-napit). Vitest-jsdom (hero, progress, callbackit) + Playwright (Aseta asettaa 1. merkin). Käyttäjä: talkoolainen. | V144,V145,V3,V9 |
| T221 | ✓ | Yleiskäyttöinen kommentti-systeemi vapaille karttapisteille (T75-jatko, VISION §Avoimet #6). TIETOMALLI LUKITTU (T75-skeema): `{id, targetType:'marker'\|'segment'\|'point', targetId?, lat?, lon?, text, iconId?, authorName?, createdAt}`. BACKEND VALMIS: `comments`-taulu (`server/db.ts`) + `server/routes/comments.ts` (`GET /api/comments?targetType=&targetId=`, `POST` kaikille autentikoiduille ml. talkoolainen, `DELETE /:id` järjestäjä+), mountattu `index.ts`. Validointi: point→lat/lon pakolliset, marker/segment→targetId, text ei-tyhjä. Bun-testit 9 kpl (POST/GET-suodatus/DELETE-auth/validointi). UI-SLICE ✓ (build 2026-07-10): client `src/logic/comments.ts` + `src/ui/comment-thread.ts` (Leaflet-vapaa lanka ∴ kerrosraja ⊥ rikkoudu) → MarkerDetailModal + SegmentView; kartta-render+poisto `src/map/comment-layer.ts` (`targetType='point'`); 29 Vitest. SIJOITUS-UI ✓ T237 (2026-07-27): kartan ⋯-nappi & merkkipickerin alapalkki → point-kommentti; selainvahvistus `e2e/t237-huomio.spec.ts` 3/3 ∴ status ✓. Per-merkki-kommentti (`locationNote`) toimii jo erikseen (T228 löydettävä herosta). Käyttäjä: molemmat. | V13,T41,T75 |
| T222 | ✓ | Talkoolaisen merkin siirto kentällä — segment-scoped (VISION r252 "tarkenna sijaintia"). TOTEUTETTU: (backend) `markers.ts` PUT V150 — talkoolaisen jokainen PUT vaatii oman pätkän merkin (auktoris. olemassa olevasta rivistä); identiteettikentät (type/description/icon/image/template/parts) 403; siirto vaatii uuden dist∈range (V150b); status/location_note-aukko suljettu (Bun: oma-status 200, vieras-status 403, siirto-range 200, ulos-raahaus 403, identiteetti 403, codeless 403). (frontend) `markers.ts` `setDraggablePredicate` — vain oman pätkän merkit draggable (wiring `currentSegmentMarkerIds`; re-apply bounds-muutoksessa); verifioitu playwright 7 oma/19 vieras. Hero "Siirretty" enabloitu (`onMoveMarker` → panTo+ohje). E2E deferred (vaatii segment+marker-seed-infran; turvaraja Bun-katettu). Audit (T226/V152) → kun T226 rakennettu. NYKYTILA (ck:review): `markers.ts:135` positionFields-gate → position-kentät jo **403 talkoolaiselle** (siirto täysin estetty), MUTTA `status`+`location_note` ungated → talkoolainen muuttaa ne missä tahansa merkissä (V150-aukko, suljettava). (backend) `PUT /api/markers/:id`: talkoolaiselle salli position-kentät JA status/location_note VAIN kun OLEMASSA oleva merkkirivi kuuluu hänen pätkäänsä (auktorisointi olemassa olevasta rivistä, EI payloadin uudesta sijainnista; `segments.ts:125`-pattern); siirto lisäksi vaatii uuden `distance_from_start ∈` oman pätkän range±ε (ei raahaa ulos pätkästä). Vieras → 403. (frontend) vain oman pätkän merkit `draggable` (`markers.ts:347` gate rooli+`getMarkersForSegment`), muut read-only/himmeät (V142). Touch-siirto säilyy (V15). Kirjaa audit ennen-tilalla (T226/V152). Testattavuus: Bun (oma ok / vieras 403 sekä position ETTÄ status; ulos-raahaus estyy), Playwright (oma drag persistoituu, vieras ei draggable). Käyttäjä: talkoolainen. | V150,V15,V43,V142 |
| T224 | x | **[Suuri]** Talkoolaisen pätkäkeskeinen näkymä — "tämä on SUN pätkä" -reshape. Rakentuu T220-heron + T223-kokoontaiton päälle; ERILLINEN T218:sta (resolveTaskMarkers/dynaaminen keräyslista pysyy T218 ⊥ blokkaa tätä). Teema VISION r36 "oma pätkä riittää" + r55. Tiedostot: `src/ui/segment-view.ts`, `src/map/route-bar.ts`, `src/app/markers-wiring.ts`, `index.html`, uusi `src/logic/segment-zoom.ts`. Käyttäjä: talkoolainen. 6 osaa: **(D) Zoom-to-segment latauksessa** — `/s/<koodi>` → kartta fittaa OMAAN pätkään (`getMarkersForSegment`-merkit + segment-overlay bounds), ei koko karttaan. Uusi pure `src/logic/segment-zoom.ts`: `chooseSegmentView(bounds, viewportPx, maxUsefulZoom) → {mode:'fit'\|'anchor', anchorEnd?:'start'}` — pätkä mahtuu käyttökelpoiselle zoomille → fit; liian pitkä → anchor startDist-päähän. Map-glue markers-wiring soveltaa fitBounds/setView. (Vitest-pure helper + Playwright fit latauksessa.) **(A) Alapalkki: pätkä-yhteenveto reittitabien tilalle** — `route-bar.ts:91-113 buildTabs` renderöi 35km/55km-tabit vain talkoolaiselle (markers-wiring:180) = kokonaiskuva ⊥ "oma pätkä riittää". Talkoolaiselle: pätkän pituus (endDist−startDist km) + merkkimäärä. Järjestäjän RouteBar/RouteVisibilityControl ennallaan (V134). km-scrubber `#route-track` ? jää pätkän sisäiseen navigointiin. (Vitest-jsdom + Playwright.) **(B) Seuraava-merkki-hero laajennus** — (b1) `firstUnsetMarker` (V145) näkyy KARTALLA koko ajan pienempänä persistent-badgena/overlaynä. (b2) primary 2 nappia: `✓ Aseta` + `Näytä kartalla` (VISION r135 max 2). (b3) `⋯`-overflow: "Ei tarpeen" (onSkipMarker, jo T220) + "Siirretty" (→T222, placeholder jos T222 kesken) + "Ota kuva"/"Laita kommentti" DISABLED "tulossa" (→T221 kommentti-systeemi, ⊥ toteuteta tässä). "Kaikki merkit" ⊥ overflowiin (oma välilehti, osa E). Nykyhero: segment-view.ts renderNext r144-225. **(C) Varustelista omaksi välilehdeksi + talkoolainen-muokkaus** — nyt readonly (renderEquipment r431-468, T27). Muutos: oma välilehti (osa E), näkyvämpi, talkoolainen lisää/poistaa/muokkaa manuaalirivit (`EquipmentItem[]`) itse (VISION r42/239, valmisteluvaihe). Auto-laskuri tyypeittäin pysyy readonly. Tallennus V38 + V93 (talkoolainen sallittu omalle pätkälle). (Vitest-jsdom; localStorage-testit `vi.stubGlobal`-mock.) **(E) Välilehtirakenne** (edellytys B/C — ei nyt olemassa) — tab-shell: [Pätkä/navigointi] · [Kaikki merkit] · [Varusteet]. Pätkä/navigointi = hero+progress+description+bounds+inspect+purku-bulk. Kaikki merkit = nykyinen renderMarkers-lista (klikattava → MarkerDetailModal, V144-suodatus säilyy). Varusteet = osa C. Kokoontaitto (T223) + mobiili-first (max-height 48vh, 44px) säilyy. (Vitest-jsdom tab-vaihto.) **(F) CLEANUP: alapalkin tupla pois** — `#gps-drive-panel` (index.html:75-82 "⇒ Seuraava"+"✓ Aseta") = GpsDrivePanel duplikoi heron; markers-wiring:207-223 jo tunnistaa "kaksi identtistä Aseta". Hero = ainoa ohjaus (V148). Poista duplikoidut Seuraava/Aseta/Ei tarpeen -napit + `#gps-drive-panel` + wiring. GPS-drive (T30/T31) ei saa rikkoutua — scrubber-palaute ? säilyy osana A:n pätkä-alapalkkia ilman duplikoituja kuittausnappeja. Ei dead codea. (Playwright: alapalkin Aseta-tupla poissa, hero-Aseta toimii.) DESIGN §K SegmentView (DESIGN.md r448-464) päivitä: välilehdet, pätkä-alapalkki, hero-kartta-badge, muokattava varuste-tab, overflow. Auto-ux → /karttamaster-ux. **TOTEUTUS (iteroitu käyttäjäpalautteella):** (E) EI välilehtiä — yksi sarake; "Kaikki merkit" = yläpalkin nappi (ei tupla). (A/F) alapalkki `#route-bar` PIILOTETTU kokonaan (ei summarya, ei ◀▶ jotka ohjasivat koko reittiä) — `gps-drive-panel.ts` poistettu. (C) varustelista omana tilavana modaalina `EquipmentModal` (`src/ui/equipment-modal.ts`) Tallenna-napilla, ei ahdasta inline-editoria. (b1) `NextMarkerHighlight` (`src/map/next-marker-highlight.ts`) korostaa seuraavan merkin kartalla. (D) `segment-zoom.ts` `planSegmentZoom` fit/anchor. B:n kuva/kommentti→T221, siirto→T222. | V148,V145,V144,V38,V93,V134,V33,V9,V3 |
| T223 | ✓ | Talkoolaisen pätkänäkymän kokoontaitto (mobiili: kartta esiin). `SegmentView` header-chevron kutistaa näkymän otsikkoon+edistymispalkkiin (`.segment-view--collapsed`) → kartta saa lähes koko ruudun navigointiin. "Seuraava merkki" -heron "Näytä kartalla" panoroi kartan merkkiin (`onShowOnMap` → `panTo`, EI avaa modaalia) JA kutistaa näkymän automaattisesti. Ratkaisee mobiili-UX-ongelman jossa 48vh-pätkänäkymä + alapalkki jättivät kartan pieneksi. Vitest-jsdom (chevron toggle, näytä-kartalla kutistaa). Käyttäjä: talkoolainen. | V127 |
| T225 | ✓ | Talkoolaisen merkin poisto = pehmeä oletus, segment-scoped. Suunniteltu (järjestäjän luoma) merkki → talkoolainen EI kovapoista vaan status `ei_tarpeen` + syy (uusiokäytä T10/`marker-status.ts` + T220 `onSkipMarker`; syy → `location_note` tai uusi kenttä, tarkista tallennus). Itse lisätty merkki (`created_by`=oma, T226) → kova DELETE sallittu. (backend) `DELETE /api/markers/:id` (`markers.ts:163`, nyt järjestäjä+): talkoolaiselle VAIN jos merkki (a) omalla pätkällä JA (b) oma luoma; muu → UI ohjaa ei_tarpeen-poluun, ei DELETE. Järjestäjä näkee mikä ohitettiin + syy. Kirjaa audit (T226). Testattavuus: Bun (DELETE oma-luotu ok / suunniteltu 403; ei_tarpeen persistoituu), Vitest-jsdom (UI-polku). Käyttäjä: talkoolainen. Vaatii T226. | V151,V9 |
| T226 | ✓ | Merkki-audit-loki (tietomalli + kirjaus). Uusi taulu `marker_audit` (`server/db.ts` idempotentti CREATE + migraatio V121-tyyliin): `id, marker_id, action('add'\|'move'\|'remove'\|'status'), actor(display_name), actor_role, segment_code(null jos järjestäjä), created_at, payload_json`. Jokainen merkkimutaatio (POST/PUT/DELETE `markers.ts`) kirjaa rivin SAMASSA transaktiossa (V152). `markers.created_by` lisätään (erottaa self-added vs suunniteltu, T225-tarve; `updated_by` on jo). `GET /api/audit?segment_code=X` (requireRole admin/järjestäjä) → aikajärjestys. Testattavuus: Bun (mutaatio→audit-rivi; GET suodattaa; created_by tallentuu). Päivitä docs/components/backend.md. Käyttäjä: järjestäjä/admin. | V152,V120 |
| T227 | ✓ | Per-pätkä aktiviteettinäkymä + massaperuutus (supervision). Admin/järjestäjän näkymä (URL/pätkä-modaalin välilehti): lista "kuka teki mitä milloin" segment_code-suodatuksella (kuluttaa `GET /api/audit`, T226). Massaperuutus: "nämä N merkkiä lisätty pätkällä X → poista kaikki?" → `POST /api/audit/undo {segment_code, action, since?}` (requireRole admin/järjestäjä) poistaa/palauttaa mutaatiot atomisesti (V153: bulk DELETE lisätyille / status-restore siirretyille), `window.confirm` ennen (tuhoava, V102). Vastaa spammaus-huoleen. Testattavuus: Vitest-jsdom (lista + confirm-flow), Bun (undo atominen, oikeat rivit). Päivitä docs/components/ui.md+backend.md. Käyttäjä: järjestäjä/admin. Vaatii T226. | V153,V152,V102 |
| T228 | x | [Pieni] Talkoolaisen SegmentView layout-fix — kartta primary oletuksena (jatkaa T224). ONGELMA: `#segment-view` `max-height:48vh`+otsikko = ~55% ruudusta ∴ kartta taitteen alla; inline `.segment-view-list` duplikoi "Kaikki merkit"-modaalin (bulk + rivi→MarkerDetailModal) → dominoiva ei-ydinkomponentti (VISION r131 kartta=päänavigointi). MUUTOS (`src/ui/segment-view.ts`, Vitest-jsdom): (1) POISTA `.segment-view-list` kokonaan — `markerListEl`, `renderMarkers()`, `buildMarkerRow()`, `<ul>`-append + `renderMarkers`-kutsu `update()`:ssä. Merkkidetalji säilyy 2 kautta: kartan merkin tap (`markers-wiring.ts:145` `setOnMarkerClick`→`onOpenMarkerDetail`) + "Kaikki merkit"-modaalin klikattavat rivit (`marker-list.ts:149-162`→`onOpenMarkerDetail`); V144-suodatus säilyy. (2) Done-tila slim: `renderNext()` `!next`-haara iso accent-kortti `.segment-view-next-done` → matala ~44px rivi "✓ Kaikki asetettu 🎉", ⊥ accent-vasenreuna. (3) CSS `src/style.css`: `#segment-view` `max-height` 48vh→38vh; poista `.segment-view-list` collapsed-selektorista (~r1420); done-slim. Kartta `#map flex:1` → ~65%, seuraava merkki näkyy ⊥ scroll. SÄILYY: hero+2nappia (V148), Varustelista-modaali (T224/C), bounds (T78), tarkastus/purku, NextMarkerHighlight (T224/b1), collapse (T223). TESTIT: `tests/t224-segment-view.test.ts` — assert `.segment-view-list` ⊥ renderöidy + hero/varuste säilyy; `e2e/segments.spec.ts` — inline-lista-odotus → marker-modaali. DESIGN.md §K SegmentView (r458/461/466). Auto-ux → /karttamaster-ux. Käyttäjä: talkoolainen (mobiili, kartta=päänavigointi, max 2 nappia). LISÄKSI: hero-overflown "Laita kommentti" enabloitu — uusi `onComment`-action → `onOpenMarkerDetail` (per-merkki-kommentti/locationNote löydettäväksi herosta; geneerinen kommentti = T221). | V148,V144,V33,V9,V3,T224,T14 |
| T229 | x | [Pieni] Talkoolaisen merkin-lisäys-affordanssi. NYKYTILA: talkoolainen VOI lisätä merkin tuplaklikkaamalla karttaa (`main.ts:102` `map.on('dblclick')`→`placeMode.openPicker`, rooliton floating-picker), MUTTA ei löydettävää nappia — sidebar-armaus (`markers-wiring.ts:281`) asuu `#left-panel`issa joka on piilotettu talkoolaiselta (`style.css` `body[data-role=talkoolainen] #left-panel{display:none}`). Mobiilissa tuplatäppäys ⊥ ilmeinen (VISION r254 "lisää merkki jota ei suunniteltu"). MUUTOS: näkyvä "+ Merkki" affordanssi talkoolaiselle — yläpalkin nappi TAI hero-overflown kohta — joka armeeraa place-moden / avaa floating-pickerin (`placeMode.openPicker` kartan keskelle tai GPS-sijaintiin). Uusi merkki liitetään talkoolaisen pätkään (V144-suodatus). `src/ui/segment-view.ts` tai `index.html`-toolbar + `markers-wiring.ts`. Auto-ux → /karttamaster-ux (nappi-sijainti + touch 44px). Vitest-jsdom (nappi näkyy talkoolaiselle, klikki triggeröi) + Playwright (merkki syntyy). Käyttäjä: talkoolainen. | V144,V9,T224 |
| T230 | x | [Keskikokoinen] "Merkitse pätkä valmiiksi" asettaminen/purku-vaiheelle. NYKYTILA: vain tarkastus-vaiheella on eksplisiittinen boolean-kuittaus (`Segment.inspected`, T147); asettaminen/purku valmius on IMPLISIITTINEN (`getPhaseProgress` done>=total merkkien statuksista). VISION r49/r259 "Merkkaa pätkä valmiiksi" = talkoolaisen tarkoituksellinen signaali järjestäjälle, eri kuin merkkimatematiikka (voi olla valmis vaikka osa ei_tarpeen). MUUTOS: (data) `Segment.completed?: boolean` (`src/logic/segments.ts`, additive) + backend sarake `segments.completed INTEGER DEFAULT 0` (idempotentti CREATE/migraatio `server/db.ts` V121-tyyliin) + `PUT /api/segments/:id` sallii `completed` talkoolaiselle VAIN omalle pätkälle (V93-pattern kuten `inspected`, auktorisointi olemassa olevasta rivistä). (UI) `segment-view.ts`: asettaminen/purku-vaiheessa "Merkitse pätkä valmiiksi"/"Merkitse keskeneräiseksi" -nappi (kuten inspect-toggle T147), erillään merkki-kuittauksesta. `SegmentPanel`-rivi (järjestäjä) näyttää "Valmis ✓" completed-lipusta. `segmentLineState` huomioi completed (valmis-väri kartalla). Bun (completed-PUT talkoolaiselle omalle pätkälle OK, vieraalle 403) + Vitest-jsdom (nappi-toggle) + Vitest-pure (segmentLineState). Auto-ux → /karttamaster-ux. Käyttäjä: talkoolainen + järjestäjä (tilannekuva). | V93,V90,V9,T144,T147 |
| T231 | x | [Pieni] Prev/next-navigointi asettamattomien merkkien välillä (Vitest-pure, `src/logic/navigation.ts`, laajenna T16). `unsetMarkersOrdered(markers): SignMarker[]` = `status==='suunniteltu'`-merkit `distanceFromStart` asc (sama predikaatti kuin `firstUnsetMarker` r7-14). `stepUnset(markers, currentId, dir:1\|-1): SignMarker\|null` = seuraava/edellinen asettamaton listalta; clamp päihin (EI wrap-around); tuntematon/asetettu id → `firstUnsetMarker`. Hero pysyy "seuraava"-ohjaimena (oletus `firstUnsetMarker` = pienin dist), ◀▶ selaa vain asettamattomia; asetettu merkki katoaa elävästä listasta. Testattavuus: Vitest-pure (pakollinen): järjestys, step eteen/taakse, clamp päissä, tyhjä→null, kaikki asetettu→null, tuntematon id. Käyttäjä: talkoolainen. Avaa: T232. | V145,V9 |
| T232 | x | [Suuri] Talkoolaisen kokoava "seuraava merkki" -hero — tiivis, muut toiminnot pois aina-näkyvistä. `src/ui/segment-view.ts` + `src/app/markers-wiring.ts` (GPS-wiring) + `src/map/next-marker-highlight.ts`. Rakentuu T220-hero + T228 päälle. **(A)** hero = ainoa aina-kirkas osio; siirrä pois oletusnäkyvistä: "Merkitse pätkä valmiiksi" (`completeSection` r111/r425) + "Muokkaa pätkän rajoja" (`boundsSection` r138/r455) → PANEL-TASON sekundäärivalikko ("Lisää ⋯", phase-riippumaton, EI marker-hero-overflow joka on vain asettaminen+next → V158). Varustelista-nappi (`renderVarusteBtn` r129/r384) EI heroon eikä sekundääriin vaan YLÄPALKKIIN (T233/V155) — poista in-panel-nappi. Tallennuslogiikka säilyy (`onEquipmentChange` V38/V93, `onComplete` V93, `onEditBounds` V43). **(B)** GPS-toggle heroon (PM-päätös: hero, EI valikkoon — VISION phase 3 core). Nyk. `#btn-gps` toolbarissa, handler `map-init.ts:66-78` (`GpsNavigator` toggle + `.gps-active`) → siirrä hero-napiksi. PLUMBING: `GpsNavigator` luodaan `map-init.ts:66` EIKÄ näy `wireMarkers`ille → altista (`initMap` palauttaa `gpsNavigator` TAI instanssi siirtyy wireMarkersiin) jotta hero-nappi togglaa start/stop + aktiivi-tila. GPS-toggle ohjaa `GpsNavigator` (T30, oma sijainti) — ERILLINEN driveModesta (T31/route-bar ◀▶), driveMode ⊥ rikkoudu. Päivitä E2E critical-paths "GPS-paikannin" (selektori `#btn-gps` → hero-GPS-nappi). toolbarista poistuu (T233). **(C)** ◀▶ heroon (`stepUnset` T231): selaa vain asettamattomia, hero näyttää valitun (oletus `firstUnsetMarker`), ✓ Aseta kohdistuu näkyvillä olevaan merkkiin. Primary max 2 (Aseta + Näytä kartalla, V148/VISION r135) säilyy; ◀▶ = navigaatiokontrollit. **(D)** otsikko: pätkän pituus (`build` r347-352 `.segment-view-range`) "Pätkäsi · 1.0 km" (endDist−startDist johdettu, `.toFixed(1)`) päänäyttönä, range "10.2–11.2 km" `.text-muted`-metana; ei uutta datakenttää. **(E)** "+ Merkki" (`#btn-add-marker`, T229) hero ⋯-overflowiin (`placeMode.openPicker` V149); toolbarista poistuu (T233). **(F)** highlight näkyvämmäksi: `next-marker-highlight.ts` `.next-marker-ring` (T224/b1) vahvempi accent/pulssi; seuraa heron valitsemaa merkkiä (◀▶ päivittää korostuksen, `updateNextHighlight` r89-95/r240); vain asettaminen-phase. Säilyy: V148 (hero ainoa ohjaus), V33/V142, collapse T223, max-height 38vh (T228), 44px-touch. Testattavuus: Vitest-jsdom (GPS-nappi näkyy, ◀▶ vaihtaa merkkiä + Aseta kohdistuu, pituus-otsikko, varuste/valmis/rajat ⊥ aina-näkyvissä; laajenna `tests/t14-segment-view.test.ts` + `tests/t224-segment-view.test.ts`) + Playwright (GPS-toggle hero:sta, ◀▶ + Aseta, highlight seuraa; `e2e/segments.spec.ts`). Auto-ux → /karttamaster-ux. DESIGN.md §K SegmentView (r452-469) päivitä. Käyttäjä: talkoolainen. Vaatii T231. | V156,V158,V159,V148,V145,V33,V142,V38,V93,V43,V9,V3 |
| T233 | x | [Keskikokoinen] Talkoolaisen yläpalkin reorg + mobiili-fix + orpo-padding-korjaus (B101). `index.html`, `src/style.css`, `src/app/map-init.ts`. Nyk. `#toolbar-actions` (index.html:13-19) 5 nappia ei mahdu mobiiliin. UUSI talkoolais-toolbar = TASAN: "Kaikki merkit" (`#btn-list`, `markers-wiring.ts:346`) + "Varustelista" (uusi toolbar-trigger → `EquipmentModal`; varuste-nappi siirtyi pois panelista T232) + "⋯" (`#btn-menu`). Karttatyyli (`#btn-layer`, `map-init.ts:28-42`) → ⋯-valikkoon (`#toolbar-menu`). GPS (`#btn-gps`) + Merkki (`#btn-add-marker`) POISTETAAN talkoolais-toolbarista (siirtyneet heroon T232). Järjestäjän toolbar ennallaan (V133/V134). Responsive: talkoolais-toolbar ⊥ rivinylitä/hajoa ≤360px. **BUGI B101:** orpo alapalkki-padding — `body[data-role="talkoolainen"] #app-main{padding-bottom:84px}` (style.css:2774) + `#app{padding-bottom:84px}` (style.css:145) varaa 84px `#route-barille` joka on talkoolaiselle `hidden` (`markers-wiring.ts:271`, V148) → kuollut alagap → poista talkoolaiselta route-bar-padding (V157). Testattavuus: Vitest-jsdom (talkoolais-toolbar-napit = oikea joukko, GPS/Merkki ⊥ toolbarissa) + Playwright (mobiili ≤360px: toolbar ⊥ hajoa, 44px-touch; alagap poissa). Auto-ux → /karttamaster-ux. DESIGN.md §K Toolbar (r135-143) päivitä. Käyttäjä: talkoolainen. Vaatii T232. | V155,V157,V148,V133,V134 |
| T234 | x | [Keski] Pilkko (⚠️ COMPONENTS §Pilkkohälytykset, arkkitehtuuri 2026-07-10): `segment-view.ts` (687r) hero-irrotus. **TOTEUTUS: `SegmentHero` (`src/ui/segment-hero.ts` 248r) — `render()` + `navStep` + selectedNavId-tila (V159) + `markerLabel`-export. `SegmentView` 562r koordinaattori. Testit vihreät e/j (70→70, koko suite 1051).** `SegmentView.renderNext()` (~180r yhdessä metodissa, r217-397) + `navStep` + `renderProgress` + `renderGpsBtn` → uusi `src/ui/segment-hero.ts` (SegmentHero-komponentti TAI pure-render-funktio). `SegmentView` jää koordinaattoriksi + sekundäärisektiot (inspect/complete/bounds/more). KÄYTTÄYTYMINEN SÄILYY — behavior-preserving refaktori, EI uutta UX:ää. Verification: testit vihreät ENNEN JA JÄLKEEN (`tests/t232-segment-view-hero.test.ts`, `tests/t224-segment-view.test.ts`, `tests/t14-segment-view.test.ts`). Kerros src/ui/ (Vitest-jsdom). Käyttäjä: talkoolainen. | V148,V145,V159,V142 |
| T235 | x | [Suuri] Pilkko (⚠️ COMPONENTS §Pilkkohälytykset, arkkitehtuuri 2026-07-10): `sign-library-panel.ts` (887r, suurin moduuli) malli-modaalin irrotus. **TOTEUTUS: `SignTemplateModal` (`src/ui/sign-template-modal.ts` 729r) — `open()`/`openImageLightbox`/`closeModal` + modal-tila. `SignLibraryPanel` 194r lista/grid. XSS-escape (B19/V44) säilyi verbatim. Testit vihreät e/j (103→103, koko suite 1051).** `openModal()` (625r YHDESSÄ metodissa, r205-830) + `openImageLightbox` + `closeModal` → uusi `src/ui/sign-template-modal.ts`. `SignLibraryPanel` jää listaksi/gridiksi (`render`/`buildGroup`/`buildRow`/`bindEvents` ~200r). KÄYTTÄYTYMINEN SÄILYY, XSS-escape säilyy (B19/V44). Verification: testit vihreät ENNEN JA JÄLKEEN. Kerros src/ui/ (Vitest-jsdom). Käyttäjä: järjestäjä. | V99,V46,V44 |
| T236 | x | [Keski] Pilkko (rajatapaus 410r, ⚠️ COMPONENTS §Pilkkohälytykset, arkkitehtuuri 2026-07-10): `markers.ts` persistointi-glue irrotus. **TOTEUTUS: evaluoitu → KEEP (ei pilkota). Peruste: API-glue on JO eriytetty V116-outboxiin (`write-outbox.ts`/`outbox-instance.ts`); `apiPut`/`apiDelete` = 5-rivin pass-through outbox.enqueueen. `reconcileFromServer`/`addImage` ovat domain-state-mutaattoreita (kirjoittavat `this.markers`-taulukkoon), ei glueta → irrotus jakaisi ainoan totuuslähteen kahteen moduuliin = VÄHEMMÄN koheesiota. `reload()` reassignaa `this.markers`. Pilkkomispäätös: koosta EI, pilko vastuusta — vastuut eivät aidosti eriydy moduulirajalla. V18/V116/V2 säilyvät (mikään ei liikkunut).** `MarkerManager` sekoittaa 3 vastuuta: domain-state + Leaflet-render + backend-API (`apiPost`/`apiPut`/`apiDelete`/`reconcileFromServer`/`addImage`). Harkitse: API-glue → oma moduuli (durable-outbox V116 -yhteensopiva) TAI jätä jos vastuut EIVÄT aidosti eriydy (pilko vastuusta, EI koosta — arkkitehtuurisääntö). KÄYTTÄYTYMINEN SÄILYY. Verification: testit vihreät ENNEN JA JÄLKEEN. Kerros src/map/→src/logic/. Käyttäjä: molemmat. | V18,V116,V2 |
| T237 | ✓ | Vapaa-pisteen kommentti-sijoitus (T221-jatko, VISION §Avoimet #6): kartan klikkaus/contextmenu → pudota `targetType:'point'`-kommentti valittuun lat/lon. UI: "Lisää kommentti kartalle" -moodi (crosshair-cursor) → klikkaus avaa mini-lomakkeen (`text` + `iconId?` + `authorName?`) → `POST /api/comments` (backend valmis T221). Render+poisto jo `src/map/comment-layer.ts` (T221). ∀ autentikoitu ml. talkoolainen (V13). Vitest-jsdom (lomake) + Playwright (klikkaus → ikoni ilmestyy). Käyttäjä: molemmat. **AMEND 2026-07-25 (käyttäjäpyyntö "huomio jonka voisi laittaa kartalle merkin mukaisesti, mutta se ei menisi merkkeihin", esim. 'tämä voisi korjata'):** (a) V245 on tämän taskin hyväksymisehto — huomio ⊥ saa vuotaa merkkilaskureihin, jäsenyyteen, navigaatioon eikä varustelistaan; Vitest-pure-regressio: pätkä jolla 3 merkkiä + 2 point-kommenttia → `getPhaseProgress`/`getSegmentStatusCounts`/`getMarkersForSegment` palauttavat 3, ⊥ 5. (b) Sijoitusmoodi ! olla SAMA moodikieli kuin muilla kartan tiloilla, ⊥ uusi rinnakkainen: kartan muokkaustila (`src/logic/map-mode.ts`, V218/T307) + `.map-mode-pill` (V219/T308) ovat olemassa ∴ "Lisää huomio" -moodi näkyy pillerinä & Esc peruu (escape-chain, §UX "toimii loogisesti läpi koko UI:n"). Talkoolaisen sisääntulo ! olla ⋯-overflow'sta (VISION max 2 primary-nappia, hero-rivi on täynnä: Aseta + Näytä kartalla). (c) DESIGN.md §K **CommentPin** kirjoitettu 2026-07-25 — muoto (pyöreä 32px kupla vs. merkin 40px kortti+kärki) on se kanava joka erottaa huomion merkistä; ⊥ toteuta erottelua värillä. Nykyiset hexit ovat inline `comment-layer.ts:60-66` — jos koskettelet niitä, päivitä §K samalla. (d) Fokus-tilan (T335/V243) tullessa huomio-pinnit himmenevät samalla luokalla, ⊥ katoa. (e) **Sisääntulo merkkivalikosta** (käyttäjäpyyntö 2026-07-25 "sen voisi valita valikosta 'huomio'"): `src/ui/place-mode.ts:65-70` `openPicker` listaa `listFavorites(library)`-mallit — lisää listan LOPPUUN erotinviivan jälkeen rivi `💬 Huomio` joka ⊥ ole `SignTemplate` vaan oma haara: valinta ⊥ luo merkkiä vaan avaa huomio-lomakkeen samaan `lat/lon`iin johon picker aukesi. Erotin on pakollinen (V245: huomio ⊥ ole merkkityyppi — jos se istuu tasavertaisena mallilistassa, se LUETAAN merkkityypiksi). Sama sisääntulo palvelee molempia rooleja ∴ talkoolainen ⊥ tarvitse erillistä ⋯-riviä. | V13,V245,V218,T221,T75 |
| T238 | ✓ | [Kriittinen bugikorjaus B103] Turva-id-apuri: irrota `genId()` omaan pure-moduuliin `src/logic/uid.ts` (Vitest-pure) — `const c = globalThis.crypto; if (c?.randomUUID) return c.randomUUID()` + kryptaton fallback (timestamp+counter+Math.random, riittävän uniikki). Korvaa KAIKKI guardaamattomat crypto.randomUUID(): src/map/markers.ts:181, src/logic/segments.ts:53, src/ui/area-panel.ts:339/360/368/380/388; write-outbox.ts käyttää samaa apuria. Toimii insecure contextissa → merkin/pätkän/alueen luonti ei kaadu http-LAN-devissä. Testattavuus: Vitest-pure (genId palauttaa uniikkeja stringejä ilman crypto:a, guard käyttää natiivia jos on). Kerros src/logic/. Käyttäjä: molemmat. | V160,B103 |
| T239 | ✓ | Merkkikirjaston malli-modaali (SignTemplateModal, src/ui/sign-template-modal.ts) — Nimi-first + auto-slug (järjestäjä). (1) Kenttäjärjestys: Nimi(label) ENSIN, Tunnus(id) sen ALLE (nyt käänteinen: id r126-147, Nimi r543-554); SignPreview pysyy ylimpänä. (2) Auto-slug: Nimi-input täyttää Tunnuksen reaaliaikaisesti (input-event) olemassa olevalla `slugify`:llä (`src/logic/sign-id-slug.ts`: lower+fi-translit ä→a ö→o å→a + välit→viiva + kelvottomat ml. @ pois + viivakutistus + reunatrim) → EXPORT slugify (nyt ei-exportattu). "Oikea"→"oikea", "Varo oikealta"→"varo-oikealta", "Määränpää"→"maaranpaa". (3) Manuaalinen ohitus: Tunnus-kentän käsin-muokkaus (input) → touched-lippu → auto-täyttö ei enää ylikirjoita. (4) VAIN luontitilassa (template===null); muokkaustilassa id lukittu (V97), id-kenttää ei renderöidä → ei auto-fillia. Testattavuus: Vitest-pure (slugify export + nimi-esimerkit + @-poisto → tests/t161-sign-id-slug.test.ts) + Vitest-jsdom (kenttäjärjestys Nimi ennen Tunnusta, Nimi→Tunnus-täyttö, manuaali-lukitus, ei id-kenttää editissä). UX: DESIGN.md §K SignTemplateModal, touch-target 44px säilyy, ei uusia värejä. Käyttäjä: järjestäjä. | V97 |
| T240 | x | [Keski] Inventaario-backend: taulu + CRUD-API (järjestäjä). Uusi taulu `inventory_items` (`server/db.ts` idempotentti CREATE IF NOT EXISTS, migraatio-tyyli kuten muut): `id TEXT PK, name TEXT NOT NULL, qty REAL NOT NULL DEFAULT 0, unit TEXT, location TEXT, note TEXT, created_by TEXT, created_at, updated_at`. Uusi `server/routes/inventory.ts` (malli: `server/routes/admin.ts`): `GET /api/inventory` (lista), `POST /api/inventory` (luo), `PUT /api/inventory/:id` (muokkaa), `DELETE /api/inventory/:id` (poista). KAIKKI routet `requireRole('admin','järjestäjä')` (V163). Palvelinvalidointi: name ⊥ tyhjä (V161→400), qty>=0 (V162→400). Rekisteröi `server/index.ts` (`app.route('/api/inventory', inventoryRoutes)`). Duplikaattirivit sallitaan (ei uniikkius-constraintia — grill-päätös 2026-07-21). Testattavuus: Taso 4 Bun (`bun test server/`, käytä `server/test-fixtures.ts` seedTestUsers+authHeaders): CRUD happy-path, V161 tyhjä name→400, V162 neg qty→400, V163 talkoolais-header→403, järjestäjä→200. Päivitä `docs/components/backend.md` + COMPONENTS.md (InventoryAPI). Ketjuta `requireAuth()` ENNEN `requireRole('admin','järjestäjä')` (requireRole lukee session → ilman requireAuthia 401; admin.ts-malli `requireAuth(),requireRole()`). PUT/DELETE puuttuva id → 404 (`result.changes===0`, admin.ts:29-malli). created_by SESSIOSTA (session.user_id/display_name), EI client-bodystä (spoof-esto). qty-validointi V162-kovennettu (finite). Käyttäjä: järjestäjä. | V161,V162,V163,V18 |
| T241 | x | [Pieni] Inventaario-validointilogiikka (Vitest-pure). Uusi `src/logic/inventory.ts`: `InventoryItem`-tyyppi + `validateInventoryItem(input): {ok:true}\|{ok:false,error}` — name ⊥ tyhjä trimmattuna (V161), qty numero >=0 (V162), unit/location/note valinnaisia stringejä. Uusiokäyttö UI (client-pre-validointi ennen POST) + referenssi backendille. `genId()` id:lle uusiokäytä `src/logic/uid.ts` (T238). Testattavuus: Taso 1 Vitest-pure (pakollinen): tyhjä/whitespace name→⊥ok, neg qty→⊥ok, qty=0 ok, valid rivi ok, unit/location/note puuttuu ok. Kerros src/logic/. Lisää COMPONENTS.md (InventoryLogic). Käyttäjä: järjestäjä. | V161,V162 |
| T242 | x | [Keski] Inventaario-sivu UI (järjestäjä, mobiili). Uusi sivu /admin-mallin mukaan: `inventory.html` (lisää vite.config `rollupOptions.input.inventory`) + `src/inventory.ts` (entry: AuthScreen-gate kuten `src/admin.ts`, rooli järjestäjä/admin — muu → renderForbidden) + `src/ui/inventory-page.ts` (render, malli `admin-page.ts`). Toiminnot: lista inventory_items (GET), lisää-lomake (name+qty+unit+location+note → POST, client-validointi T241), rivin muokkaus (PUT), poisto (DELETE, `window.confirm` tuhoava V102). Kartalta pääsy: linkki LeftPaneliin (`src/ui/left-panel.ts`) VAIN järjestäjälle (piilotus `body[data-role=talkoolainen]`-CSS, EI data-role-hide — U7-pattern), href `/inventaario` (tai /inventory.html /admin-mallin mukaan). Mobiili: touch-target 44px kaikille napeille/inputeille (§R pakollinen). Testattavuus: Taso 2 Vitest-jsdom (lista renderöityy, lisää-lomake POST, muokkaus/poisto, confirm-flow, tyhjä name estetään client-side). Auto-ux → /karttamaster-ux: DESIGN.md §K InventoryPage-sopimus (uusi) + LeftPanel-linkki. Päivitä COMPONENTS.md (InventoryPage) + docs/components/ui.md. PROD-REITITYS: lisää `nginx.conf`-blokki `location = /inventaario { try_files /inventory.html =404; }` (malli nginx.conf:15 /admin) — muuten prod-404. Renderöinti V164: user-teksti (name/unit/location/note) VAIN textContent/createElement, ⊥ innerHTML-interpolaatio. Käyttäjä: järjestäjä (mobiili kärryllä). Vaatii T240, T241. | V163,V164,V102,V161,V162 |
| T243 | x | [Suuri] Inventaario v2 — paikat + merkkikytkös (backend, revisoi T240). (1) Uusi taulu `inventory_locations` (server/db.ts idempotentti CREATE): `id TEXT PK, name TEXT NOT NULL, sort_order INTEGER NOT NULL DEFAULT 0, created_at TEXT NOT NULL`. (2) Migraatiot inventory_items (try/catch ALTER): ADD `location_id TEXT` (viittaa inventory_locations.id, nullable), ADD `template_id TEXT` (viittaa templates.id, nullable — elävä merkkilinkki). (3) server/routes/inventory.ts laajennus: paikka-CRUD (GET/POST/PUT/DELETE `/api/inventory/locations`), GET `/api/inventory?location_id=X` suodatus, POST/PUT hyväksyy location_id+template_id. Paikan poisto → `UPDATE inventory_items SET location_id=NULL WHERE location_id=?` (V166). Inventaariorivin DELETE ⊥ koske templates-tauluun (V165). name-validointi: pakko VAIN jos template_id puuttuu (V161 kovennettu). KAIKKI routet requireAuth+requireRole(admin,järjestäjä) (V163). Testattavuus: Taso 4 Bun (server/inventory.test.ts laajennus): paikka-CRUD, location_id-suodatus, template_id tallentuu+palautuu, paikan poisto nullaa itemien location_id (ei poista itemeja), item-DELETE ei poista templatea, name-pakko vain tarvikkeelle (merkkirivi ilman namea 201). Päivitä docs/components/backend.md + COMPONENTS.md. Käyttäjä: järjestäjä. | V161,V162,V163,V165,V166,V18 |
| T244 | x | [Pieni] Inventaario v2 -logiikka (Vitest-pure, laajenna src/logic/inventory.ts). InventoryItem += `templateId?:string, locationId?:string`. Uusi `InventoryLocation`-tyyppi (id,name,sortOrder). validateInventoryItem: name pakko VAIN jos `!input.templateId` (merkkirivi ei vaadi namea); qty V162 ennallaan. `resolveItemName(item, templates: Map<string,{label:string}>)`: templateId asetettu → `templates.get(id)?.label ?? item.name ?? '(poistettu merkki)'`; muuten item.name (V165). `adjustQty(qty:number, delta:number)`: `Math.max(0, qty+delta)` (stepper-clamp, V162). Testattavuus: Taso 1 Vitest-pure (tests/t241-inventory.test.ts laajennus tai t244): name-pakko-ehdollisuus (tarvike vaatii, merkki ei), resolveItemName kaikki haarat (elävä label / puuttuva template → fallback / tarvike), adjustQty clamp 0:aan + normaali. Päivitä COMPONENTS.md. Käyttäjä: järjestäjä. | V161,V162,V165 |
| T245 | x | [Suuri] Inventaario v2 UI — paikkanavigointi + ryhmitetty lista + määräsäädin (revisoi T242, src/ui/inventory-page.ts + src/inventory.ts). (1) Paikkanäkymä: valitse paikka (Kärry/Varasto…) → sen tavarat listana; paikan lisäys + nimeäminen (inventory_locations-CRUD, T243); "Ei paikkaa"-ryhmä orvoille (V166). (2) Määräsäädin per rivi: `[−][määrä][+]` step 1 (adjustQty T244, clamp 0), tap määrä → aseta tarkka luku; muutos persistoituu heti (PUT, optimistinen). (3) Minimaalinen lisäys: "Tarvike" = nimi+määrä (paikka kontekstista, ei sijainti/yksikkö-kenttää lomakkeessa). Yksikkö+kommentti lisätään jälkikäteen rivin sekundääritoiminnoista. (4) Rivi: nimi (resolveItemName T244) + määrä-säädin + meta (yksikkö/kommentti jos on). Poisto = window.confirm (V102). Kaikki user-teksti textContent (V164). Merkki-integraatio erikseen T246. Testattavuus: Taso 2 Vitest-jsdom (paikkavalinta suodattaa listan, +/− kutsuu onEdit clampilla, aseta-tarkka, minimilisäys nimi+määrä → onAdd, kommentti/yksikkö-inline-lisäys, tyhjä tarvike-nimi estetään). Auto-ux → /karttamaster-ux (DESIGN §K InventoryPage päivitys). Päivitä COMPONENTS.md + docs/components/ui.md. Käyttäjä: järjestäjä (mobiili kärryllä). Vaatii T243, T244. | V162,V163,V164,V102 |
| T247 | x | [Pieni] Inventaario — merkki-rivin visuaali + avaus + stepper-tasaus (src/ui/inventory-page.ts + src/inventory.ts). (1) Merkki-rivi (item.templateId) näyttää kyltin visuaalin nimen vieressä: uusiokäytä `buildMarkerVisual({type:tpl.id, iconId, label, parts, color}, {size:40, zoomable:true})` (T198) — ikoni/bundle-kuva/combo (V99/V107), zoomable → lightbox isolla kuvalla (kyltin avaus, V167). InventoryView.templates: `Map<id,{label}>` → `Map<id,SignTemplate>` (entry fetchTemplateMap palauttaa täydet rivit). resolveItemName (T244) param widen → `{get(id):{label}|undefined}` (Map<string,SignTemplate>-yhteensopiva). Tarvike-rivi ennallaan (ei visuaalia). Template puuttuu → fallback-nimi ilman visuaalia (V165). (2) STEPPER-TASAUS: yksikkö pois `.inv-stepper`-flow'sta → oma elementti stepperin VASEMMALLA; stepper pinnataan oikeaan reunaan kiinteillä nappileveyksillä → `− [luku] +` pysyy samalla x:llä riippumatta yksikön pituudesta. (3) "KAIKKI"-TABI: paikkapalkin ensimmäinen tabi "Kaikki" (LocationSelection += 'all', oletusvalinta) → kokoomanäkymä: itemit ryhmitelty paikoittain väliotsikoin (`.inv-section` + `.inv-section-title` = paikan nimi järjestyksessä, "Ei paikkaa"-osio orvoille V166); näyttää mitä missäkin on. Entry: 'all' hakee KAIKKI itemit (ei location_id-suodatinta), page ryhmittää locations-listan mukaan. (4) LISÄYS 'all'-tilassa: 'all' on VAIN näyttö → add-lomake näyttää paikka-`<select>` (pakko valita), oletus "Kärry" (name-match, muuten ensimmäinen paikka); ei paikkoja → "Luo paikka ensin" + esto. Paikka-tabin sisällä add käyttää kontekstia (ei selectiä). (5) SIIRTO: tiedot-editoriin paikka-`<select>` (nykyinen + "Ei paikkaa" esivalittuna) → onEditItem uudella locationId:llä siirtää kappaleen toiseen paikkaan. Testattavuus: Vitest-jsdom (merkki-rivi → .marker-visual-row-sv + zoom-nappi; tarvike-rivi ei visuaalia; yksikkö ei .inv-stepper sisällä; 'all' → väliotsikot per paikka + oikeat itemit; 'all'-lisäys ilman paikkaa → esto; oletus Kärry; siirto onEditItem locationId). Auto-ux → /karttamaster-ux. Käyttäjä: järjestäjä. Vaatii T245, T246, T198. | V167,V165,V164 |
| T248 | x | [Pieni] Inventaario — paikkojen muokkaus-mode (src/ui/inventory-page.ts + src/inventory.ts). Paikkapalkin "✎ Muokkaa" -toggle → `.inv-loc-editor`-paneeli: lista paikoista, kukin rivi = nimi-`<input>` (rename, Enter/blur → onRenameLocation → PUT `/inventory/locations/:id`) + "Poista" (onDeleteLocation → `window.confirm` "tavarat siirtyvät Ei paikkaa -kohtaan" V102 → DELETE `/inventory/locations/:id`). Poistettu paikka → sen itemit "Ei paikkaa" (V166, backend jo). Entry: wiring + reload; poistetun paikan valinta → defaultSelection (T247). textContent (V164). Backend valmis (T243). Testattavuus: Vitest-jsdom (✎ avaa/sulkee editorin; rename-input Enter → onRenameLocation(id,nimi); Poista → onDeleteLocation(loc); tyhjä nimi ei renamea). Auto-ux → /karttamaster-ux. Käyttäjä: järjestäjä. Vaatii T243, T245. | V166,V163,V164,V102 |
| T246 | x | [Keski] Inventaario v2 — merkki-integraatio UI (src/ui/inventory-page.ts; uusiokäytä src/logic/template-sync.ts + src/ui/sign-template-modal.ts + sign-visual/icon-set, ÄLÄ duplikoi). "Lisää merkki paikkaan" kaksi polkua: (a) HAE olemassa — picker kirjaston malleista (fetchTemplates T193) → valitse → luo inventaariorivi {template_id, location_id, qty}; (b) UUSI merkki — avaa SignTemplateModal (T235/T239) luontitilassa → createTemplateRemote (näkyy heti kirjastossa+kartalla, elävä V165) → luo inventaariorivi template_id:llä. Merkki-rivi renderöi template-ikonin + labelin elävästi (sign-visual/icon-set); poistettu template → fallback (V165, resolveItemName). Inventaariorivin poisto ⊥ poista templatea. Testattavuus: Taso 2 Vitest-jsdom (picker-valinta → onAdd template_id:llä; uusi-merkki-flow SignTemplateModal-stub → template + inventaariorivi; merkki-rivi näyttää template-labelin; template puuttuu → fallback-teksti). Auto-ux → /karttamaster-ux. Päivitä COMPONENTS.md + ui.md. Käyttäjä: järjestäjä. Vaatii T243, T245. | V165,V163,V164 |
| T249 | x | [Keski] Merkin kiinnitystapa `keppi` — SignTemplate-kenttä + ' - irto' -suffix kaikkialla (V168). (backend) server/db.ts idempotentti `ALTER TABLE templates ADD COLUMN keppi INTEGER NOT NULL DEFAULT 1` (olemassa olevat → keppi=true); server/routes/templates.ts POST/PUT lukee+persistoi keppi (rowToTemplate: `keppi = row.keppi === 1`, favorite-mallin mukaan); POST default keppi=1 jos puuttuu. (logic) src/logic/sign-library.ts: `SignTemplate += keppi?: boolean`; uusi puhdas `signDisplayLabel(tpl): string` → `tpl.keppi === false ? tpl.label + ' - irto' : tpl.label`. src/logic/template-sync.ts: kuljeta keppi create/update-payloadissa. (UI näyttö — suffix KAIKKIALLA) käytä signDisplayLabelia jokaisessa label-näyttöpaikassa (Explore-kartoitus: merkkikirjasto-paneeli, picker, karttamerkin label/tooltip src/map/icons.ts, marker-visual-row, inventaario resolveItemName). EI kartan compactLabel-lyhennettä (johdetaan raakalabelista). (modal) src/ui/sign-template-modal.ts: "keppi" checkbox (favorite-toggle-malli); default open-kontekstista — kartta/kirjasto = checked, inventaario-convert = unchecked (T250). Testattavuus: Taso 4 Bun (templates POST/PUT persistoi keppi, GET palauttaa boolean, migraatio: vanha rivi → keppi=true), Taso 1 Vitest-pure (signDisplayLabel: keppi=true→label, keppi=false→'label - irto', keppi=undefined→label), Taso 2 Vitest-jsdom (kirjasto/picker/inventaario näyttää suffixin kun keppi=false; modal keppi-checkbox). Auto-ux → /karttamaster-ux (DESIGN §K SignTemplateModal keppi-checkbox). Päivitä COMPONENTS.md + docs/components. Käyttäjä: järjestäjä (+ molemmat kartalla). Vaatii T193/template-sync, T235/sign-template-modal. | V168,V99,V165 |
| T250 | x | [Keski] Inventaario "Muuta merkiksi" — tekstirivi → merkki (uusi malli esitäytetyllä nimellä). Tuotannossa kymmeniä "...irtokyltti"-tekstirivejä (ei template_id) jotka järjestäjä muuntaa KÄSIN merkeiksi. (UI) src/ui/inventory-page.ts: tiedot-editoriin (`.inv-details-editor`, VAIN kun `!item.templateId`) "Muuta merkiksi" -nappi → uusi `cb.onConvertToSign?(item)`. src/inventory.ts: onConvertToSign(item) → avaa `SignTemplateModal` LUONTITILASSA (T235/T239), nimi ESITÄYTETTY `item.name`:sta (esim. "20km kyltti irto" → käyttäjä poistaa "irto"-sanan itse + valitsee kuvan/ikonin), `keppi=true` (aina default, V168) + `favorite=false` (ei suosikkipickeriin); onSaveTemplate → createTemplateRemote(tpl) + linkitä rivi `onEditItem(item.id, {...fieldsOf(item), templateId: tpl.id})` (PUT olemassa olevaan riviin, EI uutta riviä; säilyttää qty/locationId/note). V165: rivi saa elävän labelin+visuaalin, template.label voittaa item.name-snapshotin. TÄYSIN MANUAALINEN: ei automaatti-migraatiota, ei nimi→template-heuristiikkaa (käyttäjän eksplisiittinen vaatimus 2026-07-21). Konversio peruttava (modaali backdrop+Esc). Testattavuus: Taso 2 Vitest-jsdom ("Muuta merkiksi" vain tarvike-rivin tiedoissa, ei merkki-rivillä; klikki→SignTemplateModal avautuu nimi esitäytettynä; tallenna→createTemplate keppi=false + onEditItem templateId säilyttäen qty/location). Auto-ux → /karttamaster-ux. Käyttäjä: järjestäjä. Vaatii T249, T246, T235. | V165,V167,V168 |
| T251 | x | [Keski] Inventaario read-only + muokkaustila — globaali `viewMode` ('read'|'edit'). **UI-vain, EI backend/logic-muutosta.** src/ui/inventory-page.ts: `viewMode`-state (oletus `'read'`, sessiokohtainen muistissa, EI localStorage, reload→'read', V170). Yläpalkin toggle-nappi "✎ Muokkaa"↔"✓ Valmis" (edit-tila = accent, `.btn--confirm`-malli/`gps-active`-tyyli, ux viimeistelee §K). READ piilottaa KAIKKI mutaatiot (V169): add-form, per-item −/+ stepperi, ✎ Tiedot, Poista, paikkojen ✎ Muokkaa, "lisää paikka" -nappi; rivi tiivistyy yhdelle riville `[ikoni] nimi ··· määrä unit` + tiheämpi spacing (silmäiltävä "mitä kaikkea on"). READ säilyttää katselun: paikkatabit, Kaikki-tabi, zoom-lightbox, kyltin nimi→SignTemplateModal. Tabin vaihto säilyttää moodin. Kaikki-tabi edit-modessa: rivi-muokkaukset ON, add-form EI (V171). Edit-signaali (3 kpl): toggle-teksti+accent + nappien ilmestyminen riveille + kevyt yläpalkki-aksentti. Parked `?`: add-form keskeneräisen syötön kohtalo edit-poistumassa → rec tyhjennä draft. Testattavuus: Taso 2 Vitest-jsdom (oletus-render = ei mutaatio-nappeja/add-formia; toggle→napit+add-form ilmestyvät; uusi render='read'; Kaikki-tabi edit → rivi-napit näkyvät mutta ei add-formia). EI localStoragea → ei vi.stubGlobal-mockia. Auto-ux → /karttamaster-ux (DESIGN §K viewMode-aksentti + tiivis read-rivi). Päivitä COMPONENTS.md + DESIGN.md §K InventoryPage. Käyttäjä: järjestäjä/admin. Vaatii T248. | V169,V170,V171 |
| T252 | x | [Pieni] Inventaario-undo label-logiikka (Vitest-pure). Uusi `src/logic/inventory-undo.ts`: `UndoAction`-tyyppi (`kind: 'delete-item'\|'qty'\|'move'\|'delete-location'` + ennen-tila-data) + puhdas `describeUndo(action): string` joka tuottaa toast-tekstin ('Poistit: <nimi>', 'Muutit määrää: <nimi>', 'Siirsit: <nimi>', 'Poistit paikan: <nimi>'). Nimi resolvoidaan `resolveItemName`-hengessä (merkki → template.label, fallback poistetulle merkille V165). EI DOM, EI fetch — pelkkä data→teksti. Testattavuus: Taso 1 Vitest-pure (jokainen kind → oikea teksti; nimen fallback poistetulle merkille). Kerros src/logic/. Päivitä COMPONENTS.md (InventoryUndoLogic). Käyttäjä: järjestäjä/admin. | V172 |
| T253 | x | [Keski] Inventaario "Kumoa"-undo-toast — jaettu toast-komponentti + entry-wiring (client-only). (1) UUSI jaettu komponentti `src/ui/toast.ts`: `showToast(message, {actionLabel, onAction, duration=5000})` → kelluva toast alareunaan, action-nappi (Kumoa) + auto-dismiss timer; yksi toast kerrallaan (uusi korvaa edellisen + clearTimeout); action-klikki → onAction + sulje. textContent (V164, user-nimet). Touch-target 44px action-napille (§R). ARIA `role=status`/`aria-live=polite`. (2) ENTRY-WIRING `src/inventory.ts`: jokainen mutaatio (`onDeleteItem`, `onEditItem` stepper/siirto, `onDeleteLocation`) tallentaa ENNEN-tilan → `showToast(describeUndo(action), {actionLabel:'Kumoa', onAction: revert})`. `revert` palauttaa tilan V173:n mukaan (POST/PUT olemassa oleviin reitteihin) → `load()`-reload. Paikan poisto: hae affektoidut itemit ENNEN DELETEä, talleta item_id:t + vanha location_id (V173c). Uusi mutaatio korvaa undo-slotin (V172). Toast vain edit-moodissa. (3) DESIGN §K: uusi Toast-sopimus (auto-ux luo). Testattavuus: Taso 2 Vitest-jsdom (toast.ts: showToast renderöi viestin+Kumoa-napin; onAction-klikki kutsuu callbackin+sulkee; toinen showToast korvaa ensimmäisen; auto-dismiss `vi.useFakeTimers`; 44px). Entry-revert: kevyt jsdom fetch-mockilla TAI dokumentoi manuaalinen QA jos liian integraatiopainoista. Auto-ux → /karttamaster-ux (DESIGN §K Toast + Kumoa-nappi kontrasti/touch). Päivitä COMPONENTS.md (Toast) + docs/components/ui.md. Käyttäjä: järjestäjä/admin. Vaatii T252, T251. | V172,V173,V164 |
| T254 | x | [Suuri] R1 keystone (talkoolais-redesign, brief docs/REDESIGN-talkoolainen.md) — talkoolaisen kaksi-moodi-kehys (koti↔kartta). Talkoolaisen näkymä jaetaan moodeihin: KOTI = pätkänäkymä ILMAN karttaa (landing kun /s/<koodi> avataan, V174); KARTTA = kartta näkyvä ~80% (minimaali chrome). Nyt segment-view + kartta yhtä aikaa → erilliset moodit. RAJAUS: R1 = VAIN moodikehys + navigointi; sisältöjen siirrot (varuste/kaikki merkit/kommentit→koti; alapalkki seuraava-merkki; yläpalkki ⋯; merkki-tap-valikko) = erilliset R2–R8. Kerros/tiedosto: UUSI src/app/talkoolainen-mode.ts (view-mode-tila 'koti'\|'kartta' + toggle-wiring; role-view.ts-precedentti; setViewMode asettaa #app data-view-mode + Leaflet invalidateSize karttamoodissa V176) + CSS src/style.css [data-view-mode] + index.html hookit (🏠-nappi toolbariin, "Kartalle →" hook segment-viewiin). EI segment-view.ts:ään (jo 562r koordinaattori, pilkkohälytys). Mekanismi V175: koti→#segment-view-container täysi + #map piilossa; kartta→#map näkyvä + segment-chrome minimaali. Default talkoolainen=koti; "Kartalle →" (segment-view primary, iso .btn--confirm full-width)→kartta; "🏠 koti" (toolbar, näkyy vain [data-role=talkoolainen]+[data-view-mode=kartta])→koti. Ei tab, ei gate. Testattavuus: Taso 2 Vitest-jsdom (setViewMode/toggle → #app data-view-mode + nappien näkyvyys; oletus koti; vaihto ei nollaa tilaa) + Taso 3 Playwright kriittinen polku e2e/segments.spec.ts (/s/koodi → koti näkyvä + #map piilossa → "Kartalle →" → #map näkyvä + koti piilossa → 🏠 → koti). UX: §K DESIGN.md — uusi "Talkoolaisen moodit (koti/kartta)" + 🏠-nappi Toolbar §K:hon; "Kartalle →" iso primary, "🏠" 44×44 (§R). Auto-ux → /karttamaster-ux. Päivitä COMPONENTS.md (TalkoolainenMode) + docs/components/ui.md. Käyttäjä: talkoolainen (mobiili, hanskat, huono netti). | V27,V148,V155,V174,V175,V176 |
| T255 | x | [Keski] R5 (talkoolais-redesign) — karttanäkymä minimal (80% kartta + hero alapalkkina). Karttamoodissa (T254/data-view-mode=kartta) segment-view irrotetaan flow'sta (`#segment-view-container` position:absolute;bottom, pointer-events:none; `#segment-view` pointer-events:auto + kelluva kortti radius-lg+varjo) → `#map` flex:1 täyttää koko #map-area:n (VISION "80% karttaa"). Näytä VAIN seuraava-merkki-hero (.segment-view-next) + keräyslista/done; piilota .segment-view-header/-progress/-gps-btn/-desc/-varuste-btn/-more/-comments/-inspect karttamoodissa. CSS-vain (src/style.css) — EI segment-view.ts-muutosta. GPS-nappi pois karttamoodista → siirtyy ylävalikkoon R8:ssa (välissä GPS koti-moodista). Testattavuus: Taso 3 Playwright (karttamoodi: #map täyttää, hero näkyvä alhaalla, header/gps/varuste piilossa) — CSS-visuaali → Playwright/manuaali, ei Vitest-yksikkötestiä. Käyttäjä: talkoolainen. Vaatii T254. | V177,V175 |
| T256 | x | [Pieni] R6 (talkoolais-redesign) — seuraava-merkki-korostus: rengas → ikoni-hehku (käyttäjä: "ympyrän sijaan korosta ikonia huomiovärein"). Poista `NextMarkerHighlight` (src/map/next-marker-highlight.ts, accent-CircleMarker-rengas). Lisää `MarkerManager.setNextHighlight(id\|null)` (src/map/markers.ts) joka togglaa `.marker-next-highlight`-luokan merkin elementtiin (pending-luokka-pattern), uudelleensovellus addLeafletMarkerissa (setIcon/reload korvaa elementin). CSS `.marker-next-highlight` = `filter:drop-shadow` accent-glow + pulse (EI transform → ei riko Leaflet-positiota) + z-index. markers-wiring onNavigate/updateNextHighlight → setNextHighlight. Testattavuus: Vitest-jsdom (setNextHighlight togglaa luokan oikeaan elementtiin, vaihto siirtää, null poistaa) + Playwright-visuaali. Päivitä COMPONENTS.md (poista NextMarkerHighlight) + DESIGN.md. Käyttäjä: talkoolainen. Vaatii T254. | V178,V159 |
| T257 | x | [Keski] R8 (talkoolais-redesign) — yläpalkin ⋯ = pätkän core-toiminnot (VISION "GPS ym ylävalikkoon"). Karttamoodissa (T255) hero-chrome minimaali → GPS/Lisää merkki/Merkitse valmiiksi eivät saavutettavissa kartalla. Lisää `#tk-menu-actions` (`data-role-hide=järjestäjä`) `#toolbar-menu`:hin: `#btn-tk-gps` (GPS-toggle, label sync), `#btn-tk-add-marker` (openAddMarkerPicker), `#btn-tk-complete` (applyComplete, label ↔ valmis/keskeneräinen). Wiring markers-wiring.ts talkoolais-haarassa (jaettu applyComplete/openAddMarkerPicker hero:n kanssa — ei duplikointia). GPS-nappi POIS herosta (CSS `body[data-role=talkoolainen] .segment-view-gps-btn{display:none}`). Testattavuus: Playwright (talkoolainen ⋯ → GPS/Lisää/Valmis näkyvät; järjestäjällä ei; GPS-klikki vaihtaa labelin; Lisää → picker; Valmis → label vaihtuu). Päivitä DESIGN.md §K Toolbar-menu (talkoolais-toiminnot). Käyttäjä: talkoolainen. Vaatii T254, T255. | V179 |
| T260 | x | [Pieni] R4 (talkoolais-redesign) — pätkän kommentit pois koti-etusivulta valikkoon (käyttäjäpalaute "en ymmärrä mikä toi kommentti juttu on tässä etusivulla"). Siirrä `.segment-view-comments` (koko-pätkän CommentThread T221) `panel`-tasolta "Lisää ⋯":n `moreBody`-osioon (src/ui/segment-view.ts). "Lisää ⋯" (`.segment-view-more`) AINA näkyvissä (renderMoreSection → hidden=false) koska se pitää nyt kommentit; complete/bounds gaten silti itsenäisesti. Per-merkki-kommentit = merkkimodaali (ennallaan). Koti-etusivu tiivistyy: header + hero + "Lisää ⋯" + "Kartalle →". Testattavuus: Vitest-jsdom (t232: moreSection aina näkyvä + kommentit moreBodyn sisällä) + Playwright (etusivulla kommentit piilossa, "Lisää ⋯" avaa). Amend V158. Käyttäjä: talkoolainen. Vaatii T254. | V158 |
| T259 | x | [Pieni] R9 (talkoolais-redesign) — toolbar/tilivalikko-remontti (käyttäjäpalaute). (1) Teemavalitsin: nimet → "☀️ Vaalea"/"🌙 Tumma" (lyhyt+ikoni), rinnakkain, aktiivinen selvä (accent-2px-reuna + ✓); inaktiivi luki aiemmin "rikkinäiseltä" (himmeä pitkä nimi) — toggle TOIMII (varmennettu, ei bugi). (2) Karttatyyli-nappi näyttää AKTIIVISEN tyylin ("Karttatyyli: <label>", klikki kiertää) — aiemmin kiinteä teksti + vain tooltip. (3) "Kirjaudu ulos" kevyeksi (tekstityyli, ei iso danger-laatikko; säilyttää 44px touch). (4) Kartta-moodin toolbar minimaali: piilota "Kaikki merkit"+"Varustelista" talkoolaiselta karttamoodissa (koti/valikko-asioita) → toolbar = brändi+🏠+⋯, korjaa myös 🏠:n ahtauden ≤390px. src/ui/account-menu.ts + src/app/map-init.ts + src/style.css. Testattavuus: Playwright (teemanimet+ikonit, karttatyyli-label, kartta-toolbar declutter). Käyttäjä: talkoolainen (+ molemmat teema/karttatyyli). Vaatii T254. | V132,V133,V175 |
| T258 | x | [Keski] R2 (talkoolais-redesign) — varustarkastus "otin nämä" checkoff (journeyn ensiaskel "katsotaan tavarat mukana"). UUSI `src/logic/varustarkastus.ts` (client-only, localStorage per pätkä: loadChecked/setChecked/checkProgress). EquipmentModal (src/ui/equipment-modal.ts): checkbox per auto-merkki + manuaalirivi (ei-tyhjä nimi), "Varustarkastus: N/M otettu" -edistymä headerissa (vihreä kun täysi), checkatun rivin yliviivaus. Client-only V180 (peilaa inventaario-undo V172). Testattavuus: Taso 1 Vitest-pure (varustarkastus.ts: load/set/progress, pätkäkohtaisuus, rikkinäinen JSON→tyhjä; localStorage vi.stubGlobal-mock) + Playwright (modaali: checkbox+progress, toggle→progress päivittyy, reopen persistoi). Päivitä COMPONENTS.md (VarustarkastusLogic) + DESIGN.md §K EquipmentModal. Käyttäjä: talkoolainen. Vaatii T254. | V180 |
| T261 | ✓ | [Keski] keppi/irto siirto templatesta inventaarioriville + convert olemassa olevaan merkkiin (V181, supersedes V168/T249/T250-osin; renum T255→T261: main-redesign otti T255). MOTIVAATIO (käyttäjä 2026-07-21): sama kylttipinta kepillä ja irto sai ERI tunnuksen (kaksi mallia) → yksi tunnus, kiinnitys rivin attribuutiksi. (a) DATAMALLI: `inventory_items.keppi INTEGER` nullable + backfill templates.keppi=0:sta (server/db.ts); server/routes/inventory.ts validate/INSERT/UPDATE kuljettaa keppi (0/1/NULL, vain merkkirivi); templates.ts POISTAA keppi-luvun/kirjoituksen (DB-sarake orpo). (b) LOGIC: `SignTemplate` -= keppi; `signDisplayLabel(label, keppi)` (ei enää `(tpl)`); `resolveItemName` lukee `item.keppi`; InventoryItem/Fields += keppi. (c) UI näyttö: place-mode/sign-library-panel = pelkkä `t.label` (ei suffixia); inventaariorivi = suffix rivin keppistä. (d) sign-template-modal: keppi-checkbox POIS (malli=kylttipinta). (e) inventaarion Tiedot-editori: "Keppi (pois=irto)" -täppä merkkirivillä → PUT rivin keppi. (f) CONVERT "Muuta merkiksi" reititetty `renderSignPicker`-kautta (openConvertFlow): valitse OLEMASSA oleva merkki (linkitä rivi) TAI luo uusi — ei enää suoraan uuteen malliin. Testit: Taso 4 Bun (inventory keppi POST 0/NULL, PUT vaihto, sama tunnus 2 riviä, tarvike→NULL), Taso 1 Vitest-pure (signDisplayLabel uusi sig, resolveItemName rivin keppi), Taso 2 Vitest-jsdom (rivin keppi=false→suffix; modal keppi-checkbox poissa). Käyttäjä: järjestäjä. | V181,V165,V167 |
| T262 | x | [Keski] R3b/redesign (talkoolais-KOTI, brief docs/REDESIGN-talkoolainen.md Näkymä 1, jatkoa T254–T260) — KOTI-pätkänäkymän swap: varustelista+varustarkastus INLINE näkyviin, "seuraava merkki" -hero POIS kodista (kartta-moodi ennallaan). Käyttäjän 2026-07-21 lukittu päätös (AskUserQuestion): varuste→KOTI, seuraava-merkki-laatikko pois KODISTA; kartan alapalkki-hero säilyy merkkien asetukseen. MOTIVAATIO: brief "varustarkastus ensin, lähtövalmis" — koti = valmistelu+konteksti, kartta = työ. Nyt varuste piilossa yläpalkin 🎒 `#btn-varuste`→`EquipmentModal` takana, ja hero (`.segment-view-next`) vie koti-etusivun pääpaikan (ei kuulu valmisteluun). TOTEUTUS: **(a)** UUSI `src/ui/segment-equipment.ts` — pure-DOM inline-varustelista-builder (Vitest-jsdom). Renderöi: auto-laskuri merkkityypeittäin (readonly) + manuaalirivit, JOKAINEN rivi varustarkastus-checkoffilla ("otin nämä") + "Varustarkastus: N/M otettu" -edistymä (täysi→`--confirm`-vihreä). Uudelleenkäyttää `src/logic/varustarkastus.ts` (loadChecked/setChecked/checkProgress, client-only localStorage per pätkä V180) — EI duplikoi EquipmentModalin checkoff-logiikkaa; jaettu label-avainmuoto (`sign:`+type / `item:`+name) IDENTTINEN EquipmentModalin kanssa → checkoff-tila täsmää inline↔modaali. Checkoff auto-persistoi (ei Tallenna-nappia inline). "✎ Muokkaa varusteita" → avaa olemassa oleva `EquipmentModal` (manuaalilistan editointi + Tallenna säilyy siellä, `onEquipmentChange` V38/V93). `segment-view.ts` pysyy koordinaattorina: delegoi builderiin (max kasvu ~10r; 562r pilkkohälytys ei saa paisua). **(b)** Hero `.segment-view-next` piilotetaan KOTI-moodissa: CSS `#app[data-view-mode="koti"] .segment-view-next{display:none}`. Kartta-moodissa ennallaan. Hero.render()-side-effectit (onNavigate) jäävät ajoon (kartta piilossa kodissa → ei visuaalivaikutusta) — pelkkä display-piilo, EI `segment-hero.ts`-muutosta. **(c)** Uusi `.segment-view-equipment` lisätään kartta-moodin piilotuslistaan (`style.css` ~1530 `#app[data-view-mode="kartta"]` chrome-hide). **(d)** Koti-rakenne briefin mukaan: header (pätkä+pituus+progress) → VARUSTELISTA(inline) → "Lisää ⋯" → iso "Kartalle →". Yläpalkin 🎒 `#btn-varuste` koti-moodissa redundantti (inline korvaa) → UX päättää poistetaanko koti-toolbarista; ei blokkaa (T233/V155 säilyy kartta-moodille). INVARIANTIT: amend V156 (hero VAIN kartta-ohjaus); uusi V182 (koti inline-varuste + hero-piilo, checkoff jaettu inline↔modaali). Kerros/tiedostot: UUSI `src/ui/segment-equipment.ts` (Vitest-jsdom); `src/ui/segment-view.ts` (delegate); `src/style.css` ([data-view-mode]). Reunat: 44px touch, font-size:16px iOS-zoom, DESIGN-tokenit, checkoff-yliviivaus (`--done`), client-only V180. Testattavuus: Vitest-jsdom (segment-equipment: auto+manuaalirivit, checkoff togglaa+persistoi `vi.stubGlobal`-mock, progress N/M, "Muokkaa"→modaali; segment-view koti-moodissa varuste näkyy) + Vitest-pure (varustarkastus.ts jo t258) + Playwright `e2e/segments.spec.ts` (KOTI: varustelista näkyy + EI `.segment-view-next`; KARTTA: `.segment-view-next` alapalkki näkyy + EI varustelistaa; checkoff persistoi reloadissa). Auto-ux → /karttamaster-ux (§K SegmentView + inline-varuste-sopimus; DESIGN.md r458-470). Päivitä COMPONENTS.md (SegmentEquipment). Käyttäjä: talkoolainen (mobiili, hanskat, metsä). Vaatii T254, T258. | V156,V182,V180,V38,V93,V174,V175 |
| T263 | x | [Pieni] R3/redesign (talkoolais-KOTI, brief docs/REDESIGN-talkoolainen.md Näkymä 1) — "Kaikki merkit" -lista INLINE koti-näkymään (käyttäjän 2026-07-21 lukittu päätös: kyllä, inline kotiin). Nyt oman pätkän merkit vain yläpalkin modaalissa (`#btn-list`); brief haluaa listan koti-landingiin. Koti-moodissa ei karttaa → tilaa on (T228 poisti inline-listan koska se söi KARTAN tilan + duplikoi modaalin; koti-moodissa tilaperuste ei päde, ja lista on koti-only). TOTEUTUS: UUSI `src/ui/segment-marker-list.ts` (pure-DOM builder, Vitest-jsdom) — `SegmentMarkerList` renderöi `.segment-view-markers`: otsikko "Kaikki merkit (N)" + rivit (oman pätkän `currentMarkers`, distanceFromStart-järjestys): `buildMarkerVisual(36px)` + `markerLabel` + status-label + km, rivi-`<button>`-klikkaus → `ctx.onOpenDetail(id)`. SegmentView delegoi (kuten SegmentEquipment T262): container `.segment-view-markers` equipmentin jälkeen, render ctor + update(); onOpenDetail = olemassa oleva `actions.onFocusMarker` (→ `onOpenMarkerDetail` → MarkerDetailModal, EI uusi mutaatiopolku). CSS: koti näyttää; kartta-moodin piilotuslistaan `.segment-view-markers` (koti-only). segment-view.ts pysyy koordinaattorina (~+8r delegate). Kerros/tiedostot: UUSI `src/ui/segment-marker-list.ts`; `src/ui/segment-view.ts` (delegate); `src/style.css` ([data-view-mode]). Reunat: 44px touch-rivit, DESIGN-tokenit, V33/V142 (vain oman pätkän merkit — currentMarkers jo suodatettu). Testattavuus: Vitest-jsdom (rivit renderöityvät järjestyksessä, klikkaus kutsuu onOpenDetail, tyhjä→empty, status/nimi näkyvät) + Playwright `e2e/segments.spec.ts` (KOTI: `.segment-view-markers` näkyy + rivi→modaali; KARTTA: piilossa). Auto-ux → /karttamaster-ux (§K SegmentView). Päivitä COMPONENTS.md (SegmentMarkerList). Käyttäjä: talkoolainen. Vaatii T254, T262. | V183,V33,V142,V144,V175 |
| T264 | x | [Suuri] R10/redesign (talkoolais-KOTI, brief docs/REDESIGN-talkoolainen.md Näkymä 1) — koti-näkymän VÄLILEHDET + accordion pois + varuste vain inline (käyttäjä 2026-07-21: "vain varustelista, modal pois yläpalkista; kaikki merkit uusi tab jossa asetetut/asettamatta + merkitse valmiiksi + muokkaa rajoja ILMAN haitaria"). Lukittu tab-rakenne (AskUserQuestion): **3 tabia** Varustelista · Kaikki merkit · Kommentit. TOTEUTUS: **(a)** UUSI `src/ui/segment-koti-tabs.ts` (`SegmentKotiTabs`, pure-DOM Vitest-jsdom) — tab-shell: tabbar (`.segment-koti-tabbar` role=tablist, `.segment-koti-tab` napit is-active) + panelit (`.segment-koti-panel` hidden-toggle). Konstruktori ottaa tab-defit {id,label,els[]} ja REPARENTOI annetut elementit paneleihin (appendChild siirtää) → segment-view.ts EI kasva rakenteellisesti (pilkkohälytys ~600r). **(b)** SegmentView: rakenna kolme tabia — Varustelista=[equipmentEl (T262)], Kaikki merkit=[markerListEl (T263), completeSection, boundsSection], Kommentit=[commentEl]. `tabs.root` panelin sisään nextEl/collectionEl:n jälkeen (ennen bulkBtn). **(c)** POISTA "Lisää ⋯" -accordion (`.segment-view-more`/moreSection): valmis+rajat siirtyvät Kaikki merkit -tabiin, kommentit Kommentit-tabiin — EI enää haitarin alla (käyttäjän eksplisiittinen pyyntö). moreSection piilotetaan (hidden). Amend V158. **(d)** SegmentMarkerList (T263) laajennus: RYHMITTELE asetetut/asettamatta — "Asettamatta (N)" (suunniteltu) + "Asetetut (N)" (asetettu/tarkistettu/kerätty) + "Ei tarpeen (N)" (ei_tarpeen), tyhjät ryhmät pois; rivit distanceFromStart-järjestyksessä ryhmän sisällä. **(e)** Poista yläpalkin 🎒 Varustelista-modal: `#btn-varuste` pois index.html:stä + wiring (markers-wiring.ts:410) + CSS-viittaukset; varuste VAIN inline (Varustelista-tab). Amend V155. Piilota `#btn-list` (Kaikki merkit -modal) talkoolaiselta (redundantti tabin kanssa; järjestäjä ennallaan V134) — `body[data-role=talkoolainen] #btn-list{display:none}`. **(f)** Tabit VAIN koti-moodissa: kartta-moodissa CSS piilottaa `.segment-koti-tabs`, hero (nextEl) näkyy (V177 ennallaan). Kerros/tiedostot: UUSI `src/ui/segment-koti-tabs.ts`; `src/ui/segment-view.ts` (tab-wiring + moreSection pois); `src/ui/segment-marker-list.ts` (ryhmittely); `index.html` (#btn-varuste pois); `src/app/markers-wiring.ts` (#btn-varuste-wiring pois); `src/style.css` (tabbar + kartta-hide + #btn-list-talkoolais-hide). Reunat: 44px touch-tabit, DESIGN-tokenit, aktiivi-tab selvä (accent-alareuna). Invariantit: uusi V184 (koti 3-tab-shell + accordion pois + valmis/rajat Kaikki merkit -tabissa + varuste/kaikki merkit EI talkoolais-yläpalkissa); amend V155 (#btn-varuste pois, #btn-list talkoolaiselta piilo), V156 (koti = header+progress+3 tabia+Kartalle→), V158 (accordion poistettu → tabit), V183 (kaikki merkit ryhmitelty tabissa). Testattavuus: Vitest-jsdom (segment-koti-tabs: 3 tabia, klikkaus vaihtaa aktiivin+panelin; marker-list ryhmät; segment-view: moreSection piilossa, elementit tabeissa) + Vitest (t233 amend: #btn-varuste EI toolbarissa) + Playwright e2e/segments.spec.ts (KOTI: 3 tabia, tab-vaihto näyttää oikean sisällön, valmis/rajat Kaikki merkit -tabissa, #btn-varuste pois; KARTTA: tabit piilossa, hero näkyy). Auto-ux → /karttamaster-ux (§K SegmentView tab-shell + DESIGN.md). Päivitä COMPONENTS.md (SegmentKotiTabs). Käyttäjä: talkoolainen. Vaatii T262, T263. | V184,V155,V156,V158,V183,V175,V177 |
| T265 | x | [Pieni bugikorjaus B107] planSegmentZoom fittaa AINA koko pätkän (käyttäjä 2026-07-21: "fittaa aina koko pätkä"). `src/logic/segment-zoom.ts`: poista anchor-haara + `maxFitLengthM`/`SegmentZoomOpts` → `planSegmentZoom(startDist,endDist)` palauttaa aina `{mode:'fit', startDist, endDist}` (null jos rajat puuttuvat/epäkelpo). `fitMapToSegment` (markers-wiring) käyttää jo plan.startDist/endDist → koko pätkä. Testattavuus: Vitest-pure (t224-segment-zoom: pitkä pätkä → fit koko väli, ei anchor; päivitä anchor-testit). Kerros src/logic/. Käyttäjä: talkoolainen. | V185,B107 |
| T266 | x | [Pieni] Poista keppi/irto-kiinnitystapa KOKONAAN (V186, supersedes V181/T261; käyttäjäpäätös 2026-07-21). Kiinnitys ei ole strukturoitu attribuutti — oletus AINA keppi; poikkeus "sido puuhun" merkin kommentissa/paikkaohjeessa kartalla (`marker-detail-modal` note, placeholder "kiinnitä puuhun" jo olemassa). MOTIVAATIO: keppi-boolean = vain järjestäjä-kosmeettinen `' - irto'`-suffix inventaariorivissä, näkymätön talkoolaiselle, EI ohjaa materiaalilaskuria (erillinen manuaali-"keppiä"-tarvikerivi). VISION §Merkkijärjestelmä: kiinnitystapa = paikkaohje, ei kenttä. Tukee "kartat ja merkit kohtaamaan" -convergenssia. MUUTOS: **(a)** LOGIC/Vitest-pure — `src/logic/sign-library.ts`: `signDisplayLabel(label)` palauttaa AINA `label` (poista keppi-parametri + `' - irto'`-haara); `src/logic/inventory.ts`: `InventoryItem`/`Fields` -= keppi, `resolveItemName` → `signDisplayLabel(tpl.label)` ilman `item.keppi`, poista `boolOrNull`-keppi. **(b)** DATA-GLUE — `src/inventory.ts`: poista keppi `ServerItem`-tyypistä, normalisoinnista, `toBody`:sta, place-mode-payloadista. **(c)** UI/Vitest-jsdom — `src/ui/inventory-page.ts`: poista keppi-checkbox kokonaan (`.inv-field-keppi`/`.inv-d-keppi`, `keppiCheckbox`), `overrides.keppi`, `buildFields`-keppi. **(d)** BACKEND/Taso 4 Bun — `server/routes/inventory.ts`: poista keppi `ServerRow`-tyypistä, body-tyypistä, validate-luvusta, INSERT/UPDATE-SQL:stä (sarake pois). **(e)** DB — `server/db.ts`: `inventory_items.keppi`-sarake JÄÄ ORVOKSI (EI DROP, SQLite-turva; migraatio jää harmittomasti paikoilleen, kuten `templates.keppi`). TESTIT: päivitä `tests/t249-keppi.test.ts` (signDisplayLabel aina label, poista irto-assertiot), `server/inventory.test.ts` (poista keppi POST/PUT/GET-assertiot, ~18 osumaa), `tests/t250-convert-to-sign.test.ts` + `tests/t250-modal-prefill.test.ts` (convert-flow ei keppiä), inventory-page jsdom (keppi-checkbox EI renderöidy). EI E2E (ei src/map). EI uutta §K (checkbox poistuu). Kerros: src/logic/ + src/ui/ + server/. Käyttäjä: järjestäjä. | V186,V181,V165,V167 |
| T267 | x | **[Suuri]** P1 backend — yleissalasana + Model B talkoo-auth. **(a)** `server/db.ts`: uusi `settings`-taulu (`key TEXT PRIMARY KEY, value TEXT`), idempotent-migraatio (`CREATE TABLE IF NOT EXISTS`); avaimet `talkoo_password_hash` (T267) + `faq_markdown` (T269). **(b)** `server/routes/auth.ts`: `POST /api/auth/talkoo-login {password}` → bcrypt-vertaa `settings.talkoo_password_hash` → onnistuu → httpOnly-sessio role=talkoolainen 24h (sama sessio-mekaniikka kuin code-login, mutta EI sidottu segmenttiin). Jos salasanaa ei asetettu → 401 selkeä virhe. Kevyt rate-limit (viive/lukitus n väärän jälkeen, muistissa per-IP riittää). **(c)** `server/routes/admin.ts`: `PUT /api/admin/settings/talkoo-password {password}` (admin, `requireRole`) → bcrypt-hash → upsert `settings`. `GET /api/admin/settings` → `{talkooPasswordSet: bool}` (EI hashia). Testattavuus: Bun-integraatio (`server/test-fixtures.ts` `seedTestUsers`/`authHeaders`): oikea salasana → sessio+cookie; väärä → 401; asettamaton → 401; admin asettaa/vaihtaa; ei-admin PUT → 403; rate-limit laukeaa; `GET settings` ei vuoda hashia. Kerros: server/. Käyttäjä: talkoolainen (login), admin (asetus). | V188,T36,T41 |
| T268 | x | P1 admin-UI — yleissalasanan asetus. `src/ui/admin-page.ts` + `admin.html`: uusi osio "Talkoolaisten salasana" — `<input type=password>` + näytä/piilota-toggle + "Tallenna"-nappi → `PUT /api/admin/settings/talkoo-password`. Näyttää tilan "✓ asetettu / ei asetettu" (`GET /api/admin/settings`), onnistumis/virheilmoitus. §K-sopimus admin-osiolle DESIGN.md:hen (auto-ux → `/karttamaster-ux komponentti admin-settings`). Touch 44px, `field-tint`/tokenit. Testattavuus: Vitest-jsdom (tallenna-nappi kutsuu PUT oikealla bodyllä; tila-teksti näyttö). Kerros: src/ui/. Käyttäjä: admin. | V188,T267,T122 |
| T269 | x | P2 FAQ backend — sisältö settings-tauluun. `server/routes`: `GET /api/faq` (auth: talkoolainen TAI järjestäjä) → `{markdown}` (`settings.faq_markdown`, tyhjä oletus `""`); `PUT /api/admin/faq {markdown}` (admin) → upsert. Testattavuus: Bun-integraatio: PUT→GET roundtrip; auth vaaditaan (401 ilman sessiota); ei-admin PUT → 403. Kerros: server/. Käyttäjä: admin (kirjoittaa), molemmat (lukee). | V190,T267,T41,T36 |
| T270 | x | P2 FAQ WYSIWYG-editori adminissa. **RESEARCH ENSIN:** aja `/ck:research R(faq-editor)` → kirjaa lib-valinta §R:ään ENNEN buildia (rec Toast UI Editor `@toast-ui/editor`). `src/ui/admin-page.ts`: FAQ-osio jossa WYSIWYG-markdown-editori — lataa nykyinen markdown (`GET /api/faq`), tallenna → `PUT /api/admin/faq`. Inline-formatointi (boldit renderöityvät, ei raakoja tähtiä — käyttäjä: "niinkun Word"). §K-sopimus DESIGN.md:hen (editorin chrome, touch, tokenit). Testattavuus: Vitest-jsdom (editori-instanssi renderöityy containeriin; tallenna kutsuu PUT editorin markdown-outputilla). Kerros: src/ui/. Käyttäjä: admin. | V190,T269,R(faq-editor) |
| T271 | x | **[Suuri]** P3 `/patkat`-sivu — uusi entrypoint. `patkat.html` + `src/patkat.ts` (bootstrap) + `src/ui/patkat-page.ts` (render). `vite.config.ts` `rollupOptions.input += patkat`. nginx: `= /patkat` → `patkat.html` (SPA-fallback kattaa muuten). Sisältö: **(a)** "Tervetuloa talkoilemaan" -hero; **(b)** FAQ-osio (`GET /api/faq` → `marked`-render SANITOITUNA V190); **(c)** pätkälista (`GET /api/segments` → nimi + assignee-näyttönimi + status-yhteenveto (`formatStatusCounts`) + "Avaa"-linkki `/s/<slug>`); **(d)** iso "Kartalle →" (→ `/`). Auth-gate: `GET /api/auth/me` → ei sessiota → talkoo-login (T272-mekaniikka). Vanilla, EI Leafletia (src/ui-kerros). §K-sopimus PatkatPage DESIGN.md:hen (auto-ux → `/karttamaster-ux komponentti patkat-page`). Touch 44px, mobiili-first (talkoolainen puhelimella). Testattavuus: Vitest-jsdom (lista renderöi pätkät+linkit; FAQ marked-render sanitoitu — XSS-payload ei injektoidu) + Playwright (`/patkat` lataa, lista näkyy, "Kartalle" navigoi `/`:iin). Päivitä COMPONENTS.md (PatkatPage) + AppController-taso. Kerros: src/ui/ + uusi entrypoint. Käyttäjä: talkoolainen (ensisijainen), järjestäjä (T275). | V188,V190,T267,T269,T14 |
| T272 | x | **[Suuri]** Model B auth-gate — yleissalasana-login + `/s/<slug>` session-portin taakse. `src/ui/auth-screen.ts`: talkoolainen-tab = **yleissalasana-kenttä** (EI enää per-pätkä-koodi-syöttö) → `POST /api/auth/talkoo-login`. Deep-link `/s/<slug>`-parsinta (`extractSegmentCode` → slug, säilytä [a-z0-9_-]): jos EI sessiota → näytä yleissalasana-login, onnistuneen loginin jälkeen deep-select pätkä (avaa `/s/<slug>`-kohde suoraan pätkänäkymään/kartalle). Jos sessio jo voimassa → suoraan pätkälle. Järjestäjä-tab ennallaan (username+password). Amend V27 (auto-login vaatii session ensin). Testattavuus: Vitest-jsdom (yleissalasana-login POST; deep-link ilman sessiota → login-jono → kohde muistetaan) + Playwright (`/s/<slug>` ilman sessiota → salasana → pätkä avautuu). Kerros: src/ui/. Käyttäjä: talkoolainen. | V188,V42,T267,T271,T51 |
| T273 | x | Auto-generoitu ihmisluettava pätkä-slug. `src/logic/segments.ts`: `generateSegmentSlug(name: string, existing: string[]): string` (Vitest-pure) — nimestä → `[a-z0-9-]` slug (ääkköset ä→a/ö→o, välit→`-`, muut pois), uniikki (törmäys → suffiksi `-2`,`-3`… V191). Järjestäjän assign-flow (`src/ui/segment-details-modal.ts` `buildAssignSection`): järjestäjä syöttää VAIN näyttönimen → slug generoidaan auto (poista käsin koodi-kenttä `input-assign-code`); slug = `assignedCode` → `POST /api/admin/codes {code:slug, display_name, segment_id}`. Backend `talkoolainen_codes` hyväksyy slugin sellaisenaan (ei muutosta). HUOM pilkko: SegmentDetailsModal ⚠️ — slug-kutsu OHUENA, ei kasvateta. Testattavuus: Vitest-pure (slug-generointi, uniikkius+suffiksointi, ääkkös/erikoismerkki-normalisointi, tyhjä nimi → fallback) + Vitest-jsdom (assign luo slugin nimestä, ei käsin koodi-kenttää). Kerros: src/logic/ + src/ui/. Käyttäjä: järjestäjä. | V191,V42,T26 |
| T274 | x | **[Suuri]** P5 järjestäjä-crossover — client-view-flip. `src/app/role-view.ts` + tilivalikko (`src/ui/account-menu.ts`/`#toolbar-menu`): järjestäjälle valikkoon "Tee pätkä (talkoona)" → vaihda talkoo-layoutiin (`talkoolainen-mode.ts`/view-flip, järjestäjä-sessio säilyy V189) + "Järjestäjä-näkymä" -paluu. EI uutta sessiota, EI logout, EI `talkoo-login`-kutsua. Talkoolaisen rooli-piilotukset (V148/V150 ym.) EIVÄT koske järjestäjää flipissä — täydet oikeudet. Talkoolaiselle valikko-itemejä EI näytetä (`data-role-hide`). Testattavuus: Vitest-jsdom (valikko-item flippaa view-moodin; `/api/auth/me`-sessio ennallaan, ei fetch-loginia) + Playwright (järjestäjä → talkoo-layout → takaisin järjestäjä-näkymään). Kerros: src/app/ + src/ui/. Käyttäjä: järjestäjä. | V189,V13,T12,T254 |
| T275 | x | P6 pätkälista tuplaroolissa — järjestäjän linkkikoonti. `src/ui/patkat-page.ts`: kun sessio = järjestäjä (`/api/auth/me` role), pätkälista näyttää per rivi LISÄKSI koko `/s/<slug>`-linkin (absoluuttinen `origin+path`) + "Kopioi"-nappi (`navigator.clipboard.writeText`) — kaikki pätkälinkit yhdessä näkymässä (korjaa "jokaiselle reitille hankala mennä" — ei tarvitse avata jokaista SegmentDetailsModalia erikseen). Talkoolaiselle vain "Avaa". Testattavuus: Vitest-jsdom (järjestäjä-rooli → kopio-napit+linkit näkyvät; talkoolainen-rooli → vain Avaa) + Playwright. Kerros: src/ui/. Käyttäjä: järjestäjä. | T271,T26 |
| T276 | x | Bug B109 -korjaus: `src/ui/segment-details-modal.ts` — assign-osio EI sulje modalia linkin luonnin/muutoksen jälkeen. Poista `this.close()` (`:444` linkin luonti + `:410` "Muuta"-haara); sen sijaan re-renderöi `buildAssignSection` "on koodi" -haaraan → näyttää juuri luodun `/s/<slug>`-linkin + "📋 Kopioi"-nappi samassa modaalissa (`onRender`/`onUpdate` säilyvät). HUOM pilkko: SegmentDetailsModal ⚠️ — korjaus OHUENA. Testattavuus: Vitest-jsdom (koodin tallennus EI kutsu `close()`; assign-osio näyttää linkin+kopio-napin luonnin jälkeen; "Muuta" ei sulje). Kerros: src/ui/. Käyttäjä: järjestäjä. | V192,B109 |
| T277 | x | Fix B110: pinnaa+korjaa T38 e2e-fail (järjestäjä #btn-list→`.marker-item` tyhjä). Aja `bunx playwright test e2e/sprint-features.spec.ts:83 --headed`/trace → selvitä miksi mockMarkers([seededMarker]) EI renderöi `.marker-item` järjestäjälle vaikka ketju (fetchMarkers-parse OK, route '35km' matchaa ROUTE_DEFS, `renderMarkerList(undefined segmentIds)` → kaikki) näyttää oikealta. **Determinoi: koodibugi vai vanhentunut testi.** Jos koodi: korjaa `marker-list.ts:99` tyhjä-Set-footgun (`segmentMarkerIds && segmentMarkerIds.size ? filter : all`, V193) TAI manager-load-polku. Jos testi: päivitä selektori/flow. Testattavuus: e2e vihreä (koko setti) + Vitest-jsdom `renderMarkerList` tyhjä-Set→kaikki-merkit. HUOM: EI hub-regressio (pre-existing mainilla) — ei blokkaa hub-mergeä jos ship-päätös muuttuu. Kerros: src/ui/marker-list.ts + e2e/. Käyttäjä: järjestäjä. | V193,B110 |
| T278 | x | Bug B111 -korjaus (FOUC): poista CDN-`<link>` `index.html:7` (`https://unpkg.com/leaflet@1.9.4/dist/leaflet.css`); korvaa paikallisella bundlatulla importilla. **Suositus:** `@import 'leaflet/dist/leaflet.css';` `src/style.css`:n ALKUUN — kaikki 4 entryä (main/patkat/inventory/admin) tuovat jo `style.css`:n → yksi rivi kattaa karttasivut, Vite resolvoi node_modules-polun. Vaihtoehto: `import 'leaflet/dist/leaflet.css'` `src/app/map-init.ts`:ssä. Leaflet npm jo asennettu (`leaflet@^1.9.4`). Verifioi: (a) ei verkkopyyntöä unpkg:hen (Network / offline-lataus toimii), (b) ensimmäinen paint tyylitelty (ei kartta-flash). Testattavuus: Playwright — lataa `/`, assert ettei `<link[href*=unpkg]>` DOM:issa + kartta-container tyylitelty heti; offline-render. Kerros: index.html + src/style.css. Käyttäjä: molemmat (talkoolainen hyötyy eniten — offline metsässä). | V194,B111 |
| T279 | x | **[perf]** Lighthouse `cache-insight` (≈354 KiB hukkaa). `nginx.conf`: lisää `location /assets/ { expires 1y; add_header Cache-Control "public, immutable"; }` (content-hashatut → ikuinen cache turvallista) + `gzip on` (js/css/svg/json). HTML+robots.txt: `add_header Cache-Control "no-cache"` ettei deploy jää cacheen. Verifioi: `curl -I .../assets/*.js` → `Cache-Control: public, immutable`. Kerros: nginx.conf. Käyttäjä: molemmat (toistolataus metsässä). | V195 |
| T280 | x | **[seo]** `robots-txt` (104 virhettä — `try_files` palauttaa index.html:n) + puuttuva `meta-description`. (a) Lisää `public/robots.txt` (Vite kopioi dist-juureen; salli kaikki, `Sitemap:` valinnainen). (b) `<meta name="description" content="...">` (fi, ~150 merkkiä) `index.html`+`admin.html`+`patkat.html`+`inventory.html` `<head>`iin. Verifioi: `/robots.txt` palauttaa text/plain robots-syntaksin, ei HTML. Kerros: index.html/*.html + public/. Käyttäjä: — (löydettävyys). | V196 |
| T281 | x | **[a11y]** Saavutettavat nimet (Lighthouse a11y 86→↑). (a) `index.html`: kääri karttanäkymän pääsisältö `<main>`-landmarkiin (`#app-main` tai lisää `role="main"`). (b) `status-panel.ts` `.status-panel-bar role=progressbar` → `aria-label="Pätkän edistyminen"`. (c) `sign-library-panel.ts` `label-content-name-mismatch`: `sign-lib-place-btn` aria-label alkakoon näkyvällä tekstillä TAI poista aria-label kun näkyvä teksti riittää (WCAG 2.5.3). (d) `markers.ts`/`icons.ts` Leaflet-merkit: anna `alt`/`title` (merkin nimi) `L.marker`-optioihin → `aria-command-name` täyttyy. Testattavuus: Vitest-jsdom (progressbar aria-label, main landmark DOM) + Playwright a11y-snapshot. Kerros: index.html + src/ui + src/map. Käyttäjä: molemmat. | V197 |
| T282 | x | **[perf/ux]** CLS 0.171 → <0.1. `layout-shifts`-pääsyy `#route-bar` (0.17): varaa route-barin korkeus CSS:ssä (`min-height`/reservoi) ettei se työnnä layoutia kun `route-selector`/`route-km` täyttyy datalla. Tarkista myös `.left-panel-section` (pieni shift). Testattavuus: Playwright — mittaa CLS `/`-latauksessa < 0.1. Kerros: src/style.css (+ route-bar.ts jos tarpeen). Käyttäjä: molemmat. | V198 |
| T283 | x | **[perf]** Lighthouse `unused-javascript` (~49 KiB). MITATTU-RATKAISU: `marked` (~41 KB raw / ~12 KB gzip) bundlautui index-main-entryyn `areas-wiring → area-view (staattinen) → marked` -ketjun kautta, mutta area-view tarvitaan VAIN `/a/<hash>`-deep-linkillä (initAreaView early-returnaa muuten). Korjaus: `areas-wiring.ts` importtaa area-viewn dynaamisesti path-guardin takana → `marked.esm` oma chunk, EI enää index.html initial-modulepreloadissa. Vahvistettu: `dist/index.html` ei viittaa `marked.esm`/`area-view`-chunkkeihin. MITTAUS ENSIN — helpot lazy-voitot jo tehty (`@milkdown/crepe` admin-page.ts:235 oma chunk, feedback-widget main.ts:136), joten jäljellä hajanaista → älä oleta kohdetta. (a) Aja bundle-analyysi: `bunx vite build` + rollup-visualizer TAI `--sourcemap` + coverage → tunnista mikä ladataan main-sivulla (`index.html`) mutta ei käytetä ensimaalauksessa (todennäk. `marked` staattinen `patkat-page.ts:1`/`area-view.ts:1`, tai Leaflet-osat). (b) Kohdenna: dynaaminen `import()` sille mitä ei tarvita ensinäkymässä (esim. `marked` jos ei above-the-fold). **ÄLÄ lazyta Leafletia** main-kartalla (ydin, V199). (c) Verifioi mittaamalla ennen/jälkeen: `unused-javascript` pienenee, ei regressiota. Realistinen voitto voi olla <49 KiB — kirjaa todellinen. Testattavuus: bundle-mittaus (ei yksikkötestiä). Kerros: src/main.ts + src/ui (import-kohteet). Käyttäjä: talkoolainen (metsä/mobiili, huono yhteys → pienempi initial-JS). | V199 |
| T285 | ✓ | Reittidatan vaihto 2→5 reittiä + tapahtumien väriperheet (SUPERSEDES T7). Kerros: src/main.ts ROUTE_DEFS (init/wiring, data) + public/ GPX + src/style.css (route-bar overflow) + DESIGN.md. TEE: (a) Nimeä public/-GPX slugeiksi (git mv): 'SMTB 2026 30 km.gpx'→smtb-2026-30km.gpx, 'SMTB 2026 55 km.gpx'→smtb-2026-55km.gpx, 'SGF 2026 62 km.gpx'→sgf-2026-62km.gpx, 'SGF 2026 125 km.gpx'→sgf-2026-125km.gpx, 'SGF 2026 175 km.gpx'→sgf-2026-175km.gpx; poista vanhat route-35km.gpx + route-55km.gpx. (b) ROUTE_DEFS (main.ts:20-23) 5 reittiä id+label+color+file: smtb-30 '30 km' #4C97D6 /smtb-2026-30km.gpx, smtb-55 '55 km' #1E5A94, sgf-62 '62 km' #E9A13B, sgf-125 '125 km' #E2662A, sgf-175 '175 km' #C4384A. (c) DESIGN.md §Reitti-/pätkävärit (rivit 68-74): korvaa 'Reitit 35km/55km' -taulu kahdella väriperheellä + kirjaa uudet hex-arvot. (d) src/style.css #route-bar-main: overflow-x:auto (+ -webkit-overflow-scrolling:touch); .route-tab flex-shrink:0 — 5 pilliä overflow mobiililla. (e) Kontrastiauditointi: pill-tausta=reittiväri + tumma teksti (--text-body); smtb-55 #1E5A94 luettavuus → karttamaster-ux auditoi, säädä sävyä jos <3:1. (f) Testit: gpx.test.ts:36 lue slug-tiedosto route-35km.gpx sijaan; t204-route-visibility-control.test.ts + route-status.test.ts fixtuurit id 35km/55km→uudet. Testattavuus: Vitest (gpx-parse + route-status + route-vis-control) + Playwright critical-paths (5 reittiä renderöityy, drive toimii). UX: horizontal-scroll route-bar (talkoolainen mobiili), 44px touch säilyy. Käyttäjä: molemmat. HUOM: vanha markkidata routeIds 35km/55km orvoksi — uusi 2026-kausi=uudet merkit (ei migraatiota tässä). | §C,V6,V7 |
| T286 | ✓ | Reittivalitsin mobiiliystävälliseksi + suuntanuolet. (A) RouteVisibilityControl (järjestäjä): 5 vis-pilliä rivissä vuoti mobiililla ruudun ulkopuolelle (MTB-pillit piiloon) → korvattu YHDELLÄ trigger-napilla (karttatasot-ikoni + aktiivilaskuri n/5) joka avaa listapaneelin: reitit ryhmitelty tapahtumittain (event-kenttä RouteConfigissa: SyöteMTB/Gravel Fest), rivi = väripallo+label+silmä-toggle, 44px touch, outside-click sulkee. V6 säilyy (viimeistä ei voi piilottaa). CSS: `#route-selector` overflow:visible visibility-moodissa (kumoaa T285 overflow-x:n joka leikkasi paneelin). (B) Suuntanuolet reitin varrella (kulkusuunta): `bearingDeg`+`directionArrowPlacements` (bearing.ts, Vitest-pure, KIINTEÄ 800m väli → tasainen tiheys/km) + `route-direction.ts` buildDirectionArrows → L.divIcon rotatoitu nuoli reittivärillä, PIENI (11px) + opacity 0.65 (huomaamaton), interactive:false. Kytketty main.ts:ssä polyline add/remove + zoomend → näkyvät VAIN kun reitti näkyvissä JA zoom≥MIN_ARROW_ZOOM(12) (yleiskuva siisti, paljastuvat lähikuvassa; käyttäjäpyyntö). Kerros: src/map + src/logic + src/main.ts + src/style.css. (C) Migraatio `scripts/migrate-marker-routes.ts`: vanhat merkit (route_ids 35km/55km) orvoiksi → uudelleentägää sijainnin mukaan (assignRoutesToMarker 100m + recompute distance_from_start), dry-run default / --apply. Testattavuus: Vitest (t204 trigger+panel+grouping, t286 bearing/placements) + Playwright (critical-paths trigger→row). UX: talkoolainen/järjestäjä mobiili. Käyttäjä: molemmat. | V6,V134 |
| T287 | x | Pohjakartan näkyvyys-slider (karttakontrolli, "paperikarttamaster"). Käyttäjäpyyntö (järjestäjä): pohja "terhakka" → merkit erottuvat huonosti; himmennä niin että merkit+reitit terävinä. Slider = pohjan näkyvyys 0–100 %, DEFAULT 100 (koko kartta); 50 % kuultaa 50 % läpi; 0 % = pohja häviää (valkoinen jää). SIJAINTI: ⋯-toolbar-valikko `#basemap-dim-container` heti `#btn-layer` (Karttatyyli) vieressä — muiden karttakontrollien seurassa (käyttäjätoive); menu jaettu ∴ näkyy MOLEMMILLE rooleille. RATKAISU: säädä `tilePanen` opacity = slider%/100 + `.leaflet-container` valkoinen tausta (EI verho-panea — Leaflet-pane transformoituu kartan mukana ∴ ei peitä viewportia; EI `tileLayer.setOpacity` — nollautuisi karttatyyli-vaihdossa `map-init.ts:40-41`). tilePane persistoituu → säilyy tyylinvaihdossa. Kerros (pura kahtia): (a) `src/logic/basemap-dim.ts` (Vitest-pure): clamp 0–1, localStorage load/save (`'karttamaster-basemap-opacity'`, default 100 %), sliderToOpacity/opacityToSlider. Ei Leafletia. (b) `src/map/basemap-dim-control.ts` (Leaflet-glue): range-input DOM valikkoriviin, input→`tilePane.style.opacity` + persist, `stopPropagation` (ettei menu-sulku document-klikistä sulje kesken raahauksen), idempotentti `replaceChildren` (V137/B92). Wiring: `map-init.ts` instantioi `#basemap-dim-container`iin, riippumaton `currentTileLayer`-viittauksesta. Default 100 % = nykyinen kirkkaus (kukaan ⊥ törmää vahingossa). UX: §K-sopimus PUUTTUU → lisää DESIGN.md §K (menu-rivi: label + range-input, slider min-height 44px touch hanskoin). ⊥ riko max-2-nappia (passiivinen säädin). Testattavuus: `basemap-dim.ts` Vitest-pure (clamp-rajat 0/1/yli, load default 100 %, persist round-trip ml. 0, sliderToOpacity 0→0 & 100→1 & 50→0.5; localStorage `vi.stubGlobal`-mock). Kontrolli ⊥ kriittinen polku → EI uutta Playwrightia, visuaali manuaalisesti. Käyttäjä: molemmat. | V51,V108,V201 |
| T284 | x | **[perf]** Lighthouse `image-delivery` (~100 KiB). HUOM: kuvat JO webp (`src/assets/signs/*.webp`, 672K) → webp-konversio EI tarvita, vain lazy-load. (a) Lisää `loading="lazy"` offscreen-`<img>`:lle: merkkikirjaston galleria-thumbnailit (`sign-library-panel.ts`, DESIGN V130 `.sign-image-thumb`) + muut ei-heti-näkyvät. (b) Ainoa nykyinen lazy: `marker-detail-modal.ts:191`. (c) **ÄLÄ** lisää lazyä karttamarkkerien ikoneihin (`icons.ts`/`markers.ts`, V200). (d) Verifioi ettei offscreen-kuvia ladata ennen scrollia/modaalin avausta. Testattavuus: Vitest-jsdom (galleria-thumb `<img>` saa `loading=lazy`, markkeri-ikoni ei). Kerros: src/ui. UX: talkoolainen mobiili. | V200 |
| T288 | ✓ | SMTB 110km siirtymä = 6. reitti (amendaa §14 scopea 5→6). Kerros: src/main.ts ROUTE_DEFS (init/wiring, data) + public/ GPX. TEE: (a) git mv `public/'SMTB 2026 110 km siirtyma.gpx'` → `public/smtb-2026-110km-siirtyma.gpx` (kebab, välit pois); poista duplikaatti `data/SMTB 2026 110 km siirtyma.gpx`. (b) ROUTE_DEFS (main.ts:28 jälkeen) uusi rivi: `{ id:'smtb-110-siirtyma', label:'110 km siirtymä', color:'#1E5A8F', event:'SyöteMTB', file:'/smtb-2026-110km-siirtyma.gpx' }`. GPX validi: 132 trkpt, ~700m yhdyspätkä ('110 km'=SyöteMTB-tapahtuman nimi EI jäljen pituus). Renderöi kuin muut reitit (yhtenäinen viiva, oma pilli, ryhmittyy SyöteMTB-eventin alle T286-valitsinpaneelissa). Väri #1E5A8F=3. SyöteMTB-sininen (ramppi light #4C97D6 → medium #2F6FB0 → dark #1E5A8F). UX: karttamaster-ux auditoi että 110-sininen erottuu #2F6FB0:sta (sama event-perhe — säädä sävyä jos liian lähellä); pilli-kontrasti tumma teksti ≥3:1. Testattavuus: EI uutta E2E — olemassa oleva reitinrenderöinti (T285 critical-paths) kattaa; manuaali/Playwright-smoke (6 reittiä renderöityy). Käyttäjä: molemmat (talkoolainen näkee siirtymän kartalla). | V6,V7 |
| T296 | x | Fix B112 — talkoo-login → `/patkat`-hubi. `src/ui/auth-screen.ts` `loginTalkoo`: ok & ⊥ `pendingCode` → navigoi `/patkat` (⊥ `finishAuth`-jatko `/`-karttaan). `pendingCode` ∃ → nykyinen `finishAuth({code})` ennallaan (T272 deep-select). `role-view.ts wireAuth` järjestäjä-haara koskematon (V189). HUOM `reauthCallback`: re-login kesken session ⊥ heitä hubiin — redirect vain kylmästä loginista. Testattavuus: Vitest-jsdom (talkoo-login ⊥ koodi → navigointi `/patkat`; koodilla → ⊥ navigointia + `onAuthenticated(code)`; reauth → ⊥ navigointia) + Playwright (yleissalasana `/`:ssä → päädy `/patkat`). Kerros: src/ui/. Käyttäjä: talkoolainen. | V208,V188,T271,T272,B112 |
| T297 | x | Fix B113 (logiikka+backend) — slug ∀ pätkälle luonnissa. (a) `src/logic/segments.ts`: `Segment.slug?: string`; `createSegment` generoi slugin `displayName`istä (`generateSegmentSlug(name, existingSlugs)`); `updateSegment`: `displayName` muuttuu → regeneroi slug (vanha kuolee V209); `getSegmentForCode` matchaa `slug` ensin, `assignedCode` legacy-fallbackina. (b) `server/db.ts`: `ALTER TABLE segments ADD COLUMN slug TEXT` idempotent + backfill (`assignedCode` jos ∃, muuten displayName-slug); uniikkius sovelluskerroksessa (V191). (c) `server/routes/segments.ts`: slug POST/PUT-payloadiin. (d) ⊥ luo `talkoolainen_codes`-riviä slugille (V210) — assign-flow pysyy erillisenä assignee-toimintona. Testattavuus: Vitest-pure (luonti → slug ∃; rename → uusi slug, vanha ⊥ resolvoidu; törmäys → suffiksi; reititön tehtävä saa slugin) + Bun-integraatio (migraatio idempotent, backfill, roundtrip). Kerros: src/logic/ + server/. Käyttäjä: molemmat. | V209,V210,V191,B113 |
| T298 | x | Fix B113 (UI) — avaa heti, linkki aina näkyvissä. (a) `src/ui/patkat-page.ts`: "Avaa →" ∀ pätkälle (`seg.slug`), poista "Ei jaettu" -haara; järjestäjän Kopioi (T275) ∀ riville. (b) `src/ui/segment-creation-modal.ts`: tallennuksen jälkeen "Avaa pätkä →" (`/s/<slug>`) `onSaved`-polkuun. (c) `src/ui/segment-details-modal.ts` OHUENA (pilkko ⚠️): nimikenttä näyttää elävän `/s/<slug>`-linkin + Kopioi (päivittyy nimenmuutoksessa); assign-osio = vain assignee-näyttönimi, ⊥ enää "Luo linkki" -portti; nimenmuutos varoittaa "vanha linkki lakkaa toimimasta" (V209). Testattavuus: Vitest-jsdom (⊥ assignia → Avaa ∃; rename → linkki päivittyy; luontimodaali → Avaa oikealla hrefillä) + Playwright (luo pätkä → avaa suoraan). Kerros: src/ui/. Käyttäjä: järjestäjä. | V209,T275,T276,B113 |
| T299 | ✓ | **[Kriittinen]** Fix B114 — pätkänluonti mittaa km YHDESTÄ reitistä. Käyttäjän ykköstarve 2026-07-25: "kun teen pätkän, sillä on ne oikeat merkit". (a) `src/logic/segments.ts`: `Segment.primaryRouteId?: string`; `createSegment` ottaa sen; `validateRouteFields` vaatii `primaryRouteId ∈ routeIds` kun reitilliset kentät ∃; legacy ilman kenttää → `routeIds[0]` (⊥ migraatio, V211). (b) `src/ui/segment-panel.ts` `receivePoint` (r126-157): klikki 1 lukitsee `primaryRouteId` + `startDist`; klikki 2 ⊥ käytä omaa `resolveClick`-reittiään vaan snappaa PRIMARY-reittiin (`nearestPointIndex(primary.routePoints)`) → km samasta geometriasta ∴ `min/max` (r137-138) vertaa saman reitin lukuja. Etäisyys primaryReitistä > `SHARED_THRESHOLD_M` → `setError("Toinen piste ei ole samalla reitillä — klikkaa reitin varrelta")`, ⊥ hiljainen sekoitus. `routeIds` = `assignRoutesToMarker`-tyylinen jaettu osuus primaryn ympäriltä, ⊥ enää "klikki2:n reitti pushataan listaan" (r149-151). (c) `validateNoOverlap` vertailee vain saman `primaryRouteId`:n pätkiä. Testattavuus: Vitest-pure (2 klikkiä samalle reitille → oikea väli; klikki 2 eri reitille jaetulla osuudella → km primarysta, ⊥ sekoitusta; klikki 2 kaukana → virhe; legacy-segment ilman primaryRouteId toimii) + Vitest-jsdom (virheviesti näkyy) + Playwright (luo pätkä jaetulla 3-reitti-osuudella → merkkilista = vain pätkän merkit). Kerros: src/logic/ + src/ui/. Käyttäjä: järjestäjä. Avaa: T300. | V211,V25,V11,B114 |
| T300 | ✓ | **[Kriittinen]** Fix B115 (client) — km per reitti. `SignMarker.distanceByRoute: Record<routeId,m>` (V212). (a) `src/logic/types.ts`: kenttä + `distanceFromStart` säilyy (primary/legacy). (b) UUSI `src/logic/marker-distance.ts` (Vitest-pure): `computeDistanceByRoute(lat, lon, routes)` → km ∀ reitille jonka etäisyys ≤ `SHARED_THRESHOLD_M`; `distanceForRoute(marker, routeId)` → `distanceByRoute[routeId] ?? distanceFromStart` (fallback = legacy-yhteensopivuus). (c) `src/logic/task-markers.ts` (V140 kanoninen suodatin, r29-32): range-vertailu `distanceForRoute(m, source.primaryRouteId)` ⊥ `m.distanceFromStart` — TÄMÄ on rivi joka nyt kaappaa väärät merkit. (d) `src/map/markers.ts`: `nearestRouteAssignment` (r177-193) palauttaa myös `distanceByRoute`; `add`/`recompute` (r401-411) tallentavat sen; sync-luku (r151-155) + `src/logic/sync.ts:42` mappaa `distance_by_route`. (e) LAZY BACKFILL: GPX:ien latauduttua merkit joilta `distanceByRoute` puuttuu saavat sen laskettuna `lat/lon`:sta (⊥ kantamigraatiota, ⊥ kosketa vanhaan dataan §C-parkki) — laskenta ⊥ saa blokata ensirenderiä. Testattavuus: Vitest-pure (merkki 2 reitillä → 2 eri km:ää; suodatus poimii oikean; puuttuva kenttä → fallback; jaettu osuus 3 reittiä) + Vitest-jsdom (backfill täyttää muistitilan). Kerros: src/logic/ + src/map/. Käyttäjä: molemmat. Vaatii T299. Avaa: T301. | V212,V140,V25,B115 |
| T301 | ✓ | **[Kriittinen]** Fix B115 (backend) — kerrokset samasta km-lähteestä (V213). Ilman tätä talkoolainen NÄKEE oikean listan mutta saa 403:n samasta merkistä (B100-oppi toisin päin). (a) `server/db.ts`: `ALTER TABLE markers ADD COLUMN distance_by_route TEXT` idempotent (JSON-objekti, NULL = legacy → fallback `distance_from_start`); ⊥ backfill-ajoa (§C-parkki). (b) `server/routes/markers.ts`: `distance_by_route` POST/PUT-payloadiin + GET-vastaukseen. (c) `server/db.ts` segments: `primary_route_id TEXT` (T299-pari) + `server/routes/segments.ts` payloadiin. (d) `server/marker-audit.ts` `markerInOwnSegment`: range-tarkistus `distance_by_route[seg.primary_route_id] ?? distance_from_start` ±ε (V149 ε-toleranssi säilyy) ∴ peilaa `resolveTaskMarkers`ia täsmälleen. Server ⊥ laske km:ää itse (⊥ geometriaa, V149) — lukee clientin arvon samalla luottamustasolla kuin ennen. Testattavuus: Bun-integraatio (migraatio idempotent; NULL-legacy-merkki läpäisee fallbackilla; jaetun osuuden merkki oikeaan pätkään 200 & naapuripätkään 403; ε-reuna; roundtrip POST→GET). Kerros: server/. Käyttäjä: talkoolainen. Vaatii T300. | V213,V149,V150,V154,B115 |
| T302 | ✓ | **[Suuri]** Fix B116 — `nearestPointCandidates` (V214). `src/logic/bearing.ts`: UUSI `nearestPointCandidates(points, lat, lon, thresholdM)` → kaikki LOKAALIT minimit (indeksi jonka naapurit molemmin puolin kauempana) joiden etäisyys ≤ threshold, järjestettynä etäisyys asc. `nearestPointIndex` = wrapper (`candidates[0]?.idx ?? argmin`) ∴ ⊥ rikota nykykutsujia. Käyttöönotto kontekstilla: (a) `task-markers.ts` — pätkän range: ehdokas joka osuu `[startDist,endDist]` sisään VOITTAA lähimmän (lenkki ⊥ enää arpo merkkiä toiseen päähän reittiä); (b) `segment-panel.ts resolveClick` — ehdokkaat näytetään jos ∃ >1 & ne ovat >500 m erillään km-akselilla → järjestäjä valitsee kumpaa kohtaa tarkoitti (⊥ arvaa puolesta). Muut kutsujat ennallaan tässä taskissa. Testattavuus: Vitest-pure (edestakainen polyline → 2 ehdokasta; lenkki → 2; suora → 1; threshold rajaa; range-suosinta valitsee oikean; wrapper = vanha käytös). Kerros: src/logic/. Käyttäjä: molemmat. | V214,V140,B116 |
| T303 | ✓ | **[Suuri]** Fix B117+B118 — migraatioskripti korjataan, **⊥ AJETA** (§C-parkki: vanha data jätetään rauhaan). Skripti on latautunut ase kunnes korjattu — seuraava ajo toistaisi saman vahingon. (a) `scripts/migrate-marker-routes.ts`: poista kovakoodattu `ROUTE_FILES` (r22-28, 5/6 reittiä — `smtb-110-siirtyma` puuttui) → lue `ROUTE_DEFS` (`src/main.ts`; irrota tarvittaessa omaksi moduuliksi `src/logic/route-defs.ts` ettei skripti importtaa DOM-koodia). (b) primary = LÄHIN reitti (r99 `routes.find(...)` = listajärjestys → korvaa `nearestRoute`-logiikalla, sama sääntö kuin `markers.ts:181`). (c) NEAREST-fallback (r88-95): etäisyyskatto `FAR_FROM_ROUTE_M`=500 → yli katon merkkiä ⊥ tägätä, tulostuu "KÄSIN"-listalle. (d) kirjoita `distance_by_route` (T301) ⊥ vain skalaaria. (e) dry-run pysyy defaulttina; `--apply` vaatii lisäksi eksplisiittisen `--yes-i-checked-the-dry-run`. Testattavuus: Bun (reittilista = ROUTE_DEFS pituus; primary = lähin ⊥ ensimmäinen; >500 m orpo ⊥ muutu & päätyy raporttiin; dry-run ⊥ kirjoita). Kerros: scripts/. Käyttäjä: ylläpito. | V215,B117,B118 |
| T304 | ✓ | **[Keskisuuri]** Reittivärit 2 kanavaan (V216) — nyt 3 SMTB-reittiä erottuu vain vaaleudella (`#4C97D6`/`#2F6FB0`/`#1E5A8F`), katoaa auringossa & päällekkäisillä osuuksilla. (a) DESIGN.md §Reitti-/pätkävärit: uusi paletti — sävyero myös tapahtuman sisällä (MTB pysyy viileänä perheenä muttei kolmena vaaleutena; Gravel vastaavasti) + 2. kanava `dashArray`/`weight` per reitti. (b) `src/main.ts` `ROUTE_DEFS`: `dashArray?`/`weight?` kenttiin; (c) `src/map/`-polyline-rakennus + `RouteVisibilityControl`-väripallot (T286) käyttävät samaa kuviota ∴ legenda vastaa karttaa. (d) Suuntanuolet (T286 B) perivät reitin värin — tarkista kontrasti uudella paletilla. Reunat: jaetulla osuudella alla kulkeva reitti ! erottua (katkoviiva paljastaa); toimii akromaattisena; T287 pohja-dim 0–100 % ⊥ saa hukuttaa. Testattavuus: Vitest (ROUTE_DEFS ∀ reitillä uniikki sävy+kuvio-pari) + Playwright (jaettu osuus: 2 reittiä erotettavissa screenshotista) + kontrastimittaus valkoisella pohjalla (T287). Auto-ux → `/karttamaster-ux`. **Palettirajoite (T348/V96-amend 2026-07-26):** uusi `SEGMENT_COLORS` ⊥ saa sisältää VIHREÄÄ sävyperhettä — vihreä on varattu status-kanavalle (`SEGMENT_DONE_COLOR` = `--confirm`, valmis-pätkän viiva). Sama lista kuin V244:n ehto `SEGMENT_COLORS ∩ ROUTE-värit = ∅` ∴ molemmat tarkistetaan kerralla tässä taskissa. Käyttäjä: molemmat. **TOTEUTUNUT 2026-07-27:** MTB-perhe sävyeroon `#1D8CB4`(196°)/`#4D6FCB`(224°)/`#8C71D6`(256°); Gravel `#A58312`(46°)/`#E2662A`(20°)/`#C4384A`(352°) — amber siirretty 38°→46° koska testi mittasi vain 18° eron oranssiin. Kuviokanava `RouteConfig.dashArray`: ehjä/`18 8`/`6 10` per perhe, `main.ts`-polyline välittää. Legenda: väripallo → **viivaswatch** (`routeSwatchBackground`, UUSI puhdas `src/logic/route-swatch.ts`, 3 jaksoa/swatch) sekä `RouteVisibilityControl`issa että `RouteBar`issa ∴ legendan kuvio = kartan kuvio. `SEGMENT_COLORS` tummaan perheeseen (`#163A5F #552070 #681A41 #582F0F`) ⇒ V244:n leikkausehto täyttyy myös silmällä (ennen `#2F6FB0` = `smtb-55` pikselilleen) & pätkä/reitti erottuvat akromaattisesti vaaleuserolla. Luminanssibudjetti mitattu kaksipuolisesti (viiva ≥2.5:1 karttaa vasten, pillerin teksti ≥3:1 väriä vasten) — `tests/t304-route-palette.test.ts` 14 testiä + `e2e/t304-route-channels.spec.ts` (kartan kuviot + legendan swatchit). **Sivutuote: B137** — V244:n casing-ehto osoittautui ylimääritellyksi kun se mitattiin. | V216,V96,V244,T286,T287,T348 |
| T305 | . | **[Iso, FEATURE ⊥ bugi]** Klikkipolku-työkalu — "aloita → klik klik klik → lopeta" (käyttäjäpyyntö 2026-07-25: "kun reitit haarautuu tosi moneen paikkaan, mitä se osaa seurata"). VASTAA JUURISYYHYN jonka T302 vain lieventää: 2 pistettä + km-väli ⊥ voi ilmaista MITÄ HAARAA tarkoitetaan; pisteiden JONO voi. Snap-to-track (⊥ vapaa piirto): jokainen klik snappaa lähimpään reittipisteeseen & peräkkäisten klikkien väli täytetään ALLA OLEVAA jälkeä pitkin ∴ tulos on aito reittigeometrian osajono (⊥ approksimaatio), km yksikäsitteinen tätä polkua pitkin. Klikkitiheys = haaraohjauksen tarkkuus (harvat suoralla, tiheät risteyksessä). UI: aloita → live-esikatselu jokaisen klikin jälkeen → kumoa viimeinen → lopeta. Käytettävissä sekä pätkän että reitin määrittelyyn. HUOM ⊥ korvaa `[startDist,endDist]`-mallia (§C A-linja) — polku tallentuu lisätietona & km-väli johdetaan siitä; B-linja (pätkä = pistejono) jää auki jos tämä osoittautuu riittämättömäksi. Testattavuus: Vitest-pure (polunrakennus 2 klikistä, haara valitaan klikkien mukaan, kumoa, lopeta) + Playwright (haarautuva osuus → polku seuraa klikattua haaraa). Kerros: src/logic/ + src/map/ + src/ui/. Auto-ux → `/karttamaster-ux`. Käyttäjä: järjestäjä. Vaatii T299,T302. ⚠️ Iso blast-radius → `/ck:grill` + `/ck:review` ennen buildia. | V211,V214,T299,T302 |
| T306 | x | Fix B119 — talkoo-session kirjoitusoikeus (V217). `server/routes/segments.ts` PUT: `isOwnTalkoolainen` → `isTalkoolainen = session.role === 'talkoolainen'` (koodillinen sessio: nykyinen omistajuusehto säilyy rinnalla), sallittu kenttäjoukko ennallaan (`inspected/inspectionNote/completed/startDist/endDist/equipment`). `server/routes/markers.ts` PUT (`:262`-alue): koodittomalle sessiolle ohita segmenttisidos + V150b range-tarkistus (ei pätkää mihin verrata); identiteettikenttä-gate (V150) ennallaan. Dokumentoi V151-poisto-oikeuden laajentuma kommenttiin (`created_by = "Talkoolainen"` ∀). Testattavuus: Bun-integraatio (`test-fixtures.ts` `seedTestUsers`/`authHeaders` + koodillinen sessio erikseen): koodittomalla talkoo-sessiolla PUT vieraaseen pätkään 200 (inspected/completed/equipment/rajat); identiteettikenttä 403; merkin siirto vieraalla pätkällä 200; järjestäjä-kentät (description/assignedCode/slug) 403; koodillinen legacy-sessio pitää vanhat rajansa. Kerros: server/. Käyttäjä: talkoolainen. | V217,V188,V93,V150,B119 |
| T307 | x | Kartan katselu-/muokkaustila — tilalogiikka + portit (V218). UUSI `src/logic/map-mode.ts` (Vitest-pure): `MapMode = 'katselu' \| 'muokkaus'`, `createMapModeState()` + `toggleMapMode` + `canDragMarkers/canPlaceMarkers/canDragSegmentBounds`-predikaatit (⊥ DOM, ⊥ Leaflet). Wiring: `src/app/markers-wiring.ts` → `markerManager.setDraggableFn(m => mode.canDragMarkers() && <nykyinen V150-ehto>)`, uudelleenarvo tilan vaihtuessa (merkit ! päivittyvät ilman reloadia); `src/ui/place-mode.ts` → `armFromSidebar`/`pendingDblClick`-haara no-op katselussa; `src/app/segments-wiring.ts` → rajakahvat vain muokkauksessa. `document.body.dataset.mapMode` asetetaan yhdestä paikasta. DEFAULT katselu joka latauksella (⊥ localStorage-persistointi). Testattavuus: Vitest-pure (predikaatit, toggle, default) + Vitest-jsdom (dataset päivittyy; PlaceMode ei aseta merkkiä katselussa). Kerros: src/logic/ + src/app/ + src/ui/. Käyttäjä: molemmat. | V218,V150,V83 |
| T308 | x | Muokkaustilan UI + visuaalinen korostus (V219). Toggle-nappi molempiin näkymiin: järjestäjä = karttatyökalurivi, talkoolainen = yläpalkin ⋯-valikko/karttamoodin alapalkki (V179-linja) — sama `MapMode`-tila, EI rooli-eriytettyä logiikkaa. Katselussa label "✎ Muokkaa", muokkauksessa "✓ Valmis". Korostus: `#map-area` accent-kehys + pysyvä `.map-mode-pill` "Muokkaustila". `src/ui/map-mode-toggle.ts` (uusi, pure-DOM) + `src/style.css` + `DESIGN.md` §K-sopimus (auto-ux → `/karttamaster-ux komponentti map-mode-toggle`). Touch ≥44px, kontrasti ≥4.5:1 molemmissa teemoissa. Testattavuus: Vitest-jsdom (toggle vaihtaa tilan + labelin; pilleri näkyy vain muokkauksessa) + Playwright `e2e/critical-paths.spec.ts`: katselussa merkin raahaus EI siirrä merkkiä eikä tuplaklikki luo merkkiä; muokkaustilassa molemmat toimivat (päivitä olemassa olevat "Merkki kartalle" + "dblclick kartalla" -testit avaamaan muokkaustila ensin). Kerros: src/ui/ + CSS. Käyttäjä: molemmat. | V219,V218,T307,V197 |
| T309 | x | **[Suuri]** B121-korjaus: merkin siirto ⊥ saa hävittää "Seuraava merkki" -alapalkkia. Kaksi kanavaa, molemmat ! korjata. **(a) Rollback (V220):** `src/map/markers.ts:410` dragend — ota snapshot ENNEN mutaatiota (`lat/lon/distanceFromStart/distanceByRoute/routeIds`), tee `apiPut` ja vasta onnistumisella jätä uusi tila voimaan; 403/verkkovirhe → palauta kentät + `lm.setLatLng(vanha)` + `onUpdate()` + `showWarning('⚠ Siirto ei sallittu — merkki palautettiin')`. HUOM `apiPut` on nyt fire-and-forget outbox-polku (T185) → selvitä palauttaako se statuksen; jos ei, laajenna niin että 403 erottuu offline-jonotuksesta (offline ⊥ saa rollbackata — se on laillinen pending-tila). **(b) Primary-reitin km (V221):** dragend laskee `bestRouteIdx`:n yli KAIKKIEN reittien → korvaa pätkän primary-reitin km:llä (`computeDistanceByRoute` on jo oikein, `distanceFromStart` ⊥); talkoolaiskontekstissa jäsenyysratkaisu = sama sääntö kuin serverin `markerInOwnSegment` (`server/marker-audit.ts`). Testattavuus: Taso 1 Vitest-pure (siirto jaetulla osuudella → `resolveTaskMarkers` pitää merkin pätkässä; siirto oikeasti rangen ulkopuolelle → rollback-polku palauttaa alkuperäiset kentät), Taso 4 Bun (`PUT` 403 oman pätkän ulkopuolelle — regressio V150:lle), Taso 3 Playwright (talkoolainen raahaa merkkiä → `#segment-view-next` yhä näkyvissä & merkki yhä raahattava). Kerros: `src/map/markers.ts` + `src/logic/marker-distance.ts`. Käyttäjä: talkoolainen. | V220,V221,V150,V212,B121 |
| T310 | x | **[Suuri]** B122-korjaus: talkoolaiselle paluupolku `/patkat`-hubiin (V222). `src/ui/account-menu.ts:26` — poista `role !== 'talkoolainen'` -gate linkiltä; teksti roolikohtainen (talkoolainen: "🧭 Kaikki pätkät", järjestäjä säilyttää "🔧 Pätkät-sivu (tee pätkä)"). Linkki tavoitettavissa ⋯-valikosta molemmissa moodeissa (varmista ettei `[data-view-mode=kartta]`-CSS piilota yläpalkin `#btn-menu`:a). ⊥ lisää nappia hero-primaryyn (max 2 nappia -sääntö). Erota 🏠 (moodinvaihto) ja hub-linkki (sivunvaihto) labelilla ettei synny kahta "kotia". Testattavuus: Taso 2 Vitest-jsdom (role=talkoolainen → `/patkat`-linkki DOM:issa; järjestäjän teksti ennallaan) + Taso 3 Playwright (`/s/<slug>` → Kartalle → ⋯ → `/patkat` latautuu kirjautuneena, ei uudelleenkirjautumista). Auto-ux → `/karttamaster-ux`. Kerros: `src/ui/account-menu.ts` (+ CSS jos toolbar-piilotus estää). Käyttäjä: talkoolainen. | V222,V188,V189,B122 |
| T311 | x | [Pieni] `/patkat` "Kartalle →" sticky alalaitaan (V223). `src/ui/patkat-page.ts:87–94` — nappi omaan `.patkat-actionbar`iin (`position:sticky; bottom:0`) + sisällölle `padding-bottom` ettei viimeinen pätkärivi jää alle (V157/B101-oppi: ⊥ orpoa gappia kun palkki puuttuu/piilossa). Turvallinen alue mobiilissa (`env(safe-area-inset-bottom)`). DESIGN.md §K: uusi "Sticky-toimintopalkki" -sopimus (jaettu tuleville sivuille). Testattavuus: Taso 2 Vitest-jsdom (rakenne + luokat) + Taso 3 Playwright 375px (12 pätkän lista → nappi viewportissa ilman skrollausta, ei peitä viimeistä riviä). Auto-ux → `/karttamaster-ux`. Kerros: `src/ui/patkat-page.ts` + `src/style.css`. Käyttäjä: talkoolainen (mobiili). | V223,V157 |
| T312 | x | [Pieni] Hero-alapalkin ◀▶ reunoihin (V224). `src/ui/segment-hero.ts:100–144` + `src/style.css:1866` — selailurivi 3-sarakkeiseksi: `◀` kiinni vasempaan, label `flex:1; min-width:0; text-overflow:ellipsis` keskellä, `▶` kiinni oikeaan. Nuolen leveys kiinteä (≥44×44) ∴ x-sijainti ⊥ liiku merkin nimen mukaan. Disabled-clamp päihin (V159) & aria-labelit säilyvät. Testattavuus: Taso 2 Vitest-jsdom (rakenne, disabled-tilat päissä) + Taso 3 Playwright (lyhyt vs. pitkä merkkinimi → `boundingBox().x` sama molemmille nuolille ±1px). Auto-ux → `/karttamaster-ux`. Kerros: `src/ui/segment-hero.ts` + CSS. Käyttäjä: talkoolainen (hanskat, peukalo). | V224,V159 |
| T313 | x | [Keski] Talkoolaisen view-moodi säilyy refreshissä (V225). `src/app/talkoolainen-mode.ts:35` — `setViewMode('koti')` kovakoodattu → lue `sessionStorage['km:viewmode:<slug>']`; puuttuu → `koti` (V174). `setViewMode` kirjoittaa avaimen jokaisella vaihdolla. Slug-kohtainen ∴ toisen pätkän avaus alkaa kodista. Karttamoodiin palattaessa ! ajaa sama `onEnterKartta` (Leaflet `invalidateSize`, V176) kuin nappipolussa — muuten kartta renderöityy väärän kokoisena. Testattavuus: Taso 2 Vitest-jsdom (`sessionStorage` **aina** `vi.stubGlobal`-mockina — Node v26 -konflikti; tuore → koti, tallennettu `kartta` → kartta + invalidateSize kutsuttu, eri slug → koti) + Taso 3 Playwright (Kartalle → reload → kartta näkyvissä). Kerros: `src/app/talkoolainen-mode.ts`. Käyttäjä: talkoolainen. | V225,V174,V176 |
| T314 | . | [Pieni, dev-only] B111 juurisyy 2 — dev-FOUC. Prod-osuus ✓ (T278: unpkg-CDN pois, `@import 'leaflet/dist/leaflet.css'` `src/style.css:10`). Jäljellä: app-CSS tulee JS-moduulista (`import './style.css'` main/patkat/inventory/admin) & entry-HTML:ien `<head>` on ilman `<link>`:iä → Vite dev injektoi tyylit vasta moduulin ajon jälkeen = tyylitön välähdys joka dev-latauksella (prodissa Vite extractaa `<link>`:iksi ∴ ei oiretta). Vaihtoehdot: (a) kriittinen inline-tyyli (tausta+layout-runko) entry-HTML:ien headiin, (b) `<link rel="stylesheet" href="/src/style.css">` headiin (Vite hoitaa molemmat moodit), (c) hyväksy & dokumentoi tietoiseksi dev-rajoitteeksi → sulje B111. Valinta rakentajalle; verifiointi: dev-lataus ⊥ näytä tyylitöntä välähdystä, prod-bundle ⊥ saa duplikoida CSS:ää. Kerros: `index.html`/`patkat.html`/`inventory.html`/`admin.html` (+ CSS). Käyttäjä: kehittäjä (ei tuotantovaikutusta). | V194,B111 |
| T316 | ✓ | **[Kriittinen]** Fix B124 (a) — audit-rivin `segment_code` johdetaan MERKISTÄ (V227). Ilman tätä T227-undo & pätkämodaalin Aktiviteetti ovat kuollutta koodia tuotannossa (263/263 riviä NULL). (a) `server/marker-audit.ts` `logMarkerAudit`: ratkaise pätkä uusiokäyttäen `ownSegments`+`markerInOwnSegment`-unionia (V154) — ⊥ UUTTA jäsenyyslogiikkaa (kopio hajoaisi kuten B100). (b) `remove`-actionissa merkki on jo DELETEtty kutsuhetkellä ∴ ratkaisu ! tehdä ENNEN DELETEä (tai lue T318:n täydestä payloadista) — muuten poistorivi jää NULLiksi & juuri palautusta tarvitseva tapaus katoaa lokista. (c) `server/routes/markers.ts` 3 kutsupaikkaa (`:149` add, `:263` PUT, `:313` DELETE) välittävät kontekstin; kirjaus pysyy SAMASSA `db.transaction()`issa (V152). (d) ⊥ BACKFILLIÄ 263 legacy-riville: pätkärajat ovat muuttuneet kirjaushetkestä ∴ takautuva johdanto valehtelisi — legacy-NULL renderöityy "pätkä tuntematon". Testattavuus: Taso 4 Bun (talkoolaisen add/move/status/remove → oikea `segment_code`; järjestäjän mutaatio samoin; pätkätön merkki → NULL ⊥ kaada; `linked`/`typeFilter`-osuma kirjautuu kuten V154 näyttää; kirjaus+mutaatio yhä atomisia). Päivitä `docs/components/backend.md`. Kerros: `server/`. Käyttäjä: järjestäjä/admin. | V227,V154,V152,B124 |
| T317 | ✓ | **[Keski]** Fix B124 (b) — hub-login kysyy nimen (V228). Yleissalasanan takaa tulee 11 sessiota nimellä 'Talkoolainen' ∴ loki ⊥ vastaa "kuka". (a) `server/routes/auth.ts:88-115` `talkoo-login`: `body.name` pakollinen, trim, 2–40 merkkiä, muu → 400 `{error:'name_required'}`; `display_name`=annettu nimi (⊥ kovakoodattu). Rate-limit (`talkooFails`) & 7pv-sessio (V188) ennallaan. (b) Frontend KAKSI kutsupaikkaa: `src/ui/auth-screen.ts:227` & `src/patkat.ts:67` — nimikenttä salasanakentän VIEREEN, YKSI lomake & yksi lähetys (⊥ erillistä vaihetta: talkoolainen metsässä hanskat kädessä, V25-henki). (c) Nimi muistiin `localStorage['km:talkoo:nimi']` → esitäytetty seuraavalla kerralla (⊥ uudelleenkirjoitusta; testeissä `vi.stubGlobal`-mock, Node v26 -konflikti). (d) DESIGN.md §R: kenttä ≥44px touch-target. Testattavuus: Taso 4 Bun (nimetön → 400; 1-merkkinen → 400; 41-merkkinen → 400; validi → `sessions.display_name`), Taso 2 Vitest-jsdom (kenttä renderöityy molemmissa kutsupaikoissa, esitäyttö localStoragesta, tyhjä nimi estää lähetyksen). Kerros: `server/` + `src/ui/` + `src/patkat.ts`. Käyttäjä: talkoolainen (kirjautuu), järjestäjä (hyötyy lokissa). HUOM ⊥ turvaraja vaan jäljitettävyys (V228). | V228,V188,V217,V21,B124 |
| T318 | ✓ | **[Keski]** Poisto peruttavaksi — `remove`-payload = koko merkkirivi (V229). Nyt DELETE kirjaa vain merkinnän ∴ "peru tämä" ⊥ voi pitää lupaustaan juuri siinä tapauksessa jossa palautusta eniten tarvitaan. (a) `server/routes/markers.ts:313`-alue: lue merkkirivi ENNEN DELETEä & kirjaa ∀ sarake (`type,lat,lon,distance_from_start,distance_by_route,route_ids,status,location_note,color,short_label,description,label,icon_id,parts_json,image_id,template_id,created_by,updated_by`) `payload_json`iin SAMASSA transaktiossa (V152). (b) Restore INSERTillä ALKUPERÄISELLÄ `id`llä ∴ `linked_marker_ids` & `marker_images` ⊥ orvoiksi. (c) Legacy-payload → `not_undoable` (V229). Testattavuus: Taso 4 Bun (DELETE→payload sisältää ∀ sarakkeen; undo palauttaa merkin samalla id:llä & kentillä; kuvaviittaus säilyy; legacy-payload → not_undoable). Kerros: `server/`. Käyttäjä: järjestäjä/admin. | V229,V152,V153 |
| T319 | ✓ | **[Keski]** Per-rivi-undo endpoint (V230). Nykyinen `POST /api/audit/undo` on VAIN massaperuutus pätkäkoodilla ∴ loki tarvitsee "peru tämä yksi rivi". (a) `server/routes/audit.ts`: `POST /api/audit/undo/:auditId` — `requireRole('admin','järjestäjä')`, atominen `db.transaction()`, strategia actionin mukaan (V229). (b) Kirjaa palautuksen ITSENSÄ audit-riviksi (V230) ∴ peruutus peruttavissa. (c) Virhetilat eksplisiittisesti: jo peruttu → 409, merkki kadonnut välissä & action≠remove → 404, tuntematon `auditId` → 404 (⊥ hiljaista 200:aa, V21). (d) `GET /api/audit` laajennus: `actor`, `actor_role`, `since`, `until`, `limit` (oletus 200, UUSIN ENSIN — nykyinen ASC ⊥ skaalaa kasvavassa taulussa; pätkämodaalin kutsuja ! säilyä yhteensopivana). Testattavuus: Taso 4 Bun (∀ action peruuntuu oikein; atomisuus: keinotekoinen virhe kesken → mikään ⊥ muutu; 409 tuplaperuutuksesta; 404-polut; suodattimet & limit; olemassa oleva massa-undo ⊥ regressoi). Päivitä `docs/components/backend.md`. Kerros: `server/`. Käyttäjä: järjestäjä/admin. Vaatii T318. | V230,V153,V102,V21 |
| T320 | ✓ | **[Keski]** Lokin logiikkakerros — UUSI `src/logic/audit-log.ts` (Vitest-pure) + `audit-sync.ts`-laajennus. Puhdas muunnos ∴ ⊥ DOM, ⊥ Leaflet, ⊥ fetch `audit-log.ts`:ssä. (a) `describeAuditEntry(entry)` → `{verb, actorLabel, timeLabel, segmentLabel}`; `ACTION_VERB`-taulu SIIRTYY tänne `segment-details-modal.ts:573`stä ∴ yksi totuus, ⊥ kopiota (modaali importtaa). (b) `moveDeviationM(entry, current)` → poikkeama metreinä `haversineDistance`illa (`src/logic/bearing.ts:3` UUSIOKÄYTTÖ, ⊥ omaa kaavaa — `scripts/restore-marker-moves.ts`in oma kopio on TIETOINEN poikkeus: ajetaan tuotantokoneella ilman `src/`:ää). (c) `filterEntries(entries,{role,actor,segmentCode,since,until})` predikaattisuodatus. (d) `groupByMarker(entries)` → siirtoketjut aikajärjestyksessä (= analyysi jonka runbook tekee käsin). (e) `src/logic/audit-sync.ts`: `fetchAuditLog(filters)` (globaali, ⊥ `segment_code`-pakkoa) + `undoAuditEntry(auditId)` → `{ok:true}|{ok:false,error:'conflict'|'not_found'|'network'}` — ⊥ pelkkää `null`ia (UI ! kertoa MIKSI, V21). Testattavuus: Taso 1 Vitest-pure (verbit; poikkeama TODELLISILLA 2026-07-25 arvoilla regressiofixtuurina: 65.57585,27.65917 → 65.58250,27.81166 ≈ 7049 m; suodattimet; ryhmittely; vajaa/tyhjä payload ⊥ kaada) + fetch-mock `audit-sync`ille. Kerros: `src/logic/`. Käyttäjä: järjestäjä/admin. Vaatii T319. | V230,V21 |
| T321 | ✓ | **[Suuri]** `/loki`-näkymä — globaali aktiviteettiloki + per-rivi undo (V231). VASTAA 2026-07-25 tapaukseen: palautus ! olla TUOTTEESSA, ⊥ SSH-taidon varassa (`docs/RUNBOOK-merkkien-palautus.md` jää hätävaraksi). (a) UUSI `loki.html` + `src/loki.ts`, entry `vite.config.ts` `input`iin (4 olemassa olevan kuvio) — `AuthScreen`-gate `admin|järjestäjä`, muu → `renderForbidden` (`admin-page.ts:250` uusiokäyttö). (b) UUSI `src/ui/audit-log-page.ts` (Vitest-jsdom): taulukko UUSIN ENSIN — tekijä (rooli-badge) │ teko (T320-verbi) │ merkin tyyppi │ pätkä │ aika │ poikkeama m (vain `move`, korostus >100 m) │ "Näytä kartalla" (`/#marker=<id>`) │ "Peru tämä". Suodatinpalkki rooli/tekijä/pätkä/aikaväli → T320:n predikaatit clientissä; palvelinsuodatus vain `limit`+`since`. (c) Peruutus: `confirm()` ENNEN (tuhoava, V102) → `undoAuditEntry` → 409/404 selkokielellä (⊥ hiljaista epäonnistumista) → lista uudelleen. (d) UUSI `src/audit-log.css` importattuna `src/loki.ts`:stä — ⊥ RIVIÄKÄÄN `src/style.css`:ään (rinnakkainen T307–T315 muokkaa sitä ∴ konfliktipinta nollaan); tokenit peritään `:root`ista (V231). (e) DESIGN.md §K uusi sopimus "AuditLogPage": taulukko desktopilla, korttilista <768px (järjestäjän mobiili = välttävä toiminta, VISION), napit ≥44px (§R), rooli-badget `--status-*`-tokeneista. (f) Linkki `/loki`hin `src/ui/account-menu.ts`iin VASTA LOPUKSI & OMANA committina — rinnakkainen sessio muokkaa samaa tiedostoa T310:ssä. Testattavuus: Taso 2 Vitest-jsdom (rivit; suodattimet karsivat; confirm-flow; 409/404-viestit; tyhjä loki → selkeä tyhjätila) + Taso 3 Playwright UUSI `e2e/t321-audit-log.spec.ts` (järjestäjä avaa /loki → näkee rivit → peruu siirron → merkki palautuu; talkoolainen → forbidden). Auto-ux → `/karttamaster-ux`. Päivitä `docs/components/ui.md` + `COMPONENTS.md` (AuditLog, AuditLogPage). Kerros: `src/ui/` + entry. Käyttäjä: järjestäjä/admin. Vaatii T319,T320. | V231,V230,V102,V21 |
| T322 | ✓ | **[Keski]** Nimi kesken session — siirtymä T317:ään (V228). Käyttäjähavainto 2026-07-25: pätkiä oli jo alettu tehdä & kirjautuneilta puuttuu nimi. T317 kysyy nimen VAIN kirjautuessa ∴ olemassa olevat sessiot (7 vrk TTL, V188) jäisivät nimettömiksi koko tapahtuman ajaksi & audit-loki ⊥ vastaisi "kuka" juuri niiltä joiden työ on jo käynnissä. (a) `server/routes/auth.ts`: `POST /api/auth/name` — `requireAuth`, sama validointi (trim 2–40), `UPDATE sessions SET display_name`. Sessio EI katkea (⊥ uutta cookieta, ⊥ uloskirjausta) — kenttätyö jatkuu keskeytymättä. (b) UUSI `src/ui/name-prompt.ts` + `src/name-prompt.css` (⊥ `style.css`:ään): palkki "Kuka olet? Nimi näkyy muutoshistoriassa" + kenttä + Tallenna + **Ei nyt**. `needsNamePrompt(displayName)` → tosi kun nimi puuttuu tai on legacy-geneerinen `'Talkoolainen'`. Ohitus muistetaan `localStorage['km:talkoo:nimi-ohitettu']` ∴ palkki ⊥ jankuta joka latauksella metsässä. Epäonnistunut tallennus ⊥ sulje palkkia (⊥ hiljaista nimettömyyttä, V21). (c) Mount `/patkat`-hubissa (`src/patkat.ts`, talkoolaisen koti V188) — ⊥ karttanäkymän wiringiin (rinnakkainen T307–T315). Testattavuus: Taso 4 Bun (vanha sessio saa nimen ilman uudelleenkirjautumista; sessio pysyy voimassa; 400-rajat ⊥ hävitä vanhaa nimeä; 401 ilman sessiota; nimen JÄLKEEN kirjatut audit-rivit kantavat oikean tekijän), Taso 2 Vitest-jsdom (palkki vain nimettömälle; esitäyttö; validointi; onnistunut tallennus poistaa palkin; epäonnistunut ei; "Ei nyt" muistetaan; estetty localStorage ⊥ kaada). Kerros: `server/` + `src/ui/`. Käyttäjä: talkoolainen (kenttätyö käynnissä), järjestäjä (hyötyy lokissa). | V228,V188,V21,B124 |
| T323 | . | **[Keski]** Siirron "Kumoa" tekijälle itselleen (V232). Käyttäjäpäätös 2026-07-25: vahvistusdialogi ennen isoa siirtoa HYLÄTTIIN — kitka kohdistuisi laillisiin siirtoihin; oma undo on parempi. Tausta: 2026-07-25 IHMINEN siirsi 14 merkkiä 1–7 km (⊥ bugi) & vasta järjestäjä huomasi ∴ tekijälle ! antaa keino korjata heti. (a) `src/map/markers.ts` `handleDragEnd` (T309-muoto): kun `delivered` & ⊥ rollback → `showToast` (`src/ui/toast.ts`, T253-infra valmis: `actionLabel`+`onAction`+`duration`). Viesti kantaa MATKAN: `Siirretty ${m} m — Kumoa` (>1000 m → km yhdellä desimaalilla) ∴ tekijä näkee heti jos veto oli zoomatulla kartalla odotettua pidempi. (b) `onAction` → `applyMarkerPosition(m, before)` (V220-snapshot, sama muuttuja jonka rollback käyttää) + `lm.setLatLng` + `onUpdate()` + `apiPutAwaited` samoilla kentillä ∴ kumoaminen on tavallinen siirto taaksepäin (kirjautuu `marker_audit`iin omana `move`-rivinään — järjestäjä näkee lokista että tekijä korjasi itse, ⊥ hiljaista poistoa historiasta). (c) Kesto: oletus 5000 ms lyhyt hanskoille → `duration` 9000 ms siirroille; iso siirto (>500 m) ⊥ saa erikoiskohtelua paitsi matkan korostus viestissä (⊥ toista koodipolkua). (d) ⊥ blokkaavaa `confirm`ia missään polussa (V232). Testattavuus: Taso 1 Vitest-pure (matkan muotoilu m/km-kynnyksellä — uusi puhdas apuri `src/logic/marker-distance.ts`:ään tai `audit-log.ts`:n `formatDistance`-uusiokäyttö), Taso 2 Vitest-jsdom (toast saa Kumoa-napin & matkan; klikki palauttaa snapshotin kentät; uusi siirto korvaa slotin; 403-siirto ⊥ näytä toastia), Taso 3 Playwright (talkoolainen raahaa merkkiä → toast → Kumoa → merkki palaa & `#segment-view-next` yhä näkyvissä). Auto-ux → `/karttamaster-ux` (toast-sopimus DESIGN.md §K Toast, T253 — tarkista ettei 9 s kesto riko yhtä-kerrallaan-sääntöä). Kerros: `src/map/markers.ts` + `src/ui/toast.ts` (olemassa) + `src/logic/`. Käyttäjä: talkoolainen (korjaa oman virheensä kentällä), järjestäjä (sama polku, sama hyöty). **Vaatii T309** — sama `handleDragEnd`, samat V220-snapshot-apurit; rakennettava T309:n JÄLKEEN ⊥ rinnalla. | V232,V220,V172,V116,B121 |
| T315 | x | **[Suuri]** Fix B123 — koti-moodin pätkänäkymä scrollaa (V226). Talkoolaisen kenttätyö estyy (alimpia merkkejä ⊥ voi kuitata) ∴ prioriteetti. (a) `src/style.css:1646` `#app[data-view-mode="koti"] #segment-view{max-height:none}` → sidottu korkeus: `#segment-view-container` `flex:1;min-height:0;display:flex` + `#segment-view` `flex:1;min-height:0` (base `overflow-y:auto` r1466 & `overscroll-behavior:contain` r174 säilyvät, V187a). Kartta piilossa kodissa (`#map{display:none}` r1645) ∴ `#map-area`n flex-tila vapautuu näkymälle. (b) `#btn-to-map` (r1627) = kodin primary → ⊥ saa jäädä scrollerin sisään katoamaan; jos jää, siirrä container-tason sticky-alalaitaan (V223-sopimus, sama kuvio kuin T311). (c) Kartta-moodin 46vh-mitoitus (r1691) + `position:absolute`-container (r1683) ennallaan — ⊥ regressoi. (d) Koti-tabit (`.segment-koti-panel`: Varustelista/Kaikki merkit/Kommentit) jakavat saman scrollerin ∴ tab-vaihto ! nollata `scrollTop` ettei uusi tabi avaudu keskeltä. Testattavuus: Taso 3 Playwright (pätkä ≥30 merkkiä → koti-moodi → viimeinen `.segment-view-markers-item` tavoitettavissa scrollilla & näkyvissä, 375×667 JA 1280×720; kartta-moodi ennallaan) + Taso 2 Vitest-jsdom vain jos mitoitusta siirtyy JS:ään (⊥ oletettavasti — CSS-only). Auto-ux → `/karttamaster-ux`. Kerros: `src/style.css` (+ `index.html` jos container tarvitsee luokan). Käyttäjä: talkoolainen. | V226,V187,V175,B123 |

| T324 | ✓ | [Keski, TESTIVELKA] Diagnosoi & korjaa alue-kerroksen punaiset e2e:t (B128, ryhmä A+D — 5/9). `e2e/area-interaction.spec.ts` ×4: `dblclickPolyEdge` → `.area-corner-handle` count 0 (odotus 4) ∴ `MapRectEditor.startEdit` ⊥ käynnisty polygonin `dblclick`ista (`src/map/area-overlay.ts:104`), 14× retry ⇒ ⊥ ajoitusflaky vaan aito ei-laukeaminen. `e2e/critical-paths.spec.ts` "T108 AreaOverlay flyTo zoom 18" (sama tiedosto: `poly.on('click')` → `map.flyTo(...,18)`, `area-overlay.ts:100`) ∴ EPÄILY: yhteinen juurisyy = polygonin pointer-eventit ⊥ tavoita layeria (peittävä overlay TAI `L.DomEvent.stopPropagation`-ketju), ⊥ kaksi erillistä bugia — verifioi ennen kuin korjaat kahdesti. HUOM ⊥ liity V218-muokkaustilaan: alueet EIVÄT ole moodin takana (T307 gettaa vain merkit/sijoituksen/pätkän rajat) & nämä olivat punaisia jo ennen T307:ää. Ratkaise kumpi on rikki: sovellus vai testin geometria (vrt. T309-oppi: `elementFromPoint` paljasti että hero-palkki+palautewidget peittivät kohteen). **DIAGNOOSI VALMIS 2026-07-25 (PM-ajo, mitattu ⊥ arvattu): SOVELLUS EHJÄ, TESTIT HAPANTUNEET — yksi juurisyy molemmille ryhmille.** `src/logic/route-defs.ts` kasvoi 2→6 reittiin (T285: SyöteMTB 30/55/110-siirtymä + Gravel Fest 62/125/175) ∴ `fitBounds` kattaa moninkertaisen alueen → ALKUZOOM ROMAHTI. Seuraukset: (A) 500×300 m testialue renderöityy **12×8 px**:iin & keskellä oleva `area-drag-handle` peittää sen kokonaan — mitattu `elementFromPoint(box.x+4, box.y+4)` → `DIV.leaflet-marker-icon area-drag-handle` ∴ `dblclickPolyEdge`in "klikkaa kulmaa, kaukana center-handlesta" -oletus (`area-interaction.spec.ts:73-78`) ⊥ pidä enää; `MapRectEditor.startEdit` ⊥ laukea koska dblclick ⊥ koskaan tavoita polygonia. (D) flyTo lähtee zoomista **13.4** ⊥ ~17 ∴ animaatio kestää ~4 s: mitattu ZOOM@1s=13.40, @2s=16.18, @3s=17.91, @4s=18.00 — testi odottaa 2500 ms & lukee 17.21. Sovelluslogiikka (`area-overlay.ts:100,104`) on muuttumaton & oikea. Korjaus = ZOOM-RIIPPUMATTOMUUS, ⊥ odotusarvojen löysennys: (a) aluetestit `page.evaluate(() => __testMap.setView([65.6277,27.6275], 16))` ennen `boundingBox`ia ∴ polygoni on isompi kuin center-handle & kulma vapaa; (b) flyTo-testi `waitForFunction(() => __testMap.getZoom() === 18, {timeout: 8000})` ⊥ `waitForTimeout(2500)`. Kirjaa juurisyy `docs/E2E-NOTES.md`:hen kohdaksi 4 ("reittimäärä ohjaa alkuzoomia — ⊥ oleta kartan mittakaavaa"). Testattavuus: ryhmän 5 testiä vihreiksi ILMAN odotusarvojen löysentämistä. Kerros: `e2e/` (tuotantokoodi ⊥ muutu). Käyttäjä: järjestäjä. | V233,V69,B128 |
| T325 | ✓ | [Keski, TESTIVELKA] Diagnosoi & korjaa loput punaiset e2e:t (B128, ryhmä B+C+E — 4/9). (a) `e2e/sprint-features.spec.ts:9` "status panel näkyy järjestäjälle toolbarin alla" + `:46` "päivittyy kun merkki lisätään" — `:46` kulkee `dblclick`→picker-polun jonka `docs/E2E-NOTES.md` on jo merkinnyt headless-flakyksi (T38 pudotti saman polun 2026-07-10) ∴ harkitse merkin seedaamista `/api/markers`-mockilla picker-polun sijaan, ⊥ jahtaa Leaflet-dblclickia. (b) `e2e/critical-paths.spec.ts` "401 → talkoolainen-tab → yleissalasana → kirjautuminen (Model B, T272)" — auth-polku, kriittisin: varmista ettei kyse ole AIDOSTA Model B -regressiosta ennen kuin syytät testiä (V188/T272). (c) `e2e/segments.spec.ts:394` "pätkän voi poistaa listasta". Sama sääntö kuin T324: ⊥ löysennä odotusarvoa; sovelluksen bugi → oma §B-rivi + korjaus, testin bugi → syy `docs/E2E-NOTES.md`:hen. **DIAGNOOSI VALMIS 2026-07-25 (PM-ajo): kaikki 4 = HAPANTUNUT TESTI, ⊥ yhtään sovellusbugia — & (b) ⊥ OLE Model B -regressio.** (a) `sprint-features.spec.ts:9` odottaa `.status-panel-row` count **2**, saa **6** — status-panel renderöi rivin per reitti (`status-panel.ts:6-11`) & `route-defs.ts` kasvoi 2→6 (sama T285-juuri kuin T324). Korjaus: johda odotus datasta (`ROUTES.length`) ⊥ hardkoodaa lukua — luku mätänee uudelleen seuraavalla reitillä. `:46` EI kaadu picker-polkuun (testi on jo päivitetty T307/V218-muokkaustilaan & `#floating-picker` aukeaa): se lukee `.status-panel-detail` **`.first()`** = ENSIMMÄISEN reitin rivi, mutta merkki osuu johonkin 6:sta ∴ luettu rivi on eri reitti kuin mihin merkki lisättiin → jää '—'. Korjaus: lue rivi `[data-route-id="<merkin reitti>"]`-valitsimella (rivillä on jo `dataset.routeId`, `status-panel.ts:16`) TAI assertoi "jokin rivi muuttui", ⊥ `.first()`. (b) `critical-paths.spec.ts:419` — auth EHJÄ: `src/ui/auth-screen.ts:242-247` (T296/V208) navigoi kylmän talkoo-loginin `/patkat`-hubiin ∴ `#auth-screen` ⊥ ole enää DOMissa & `not.toHaveClass(/open/)` kaatuu "element(s) not found" ⊥ "has class open" — testi jäi hub-redesignin (T296) jälkeen päivittämättä. Korjaus: odota navigointia (`page.waitForURL('**/patkat')`) & mockkaa `/api/segments` hubille; V188-polku itse on vihreä (`talkoolais-hub.spec.ts` menee läpi). (c) `segments.spec.ts:394` — sovellus ehjä: `segment-panel.ts:63-65` (T298/V209/B113) avaa luodun pätkän SUORAAN details-modaaliin ∴ `.segment-details-modal-backdrop` on jo auki kun testi yrittää klikata `.btn-segment-details-open`ia sen takaa → 30 s timeout "backdrop intercepts pointer events". Korjaus: poista ···-klikki, klikkaa `.btn-segment-delete-modal`ia suoraan (modaali on auki). Kirjaa (b) & (c) `docs/E2E-NOTES.md`:hen: "tuotemuutos (T296 navigointi, T298 automaattinen modaali) ⇒ testin ENNAKKOEHTO muuttui, ⊥ flaky". Testattavuus: ryhmän 4 testiä vihreiksi; lopputila = `bunx playwright test e2e/` 0 fail yhdessä T324:n kanssa. Kerros: `e2e/` (tuotantokoodi ⊥ muutu). Käyttäjä: molemmat. | V233,V188,V208,V209,B128 |
| T326 | . | [Pieni, A11Y] `.inv-mode-toggle.active`-kontrasti V234-linjaan (B125). Inventaarion (T251) aktiivi-kytkin käyttää accent-täyttöä valkoisella tekstillä = 3.46:1 vaalealla teemalla < 4.5:1. Korjaa samalla kuviolla kuin T308:n `.map-mode-toggle.active`: `--text-body` accent-tintin päällä + accent kantaa reunan. `src/style.css` (`.inv-mode-toggle`-lohko) + DESIGN.md §K-maininta "korjattu". Tarkista samalla greppaamalla ettei muita `.active`-täyttöjä ole jäänyt (`--accent` + `#fff`/`--confirm-text`). Testattavuus: Taso 2 Vitest-jsdom ⊥ mittaa kontrastia ∴ manuaali/laskennallinen verifiointi + DESIGN.md-kirjaus; regressiosuoja = V234 §K-sopimuksessa. Kerros: CSS. Käyttäjä: järjestäjä (inventaario). | V234,V197,B125 |
| T327 | x | **[Kriittinen]** Fix B126 — kulkusuunnan järjestys reitin km:llä (V235). Talkoolainen ohjataan ajamaan väärään suuntaan ∴ korkein prioriteetti. (a) `src/logic/navigation.ts`: kaikki 5 funktiota lukevat km:n `distanceForRoute(m, routeId)`illa (`marker-distance.ts:57`), ⊥ `m.distanceFromStart`ista. `firstUnsetMarker`/`unsetMarkersOrdered` saavat `routeId`-parametrin (`stepUnset` välittää). `nearestUnsetMarker`/`nextMarkerAhead` PITÄVÄT nyk. `routeId`-parametrinsa mutta se ohjaa nyt MYÖS km:n — tällä hetkellä sitä käytetään vain `routeIds.includes`-jäsenyysfiltteriin = puolitiehen jäänyt korjaus. `nextMarkerAhead`in strict `>` säilyy (toistopainallus etenee); vertailu ! katsoa `distancesForRoute`in KAIKKIA ehdokkaita (lenkillä useampi, V214) & järjestysavain = pienin ehdokas > currentDist. (b) `src/ui/segment-hero.ts`: akseli = `segmentPrimaryRouteId(this.ctx.getSegment())`. (c) `src/app/markers-wiring.ts:479-480`: `driveMode.jumpToDistance(distanceForRoute(target, route.id))` — ⊥ `target.distanceFromStart` (eri akseli ⇒ kursori hyppää väärään kohtaan). (d) Greppaa muut suorat `distanceFromStart`-luvut järjestys-/km-näyttökonteksteissa (`segment-marker-list.ts`, `marker-list.ts`): jos lista NÄYTTÄÄ eri km:n kuin hero navigoi, se on sama bugi — kirjaa löydös §B:hen, ⊥ korjaa sokkona samassa taskissa. TODISTE (prod-data 2026-07-25, `dev.db`-kopio): Pätkä 4 (20.68–25.65 km, 13 merkkiä) — 3 merkkiä kantaa km:ää 0.00/0.03/0.57 koska ne on mitattu `smtb-30`:ltä (4.6 km pitkä) vaikka sijaitsevat pätkän LOPUSSA (25.18/25.21/25.65) ∴ nykyjärjestys nostaa ne ENSIMMÄISIKSI. Laajuus: 3/9 pätkää rikki (Pätkä 1 tau 0.850, Pätkä 4 tau 0.282, Pätkä 9 tau 0.912), 8 merkkiä väärällä akselilla; 5 pätkää muuttumattomia ∴ korjaus ⊥ saa muuttaa niiden järjestystä (regressiotesti). Testattavuus: Taso 1 Vitest-pure (`src/logic/navigation.test.ts`) — (i) 2 merkkiä joilla `distanceFromStart` eri reiteiltä (A:45000, B:5000) mutta `distanceByRoute[primary]` (A:1200, B:1800) → järjestys A,B; (ii) Pätkä 4:n oikea tapaus regressiona (merkki jonka skalaari ~0 mutta primary-km 25.18 → viimeiseksi ⊥ ensimmäiseksi); (iii) legacy-fallback (`distanceByRoute` puuttuu → entinen käytös); (iv) `stepUnset`-clamp päihin ennallaan (V159). Kerros: `src/logic/` (pure) + 2 kutsujaa. Käyttäjä: talkoolainen. | V235,V212,V214,V159,B126 |
| T328 | x | **[Kriittinen]** Pätkä omistaa km-akselin (V237/V238) — fix B129. **AJETAAN ENSIN & DEPLOYATAAN**: tapahtuma on PÄÄLLÄ & talkoolaiset ⊥ pääse tekemään oikeassa järjestyksessä ennen tätä. Korvaa T327:n parametrimallin: akseli ⊥ kulje kutsuparametrina vaan tulee pätkästä joka kontekstilla on jo kädessä. (a) UUSI `src/logic/segment-order.ts` (Vitest-pure, ⊥ DOM ⊥ Leaflet): `segmentKm(marker, segment): number | null` (V237 — ehdokas `[startDist−ε,endDist+ε]`:ssä, ε=`RANGE_EPS_M` 50 m serverin `marker-audit.ts`-parina, null = ei osu) + `orderMarkersInSegment(markers, segment): {onRoute: SignMarker[], offRoute: SignMarker[]}` (V238 — suunta `phase`-lookupista, `purku` laskeva; `offRoute` listan ALKUUN). Reititön pätkä (V139: `routeIds`/`startDist`/`endDist` puuttuu) → kaikki `offRoute`-ryhmään järjestämättä, ⊥ kaadu. (b) Kuluttajat lukevat VAIN näitä: `src/ui/segment-hero.ts` :131 näyttö (nyt skalaari = B129:n oire) + :69/:254 järjestys, `src/ui/segment-marker-list.ts` :31 sort + :90 näyttö, `src/ui/segment-view.ts` :278 sort, `src/ui/segment-details-modal.ts` :256 sort + :267 näyttö (järjestäjän modaali — sama luku kuin talkoolaisella, V236-henki). (c) `src/logic/navigation.ts`: erilliset `routeId`-parametrit POISTUVAT `firstUnsetMarker`/`unsetMarkersOrdered`/`stepUnset`ista → `segment`; `nearestUnsetMarker`/`nextMarkerAhead` PITÄVÄT `routeId`:n (drive-moodi ajaa reittiä ⊥ pätkää — eri konteksti, eri omistaja). `orderKm`n "pienin ehdokas" -arvaus korvautuu `segmentKm`illä pätkäkonteksteissa. (d) UX: "ei reitillä" -ryhmä käyttää T264:n olemassa olevaa ryhmittelykuviota (`segment-marker-list` asetetut/asettamatta/ei tarpeen) ∴ ⊥ uutta DESIGN.md §K-sopimusta; touch-target 44px säilyy. AVOIN (⊥ blokkaa): `segment-hero.ts:59` piilottaa heron muissa vaiheissa kuin `asettaminen` ∴ purussa suunta näkyy VAIN listassa — tarvitaanko purkuun oma hero ("Seuraava kerättävä")? Käyttäjäpäätös myöhemmin. ⊥ KUULU TÄHÄN: prod-datan uudelleenlaskenta (§C-parkki pysyy — `lat/lon` on totuus & skalaarista tulee merkityksetön) & `/patkat`-hubin merkkimäärä (B127/V236 → T329) ∴ hub näyttää TÄMÄN JÄLKEENKIN eri luvun kuin pätkänäkymä, se on tiedossa ⊥ uusi rikko. TODISTE (ajettu prod `dev.db` + GPX 2026-07-25): Pätkä 4 = 20.68–25.65 km smtb-55, 15 merkkiä; backfill osuu 15/15 ∴ akseli ON saatavilla, sitä vain ⊥ käytetä näytössä; 6 merkkiä smtb-30+smtb-55 -jaetulla osuudella kantaa skalaaria 0.00–0.57 vaikka smtb-55-akselilla ovat 25.14–25.69 → hero ◀▶ näyttää 18.9→…→25.1→0.0→0.0→25.6→0.6→0.5 km (käyttäjä: "hyppii sinne tänne"). Testattavuus: Taso 1 Vitest-pure (`tests/segment-order.test.ts`) — (i) Pätkä 4:n prod-tapaus: 6 jaetun osuuden merkkiä → 25.14–25.69 ⊥ 0.0–0.6; (ii) `phase='purku'` → käänteinen järjestys samalla datalla; (iii) lenkki (V214): merkillä 2 ehdokasta samalla reitillä → `[start,end]` valitsee oikean ⊥ pienimmän; (iv) `offRoute`-ryhmä (merkki yli `end+ε`); (v) reititön pätkä ⊥ kaadu; (vi) REGRESSIO: 5 muuttumatonta pätkää säilyttävät järjestyksensä (T327-kuvio). Taso 2 jsdom (`tests/t232-segment-view-hero.test.ts`): hero ▶ 8× → näytetty km monotonisesti kasvava. E2E: päivitä `e2e/segments.spec.ts` (SegmentMarkerList-järjestys). Kerros: `src/logic/` (uusi, pure) + 4 `src/ui/`-kuluttajaa + `src/logic/navigation.ts`. Käyttäjä: talkoolainen (kenttätyön järjestys) + järjestäjä (modaali sama luku). | V237,V238,V235,V214,V154,V139,B129 |
| T329 | . | **[Suuri]** `distance_by_route` täytetään kerran → YKSI merkkimäärä kaikkialla (V236, sulkee B127). Ajetaan T328:n JÄLKEEN. Löydös 2026-07-25: serveri OSAA jo tallentaa kentän (`server/routes/markers.ts` :125 INSERT, :243 UPDATE) & sillä on kanoninen jäsenyys `markerInOwnSegment`+`distsOnSegmentAxis` (`marker-audit.ts:100`, V154) — sarake on NULL 190/190 VAIN koska kukaan ⊥ ole koskaan kirjoittanut sitä: client laskee backfillin muistiin (`main.ts:66`, `markers.ts:259`) & heittää menemään. ∴ ⊥ tarvita GPX:ää serverille & ⊥ migraatioskriptiä & ⊥ datan uudelleenlaskentaa (`distance_from_start` ⊥ liikahda — täytetään VAIN johdettu kenttä jonka lähde on `lat/lon`). (a) `POST /api/markers/backfill-distances` — `requireRole('admin')`, payload `{[id]: Record<routeId, number[]>}`, atominen `db.transaction()`, kirjoittaa VAIN rivit joilla `distance_by_route IS NULL` (idempotentti, ⊥ ylikirjoita tuoretta). (b) **⊥ AUDIT-RIVEJÄ** — 190 normaalia PUT:ia täyttäisi juuri rakennetun lokin (T320/T321) roskalla & tekisi 2026-07-25-incidentin jäljityksestä lukukelvottoman; tämä on taskin kriittisin yksityiskohta. (c) Client: järjestäjä/admin laukaisee kerran (⊥ automaattinen jokaisella latauksella — kirjoitus ⊥ kuulu talkoolaisen kenttäpolkuun). Sen jälkeen hub `/patkat`, pätkänäkymä, järjestäjän paneeli & serverin auktorisointi lukevat SAMAA lukua ∴ "järjestäjän tekemä pätkä = se luku joka näkyy kaikkialla" (käyttäjävaatimus 2026-07-25). Testattavuus: Taso 4 Bun (`server/test-fixtures.ts` `seedTestUsers`+`authHeaders`): admin 200 & rivit täyttyvät; järjestäjä/talkoolainen 403; idempotenssi (2. ajo ⊥ muuta mitään); `marker_audit`-rivimäärä ENNEN == JÄLKEEN. Taso 1: hub-laskenta == näkymän laskenta samalla datalla. Kerros: `server/routes/markers.ts` + client-laukaisin. Käyttäjä: järjestäjä (tilannekuva jonka lukuun voi luottaa). | V236,V154,V212,B127,T328 |
| T330 | . | [Keski] `marker.distanceFromStart` tyyppitason lukkoon — kääntäjä korvaa 6 invarianttia. Tapahtuman JÄLKEEN (⊥ kiire, ⊥ käyttäjänäkyvä). V211/V212/V214/V221/V235/V237 vahtivat kaikki samaa sääntöä jonka tyyppi voi pakottaa: 9 bugia samasta juuresta (B114,B115,B116,B117,B120,B121,B126,B127,B129) ∴ sääntö ⊥ pysy pystyssä dokumentaationa. (a) `SignMarker.distanceFromStart` → nimeä uudelleen niin että suora luku on eksplisiittinen valinta (esim. `distanceFromStartLegacy`); ainoa sallittu lukija `src/logic/marker-distance.ts` (+ V139-reitittömät & legacy-fallback). (b) MITTAKAAVA ⊥ ole 83 osumaa: `grep -c distanceFromStart src/` = 83 mutta VALTAOSA on `RoutePoint.distanceFromStart` (`bearing.ts`, `drive.ts`, `segment-panel.ts`, `segment-overlay.ts`, `progress-bar.ts`) = reitin oma piste OMALLA reitillään, YKSIKÄSITTEINEN ∴ ⊥ bugi & ⊥ muuteta. Todelliset `marker.`-lukupaikat ovat kourallinen — kääntäjä listaa ne tarkasti, ⊥ arvata. (c) Vanhentuneet invariantit merkitään korvatuiksi (⊥ poisteta, V86-kuvio). Testattavuus: `bun run test` vihreä + `tsc` puhdas = todiste; ⊥ uusia testejä (mekaaninen, kääntäjän todistama). Kerros: `src/logic/types.ts` + kutsujat. Käyttäjä: — (sisäinen laatu, estää 10. bugin). | V237,V212,V211,T328 |
| T331 | . | **[Keski]** Loki erottaa palautuksen alkuperäisestä (V239). Käyttäjävaatimus 2026-07-25: "lokiin sitten myös jos tekee jonkun muutoksen niin siellä sit lukis että palautettu". Nyt `audit.ts:131` kirjaa peruutuksen käänteisaktiolla (`move`→`move`) ∴ `/loki` näyttää "siirsi merkkiä" myös palautukselle — 2026-07-25 incidentin selvittelyssä lukija ⊥ erota kumpi rivi oli ihmisen siirto & kumpi korjaus. (a) `server/db.ts`: `marker_audit.undo_of_id TEXT` (migraatio-kuvio kuten `distance_by_route`, NULL = ei palautus; legacy-rivit jäävät NULLiksi ⊥ arvata takautuvasti, B124-oppi). (b) `server/routes/audit.ts` :131-144: peruutuksen kirjaama rivi saa `undo_of_id = auditId`; sama myös massaperuutuksessa (`:179`-haara) ∴ molemmat polut merkitsevät jälkensä. (c) `src/logic/audit-log.ts` `describeAuditEntry`: `undo_of_id != null` → verbi `UNDO_VERB`-taulusta ("palautti siirron" / "palautti poiston" / "palautti lisäyksen" / "palautti tilan") — ⊥ if-ketju, sama lookup-kuvio kuin `ACTION_VERB` (T320: yksi totuus, lokinäkymä & pätkämodaali lukevat samaa). (d) `src/loki.ts` + `segment-details-modal.ts`: rivi saa erottuvan merkinnän (esim. `↩`-etuliite tai `.audit-row--undo`-luokka) ⊥ vain eri verbi — silmäiltävyys on koko lokin pointti; DESIGN.md §K jos uusi luokka. (e) Peruutuksen peruutus (⊥ estetty nyt) → ketju `undo_of_id`:n kautta; ⊥ rakenneta silmukkasuojaa spekulatiivisesti, mutta ⊥ myöskään oleteta ettei sitä tapahdu. Testattavuus: Taso 4 Bun (`server/test-fixtures.ts`) — undo → uusi rivi kantaa `undo_of_id`n & alkuperäinen `undone_at`n; massaperuutus sama; legacy-rivi (NULL) renderöityy entiseen tapaan. Taso 1 Vitest-pure `describeAuditEntry`: `undo_of_id` → palautusverbi, ilman sitä entinen verbi. Taso 2 jsdom `/loki`-rivin merkintä. (f) NIMETÖN ADMIN (V240, käyttäjähavainto 2026-07-25 "adminina siinä lukee tuntematon"): `users.display_name` on NULL `admin`-käyttäjällä ∴ 12 sessiota & 4 audit-riviä ilman tekijää. `server/routes/auth.ts:44` → `user.display_name ?? user.username`; audit-kirjaus (`marker-audit.ts`) → `session.display_name ?? session.role` ∴ ketju on katkeamaton myös vanhoilla sessioilla joilla nimi puuttuu (12 kpl prodissa, 7 vrk TTL — ⊥ voi vaatia uudelleenkirjautumista kesken tapahtuman, V228-siirtymälinja); lisäksi kertakorjaus `users`-riville (`display_name='admin'`). Vanhoja NULL-rivejä ⊥ täytetä takautuvasti (B124-oppi) — `/loki` näyttää niille rehellisesti "tuntematon". Bun-testi: admin-login → sessio kantaa nimen → audit-rivi kantaa nimen. Kerros: `server/` + `src/logic/audit-log.ts` + 2 UI-kuluttajaa. Käyttäjä: järjestäjä (jälkiselvittely). | V239,V240,V230,V228,T321,T320 |

| T332 | ✓ | **[Pieni]** `/loki`-peruutuksen vahvistus selviää uudelleenlatauksesta (V241, fix B130). `src/ui/audit-log-page.ts`: `renderAuditLogPage` ottaa vastaan alkuviestin (esim. `opts.statusMessage`) & `onReload`-kutsuja välittää sen ∴ `loki.ts:36` `load()` saa parametrin joka renderöityy uuteen `.audit-status`-solmuun — viesti elää renderin ULKOPUOLELLA, ⊥ sen sisällä. ⊥ ratkaista `setTimeout`illa tai jättämällä reload tekemättä: lista ! päivittyä (rivi katoaa) & viesti ! näkyä, molemmat. Testattavuus: Taso 2 Vitest-jsdom (`renderAuditLogPage` + välitön onReload → `.audit-status` sisältää viestin) + olemassa oleva `e2e/t321-audit-log.spec.ts:51` menee läpi KOKO suitessa ⊥ vain yksin (se on regressio-ehto: B130 löytyi juuri tästä erosta). Kerros: `src/ui/` + `src/loki.ts`. Käyttäjä: järjestäjä (jälkiselvittely). | V241,V231,V21,B130 |

| T333 | ✓ | **[Pieni]** Fix B131 — "Näytä kartalla" ⊥ kutista heroa (V242). Käyttäjäpäätös 2026-07-25: "se voi jäädä sit vain auki, ei se haittaa" ∴ ratkaisu = POISTA sivuvaikutus, ⊥ lisää paluunappia. (a) `src/ui/segment-hero.ts:178`: poista `this.ctx.setCollapsed(true)` → jää `actions.onShowOnMap(current.id)` (panTo, `markers-wiring.ts:251`). (b) `src/ui/segment-view.ts:319` keräyslistan riviklikki: sama poisto, sama sääntö (⊥ jätä toista kutsupaikkaa elämään — B131 toistuisi keräystehtävällä). (c) `SegmentHeroCtx.setCollapsed` (`segment-hero.ts:29`) jää käyttämättömäksi ∴ poista ctx-tyypistä & `segment-view.ts:135`-välityksestä — kuollut koukku houkuttelee seuraavan taskin kytkemään sen takaisin. `SegmentView.setCollapsed` (`:248`) EI poistu: chevron & ulkoiset kutsujat käyttävät sitä. Kartta-moodissa paneeli on 38vh-yläkortti (`style.css:1470`) ∴ hero auki ⊥ peitä karttaa enempää kuin ennen painallusta. Testattavuus: Taso 2 Vitest-jsdom — "Näytä kartalla" -klikki → `onShowOnMap` kutsuttu & `.segment-view--collapsed` ⊥ ilmesty paneeliin; keräyslistan rivi sama; chevron-klikki kutistaa edelleen (regressio, ⊥ saa mennä rikki). Kerros: `src/ui/`. Käyttäjä: talkoolainen. | V242,B131 |

| T334 | ✓ | **[Pieni]** Fokus-valinta puhtaana logiikkana (V243) — UUSI `src/logic/marker-focus.ts`. `focusState(markers, focusSegment|undefined) → Map<markerId, 'focus'|'dim'>` TAI `isFocused(markerId)`-predikaatti; `focusSegment === undefined` ⇒ ∀ merkki `'focus'` (ei fokusta = ei himmennystä, sama kuin `contextSegmentStyle`in `contextOwnId===undefined`-haara). Jäsenyys tulee `getMarkersForSegment`/`resolveTaskMarkers`ista (V140) — ⊥ kirjoita omaa jäsenyyssääntöä, se on 9 kerran juurisyy (B114–B129). ⊥ Leafletia, ⊥ DOM:ia. Kerros: `src/logic/`. Testattavuus: Taso 1 Vitest-pure — (i) undefined → kaikki fokuksessa; (ii) reitillinen pätkä → vain jäsenet fokuksessa; (iii) reititön `markerTypeFilter`-tehtävä (V139/V143) → tyyppiosumat fokuksessa; (iv) tyhjä pätkä → ⊥ heitä, kaikki dim. Käyttäjä: molemmat (primitiivi). | V243,V140,V139 |

| T335 | ✓ | **[Keski]** MarkerFocus kartalle — talkoolaisen automaatti + järjestäjän kytkin (V243, DESIGN.md §K MarkerFocus). Vaatii T334. (a) `src/map/markers.ts`: `setFocusSegment(seg|undefined)` + sovellus `getElement()?.classList.toggle('marker-dimmed', …)` — ⊥ `createSignIcon`-uudelleenluontia (nollaisi `.marker-next-highlight`in `:472` & vilkuttaisi koko kartan); sama kuvio kuin `setNextHighlight` `:111-121` ∴ kopioi se, ⊥ keksi toista. Uusien/reconciloitujen merkkien ! saada luokka renderissä (`:472`-haara), muuten fokus katoaa jokaisesta päivityksestä. (b) `src/style.css` `.marker-dimmed { opacity:.4; filter:grayscale(1); transition:opacity .12s,filter .12s }` + talkoolais-haara `pointer-events:none` (V142 read-only; järjestäjällä himmennetty PYSYY klikattavana — korostus on lukemisen apu ⊥ lukko) ∴ luokkaparia tarvitaan 2 (esim. `.marker-dimmed` + `.marker-dimmed--locked`), ⊥ roolia CSS:ssä arvattuna. (c) Talkoolais-kytkentä: `src/app/segments-wiring.ts:67` asettaa jo `segmentOverlay.setContextOwn(own?.id)` — LISÄÄ viereen `markerManager.setFocusSegment(own)`, sama lähde molemmille kerroksille (⊥ toista omistajapäättelyä). (d) Järjestäjän kytkin `src/ui/segment-details-modal.ts`: `.btn.btn--ghost` + `aria-pressed`, oletus POIS, teksti `◎ Korosta vain tämä pätkä` ↔ `◉ Korostus päällä`. (e) Poistumis-pilleri (V243, PAKOLLINEN): `.map-mode-pill`-kuvio (`map-mode-toggle.ts`, T308/V219) — `Korostus: <pätkän nimi> ✕`, näkyy niin kauan kuin tila on päällä myös modaalin sulkeuduttua; ✕ nollaa. Tila elää wiringissä (⊥ modaalin sisällä — modaali tuhoutuu sulkiessa). ⊥ persistoi localStorageen: korostus on hetken työkalu, ⊥ asetus. Testattavuus: Taso 2 Vitest-jsdom (kytkin togglaa `aria-pressed`n & kutsuu callbackin; pilleri ilmestyy/katoaa; ✕ nollaa) + Taso 3 Playwright `e2e/critical-paths.spec.ts` (COMPONENTS.md:75 MarkerManager-sarake ei-tyhjä ∴ karttapolku ! olla katettu): talkoolaisnäkymä → oman pätkän merkki ⊥ ole `.marker-dimmed`, vieraan pätkän merkki ON; regressio: merkin status-päivitys ⊥ pudota luokkaa & `.marker-next-highlight` säilyy. Kerros: `src/map/` + `src/ui/` + `src/app/`-wiring. Käyttäjä: talkoolainen (automaattinen), järjestäjä (kytkin). | V243,V142,V178,T334 |

| T336 | ✓ | **[Keski]** SegmentCasing — pätkä & reitti luettavissa yhtä aikaa (V244, DESIGN.md §K SegmentCasing). **AJETAAN T304:N JÄLKEEN** — ⊥ aloita ennen kuin paletti-päätös on tehty, muuten värit kalibroidaan kahdesti. `src/map/segment-overlay.ts:87-105`: yksi `L.polyline` → **KOLME** samalle `pts`-geometrialle (V244-amend/B137): casing (pätkäväri `segmentLineColor(id,state)` T348, `weight 15`, `LINE_STATE_STYLE`in `opacity`+`dashArray`) & valkoinen erotin (`#FFFFFF`, `weight 11`, ehjä) & sisus (reitin väri `ROUTE_DEFS`istä `segmentPrimaryRouteId(seg)`-akselilla, `weight 9`, ehjä, täysi opacity). Erotin ⊥ ole koriste: ilman sitä reunus & sisus ovat vierekkäin & niiden kontrastiehto on ylimääritelty (B137, mitattu huonoin pari 2.19). Piirtojärjestys ! olla casing ENSIN (Leaflet: myöhempi päälle) & molemmat samaan `this.layers`-listaan ∴ `clear()` poistaa parin, ⊥ jätä orpoa viivaa (nykyinen puhdistus `:45`). Klikkikäsittelijä (`onSegmentClick`) VAIN toiseen — kaksi layeria = kaksi eventtiä samasta klikistä. Reititön pätkä (V139): ⊥ sisusviivaa (ei reittiä) → yksi viiva kuten nyt, ⊥ kaadu. Talkoolaisen ei-oma pätkä: ⊥ casingia (`contextSegmentStyle` -haara ennallaan) — casing on korostuksen kieli, taustalle sitä ⊥ anneta. Gap-viiva (`GAP_COLOR :76`) ennallaan. Testattavuus: Taso 1 Vitest-pure (tyylifunktio: oma+reitillinen → 2 tyyliä joiden weight 15/9 & sisusväri = primary-reitin väri; reititön → 1; ei-oma → 1 himmeä) + Taso 3 Playwright `e2e/segments.spec.ts` olemassa oleva "viivatyyli koodaa statuksen" ! pysyä vihreänä (statuskieli siirtyy casingiin ∴ valitsin voi muuttua — päivitä testi, ⊥ löysennä odotusarvoa). Kerros: `src/map/` (+ `src/logic/segments.ts` jos tyylifunktio erotetaan pureksi, suositus: erota). Käyttäjä: järjestäjä (tilannekuva), talkoolainen (oma pätkä erottuu). **TOTEUTUNUT 2026-07-27:** tyylit puhtaaseen `src/logic/segment-style.ts`:ään (`segmentLayerStyles` palauttaa kerrokset piirtojärjestyksessä) ∴ Leaflet vain soveltaa & kerrosgeometria on Vitest-pure-testattavissa ilman karttaa. `RouteRef` sai valinnaisen `color`-kentän (puuttuva väri → yksi viiva, ⊥ kaadu). Klikki & nimilappu VAIN casingissa. 8 Vitest-pure-testiä ml. B137-regressiodokumentti ("jos joku poistaa erottimen turhana, tämä kertoo miksi se oli siellä"). **T348 (2026-07-26) EDELSI tätä tätä ∴ casing PERII sen kaksi päätöstä, ⊥ palauta vanhoja arvoja:** casingin väri = `segmentLineColor(seg.id, state)` (⊥ `colorForSegment` — valmis-pätkän casing on `--confirm`-vihreä reuna reitin värin ympärillä, sisus ehjä) & `LINE_STATE_STYLE`n dashArray on `'10 8'`-luokkaa (V252), ⊥ `'1 9'`. Nimilapun `--done`-tila (✓ + vihreä reunus) on riippumaton casingista & säilyy sellaisenaan. | V244,V216,V96,V252,V139,T304,T348 |

| T337 | ✓ | **[Pieni]** Fix B132 — valokuva auki täysikokoisena (V246). Erota nykyinen lightbox jaetuksi: UUSI `src/ui/image-lightbox.ts` (`openImageLightbox(src, caption?)`) — nosta se `marker-visual-row.ts:126-160`:n rakenteesta (`createBackdrop` `modal-helpers.ts` + `.marker-visual-lightbox*`-luokat) ⊥ kirjoita toista backdrop-käytöstä; `marker-visual-row` KÄYTTÄÄ uutta moduulia ∴ sulkeminen (Esc, backdrop-klikki, ✕) on yksi toteutus kaikille kuville. `marker-detail-modal.ts:187-199`: thumb saa `click` → `openImageLightbox(url)`, `cursor:pointer`, `role="button"` + `tabindex=0` + Enter/Space (näppäimistö), `aria-label="Avaa kuva"`; hit-area ≥44px (§R — 72px thumb riittää, mutta wrapper ⊥ saa kutistua alle). Lightboxissa `object-fit:contain` + `max-width/height:90vw/90vh` ∴ kuva ⊥ rajaudu (juuri se mikä thumbissa hukkuu). Thumb-gridin `object-fit:cover` säilyy (rajaus on OK kahvassa, ⊥ esityksessä). Testattavuus: Taso 2 Vitest-jsdom — thumb-klikki avaa lightboxin oikealla `src`illä; Esc & backdrop-klikki sulkee; `marker-visual-row`in kylttivisuaali-lightbox toimii ennallaan (regressio, jaettu moduuli ⊥ saa muuttaa sen käytöstä). Kerros: `src/ui/`. Käyttäjä: molemmat. | V246,B132,V21 |

| T338 | ✓ | **[Keski]** Huomio kantaa kuvan (V246, käyttäjäpyyntö 2026-07-25: "siinä olisi huomio ja voisi liittää kuvan"). Vaatii T337 (lightbox) & T237 (sijoitus). (a) `server/db.ts`: `comment_images`-taulu idempotentisti (`id TEXT PK, comment_id TEXT NOT NULL, mime TEXT, data BLOB, created_at`) — sama kuvio kuin merkkikuvilla (`markers.ts:41 imageUrls`, ⊥ keksi toista tallennusmallia). (b) `server/routes/comments.ts`: `POST /api/comments/:id/images` (multipart, field `image`) + `GET /api/comments/:id/images/:imageId`. **Oikeus: ∀ autentikoitu ml. talkoolainen (V13/V246)** — TÄSMÄLLINEN ERO merkkikuviin (`markers.ts:366` järjestäjä+): huomion arvo on juuri siinä että kentällä oleva liittää todisteen. DELETE-cascade: kommentin poisto (`DELETE /:id`, järjestäjä+) poistaa myös kuvat ⊥ jätä orpoja BLOBeja. (c) `src/logic/comments.ts`: `Comment.images?: string[]` (sama muoto kuin `SignMarker.images`, `types.ts:35`) + `addCommentImage(id, file)`. (d) UI: kommenttilomake (`comment-thread.ts` + T237:n kartta-mini-lomake) saa `📷 Lisää kuva` -napin `<input type=file accept="image/*" capture="environment">` (mobiilikamera suoraan); `.comment-thread-item` renderöi thumbit → `openImageLightbox` (T337). Kuvaton huomio ⊥ näytä tyhjää galleriaa. (e) Kartan `CommentPin` ⊥ muutu kuvan takia (kupla pysyy 32px, DESIGN.md §K) — kuva näkyy vasta avatessa; kartta ⊥ saa täyttyä thumbeista. Testattavuus: Taso 4 Bun (`server/test-fixtures.ts`): talkoo-header POST kuva → 200 (V13, tämä on koko taskin ydinehto), GET palauttaa bytet, kommentin DELETE poistaa kuvarivit, väärä mime → 400. Taso 2 Vitest-jsdom: lomake lähettää tiedoston & rivi renderöi thumbin joka avaa lightboxin. Kerros: `server/` + `src/logic/` + `src/ui/`. Käyttäjä: talkoolainen (liittää), järjestäjä (katsoo). | V246,V13,T337,T237,T221 |

| T340 | ✓ | **[Keski]** Järjestäjän sivupalkkiin "Huomiot" -osio (käyttäjäpyyntö 2026-07-25: "järjestäjä sivupalkissa olisi huomiot-kohta josta voisi selata niitä"). Vaatii T237 (huomioita ⊥ ole ennen sijoitusta). (a) `index.html`: uusi `.left-panel-section` + `<div id="comment-panel-container">` `#area-panel-container`in jälkeen, ENNEN inventaario-linkkiä (linkki on aina viimeisenä, se vie pois sivulta). (b) UUSI `src/ui/comment-panel.ts` — DOM ilman Leafletia (kerrosraja kuten `comment-thread.ts`, Vitest-jsdom): otsikko `Huomiot (N)`, lista `fetchComments('point')`-tuloksesta (`src/logic/comments.ts:47` on jo olemassa), rivi = `[ikoni] [tekstin 1. rivi, ellipsis] [nimi · pvm]` + `📷` -indikaattori jos kuvia (T338). Tyhjä tila: "Ei huomioita." (c) Rivin klikkaus → `onFocus(comment)` → wiring panoroi kartan (`map.setView(lat,lon)`) & korostaa pinnin — ⊥ avaa modaalia (sama kuvio kuin talkoolaisen keräyslista `segment-view.ts:317`: näytä kartalla, ⊥ hautaa modaaliin). (d) Poisto rivin ⋯/🗑 → `DELETE /api/comments/:id` (järjestäjä+, backend valmis T221) + vahvistus (`confirm`-kuvio kuten muut destruktiiviset), lista päivittyy. (e) Sivupalkki on **240px** (DESIGN.md §K LeftPanel) ∴ rivi ⊥ saa vuotaa vaakasuunnassa — teksti `text-overflow:ellipsis`, ⊥ vaakascrollia (B104-oppi: header-vuoto 375px:llä). DESIGN.md §K **CommentPanel** kirjoitettu 2026-07-25 — noudata sitä. (f) Näkyy VAIN järjestäjälle (`#left-panel` on jo roolisuojattu CSS:llä, ⊥ lisää toista porttia). Testattavuus: Taso 2 Vitest-jsdom — lista renderöi N riviä + tyhjä tila; klikkaus kutsuu `onFocus`in oikealla lat/lonilla; poisto kutsuu API:a & poistaa rivin; pitkä teksti ⊥ kasvata leveyttä (ellipsis-luokka läsnä). Kerros: `src/ui/` + `index.html` + wiring. Käyttäjä: järjestäjä. | V245,V13,T237,T221 |
| T341 | ✓ | **[Kriittinen, HOTFIX tuotantoon]** Fix B133 (V247) — GPS takaisin talkoolaiselle. Minimaalinen & kohdennettu, ⊥ refaktorointia (menee tuotantoon heti, tapahtuma on PÄÄLLÄ). (a) `src/app/markers-wiring.ts`: siirrä `#btn-tk-gps`-lohko (rivit 328–334) `if (seg)`:n ULKOPUOLELLE `if (talkoolainenCode)`-tasolle — kuuntelija & `syncGpsLabel` kytkeytyvät myös ilman pätkää. `#btn-tk-add-marker`/`#btn-tk-complete` JÄÄVÄT `if (seg)`:n sisään (ne TARVITSEVAT pätkän). (b) `src/map/gps-navigator.ts`: `start(onState?: (s: 'haetaan'|'päällä'|'pois', msg?: string) => void)` — error-callback mäppää `err.code` → suomenkielinen syy (1 PERMISSION_DENIED "Sijaintilupa evätty — salli selaimen asetuksista", 2 POSITION_UNAVAILABLE "Sijaintia ei saada — tarkista että paikannus on päällä", 3 TIMEOUT "Sijainnin haku kesti liian kauan"), kutsuu `onState('pois', msg)` & `stop()`. `!navigator.geolocation` → 'pois' + "GPS vaatii HTTPS-yhteyden". TIMEOUT-erikoiskäsittely: yksi automaattinen uudelleenyritys `{enableHighAccuracy:false, timeout:30000, maximumAge:0}` ennen luovuttamista (metsä + puulatvat: korkea tarkkuus ⊥ saa lukita talkoolaista ulos). Ensimmäinen fix → `onState('päällä')`. (c) wiring & napin label: 'haetaan' → "📍 Haetaan…" (⊥ disabloi nappia), 'päällä' → "📍 GPS päällä", 'pois' → "📍 GPS" + `showWarning(msg, 5000)`. Sama tila molempiin sisääntuloihin (hero `renderGpsBtn` `segment-view.ts:194` + ⋯ `#btn-tk-gps`) — ⊥ kaksi totuutta. Testattavuus: Taso 2 Vitest-jsdom, `navigator.geolocation` stubattuna (`vi.stubGlobal`): (1) talkoolainen ILMAN pätkää → klikkaus `#btn-tk-gps` kutsuu `watchPosition` (B133-regressio, taskin ydinehto), (2) error code 1 → näkyvä varoitus + label palaa "📍 GPS", (3) code 3 → toinen kutsu `enableHighAccuracy:false`, (4) label "📍 Haetaan…" ennen fixiä, "📍 GPS päällä" fixin jälkeen. Kerros: `src/map/` + `src/app/`. Käyttäjä: talkoolainen (kenttä). | V247,V156,V179,T30,T257 |
| T342 | ✓ | **[Suuri]** Vitest-suite 2m30s → tavoite <45s (V248). **TULOS: 150s → 28.3s, 1599 testiä (28 todo) vihreänä, 6/6 ajoa vakaa.** Toteutus: (1) `environment: 'node'` default + `// @vitest-environment jsdom` -docblock 91 tiedostoon (grep-luokittelu paljasti 4 EPÄSUORAA DOM-riippuvuutta joita sanahaku ⊥ nähnyt: `gpx.test.ts` ym. `DOMParser`in kautta `src/logic/gpx.ts:2` — env-virhe failasi äänekkäästi kuten V248 vaatii). (2) `isolate: false` + `unstubGlobals: true` + UUSI jaettu `tests/helpers/leaflet-mock.ts` (kuusi kilpailevaa `vi.mock('leaflet')`-tehdasta → yksi; `installLeafletMock()` beforeEachissä koska muiden tiedostojen `vi.resetAllMocks()` pyyhkii jaetut implit) + t309:n `src/map/icons`-tynkä poistettu (se söi neljän muun tiedoston oikean `createSignIcon`in). (3) RAJA JOKA LÖYTYI: `vi.mock` & jaettu moduulirekisteri ovat RAKENTEELLISESTI ristiriidassa — mockattu moduuli sitoutuu siihen tilaan jossa ensimmäinen importtaaja sen jätti ∴ vuotoa ⊥ voi "siivota pois". 9 tiedostoa omaan `isolated`-projektiin (`vite.config.ts` `ISOLATED`, syy per tiedosto kommentoitu, maksaa 3s): 6× `vi.mock` + 3× moduulitason tila/history/sessionStorage (t51, t254, t296). Lista on näkyvä ∴ sen kasvu on signaali. HYLÄTYT vaihtoehdot mitattuna: `pool=threads` 114s (ei vaikutusta — eristys pakottaa tuoreen jsdomin), happy-dom 68s mutta 17 testiä hajoaa yhteensopivuuteen. | V248 | Deploy-putken suurin yksittäinen pullonkaula & mitattu: `bun run test:ci` 150s, `bun run build` 5.2s, `bun run test:server` 4.5s ∴ 94% paikallisesta ajasta on vitest. KAKSI erillistä juurisyytä, molemmat korjattava (kumpikaan yksin ⊥ riitä). **(1) Globaali jsdom.** `vite.config.ts:41` `environment: 'jsdom'` koskee kaikkia 144 tiedostoa, mutta grep-luokittelu: 87 koskee DOM:ia (`document.`\|`innerHTML`\|`HTMLElement`\|`createElement`), 8 tarvitsee vain localStorage/window-stubin, 49 on täysin puhdasta logiikkaa. Mitattu otos 20 puhdasta tiedostoa: jsdom 18.5s → `--environment node` 3.7s = **5×**. Vitest 4 ⊥ tue `environmentMatchGlobs` (poistettu v3:ssa) ∴ vaihtoehdot: per-tiedosto-docblock `// @vitest-environment jsdom` (koskettaa 87 tiedostoa, robusti) | `test.projects` (kaksi projektia node/jsdom, vähemmän churnia mutta `tests/` on LITTEÄ 144 tiedoston hakemisto & tiedostonimi ⊥ kerro kerrosta ∴ glob-jako on hauras). Rakentaja päättää; ehto: luokittelu johdetaan grepillä ⊥ arvaamalla, & väärä env FAILAA äänekkäästi (`ReferenceError: document`) ⊥ hiljaa. **(2) Per-tiedosto-eristys maksaa enemmän kuin jsdom itse.** Otos 20 DOM-tiedostoa: eristettynä 27.8s → `--no-isolate` 8.6s = **3.2×**, samat 213 testiä läpi. Koko suite `--no-isolate`: **23.3s vs 150s = 6.4×** — MUTTA 51 testiä hajoaa tiedostojen väliseen tilavuotoon 5 tiedostossa: `tests/sign-icon-status.test.ts`, `tests/t140-status-ring-color.test.ts`, `tests/t158-sign-visual.test.ts`, `tests/t254-talkoolainen-mode.test.ts`, `tests/t309-marker-drag-rollback.test.ts` (t309 kaatuu jo `new MarkerManager`iin, `src/map/markers.ts:72`). Vuoto on TESTIN vika ⊥ tuotantokoodin — moduulitason tila/DOM jää siivoamatta ∴ korjaa vuodot (`beforeEach`-siivous, `vi.resetModules`, `document.body` tyhjennys) ÄLÄ löysennä odotusarvoja. Jos jokin vuoto ⊥ ratkea järkevästi → se tiedosto omaan eristettyyn projektiin, MUTTA lista kirjataan §T-riville näkyviin ettei "väliaikainen" poikkeus kasva hiljaa. Testattavuus: mittari ON hyväksymiskriteeri — `bun run test:ci` ajaa saman **1599 testiä (28 todo)** vihreänä, wall-clock <45s. ⊥ yhtään testiä poisteta, skipata | löysennetä nopeuden vuoksi (V248). Kerros: tooling (`vite.config.ts` + `tests/`), ⊥ `src/`-muutoksia. Käyttäjä: kehittäjä (deploy-putki). | V248 |
| T343 | ✓ | **[Keski]** backup.sh pois deployn kriittiseltä polulta (V249). `deploy.sh:23` ajaa `./backup.sh`n synkronisesti ENNEN `fly deploy`ta. backup.sh tekee KAKSI asiaa joilla on eri hinta & eri turvallisuusarvo: **(a)** `fly volumes snapshots create` (`backup.sh:26`) — nopea, ⊥ vaadi käynnissä olevaa konetta, & on varsinainen pre-deploy-turvaverkko; **(b)** off-site-kopio (`backup.sh:37-52`) — herättää nukkuvan koneen (`fly.toml` `min_machines_running = 0`) health-curlilla, sitten ≤5× `fly ssh console` retry (`sleep 3` välissä) + `VACUUM INTO` + `fly ssh sftp get` + `rm` = **neljä ssh-roundtrippiä kylmään koneeseen** jokaisen deployn edessä. Muutos: `deploy.sh` ajaa VAIN (a):n synkronisesti & käynnistää (b):n vasta deployn JÄLKEEN — kone on silloin joka tapauksessa hereillä deployn takia ∴ herätysviive katoaa kokonaan ⊥ vain siirry. `backup.sh` saa lipun (`--snapshot-only` / `--offsite-only`); käsin ajettu `./backup.sh` ilman lippua tekee edelleen molemmat ⊥ kahta rinnakkaista backup-skriptiä. EHTO joka ⊥ saa rikkoutua: volume-snapshot on otettu ENNEN kuin uusi koodi käynnistyy (migraatio/bugi voi korruptoida DB:n) ∴ (a) pysyy BLOKKAAVANA & sen epäonnistuminen varoittaa yhtä äänekkäästi kuin nyt. Off-site-kopion epäonnistuminen deployn jälkeen ! NÄKYÄ — ⊥ hiljainen `&`-tausta jonka output häviää; kirjaa tulos & tulosta se deployn lopuksi. `SKIP_BACKUP=1`-pakotus säilyy. Testattavuus: Taso 4 / manuaalinen — `shellcheck` puhdas + kuiva-ajo: `--snapshot-only` ⊥ kutsu `fly ssh`ää kertaakaan, `--offsite-only` ⊥ luo snapshottia, & ilman lippua = nykyinen käytös. Kerros: `deploy.sh` + `backup.sh` (tooling, ⊥ `src/`). Käyttäjä: kehittäjä (deploy-putki). | V249 |
| T344 | ✓ | **[Pieni]** Järjestäjän sivupalkin & pätkämodaalin pikavoitot (V250, UX-audit 2026-07-26). (a) `src/ui/segment-panel.ts:359` `buildRow`: `.segment-info` `span` → `button` joka avaa `detailsModal.open(seg)` — `flex:1`, `min-height:44px`, `text-align:left`, `aria-label` `Avaa <nimi> lisätiedot`; `···` säilyy ennallaan (T345 muuttaa sen, ⊥ tässä). Rivin oma `li` EI saa klikkikuuntelijaa (kaksi sisäkkäistä kohdetta = tuplalaukaisu). (b) `src/ui/segment-details-modal.ts`: nimi (`buildNameSection`) & kuvaus (`buildDescSection`) tallentuvat `blur`illa samalla kutsuparilla kuin varusteet (`updateSegment` + `updateSegmentRemote`, ⊥ uutta kanavaa); `buildSaveFooter` → `Valmis` joka VAIN sulkee (`saveAll` poistuu ∴ `buildBody`n paluuarvosta myös). Blur-tallennus ! olla idempotentti: sama arvo ⊥ lähetä turhaa PUT:ia (rivi 205 tekee jo `updated`-vertailun — käytä sitä, ⊥ keksi toista). (c) `buildDangerZone` `modal.appendChild`-järjestyksessä ENNEN footeria (`:82`↔`:83`). Testattavuus: Taso 2 Vitest-jsdom (`tests/t69-segment-details-modal.test.ts` päivitys: nimi tallentuu blurilla ilman footeria; footer ⊥ enää tallenna; poisto-osio on footeria ennen DOM-järjestyksessä) + `tests/t344-segment-row-name.test.ts` (nimi-button avaa modaalin, `···` ei ole ainoa sisääntulo). Kerros: `src/ui/`. Käyttäjä: järjestäjä. | V250,V62 |
| T345 | ✓ | **[Keski]** Pätkärivin `···`-pikavalikko (V250). Vaatii T344. UUSI `src/ui/segment-row-menu.ts` — `openSegmentRowMenu(anchor, seg, actions)`; ei omaa tilaa, palauttaa `close()`. Rivit: `🔍 Näytä kartalla` · `◎ Korosta vain tämä pätkä` (↔ `◉ Korostus päällä`, sama tila kuin T335-kytkin ∴ LUKEE `isFocusSegment`in, ⊥ omaa lippua) · `🔗 Kopioi talkoolaislinkki` (VAIN jos `assignedCode`) · `⚙ Lisätiedot & varusteet…`. Sulkeminen: valinta, Esc, ulkoklikki, scroll — sama `createBackdrop`-kuvio kuin `modal-helpers.ts`ssä, ⊥ omaa dokumenttikuuntelijaa. **Kaksi duplikaattia poistuu samalla:** (i) `fitMapToSegment` (`src/app/markers-wiring.ts:95`) → UUSI `src/map/segment-fit.ts` exportiksi, molemmat wiringit käyttävät sitä (⊥ toista zoom-sääntöä; `planSegmentZoom` V185 pysyy logiikan lähteenä); (ii) jaettu linkkiosoite → `segmentShareUrl(seg)` `src/logic/segments.ts`:ään (`/s/<assignedCode>`), `segment-details-modal.ts:437` käyttää samaa (nyt merkkijono kahdessa paikassa). Kytkentä: `SegmentPanelCallbacks` saa `onShowOnMap` + käyttää olemassa olevia `isFocusSegment`/`onToggleFocusSegment`ia (T335); `segments-wiring.ts` täyttää ne. Reititön pätkä (V139): `Näytä kartalla` fallback merkkien boundeihin, ⊥ disabloi riviä hiljaa. DESIGN.md §K: uusi sopimus `SegmentRowMenu` (44px rivit, `surface-card`, ankkurointi napin alle, ei ikoni ilman tekstiä V197). Testattavuus: Taso 2 Vitest-jsdom (rivit renderöityvät oikein assignedCoden kanssa/ilman; Esc & ulkoklikki sulkee; korostusrivi heijastaa T335-tilaa) + Taso 3 Playwright `e2e/critical-paths.spec.ts` (`···` → `Näytä kartalla` siirtää karttaa; `Korosta vain tämä` himmentää muut ILMAN modaalia). Kerros: `src/ui/` + `src/map/segment-fit.ts` + `src/logic/segments.ts`. Käyttäjä: järjestäjä. | V250,V243,V185,V139,T344,T335 |
| T346 | ✓ | **[Keski]** `SegmentDetailsModal`in osioryhmittely (V250). Vaatii T344. 8 irrallista lohkoa → 5 ryhmää väliotsikoin: **Tiedot** (nimi, kuvaus) · **Sisältö** (merkit + varusteet) · **Jako** (assign + linkki + aktiviteettiloki) · **Kartta** (rajojen muokkaus, korostus) · **Vaiheet** (klooni) — ja vaaravyöhyke omanaan footerin edellä (T344 c). Väliotsikko: sama `.segment-details-section-title`-kuvio kuin nykyisin (11px uppercase `--text-muted`), ⊥ uutta luokkaperhettä. Ryhmittely on DOM-rakennemuutos ⊥ toiminnallisuusmuutos: jokainen olemassa oleva `build*Section` säilyy sellaisenaan & vain sijoittuu ryhmän sisään ∴ olemassa olevat testit (`t69`, `t199`, `t335`) ! pysyä vihreinä ilman valitsinmuutoksia — jos valitsin hajoaa, ryhmittely meni liian syvälle. TÄMÄ PÄÄTTÄÄ lopullisen osiojaon ennen moduulin pilkkomista (713r, ⚠️ COMPONENTS.md §Pilkkohälytykset) ∴ pilkko-§T kirjataan vasta tämän jälkeen — ryhmä = tuleva moduuliraja. Testattavuus: Taso 2 Vitest-jsdom (viisi väliotsikkoa oikeassa järjestyksessä; jokainen osio löytyy oikean otsikon alta). Kerros: `src/ui/`. Käyttäjä: järjestäjä. | V250 |
| T347 | ✓ | **[Pieni]** Pätkän nimilappu KARTALLA klikattavaksi sisääntuloksi — V250:n "Item-label: klikkaus = toiminto" laajenee sivupalkista karttapinnalle (T344:n sisar). Nykytila: pätkäVIIVA on jo klikattava (`src/map/segment-overlay.ts:116-122` → `segments-wiring.ts:144` `openDetailsModal`) mutta `bindTooltip` (`:110-114`) ⊥ saa `interactive`-optiota ∴ Leaflet-tooltip on `pointer-events:none` & klikkaus nimen päältä LÄPÄISEE kartalle: käyttäjä osoittaa nimeä (ainoa luettava kohde), ⊥ tapahdu mitään, & osuma vaatii ~8px viivan tarkkaa klikkausta lapun VIERESTÄ. (a) `bindTooltip`-optioihin `interactive: style.interactive` — SAMA lippu joka jo ohjaa polylinen `interactive`ia (`:107`) & click-kytkentää (`:116`) ∴ ⊥ uutta ehtolauseketta, ⊥ toista totuutta siitä kuka saa klikata. Leaflet välittää interaktiivisen tooltipin klikin lähdelayerille (`_source.fire`) ∴ olemassa oleva `line.on('click')` avaa modaalin — tooltipille ⊥ omaa kuuntelijaa (kaksi kuuntelijaa = tuplalaukaisu). (b) V142-suoja pitää kahdella lukolla: `contextSegmentStyle` (`:40`) antaa `interactive:false` himmennetylle kontekstipätkälle & `.segment-label--dim` on jo `pointer-events:none` (`style.css:3449`) ∴ talkoolaisen näkymässä VAIN oma pätkä on klikattava. (c) `style.css:3430`: `cursor:pointer` VAIN `.segment-label.leaflet-interactive`ille ⊥ luokkaan yleisesti — himmeä lappu ⊥ saa valehdella klikattavuudesta; `padding:2px 6px` → `6px 8px` (~17px → ~26px korkea). 44px-sääntöä (§A) EI täytetä & se on PERUSTELTU POIKKEUS joka ! kirjata ⊥ ohittaa hiljaa: 44px-lappu peittäisi naapuripätkän reitin tiheässä ruudukossa, tämän sisääntulon käyttäjä on järjestäjä (desktop+hiiri), & talkoolaisen mobiilipolku ⊥ riipu tästä (hänen kontekstilappunsa ovat ei-klikattavia & oma pätkä avautuu herosta/sivupalkista). ∴ DESIGN.md §K saa UUDEN sopimuksen **SegmentLabel**: tausta `rgba(15,23,42,0.85)`, teksti `#fff` AINA (B106 — tausta on kiinteä navy ⊥ teemamuuttuja), `11px/700`, `padding:6px 8px`, `cursor:pointer` vain interaktiivisena, `--dim` = `opacity .4` + `pointer-events:none`, + 44px-poikkeus perusteluineen. (d) Interaktiivinen tooltip ⊥ saa syödä kartan elettä: pan/drag joka ALKAA lapun päältä ! panoroida karttaa & klikkaus ⊥ saa avata modaalia kahdesti — varmistetaan testillä ⊥ oletuksella. Testattavuus: Taso 2 Vitest-jsdom, jaettu `tests/helpers/leaflet-mock.ts` (⊥ omaa `vi.mock('leaflet')`-tehdasta — T342/V248, ⊥ kasvata `ISOLATED`-listaa): (1) järjestäjän pätkä → `bindTooltip` saa `interactive:true`; (2) `setContextOwn('X')` → vieraan pätkän tooltip `interactive:false`, oman `true`; (3) click-kuuntelijoita per line pysyy 1. Taso 3 Playwright `e2e/critical-paths.spec.ts` UUSI "Pätkän nimi kartalla avaa modaalin": klikkaus `.segment-label`-tekstin päältä avaa modaalin KERRAN; drag lapun päältä siirtää kartan keskipistettä & modaali ⊥ avaudu. Kerros: `src/map/segment-overlay.ts` + `src/style.css` + `DESIGN.md` §K. Käyttäjä: järjestäjä. **TOTEUTUNUT:** tooltip-optiot puhtaaksi funktioksi `segmentLabelOptions(interactive)` ∴ testi ei tarvitse `vi.mock('leaflet')`ia & `ISOLATED`-lista ⊥ kasvanut (V248). E2E paljasti että EI VAIN lappu vaan koko klikkipolku oli kuollut latauksen jälkeen → **B134/V251** (`renderSegmentOverlay()` ajettiin ennen `setOnSegmentClick`ia) korjattu samalla siirtämällä ensimmäinen render `segments-wiring.ts`:n loppuun. Testit: 4 Vitest-pure + 3 Playwright (lapun klikkaus avaa modaalin kerran; drag lapun päältä panoroi & ⊥ avaa modaalia; talkoolaisen vieras lappu ⊥ `leaflet-interactive`). Suite 1632 vihreää, E2E 143/143. | V250,V251,V142,V62,T344 |
| T348 | ✓ | **[Keski]** Pätkän valmis-status kartalle NÄKYVÄKSI — väri + nimilappu (V96-amend, B135). PM-päätös 2026-07-26: hybridi-malli. Hylätyt vaihtoehdot kirjattu ⊥ toistettavaksi: "kaikki statusvärillä" kumoaisi V96:n tunniste-kanavan & sulattaisi vierekkäiset kesken-pätkät yhdeksi oranssiksi viivaksi; "vain lappu + katko-korjaus" jättäisi valmiin pätkän VIIVAN yhä pääteltäväksi katkon puuttumisesta (V252). **(a) Väri-valinta pureksi** `src/logic/segments.ts`: uusi `segmentLineColor(id: string, state: SegmentLineState): string` `colorForSegment`in RINNALLE (⊥ tilalle) — `state==='valmis'` → `SEGMENT_DONE_COLOR`, muuten `colorForSegment(id)`. `colorForSegment` säilyy ennallaan: sivupalkki/lista käyttää sitä tunnisteena ilman status-kontekstia ∴ pätkän väri listassa ⊥ vaihdu valmistumisesta (rivi & kartta ⊥ saa olla eri mieltä identiteetistä). `SEGMENT_DONE_COLOR` = §C-tokenin `--confirm` peili (`#1F8A50` vaalea / `#2FA35B` Kaamos) — Leaflet-polyline ⊥ lue CSS-muuttujaa ∴ JS-vakio on pakko, mutta se ! olla YKSI paikka & kommentoitu tokeniin sidotuksi (V132-sääntö: ⊥ hajota hexiä funktioihin). Törmäystarkistus tehty: `--confirm` ∉ ROUTE_DEFS (`#4C97D6 #2F6FB0 #1E5A8F #E9A13B #E2662A #C4384A`); lähin naapuri `SEGMENT_COLORS[2]='#0E9594'` (turkoosi) & ero riittää koska valmis kantaa TOISEN kanavan (ehjä w11 vs katko). **(b) Overlay** `src/map/segment-overlay.ts`: (b1) `:101` `colorForSegment(seg.id)` → `segmentLineColor(seg.id, state)` — `state` lasketaan jo samassa silmukassa (`:102`). (b2) `:107` `labelSuffix`in `seg.phase==='tarkastus'`-rajaus POISTUU: ✓ näkyy JOKAISEN valmis-pätkän lapussa phasesta riippumatta & siirtyy prefixiksi (`✓ Pätkä 3` — nimi voi katketa, merkki ⊥ saa). (b3) `segmentLabelOptions(interactive)` (`:53`) saa state-tiedon & kokoaa `className`iin `segment-label--done` — luokkajonoa ⊥ rakenneta kutsupaikalla (yksi kokoamispaikka, T347:n opetus). Yhdistelmä `--dim` + `--done` on LAILLINEN (talkoolaisen konteksti-lappu voi olla valmis) ∴ luokat ⊥ saa olla toisensa poissulkevia. **(c) `LINE_STATE_STYLE` luettavaksi** (`:12-16`, V252): `kesken` `dashArray '1 9'` → `'10 8'` (näkyvä viivanpätkä, ⊥ pistesarja); `ei_alkanut` sama rytmi harvempana/haaleampana (`opacity .4, weight 9`) ∴ kaksi katkotilaa ! erottua yhä TOISISTAAN ⊥ vain leveydellä; `valmis` ennallaan (ehjä, `opacity .9, weight 11`). Kolmiportaisuus ! toimia akromaattisena. Valkoinen casing-alaviiva kirkkaalle taustakartalle: ⊥ tässä taskissa — se on T336:n (V244) kuvio, ⊥ rakenna kahta casing-mekanismia. **(d) `.segment-label--done`** `src/style.css:3430-3456`: `border-color: var(--confirm)` (nyk. `rgba(255,255,255,0.15)`) + paksumpi/kaksinkertainen reuna jos 1px ⊥ erotu tummaa taustaa vasten. **B106 sitova: tausta pysyy kiinteänä navyna `rgba(15,23,42,0.85)` & teksti `#fff`** — ⊥ vaihda lapun taustaa vihreäksi (teema-agnostinen kontrastisopimus rikkoutuisi; ✓ + reunus riittävät). `.segment-label--dim`in `opacity .4` + `pointer-events:none` (V142) säilyy & pätee myös done-lappuun. **(e) DESIGN.md §K**: `SegmentOverlay` (:711-723) väri-kanavan sääntö "väri = tunniste, PAITSI valmis = `--confirm`" + uudet dashArray-arvot + `:719` "tarkastus-vaiheen valmis-pätkä saa ✓" → "valmis-pätkä saa ✓ KAIKISSA phaseissa"; `SegmentLabel` (:725-742) taulukkoon rivi `Valmis (--done)` = `border-color: var(--confirm)` + ✓-prefix + maininta että `--dim` & `--done` yhdistyvät. **(f) Järjestys:** T348 ajetaan ENNEN T304/T336 — ⊥ riipu paletti-kalibroinnista (vihreä on statuskanava, ⊥ osa tunnistepalettia). T336 kantaa nämä casingiin (ks. sen rivi). **Testattavuus.** Taso 1 Vitest-pure (`src/logic/segments.ts`): `segmentLineColor` — valmis → `SEGMENT_DONE_COLOR` riippumatta id:stä; kesken/ei_alkanut → identtinen `colorForSegment`in kanssa; sama id + eri state → eri väri; **stabiilius-regressio: poista lista keskeltä pätkä → jäljelle jäävien `kesken`-värit ⊥ muutu** (V96-ydin). Taso 3 Playwright: `e2e/segments.spec.ts:432` "pätkän viivatyyli koodaa statuksen — tyhjä pätkä = katkoviiva (T152)" ! pysyä vihreänä & dashArray-arvo muuttuu ∴ **päivitä ODOTUSARVO, ⊥ löysennä testiä `toBeTruthy`-tasolle** (V252); UUSI case: valmis pätkä = `stroke` on `--confirm` + ⊥ `stroke-dasharray` + lapussa `✓` + `.segment-label--done`. `e2e/critical-paths.spec.ts:915/970` (lapun klikkaus + V142-dim) ! pysyä vihreinä. COMPONENTS.md SegmentOverlay-rivin E2E-sarake päivitetään. **Kerros:** `src/logic/` (väri-valinta) + `src/map/` (glue) + CSS. Ei uutta komponenttia, ei localStoragea, ei backend-muutosta, ei migraatiota (johdettu visuaali olemassa olevasta datasta). Auto-ux → `/karttamaster-ux` (§K-amend). Käyttäjä: **järjestäjä** (tilannekuva kartalta yhdellä silmäyksellä, VISION §P); talkoolainen hyötyy sivutuotteena (oma pätkä näyttää itse milloin se on valmis, ⊥ tarvitse lukea sivupalkkia). **UX-audit 2026-07-27 (toteutuma):** `LINE_STATE_STYLE.ei_alkanut` alfa `0.4` → `0.7`. Mitattu ⊥ makuasia: `#7A4E9C` @ 0.4 vaaleaa MML-taustaa (`#F2F0EA`) vasten = efektiivinen kontrasti **1.79:1** eli reilusti alle WCAG non-text-minimin 3:1 ∴ "ei aloitettu" katosi kirkkaassa & se on juuri se tila jonka järjestäjän ! bongata kartalta (kuka ⊥ ole vielä aloittanut) ⇒ V252 ⊥ täyttynyt kolmannelle tilalle vaikka dashArray korjattiin. 0.7 = **3.0:1**. Kolmiportaisuus säilyy kolmella riippumattomalla kanavalla (kuvio `''`/`'10 8'`/`'6 12'`, leveys 11/11/9, alfa .9/.85/.7) ∴ kevein on yhä kevein, muttei näkymätön. Mitatut: valmis 3.83, kesken 3.57, ei_alkanut 3.0. DESIGN.md §K SegmentOverlay sai alfa-alarajasopimuksen. | V96,V252,V244,V216,V142,V132,B135,B106,T152 |
| T349 | ✓ | **[Pieni]** Karttapinnan valmis-vihreä yhdestä lähteestä (B136/V253). Nyt viiva lukee JS-vakion `SEGMENT_DONE_COLOR` & lappu CSS-tokenin `var(--confirm)` ∴ Kaamos-teemassa pari hajoaa (mitattu: `#1F8A50` vs `#2FA35B`). (a) `src/style.css`: uusi karttapinta-token `--segment-done: #1F8A50` **VAIN `:root`issa** — `[data-theme="dark"]` ⊥ ylikirjoita sitä (kommentoi miksi: karttapinta ⊥ vaihdu teeman mukana, V253). (b) `.segment-label--done` `border-color: var(--segment-done)` (⊥ `--confirm`). (c) `src/logic/segments.ts` `SEGMENT_DONE_COLOR`-kommentti osoittaa `--segment-done`iin `--confirm`in sijaan (arvo ⊥ muutu). (d) Tarkista sama vuoto muualta karttapinnalta: grep `var(--` niistä CSS-luokista jotka renderöityvät kartan päälle (`.segment-label*`, `.marker-*`, `.map-mode-pill`, `.leaflet-*`-johdannaiset) — jos joku muu lukee teemariippuvaista tokenia, se on sama bugi & kirjataan omaksi §T:ksi (⊥ korjata sokkona tässä). Testattavuus: Taso 3 Playwright UUSI `e2e/segments.spec.ts`-case tai `t349-*.spec.ts` — sama valmis-pätkä renderöidään `localStorage.karttamaster-theme = light` JA `dark`, & karttapinnan värit (lapun `borderColor`, viivan `stroke`, lapun `backgroundColor`) ! olla IDENTTISET molemmilla (V253 + B106 samalla assertilla). Ilman teemakierrosta tämä vika ⊥ jää kiinni — vaaleassa se on näkymätön. DESIGN.md §C: `--segment-done` karttapinta-tokeniksi + §K SegmentLabel `--done`-rivi osoittaa siihen. Käyttäjä: molemmat (teema on käyttäjäpreferenssi V132). | V253,V132,B136,B106,T348 |
| T350 | ✓ | **[Pieni]** Merkkien karttahehku irti chrome-tokenista (V253-jälkiseuranta, T349(d)-kartoitus). `.marker-next-highlight` (`@keyframes marker-next-glow`) & `.leaflet-marker-pending` lukevat `var(--accent)`ia `filter: drop-shadow`iin ∴ hehku piirtyy suoraan POHJAKARTAN päälle chrome-tokenilla = V253-rikkomus. **⊥ näy käyttäjälle NYT**: `--accent` on `#F2542D` sekä `:root`issa että `[data-theme="dark"]`issa ∴ pari ⊥ hajoa — mutta se on sattuma, ⊥ suoja: Kaamoksen aksentin kalibrointi (esim. tummalle taustalle kirkkaampi) siirtäisi hehkun värin vaikka pohjakartta ⊥ muutu. Korjaus: `--marker-glow` karttapinta-tokeniksi (`:root`-only, arvo `#F2542D`) & molemmat säännöt lukevat sitä. Testattavuus: laajenna `e2e/t349-map-surface-theme.spec.ts` — seuraava-merkki näkyviin & `filter`-computed style identtinen light/dark. Sama teemakierros-kuvio, ⊥ uutta testitiedostoa. Kartoituksessa OK-tuomion saaneet (`.map-mode-pill`, `#marker-focus-pill`) ⊥ kuulu tähän: ne kantavat oman läpinäkymättömän pintansa ∴ ovat chromea & saavat seurata teemaa. Käyttäjä: molemmat. | V253,V178,B136,T349 |
| T351 | ✓ | **[Suuri]** Valmis-toggle hero:n done-riville + ⋯-duplikaatti pois (B140/V254). (a) `src/ui/segment-hero.ts` done-haara (`!current`, rivit ~80-88): `total > 0` → lisää done-rivin alle nappi joka kutsuu `actions.onComplete?.(!(segment.completed ?? false))`. Label toggle `segment.completed`sta: "✓ Merkitse pätkä valmiiksi" / "↩ Merkitse keskeneräiseksi", luokat `btn btn--confirm segment-view-complete-btn` / `btn btn--secondary segment-view-complete-btn` — SAMA sanamuoto & tokenit kuin `renderCompleteSection` (`segment-view.ts:206-224`) ∴ ⊥ uutta CSS:ää (`.segment-view-complete-btn` = width 100% + center, 44px `.btn`:stä). `total === 0` ("Ei merkkejä tällä pätkällä") → ⊥ nappia: tyhjä pätkä ⊥ ole valmiussignaali. Nappi vain jos `actions.onComplete` annettu (järjestäjä ⊥ saa heroa muutenkin). (b) POISTA duplikaatti: `#btn-tk-complete` `index.html`:stä + sen wiring & `syncCompleteLabel` `markers-wiring.ts`:stä — `applyComplete` JÄÄ (sitä käyttää `onComplete`, T230). (c) `completeSection` "Kaikki merkit" -tabissa JÄÄ: hero renderöityy vain `asettaminen`-phasessa ∴ purku-vaiheen kuittaus katoaisi. Testit: Vitest-jsdom `segment-hero` (done-rivi+merkkejä → nappi olemassa; klikki → `onComplete` kutsuttu `!completed`:llä; `completed=true` → "↩"-label; `total===0` → ⊥ nappia; `onComplete` puuttuu → ⊥ nappia) + Playwright talkoolaispolku (kaikki merkit asetettu → nappi hero:ssa, ⊥ ⋯-valikossa). Käyttäjä: talkoolainen. | V254,B140 |
| T352 | ✓ | **[Keski]** Järjestäjälle valmis-toggle pätkän modaaliin (B141/V255). `src/ui/segment-details-modal.ts`: valmis-rivi (malli `area-details-modal.ts:313` — "✓ Merkitse valmiiksi" / "↩ Peru valmis"), näkyy vain `asettaminen`/`purku`-phaseissa (sama ehto kuin `renderCompleteSection`). Kutsuu samaa polkua kuin talkoolainen: `updateSegment(store, id, {completed})` + `updateSegmentRemote(id, {completed})` + `renderSegmentOverlay()` ∴ paneeli & overlay uudelleenrenderöityvät samalla tavalla kuin talkoolaisen kuittauksesta. HUOM (verifioitu T352:n toteutuksessa): viiva ⊥ viherry tästä — `completed` on erillinen merkkien statuksista johdetusta `segmentLineState`sta (B142). Epäonnistunut PUT → näkyvä syy (modaalilla ⊥ ole pääsyä `main.ts`:n `showWarning`iin ∴ inline-status osiossa, ⊥ hiljainen `.catch(() => {})`). Harkitse `applyComplete`-tyylisen apurin nostoa jaettuun paikkaan jos wiring-duplikaatti kasvaa. Testit: Vitest-jsdom modaali (toggle näkyy asettaminen/purku ⊥ tarkastus, klikki → `updateSegment` + PUT `{completed}`, PUT-fail → näkyvä virhe) + Playwright järjestäjäpolku. Käyttäjä: järjestäjä. | V255,B141,T351 |
| T353 | ✓ | **[Suuri]** `completed` näkyviin järjestäjän tilannekuvaan (B142/V256 — PM-päätös (a)). (a) `src/logic/segments.ts`: `segmentLineState(progress, completed = false)` → `completed` ⇒ `valmis` ennen laskurihaaraa. Valinnainen parametri ∴ ⊥ rikkoa kutsupaikkoja. (b) `src/map/segment-overlay.ts:119`: välitä `seg.completed` ∴ kuitattu pätkä saa vihreän ehjän viivan + ✓-lapun (T348-polku ennallaan). (c) `src/ui/segment-panel.ts` `buildRow`: `.segment-kuitattu` ("✓ Kuitattu", `title` selittää lähteen) `hidden`-kytkimellä, `.segment-km`-laskurin JÄLKEEN ⊥ tilalle. CSS `--confirm` (sivupalkki = chrome, V253 sallii teematokenin). Testit: Vitest-pure `segmentLineState` (kuitattu voittaa 0/5, 2/5, tyhjän & tarkastuksen; oletusparametri ennallaan) + Vitest-jsdom lista (näkyy/piilossa, ⊥ korvaa laskuria, molemmat ristiriitasuunnat) + Playwright järjestäjä (kuitattu+merkit kesken → `#1f8a50` ehjä viiva & "0/1 asetettu" listassa). Käyttäjä: järjestäjä. | V256,B142,T351,T352 |
| T354 | ✓ | **[Keski]** `SegmentDetailsModal`in VÄLILEHDET (V257) — sama UI-ratkaisu kuin talkoolaisen `/s/<koodi>`-kotinäkymässä, ⊥ toista tabitoteutusta. T346:n 5 väliotsikkoa peräkkäin bodyssa → KOLME tabia; T346:n moduulirajat pysyvät voimassa tulevaa pilkkoa varten (⊥ keksi uusia), ne vain kääriytyvät tabeiksi. (a) **Jaettu komponentti yleistetään:** `SegmentKotiTabs.scroller()` on kovakoodattu `this.root.closest('#segment-view')` ∴ modaalin bodyssa tab-vaihto ⊥ nollaisi scrollTopia (T315/V226 katoaisi HILJAA). Konstruktoriin valinnainen scroller-valitsin (oletus `'#segment-view'` — talkoolaispolku ⊥ muutu), modaali antaa `.segment-details-modal-body`. ⊥ toista tilamuuttujaa, ⊥ toista `setActive`-logiikkaa. Luokan nimi säilyy: uudelleennimeäminen koskisi `segment-view.ts` + `t264` + `t315` ilman toiminnallista hyötyä ∴ jaettu käyttö dokumentoidaan COMPONENTS.md-riville. (b) **Tabijako (PM-päätös 2026-07-27, ⊥ neuvoteltavissa buildissa):** `varuste` `🎒 Varustelista` (Lisävarusteet-lista + lisäysrivi) · `merkit` `Kaikki merkit` (merkkilista + Yhteenveto-chipit) · `asetukset` `Asetukset` (Tiedot: nimi+linkki+kuvaus · Jako: assign + aktiviteettiloki · Kartta: muokkaa pisteitä · Vaiheet: klooni). Oletustabi `varuste`. (c) `buildMarkersAndEquipmentSection` → `buildMarkersSection` + `buildEquipmentSection`. T199:n "yksi lista" -perustelu raukeaa kun ne ovat eri tabeilla; kummankin SISÄINEN DOM & valitsimet (`.segment-details-marker-list`, `.segment-details-marker-item`, `.segment-equipment-chip-list`, `.segment-equipment-manual-list`, `.segment-equipment-item`, `.segment-equipment-add`) säilyvät ENNALLAAN. Tyhjä pätkä: merkit-tab renderöi tyhjätilan, ⊥ katoa tabipalkista — katoava tabi = paikka joka liikkuu datan mukana. (d) `.segment-details-group-title` poistuu bodyn tasolta; Asetukset-tabin SISÄLLÄ otsikot säilyvät (Tiedot/Jako/Kartta/Vaiheet), muuten tabista tulee neljän eri kysymyksen jäsentämätön lista. (e) **CSS-tarkistus ⊥ oletus:** `src/style.css:1748` `#app[data-view-mode="kartta"] .segment-koti-tabs{display:none}` — modaali on `document.body`-lapsi `#app`:n ULKOPUOLELLA ∴ sääntö ⊥ osu, mutta tabipalkin näkyvyys modaalissa ! olla TESTATTU, ⊥ päätelty selektorista. Tabipalkki modaalissa `position:sticky; top:0` bodyn sisällä (⊥ scrollaa pois pitkällä Asetukset-tabilla), `surface-card`-tausta. Testattavuus: Taso 2 Vitest-jsdom — `tests/t264-segment-koti-tabs.test.ts` laajenee (scroller-valitsin konfiguroitavissa, oletus ennallaan); UUSI `tests/t351-modal-tabs.test.ts` (3 tabia oikeassa järjestyksessä; oletus `varuste`; tab-vaihto `hidden` + `aria-selected`; merkit-tab tyhjätila merkittömälle pätkälle; Asetukset-tabin 4 sisäotsikkoa). PÄIVITETTÄVÄT (odotettu ⊥ regressio, V257): `t69`, `t199`, `t346` (5 väliotsikkoa → 3 tabia, T346:n rakenneväite korvautuu), `t335`-kytkintestit ! aktivoida tabi ensin. Taso 3 Playwright `e2e/segments.spec.ts`: modaali 390px-leveydellä → tabipalkki mahtuu yhdelle riville & tab-vaihto ⊥ avaa näkymää keskeltä. Kerros: `src/ui/segment-details-modal.ts` + `src/ui/segment-koti-tabs.ts` + `src/style.css` + DESIGN.md §K SegmentDetailsModal. COMPONENTS.md-sync tämän & T355/T356:n jälkeen: rivi 102 (SegmentDetailsModal: 5 ryhmää → 3 tabia + modal-footer-pattern), rivi 108 (SegmentKotiTabs jaettu kotinäkymän JA pätkämodaalin kesken), rivi 182 (pilkkohälytys: moduulirajat = T346:n ryhmät, nyt näkyvissä tabeina). Käyttäjä: järjestäjä. **TOTEUTUNUT:** `SegmentKotiTabs` sai `KotiTabOptions`-parametrin (`scrollerSelector`, oletus `#segment-view`; vanha string-`initial` yhä tuettu ∴ talkoolaispolku muuttumaton). `buildMarkersAndEquipmentSection` → `buildMarkersSection` + `buildEquipmentSection`. Testit: 8 uutta (`t351-modal-tabs`) + 3 uutta `t264`:ään; `t346` kertoo nyt 4 ryhmäotsikosta (Sisältö = omat tabinsa), `t56b`-E2E klikkaa Asetukset-tabin ennen valitsinta. E2E-korjaus samalla: `T56b` ajoi ilman `mockSegmentWrites`ia ∴ luonti-POST → 401 → `#auth-screen` kaappasi klikit (E2E-NOTES juurisyy 1) — vika oli piilossa koska vanha assertio ei klikannut mitään. Suite 1706 vihreää, e2e 155/155. **Rinnakkaistörmäys ratkaistu rebasessa:** main varasi T351–T353/V254–V256 eri featurelle (valmis-kuittaus) ∴ tämä sarja on T354–T356/V257. LISÄKSI main'in T352-valmis-toggle olisi jäänyt kolmannen tabin pohjalle vaikka V255 sanoo sen olevan järjestäjän AINOA sisääntulo ∴ se siirrettiin `Kaikki merkit` -tabiin — SAMA paikka kuin talkoolaisen kotinäkymässä (`segment-view.ts` merkit-tab) ⇒ kaksi roolia ⊥ etsi samaa toimintoa eri paikasta (V236-henki). | V257,V226,V250,T346,T264,T199 |
| T355 | ✓ | **[Pieni]** Pätkämodaalin footer jaettuun `modal-footer`-patterniin (V250-korjaus, DESIGN.md §Modal footer -pattern). Vaatii T354. **Auto-save-malli SÄILYY** (V250 ydin): kentät tallentuvat muutoksesta, footer ⊥ kerää mitään ∴ `Tallenna`-label on KIELLETTY tässä modaalissa — se lupaisi tallennuksen jonka kenttä on jo tehnyt & antaisi ymmärtää että ✕ menettää muutokset (juuri se väärinkäsitys jonka V250 poisti). (a) `buildCloseFooter`: `.segment-details-modal-footer` + `.btn-segment-modal-save` (`Valmis`) → `.modal-footer` > `.modal-footer-actions` > `.modal-btn-secondary` `Sulje`. Primary-paikka jää TYHJÄKSI: `confirm`-täytteinen nappi joka vain sulkee on sama valhe kuin `Tallenna`. (b) `buildDangerZone` (`.segment-modal-danger-zone`, bodyn viimeinen lapsi) → `.modal-footer-destructive` > `.modal-btn-destructive` `Poista pätkä` footerin omalla rivillä; `confirm()` + `onExitEditMode` + remote-poisto ennallaan. (c) `.modal-footer`/`.modal-btn-*` ovat JO olemassa (`src/style.css:3847-3930`) ∴ tämä on luokkien vaihto, ⊥ uusi footer-CSS-perhe. Vanhentuneet säännöt (`.segment-details-modal-footer`, `.btn-segment-modal-save`, `.segment-modal-danger-zone`) poistetaan VAIN jos ⊥ muita käyttäjiä — grep ensin, `.btn-segment-modal-save` esiintyy jaetuissa hover/touch-säännöissä (`:3948`,`:3983`,`:3989`). (d) §V250-teksti päivitetty tämän taskin mukana (destruktiivinen footeriin, `Valmis`→`Sulje`) ∴ SPECissä ⊥ jää ristiriitaa. Testattavuus: Taso 2 Vitest-jsdom `tests/t69-segment-details-modal.test.ts`: footerissa `.modal-btn-secondary` teksti `Sulje`; `.modal-btn-primary` ⊥ ole olemassa; poistonappi footerin SISÄLLÄ (`.modal-footer .modal-btn-destructive`) & DOM-järjestyksessä actions-rivin JÄLKEEN; sulkuklikki ⊥ laukaise yhtään `updateSegment`-kutsua. Kerros: `src/ui/segment-details-modal.ts` + `src/style.css` + DESIGN.md §K. Käyttäjä: järjestäjä. **TOTEUTUNUT:** footer `.modal-footer` + `.modal-btn-secondary` `Sulje`; `buildDangerZone` palauttaa `.modal-footer-destructive`-rivin footerin sisällä. Kuollut CSS poistettu (`.btn-segment-modal-save` + sen 3 jaettua hover/touch-esiintymää, `.segment-modal-danger-zone`-layout). §V250 päivitetty samalla. | V250,V62,T354 |
| T356 | ✓ | **[Pieni]** Korostuskytkin modaalin HEADERIIN (V243/V197). Vaatii T354. `buildFocusSection`in nappi siirtyy bodyn Kartta-ryhmästä headeriin otsikon & ✕:n väliin: korostus on modaalin TILAKYTKIN (vaikuttaa karttaan taustalla heti) ⊥ Kartta-ryhmän asetus muiden joukossa — tabien jälkeen se hautautuisi Asetukset-tabin kolmanneksi alaotsikoksi. Tilan omistaja pysyy wiringissä (`callbacks.isFocusSegment`/`onToggleFocusSegment`, T335): ⊥ muutosta tilamalliin, ⊥ persistointia, poistumis-pilleri (V243) vastaa edelleen tilan näkyvyydestä modaalin sulkeuduttua. `focusToggleLabel` säilyy jaettuna (T345 rivivalikko lukee samaa tilaa). **⊥ ikoniksi:** V197 — näkyvä teksti on saavutettava nimi; pelkkä `◎` headerissa rikkoisi saman säännön jonka T345 §K kirjaa (`ei ikoni ilman tekstiä`). ∴ header-layout `[otsikko flex:1; min-width:0; text-overflow:ellipsis] [kytkin flex:0 0 auto] [✕ 44×44]`: otsikko ! typistyä ENNEN kuin kytkin | ✕ kutistuu (340px-modaali on mitoituksen ehto). Kytkin `.btn.btn--ghost` kutistettuna (`min-height:44px`, `padding:0 10px`, `12px`), lyhyt muoto `◎ Korosta` ↔ `◉ Korostettu`, pitkä muoto `title`-lisäselitteeksi, `aria-pressed` säilyy. Testattavuus: Taso 2 Vitest-jsdom (kytkin on headerin lapsi otsikon JA ✕:n VÄLISSÄ DOM-järjestyksessä; klikkaus togglaa `aria-pressed`n & kutsuu `onToggleFocusSegment`in; alkutila luetaan `isFocusSegment`istä; bodyssa ⊥ enää `.btn-segment-focus-toggle`ia — päivitä `t335`-modaalitestit). Kerros: `src/ui/segment-details-modal.ts` + `src/style.css` + DESIGN.md §K SegmentDetailsModal (otsikkorivin sopimus laajenee kolmeen elementtiin). Käyttäjä: järjestäjä. **TOTEUTUNUT:** `buildFocusSection` → `buildFocusToggle`, header-lapsi otsikon ja ✕:n välissä. Uusi export `focusToggleShortLabel` (`◎ Korosta` ↔ `◉ Korostettu`), pitkä muoto `title`-attribuuttiin. Otsikko `flex:1; min-width:0; ellipsis` ∴ 390px-ruudulla typistyy otsikko, ⊥ kytkin eikä ✕ (E2E mittaa ✕:n ≥44×44). | V243,V197,V250,T335,T354 |
| T357 | ✓ | **[Pieni]** Pätkämodaalin välilehtijärjestys: **Asetukset ensin** (PM-päätös 2026-07-27 — KORVAA T354(b):n järjestyksen & oletustabin). Vaatii T354. `src/ui/segment-details-modal.ts:171-180` tab-array → `asetukset` `Asetukset` · `varuste` `🎒 Varustelista` · `merkit` `Kaikki merkit`. Tabien ID:t, labelit & SISÄINEN DOM ENNALLAAN — tämä on PELKKÄ järjestys ⊥ rakennemuutos: jokainen valitsin (`.segment-details-marker-list`, `.segment-equipment-chip-list`, `.segment-equipment-manual-list`, `.segment-equipment-add`, `.segment-audit-section`, Asetukset-tabin 4 sisäotsikkoa) säilyy. (a) **Oletustabi siirtyy Asetuksiin** — `SegmentKotiTabs` avaa `tabs[0]`:n kun `initial` puuttuu (`segment-koti-tabs.ts:65`) ∴ järjestys SIIRTÄÄ oletuksen. Tarkoitettu seuraus ⊥ sivuvaikutus: järjestäjä avaa modaalin HALLITAKSEEN pätkää (nimi, jako, rajat, klooni), ⊥ selatakseen varustelistaa. `initial`-parametria ⊥ anneta — kaksi totuutta järjestyksestä (array + `initial`) ajautuu erilleen seuraavassa uudelleenjärjestyksessä. (b) **Valmis-kuittaus (`buildCompleteSection`) JÄÄ `merkit`-tabiin** — PM-päätös, ⊥ neuvoteltavissa buildissa. Se valuu KOLMANNEKSI ∴ ristiriidassa T354:n TOTEUTUNUT-perustelun kanssa ("⊥ saa haudata itseään kolmanteen tabiin"); voittava perustelu: kaksi roolia ⊥ etsi samaa toimintoa eri paikasta (V236-henki) **>** tabi-indeksi — talkoolaisen kotinäkymässä valmis-toggle on `Kaikki merkit` -tabissa (`segment-view.ts`) & järjestäjä ! löytää se samasta paikasta. LISÄKSI V254 antaa talkoolaiselle heron done-rivin sisääntulon ∴ syvyys on järjestäjän desktop-ongelma ⊥ metsässä-ongelma. `segment-details-modal.ts:162-166` -kommentti PÄIVITETTÄVÄ vastaamaan tätä (vanhentunut perustelu koodissa = seuraava lukija kumoaa PM-päätöksen vahingossa). §V255 ⊥ vaadi muutosta: se ⊥ ota kantaa tabi-indeksiin, vain siihen ETTÄ sisääntulo on olemassa & käyttää samaa `updateSegment` + `updateSegmentRemote` -polkua. (c) Talkoolaisen `segment-view.ts` tab-järjestys (Varustelista · Kaikki merkit · Kommentit) EI muutu — `SegmentKotiTabs` saa listan KUTSUJALTA ∴ V257 (yksi tabitoteutus) säilyy: roolikohtainen järjestys on kutsujan dataa ⊥ komponentin sisäinen haara. (d) `DESIGN.md:448` §K SegmentDetailsModal: "`🎒 Varustelista` (oletus) · `Kaikki merkit` · `Asetukset`" → "`Asetukset` (oletus) · `🎒 Varustelista` · `Kaikki merkit`". Testattavuus: **Taso 2 Vitest-jsdom** `tests/t354-modal-tabs.test.ts:49-56` — `toEqual(['asetukset','varuste','merkit'])` & oletuspaneeli `asetukset` näkyvissä, kaksi muuta `hidden`. Saman tiedoston MUUT testit (varusteet/merkit eri paneleissa, merkitön tyhjätila, 4 sisäotsikkoa) pysyvät MUUTTUMATTOMINA ∴ jos ne hajoavat, järjestysmuutos vuoti rakenteeseen — se on signaali ⊥ päivitettävä assertio. **Taso 3 Playwright** `e2e/segments.spec.ts:450-452` oletustabi-assertio `varuste`→`asetukset`; rivi 347-348 (klikkaa `asetukset`-tabin eksplisiittisesti ennen rajojen muokkausta) toimii ENNALLAAN ⊥ vaadi muutosta. Kerros: `src/ui/segment-details-modal.ts` + `DESIGN.md` §K. ⊥ CSS-muutosta — `position:sticky`-tabipalkki & panelit ovat järjestysriippumattomia. Käyttäjä: järjestäjä. **TOTEUTUNUT:** tab-array uudelleenjärjestetty, `initial` edelleen antamatta ∴ oletus = `asetukset`. `t354-modal-tabs`: järjestys+oletus-assertio päivitetty; tab-vaihtotesti valitsee napin `data-tab`illa ⊥ indeksillä (järjestys on PM-päätös, tab-vaihdon sopimus ⊥ ole). Tiedoston muut 4 testiä pysyivät koskemattomina ∴ rakenne ⊥ vuotanut. `e2e/segments.spec.ts:450` oletustabi `varuste`→`asetukset` & klikki `data-tab="merkit"`-valitsimella. Vitest 1706 vihreää, `e2e/segments` 23/23 + `critical-paths` 31/31. | V257,V255,T354,T352 |
| T358 | ✓ | **[Suuri]** **SegmentTrack-perusta** (V258/V261). Uusi `src/logic/segment-track.ts` — ⊥ Leafletia, ⊥ DOM:ia. Tyyppi `TrackPoint = {lat,lon,d}`, `SegmentTrack = TrackPoint[]`. Funktiot: (a) `deriveTrackFromBounds(routePoints, startDist, endDist)` — legacy-backfill (T361) & rajamuokkaus (T363), sama siivu kuin `segment-overlay.ts:290 sliceRoutePoints` ∴ piirto ⊥ siirry; (b) `buildTrackFromAnchors(routePoints, anchorIdx[])` — klik-klik (T362): kerää pisteet kulkemalla reittiä ETEENPÄIN edellisestä indeksistä ∴ lenkki/edestakainen osuus ⊥ ole enää arvaus (B144(b)); ei-monotoninen ankkuri = virhe kutsujalle ⊥ hiljainen käännös; (c) `trackLengthM(track)`; (d) `distanceToTrackM(track, lat, lon)` KOHTISUORALLA projektiolla janalle (V261) ⊥ kärkipisteellä; (e) `kmAlongTrackM(track, lat, lon)` = projektiopisteen `d`. `src/logic/bearing.ts`:n `nearestPointIndex`/`nearestPointCandidates` JÄÄVÄT ennalleen (reitti-/km-käyttö) — ⊥ muuteta niitä tässä. Riippuvuudet: ⊥ (perusta). Testattavuus: **Taso 1 Vitest-pure** `tests/segment-track.test.ts` — ! sisältää regressiotapauksen B143:n OIKEASTA datasta: merkki jolla smtb-55 km 18.85 JA 20.69, etäisyys Pätkä3:n & Pätkä4:n jälkiin on eri ∴ lähin ratkeaa (V259:n edellytys); lisäksi projektiotesti joka EPÄONNISTUISI kärkipiste-toteutuksella (piste 20 m janan keskeltä, kärjet 30 m päässä). Käyttäjä: molemmat (näkymätön perusta). **TOTEUTUNUT:** `src/logic/segment-track.ts` — `TrackPoint`/`SegmentTrack`, `deriveTrackFromBounds`, `buildTrackFromAnchors` (heittää ei-monotonisesta ankkurista ⊥ käännä hiljaa), `trackLengthM`, `distanceToTrackM`, `kmAlongTrackM`. Projektio paikallisella tasoprojektiolla; `bearing.ts` koskematon (V261: kärkipistemittaus jää reitti-/km-käyttöön). `tests/segment-track.test.ts` 14 testiä: V261-pari kaatuisi kärkipiste-toteutuksella (20 m vs 102 m; harva GPX 30 m vs 47.6 m), V258 siivuvastaavuus `sliceRoutePoints`iin, B144-lenkkiregressio rinnakkaisella paluu-uralla, V259 km-interpolointi. Lisäksi ajettu TUOTANTOGEOMETRIAA vasten (`smtb-2026-55km.gpx` + backup): B143:n 3 duplikaattimerkkiä ratkeavat yksikäsitteisesti (7.5 vs 46.2 m · 1.3 vs 22.0 m · 19.3 vs 6.6 m), 0 tasapeliä; jälkien pituudet vastaavat rajoja (4.91 km / 4.98 km). Vitest 1720 vihreää, tsc 0. | V258,V261 |
| T359 | ✓ | **[Suuri]** **Jäsenyys jäljestä + eksklusiivisuus** (V259). Uusi `src/logic/segment-membership.ts`: kanoninen `resolveSegmentMarkers(segments[], markers[]) → Map<segId, SignMarker[]>` — ehdokasjoukko `routeIds`-leikkauksesta (V25 scope), LÄHIN jälki voittaa AINA (⊥ kynnystä, V259/PM-päätös), tasapeli pätkän id:llä, `linkedMarkerIds` lisää & `excludedMarkerIds` poistaa geometrian yli, reititön (V139) ennallaan. LISÄKSI V259:n km-lupauksen toteuttaja: `src/logic/segment-order.ts` `segmentKm`/`displayKm` saavat jälkihaaran (`kmAlongTrackM`, T358) kun `track` on — ilman tätä V259 lupaa km:n jäljeltä & 7 kutsupaikkaa (`segment-view.ts:286`, `segment-marker-list.ts:39,105`, `segment-details-modal.ts:321,333`, `segment-hero.ts:152`, `navigation.ts:30`) lukee yhä reitin km-akselia. HUOM mitattu: nykydatalla 0 jäsentä putoaisi "ei reitillä" -ryhmään ilman tätä (ck:review H-6) ∴ vika on LATENTTI ⊥ näkyvä — se ⊥ tee siitä valinnaista, se tekee siitä testattavan vain synteettisellä tapauksella (merkki jonka km-ehdokas on pätkän välin ulkopuolella mutta jälki lähin). `Segment.excludedMarkerIds?: string[]` lisätään `src/logic/segments.ts`:ään (valinnainen ∴ vanha data kelpaa). `getMarkersForSegment` delegoi tähän kun jälki on, muuten nykyiseen `resolveTaskMarkers`iin (V260). `src/logic/task-markers.ts` SÄILYY AreaMarkerin (V140) lähteenä & ⊥ saa alkaa vaatia pätkäjoukkoa — se on strukturaalinen yhden lähteen resolveri, eksklusiivisuus on pätkäkohtainen lisäkerros. **HUOM riski:** `getMarkersForSegment(segment, markers)` -signature on laajassa käytössä (`segment-view`, `segment-hero`, `markers-wiring`, `segment-panel`, `segment-details-modal`) & eksklusiivisuus vaatii GLOBAALIN näkymän pätkäjoukkoon ∴ kutsupaikkojen läpikäynti kuuluu TÄHÄN taskiin ⊥ jälkityöksi; jos kutsupaikka ⊥ tunne pätkäjoukkoa, se saa sen wiringistä ⊥ arvaa yksinään. Riippuu T358. Testattavuus: **Taso 1 Vitest-pure** `tests/segment-membership.test.ts` — hyväksymiskriteerit MITATTU tuotantodatasta (ck:review B-2, korjaa aiemman virheellisen "118→118"): duplikaatit **10 → 0**; nykyjäsenistä **0** menettää omistajansa; omistajallisia **117 → 133** (16 km-aukkomerkkiä saa lähimmän pätkän, V259 tietoinen seuraus ∴ jos luku ⊥ 133, sääntö on eri kuin speksattu); minkään pätkän reitin ulkopuoliset **61** pysyvät orpoina. **TOTEUTUNUT:** `src/logic/segment-membership.ts` — `resolveSegmentMarkers` ryhmittelee pätkät VAIHEEN mukaan itse (kutsuja ⊥ voi rikkoa V91:tä väärällä joukolla), eksplisiittinen tahto (linked/typeFilter) ohittaa geometrian & estää saman merkin geometrisen adoption muualle, muuten lähin jälki voittaa & tasapeli ratkeaa id:llä. `excludedMarkerIds` + `track` lisätty `Segment`iin; `getMarkersForSegment(seg, markers, peers)` delegoi & uusi `segmentPeers(store, seg)` antaa kilpailijat 9 kutsupaikalle (markers-wiring 7, segments-wiring 1, segment-details-modal 1). `segment-order.ts`: `segmentKm` lukee jäljen akselia kun jälki on (V259 km-lupaus, H-6) ∴ järjestys & jäsenyys samasta lähteestä. Jäljetön pätkä putoaa `resolveTaskMarkers`iin ∴ V260-välitila ennallaan. `tests/segment-membership.test.ts` 18 testiä (lenkki, jaettu korridori, kynnyksettömyys + aukkoadoptio, excluded/linked/typeFilter, vaiherajaus, tasapelideterminismi). **Hyväksymiskriteerit ajettu tuotantodataa vasten: duplikaatteja 0 (kriteeri 0), omistajallisia 133 (kriteeri 133), orpoja 61 (kriteeri 61).** Vitest 1738 vihreää, tsc 0, `e2e/segments.spec.ts` 23/23. Käyttäjä: molemmat (talkoolainen ⊥ näe vieraita merkkejä, järjestäjä näkee ehjän jaon). | V259 |
| T360 | ✓ | **[Suuri]** **Jäljen persistointi: kanta + API + sync + serverin omistajuus** (V258/V260/V261). (a) `server/db.ts`: `ALTER TABLE segments ADD COLUMN track TEXT` **& `ADD COLUMN excluded_marker_ids TEXT`** (JSON) try/catch-kuviolla nykyisten migraatioiden (rivi ~288 `primary_route_id`) perään. (b) `server/routes/segments.ts` ottaa vastaan & palauttaa MOLEMMAT — HUOM `routes/segments.ts:91` luettelee sarakkeet EKSPLISIITTISESTI `INSERT` + `ON CONFLICT DO UPDATE` -lauseessa ∴ kenttä joka ⊥ ole listalla katoaa HILJAA ensimmäisessä pushissa (ck:review B-3: `excludedMarkerIds` oli T359:ssä tyypissä mutta ⊥ missään kannassa — järjestäjän ohitus joka häviää reloadissa on pahempi kuin ohituksen puuttuminen). (c) `src/logic/segment-sync.ts` push/pull. (d) `server/marker-audit.ts`: `markerInOwnSegment` saa `track`-haaran (jälki kannassa → kohtisuora etäisyys; muuten vanha km-haara, V260) & `segmentCodeForMarker` seuraa SAMAA unionia ∴ 403-portti & audit-loki ⊥ voi olla eri mieltä (B100/V213). **Eksklusiivisuus ! toteutua MYÖS serverillä** (ck:review H-5): nykyinen `marker-audit.ts:100` on `segs.some(...)` = "kuuluuko merkki JOHONKIN minun pätkääni" ∴ se ⊥ voi olla eksklusiivinen — se ⊥ näe muita pätkiä. `ownSegments`in rinnalle kysely joka lukee KAIKKI saman phasen pätkät & ratkaisee lähin-voittaa samalla säännöllä kuin client; muuten V260:n lupaus ("kerrokset ⊥ voi olla eri mieltä jäsenyydestä") on vahvempi kuin toimitus & talkoolainen saa kirjoittaa merkkiin jota client ⊥ näytä hänelle. `segments`-taulussa on kaikki rivit ∴ tämä on kysely ⊥ arkkitehtuurimuutos. Serverin projektiofunktio on tarkoituksellinen duplikaatti (V261) omalla testillään. (e) `server/snapshot-data.ts` toimii ilman muutosta (`SELECT *` + `insertRows`) — VARMISTA testillä ettei snapshot pudota saraketta (hiljainen datamenetys palautuksessa olisi pahempi kuin bugi jonka korjaamme). Riippuu T358, T359. Testattavuus: **Taso 4 Bun-integraatio** `server/` (`test-fixtures.ts`: `seedTestUsers` + `authHeaders`) + lisäys `server/db-migration.test.ts`:ään (sarake syntyy vanhaan kantaan, olemassa oleva data säilyy) + serverin projektiotesti joka vertaa samaan odotusarvoon kuin T358:n frontti-testi. Käyttäjä: molemmat. **TOTEUTUNUT:** `server/db.ts` +2 saraketta (`track`, `excluded_marker_ids`) try/catch-kuviolla; `routes/segments.ts` kuljettaa molemmat POST/PUT/GET:ssä — PUT käyttää `'track' in body` -testiä ⊥ `??`-fallbackia ∴ patch joka ⊥ mainitse jälkeä säilyttää sen & eksplisiittinen tyhjä nollaa. Talkoolaisen sallittuun kenttäjoukkoon (V93/V43) lisätty `track`: rajamuokkaus johtaa jäljen uudelleen ∴ ilman tätä T363 tallentaisi rajat & jättäisi vanhan jäljen. Uusi `server/track-geo.ts` = V261:n tarkoituksellinen duplikaatti omalla testillään joka mittaa SAMAN geometrian kuin `tests/segment-track.test.ts` (20 m ⊥ 102 m, 30 m ⊥ 47.6 m). `marker-audit.ts`: uusi `allSegments(db)` + `ownerSegmentIds(allSegs, marker)` peilaa clientin `resolveSegmentMarkers`ia (vaiheryhmittely, eksplisiittinen tahto, lähin jälki voittaa, tasapeli id:llä); `markerInOwnSegment(segs, marker, allSegs=segs)` — oletusarvo degradoi SALLIVAAN suuntaan ∴ portti ⊥ hylkää kirjoitusta siksi että kutsuja unohti joukon. `segmentCodeForMarker` kysyy nyt koko taulun & poimii koodillisen VASTA voittajista (ennen: koodillinen kaukainen olisi voittanut koodittoman lähemmän → loki valehtelisi tekijästä). `routes/markers.ts` 4 kutsupaikkaa saavat `lat/lon` + `allSegments(db)`; siirto arvioidaan UUDELLA sijainnilla. Merkki ilman `lat/lon` (vanha client) putoaa km-haaraan ∴ ⊥ hylkäystä. `server/t360-track-persist.test.ts` 14 testiä (persistointi, PUT-semantiikka, talkoolaisen jälkioikeus, snapshot-kierros, migraatio TUOTANNON kaltaiseen vanhaan kantaan `db-migration.test.ts`-kuviolla, omistajuus + V260-fallback + V91-vaiherajaus, V261-peilitestit). Bun 360 pass (+14), Vitest 1738, `e2e/segments`+`critical-paths` 54/54. | V258,V260,V261 |
| T361 | ✓ | **[Keski]** **Legacy-jäljen backfill** (V260). Pätkä jolta `track` puuttuu saa sen `deriveTrackFromBounds`illa kun GPX:t ovat latautuneet & tulos pushataan kantaan — sama laiska kuvio kuin `backfillDistanceByRoute` (T300/V212, `src/logic/marker-distance.ts`) ∴ ⊥ erillistä migraatioskriptiä (GPX-geometria elää selaimessa; `scripts/`-skripti tarvitsisi oman kopionsa reittilistasta — juuri se virhe jonka T303/B117 korjasi). Ennen pushia serveri käyttää vanhaa km-haaraa (V260) ∴ välitila on LAILLINEN ⊥ rikki. LISÄKSI `cloneSegmentToNextPhase` (`src/logic/segments.ts:168`) ! johtaa jälki kloonille (ck:review H-7): se kopioi nyt `startDist`/`endDist` mutta jälkeä ⊥ ole ∴ tarkastus-/purku-vaiheen pätkä jäisi ikuisesti legacy-tilaan (V260 sallii sen ∴ ⊥ rikki, mutta se ⊥ koskaan saisi V259:n jäsenyyttä). Hyväksymiskriteeri: tuotannon 10 pätkää saavat jäljen & kartan piirto ⊥ siirry pikselilläkään (sama `sliceRoutePoints`-siivu ∴ vertailu on eksakti ⊥ silmämääräinen). Riippuu T358, T360. Testattavuus: **Taso 1 Vitest-pure** (backfill palauttaa muutettujen määrän, ⊥ ylikirjoita olemassa olevaa `track`ia, reititön pätkä ohitetaan) + **Taso 3** `e2e/segments.spec.ts` regressio (olemassa olevat pätkät piirtyvät & merkkilistat pysyvät). Käyttäjä: järjestäjä (⊥ näe mitään — se on onnistuminen). **TOTEUTUNUT:** uusi `src/logic/segment-backfill.ts` (`backfillSegmentTracks(store, routes) → muutetut`) + kytkentä `segments-wiring.ts`:ään heti storen täytön jälkeen. Ohittaa: jäljen jo omaavan (⊥ ylikirjoita klik-klikkiä/kenttäsäätöä), reitittömän (V139), reitin jota ⊥ löydy, & rajat joista tulee <2 pistettä (tyhjä jälki kannassa olisi valhe: näyttäisi migroidulta muttei omistaisi mitään — kattaa myös T358:n ristikkäiset rajat). `cloneSegmentToNextPhase` perii jäljen SYVÄKOPIONA (ck:review H-7). `tests/segment-backfill.test.ts` 10 testiä ml. idempotenssi. **Tuotantodataa vasten: 10/10 pätkää sai jäljen & jälki on PISTE PISTEELTÄ identtinen `sliceRoutePoints`-piirron kanssa (0 poikkeamaa)** — pituudet 1.77–10.45 km. **B145 löytyi & korjattiin buildin aikana:** push teki sivulatauksesta kirjoituksen → outboxin 401 nosti reauth-overlayn → 8 talkoolais- + 3 järjestäjä-E2E:tä jumiin; korjaus = järjestäjärajaus + `pushSegmentTrack` outboxin ohi. LISÄKSI `e2e/segments.spec.ts` hero-fixture korjattu OIKEILLE reittipisteille (oli lat 63.0/27.0 = ~290 km reitistä; km-säännöllä keksitty skalaari riitti, jäljen akselilla ⊥ mittaa mitään todellista) & odotus 3.0 km → 1.0 km = V259:n km-lupaus näkyvissä. Vitest 1748, Bun 360, E2E 56/56 (segments+critical-paths+hub). | V260 |
| T362 | ✓ | **[Suuri]** **Klik-klik-luonti** (V258/V261, korjaa B144). `src/ui/segment-panel.ts`-tilakone: `vaihe2` → `polku` — alku → N välipistettä → "Valmis" → `tiedot`. Jokainen klikki resolvoituu reitin PISTEINDEKSIKSI & ankkurit kerätään monotonisesti eteenpäin (T358 `buildTrackFromAnchors`) ∴ kierrosarvaus (B144(b)) katoaa. **Ensimmäinen klikki LUKITSEE reitin & se NÄYTETÄÄN** (reitin nimi + km) — B144(a):n hiljainen valinta oli puolet bugista: 3 reittiä ≤100 m päässä & UI ⊥ kertonut kumpi valittiin. Myöhempi klikki reitin ulkopuolelta antaa jo nyt virheilmoituksen (`segment-panel.ts:155`) — SÄILYTÄ. `src/ui/segment-creation-modal.ts`: askel 2 näyttää ankkurilistan (km per ankkuri) + "Poista viimeinen" + "Valmis"; askelmerkki 1—2—3 säilyy. `validateNoOverlap` ⊥ poistu mutta se muuttuu VAROITUKSEKSI eri primaryn tapauksessa (V25 sallii jaetut osuudet; esto olisi väärä — B143:n Pätkä8/Pätkä9 on laillinen jaettu korridori jonka jäsenyys ratkeaa nyt V259:llä ⊥ luontieston kautta). **DESIGN.md §K:416 `SegmentCreationModal` -sopimus AMENDATTAVA:** uudet napit `min-height:44px`, ankkurilista `max-height` + `overflow-y:auto` (sama kuvio kuin `.segment-creation-marker-checklist`). Riippuu T358, T360. Testattavuus: **Taso 2 Vitest-jsdom** (tilakone: ankkurien kertyminen, "Poista viimeinen", "Valmis" vasta ≥2 ankkurilla, reitin lukitus 1. klikillä) + **Taso 3 Playwright** uusi `e2e/segments.spec.ts`: "klik-klik-pätkä välipisteillä" (3 klikkiä + Valmis → tallennettu pätkä jonka jälki kulkee välipisteen kautta). Käyttäjä: järjestäjä (desktop ensisijainen, mobiili välttävä — VISION §Järjestäjä/Mobiili). **TOTEUTUNUT:** `CreationState.vaihe2` → `polku{routeId, routeLabel, anchors[]}`; uusi `nextAnchorIndex` (`segment-track.ts`) hakee ankkurin ETEENPÄIN edellisestä indeksistä ∴ kierrosarvaus poistui. **Ensimmäinen klikki EI käytä kynnystä** (snap kuten ennen) — kynnys siinä olisi ollut hiljainen ei-mitään-tapahdu; jatkoklikeillä kynnys ON koska niillä on virheteksti. `onSnapClick` ohjaa saman ankkurilogiikan läpi ⊥ luota snapin routeId/dist-arvoihin (muuten snap olisi takaovi jolla B144 palaisi). Päällekkäisyys → `onNotify`-VAROITUS ⊥ este (V25/V259). Tallennettu pätkä saa `track`in heti ∴ ⊥ odota T361:n backfilliä. **UX-audit löysi 2 vikaa, korjattu:** (a) jaetut `.btn--confirm`/`.btn--secondary` ⊥ määrittele `:disabled`-tilaa ∴ "Valmis" näytti painettavalta yhdellä ankkurilla & klikkaus ⊥ tehnyt mitään (V250 kuollut pinta) → oma `:disabled`-sääntö `.btn-bulk-apply`-sopimuksen mukaan; (b) ankkurilista rakentuu uudelleen joka klikillä ∴ scroll palasi alkuun & juuri klikattu ankkuri jäi fold-rajan alle 6:nnesta eteenpäin → `scrollTop = scrollHeight`. DESIGN.md §K:416 amendattu (polku-tila, disabled-pakko, scroll-sopimus). **E2E-fixture korjattu:** testit klikkasivat reittipolun BOUNDING BOXIN sisältä — piste joka ⊥ ole viivalla; se toimi vain koska vanha flow snappasi molemmat klikit & lajitteli min/max:lla eli juuri B144:n arvauksen varassa. Uusi `clickRoutePoints`-apuri projisoi oikeat reittipisteet kartan kautta. Vitest 1752, `e2e/segments` 24/24 (ml. uusi "klik-klik-pätkä välipisteillä": 4 ankkuria, "Poista viimeinen", tallennettu jälki), `critical-paths` 31/31. | V258,V261 |
| T363 | ✓ | **[Keski]** **Rajojen muokkaus kentällä säilyy** (V258/V260). `segment-view.ts:641 onEditBounds` / `segment-overlay.ts:231 enterEditMode` (T78/V43 — talkoolaisen kenttäoikeus, VISION §Talkoolainen: "Muokkaa pätkän pituutta kentällä") laskee jäljen UUDELLEEN `deriveTrackFromBounds`illa tallennuksessa ∴ km-pohjainen säätö-UI säilyy ENNALLAAN (hanskat, yksi käsi, ⊥ uutta vuorovaikutusta metsään) mutta jälki ⊥ jää jälkeen — vanhentunut jälki tarkoittaisi että merkit ⊥ seuraa rajaa jota talkoolainen juuri siirsi. ⊥ tee rajamuokkauksesta klik-klik-flowta: se on kentän kiireinen korjaus ⊥ suunnittelutyökalu. Riippuu T358, T360. Testattavuus: **Taso 1 Vitest-pure** (rajojen muutos → uusi jälki, pituus vastaa uusia rajoja) + **Taso 3** olemassa oleva `e2e/segments.spec.ts`-rajamuokkaustesti päivitettynä (rajan siirto → merkkilista muuttuu vastaavasti). Käyttäjä: talkoolainen (metsässä, yksi käsi). **TOTEUTUNUT:** uusi `boundsPatch(seg, routes, start, end)` (`segment-backfill.ts`) palauttaa rajat JA jäljen samasta johtofunktiosta kuin backfill/legacy-migraatio (V258: yksi johtotapa, ⊥ kolme kopiota). Kytketty KOLMEEN tallennuspolkuun: talkoolaisen numeerinen + kartan raahaus (`markers-wiring.ts:286`) & järjestäjän raahaus (`segment-details-modal.ts:674`, reitit `getRoutes`-callbackilla paneelilta) ∴ rajat & jälki liikkuvat yhdessä riippumatta roolista. km-pohjainen säätö-UI ENNALLAAN — ⊥ uutta vuorovaikutusta metsään. **T358:n sopimusepäjatkuvuus korjattu (testaajan löydös):** `deriveTrackFromBounds` HEITTÄÄ nyt ristikkäisistä/yhtäsuurista rajoista kuten sisarfunktio `buildTrackFromAnchors` heitti jo ei-monotonisesta ankkurista; hiljainen tyhjä jälki olisi tallentanut pätkän joka menettää kaikki merkkinsä (V259: tyhjä jälki ⊥ omista mitään). Kutsujat validoivat ennen kutsua: UI esti jo `endKm <= startKm` & raahaus `editStart < editEnd`; backfill sai vartijan ettei rikkinäinen legacy-rivi kaada koko ajoa. `boundsPatch` palauttaa rajat ilman jälkeä jos reittiä ⊥ löydy ∴ pätkä jää V260-km-haaraan ⊥ saa tyhjää jälkeä. Testit: 6 uutta Vitest-pure + `e2e/segments.spec.ts` rajamuokkaustesti laajennettu tarkistamaan että PUT kantaa jäljen & että jäljen pituus on TODELLINEN (smtb-30 = 4.6 km ∴ 15 km raja typistyy reitin loppuun, V258: pituus = jäljen viimeinen `d` ⊥ `endDist−startDist`). Vitest 1758, Bun 360, `e2e/segments` 24/24, `critical-paths` 31/31. | V258,V260 |
| T364 | ✓ | **[Keski]** Huomion kuittaus & kategoria (V263). **RETRO-KIRJAUS 2026-07-27:** rakennettiin huomiot-haarassa ilman §T-riviä ∴ koodi siteerasi numeroita joita SPEC ⊥ tuntenut — tämä rivi sulkee raon, ⊥ ole uusi työ. (a) `src/logic/comments.ts`: `Comment.resolvedAt`/`resolvedBy` + `resolveComment(id, resolved)` + kategoria-lista. (b) `server/routes/comments.ts`: kuittaus-endpoint järjestäjä+ (gate serverissä ⊥ vain UI:ssa — V13-linja). (c) `src/ui/comment-point-modal.ts`: kuittausnappi `canResolve`-ehdolla, kategorianapit rivinä, kuitattu tila sanoin (kuka · milloin). (d) `src/map/comment-layer.ts`: kuitattu → himmennys inline-tyylissä (DESIGN.md §K CommentPin). (e) `src/ui/comment-panel.ts`: otsikkolaskuri = AVOIMET. Testit ajettu: Vitest-jsdom (`tests/t237-comment-point-modal.test.ts`) + Bun (`server/comment-images.test.ts` naapurina). Kerros: `src/logic/` + `src/ui/` + `src/map/` + `server/`. Käyttäjä: talkoolainen ilmoittaa, järjestäjä kuittaa. | V263,V245,V13,T237,T340 |
| T365 | ✓ | **[Pieni]** Fix B149 — `e2e/t349-map-surface-theme.spec.ts`:n fixture jälkigeometrian aikakauteen. Anna neljälle merkille ERI koordinaatit smtb-30:n varrelta niin että ne osuvat pätkien km-väleihin (`s-valmis` 200–1400, `s-kesken` 1700–2900) MYÖS kun jäsenyys ratkeaa kohtisuoralla etäisyydellä pätkän jälkeen (V259) — nykyinen `65.62, 27.62` ∀ merkille tekee neljästä merkistä yhden pisteen ∴ eksklusiivinen sääntö antaa kaikki yhdelle pätkälle. Koordinaatit ! lukea reitin omasta geometriasta (`public/`-GPX), ⊥ arvata käsin: arvattu piste voi pudota `SHARED_THRESHOLD_M`:n ulkopuolelle & testi kaatuisi toisesta syystä. Testin VÄITE ⊥ muutu (karttapinnan värit identtiset teemojen välillä, V253) — vain sen data. Hyväksymisehto: 3/3 vihreä & `.segment-label--done` ∃ molemmissa teemoissa. Kerros: `e2e/`. | V253,V259,B149,B136 |
| T366 | ✓ | **[Suuri]** Fix B150+B152 — huomion luonti luonnoksena (V264). (a) `src/ui/comment-point-modal.ts` `openCreate`: modaali + kartalle RAAHATTAVA luonnospinni (`L.marker({draggable:true})`, sama CommentPin-visuaali DESIGN.md §K + `--draft`-tila) — raahaus päivittää luonnoksen lat/lonin, modaali ⊥ sulkeudu raahauksen ajaksi (mobiilissa modaali ⊥ saa peittää pinniä: sheet alas, kartta näkyviin). (b) Kuvat luonnoksessa PAIKALLISIA: `File[]`-lista + `URL.createObjectURL`-thumbit + ✕ per kuva; ⊥ POSTia ennen `Lähetä`ä. `URL.revokeObjectURL` sulkiessa (vuoto mobiilissa on oikea muistivuoto, ⊥ kosmetiikkaa). (c) `Lähetä`: `postComment` → jokainen kuva `addCommentImage` järjestyksessä → `onChanged` → SULJE modaali & poista luonnospinni (oikea pinni tulee refreshistä). Osittainen epäonnistuminen (huomio meni, kuva ⊥): huomio JÄÄ & virhe kertoo mitkä kuvat puuttuvat — ⊥ hiljaista pudotusta, ⊥ myöskään koko huomion perumista (teksti on arvokkaampi kuin kuva). (d) Esc/peruutus: pinni katoaa, mitään ⊥ tallennettu. Testattavuus: Taso 2 Vitest-jsdom — luonnoksen kuva ⊥ kutsu `addCommentImage`ä ennen Lähetä-klikkiä; Lähetä kutsuu postCommentin JA kuvat & sulkee; peruutus ⊥ kutsu kumpaakaan. Taso 3 Playwright: raahaus siirtää pinniä & tallennettu huomio on uudessa paikassa. Kerros: `src/ui/` + `src/map/`. Käyttäjä: talkoolainen. | V264,V262,V245,T237,T338 |
| T367 | ✓ | **[Keski]** Fix B151 — huomion omistajuus & siirto (V265). (a) `server/db.ts`: `comments.created_by TEXT` idempotentti `ALTER TABLE`-migraatio (V121-tyyli). (b) `server/routes/comments.ts`: POST tallentaa `created_by = session.user_id ?? session.talkoolainen_code`; UUSI `PUT /:id` (lat/lon, myös text/iconId) & `DELETE /:id` löysää `requireRole('admin','järjestäjä')` → **omistaja TAI järjestäjä**, gate luetaan rivin `created_by`sta (⊥ bodystä — client ⊥ saa nimetä itseään omistajaksi). `created_by IS NULL` ⇒ vain järjestäjä. (c) `src/logic/comments.ts`: `updateComment(id, {lat,lon})` + `Comment.createdBy`. (d) UI: olemassa olevan huomion pinni on raahattava kun `canEdit(comment)`; `comment-point-modal`in 🗑 näkyy samalla ehdolla (nyt `canDelete: () => getRole() === 'järjestäjä'`). Raahaus → `PUT` → 403 palauttaa pinnin takaisin & kertoo syyn (⊥ hiljainen paluu: käyttäjä luulisi siirron onnistuneen). Testattavuus: Taso 4 Bun (`server/test-fixtures.ts`) — tekijä siirtää omansa 200, vieras talkoolainen 403, järjestäjä 200, `created_by` NULL + talkoolainen → 403. Taso 2 Vitest-jsdom: 🗑 näkyy omalle & järjestäjälle, vieraalle ⊥. Kerros: `server/` + `src/logic/` + `src/ui/` + `src/map/`. Käyttäjä: talkoolainen (korjaa omansa), järjestäjä (kaikki). | V265,V13,V121,T221,T366 |
| T368 | ✓ | **[Pieni]** Fix B152(b) — tallennetun kuvan poisto. `DELETE /api/comments/:id/images/:imageId` — oikeus sama kuin huomion muokkauksella (omistaja + järjestäjä, V265) ⊥ löysempi: kuva on todiste. UI: `comment-point-modal`in galleria-thumbiin ✕-nappi (≥44px hit-area §R, ⊥ peitä kuvaa kokonaan) + `confirm`; poiston jälkeen galleria päivittyy paikallaan ⊥ sulje modaalia. Kuvaton huomio ⊥ näytä tyhjää galleriaa (nykysääntö säilyy). Testattavuus: Taso 4 Bun — omistaja poistaa 200 & rivi katoaa `comment_images`ista, vieras 403, tuntematon imageId 404. Taso 2 Vitest-jsdom: ✕ kutsuu API:a & thumb katoaa. Kerros: `server/` + `src/logic/` + `src/ui/`. Käyttäjä: molemmat. | V265,V246,T338,T367 |
| T369 | ✓ | **[Pieni]** Fix B153 — järjestäjä NÄYTTÄÄ missä huomio on. `src/ui/comment-panel.ts` rivi + `markers-wiring`in `onFocus`: nyt panorointi avaa myös modaalin ∴ kartta jää sen alle juuri kun käyttäjä halusi KATSOA karttaa. Erota kaksi tekoa: rivin klikkaus = **näytä kartalla** (`map.setView(lat,lon,16)` + pinnin korostuspulssi ~2 s, `.comment-pin-located`), rivin oma nappi/⋯ = avaa huomio. Sama kuvio kuin talkoolaisen keräyslistalla (`segment-view.ts:317`: näytä kartalla, ⊥ hautaa modaaliin) & pätkän nimilapulla (T347). Korostus ! erottua kuittaus-himmennyksestä (V263) ⊥ kilpailla sen kanssa. Testattavuus: Taso 2 Vitest-jsdom — rivin klikkaus kutsuu `onFocus`in oikealla lat/lonilla & EI avaa modaalia; nappi avaa. Taso 3 Playwright: klikkaus → kartta liikkuu & pinni pulssaa. Kerros: `src/ui/` + wiring. Käyttäjä: järjestäjä. | V266,V245,T340,T347 |
| T370 | ✓ | **[Pieni]** Fix B154 — talkoolaisen oma huomio ⊥ synny harmaana (V266). `src/app/markers-wiring.ts`: `commentLayer.setFocusActive(true)` talkoolaisen automaattifokuksesta POISTETAAN — himmennys jää VAIN järjestäjän eksplisiittiseen pätkäkorostukseen (`segments-wiring` `setFocusSegment`). Regressio syntyi T237(d):n integraatiossa 2026-07-27: V243 luettiin "fokus ⇒ ∀ huomio kontekstia", mutta talkoolaisen fokus on automaatti jota hän ⊥ kytkenyt ∴ hänen oma tuore havaintonsa ilmestyi kartalle harmaana. Testattavuus: Taso 2 Vitest-jsdom (`tests/t237-comment-focus-dim.test.ts` laajennus) — talkoolaispolku ⊥ himmennä; järjestäjän korostus himmentää edelleen. Kerros: `src/app/`. Käyttäjä: talkoolainen. | V266,V243,T237 |
| T371 | ✓ | **[Keski]** Jaettu left-panel section-header -apuri (V267) — poistaa V61-patternin 3 rinnakkaistoteutusta. Uusi `src/ui/section-header.ts`, puhdas DOM (⊥ Leafletia, ⊥ moduulitilaa): `createSectionHeader(opts:{name,collapsed,countClass?,onToggle}) → {el,setCollapsed(c),setCount(text)}`. Tuottaa TÄSMÄLLEEN sen DOM:n jolle CSS jo on (`style.css:4219-4242`): `div.left-panel-section-header[role=button][tabindex=0][aria-expanded]` > `span.section-header-toggle` (`▼` auki / `▶` kiinni) + `span.section-header-name` + valinnainen count-span (+ `countClass`). Klikkaus JA `Enter`/`Space` → `onToggle`: header on jo `role=button` (`area-panel.ts:51-52`) mutta ⊥ kuuntele näppäimistöä ∴ rooli valehtelee tänään. `aria-expanded` päivittyy `setCollapsed`issa — nyt vain SignLibraryPanel asettaa sen, kerran renderissä. Migraatio 3:een, KÄYTTÄYTYMINEN ⊥ MUUTU (oletukset säilyvät: Merkkikirjasto auki, Reittipätkät kiinni, Alueet kiinni). (a) `sign-library-panel.ts:75-87` — innerHTML-template pois; V126:n "ei accordionia" koskee Suosikit/Muut-VÄLIOTSIKOITA (kommentti :100), ⊥ section-headeria ∴ ⊥ poista haitaria. (b) `segment-panel.ts:290-307` + `applyCollapsed():272-278` — säilytä `.btn-segment-toggle` lisäluokkana, nimi+count yhtenä stringinä `Reittipätkät (N)` kuten nyt. (c) `area-panel.ts:49-71,90-95,110` — 3 inline `style.cssText`-riviä pois & `.section-toggle-icon` → `.section-header-toggle`; `countClass:'area-section-count'` PAKOLLINEN (`tests/t109-area-panel.test.ts:61` lukee sen); `toggleCollapse():92` ⊥ enää querySelectoroi ikonia vaan kutsuu `setCollapsed`. Footerin (`btn-area-add`) inline-tyylit ∉ tämä task — eri sopimus (DESIGN.md §K Section footer), oma rivi myöhemmin. Testattavuus: Vitest-jsdom, uusi `tests/t371-section-header.test.ts` (DOM-rakenne, ikonin vaihto, `aria-expanded`, Enter/Space). Olemassa olevat area-/segment-/sign-library-testit ! pysyä vihreinä ILMAN muutoksia — se on migraation mittari. Kerros: `src/ui/`. Käyttäjä: järjestäjä. | V61,V267 |
| T372 | ✓ | **[Pieni]** Huomiot-osio haitariksi — V61-rikkomuksen korjaus. `src/ui/comment-panel.ts:33-41`: `.left-panel-section-title.comment-panel-title`-div → `createSectionHeader` (T371). Otsikko pysyy muodossa `Huomiot (N)`, N = AVOIMET (V263, `isOpenNote`-suodatus :70-72) — ⊥ "(N avointa)": V61 määrää count-muodoksi `(3)` & naapurit ovat `Alueet (0)` / `Reittipätkät (0)` ∴ oma sanamuoto kumoaisi juuri sen yhtenäisyyden jonka vuoksi task on olemassa. Tyhjä lista: `Huomiot` ilman lukua, kuten nyt. `countClass:'comment-panel-title'` PAKOLLINEN — `tests/t237-comment-point-modal.test.ts:282,291,420` & `e2e/t237-huomio.spec.ts:38` nojaavat siihen; HUOM nykyiset assertiot lukevat KOKO otsikon (`'Huomiot (2)'`) YHDESTÄ elementistä ∴ kun nimi & count jakautuvat kahteen spaniin, assertiot ! lukea header-elementin `textContent` (tiedossa etukäteen, ⊥ rakentajan yllätys). Oletustila KIINNI = sama kuin Reittipätkät & Alueet: 240px sivupalkissa (B104-oppi) 4 aina-auki-listaa syö pystytilan & avointen määrä näkyy silti otsikosta ∴ tilannekuva ⊥ katoa kiinni-tilassa. Tila ⊥ persistoidu localStorageen — naapurit eivät persistoi, poikkeus olisi uusi epäjohdonmukaisuus. DESIGN.md §K CommentPanel (~603-605) **Otsikko**-kohta ! päivittää: `.left-panel-section-title`-viittaus → V61-section-pattern + kiinni-oletus. `style.css:5484` `.comment-panel-list` — varmista ettei poistettu title jätä orpoa marginaalia. Testattavuus: Vitest-jsdom (`tests/t237-comment-point-modal.test.ts` laajennus: kiinni-oletus piilottaa listan, klikkaus avaa, laskuri näkyy kiinnikin); `e2e/t237-huomio.spec.ts` ! avata haitari ennen lista-assertioita. Riippuu T371. Kerros: `src/ui/`. Käyttäjä: järjestäjä. | V61,V263,V267 |
| T373 | ✓ | **[Keski]** Section-header = oikea `<button>` & 44×44px (V268, DESIGN.md §A). Nyt `div[role="button"]` ~28px korkea (padding `8px 10px` + 11px rivi) 4 osiossa — §A sanoo `44×44px` PAKOLLINEN kaikille interaktiivisille. Juurisyy ⊥ ole CSS vaan TAGI: kosketusvahti (`e2e/critical-paths.spec.ts:485`) valitsee `page.locator('button')` & assertoi `violations.toHaveLength(0)` (`:501`) ∴ `div[role=button]` ⊥ koskaan mitattu — rikkomus eli vahdin katveessa T106:sta asti. (a) `src/ui/section-header.ts`: `div` → `<button type="button">`; `role`+`tabindex` POIS (natiivit) & **keydown-kuuntelija POIS** — natiivi nappi laukaisee `click`in jo Enteristä/Spacesta ∴ kuuntelijan jättäminen togglaisi KAKSI kertaa per painallus = ei näkyvää muutosta (hiljainen regressio, testi ! lukita kutsumäärä 1:een). `aria-expanded` säilyy. (b) `src/style.css` `.left-panel-section-header`: `min-height:44px; box-sizing:border-box; width:100%; background:none; border:0; border-bottom:1px solid var(--border-subtle); color:inherit; text-align:left; font:inherit` + säilytä 11px/700/uppercase/letter-spacing — globaali `button`-sääntö (`padding:6px 12px`, DESIGN.md §A "LIIAN PIENI") ⊥ saa vuotaa headeriin. (c) `e2e/critical-paths.spec.ts:485`: valitsin → `page.locator('button, [role="button"]')` (V268). Jos laajennus paljastaa MUITA rikkomuksia (`comment-point-modal.ts:281,453`, `marker-detail-modal.ts:196`, `sign-template-modal.ts:271` ovat `role=button`; modaalit kiinni sivun latauksessa ⇒ `boundingBox()` null ⇒ ohittuvat, mutta varmista) → oma §B-rivi per rikkomus, valitsinta ⊥ kapeuteta vihreän saamiseksi. (d) `tests/t371-section-header.test.ts`: `role`/`tabindex`-assertiot → `tagName === 'BUTTON'` + `type === 'button'`; Enter/Space-testi ! varmistaa `toHaveBeenCalledTimes(1)` (jsdom ⊥ synnytä natiivia click-defaultia keydownista ∴ testi ! kutsua `.click()`iä erikseen — ⊥ oleta jsdomin käyttäytyvän kuin selain, ja lisää siksi Playwright-varmistus näppäintoggle-kerroista). (e) DESIGN.md: §A touch-taulukkoon rivi `.left-panel-section-header` ✓, §K Section pattern -taulukon Header-rivi → `min-height:44px`. HINTA (tietoinen): 4 osiota × ~16px = ~64px pystytilaa pois 240px sivupalkin listoilta — §A on ehdoton, & T372:n kiinni-oletukset vapauttivat tilaa juuri tähän. V135-jännite: header ⊥ ole `.btn`-variantti vaan rakenne-elementti — kirjaa poikkeus DESIGN.md §A:han ettei seuraava lukija yritä pakottaa sitä variantiksi. Testattavuus: Vitest-jsdom (`t371`-laajennus: tagi, type, ei tuplakutsua) + Playwright (kosketusvahti kattaa nyt headerit; `e2e/t237-huomio.spec.ts` haitarin toggle säilyy). Kerros: `src/ui/` + CSS. Käyttäjä: järjestäjä (sivupalkki on roolisuojattu) — mutta §A ei ole roolikohtainen: sama header renderöityy 375px-viewportissa. | V268,V61,V267,V135 |
| T374 | ✓ | **[Keski, korjaa B157] ERÄ 1 — tuotantoon heti (tapahtuma PÄÄLLÄ).** Reitin piilotus vie pätkät mukanaan (V269). NYKYTILA: `RouteVisibilityControl.applyVisible` (`src/map/route-visibility-control.ts:55-63`) piilottaa polylinen + kutsuu `markerManager.setVisibleRoutes(ids)`, mutta `SegmentOverlay` ⊥ kuule reittinäkyvyyttä lainkaan ∴ piilotetun reitin pätkäviivat (T336-kolmikko) & nimilaput jäävät kartalle leijumaan. (a) UUSI puhdas predikaatti `segmentVisibleOnRoutes(seg, visibleIds): boolean` → `src/logic/` (⊥ jätä sääntöä map-kerrokseen; reititön V139 → aina `true`). (b) `SegmentOverlay.setVisibleRoutes(ids: string[])` V137-tyylinen setteri, kutsu ENNEN `update()`:a kuten `setContextOwn`; `update()` skippaa pätkän & sen lapun kun predikaatti false. (c) `applyVisible` kutsuu `segmentOverlay.setVisibleRoutes(ids)` + `update()` (wiring `src/app/segments-wiring.ts`). Testattavuus: Taso 1 Vitest-pure (predikaatti: osuma / ei-osuma / reititön ⊥ katoa / tyhjä visibleIds) + Taso 3 `e2e/segments.spec.ts` (piilota reitti → pätkäviiva & nimilappu katoavat, reititön tehtävä säilyy). Kerros: `src/logic/` (uusi predikaatti) + `src/map/segment-overlay.ts` + `src/map/route-visibility-control.ts` + `src/app/segments-wiring.ts`. Käyttäjä: järjestäjä. Riippuvuudet: — (pohja muille). | V269,V137,V139,V211,B157 |
| T375 | ✓ | **[Keski, korjaa B158] ERÄ 1 — tuotantoon heti.** Järjestäjän korostus himmentää myös pätkäviivat (V270). NYKYTILA: `setFocusSegment` → `marker-focus.focusState` himmentää VAIN merkit; pätkäviivan himmennys (`contextSegmentStyle`, `segment-overlay.ts:41`, `CONTEXT_DIM_OPACITY 0.22`) laukeaa VAIN `contextOwnId`:stä = talkoolaisen automaattifokus (V142) ∴ järjestäjän `◎ Korosta vain tämä pätkä` (T335/T356) jättää muut pätkäviivat täyteen kirkkauteen. DESIGN §K MarkerFocus VÄITTÄÄ jo päinvastaista — koodi ⊥ toteuta väitettään. (a) `contextSegmentStyle` saa `locked: boolean` -parametrin joka ohjaa `interactive`-lippua ERILLÄÄN opacity-himmennyksestä (V270 — nyt yhdessä boolissa, se on solmu joka esti järjestäjän himmennyksen); sama `segmentLabelOptions`ille. (b) `segments-wiring.ts:114 setFocusSegment` asettaa `segmentOverlay.setContextOwn(seg?.id)` samalla kutsulla kuin `markerManagerRef.current?.setFocusSegment(...)` → yksi fokuskäsite kahdelle laukaisimelle. (c) Talkoolainen `locked=true` — nykykäytös ⊥ muutu (V142/V93/V150 tapahtumakriittinen); järjestäjä `locked=false` = himmeä MUTTA klikattava. Testattavuus: Taso 1 Vitest-pure (`locked=true` → `interactive:false`; `locked=false` → himmennetty & `interactive:true`; ∀ dimLevel-portaalla) + Taso 3 `e2e/critical-paths.spec.ts` "merkkien korostus" -laajennus (korostus päällä → muun pätkän viiva himmeä JA klikattava järjestäjänä) + V142-regressiovahti talkoolaiselle. Kerros: `src/map/segment-overlay.ts` + `src/app/segments-wiring.ts` + `src/app/markers-wiring.ts`. Käyttäjä: järjestäjä (talkoolaisen käytös ⊥ muutu). Riippuvuudet: —. | V270,V142,V93,V150,V243,B158 |
| T376 | ✓ | **[Suuri] ERÄ 2.** UUSI `src/logic/map-filter.ts` — yksi kanoninen suodatinpredikaatti (V271). Tila: `MapFilter = { visibleRouteIds: string[], isolatedSegmentId?: string, markerStatuses: Set<MarkerStatus>, segmentStates: Set<'ei_alkanut'\|'kesken'\|'valmis'>, dimLevel: 'kevyt'\|'vahva'\|'piilota', showGaps ⊥ (V273 poisti akselin) }`. Funktiot `markerVisibility(marker, filter, ctx) → 'full'\|'dim'\|'hidden'` & `segmentVisibility(seg, filter, ctx) → 'full'\|'dim'\|'hidden'` — kolmiarvoinen, ⊥ boolean (V271). KRIITTINEN: jäsenyys tulee `segment-membership.ts`in `markersForSegment`ista & `focusState`ista/`segmentVisibleOnRoutes`ista (T374) — `map-filter` KOOSTAA, ⊥ keksi omaa jäsenyyssääntöä (B114–B129 = 9 kertaa sama juurisyy). `dimLevel` on JÄRJESTÄJÄN asetus (V243-amend): talkoolaisen polku kutsuu samaa moduulia mutta kiinteällä `'kevyt'`illa ∴ kenttänäkymä ⊥ muutu järjestäjän säädöstä. Tyhjä `markerStatuses`/`segmentStates` ⊥ ole saavutettava tila (V272) — setterit torjuvat viimeisen poiston. Persistointi `localStorage('karttamaster-map-filter')` (V5-kuvio), vioittunut/vanha JSON → oletukset ⊥ kaadu. HUOM CLAUDE.md: localStorage-testit AINA `vi.stubGlobal` (Node v26 -konflikti). Testattavuus: **Taso 1 Vitest-pure** — ∀ akseli erikseen + kombinaatiot (isolointi + status-suodatin yhtä aikaa; `dimLevel='piilota'` + isolointi → `hidden` ⊥ `dim`; viimeisen statuksen poisto torjutaan; reititön tehtävä reittisuodattimessa; korruptoitu localStorage). Kerros: `src/logic/`. Käyttäjä: molemmat. Vaatii T374,T375. | V271,V243,V259,V269,V272,V5 |
| T377 | ✓ | **[Suuri, UI] ERÄ 2.** **Suodatinbar** — UUSI `src/ui/map-filter-bar.ts` (puhdas DOM, ⊥ Leafletia → Vitest-jsdom). PM-päätös 2026-07-28 **KORVAA** aiemman "karttatasot-nappi laajenee paneeliksi" -linjan: suodatin on ohut bar headerin & kartan VÄLISSÄ, aina näkyvissä ⊥ piilossa napin takana. Sisältö: select-dropdownit **Reitit** (näytä/piilota + "vain tämä" -oikotie, V6 säilyy) · **Pätkät** (isolointi = TILA+nollaus ⊥ toinen laukaisin — valinta tehdään kartalta/modaalista T335/T356; + tila-monivalinta ei aloitettu/kesken/valmis `segmentLineState`-ämpäreistä) · **Merkit** (status-monivalinta, rivit §C `--status-*`-tokeneilla ∴ legenda vastaa karttaa, V216) · **Himmennys** (kevyt/vahva/piilota, oletus vahva, V243-amend) + AKTIIVILASKURI + ✕"Nollaa suodattimet" + BANNERI kun tila ≠ oletus (V272 — persistoitu suodatin ⊥ saa lukeutua kadonneena datana). Mobiili ≤480px: bar → oma valikko/bottom sheet (V114: paneeli ⊥ saa varastaa kartan leveyttä; vaikutusta ! nähdä säätäessä). `RouteVisibilityControl` LUOVUTTAA reittilistan barille — sen trigger+panel-DOM (`route-vis-trigger`/`route-vis-panel`) poistuu, mutta polyline-sovellus, `markerManager.setVisibleRoutes`, `getActiveRoute`/`getActiveTotalM` (ProgressBar/StatusPanel-sopimus) JÄÄVÄT: bar päättää, kontrolli soveltaa (V271). V137 idempotentti render (`replaceChildren()`). §A/V268: 44×44px ∀ kontrollille. V201: pohjakartan slider on eri pane ∴ ⊥ kertolaskua. DESIGN.md: UUSI §K-sopimus `MapFilterBar` + VANHENTUNEEN §K "Route-bar" -kohdan korjaus (kuvaa T286:ta edeltävät pyöreät pillit). Auto-ux → `/karttamaster-ux`. Pystytilan hinta ~44px kartalta = tietoinen valinta (löydettävyys > pikselit). Testattavuus: Taso 2 Vitest-jsdom (dropdownit renderöityvät, monivalinta päivittää tilan, viimeistä ⊥ voi poistaa, nollaus palauttaa oletukset, aktiivilaskuri, banneri ilmestyy/katoaa) + Taso 3 `e2e/critical-paths.spec.ts` uusi "Suodatinbar" (status-suodatin päälle → vain suodatetut merkit `full`ina; reload → suodatin & banneri säilyvät; kosketusvahti V268 kattaa barin automaattisesti). Kerros: `src/ui/` (uusi) + `src/map/route-visibility-control.ts` (kutistuu) + `src/app/`-wiring + CSS. Käyttäjä: järjestäjä (desktop ensisijainen, mobiili välttävä). Vaatii T376. | V272,V271,V243,V6,V137,V268,V114,V201,V216 |
| T378 | ✓ | **[Pieni, korjaa B159] ERÄ 2.** Aukkorender POIS (V273) — PM-päätös 2026-07-28: aukko luetaan paljaasta reittiviivasta, ⊥ omasta visualisoinnista. POISTA `computeGapRanges` (`src/map/segment-overlay.ts:301`) + `GAP_COLOR` + aukkoviivojen piirto `update()`:sta + niiden aukko-caset testeistä (`t217-segment-overlay-routeless`, `t299-segment-primary-route`) — kuollutta kuvaa jota ⊥ ole koskaan nähty (`#94a3b8 @ opacity 0.3` maastokartalla). Sivutuote: arkkitehtuurivelka (puhdas laskenta `src/map/`ssä ∴ Leaflet roikkui Taso 1 -testeissä) ratkeaa poistolla ⊥ siirrolla — ⊥ luo `src/logic/segment-coverage.ts`:ää. VARMISTA poiston jälkeen: pätkä erottuu paljaasta reitistä §K SegmentOverlay alfa-alarajalla (≥3:1, alaraja 0.7 UX-audit 2026-07-27) ∀ pätkätilassa — jos ⊥ erotu, se on PÄTKÄN kontrastikorjaus (oma §T), ⊥ aukon paluu. DESIGN.md §K SegmentOverlay: "Aukko (gap)" -rivi → korvataan V273:n muotoilulla. Testattavuus: Taso 1 (poistetut testit pois, jäljelle jäävät vihreinä) + Taso 3 visuaalinen tarkistus `e2e/segments.spec.ts` (ei aukkoviiva-elementtejä kartalla). Kerros: `src/map/segment-overlay.ts` + testit + DESIGN.md. Käyttäjä: järjestäjä. Vaatii — (voi ajaa ERÄ 2:ssa milloin vain). | V273,V211,B159 |
| T379 | ✓ | **[Pieni] ERÄ 2.** Talkoolaisen kapea suodatin — SAMA `map-filter-bar` (T377) yhdellä valinnalla: `Näytä: kaikki \| vain asettamattomat` (esiasetus `markerStatuses={suunniteltu}`, T376:n predikaatti ∴ ⊥ omaa suodatinlogiikkaa). ⊥ paneelia, ⊥ uutta yläpalkkinappia: V155 lukitsee `#toolbar-actions` = TASAN 3 nappia. Bar näkyy KARTTA-moodissa (siellä suodatinta tarvitaan) ja koti-moodissa merkkilistan yllä (V184-tab-shell). VISION max-2-nappia: tämä on LUKEMISEN apu ⊥ kriittisen flown askel ∴ ⊥ saa työntää "✓ Aseta" -nappia kauemmas eikä kasvattaa heron korkeutta. Talkoolaisen himmennys pysyy kevyt-portaassa & read-only-lukittuna (V243-amend, T375:n `locked=true`). V266 säilyy: talkoolaisen OMA työ (huomiot) ⊥ himmene suodattimesta — suodatin koskee merkkejä. Testattavuus: Taso 2 Vitest-jsdom (toggle päällä → vain `suunniteltu`-status `full`ina, muut `dim`; V155-regressiovahti `#toolbar-actions` lapsimäärä pysyy 3; hero-korkeus ⊥ kasva) + Taso 3 `e2e/segments.spec.ts`. Kerros: `src/ui/map-filter-bar.ts` (rooli-haara) + `src/ui/segment-view.ts` + `src/app/markers-wiring.ts`. Käyttäjä: talkoolainen. Vaatii T376,T377. | V155,V184,V266,V271,V243 |
| T380 | ✓ | **[Suuri] ERÄ 1 — kommenttisysteemin frontend-poisto (V275).** PM-päätös 2026-07-29 tuotantodatan nojalla (`backups/karttamaster-20260729-042200.db`, 194 merkkiä / 10 pätkää): `comments`=1, `comment_images`=0, pätkäkommentteja=0, pistehuomioita=0 — & se AINOA kommentti on duplikaatti saman merkin `location_note`sta (V275). Ohjekenttä elää (`location_note` 6/194, `segments.description` 1/10), keskustelu ⊥ elä. POISTA tiedostot: `src/ui/comment-thread.ts` (273), `src/ui/comment-panel.ts` (174), `src/ui/comment-point-modal.ts` (552), `src/map/comment-layer.ts` (184), `src/logic/comments.ts` (207), `src/ui/image-downscale.ts` (AINOA kuluttaja `comment-point-modal.ts:8` — greppaa ennen poistoa, jos merkkikuvat ovat sillä välin ottaneet sen käyttöön ⇒ JÄTÄ). Kutsupaikat: `src/app/markers-wiring.ts:25-28` (4 importtia) + `:607` `comment-panel-container`-wiring; `src/ui/segment-view.ts:12` CommentThread-import + `:58` injektoitava kommentti-api; `src/ui/marker-detail-modal.ts:9` import + `:250` kommenttilanka-lohko; `src/ui/place-mode.ts` huomio-tila; `src/ui/segment-koti-tabs.ts` Kommentit-välilehti; `index.html` `#comment-panel-container`. V184-kolmikko Varustelista·Kaikki merkit·Kommentit → KAKSIKKO — T354/V257: sama komponentti elää MYÖS järjestäjän `SegmentDetailsModal`issa ∴ molemmat kuoret ! tarkistaa, `scrollerSelector`-haara ⊥ saa jäädä orvoksi. SÄILYY EHDOTTOMASTI: `markers.locationNote` (renderöinti `segment-view.ts:319`, `segment-hero.ts:161`, `marker-list.ts:199` noteDot; editointi `marker-detail-modal.ts:136-146`) & `segments.description` (`segment-view.ts:433`) — nämä ovat OHJEITA ⊥ keskustelua & `marker-detail-modal.ts:250` sanoo eron jo omassa kommentissaan. `segment-view.ts:28` overflow-nappi "Laita kommentti" → **"Lisää ohje"**: toiminto (avaa MarkerDetailModalin locationNote-kenttään) säilyy muuttumattomana, vain nimi korjautuu vastaamaan sitä mitä se aina teki. Poista testit `tests/t237-comment-focus-dim.test.ts`, `tests/t237-comment-point-modal.test.ts`, `e2e/t237-huomio.spec.ts` & `tests/t264-segment-koti-tabs.test.ts`in kommentti-caset. V243/V245 (CommentLayer `setFocusActive`), V263 (avointen laskuri), V266 (talkoolaisen huomiot ⊥ himmene) VANHENTUVAT kommenttiosaltaan — merkitse vanhentuneeksi (T380), ⊥ poista riviä: V243:n & V266:n merkkiosuudet elävät yhä T376/T379:n suodattimessa. T340 & T372 kuolevat mukana. Testattavuus: Taso 1+2 vihreä ilman poistettuja tiedostoja (V184-tabiassertiot kolmikko→kaksikko) + Taso 3 `e2e/critical-paths.spec.ts` & `e2e/segments.spec.ts` vihreinä. Kerros: `src/ui/` + `src/map/` + `src/logic/` + `src/app/` + `index.html`. Käyttäjä: molemmat. | V275,V184,V257,V243,V266 |
| T381 | . | **[Keski] ERÄ 1 — kommenttisysteemin backend-poisto (V275).** POISTA `server/routes/comments.ts` (361) + `server/index.ts:14` `commentsRoutes`-mount + testit `server/comments.test.ts`, `server/comment-images.test.ts`, `server/comment-ownership.test.ts`. Taulut `comments` & `comment_images` (`server/db.ts:168-190` CREATE + `:250,:251,:255` ALTERit + `idx_comment_images_comment`): DROP vasta kun T380 on TUOTANNOSSA & vanha frontend ⊥ enää kutsu API:a — vanha selainvälilehti kutsuu poistettua reittiä ∴ 404 on parempi kuin 500, mutta pudonnut taulu on peruuttamaton. ENNEN DROPia: `./backup.sh` (fly-snapshot + off-site) & talteen se yhden rivin sisältö commit-viestiin — teksti on jo tallessa saman merkin `location_note`ssa sanatarkasti kattavampana ∴ tietoa ⊥ häviä, mutta todiste siitä ! olla kirjattuna. Migraatio `DROP TABLE IF EXISTS` (idempotentti, ajettava vanhaan & uuteen kantaan). Greppaa ettei `server/snapshot-data.ts`, `server/gpkg/`-export tai `server/marker-audit.ts` viittaa `comments`-tauluun — snapshot joka kysyy pudonnutta taulua kaatuu vasta yöllä cronissa. Testattavuus: Taso 4 `bun test server/` vihreä; `server/db-migration.test.ts` kattaa DROPin idempotenssin (aja migraatio kahdesti peräkkäin). Kerros: `server/`. Käyttäjä: — (infra). Vaatii T380. | V275 |
| T382 | ✓ | **[Pieni] ERÄ 2 — dokumentaatio seuraa päätöstä (V275).** `VISION.md`: §267 kommentti/huomio-systeemi & §268 este/blokki POIS — este/blokki ⊥ ole enää kommentti+ikoni-yhdistelmä vaan siirtyy §Avoimet 7:ään (karttamerkki-/POI-järjestelmä, suunnittelematta); §Avoimet 6 ("Kommentti-systeemi: suunnitellaan ennen toteutusta — vaikuttaa tietomalliin laajasti") SULJETAAN päätöksellä *ei rakenneta* + päivämäärä + todiste (1 kpl käyttöä, sekin duplikaatti ohjekentästä) ∴ seuraava lukija ⊥ herätä sitä henkiin tietämättä miksi se kuoli. `VISION.md:46`, `:272`, `:295` kommenttimaininnat → ohjekenttä. `COMPONENTS.md`: poista rivit CommentLayer (82), CommentPointModal (129), CommentPanel (130), CommentsAPI (154), ImageDownscale (128, jos T380 poisti sen); SectionHeader (105) & SegmentKotiTabs (117) kuluttajalistoista `Comment*` pois. `DESIGN.md` §K: CommentPanel-sopimus (~603-605) + CommentLayer- & CommentPointModal-sopimukset pois. Kerros: dokumentaatio. Käyttäjä: —. Vaatii T380,T381. | V275 |
| T383 | x | **[Keski] ERÄ 3 — inventaario↔kartta, markers-backfill (V276/V277).** 67 merkkiä on `template_id IS NULL` mutta niiden `label` täsmää SANATARKASTI `templates.label`iin: vasen×21, oikea×18, Ylös×5, Tuleva vasen×4, Alueella pyöräkilpailu×3, U-käännös oikea×2 + 14 yksittäistä (10,1 km · 26 asteen nousu · 31 asteen nousu · Kapeneva ja vaara · Luokan vaara · Myllyn harju · Myllyn laavu · Peikko · Pikku-syöte · Pumppaamo · Soininsuo · Tuleva oikea · U-käännös vasen · Ylös ja soininsuo). Migraatio `server/db.ts`-lohkoon muiden idempotenttien ALTERien tapaan: `UPDATE markers SET template_id=(SELECT id FROM templates t WHERE t.label=markers.label) WHERE template_id IS NULL AND (SELECT COUNT(*) FROM templates t WHERE t.label=markers.label)=1` — COUNT=1 -ehto on OLENNAINEN: kaksi templatea samalla labelilla = monitulkintainen ∴ ihmisen asia, ⊥ arvaa. ⊥ fuzzy tässä (V277), täsmäämättömät jäävät nulliksi & päätyvät T386:n listalle. Migraatio ! `console.log`ata päivitettyjen rivien määrä — hiljainen datamigraatio on TÄSMÄLLEEN se mitä 2026-07-25 opetti välttämään. ENNEN TUOTANTOAJOA `./backup.sh`. Testattavuus: Taso 4 `bun test server/` — `server/db-migration.test.ts` uudet caset: aja migraatio 2× peräkkäin (idempotenssi, 2. ajo päivittää 0 riviä), täsmäämätön label jää nulliksi, duplikaattilabel ⊥ päivitä kumpaakaan, jo linkatun merkin template_id ⊥ ylikirjoitu. Kerros: `server/`. Käyttäjä: — (infra, mahdollistaa T387:n). Vaatii —. | V276,V277 |
| T384 | x | **[Keski] ERÄ 3 — `src/logic/template-match.ts` fuzzy-ehdotusmoottori (V276/V277/V278).** Cherry-pick branchista `inventaario` (commit `9f1c06d`: `rankTemplates` + `tests/t289-template-match.test.ts`) & sovita mainiin — branch on 94 committia jäljessä ∴ poimi VAIN nämä kaksi tiedostoa, ⊥ `git merge inventaario`. `normalizeName(s)`: pienet kirjaimet, ä/ö→a/o, välimerkit+välilyönnit pois, "irtokyltti"/"lisäkilpi"-suffiksi pois (V278). `rankTemplates(itemName, templates) → {template,score}[]` laskevassa score-järjestyksessä, Levenshtein-toleranssi kirjoitusvirheille — tuotantodata sisältää "Ennako"/"Ennnako"/"Ennakko" & template-id:ssä ITSESSÄÄN on typo `ennako-oikea-135` ∴ toleranssi ! toimia molempiin suuntiin (virhe voi olla kummassa tahansa päässä). Puhdas laskenta, ⊥ DOM ⊥ Leaflet ⊥ fetch. Testattavuus: **Taso 1 Vitest-pure** (⊥ jsdom-docblockia — tiedosto ⊥ koske DOM:iin) — välilyöntiero "10,1km" vs "10,1 km" → ykköspaikka; irtokyltti-variantti osuu kantamuotoon (V278); typo-pari "Ennako"/"Ennakko" osuu; vieras nimi ("Taittopöytä") ⊥ saa korkeaa scorea; tyhjä templates-lista ⊥ kaadu; tulos on deterministisesti järjestetty (tasapeli ⊥ heilu ajojen välillä). Kerros: `src/logic/`. Käyttäjä: järjestäjä. Vaatii — (rinnakkain T383/T385:n kanssa). | V276,V277,V278 |
| T385 | x | **[Keski] ERÄ 3 — backend-tuki yhdistämiselle (V279).** (a) UUSI sarake `inventory_items.not_sign INTEGER NOT NULL DEFAULT 0` (idempotentti ALTER `server/db.ts`, `try/catch`-kuvio) = "tämä ⊥ ole merkki, älä kysy uudelleen". (b) `server/routes/inventory.ts` POST+PUT: `not_sign=1` & `template_id` yhtä aikaa → 400 (V279). (c) UUSI `POST /api/inventory/merge {sourceId,targetId}` → summaa `source.qty` targettiin, poistaa sourcen, palauttaa päivitetyn targetin; eri `location_id` ⇒ 400 (paikka on MERKITSEVÄ: kärryn & varaston kappaleet ⊥ summaudu vahingossa — tuotannossa "26 asteen nousu"×2 elää Kärryssä JA Kelkkavarastossa & ne ⊥ ole duplikaatteja); `sourceId===targetId` ⇒ 400; puuttuva id ⇒ 404. Aidot duplikaatit joita merge palvelee: "Kapeneva tie, kolmio"×2 samassa paikassa, "Huolto, service, 200m"×2 samassa paikassa (toinen linkattu `huolto-service-200m` qty 4, toinen linkittämätön qty 5). (d) GET /api/inventory palauttaa `not_sign`in. Testattavuus: **Taso 4** `bun test server/` `server/test-fixtures.ts`-apureilla (`seedTestUsers`+`authHeaders`, ⊥ omaa fixture-tehdasta): merge summaa & poistaa sourcen, eri paikka→400, itseensä→400, puuttuva→404, ristiriita `not_sign`+`template_id`→400, ALTER idempotentti (2× ajo), rooligate talkoolainen→403 (V163). Kerros: `server/`. Käyttäjä: järjestäjä. Vaatii — (rinnakkain T383/T384:n kanssa). | V279,V276,V163 |
| T386 | x | **[Suuri, UI] ERÄ 3 — järjestäjän "Yhdistä"-työkalu (V277/V279).** UUSI `src/ui/inventory-merge-panel.ts` — ⊥ kasvata `src/ui/inventory-page.ts`:ää (873 riviä, jo pilkkorajalla; yhdistäminen on oma näkymä omalla tilallaan). Avaus inventaariosivun headerista edit-modessa (`.inv-mode-toggle`-naapuri): **"🔗 Yhdistä (N)"**, N = `template_id IS NULL AND not_sign=0` -rivien määrä — laskuri ITSE on työkalun arvo: järjestäjä näkee että työtä on jäljellä & milloin se loppui. Per rivi: nimi + määrä + paikka, `rankTemplates`in (T384) **top-3 ehdotusta** merkkivisuaalilla (`signVisual` uusiokäyttönä `sign-picker`istä, ⊥ omaa renderöintiä) + KOLME toimintoa: (a) **"Linkitä"** valittuun ehdotukseen → PUT template_id; (b) **"Luo merkkipohja"** → uusiokäyttää `SignTemplateModal` (T239) luontitilassa, luotu template linkittyy heti riviin & näkyy samalla kirjastossa+kartalla (V165-kuvio); (c) **"Ei merkki"** → PUT `not_sign=1`, rivi katoaa listalta pysyvästi (V279). Duplikaattiehdotus: jos SAMAN PAIKAN toinen rivi normalisoituu samaksi (T384) → "Yhdistä riviin X (määrät N+M)" → `POST /api/inventory/merge` (T385). **V277:** ⊥ esivalintaa, ⊥ "linkitä kaikki varmat" -massanappia — jokainen rivi kuitataan erikseen. Työkalu ! kestää keskeytys: jokainen kuittaus persistoituu HETI omalla pyynnöllään, ⊥ "Tallenna kaikki" -nappia ∴ sulkeminen kesken ⊥ hukkaa tehtyä työtä (101 rivin urakka ⊥ mahdu yhteen istuntoon). Epäonnistunut pyyntö → rivi JÄÄ listalle + virheteksti kertoo MIKSI (V21). Undo: `showToast` "Kumoa" (T253/V172/V173, client-only yksi slotti) — linkityksen kumous = PUT vanha template_id takaisin. XSS: kaikki nimet `textContent` (V164). DESIGN.md: UUSI §K-sopimus `InventoryMergePanel` (44px §A/V268, reuse accent/field-tint/border/danger §C ⊥ uusia tokeneja, tyhjätila "Kaikki rivit yhdistetty ✓" kun N=0) → auto-ux `/karttamaster-ux`. Testattavuus: **Taso 2 Vitest-jsdom** (`// @vitest-environment jsdom` ENNEN importteja) — lista näyttää vain linkittämättömät (linkatut & `not_sign` ⊥ näy), top-3 oikeassa järjestyksessä, "Ei merkki" poistaa rivin & laskuri päivittyy, epäonnistunut PUT jättää rivin listalle + näyttää virheen, **V277-regressiovahti: ⊥ massanappia DOM:issa**, V164-vahti (`<script>`-niminen rivi ⊥ suoriudu). Kerros: `src/ui/` (uusi) + `src/inventory.ts` (wiring) + CSS. Käyttäjä: järjestäjä (desktop ensisijainen — 101 rivin läpikäynti ⊥ ole kärry-mobiilityötä; mobiili välttävä VISION-linjan mukaan). Vaatii T384,T385. | V277,V279,V276,V164,V21,V268 |
| T387 | x | **[Keski] ERÄ 3 — kartalla/varastossa-laskenta (V276).** Cherry-pick branchista `inventaario`: `src/logic/marker-stock.ts` (`computeMarkerStock`, commit `3ed6d5b`) + "kartalla N" -badge (commit `096e8cc` — HUOM: ota SE versio, se muutti badgen emojista tekstilabeliksi; `432e27b` on vanhentunut emoji-versio). `computeMarkerStock(items, markers) → Map<templateId,{kartalla,varastossa}>` laskee VAIN `template_id`:llä (V276), ⊥ nimivertailulla. Status ratkaisee ämpärin: `ei_tarpeen` & `kerätty` ⊥ ole "kartalla" — kerätty merkki on takaisin varastossa. Näyttö inventaariorivillä vain merkki-riveille (`.inv-card-name-sign`), ⊥ tarvikkeelle. RIIPPUVUUSSYY: ilman T383:a 67 merkkiä puuttuisi luvuista ∴ luku olisi VÄÄRIN ⊥ vain vajaa — väärä luku on pahempi kuin ei lukua (järjestäjä tilaa liikaa kylttejä luottaen siihen). Testattavuus: Taso 1 Vitest-pure (kartalla/varastossa-jako per template, status-ämpärit, linkittämätön item → ⊥ laskuria, tuntematon template_id ⊥ kaada, tyhjä markers-lista) + Taso 2 Vitest-jsdom (badge vain merkki-rivillä). Kerros: `src/logic/` (uusi) + `src/ui/inventory-page.ts`. Käyttäjä: järjestäjä. Vaatii T383,T386. | V276 |
| T388 | ✓ | **[Suuri, KRIITTINEN] Pätkän luonnin karttapalaute + tuotanto-repro (V280, B162/B163).** (a) `src/ui/segment-panel.ts` `receivePoint` polku-haara (:176) ! kutsua karttacallbackia JOKAISESTA ankkurista: `onFirstPoint` → `onAnchorsChanged(anchors)` (myös `undoAnchor` :184 kutsuu). `src/app/segments-wiring.ts:143` pitää LISTAA ⊥ yhtä markeria (`tempCreationMarker` → `tempAnchorMarkers: L.CircleMarker[]`), kaikki `interactive:false` (B147 pysyy voimassa). `cancelCreation`/`finishPath` (:220) siivoavat KAIKKI — yksikin jäänyt kiekko on haamu seuraavassa luonnissa. Viimeinen ankkuri saa korostuksen (se on se jota "Poista viimeinen" koskee, vrt. `segment-creation-modal.ts:160`). (b) Esikatseluviiva ankkurien läpi — `buildTrackFromAnchors` on jo olemassa (`segment-panel.ts:196`), aja se joka klikillä & piirrä polyline ∴ järjestäjä näkee jäljen ENNEN "Valmis"-nappia ⊥ vasta tallennuksen jälkeen. (c) REPRO ENSIN paikallisesti (`bun run dev`) oireelle "koko pätkä ⊥ tallennu" (B163): fly-logi sulkee serverin pois ∴ vika on clientissä. Jos syy on eteenpäin-haku (`nextAnchorIndex(…, last.idx+1)` → `null` reitin suuntaa vastaan klikatessa), virhe ! nostaa näkyviin kartan tasolle (`callbacks.onNotify`) ⊥ vain modaalin pohjalle. **⊥ löysennä V258:n eteenpäin-sääntöä** — se ratkaisee kierroksen & B144 palaisi. Korjaa NÄKYVYYS ⊥ sääntö. (d) Vahdit: vitest-jsdom `segment-panel` — N ankkuriklikkiä → callback N kertaa & ankkurilista N; e2e `e2e/segments.spec.ts` — 3 klikkiä reittiä pitkin → `.segment-creation-marker` count 3 & esikatseluviiva olemassa ennen "Valmis". | V280,V258,B162,B163 |
| T389 | ✓ | **[Keski, UI] Reitin valinta luonnissa (V281, B164).** (a) `src/ui/segment-panel.ts`: UUSI `setVisibleRoutes(ids: string[] \| undefined)` (tila paneelissa) & `resolveClick` iteroi vain näkyvät reitit; tyhjä/kaikki-piilotettu → fallback `this.routes` (⊥ umpikujaa). Kytkentä `src/main.ts:118` — sama `setSegmentVisibleRoutes`-kanava jolla overlay jo kuulee suodattimen (V269/B157), ⊥ uutta kanavaa. (b) `CreationState.polku` saa `origin: {lat,lon}` (ensiklikin RAAKA koordinaatti) & `routeChoices: {id,label}[]` = näkyvät reitit joilla on osuma ≤`SHARED_THRESHOLD_M` originista. `segment-creation-modal.ts appendPathSection` renderöi pillerit (`.segment-creation-route-choice`, aktiivinen `.active`) kun `routeChoices.length ≥ 2` — yhdellä ehdokkaalla ⊥ pillereitä (⊥ kohinaa). (c) Pillerin klikki → `SegmentPanel.switchRoute(routeId)`: re-resolvoi ankkuri originista uudelle reitille, ankkurit → 1, `emitAnchors()` ∴ kartta seuraa heti (V280). Nollaus ! näkyä käyttäjälle (ankkurilista lyhenee) ⊥ tapahtua hiljaa. (d) Vahdit (vitest-jsdom): piilotettu reitti ⊥ lukitu ensiklikissä; kaikki piilotettu → luonti silti toimii; pillerit ⊥ näy yhdellä ehdokkaalla; vaihto nollaa ankkurit & kutsuu karttacallbackia. §A: pilleri ≥44px kosketuskohde (V268). | V281,V280,V271,B164 |
| T390 | . | **[Suuri, EPÄVARMA — aja `/ck:grill` ennen buildia] Pätkä joka jatkuu reitiltä toiselle (V282, V258-amend).** Järjestäjän tarve: reitit eroavat maastossa & saman talkoolaisen pätkän ! jatkua reitiltä A reitille B. Nyt `nextAnchorIndex` hakee vain lukitun reitin pisteistä ∴ klikki toisen reitin varrella → "Ei osumaa reitillä X eteenpäin" & luonti kuolee (T389 antoi reitin VAIHDON joka nollaa ankkurit — ⊥ jatkoa). **Ratkaistavat päätökset (grillin työ, ⊥ arvaa buildissa):** (1) Ankkuri `{idx}` → `{routeId, idx}` ∴ `buildTrackFromAnchors` ketjuttaa siivuja: kukin siivu monotoninen OMALLA reitillään (V258 säilyy siivun sisällä), `d` kumuloituu ketjun yli. (2) Vaihtopiste ! olla ≤`SHARED_THRESHOLD_M` MOLEMMISTA reiteistä — muuten jälki teleporttaisi & V259:n lähin-voittaa antaisi merkkejä pätkälle jonka alla ⊥ ole polkua. Ehto rikki → kieltäydy & kerro miksi (⊥ hiljainen suora viiva). (3) `startDist`/`endDist` + `primaryRouteId` (V211) menettävät merkityksensä monireittisenä: säilytä ne primaryn OMISTA ankkureista johdettuina legacy-/UI-arvoina & merkitse pätkä monireittiseksi; `validateNoOverlap` → pelkkä varoitus (V25-kuvio). (4) `routeIds` = kaikki ketjun reitit (jäsenyys jo olemassa, T299/V211). (5) Backend: `track` on jo JSON-sarake & `route_ids` lista ∴ migraatio todennäköisesti NOLLA — varmista `server/t360-track-persist.test.ts`illa ⊥ oleta. (6) Talkoolaisen km-akseli on jäljen oma `d` (V259) ∴ ⊥ muutu. (7) UI: ankkurilista näyttää vaihdon eksplisiittisesti ("→ Reitti 125"), kartan esikatselu (V280) piirtää ketjun yhtenä viivana. **Riippuu:** T388 ✓ (karttapalaute), T389 (reittipillerit — sama pilleririvi on luonteva vaihdon laukaisin). | V282,V258,V259,V281 |
| T391 | ✓ | **[Suuri, KRIITTINEN — tuotantodata väärin] Jäsenyyskynnys (V283/V259-amend, B165).** (a) `src/logic/segment-membership.ts:100-112`: geometriahaaraan kynnys — `if (winner && best <= MEMBERSHIP_THRESHOLD_M) add(...)`, muuten merkki jää orvoksi. Vakio (200 m) `segment-membership.ts`:ään nimettynä exportina ⊥ maagisena lukuna; `isCandidate` (reittijäsenyys) säilyy scopena. Eksplisiittinen tahto (`linkedMarkerIds`/`markerTypeFilter`) ohittaa kynnyksen kuten se ohittaa etäisyydenkin (V259) — järjestäjä joka poimii merkin käsin tietää enemmän kuin geometria. (b) `resolveTaskMarkers`-legacy-haara (V260, jäljetön pätkä) EI muutu — sillä ⊥ ole jälkeä johon mitata. (c) Järjestäjälle NÄKYVÄ orpolaskuri (V283): "N merkkiä ilman pätkää" siellä missä pätkälista on, klikki → korostus kartalle. ⊥ automaattista korjausta — orpo on työjono ⊥ virhe. (d) Vahdit: `tests/` — yksi pätkä + kaukainen merkki → orpo (ennen: omistettu); naapuripätkät jakavat rajamerkin ennallaan (⊥ regressiota V259:ään); eksplisiittinen linkki voittaa kynnyksen; jäljetön pätkä ennallaan. Tuotantodatalla ajettu regressio: G62 38→14, Pätkä 8 14→10, muut −0…−1. **Tuotantovaikutus:** talkoolaiset joilla on koodi (PATKA-5A, PATKA-9, PATKA-11-…) näkevät korjauksen jälkeen VÄHEMMÄN merkkejä — tarkista ettei kukaan ole jo kuitannut merkkiä joka siirtyy orvoksi (`marker_audit`). | V283,V259,V25,B165 |
| T392 | ✓ | **[Suuri] Merkin lähin reitti tallennetaan & rajaa jäsenyyden (V284, jatkaa T391/B165).** (a) `src/logic/segment-track.ts`: irrota `distanceToPathM(points: {lat,lon}[], lat, lon)` — sama kohtisuora projektio jota `distanceToTrackM` jo käyttää (delegoi siihen) ∴ reitti & jälki mitataan SAMALLA mittarilla (V284). (b) `src/logic/multi-route.ts`: UUSI `nearestRouteByPath(lat, lon, routes) → {routeId, distM} \| null`. ⊥ käytä `nearestPointIndex`iä (kärkipiste) — se on eri mittari. (c) `src/logic/types.ts`: `nearestRouteId?: string`, `nearestRouteDistM?: number` (valinnaisia = vanha data toimii). (d) `src/map/markers.ts`: `add()` & `nearestRouteAssignment()`/siirto täyttävät kentät — merkin siirto ! päivittää ne tai sääntö jää vanhaan sijaintiin. (e) `server/db.ts` idempotentti ALTER `markers.nearest_route_id TEXT` + `nearest_route_dist_m REAL`; `server/routes/markers.ts` POST/PUT persistoi & GET palauttaa (Bun-integraatiotesti kuten `t360-track-persist.test.ts`). (f) BACKFILL vanhoille 196 merkille: client-side kun GPX:t ovat ladattu, hiljainen `PUT` outboxin OHI (T361/B145-kuvio — taustamigraatio ⊥ saa nostaa reauth-overlaytä). Aja ENSIN kuivana tuotantokopiolla & vertaa lukemat ennen/jälkeen (odotus: −9 merkkiä yhteensä). (g) `segment-membership.ts`: `ROUTE_TOLERANCE_M = 25`, geometriahaaraan ehto `d <= (marker.nearestRouteDistM ?? Infinity) + ROUTE_TOLERANCE_M`; puuttuva kenttä → sääntö ohitetaan. Eksplisiittinen linkitys (V259) ohittaa tämänkin. (h) Vahdit: kahden rinnakkaisen reitin fixture — merkki reitin B varrella ⊥ päädy A:n pätkälle; jaetun osuuden merkki (reitit <25 m) pysyy; puuttuva kenttä → entinen käytös; siirretty merkki saa uudet arvot. | V284,V283,V259,V261,V25 |
| T393 | ✓ | **[Keski, logiikka] Varustelistan phase-tietoinen tyyppilaskuri (V285).** UUSI `src/logic/equipment-counts.ts` — ⊥ kasvateta `segments.ts`:ää (377 r). (a) `getEquipmentCounts(segment, markers) → {type, label, take, done, total}[]`: ryhmittele `m.type`, `ei_tarpeen` pois KAIKISTA luvuista (⊥ take, ⊥ done, ⊥ total — V285). `phase: 'asettaminen'` → `done = asetettu+tarkistettu+kerätty`, `take = suunniteltu`, `label = 'asetettu'`. `phase: 'purku'` → `done = kerätty`, `take = total−done`, `label = 'kerätty'`. `phase: 'tarkastus'` → sama kuin asettaminen (per-merkki-"tarkastettu"-statusta ⊥ ole, V91 — sama peruste jolla `getPhaseProgress` early-returnaa). Toteuta `COUNT_PHASE_TARGET`-lookupilla kuten `segments.ts:256`, ⊥ if-ketjulla — uusi phase ! olla yhden rivin lisäys. (b) `getEquipmentSummary(segment, markers) → {total, done, take, notNeeded}` sektioriville (T394). Puhdas laskenta: ⊥ DOM ⊥ Leaflet ⊥ localStorage. Testattavuus: **Taso 1 Vitest-pure** (⊥ jsdom-docblockia — tiedosto ⊥ koske DOM:iin) `tests/t393-equipment-counts.test.ts`: kaikki 5 statusta per tyyppi; kolme phasea samalla datalla ⊥ anna samaa lukua; `ei_tarpeen` ⊥ näy total/done/take:ssa; tyhjä markers-lista → tyhjä taulukko ⊥ kaadu; tyyppi jolla vain `ei_tarpeen`-merkkejä katoaa riveiltä kokonaan (⊥ jää `0×`-haamuriviksi); järjestys deterministinen. Kerros: `src/logic/`. Käyttäjä: talkoolainen. Vaatii —. | V285,V90,V91 |
| T394 | ✓ | **[Keski, UI] Varustelista näyttää mitä pakata & mikä on jo maastossa (V285, vaatii T393).** Kolme kutsupaikkaa `getEquipmentCounts`iin — nykyinen `for (const m of markers) counts.set(m.type, …)` (`segment-equipment.ts:44-45`, `equipment-modal.ts:80-82`) & chip-rakennus (`segment-details-modal.ts:355-370`) korvautuvat, ⊥ kolmea laskentaa. (a) **Rivi:** iso luku = `take` (`tabular-nums`, `text-primary`) + olemassa oleva merkkivisuaali + tyyppilabel + meta oikealla `.equipment-count-meta` (`11px text-muted`) `"N/M asetettu"` (purussa `"N/M kerätty"` — sana tulee T393:n `label`ista, ⊥ UI:n if-lauseesta). `done === 0` → meta jätetään renderöimättä (⊥ kohinaa kun mikään ⊥ ole vielä maastossa ∴ nykytila näyttää identtiseltä). `take === 0` → rivi saa OLEMASSA OLEVAN `.equipment-check--done`-kohtelun (yliviivaus + himmennys), ⊥ uutta CSS-tokenia. (b) **Sektiorivi** `.equipment-modal-progress`in viereen `.equipment-summary`: `"Pätkällä N merkkiä · M jo asetettu · ota mukaan K"` (+ `" · J ei tarpeen"` vain kun `J > 0`). (c) **Varustarkastus-kytkös (V285, KRIITTINEN):** `take === 0` -tyyppi POIS `allLabels`-listalta (`segment-equipment.ts:49-52`, `equipment-modal.ts:40`) ∴ `"Varustarkastus: N/M otettu"` täyttyy & V182:n "lähtövalmis"-signaali pitää paikkansa. Ilman tätä rasti jäisi vaatimaan tavaraa jota ⊥ oteta (V250: kuollut pinta). (d) DESIGN.md §K-amendit: `SegmentEquipment` (:591), `EquipmentModal` (:639), `SegmentDetailsModal` yhteenveto-chipit (:493). Reuse `§C` (`text-muted`, `field-tint`) — ⊥ uusia värejä; rivikorkeus 44px ennallaan (V268). Testattavuus: **Taso 2 Vitest-jsdom** (`// @vitest-environment jsdom` ENNEN importteja) `tests/t394-equipment-counts-ui.test.ts`: meta renderöityy oikein; `done=0` → ⊥ metaa DOM:issa; `take=0` → `--done`-luokka; sektiorivi laskee oikein & `ei tarpeen` -osa näkyy vain kun `J>0`; **täysi tyyppi ⊥ ole varustarkastuksen nimittäjässä**; sama data kolmeen komponenttiin → samat luvut (regressiovahti "kolme totuutta" -paluulle). localStorage (`varustarkastus.ts` V180) → `vi.stubGlobal`-mock (CLAUDE.md). Kerros: `src/ui/` (3 tiedostoa) + `src/style.css`. Käyttäjä: talkoolainen (ensisijainen — mobiili, metsä, pakkaaminen), järjestäjä (sama data pätkä-modaalissa). Vaatii T393. | V285,V182,V180,V268,V250 |
| T395 | ✓ | **[Pieni, logiikka] "Navigoi tähän" -URL puhtaana funktiona (V286).** UUSI `src/logic/nav-link.ts` — ⊥ kasvateta `navigation.ts`:ää: se omistaa pätkän SISÄISEN merkiltä-merkille-navigoinnin, tämä on ULOSPÄIN-handoff toiseen appiin (eri vastuu, eri elinkaari). (a) `navUrl(target: {lat: number, lon: number}) → string \| null`: `https://www.google.com/maps/dir/?api=1&destination=<lat>,<lon>&travelmode=driving`. Universal URL toimii Androidilla, iOS:llä & desktopilla SAMANA merkkijonona ∴ ⊥ platform-sniffausta, ⊥ `geo:`-intentiä, ⊥ Waze-haaraa (V286). Koordinaatit `toFixed(6)` (~11 cm — riittää metsämerkille & lyhentää URLia). (b) Validointi: ei-äärellinen luku, `lat` ∉ [-90,90] tai `lon` ∉ [-180,180] → `null` ∴ kutsupaikka jättää napin renderöimättä ⊥ tuota rikkinäistä linkkiä. (c) `navTarget(marker: Pick<SignMarker,'lat'\|'lon'>) → {lat, lon}`: NYT palauttaa merkin omat koordinaatit. Tämä on featuren AINOA laajennuskohta — VISION §Avoimet 8 (pysäköintipiste-POI) vaihtaa kohteen TÄSSÄ, ⊥ kutsupaikoissa (V286). Kirjoita peruste auki kommenttina tiedostoon jotta seuraava ⊥ hajota kohdetta kutsupaikkoihin. (d) ⊥ reittilaskentaa, ⊥ verkkokutsua, ⊥ backendiä, ⊥ uutta datamallia — tieverkkoon snappaus on nav-apin työ (perustelu V286 & VISION §Avoimet 8). Puhdas: ⊥ DOM ⊥ Leaflet ⊥ fetch. Testattavuus: **Taso 1 Vitest-pure** (⊥ jsdom-docblockia — tiedosto ⊥ koske DOM:iin) `tests/t395-nav-link.test.ts`: tunnettu koordinaatti → TARKKA odotettu URL (regressiovahti parametrien nimille — `api=1` & `destination` ovat Googlen sopimus ⊥ meidän); pyöristys 6 desimaaliin; NaN/Infinity/rajojen ulkopuoliset → `null`; negatiivinen lat/lon säilyttää etumerkin; `navTarget` palauttaa merkin koordinaatit muuttumattomina. Kerros: `src/logic/`. Käyttäjä: talkoolainen (ensisijainen — ajaa metsän laitaan & kävelee loput), järjestäjä (sama nappi, tekee itse pätkiään). Vaatii —. | V286 |
| T396 | ✓ | **[Pieni, UI] "Navigoi tähän" -nappi merkkimodaaliin (V286, vaatii T395).** `src/ui/marker-detail-modal.ts`: oma rivi modaalin bodyyn otsikkorivin (type-label + km) ALLE, ENNEN `locationNote`-textareaa — kohde vastaa kysymykseen "missä tämä on", ⊥ ole merkin statustoiminto ∴ ⊥ footeriin: footerin sisältö per rooli on lukittu DESIGN §K MarkerDetailModalissa (talkoolainen `[Aseta] [Ei tarpeen]`, järjestäjä `[Tallenna] [↻ Käännä]`) & kolmas nappi sinne rikkoisi sopimuksen & ahtaisi mobiilin. (a) Elementti on `<a class="marker-detail-nav" target="_blank" rel="noopener noreferrer">` ⊥ `<button>`+`window.open`: ankkuri antaa SELAIMEN hoitaa app-handoffin (Android-intent / iOS universal link), säilyttää pitkän painalluksen "kopioi linkki" & toimii ilman JS-käsittelijää. Sisältö: 📍 + `"Navigoi tähän"`. (b) `href = navUrl(navTarget(marker))`; `null` → riviä ⊥ renderöidä LAINKAAN — ⊥ disabloitua nappia (kuollut pinta, V250). (c) Näkyy MOLEMMILLE rooleille: järjestäjä ajaa itse pätkänsä (VISION §Järjestäjä tekee pätkiä itselleen) ∴ ⊥ `data-role-hide`iä. (d) Tyyli: olemassa oleva `.btn--ghost`-kuvio + `min-height:44px; width:100%` (§R/V268 — hanskoin osuttava) + `field-tint`-tausta. ⊥ uusia värejä §C:hen. (e) DESIGN.md §K MarkerDetailModal (:560) -amend: rivi navigointi-ankkurista, sen paikasta DOM-järjestyksessä & `null`-tapauksesta. Testattavuus: **Taso 2 Vitest-jsdom** (`// @vitest-environment jsdom` ENNEN importteja) `tests/t396-nav-link-ui.test.ts`: ankkurin `href` === `navUrl(navTarget(m))` (kutsu SAMAA funktiota ⊥ toista URL-rakennusta testissä — muuten testi vahtii itseään); `target="_blank"` & `rel` sisältää `noopener`; kelvottomat koordinaatit → `.marker-detail-nav` puuttuu DOM:ista kokonaan; ankkuri renderöityy molemmilla rooleilla; ankkuri on ENNEN `locationNote`-kenttää DOM-järjestyksessä. ⊥ Playwrightia: ⊥ Leaflet-riippuvuutta & ulkoiseen appiin siirtymistä ⊥ voi E2E-testata. Kerros: `src/ui/` + `src/style.css` + DESIGN.md. Käyttäjä: talkoolainen, järjestäjä. Vaatii T395. | V286,V250,V268 |
| T397 | ✓ | **[Pieni, map] GPS-sijaintipiste omaan Leaflet-paneen (V287, korjaa B166).** `src/map/gps-navigator.ts`: (a) UUSI privaatti `ensurePane()`: `this.map.getPane(GPS_PANE) ?? this.map.createPane(GPS_PANE)` + `pane.style.zIndex = '675'`. Idempotentti — `onPosition` voi kutsua joka kerta & `stop()`+`start()` ⊥ tuota duplikaattia. Vakiot `const GPS_PANE = 'gps'` & `GPS_PANE_Z = 675` tiedoston yläosaan kommentilla joka listaa Leafletin oletuspanet (400/600/650/700) — seuraava lukija ⊥ arvaa mistä 675 tulee. (b) `onPosition`:n `L.circleMarker(…)`-optioihin `pane: GPS_PANE`. ⊥ omaa `L.svg()`-rendereriä: Leaflet luo pane-kohtaisen SVG-rendererin itse (`Map._getPaneRenderer`, V287). (c) ⊥ muuteta `SegmentOverlay`/`AreaOverlay`/`MarkerManager`-piirtoa: korjaus on GPS-pisteen paikassa ⊥ muiden piirtojärjestyksessä — `bringToFront()`-kutsu overlaydeissä olisi sama vika toisin päin (jokainen uusi overlay pitäisi muistaa). (d) `.gps-dot`-CSS puuttuu `style.css`:stä kokonaan vaikka `className: 'gps-dot'` on asetettu — ⊥ lisätä sitä tässä (kuollut luokka ⊥ ole tämän taskin vika; z-index ⊥ ratkea CSS:llä koska koko SVG-pane on yksi stacking-konteksti). Testattavuus: **Taso 2 Vitest** (`tests/t397-gps-pane.test.ts`, `// @vitest-environment jsdom`) `vi.mock('leaflet')` jaetun `tests/helpers/leaflet-mock.ts`:n kautta ∴ tiedosto ! lisätä `vite.config.ts`:n `ISOLATED`-listalle (a-lohko, "vi.mock"): fake-map + stubattu `navigator.geolocation` → ensimmäinen fix luo panen nimellä `'gps'` & `zIndex === '675'`; `circleMarker`-optiot sisältävät `pane: 'gps'`; toinen fix ⊥ kutsu `createPane`a uudelleen (`getPane` palauttaa olemassa olevan); `stop()` → `start()` → uusi fix ⊥ tuota toista panea. Playwright ⊥ pysty tähän: geolocation on selaimen lupamuurin takana & z-indexin todentaminen kuvasta on flaky (E2E-NOTES). Kerros: `src/map/`. Käyttäjä: talkoolainen (kriittinen — navigointi metsässä). Vaatii —. | V287 |
| T398 | x | **[Keski, logiikka] Käänteinen suunta T386:lle — varastorivien ranking template-labelia vasten (V276/V277/V278).** UUSI `src/logic/inventory-link.ts`. (a) `rankInventoryRows(label, rows) → {row, score}[]` kääntää `rankTemplates`in (T384) toisin päin: varastorivien NIMET rankataan kirjoitettua template-labelia vasten. Adapteri on TARPEEN koska `rankTemplates` lukee `label`-kenttää & inventaariorivillä on `name` — ⊥ kopioi Levenshteinia, käärii sen. Sama `SUGGESTION_THRESHOLD`, sama determinismi (tasapeli → syötejärjestys). (a2) `cleanDisplayName(name)` — NÄYTTÖtason siivous merkkipohjan nimeksi: poistaa kiinnitystapa-sanat (`irtokyltti`/`lisäkilpi` + taivutukset) mutta SÄILYTTÄÄ välit, ison alkukirjaimen & välimerkit (⊥ `normalizeName`, joka murskaa kaiken vertailumuotoon): "Nuoli irtokyltti, valkoinen tausta" → "Nuoli, valkoinen tausta", "Ennakko oikea, 90 irtokyltti" → "Ennakko oikea, 90". Siivoaa myös roikkumaan jäävät välimerkit & tuplavälit. **Kiinnitystapa kuuluu VARASTORIVILLE ⊥ merkkipohjan nimeen** (V278/V186): laatikon kannalta on olennaista onko kyltti irrallinen, merkkipohjan kannalta ⊥ ole. `ATTACHMENT_TOKENS` (`template-match.ts`) täydennetään taivutuksilla (`irtokyltillä`, `lisäkilvellä`, `lisäkilvet`) — tuotantodata sisältää ne. (b) **SIIRRÄ** `unlinkedItems` + `unlinkedCount` tänne `src/ui/inventory-merge-panel.ts`:stä (T386) & re-importtaa ne siellä: suodatinsääntö (`template_id IS NULL AND not_sign=0`) on LOGIIKKAA ⊥ DOM:ia, & nyt sillä on KAKSI kuluttajaa ∴ se ⊥ saa asua UI-moduulissa. `tests/t386-*.test.ts` pysyvät vihreinä (sama vienti, uusi koti) — jos ne hajoavat, siirto muutti semantiikkaa. Testattavuus: **Taso 1 Vitest-pure** (⊥ jsdom-docblockia): typo osuu MOLEMPIIN suuntiin ("Ennako"↔"Ennakko"), irtokyltti-suffiksi ⊥ estä osumaa (V278), tyhjä/whitespace-label → ⊥ ehdotuksia, tyhjä `rows` ⊥ kaadu, kynnyksen alle jäävä ⊥ päädy tulokseen, järjestys deterministinen kahdella ajolla; `cleanDisplayName` poistaa suffiksin & taivutukset säilyttäen välit/isot kirjaimet, siivoaa roikkuvan pilkun & tuplavälin, nimi ILMAN suffiksia palautuu muuttumattomana, tyhjä → tyhjä; `unlinkedItems` säilyttää T386:n semantiikan (linkattu & `not_sign` pois). Kerros: `src/logic/`. Käyttäjä: järjestäjä. Vaatii T384, T386. | V276,V277,V278 |
| T399 | x | **[Suuri, UI] Merkkipohjaa luotaessa näkyy mitä varastossa on (V288/V289, käänteinen T386).** Tuotantotodiste 2026-07-29: 124 rivistä 101 linkittämättä & iso osa saa T386:ssa "Ei ehdotuksia" koska niille **⊥ OLE** merkkipohjaa — ne ! LUODA ⊥ linkittää. Merkkipohja luodaan yleensä juuri siksi että laatikossa on kyltti ∴ linkitys kuuluu LUONTIHETKEEN, ⊥ erilliseen jälkikäyntiin. UUSI `src/ui/inventory-link-picker.ts` — **⊥ KASVATA** `src/ui/sign-template-modal.ts`:ää (748 riviä, jo iso); modaali mounttaa pickerin YHTEEN kohtaan nimikentän alle. (a) **Ehdotuskaista:** kirjoitettu nimi → top-3 varastoriviä kynnyksen yli (`rankInventoryRows`, T398), per rivi nimi + "N kpl · paikka" + valintanappi; päivittyy `labelInput`in `input`-eventistä (debounce). Ei osumia → koko kaista PIILOSSA, ⊥ tyhjää laatikkoa. (b) **"Näytä kaikki varastorivit (N)"** → haettava täysi lista linkittämättömistä (`.inv-sign-picker`-kuoren idiomi, ⊥ omaa modaalikuorta); valinta sulkee listan & palaa modaaliin. **V288 — valinta ⊥ ole heti-kirjoitus:** templatea ⊥ ole vielä olemassa ∴ `template_id`:tä on FYYSISESTI mahdoton kirjoittaa; valinta näkyy chippinä "Linkitetään: <nimi> (N kpl)" + "Poista valinta", PUT ajetaan VASTA `onSaveTemplate`n jälkeen. **Nimen esitäyttö & muokkaus (käyttäjävaatimus 2026-07-29 "irtokyltti on vähän hassu nimessä"):** valinta täyttää nimikentän AINA `cleanDisplayName(row.name)`:llä (T398) — myös jos kentässä on jo tekstiä. Perustelu: kirjoitettu teksti on tässä vaiheessa HAKUSANA ⊥ nimi ("bus" → valitset "Shuttle-bus aikataulu"; "bus" merkkipohjan nimenä olisi väärin). Ylikirjoitus ⊥ ole tiedon menetys koska kenttä on heti muokattavissa: valinnan jälkeen **fokus siirtyy nimikenttään & teksti valitaan** (`select()`) ∴ yksi näppäily kirjoittaa yli, nuolinäppäin jättää ehdotuksen. Slug ! johtua uudesta nimestä: ohjelmallinen `value`-asetus ⊥ laukaise `input`-eventtiä (tunnettu ansa, `sign-template-modal.ts:175`) ∴ slug-päivitys kutsutaan EKSPLISIITTISESTI — & vain jos käyttäjä ⊥ ole koskenut tunnukseen (`idTouched`). **Varastorivin OMA nimi ⊥ muutu** — se on laatikon kirjanpitoa & kiinnitystapa on siellä hyödyllistä tietoa (V278/V186); rivin nimeä muokataan inventaariosivulla kuten ennenkin. **Rajapinta:** `SignTemplateModalCallbacks` saa KAKSI VALINNAISTA kenttää `inventoryRows?: () => Promise<InventoryLinkRow[]>` & `onLinkInventory?: (itemId, template) => Promise<void>`; puuttuvat → osiota ⊥ renderöidä (V289). **Näkyy VAIN luontitilassa** (`template === null`) — muokkaustilassa linkki on jo olemassa tai kuuluu inventaariosivulle. **Kaikkialla missä merkkipohja luodaan:** `src/inventory.ts` (3 kohtaa: `openSignFlow`, `openConvertFlow`, T386:n `onCreateTemplate`) JA `src/ui/sign-library-panel.ts` (kartan merkkikirjasto) — kartalla `/api/inventory` on `admin|järjestäjä` (V163) ∴ 403/verkkovirhe = osio pois HILJAA (V289). **Epäonnistunut linkitys tallennuksen jälkeen:** template ON tallennettu (⊥ rollbackia, V288) → `showToast` kertoo että merkkipohja syntyi mutta linkitys ⊥, ohje: inventaariosivun 🔗 Yhdistä (V21). XSS: rivinimet `textContent` (V164). Touch 44px (§A/V268). DESIGN.md UUSI §K-sopimus `InventoryLinkPicker` → auto-ux `/karttamaster-ux`; ⊥ uusia väritokeneja. Testattavuus: **Taso 2 Vitest-jsdom** (`// @vitest-environment jsdom` ENNEN importteja) — kaista näyttää top-3 oikeassa järjestyksessä; ⊥ osumia → kaista piilossa; valinta näyttää chipin & **⊥ kutsu `onLinkInventory`a**; tallennus kutsuu sen valitulla id:llä; **peruutus/Esc ⊥ kutsu sitä (V288-vahti)**; callbackit puuttuvat → **⊥ osiota DOM:issa (V289-vahti)**; valinta täyttää nimen siivottuna MYÖS kun kentässä on jo tekstiä ("bus" → "Shuttle-bus aikataulu"), fokus+select nimikentässä valinnan jälkeen, slug johtuu uudesta nimestä kun `idTouched=false` & EI kun käyttäjä on muokannut tunnusta; V164-vahti `<script>`-nimisellä rivillä. Kerros: `src/ui/` (uusi) + `sign-template-modal.ts` (wiring) + `src/inventory.ts` + `src/ui/sign-library-panel.ts` + CSS. Käyttäjä: järjestäjä. Vaatii T398. | V288,V289,V276,V277,V164,V21,V268 |
| T400 | . | **[Keski, logiikka] Merkkijonon ryhmittely puhtaana funktiona (V290).** TAUSTA: tapahtuman jäljiltä "pari merkkiä jäi pistämättä" — järjestäjä ⊥ näe kartalta MITKÄ merkit jäivät asettamatta eikä pysty kokoamaan niistä jälkihoitotehtävää. UUSI `src/logic/marker-overview.ts` — ⊥ kasvateta `map-filter.ts`:ää (se päättää näkyvyyden, TÄMÄ järjestää sen tuloksen). `groupMarkersForOverview(markers, segments, filter, phase, ctx) → OverviewGroup[]`, `OverviewGroup = {status, subgroups: {segment: Segment\|null, markers: SignMarker[]}[]}` + viimeisenä `filteredOut`-ryhmä. Ryhmittely status → pätkä; "Asettamatta" (⊥ terminaali status) ENSIN; pätkättömät `segment: null` -alaryhmään; kartan suodattimen `hidden`-merkit `filteredOut`iin ⊥ pudoteta. Jäsenyys ← `resolveSegmentMarkers` (V259, ⊥ oma sääntö), järjestys alaryhmässä `displayKm`illä (V237/V238), vaiherajaus `phase`illa (V91). Kerros: `src/logic/`. Testattavuus: **Taso 1 Vitest-pure** — (i) asettamattomat ensin & pätkittäin; (ii) orpo merkki → `segment: null` ⊥ katoa; (iii) suodatettu merkki → `filteredOut` ⊥ pudonnut; (iv) väärän vaiheen pätkä ⊥ näy; (v) tyhjä syöte ⊥ heitä; (vi) sama merkki esiintyy TASAN kerran koko rakenteessa. Käyttäjä: järjestäjä. | V290,V271,V259,V237,V91 |
| T401 | . | **[Pieni, logiikka] Omistajuussiirron esikatselu (V291).** `src/logic/segment-membership.ts` — ⊥ uutta moduulia, kysymys "kuka omistaa" on TÄMÄN moduulin vastuu. `previewLinkTransfer(markerIds, segments, markers) → {markerId, fromSegmentId}[]` = ne poimittavat merkit joilla ON nykyinen omistaja & mikä. Lukee saman `resolveSegmentMarkers`in kuin varsinainen ratkaisu ∴ esikatselu ⊥ voi olla eri mieltä lopputuloksen kanssa (kaksi sääntöä = B100-kuvio). Tyhjä tulos = ⊥ vahvistusaskelta (V291). Kerros: `src/logic/`. Testattavuus: **Taso 1 Vitest-pure** — (i) orpo merkki → tyhjä tulos; (ii) pätkään kuuluva → `fromSegmentId` oikein; (iii) merkki joka on JO kohde-tehtävän `linkedMarkerIds`issä ⊥ raportoidu siirtyväksi; (iv) sama merkki ⊥ esiinny kahdesti. Käyttäjä: järjestäjä. | V291,V259 |
| T402 | . | **[Suuri, UI] MarkerOverviewPanel — telakoitu merkkijono järjestäjälle (V290, vaatii T400).** Korvaa `#marker-modal`in järjestäjältä. UUSI `src/ui/marker-overview-panel.ts` renderöi T400:n ryhmät. **SIJAINTIPÄÄTÖS (ux-passi 2026-07-29, kumoaa SEKÄ modaalin ETTÄ left-panel-sektion):** oikeaan reunaan telakoitu työpaneeli `.marker-overview`, `width:min(380px,100vw)`, `#map-area`n sisar, **⊥ backdropia ⊥ `position:fixed`-overlaytä** — kartta ! pysyä näkyvänä & klikattavana (V114: vaikutus katsotaan kartalta). Modaali peittää juuri sen minkä rivin klikkaus liikuttaa = nykyisen näkymän käyttämättömyyden syy ("heikko ja ongelmallinen, eikä sitä olla käytetty"). Left-panel torjuttu MITATULLA perusteella: sisältöleveys 240px ∴ `.marker-item`in 5 elementtiä (checkbox·ikoni·label·km·status-badge) ⊥ mahdu & status-badge — koko näkymän kysymys — putoaisi; lisäksi 194 merkin lista veisi SignLibrary/SegmentPanel/AreaPanel-kolumnin foldin taakse. **Uusiokäyttö pakollinen, ⊥ uutta visuaalia:** `createSectionHeader` (T371/V267) · `.marker-item` (DESIGN §K Listarivit) · `buildMarkerVisual` (T198) · sticky-toimintopalkki (T311/V223: scroll-sisällön VIIMEINEN lapsi + `position:sticky;bottom:0`, ⊥ `fixed` — B101). Rivin klikkaus → kartta panoroi; `···` → MarkerDetailModal; V62 (⊥ inline-poistoa rivillä). **Panorointi ! kompensoida paneelin leveys** (`SegmentFit`-tason apuri saa `paddingBottomRight`-option) — ilman sitä "näytä kartalla" osoittaa paneelin ALLE & näkymän ydintoiminto on rikki. Toggle `#btn-list` (järjestäjä) `aria-expanded`illa, Esc sulkee, auki-tila `localStorage` (V5-kuvio — työjono johon palataan ⊥ nollaudu sivulatauksessa); toggle muuttaa `#map-area`n leveyttä ∴ **`map.invalidateSize()` pakollinen** (T179-oppi). ≤700px → irtoaa telakasta bottom sheetiksi (`max-height:70dvh`, kartta jää yläpuolelle NÄKYVIIN — ⊥ full-screen, panorointi on toiminnon puolikas), sama kuvio kuin `.map-filter-groups` (B160/V274); scroll-alue `overscroll-behavior:contain` (§R-selektorilista). Tyhjätilat: ⊥ merkkejä → "Ei merkkejä"; kaikki asetettu → "Kaikki merkit asetettu ✓" `--confirm`-sävyllä (onnistuminen ⊥ tyhjä lista). **Hyväksymiskriteeri: DESIGN.md §K saa `MarkerOverviewPanel`-lohkon** (valmis luonnos ux-passissa 2026-07-29). ⊥ uusia värejä ∴ §C ennallaan. Kerros: `src/ui/` + pieni `src/map/`-lisäys (panorointi-padding). Testattavuus: **Taso 2 Vitest-jsdom** (ryhmäotsikot & järjestys T400:n tuloksesta; "Suodattimen ulkopuolella" renderöityy KIINNI; rivin klikkaus → panorointi-callback oikealla id:llä; tyhjätilat; talkoolaisrooli ⊥ renderöi paneelia) + **Taso 3** `e2e/t179-map-invalidate-size.spec.ts` LAAJENNUS (⊥ uutta tiedostoa — sama vika samasta syystä) + `critical-paths.spec.ts` "Touch targets" kattaa napit automaattisesti (T373/V268). Käyttäjä: järjestäjä. | V290,V114,V223,V267,V268,V5,V62 |
| T403 | . | **[Keski, UI] Valituista merkeistä reititön tehtävä (V291, vaatii T401,T402).** `src/ui/marker-overview-panel.ts`: rivin checkbox (`22×22px`, `accent-color:var(--accent)`) + ryhmäotsikon "valitse ryhmä"; valinta SÄILYY uudelleenrenderöinnin yli (`marker-list.ts`:n `prevSearch`-kuvio — kartan päivitys ⊥ saa nollata 20 merkin valintaa). Sticky-palkki `[Luo tehtävä valituista (N)]` `.btn--confirm` `min-height:52px`; **disabloitu tila ! merkitä näkyviin** (`field-tint`+`text-muted`+`cursor:not-allowed`) — jaetut `.btn--*` ⊥ määrittele `:disabled`ia ∴ ilman omaa sääntöä nappi näyttää painettavalta & ⊥ tee mitään (V250 kuollut pinta, SegmentCreationModal-oppi); N=0 → disabloitu ⊥ piilotettu. Tallennus luo reitittömän tehtävän (V139) jonka merkkijoukko = `linkedMarkerIds` — **T216:n olemassa oleva liitosmekanismi, ⊥ uutta**. Vahvistusaskel T401:n tuloksesta: `.marker-overview-confirm` toimintopalkin päällä (`--warn`-reunus) "N merkkiä siirtyy pois pätkiltä: <nimet>" + `[Peruuta]` `[Luo silti]`; siirtyvien rivit saavat `--warn`-vasemman reunuksen ∴ varoitus OSOITTAA mihin; 0 siirtyvää → askel ohitetaan (V291). Luotu tehtävä ilmestyy samaan listaan omana pätkäryhmänään (se on `Segment` ∴ tulee T400:n ryhmittelystä ilmaiseksi). Kerros: `src/ui/`. Testattavuus: **Taso 2 Vitest-jsdom** — (i) 0 valittua → nappi disabloitu & disabled-tyyli asetettu; (ii) valinta säilyy re-renderin yli; (iii) omistajallisia valittu → vahvistus näkyy & listaa pätkänimet & rivit merkitty; (iv) vain orpoja valittu → vahvistus EI näy, tallennus suoraan; (v) Peruuta ⊥ luo tehtävää & valinta säilyy; (vi) luonti kutsuu tehtävänluontia `linkedMarkerIds`illä ⊥ route-kentillä. Käyttäjä: järjestäjä. | V291,V139,V250,V277 |
| T404 | . | **[Keski, UI] Talkoolaisen valikoiva bulk-kuittaus turvaan, SITTEN `marker-list.ts` pois (V292).** **JÄRJESTYS ON SOPIMUS: (a) ennen (b):tä.** (a) `src/ui/segment-marker-list.ts`: checkbox per ei-terminaali rivi + `.bulk-action-bar` (DESIGN §K BulkActionBar talkoolainen, T17: `[☐ Valitse kaikki]` + `[✓ Aseta valituille (N)]` + `[Ei tarpeen (N)]`, `min-height:44px`, `field-tint` disabloituna) koti-tabin "Kaikki merkit" -listaan. Mutaatio kulkee OLEMASSA OLEVAA reittiä (`manager.bulkSetStatus`) ⊥ uutta polkua. (b) `#btn-list` pois talkoolaisen toolbarista (V155-amend); `src/ui/marker-list.ts` + `#marker-modal`-kuori + `renderMarkerList`-kutsut (`src/app/markers-wiring.ts`, 4 kutsupaikkaa) POISTETAAN. **HUOM `renderSignDots`:** `src/ui/progress-bar.ts` importtaa sen `marker-list.ts`:stä ∴ siirrä ENNEN poistoa, muuten ProgressBar hajoaa. §K-lohkot `Marker-modaali` / `BulkStatusToolbar` merkitään korvatuiksi (V86-kuvio: ⊥ poisteta, merkitään). Kerros: `src/ui/` + `src/app/`. Testattavuus: **Taso 2 Vitest-jsdom** (bulk-valinta koti-tabissa → `bulkSetStatus` oikeilla id:illä; terminaali rivi ⊥ saa checkboxia; 0 valittua → napit disabloitu) + **Taso 3** `e2e/segments.spec.ts` (talkoolainen: koti-tab → valitse 2 → "Aseta valituille" → statukset muuttuvat) + regressio: `critical-paths.spec.ts` ⊥ saa viitata `#marker-modal`iin talkoolaisena. Käyttäjä: talkoolainen (⊥ menetä toimintoa) + järjestäjä (⊥ kaksi listanäkymää). Vaatii T402,T403 — järjestäjän korvaaja pystyssä ENNEN poistoa. | V292,V148,V155,V183,V184,V86 |

## §UX Kenttämuistio

UX-simulaatio 2026-06-07. Kaksi roolia läpikäyty — löydöt kirjattu taskeihin ja invariantteihin.

### Talkoolainen metsässä — kriittiset kitkat

| kitka | vakavuus | task |
|-------|----------|------|
| Ei GPS-pistettä — ei tiedä missä on kartalla | kriittinen | T30 |
| Ei kuittaustoimintoa — ei voi merkitä asetetuksi | kriittinen | T24 |
| Kaikki merkit näyttää samalta — ei status-värikoodausta | suuri | T23 |
| Ei "seuraava asettamaton" -navigointia — selaa koko lista | suuri | T16, T31 |
| Drive mode 50m askeleet = hidas hypätä merkkien välillä | pieni | T39 |
| Tupla-klik hanskat kädessä — epätarkka kohdennus | pieni | — |

### Järjestäjä toimistossa — kriittiset kitkat

| kitka | vakavuus | task |
|-------|----------|------|
| Drag-to-move puuttuu — väärässä paikassa → delete + redo | suuri | T37 |
| Rotation arm häviää ulkoklikillä — turha reset | suuri | T40 |
| Ei tyypin vaihto-toimintoa — väärä tyyppi → delete + redo | suuri | T38 |
| Ei kokonaistilannekuvaa — ei merkkimäärää per reitti helposti | suuri | T15, T28 |

### Hyvää (säilytä)

- Tupla-klik + floating picker — nopea desktop-flow ✓
- Automaattinen bearing — ei tarvitse säätää manuaalisesti ✓
- Rotation arm aktivoituu heti lisäyksen jälkeen ✓
- Marker-lista etäisyysjärjestyksessä ✓
- Escape-chain toimii loogisesti läpi koko UI:n ✓
- Reitin vaihto + näkyvyystoggle toimii ✓

---

## §B Bugs

| id | date | cause | fix |
|----|------|-------|-----|
| B1 | 2026-06-07 | `MarkerManager.add()` käyttää `assignRoutesToMarker` (100m threshold) routeIds:lle mutta ei fallback-logiikkaa — klikki >100m reitistä → routeIds:[], merkki tallentuu mutta katoaa hiljaa | V21 → T44 |
| B2 | 2026-06-07 | `PlaceMode.exit()` asettaa btnAddSign.textContent = '+ Lisää merkki' eikä alkuperäiseen '+ Merkki' — teksti ei resetoidu oikeaksi | T44 (samalla korjauksella) |
| B3 | 2026-06-07 | Route-tab-napit ja eye-icon-toggle ≤30px korkeus mobiililla (375px viewport) — alle 44px touch target | V21→T45 |
| B4 | 2026-06-10 | `auth-screen.ts:48–53` — non-401 (esim. 404 Vite dev-server) tai verkkovirhe → `onAuthenticated` kutsutaan ilman kirjautumista. Sovellus aukeaa ilman autentikaatiota. | V29→T54 |
| B5 | 2026-06-10 | `createSegmentStore()` = `new Map()` — segmenteillä ei localStorageen tallennusta. Kaikki pätkäjako katoaa sivun päivityksellä. | V30→T55 |
| B6 | 2026-06-10 | Pätkän luonnissa ensimmäinen klikattu piste ei näy kartalla — ei visuaalista palautetta. Luonnin jälkeen startDist/endDist ei muokattavissa — ainoa vaihtoehto poista+redo. | V31,V32→T56 |
| B7 | 2026-06-10 | `snapshot-panel.ts` renderöi aina koko listan — vie liikaa tilaa admin-näkymästä kun varmuuskopioita on paljon. | T57 |
| B8 | 2026-06-10 | `main.ts:186-188` — `openMarkerModal` kutsuu `renderMarkerList(markerManager)` ilman pätkäfiltteriä. Talkoolainen näkee kaikki merkit eikä vain oman pätkän. | V33→T58 |
| B9 | 2026-06-11 | `segment-panel.ts buildAssignSection()` tallentaa `assignedCode` vain localStorageen — ei kutsu `POST /api/admin/codes`. Backend `talkoolainen_codes`-taulu pysyy tyhjänä → `/api/auth/code-login` palauttaa 401. Talkoolainen ei pääse sisään jaetulla linkillä. | V34→T59 |
| B10 | 2026-06-11 | `segment-persistence.ts saveSegments()` catch {} ignoroi localStorage quota/write virheet hiljaa — käyttäjä ei tiedä tallennuksen epäonnistuneen, pätkädata häviää näkymättömästi. | V35→T60 |
| B11 | 2026-06-11 | `segments`-taulua ei ole backendissä — segmentit ovat localStorage-only. Talkoolainen eri laitteella avaa `/s/<koodi>` → `loadSegments()` palauttaa tyhjän storen → `getSegmentForCode` → `undefined` → `segmentView` ei luoda → talkoolainen näkee tyhjän kartan ilman pätkänäkymää. | V36→T61,T62 |
| B12 | 2026-06-11 | `#map-state-banner` oli `position:fixed;top:64px` → peitti talkoolaisen `#segment-view` headerin (nimi, range, description näkymätön). | ✓ siirretty `#segment-view-container`:iin flow-elementtinä |
| B60 | 2026-07-03 | T99 (segment-panel-rivin uudelleenkirjoitus, `[nimi][km][···]`) poisti T95:n lisäämän %-näytön huomaamatta — `getSegmentProgress()` (`src/logic/segments.ts:81`) jäi sen jälkeen kutsumatta mistään koko src/:ssä. Järjestäjä ei nähnyt pätkän edistymistä missään. Lisäksi alkuperäinen konsepti (pelkkä %-luku sivupalkin rivin perässä) oli huono formaatti — käyttäjä haluaa lukumääräpohjaisen breakdownin (esim. "10/10 asetettu") kartan alle, ei sivupalkkiin. | V88→T141 (korvaa T95) |
| B61 | 2026-07-03 | T141 rakennettu speciä vastaan (V88: kartan-alle-elementti), mutta live-testissä käyttäjä totesi sen huonoksi — status-lukumäärä haluttiin takaisin sivupalkkiin pätkäjako-listan riville, korvaten km-alue-tekstin ("0-2.2km" koettiin hyödyttömäksi/"tyhjäksi"). Nopea spec-korjaus samassa sessiossa. | V88→V89, T141→T142 |
| B13 | 2026-06-11 | `SegmentPanel.render()` uudelleenrakentaa koko listan → "Lisätiedot & varusteet" -toggle-tila nollautuu aina — käyttäjä menettää avoimena olevan näkymän esim. assign-tallennuksen yhteydessä. | V37 |
| B14 | 2026-06-11 | Teksti `#segment-desc-input`:ssä (ei blur vielä) katoaa kun `this.render()` triggeröityy (esim. assign-tallennus) — `change`-event vaatii eksplisiittisen blurin/enterin ennen render-kutsua. Näppäin-flow: kirjoita desc → tallenna koodi heti klikkaamalla → textarea tyhjänä. | V38 |
| B15 | 2026-06-11 | Järjestäjä-segment-panel visuaalinen ilme on heikko — ei selkeää hierarkiaa, isot tyhjät alueet, kontrollit vaikeasti löydettävissä. | UX-parannustehtävä (ei kriittinen toiminnallisuus) |
| B16 | 2026-06-11 | `init()` hakee segmentit backendistä (`fetchSegmentByCode`) vain talkoolaiselle — järjestäjä näkee tyhjän pätkälistan uudessa selainistunnossa vaikka backend tietää segmenteistä. V36: localStorage on cache, ei lähde. | V36 — tarvitsee `GET /api/segments` kutsun järjestäjälle sivun latautuessa |
| B17 | 2026-06-11 | `RoleSelector` toggle-nappi näkyy kaikille — talkoolainen voi klikata järjestäjä-rooliin ja saada admin-UI:n (merkkien poisto, segment-hallinta, snapshots). `setRole()` kirjoittaa vain localStorage:n eikä tarkista backend-sessiota. Kriittinen tietoturvariski. | V39 → T67 |
| B18 | 2026-06-11 | `#auth-screen { display:none }` + `start()` kutsuu `fetch` ennen `show()` → karttanäkymä välähtää 100–300ms ennen kirjautumislomaketta. `main.ts:34` alustaa Leaflet-kartan moduulitasolla heti — kartta renderöityy ennen kuin auth ratkeaa. | V40 → T68 |
| B19 | 2026-06-19 | `sign-library-panel.ts buildRow()/buildForm()` interpoloi `t.label`, `t.shortLabel`, `t.description` suoraan `innerHTML`-templateen — XSS. Esim. label `<img src=x onerror=alert(1)>` suoritetaan. T11:ssä ennakkotapaus: "XSS-safe (DOM-assign)". | ✓ escapeHtml() kaikille käyttäjäsyötteille (b45db2d) + XSS-regressiotestit t22 |
| B20 | 2026-06-19 | `main.ts:215` — `GpsDrivePanel` alustuu kaikille käyttäjille; `update()` kutsuu `this.el.hidden = false` kun unset-merkkejä löytyy → `#gps-drive-panel` tulee näkyviin järjestäjälle (Seuraava/Aseta/Ei tarpeen -napit). Järjestäjä ei tarvitse drive modea (VISION.md). | V45 → T86 |
| B21 | 2026-06-19 | `place-mode.ts openPicker()` interpoloi `t.id`, `t.color`, `t.shortLabel`, `t.label` suoraan `innerHTML`-templateen ilman `escapeHtml` — V44-rikkomus. XSS-payload template-labelissa suoritetaan kun floating picker aukeaa dblclick-kartalla. | V44 → T87 |
| B22 | 2026-06-19 | `sign-library.ts` käyttää in-memory `Map`ia ilman persistointia — `createSignLibrary()` seedaa vain 4 defaultia joka latauksessa. Custom-mallit katoavat sivun päivityksellä. Ei `saveLibrary()`/`loadLibrary()`-funktiota. | V46 → T88 |
| B23 | 2026-06-19 | Custom-template kartalla näyttää `→` nuolen ympyrässä: (a) `icons.ts circleSvg()` default-case asettaa `arrow='→'` ja SVG renderöi `${arrow}` — pitäisi näyttää `shortLabelOverride ?? '?'`. (b) `MarkerManager.add()` ei välitä `color`/`shortLabel` templatestä, `addLeafletMarker()` kutsuu `createSignIcon(m.type, m.bearing, m.status)` ilman väriä/lyhennettä. `SignMarker`-tyypissä ei `color`/`shortLabel`-kenttiä. | V47 → T89 |
| B24 | 2026-06-19 | `sign-library-panel.ts buildForm()` väri-picker rivi: flex-container jossa `<span>Väri</span>` + `width:44px` color-input ei mahdu kapeaan left-paneliin — vaakasuuntainen overflow. Ei `min-width:0` flex-lapsille eikä `overflow:hidden` containerille. | UX → T90 |
| B25 | 2026-06-19 | `sign-library-panel.ts saveForm()` luo uudet custom-mallit `{favorite:false}` — uusi malli ei näy floating pickerissä (T84 käyttää `listFavorites()`). Käyttäjä ei löydä juuri luomaansa mallia kartalta dblclick-pickerin kautta. | T91 |
| B26 | 2026-06-19 | `markers.ts updateType()` ei päivitä `m.color`/`m.shortLabel` — vaihto custom-tyyppiin jättää vanhan/tyhjän värin (harmaa #94a3b8). Latent: `marker-list.ts` käyttää toistaiseksi vain SIGN_TYPES (default-4), joten callsite ei tee custom-vaihto-kutsuja. Aktivoituu kun marker-lista laajennetaan SignLibrary-tuelle. | V47,V48 → T92 |
| B27 | 2026-06-22 | `main.ts` loi `SegmentOverlay` mutta ei kutsunut `update(segmentStore)` initialisaatiossa — overlay päivittyi vain `onUpdate`-callbackin kautta (create/delete/edit). Pätkät hävisivät kartalta sivun refreshin jälkeen. Fix: `segmentOverlay.update(segmentStore)` heti konstruktorin jälkeen. | V55 |
| B28 | 2026-06-22 | `openDetailsModal()` save-nappi (`btn-segment-modal-save`) ei kutsunut `this.onUpdate()` — `segmentOverlay.update()` ei triggeröitynyt nimen/kuvauksen tallennuksen jälkeen. Kartta ei päivittynyt (esim. pätkälabel pysyi vanhana). Fix: `this.onUpdate()` save-handlerin loppuun. | V56 |
| B29 | 2026-06-22 | `renderMarkerList` ja `renderSignDots` käyttivät `typeInfo(m.type)` SIGN_TYPES-fallbackia custom-kirjastomerkeille — `m.color`/`m.shortLabel` ignoroitu. Kirjastomerkit näkyivät väärällä värillä ja labelilla listassa ja alapalkissa. Fix: `displayColor = m.color ?? info.color`, `displayShortLabel = m.shortLabel ?? info.shortLabel` kummassakin funktiossa. `src/ui/marker-list.ts`. | ✓ V47,V48 |
| B30 | 2026-06-22 | Merkin poisto mahdoton: T98 poisti `btn-delete` context-menusta, T104 poisti `btn-delete` `marker-list.ts`:stä (siirtyi T105:een), T105 rakentamatta → poisto katosi kokonaan. Fix: `btn-delete`-nappi palautettu järjestäjälle kompaktiin merkkiriviin `marker-list.ts`:ssä (siirretään T105:een kun rakennetaan). `src/ui/marker-list.ts`. | ✓ V53 |
| B31 | 2026-06-22 | `MarkerDetailModal.buildContent()` rivi 149: `else if (talkoolainenCode)` — talkoolainen ilman pätkäkoodia (kirjautunut, ei segmenttiä) ei näe status-nappeja. Korjaus: `else if (role === 'talkoolainen')`. | V59 |
| B32 | 2026-06-25 | ~~`bearing_manual` ei persistoitu kolmessa kohdassa...~~ MOOT T129/T132 — bearing-feature poistettu kokonaan, ei enää relevantti | V52→V60 (molemmat poistettu) |
| B33 | 2026-06-26 | `area-panel.ts` käytti "Huoltopisteet"/"Huoltopiste" UI-teksteissä (5 kohtaa: header, add-footer, aria-label, prompt, modal-otsikko) — pitäisi olla "Alueet"/"Alue". | ✓ V63 |
| B34 | 2026-06-26 | `area-sync.ts` sisälsi vain `fetchAreas()` — `createArea/updateArea/deleteArea` puuttui. `onAreaAdd/onAreaUpdate/onAreaDelete` callbackit `main.ts`:ssä päivittivät vain in-memory `areas`-arrayta, ei backendiä. Kaikki aluemuutokset hävisivät refreshilla. | ✓ V64 |
| B35 | 2026-06-26 | `setOnAreaMove` ei kutsuttu `main.ts`:ssä — alueen raahaus kartalla päivitti vain `AreaOverlay`:n lokaalin renderin (`onDragEnd` → `renderArea`). `AreaPanel`-tila ja backend eivät tienneet siirrosta. AreaPanel-ref ei tallennettu (`new AreaPanel(...)` ilman sijoitusta). | ✓ V64 |
| B36 | 2026-06-26 | Area details -modaalissa ei `widthM`/`heightM`/`rotation`-kenttiä — alueen kokoa tai kiertoa ei voinut muokata. Ainoa vaihtoehto: poista + lisää uudelleen. | ✓ V65 |
| B37 | 2026-06-26 | `.area-drag-handle` CSS puuttui `style.css`:stä — Leaflet `divIcon` näkyi tyylittelemättömänä, lähes näkymättömänä ⊕-merkkinä. Raahausnuppi ei erottunut kartalta. | ✓ V64 |
| B38 | 2026-06-30 | `MapRectEditor.startEdit` lisää Esc-handlerin mutta ei karttaklikki-handleria — ulkoklikki ei poistu edit-tilasta. `area-overlay.ts` polygon-click kutsuu aina `onAreaClick` tarkistamatta `isEditing()` → detail-modaali aukeaa edit-tilan päälle. | V69 → T117 |
| B39 | 2026-06-30 | `rotationArmTip` käyttää CCW-rotaatiomatriisia (`rU = uM*cos - vM*sin`) — rotation=90° lähettää armin länteen eikä itään. `onRotationDrag` laskee kulman oikein mutta tip-positio on 180° väärässä → käyttäjä vetää itään, arm hyppää länteen. | V70 → T117 |
| B40 | 2026-06-30 | `onCornerDrag` kutsuu `cornersToRect(dragged, opposite)` joka laskee akseli-aligned widthM/heightM (`Math.abs(lng_diff)`, `Math.abs(lat_diff)`) — rotatoidulle neliölle antaa väärät mitat. Vastakkainen kulma liikkuu sen sijaan että pysyisi paikallaan. Rikoo V66 + V71. | V71 → T117 |
| B41 | 2026-06-30 | `area-details-modal.ts buildDescSection()` labeloi kentän "Ohjeteksti (Markdown)" + erillinen "Tallenna kuvaus" -nappi — T111 rakentamatta → markdown on harhaanjohtava lupaus. Auto-save puuttuu (eri pattern kuin T69). | V72 |
| B42 | 2026-06-30 | `area-details-modal.ts` sisältää kolme erillistä "Tallenna"-nappia (buildNameSection, buildSizeSection, buildDescSection) eikä footer-patternia — rikkoo V62 + V73. Puuttuu "Poista alue" -nappi footerista. Epäjohdonmukainen T69/T105-patternia vastaan. | V73 |
| B43 | 2026-07-02 | `src/ui/feature-color-picker.ts:2` — `openFeatureColorPicker(anchor: HTMLElement, ...)` ei koskaan käyttänyt `anchor`-parametria (input positioidaan aina `position:fixed;opacity:0` piiloon, ei anchor-suhteessa) — TS6133 esti `bun run build` (tsc strict) kokonaan. Löytyi T128:n Docker-buildin verifioinnin yhteydessä, ei liity gpkg-featureen. | ✓ poistettu `anchor`-parametri signatuurista + 4 kutsupaikkaa (area-panel.ts×3, area-details-modal.ts×1) |
| B44 | 2026-07-02 | `server/gpkg/convert.ts gpkgToGeoJSON()` vaati kovakoodatun layer-nimen `"kyltit"` ogr2ogr-lukukutsussa. QGIS nimeää layerin tallennuksen/exportin yhteydessä tiedostonimen mukaan (esim. `"kyltit"` → `"2026_testikyltit__kyltit"`) — oikea käyttäjän QGIS:stä ladattu `.gpkg` ei löytänyt layeria `"kyltit"` → ogr2ogr `"Couldn't fetch requested layer 'kyltit'!"` (exit 1) → `POST /api/gpkg/import` kaatui 500:aan. Löytyi manuaalisessa selaintestissä heti T124-T128:n jälkeen oikealla testitiedostolla. | V77 → ✓ korjattu: `gpkgToGeoJSON` ei enää välitä layer-nimeä lukiessa (ogr2ogr valitsee ainoan/ensimmäisen layerin automaattisesti); export pysyy nimettynä `"kyltit"` (kirjoitamme sen itse `-nln`:llä) |
| B45 | 2026-07-02 | GPKG-importilla luotu uusi merkki (`route_ids:[]`, `POST /api/gpkg/import` ei aseta reittiä koska palvelimella ei ole GPX-geometriaa) katosi hiljaa kartalta — sama B1/V21-ongelma toisessa reitissä. `MarkerManager` filtteröi näkyvät merkit `routeIds.some(id => visibleRouteIds.includes(id))`, tyhjä `routeIds` ei koskaan täsmää. Lisäksi `MarkerManager`:n paikallinen tila ei päivittynyt lainkaan importin jälkeen (raw `fetch`, ei kulkenut `MarkerManager`:n kautta). Löytyi käyttäjän raportoimana: "en näe tuota up arrow juttua" oikean importin jälkeen. | V78 → ✓ korjattu: `MarkerManager.reload()` (hakee tuoreet merkit + piirtää näkyvät) + `fixOrphanRouteIds()` (lähin-reitti-fallback tyhjille) kutsutaan `GpkgControls`-onImported-callbackista |
| B46 | 2026-07-03 | `e2e/sprint-features.spec.ts:140-150` testaa `#map-state-badge`-elementtiä, mutta `MapStateBadge` (src/ui/map-state-badge.ts) stubattiin tyhjäksi kun approval-konsepti poistettiin ("kaikki merkit aina näkyvissä kaikille") — elementtiä ei enää renderöidä. Testi testasi poistettua ominaisuutta, ei regressiota. Löytyi e2e-täydennyksen yhteydessä (18/69 epäonnistui, valtaosa vanhentuneita selektoreita T85 dblclick-flow-siirtymän jäljiltä). | V79 → ✓ testi poistettu |
| B47 | 2026-07-03 | `#left-panel-toggle` ("◀"/"▶", `src/style.css:2172-2189`) kiinteä 28×28px — alle 44px touch target mobiililla (375px). Sama bugiluokka kuin B3. (`#btn-route-prev`/`#btn-route-next` sai ennaltaehkäisevästi `min-width:44px` samalla.) | V21 → ✓ `width/height:44px` `#left-panel-toggle`:lle |
| B51 | 2026-07-03 | UX-audit (`/karttamaster-ux auditoi`): `--text-meta` (#64748b) `--surface-card`illa (#1e293b, tumma teema) ≈3.1:1 kontrasti — alle WCAG AA 4.5:1. Käytössä laajasti (km-lukemat, meta-labelit, `.segment-info` ym). | V81 |
| B52 | 2026-07-03 | UX-audit: DESIGN.md merkitsee U18 (`.btn-segment-details-toggle`) ja U19 (equipment-inputit/napit, `src/style.css:1566,1578,1594,1616`) "✓ korjattu (36px)" mutta §R vaatii 44px — ei oikeasti korjattu, DESIGN.md-merkintä virheellinen. `src/style.css:1480`. | V21 |
| B53 | 2026-07-03 | UX-audit: `.marker-item-checkbox` (`src/style.css:581`) 18×18px ilman min touch-target-aluetta, ei dokumentoitu DESIGN.md:ssä. Rivikohtainen valintaruutu merkkilistassa, selvästi alle 44px. | V21 |
| B54 | 2026-07-03 | `src/map/markers.ts:289` `addLeafletMarker()` sitoo native `el.addEventListener('click', ...)` suoraan marker-DOM-elementtiin joka avaa `MarkerDetailModal`in — Leafletin drag-jälkeinen click-suppressio toimii vain `marker.on('click',...)`:ssä, raaka DOM-listener ohittaa sen. Jokainen dragend laukaisee synteettisen clickin → modaali aukeaa tahattomasti siirron jälkeen. Käyttäjän raportoima 2026-07-03. | V82→T135 |
| B55 | 2026-07-03 | `src/ui/sign-library-panel.ts` `buildRow()` renderoi klikattavan place-napin vain `DEFAULT_IDS`-neljälle tyypille, custom-mallit saavat pelkän staattisen divin. `bindEvents()` ei koskaan sido click-kuuntelijaa `.sign-lib-place-btn`:lle ollenkaan — nappi kuollut myös oletustyypeille. Ainoa toimiva reitti: dblclick kartalla → floating picker (`place-mode.ts`) joka listaa vain `listFavorites()` — custom-malli vaatii ensin suosikiksi merkitsemisen. Käyttäjän raportoima 2026-07-03. | V83→T136 |
| B56 | 2026-07-03 | `src/ui/marker-detail-modal.ts` `buildJarjestajaFooter()` (rivit 232-308) ei sisällä status-transitionappeja — vain `buildTalkoolainenFooter()` renderoi `validActions()`-pohjaiset status-napit. Järjestäjällä ei ole UI-polkua asettaa merkin elinkaari-statusta itse. Käyttäjän raportoima 2026-07-03. | V84→T137 |
| B57 | 2026-07-03 | `src/map/icons.ts` `circleSvg()` toistaa `shortLabel`-tekstin kahdesti (pääympyrä + nurkkabadge) ilman lisäinfoa. Statusindikaattori (`STATUS_DOT_COLOR`) on vain 8px piste — liian huomaamaton, rikkoo VISION.md järjestäjän "tilanne yhdellä silmäyksellä" -UX-testin. Käyttäjän raportoima 2026-07-03. | V85→T138 |
| B58 | 2026-07-03 | T138:n jälkeen käyttäjä totesi statusbadgen värit (#4ade80 asetettu, #93c5fd tarkistettu, #6ee7b7 kerätty) liian samankaltaisiksi — erityisesti asetettu vs tarkistettu ja tarkistettu vs kerätty vaikea erottaa yhdellä silmäyksellä pienestä badgesta. Käyttäjän ehdotus: koko merkin ympyrä vaihtaa väriä TAI ikoni vaihtuu statuksen mukaan. Käyttäjän raportoima 2026-07-03. | V86→T139 |
| B59 | 2026-07-03 | T139:n jälkeen käyttäjä halusi kerätty-tilan tyyppikuvan/-ikonin säilyvän (T139 korvasi sen ✓-glyfillä) — status pitää näkyä ulkoreunassa (`stroke`) sen sijaan että täyttö (kuva) vaihtuu, koska täyttö on se mikä näyttää tyypin. Käyttäjän raportoima 2026-07-03. | V87→T140 |
| B50 | 2026-07-03 | `e2e/sprint-features.spec.ts` T38-testit testasivat `.marker-type-select`-elementtiä `marker-list.ts`:ssä — poistettu, korvattu staattisella `.marker-type-label`-spanilla. Tyypin vaihto siirtyi `MarkerDetailModal.buildJarjestajaFooter()`:iin (`.marker-detail-type-select`, avataan klikkaamalla `.marker-item` listassa). Testi ei päivittynyt siirron mukana. | ✓ testit päivitetty avaamaan detail-modaali |
| B49 | 2026-07-03 | `e2e/critical-paths.spec.ts` "Drag-to-move — T37" luki merkkien sijainnin `localStorage.getItem('karttamaster-markers')` — avain ei enää olemassa, merkit persistoituvat backendiin (`src/map/markers.ts` → `fetch('/api/markers', …)`, ei enää localStorage). Testi kaatui `Cannot read properties of null`. Testi ei päivittynyt kun merkkitallennus siirtyi backendiin. | ✓ testi lukee nyt `fetch('/api/markers')` sivulta, kentät `lat`/`lon`/`route_ids` (snake_case, ks. server/markers.test.ts) |
| B48 | 2026-07-03 | `RoleSelector` (`src/ui/role-selector.ts:13-14`) piilottaa `#btn-role` (`btn.hidden=true`) aina kun `lockedRole !== undefined`. `main.ts:403-407` kutsuu sitä aina todellisella backend-roolilla (ei koskaan `undefined`) → `#btn-role`-toggle on aina piilossa, klikkaus kuollutta koodia. Tarkoituksellinen seuraus oikean tili-per-rooli-authin käyttöönotosta, mutta e2e-testit (critical-paths "Rooli-toggle", sprint-features T28/T38 talkoolainen-variantit) yrittivät yhä klikata sitä ja timeoutasivat. | V80 → testit korjattu kirjautumaan suoraan oikealla roolilla (`mockAuthAsTalkoolainen`); PÄÄTÖS AUKI: poistetaanko `#btn-role`/`RoleSelector`-toggle kokonaan dead codena vai säilytetäänkö admin-käyttöön roolin esikatseluun — kysy käyttäjältä |
| B62 | 2026-07-04 | PM-analyysi: T147 tarkastuskuittaus ei persistoidu. `segments`-taulussa ei `inspected`/`inspection_note`-sarakkeita — `main.ts` onInspect kutsuu `updateSegmentRemote(seg.id,{inspected,inspectionNote})` mutta serverin POST/PUT droppaa kentät & `rowToSegment` ei palauta niitä → kuittaus katoaa refreshillä, järjestäjän tilannekuva näyttää aina "Ei vielä tarkastettu". Lisäksi `PUT /api/segments/:id` on `requireRole('admin','järjestäjä')` → talkoolainen-tarkastaja saa 403 jo ennen droppia (T147-flow + V43 vaativat talkoolais-writen omaan pätkään). `updateSegmentRemote`-paluu ignoroitu (`.catch(()=>{})`) → hiljainen häviö. | V93→T149 |
| B63 | 2026-07-04 | PM-analyysi: `segment-creation-modal.ts` hardcode `phase:'asettaminen'` — järjestäjän T148 phase-vaihtimen purku/tarkastus-näkymässä luotu pätkä syntyy asettaminen-vaiheeseen → katoaa heti näkyvistä (`getSegmentsForPhase`-suodin). VISION "Purku → uudet pätkät purkamista varten (eri jako kuin asettaminen)" mahdoton — ainoa reitti purku-pätkään on identtinen klooni (T146). Lisäksi `showCreationSnapMarkers` saa koko storen → snap-pisteitä piilotettujen vaiheiden pätkistä. | V94→T150 |
| B64 | 2026-07-04 | PM-analyysi: `validateNoOverlap` (`src/logic/segments.ts`) tarkistaa kaikki segmentit phasesta riippumatta — V91 vastaisesti kloonin jälkeen samalle välille ei voi luoda uutta pätkää missään vaiheessa (estää "eri jako per vaihe" -periaatteen). `cloneSegmentToNextPhase` taas ohittaa validoinnin kokonaan → "Kloonaa"-tuplaklikki luo kaksi identtistä saman phasen pätkää (V49-rikkomus). | V95→T151 |
| B65 | 2026-07-04 | PM-analyysi: `SegmentOverlay.update()` värittää pätkät lista-indeksillä (`SEGMENT_COLORS[idx % 6]`) — väri ei kerro statusta/progressia → VISION järjestäjä-UX-testi "näenkö kartalta yhdellä silmäyksellä missä vaiheessa mikäkin pätkä on" ei toteudu (status vain sivupalkin rivillä T143). Väri myös epästabiili: pätkän poisto vaihtaa kaikkien seuraavien pätkien värit. | V96→T152 |
| B66 | 2026-07-06 | PM-analyysi (data-turva ennen tuotantokäyttöä): snapshot + restore kattaa vain `markers`-taulun. `snapshot-scheduler.ts:15` & `admin.ts:107` `SELECT * FROM markers` → `markers_json`; restore `admin.ts:163` `DELETE FROM markers` + re-insert. Segments (pätkäjako), areas, area_features EI missään snapshotissa → järjestäjä palauttaa snapshotin ja menettää koko pätkäjaon + huoltoalueet hiljaa. Puoli suunnittelutyötä ilman turvaverkkoa. Lisäksi serialisointi copy-pastettu kahteen paikkaan (scheduler + route). | V100→T162 |
| B67 | 2026-07-06 | PM-analyysi: automaattinen yövarmuuskopio ei laukea luotettavasti. `snapshot-scheduler.ts` `setTimeout`/`setInterval` in-process, mutta fly.toml `auto_stop_machines="stop"` + `min_machines_running=0` → kone pysähtyy 5min idlen jälkeen, ajastin kuolee koneen mukana. Yökopio klo 03 UTC laukeaa vain jos joku sattuu käyttämään appia juuri silloin → käytännössä ei automaattista app-tason varmuuskopiota. | V101→T163 |
| B68 | 2026-07-06 | Deploy-audit (fly.io/Docker, live v15): `fly.toml [[http_service]]` ei sisällä `[[http_service.checks]]`-lohkoa → fly ei valvo `/api/health`. nginx (:80) palauttaa 200 staattiselle indexille vaikka taustan bun-serveri (`docker-entrypoint.sh` `bun /app/server/index.ts &`) olisi kaatunut → kone ei auto-restarttaa, `/api/*` kuollut hiljaa (401/502 vasta kun käyttäjä osuu). CHECKS-sarake tyhjä `fly status`issa. | V103→T165 |
| B69 | 2026-07-06 | Deploy-audit: `.dockerignore` tyhjä + `Dockerfile` `COPY . .` → build-kontekstiin ja tuotanto-imageen kopioituu `.env`, `dev.db`+`dev.db-wal` (~688KB), `dev.e2e.db`+wal (~222KB), `node_modules`, `.git`, `.htpasswd`. Kehitysdata/salaisuudet vuotaa imageen + turha bloat. | V104→T166 |
| B70 | 2026-07-06 | Deploy-audit: `VITE_MML_API_KEY` kovakoodattu `fly.toml [build.args]`:iin (committed gitiin). Avoin MML-avain päätyy client-bundleen joka tapauksessa → riski matala, mutta secret ⊥ versionhallintaan. | T167 |
| B71 | 2026-07-06 | Deploy-audit: `.htpasswd` repossa mutta `nginx.conf` ei viittaa siihen (`auth_basic` puuttuu) — kuollut tiedosto, harhaanjohtava (näyttää suojaukselta jota ei ole). Poisto kattuu T166:n .dockerignore-excludeen. | T166 |
| B72 | 2026-07-06 | Deploy-audit (secret-vuoto): `.gitignore` (rivi 4-5) kattaa vain `.env.local`+`.env*.local`, EI `.env`:iä. `.env` sisältää `ADMIN_PASSWORD`+`ADMIN_USERNAME` plaintextinä → `git add .` committoisi admin-salasanan (nyt untracked, mutta suojaamaton). Yhdessä B69:n kanssa (tyhjä `.dockerignore` + `COPY . .`) `.env` bundlautuu Docker-imageen → admin-salasana plaintextinä prod-image-layerissa. HUOM: runtime lukee `process.env`istä jonne fly secrets surfataan → tuotannon *auth* toimii oikein fly-secretillä, tämä on vuoto-, ei toiminta-bugi. | V105→T168 |
| B73 | 2026-07-06 | Deploy-audit: `seedAdmin` (`server/db.ts:157`) `if (exists) return` → seedaa admin-käyttäjän vain kerran. Fly `ADMIN_PASSWORD`-secretin rotaatio ei päivitä olemassaolevan admin-käyttäjän `password_hash`ia → salasanan vaihto secretissä on no-op, vanha salasana jää voimaan. Salasanan vaihto vaatii manuaalisen DB-updaten tai `reset-password`-reitin. | T169 |
| B74 | 2026-07-07 | `src/map/markers.ts` kaikki `createSignIcon()`-kutsut (rivit ~204,215,228,260) välittävät `iconId`-parametrin aina `undefined`:na — vain `signImageSrc(m.type)` välitetään kuvaksi. `SignTemplate.iconId` (V50/T93, Lucide-ikoni ilman kuvaa) ei koskaan päädy kartalle: merkki näyttää compactLabel-fallbackin (V99 tier 3) vaikka ikoni (tier 2) pitäisi näkyä. `SignMarker`-tyypissä (`src/logic/types.ts`) ei ole `iconId`-kenttää lainkaan — data puuttuu renderöintihetkellä kokonaan. Löytyi yhdistelmämerkki-speciköinnin yhteydessä (combo-sign-feature riippuu tästä: per-slotti ikonin pitää renderöityä). | V106 → T170 ✓ |
| B75 | 2026-07-07 | `e2e/critical-paths.spec.ts:56` "dblclick kartalla avaa floating pickerin kirjaston suosikeilla" odottaa `#floating-picker .sign-type-btn` määrää 4, saa 10 — testi jäänyt jälkeen kirjaston kasvusta (`sign-library-panel.ts` `seedDefaults`/`signCatalog` tuottaa nyt enemmän `favorite:true`-oletusta kuin testin kirjoitushetkellä). Ei regressio — vahvistettu `git stash`illa ennen T170-muutoksia, sama epäonnistuminen jo silloin. Löytyi T170:n Playwright-ajon yhteydessä. | T173 |
| B76 | 2026-07-07 | Käyttäjä raportoi: uusi iconId-malli näyttää oikein sivupaneelissa (`sign-library-panel.ts buildRow`), mutta floating-pickerissä (`place-mode.ts openPicker()`, dblclick kartalla) näkyy aina compactLabel-lyhenne, ei koskaan ikonia — `openPicker()`-swatch renderöi vain `compactLabel(t.label)` + `signImageTag`, ei koskaan tarkistanut `t.iconId`:tä (V99-precedence puuttui kokonaan tästä yhdestä renderöintipaikasta, vaikka sivupaneeli ja `signPreviewHtml` tekivät sen oikein). Korjattu heti: `openPicker()` käyttää nyt samaa `iconEntry ? renderIconSvg : compactLabel`-logiikkaa kuin `buildRow`. | V99 → ✓ korjattu (place-mode.ts) |
| B77 | 2026-07-07 | Käyttäjä raportoi: kutsulinkki (uusi järjestäjä) ja salasanan reset eivät toimi UI:ssa — linkin avaaja näkee pelkän normaalin login-lomakkeen, ei rekisteröinti/reset-lomaketta. Syy: backend-reitit toimivat (`GET /api/auth/invite/:token`, `POST /api/auth/register`, V76-token-mekaniikka), mutta frontendissä ei ole mitään joka route:aisi `/auth/invite/:token`-polun — `auth-screen.ts` (T51) tunnistaa vain `/s/<code>`-polun, ei invite-tokenia. Puuttuva client-side route, ei backend-bugi. | V110 → T177 |
| B81 | 2026-07-07 | UX-audit (`/karttamaster-ux`, mobiili ≤480px): `#left-panel` (`src/style.css:2257`) on 260px kiinteä `flex-shrink:0` -sibling `#map-area`:lle (`#app-main` row-flex). `LeftPanel` avautuu oletuksena auki (`collapsed=false`). 375px-viewportissa paneeli vie 260px → kartalle jää ~115px sliver, ei overlay. Osasyy käyttäjän "puoli karttaa"-raporttiin (yhdessä B79-invalidateSizen kanssa). Koko `src/style.css`:ssä ei yhtään leveys-`@media`-breakpointtia — layoutia ei ole koskaan sovitettu kapealle näytölle. Rikkoo VISION "kartta on pääasia myös mobiilissa". | V114 → T181 |
| B80 | 2026-07-07 | Käyttäjä raportoi (mobiili): oikean yläkulman valikko herkkä klikkauksille, moodi vaihtuu vahingossa. **Alkuperäinen analyysi (kolme nappia rykelmänä, `#btn-role`-vahinkotoggle) osoittautui T180-suunnitteluvaiheessa vääräksi** — `#btn-role` on aina piilossa ajossa (V80), `#btn-gps` piilossa järjestäjältä, joten järjestäjän toolbarissa on ajonaikaisesti vain 1 nappi. Oikea juurisyy: `src/ui/phase-switcher.ts` `<select>` (Asetus/Tarkastus/Purku, "moodi") overflow-valikon sisällä — `src/app/map-init.ts` dokumentti-tason `click`-sulkijalistener sulkee `#toolbar-menu`:n kesken select-interaktion (ei poikkeusta select-elementille), mikä katkaisee natiivin pudotusvalikon käytön ja tuntuu "herkältä". | V113 → T180 |
| B79 | 2026-07-07 | Käyttäjä raportoi (mobiili): kartan puolikas-lataus. `invalidateSize()` ei esiinny koodissa lainkaan. `left-panel.ts:15 toggle()` sulkee/avaa paneelin CSS-luokalla (`applyState()` → `classList.toggle('collapsed')`) ilmoittamatta Leafletille kontainerin resizesta → laatat jäävät vanhaan viewport-kokoon = puoli karttaa. Mobiilissa sama oire kun osoitepalkki piiloutuu/ilmestyy scrollatessa (viewport-korkeus muuttuu): sivu aukeaa paneeli auki, käyttäjä sulkee paneelin + scrollaa → puoli karttaa. Ei `resize`/`orientationchange`/`visualViewport`-kuuntelijaa joka kutsuisi invalidateSizea. | V112 → T179 |
| B78 | 2026-07-07 | Käyttäjä raportoi: SignLibraryPanel edit-modaalissa (T178) pää-visuali (yllä, yksittäinen ikoni/kuva-valinta) ja "Osat"-lista (yhdistelmämerkki, alla) olivat kaksi täysin erillistä tilaa — kun järjestäjä valitsi pää-ikonin ja sitten klikkasi "+ Lisää osa", ensin valittu merkki EI päätynyt osalistalle, vaan piti valita se uudestaan osana. Juurisyy: `selectedIconId`/`selectedImageId` (top) ja `selectedParts` (lista) olivat aina olleet arkkitehtuurisesti riippumattomia muuttujia (T171: "erillinen valinnainen lisä ylemmän yksi-ikoni-kentän rinnalla, ei korvaa sitä") — kukaan ei kirjoittanut synkronointia kun T178 lisäsi kuvatuen osiin. Korjattu: `comboActive`-tila + `syncPart0FromTop()`/`syncTopFromPart0()` pitävät top-valitsimen ja `parts[0]`:n synkassa molempiin suuntiin (top→parts kun valitaan yläosasta, parts→top kun poistetaan/järjestetään osalistalta). "+ Lisää osa" -klikkaus siirtää senhetkisen top-valinnan listan riville 1 automaattisesti ennen pickerin avaamista. | V111 → ✓ korjattu (sign-library-panel.ts) |
| B82 | 2026-07-07 | Käyttäjä raportoi + PM-tutkinta prod-DB:stä suoraan: tuotannon `markers`-taulussa 0 riviä vaikka käyttäjä tehnyt paljon kylttejä kartalla; yövarmuuskopiot tyhjiä (`markers_json` len=2 = `"[]"`) 2026-06-11 asti. Juurisyy: `src/map/markers.ts` `apiPost()`/`apiPut()`/`apiDelete()` — `fetch(...).catch(() => {})` nappaa vain verkkovirheen, ei koskaan tarkista `response.ok`. `POST /api/markers` vaatii `requireRole('admin','järjestäjä')` (`server/routes/markers.ts:47`) — jos kirjoitus torjutaan (401/403/muu 4xx/5xx) fetch silti resolvaa onnistuneesti, virhe ei koskaan näy. Lokaali `this.markers.push()` + Leaflet-piirto tapahtuu ennen serverivastausta → merkki näkyy kartalla vaikka tallennus kaatuu palvelimella joka kerta, ei UI-ilmoitusta, ei retry. Sama antipattern kuin B62:ssa segmenteille. | V115 → T182 |
| B83 | 2026-07-07 | PM-tutkinta (käyttäjä epäili "fly-DB toimii eri lailla"): epäily KUMOTTU — WAL persistoi reopen-testissä fly-volumella, `/data` mountattu oikein, `DB_PATH` oikea. Prod `markers=0` -oireen VAHVISTETTU juurisyy on B82 (client hiljainen fail), jo speksattu T182–T187 (T183 [Kriittinen] auki = varsinainen datakadon korjaus). Erillinen, aiemmin kirjaamaton SERVER-durability-aukko silti olemassa: `server/db.ts:7` asettaa `journal_mode=WAL` mutta jättää `synchronous`-arvon Bunin oletukseen NORMAL (ei fsync per-commit); `server/index.ts` ei rekisteröi SIGTERM/SIGINT-checkpointtia eikä `db.close()`. `fly.toml auto_stop_machines=stop` pysäyttää VM:n 5min idlestä (sama ilmiö V101/B67, siellä ratkaistu vain schedulerille). Kun T183 saa clientin toimittamaan kirjoitukset luotettavasti, palvelin ei saa hukata niitä checkpointtaamattomassa WAL:ssa VM-stopin yhteydessä. Tod.: main `/data/karttamaster.db` 4KB, mtime luontihetkessä — WAL foldautuu main-tiedostoon vain clean-closessa jota `auto_stop` ei tee. EI vahvistettu nykyisen datakadon PÄÄsyyksi (B82 on) — defensiivinen toinen linja. | V120 → T188 |
| B84 | 2026-07-07 | Prod-loki (deployn + diagnostiikkalokituksen jälkeen) paljasti B82:n VARSINAISEN alla olleen juurisyyn (T182 kohta c, aiemmin selvittämättä): `POST /api/markers` kaatuu 500 — `SQLiteError: NOT NULL constraint failed: markers.bearing` (`server/routes/markers.ts:78` INSERT). Tuotannon `markers`-taulu (luotu 2026-06-11 ennen T129/T132 bearing-poistoa) säilyttää `bearing REAL NOT NULL` -sarakkeen ilman defaultia; koodi ei enää INSERT-aa bearingia → JOKAINEN merkkikirjoitus kaatuu → `markers`=0, tyhjät yösnapshotit. bearing poistettiin `CREATE TABLE`-lauseesta muttei koskaan DROP-migraatiolla; `CREATE TABLE IF NOT EXISTS` ei koske olemassa olevaan tauluun, ja dev-DB syntyi puhtaana (ei bearingia) → lokaali build/testit eivät paljastaneet. EI siis auth (401/403) EIKÄ WAL-durability (B83) — nämä olivat sivupolkuja. Korjaus: idempotentti `ALTER TABLE markers DROP COLUMN bearing` `server/db.ts`-migraatioissa. | V121 → ✓ korjattu (server/db.ts) |
| B85 | 2026-07-07 | T183-T187-durabiliteettiauditin sivulöytö: alue-kirjoitukset (huoltoalueet) kärsivät samasta B82/V115/V116-antipatternista kuin merkit ennen korjausta. `src/logic/area-sync.ts` `updateArea()`/`deleteArea()` ovat puhtaita fire-and-forgeteja — `await fetch(PUT/DELETE)` ei tarkista `res.ok`:ia, ei kulje durable-outboxin kautta, ei surfaa virhettä (koodikommentti: "silent — overlay already updated in-memory"). `createArea()` tarkistaa `res.ok` mutta kutsuja (`areas-wiring.ts:25`) ei käytä paluuarvoa eikä ilmoita virhettä; ei outboxia. `fetchAreas()` palauttaa `[]` virheessä → V118-gap. Järjestäjän aluemuokkaus/-poisto voi siis kadota hiljaa 401/500/verkkovirheessä. Juurisyy: aluekerros (T108-T116) rakennettiin ennen outbox-durabiliteettia (T183) eikä liitetty V116:n piiriin. | V116,V118 → T190 |
| B86 | 2026-07-08 | Mobiili: Leaflet zoom-kontrolli (+/-) on kartan vasemmassa yläkulmassa (`map-init.ts:14` `L.map('map', {...})` — ei `zoomControl`-position-overridea → Leaflet default `topleft`). Sivupalkin `▶`-toggle (`left-panel.ts`, `collapsed`-default mobiililla `window.innerWidth <= MOBILE_BREAKPOINT`) on samassa vasemmassa yläkulmassa → +/- napit peittää `▶`-napin, talkoolainen ei saa sivupalkkia auki puhelimella. Juurisyy: zoom-kontrolli ja panel-toggle kilpailevat samasta topleft-ankkurista. | V122 → T191 |
| B87 | 2026-07-08 | Mobiili: merkin valinta sivupalkin listasta (`marker-list.ts:156` — `onOpenDetail(id)`) ei sulje `#left-panel`ia. Mobiilissa (≤480px, V114/T181) left-panel overlayaa kartan (`z-index:500`), joten valittu merkki ja avautuva detail-modaali jäävät ahtaan paneelin taakse — järjestäjä ei näe merkkiä kartalla eikä saa tilaa. Juurisyy: select-handler avaa detailin muttei collapse:a overlay-paneelia. | V127 → T197 |
| B88 | 2026-07-08 | Mobiili: `marker-detail-modal.ts:60` `open()` kutsuu `.marker-detail-note`.focus() → virtuaalinäppäimistö aukeaa automaattisesti heti modaalin avautuessa. Näppäimistö vie ~puolet ruudusta ennen kuin käyttäjä ehtii lukea merkin tietoja; ahtaassa mobiilinäkymässä hukkaa kriittistä tilaa. Juurisyy: pakotettu autofocus textarea-kenttään avausvaiheessa. | V128 → T197 |
| B89 | 2026-07-08 | T198-testaajan löytö: `src/ui/marker-visual-row.ts:101` `.marker-visual-row-zoom` klikattava hit-area on `20×20px` — alle DESIGN.md §A:n 44×44px touch-target-vaatimuksen ja ristiriidassa T198-taskin oman speksitekstin kanssa. Juurisyy: zoom-nappi kirjoitettiin vain näkyvän badgen kokoisena, ei erotettu näkyvää glyfiä (pieni) ja klikattavaa hit-aluetta (44px) — sama pattern joka jo ratkaistu `sign-library-panel.ts` `.sign-image-zoom-btn`:ssä (44px hit-area, 18px näkyvä badge sisällä) ei toistunut uudessa komponentissa. | V129 → ✓ korjattu (marker-visual-row.ts) |
| B90 | 2026-07-08 | UX-tarkistuksen löytö T198:sta: `buildMarkerVisual` käytti kiinteää `var(--accent)` (oranssi) taustavärinä kaikille ikoni/label-merkeille riippumatta merkin todellisesta tyypistä/väristä. Rikkoo V87:n ("täyttö on aina tyyppiväri") — lista olisi näyttänyt kaikki merkit samanvärisinä vaikka kartalla ne ovat eri värisiä (esim. Vasemmalle=sininen `#2563eb`, Oikealle=vihreä `#16a34a`), mikä olisi tuhonnut kartan↔listan tunnistettavuuden — juuri se ongelma jonka T198/T199 piti korjata. Juurisyy: uutta komponenttia kirjoittaessa ei katsottu miten `src/map/icons.ts` `circleSvg`/`comboMarkerSvg` resolvoi värin (`marker.color ?? SIGN_TYPES-tyyppiväri ?? neutraali`) — kopioitiin vain rakenteellinen pattern (kuva>ikoni>label) muttei väri-precedenceä. Korjaus: `resolveColor()` samalla precedence-ketjulla, käyttää `SIGN_TYPES`-taulukkoa (`src/logic/sign-picker.ts`, jo olemassa pure export). | V87 → ✓ korjattu (marker-visual-row.ts) |
| B91 | 2026-07-08 | Käyttäjä raportoi (mobiili): uuden merkin muokkausmodaalin "Kuva"-välilehden kuvagalleria ei näytä valittavia thumbnaileja puhelimella; desktopilla näkyy normaalisti. PM-tutkinta: EI latausvika, EI deploy-ongelma, EI server. webp bundlaantuu tuotanto-buildiin (`bun run build` → 65 tiedostona + loput inline, 89 lähdettä `src/assets/signs/`), nginx serveroi (`try_files $uri` OK), `.dockerignore` ei blokkaa `src/assets`. Fly-logit 2026-07-08 puhtaat: ei asset-404, ei 500, ei kuvavirhettä (sivulöytö: `DELETE /api/markers/df78c657… → 404 (admin)` toistuu ~10× = outbox retryaa jo-poistetun merkin poistoa — erillinen, ei tämä bugi). Sama `signImageSrc()`-URL (Vite-glob, `sign-images.ts`) toimii desktopilla → `onerror="this.remove()"` ei laukea, kuva latautuu mutta layout piilottaa. Juurisyy: sisäkkäiset scroll-containerit — modaali (`sign-library-panel.ts:249` `max-height:80vh;overflow-y:auto`) sisältää `.sign-image-gallery`n (rivi 379 `max-height:min(50vh,420px);overflow-y:auto`) flex-column-lapsena → matalalla mobiiliviewportilla galleria collapsaa; `style.css` `@media (max-width:480px)` (rivi 2369) käsittelee vain `#left-panel`in, modaalin mobiililayoutia ei ole koskaan sovitettu. EI liity T195/T196:een. | V130 → ✓ korjattu (sign-library-panel.ts `.sign-image-gallery` flex-shrink:0) |
| B92 | 2026-07-08 | Järjestäjä/admin-loginissa route-vis-pillerit tuplautuvat kartan alanurkassa ("35km 55km 35km 55km"), sivun refresh korjaa. `src/map/route-visibility-control.ts:70-77` `buildPills()` tekee `container.appendChild(pill)` tyhjentämättä `#route-selector`-containeria ensin. Tuore login-polku (`auth-screen.ts finishAuth → onAuthenticated → main.ts:122 init() → markers-wiring:152 new RouteVisibilityControl`) laukeaa kahdesti (vs. `/api/auth/me`-refresh joka ajaa `init()`:n kerran) → append-only-render lisää pillerit kahdesti. | V137 → T209 |
| B93 | 2026-07-08 | Karttamerkki on suurimmillaan "miltei" loppuzoomissa mutta näyttää **pienenevän** kun zoomaa aivan loppuun asti (käyttäjä toivoo: loppuzoomissa saa olla suurempi kuin miltei-lopussa). `src/logic/marker-scale.ts` `MARKER_SCALE_ZOOM_MAX=17` + `MARKER_SCALE_MAX=1`, mutta kartan `maxZoom=19` (`src/logic/tile-layers.ts:25,38`). Zoom 17→19 merkin scale on klampattu 1:een (kiinteä ruutukoko) samalla kun kartta jatkaa zoomausta 2 tasoa → karttadetalji kasvaa, merkki ei → merkki näyttää kutistuvan suhteessa loppuzoomissa. `markerScaleForZoom` skaalaa vain 11→17. | V138 → T210 |
| B94 | 2026-07-08 | Talkoolaisen merkkimodaali menetti pätkäsuodatuksen ekan status-muutoksen jälkeen: `markers-wiring.ts` re-render-callbackit (MarkerManager `onUpdate` :75, modal-refresh :92, outbox-change :102) välittivät `renderMarkerListille` `segmentMarkerIds=undefined` → `marker-list.ts:63` näytti kaikki reitin merkit. Vain modaalin avaus (`openMarkerModal`) laski suodatuksen oikein. Talkoolainen näki "väärät merkit" heti kun kuittasi yhden. | V144 → ✓ `currentSegmentMarkerIds()`-helper välitetään kaikkiin kolmeen kutsuun |
| B95 | 2026-07-08 | Talkoolaisen "Aseta" asetti "randomin": `nearestUnsetMarker` (`navigation.ts`) suodatti vain reitillä (`routeIds.includes(routeId)`, EI pätkän `[startDist,endDist]`) ja valitsi lähimmän kursoriin/scrubber-sijaintiin (`min|distanceFromStart−currentDist|`), ei pienintä `distanceFromStart`. Scrubberin liikuttua valittu merkki oli mikä tahansa lähin suunniteltu — myös toisen talkoolaisen pätkältä. | V145 → ✓ `firstUnsetMarker` (pienin distanceFromStart) SegmentView-herossa + GpsDrivePanel rajattu `getMarkersForSegment`-merkkeihin |
| B96 | 2026-07-08 | Purku bulk-kuittaus heitti "herjaa": `markers-wiring.ts:114` sovelsi `bulkCollect`-outputiin (jo `kerätty`-tilassa) `updateStatus(m.id, 'kerää')`-actionia. `transitionStatus('suunniteltu','kerää')`/`('ei_tarpeen','kerää')` heittää "Virheellinen siirtymä" (ei siirtymää) → uncaught throw mid-loop, osittainen päivitys, EI virhebanneria. Purku-pätkä jossa suunniteltu/ei_tarpeen-merkki kaatoi kuittauksen. | V146 → ✓ `bulkSetStatus(ids, 'kerätty')` |
| B97 | 2026-07-08 | Write-outbox retryasi pysyvät 4xx-virheet ikuisesti: `attemptEntry` (`write-outbox.ts:168`) piti jonossa KAIKKI ei-2xx (`isOk` vain 200–299). Pysyvä 403/400/404 (esim. talkoolaisen estetty POST) → entry ei koskaan poistu, merkki jää "tallentamatta"-tilaan pysyvästi, virhebanneri toistuu joka retry-kierroksella (startup/'online'/30s). Vain 401 sai erikoiskäsittelyn. | V147 → ✓ `isPermanent`-luokittelu: pysyvä 4xx → dead-letter + kertaviesti; 401/408/429/5xx/null jäävät jonoon |
| B98 | 2026-07-09 | UX (Playwright-kävely): alueiden latausvirhe (`fetchAreas` fail) näytti talkoolaiselle pysyvän "⚠ Alueiden lataus epäonnistui — päivitä sivu" -bannerin joka peitti pätkänäkymän otsikon (nimi+edistymispalkki). Alueet ovat talkoolaiselle vain kontekstia (noutopisteet), ei työkalu → ei saa peittää ydinnäkymää. | ✓ `main.ts` wireAreas onLoadError vain järjestäjälle (`if (!talkoolainenCode)`) |
| B99 | 2026-07-09 | UX (Playwright-kävely): talkoolaisen latausnäkymässä kaksi identtistä "Aseta"-pintaa — uusi "Seuraava merkki" -hero (ylhäällä) JA alapalkin gps-drive-panel (`gpsDrivePanel.update(0)` pakotti sen näkyviin heti). Molemmat samaan seuraavaan merkkiin → sekava. VISION: "jos GPS ei ole päällä, näyttää listan" → hero on oletusohjaus. | ✓ poistettu eager `gpsDrivePanel.update(0)`; paneeli näkyy vasta kun talkoolainen navigoi (scrubber/◀▶/GPS → driveMode.onProgress) |
| B100 | 2026-07-09 | Talkoolaisen "Aseta" (status-PUT) herjasi "⚠ Tallennus estetty — ei oikeutta" (403) merkeille jotka pätkänäkymä näytti `linked_marker_ids`- tai `marker_type_filter`-kautta. Syy: backend `talkoolainenMayPlace` tarkisti VAIN reittirangen, kun frontend `resolveTaskMarkers` näyttää pätkän merkit unionina (route+dist ∪ linked ∪ typeFilter). Kerrokset erimielisiä "oma pätkä" -jäsenyydestä. T222/V150-muutos laajensi gaten status-kenttään → aukko paljastui. | ✓ kanoninen `markerInOwnSegment` (`server/marker-audit.ts`) peilaa unionia; V154 vahtii; +ε=50m. Bun: link/typeFilter-status 200, ε-reuna 201 |
| B102 | 2026-07-09 | Orpo E2E-testi: `critical-paths.spec.ts` "Drive mode › käynnistyy + navigoi eteenpäin" ajoi talkoolaisen `#btn-route-next`/`#route-km`-drive-kontrolleja, jotka T224/V148 PIILOTTI (talkoolaisen `#route-bar` hidden; järjestäjällä drive-kontrollit piilossa V134) → drive-UI tavoittamaton molemmilla rooleilla, testi klikkasi näkymätöntä nappia → timeout. Ristiriidassa sisartestin "RouteBar-jako → talkoolainen: alapalkki piilotettu" kanssa. Juurisyy: T224 lisäsi piilotus-assertit muttei poistanut vanhaa drive-testiä. Sivulöytö: `DriveMode`-luokka (`src/map/drive.ts`) + route-bar-drive-kontrollit + `main.ts:117-119` ArrowRight/Left-nav ovat nyt UI:sta tavoittamattomia (dead code) → erillinen siivoustaski. Korjaus: poista orpotesti osana T233. | V148 → T233 |
| B101 | 2026-07-09 | PM+ux-refaktori-löytö (talkoolaisnäkymä): orpo alapalkki-tila. `body[data-role="talkoolainen"] #app-main{padding-bottom:84px}` (style.css:2774) + `#app{padding-bottom:84px}` (style.css:145) varaavat 84px `#route-barille`, mutta talkoolaiselle `#route-bar` on `hidden` (markers-wiring.ts:271, T224/V148) → kuollut alagap kartan alla, kartta jää pienemmäksi kuin voisi (VISION r131 kartta=päänavigointi). Juurisyy: T224 piilotti route-barin muttei poistanut sille varattua padding-tilaa talkoolaiselta. Korjaus osana T233. | V157 → T233 |
| B103 | 2026-07-21 | Merkin/pätkän/alueen luonti hajoaa HILJAA (ei user-herjaa) insecure-kontekstissa (http + ei-localhost, esim LAN-dev http://192.168.1.132:5173). Selainloki "TypeError: crypto.randomUUID is not a function" @ src/map/markers.ts:181 MarkerManager.add ← PlaceMode.placeArmedAt (place-mode.ts:46) ← main.ts:94. Juurisyy: crypto.randomUUID() vain secure contextissa (https/localhost) → insecuressa undefined → heitto unhandled → placeArmedAt kaatuu ennen näkyvää palautetta. Guardaamattomat: markers.ts:181, segments.ts:53, area-panel.ts:339/360/368/380/388. Vastakohtana write-outbox.ts:54-56 tekee jo oikean guard+fallbackin. Tuotanto fly.io=https ei osu; LAN-dev + talkoolaisen http-avaus osuu. | V160 → T238 |
| B105 | 2026-07-21 | Talkoolaisen kommenttilomake (`.comment-thread*`, CommentThread) tyylitön — `src/style.css` EI sisältänyt yhtään `.comment-thread`-sääntöä → selaimen defaultit: `.comment-thread-input` textarea 178px kapea + monospace; `.comment-thread-icon-select`/`-name` natiivi 19px korkeat (« 44px touch). "Ei toimi yhteen" muun UI:n kanssa; "Ei ikonia" -valitsin orpo (ei labelia). Korjaus: DESIGN-tokenit (field-tint/border-default/radius-sm) + 44px touch + `font-size:16px` (iOS-zoom) + full-width textarea/Lähetä + näkyvä "Ikoni ja nimi (valinnainen)" -label + "Lisää ⋯" himmeä teksti → bordered napiksi. §K CommentThread lisätty DESIGN.md:hen. Commit 4f47b11 (T254 yhteydessä). | T254-fix |
| B106 | 2026-07-21 | Kartan pätkä-label ("Pätkä 1", `.segment-label` Leaflet-tooltip) lukukelvoton VAALEASSA teemassa: `background: rgba(15,23,42,0.85)` (fixed tumma navy) mutta `color: var(--text-body)` joka vaaleassa teemassa = `#17221D` (tumma) → tumma teksti tummalla pillillä. Käyttäjä: "Pätkä 1 näkyy tummalla kartalla eikä sitä saa kunnolla näkyviin." Juurisyy: teemariippuvainen tekstiväri fixed-tummalla taustalla. Korjaus: `color:#fff` (tausta on AINA tumma → teksti AINA vaalea, ei teematokenia). Playwright: computed color rgb(255,255,255). | T260-fix |
| B107 | 2026-07-21 | Kartan pätkä näytti latauksessa "liian lyhyeltä": yli 4 km pätkä zoomattiin VAIN ensimmäiseen 4 km:iin. Juurisyy: `planSegmentZoom` (`src/logic/segment-zoom.ts`) `maxFitLengthM=4000` → pitkä pätkä `mode:'anchor'`, `endDist=startDist+4000` → `fitMapToSegment` slice kattoi vain alkupään → koko pätkä ei näkynyt. Alun perin tahallinen T224/D (merkit isompina), mutta käyttäjä 2026-07-21: fittaa AINA koko pätkä. Korjaus: planSegmentZoom aina `mode:'fit'` koko välille [startDist,endDist], anchor-haara + maxFitLengthM poistettu. | V185 → T265 |
| B104 | 2026-07-21 | Inventaario-headerin mobiili-vaakavuoto (375px, järjestäjä primääri): T251 lisäsi "✎ Muokkaa" -mode-togglen `.inventory-header-actions`iin → 3 nappia (✎ Muokkaa/← Kartta/Kirjaudu ulos) + h1 ei mahdu yhdelle riville, `.inventory-header-actions` työntyi ~n px yli näytön. Juurisyy: `#inventory-header` + `.inventory-header-actions` `flex` ilman `flex-wrap` → napit eivät rivittyneet. Löytyi T253-e2e-ajossa (inv-mobile "ei vaakavuotoa"); T251:n oma e2e ei kattanut mobiilia. Korjaus: `flex-wrap:wrap` molempiin (style.css). Sivuvaikutus: inv-mobile.spec.ts-testit klikkasivat T251:n piilottamia mutaationappeja read-modessa → 3 testiä lisäsi `#inv-mode-toggle`-klikin edit-tilaan (test-maintenance). Korjattu T253:n yhteydessä. | T253-fix |
| B108 | 2026-07-21 | Mobiili-scroll-audit (käyttäjä: "kaikki vähän liikkuu, modal on paikallaan"). Kolme juurisyytä pienellä puhelinruudulla (320–360px). (1) **Scroll chaining:** yksikään sisäinen scroll-alue (`#marker-modal-items`, `#segment-view`, `#segment-panel`, `#toolbar-menu`, `#left-panel-content`, `.equipment-modal-body`, `.snapshot-modal/-list`, `.segment-list/-details-modal-body/-audit-items/-creation-marker-checklist`, `.marker-detail-body`, `.inv-sign-picker`) EI määrittänyt `overscroll-behavior`ia → listan reunaan asti scrollatessa liike valui taustan Leaflet-karttaan/toiseen paneeliin ("kaikki liikkuu" fixed modalin takana). (2) **Koko-sivun rubber-band + 100dvh-hyppy:** `html,body` scrollasi vapaasti → mobiilin pull-to-refresh + address-barin piiloutuminen kesken eleen → 100dvh-reflow työnsi koko layoutin. (3) **`#toolbar-menu` ilman `max-height`ia:** järjestäjän valikko (tili+teema+vaihe+varmuuskopiot+gpkg) kasvoi yli 568px-ruudun → alimmat kohteet route-barin takana eikä valikkoa voinut scrollata. (4) **`.bulk-status-toolbar` vaakaleikkaus:** [checkbox]+[select]+[Aseta-nappi] ei mahtunut 294px-modaaliin → "Aseta (0)" leikkautui. Korjaus (CSS-only, tuettu 2022+): `overscroll-behavior:contain` kaikkiin scroll-alueisiin + `-webkit-overflow-scrolling:touch`; `overscroll-behavior:none` globaali (kill pull-to-refresh) + `overflow:hidden` VAIN karttasivun kuoreen (`html:has(#app),body:has(#app)` — EI inventaario/admin -normaali-flow-sivuille); backdropeille `touch-action:none;overscroll-behavior:none` (drag backdropin päällä ei panoroi karttaa); `#toolbar-menu` `max-height:calc(100dvh-68px);overflow-y:auto`; `.bulk-status-toolbar` `flex-wrap:wrap`, `.bulk-status-select flex:1 1 140px`. Verifiointi: Playwright mobiili-audit (320/360px) + koko mobiili-regressiosetti 29/29 vihreä (inventaario-scroll ehjä). DESIGN.md §R "Scroll ja overscroll" lisätty. | V187 |
| B109 | 2026-07-22 | Pätkä-linkin luonti hankala: järjestäjä luo talkoolais-linkin `SegmentDetailsModal`-modaalissa, mutta `segment-details-modal.ts:444` (linkin luonti) ja `:410` ("Muuta"-haara) kutsuvat `this.close()` heti onnistuneen `POST /api/admin/codes`-jälkeen → modaali katoaa ENNEN kuin järjestäjä näkee tai kopioi juuri luomansa `/s/<koodi>`-linkin. Pitää avata pätkä uudelleen kopioidakseen. Käyttäjäpalaute 2026-07-22 ("tekee linkin mutta modal sulkeutuu"). Korjaus: älä sulje modalia linkin luonnin/muutoksen jälkeen — re-renderöi assign-osio näyttämään linkki + kopio-nappi samassa näkymässä. | V192 → T276 |
| B110 | 2026-07-22 | E2E T38 (`sprint-features.spec.ts:83` "järjestäjä näkee type-selectin merkin detail-modaalissa") FLAKY (1/3 fail, ei koodibugi): `page.click('.marker-item')` TIMEOUT 30s. **Root cause VAHVISTETTU (repeat-each=3 → 1 fail / 2 pass):** race. `auth-screen.ts:55-59` `this.hide()` poistaa `#auth-screen` `open`-luokan ENNEN `onAuthenticated`→async `init()`:iä; `init()` awaittaa `fetchMarkers`+`loadGpx`(2 GPX) ja kiinnittää `#btn-list`-click-handlerin vasta lopussa. Testin `expect(#auth-screen not open)` läpäisee heti `hide()`:stä → `page.click('#btn-list')` osuu ikkunaan jossa handler ei vielä kiinni → klikki katoaa → modal ei aukea → `.marker-item` ei renderöidy. EI hub-regressio (testi 2026-07-03, marker-list.ts 0 diffiä branchilla — latentti flaky jonka T272-auth-gate teki todennäköisemmäksi). EI tuotantobugi: käyttäjä ei klikkaa sub-sekunnin ikkunassa, mikään ei riko pysyvästi. Sivutuote: löysin latentin footgunin `marker-list.ts:99` (tyhjä Set truthy) → V193, EI tämän bugin syy. | T277 ✓ testfix (retry `#btn-list`-avaus `.toPass()`-loopilla kunnes `.marker-item` renderöityy; 5/5 pass). V193 = erillinen latentti hardening. |
| B111 | 2026-07-24 | FOUC (flash of unstyled content) — sivut näyttävät tyylittömältä ruman välähdyksen ennen kuin oikea UI latautuu. **Juurisyy 1 (prod):** Leaflet CSS haetaan ulkoiselta CDN:ltä `index.html:7` (`https://unpkg.com/leaflet@1.9.4/dist/leaflet.css`) → render-blocking verkkopyyntö unpkg:hen joka latauksella → kartta+layout renderöityy tyylittömänä kunnes CSS saapuu; hidas/katkonainen yhteys (talkoolainen metsässä!) = pitkä flash, offline ei toimi lainkaan. Leaflet npm-paketti jo asennettu (`package.json` `leaflet@^1.9.4`) → CDN täysin turha. **Juurisyy 2 (dev):** app-CSS tuodaan JS-moduulista (`import './style.css'` main.ts/patkat.ts/inventory.ts/admin.ts) → Vite injektoi tyylit vasta moduulin ajon jälkeen = dev-FOUC aina (prodissa Vite extractaa `<link>`:iksi joten yksin ok, mutta yhdessä CDN-leafletin kanssa prod flashaa silti). | V194 → T278 |
| B112 | 2026-07-25 | `auth-screen.ts:230` `loginTalkoo` → `finishAuth({code: pendingCode ?? undefined})` → `role-view.ts wireAuth` → `onAuthenticated(code)` = main.ts init `/`-kartalla. ⊥ redirectiä `/patkat`iin kun koodi puuttuu. Spec-gap: T272 määritteli vain deep-link-haaran; V188/T271 lupasi hubin landingiksi mutta ⊥ §T toteuttanut. ∴ talkoolainen ⊥ löydä pätkälistaa & FAQ:ta. | V208 → T296 |
| B113 | 2026-07-25 | Slug ∃ vain `assignedCode`-kentässä, jonka asettaa VAIN `segment-details-modal.ts:445` assign-flow ("Luo linkki"). `createSegment` (`segments.ts:48`) ⊥ generoi slugia ∴ jakamaton pätkä ⊥ resolvoidu (`getSegmentForCode` matchaa `assignedCode`) & `patkat-page.ts:141` näyttää "Ei jaettu" ilman Avaa-linkkiä. Järjestäjä ⊥ voi avata juuri luomaansa pätkää. Vahvistettu käyttäjätestillä: kopioitu `/s/<slug>` TOIMII kun assign tehty ∴ ⊥ reititys/auth-gate-bugi. Sivulöytö: `POST /api/auth/code-login` (`auth.ts:51`) antaa talkoo-session ∀ `talkoolainen_codes`-riville & slugit ihmisluettavia (T273) ∴ auto-slug ⊥ saa mennä code-tauluun (V210). | V209,V210 → T297,T298 |
| B114 | 2026-07-25 | Pätkänluonti sekoittaa KAHDEN reitin km:t samaan intervalliin. `segment-panel.ts receivePoint:137-138` `Math.min/max(first.startDist, dist)` — mutta `resolveClick:159+` snappaa kunkin klikin ITSENÄISESTI lähimpään reittiin kaikista 6:sta. 3 SMTB-reittiä kulkee samaa polkua (`SHARED_THRESHOLD_M`=100 m) ∴ klikki 1 → reitti A km 12.4, klikki 2 → reitti B km 47.1 (metrien snap-ero ratkaisee) → intervalli [12.4, 47.1] ⊥ vastaa kummankaan reitin todellista osuutta, & r149-151 pushaa MOLEMMAT routeIds:iin → suodatin (V140 `routeIds ∩ [startDist,endDist]`) kaappaa merkkejä pitkin molempia reittejä. Käyttäjän oire 2026-07-25: "kun aloitin alusta, se otti ihan random merkkejä". ⊥ vaadi migraatiota laukaistakseen — syntyy tuoreena joka luonnissa. | V211 → T299 |
| B115 | 2026-07-25 | Merkillä 1 skalaari-km (`types.ts:18 distanceFromStart // nearest route at placement time`) mutta N `routeIds` (`types.ts:19`). Km mitataan SIJOITUSHETKEN lähimmästä reitistä; pätkäsuodatus (`task-markers.ts:29-32`, V140) vertaa sitä pätkän rangeen joka on määritelty TOISTA reittiä vasten. Jaetulla osuudella merkki tägätään 3 reitille (`assignRoutesToMarker` 100 m) mutta km on vain yhdestä ∴ osumat toisten reittien pätkiin ovat sattumaa. Sama vika backendissä: `markerInOwnSegment` (`server/marker-audit.ts`, V154) tekee saman range-vertailun ∴ talkoolaisen lista & 403-portti voivat olla eri mieltä samasta merkistä (B100 päinvastoin). Rakenteellinen, ⊥ regressio — paljastui kun 6. reitti (110km-siirtymä, commit d144d1b) lisäsi jaettua osuutta. | V212,V213 → T300,T301 |
| B116 | 2026-07-25 | `nearestPointIndex` (`bearing.ts:26-34`) = globaali argmin koko polylinen yli ILMAN jatkuvuusrajoitetta. Reitti joka palaa itsensä lähelle (lenkki, edestakainen osuus, risteys): 2 fyysisesti vierekkäistä polyline-pistettä voivat olla km 12 & km 47 → argmin valitsee sen joka sattuu olemaan metrin lähempänä ∴ km-arvo on KOHINATASON ARPA, ⊥ sijainti. Osuu kaikkeen mikä olettaa km:n monotoniseksi & yksikäsitteiseksi: merkin sijoitus (`markers.ts:181`), pätkäklikki (`segment-panel.ts resolveClick`), suuntanuolet (T286 B), `nextMarkerAhead`, GPS-drive, edistymä-%. Käyttäjän kysymys 2026-07-25: "kun reitit haarautuu tosi moneen paikkaan, mitä se osaa seurata" — vastaus: ei mitään, se arpoo. T302 lieventää (ehdokkaat+konteksti), T305 poistaa syyn (klikattu polku). | V214 → T302,T305 |
| B117 | 2026-07-25 | `scripts/migrate-marker-routes.ts` ajettu tuotantoon VAILLINAISELLA reittilistalla & eri säännöllä kuin sovellus. (1) `ROUTE_FILES:22-28` = 5 kovakoodattua reittiä, `smtb-110-siirtyma` puuttuu (lisätty `main.ts:26`, commit d144d1b) → siirtymän merkit tägättiin naapurireiteille & km laskettiin naapurin geometriaa vasten. (2) `r99` primary = `routes.find(r => newIds.includes(r.id))` = ROUTE_DEFS-LISTAJÄRJESTYS, kun sovellus (`markers.ts:181-192 nearestRouteAssignment`) käyttää LÄHINTÄ → migraation jälkeen km ⊥ vastaa mitään sovelluksen oletusta. Kaksi eri primary-määritelmää samassa koodipohjassa. Tuotantodata jo tässä tilassa. PÄÄTÖS 2026-07-25 (käyttäjä): data jätetään rauhaan ("seuraa reittiä pääsääntöisesti ok"), SKRIPTI korjataan ettei seuraava ajo toista vahinkoa. | V215 → T303 (§C-parkki: ⊥ recompute) |
| B118 | 2026-07-25 | Migraation NEAREST-fallback (`migrate-marker-routes.ts:88-95`) tägää orvon merkin (⊥ reittiä ≤100 m) LÄHIMPÄÄN reittiin ILMAN etäisyyskattoa — myös kilometrien päässä olevan — & antaa sille sen reitin km:n → merkki putoaa suoraan jonkun pätkän km-rangeen ja ilmestyy talkoolaisen listalle vaikka ei ole reitillä lainkaan. Kommentti r64-66 perustelee: "ettei jää piiloon, käyttäjä siirtää käsin" — mutta hinta on väärä pätkäjäsenyys, joka on huonompi kuin näkymättömyys (talkoolainen lähtee hakemaan merkkiä joka ei ole siellä). Sovelluksessa vastaava raja ∃ (`FAR_FROM_ROUTE_M`=500, `marker-assign.ts:1`) mutta se vain VAROITTAA (`onFarFromRoute`) ⊥ estä. | V215 → T303 |
| B119 | 2026-07-25 | `server/routes/segments.ts` PUT auktorisoi talkoolaisen ehdolla `session.talkoolainen_code === existing.assigned_code`, mutta `POST /api/auth/talkoo-login` (`auth.ts:80`) kirjoittaa sessioon `talkoolainen_code = null` (V188: sessio ⊥ sidottu segmenttiin) ∴ yleissalasanalla kirjautunut talkoolainen saa 403 JOKAISESTA kenttätyön tallennuksesta (inspected/completed/equipment/rajat); sama sidos `markers.ts:262` PUT:ssa (siirto/status/kommentti). Model B (T267/T272) vaihtoi autentikoinnin mutta auktorisointi jäi koodisidonnaiseksi ∴ koko kenttätyö rikki yleissalasana-polulla — vain legacy `code-login` toimii. Havaittu tuotantodeployn jälkeen 2026-07-25. | V217 → T306 |
| B120 | 2026-07-25 | B114:n sama oletus (km-väli = jäsenyys ⊥ yksi kanoninen reitti) toistui `src/map/segment-overlay.ts`:ssä NELJÄSSÄ kohdassa — löytyi vasta rakennettaessa T299:ää, ⊥ ollut alkuperäisessä analyysissä. (1) `:94` polyline piirtyi JOKAISELLE jäsenreitille samalla `[startDist,endDist]`-välillä → pätkä näkyi kartalla naapurireitin km-kohdassa. (2) `:148` snap-pisteet samoin. (3) `:175` A/B-raahausmerkit valitsivat reitin `ROUTE_DEFS`-JÄRJESTYKSESTÄ ⊥ primarysta (sama listajärjestysvika kuin B117). (4) `:239` `computeGapRanges` laski kattavuuden jäsenyydellä ⊥ primarylla → aukkoraportti valehteli jaetulla osuudella. Korjattu commitissa dae4ee5 (T299): kaikki neljä lukevat `primaryRouteId`. NÄKYVÄ KÄYTÖSMUUTOS (tarkoitettu): pätkä piirtyy vain primary-reitille. | V211 → T299 (korjattu) |
| B121 | 2026-07-25 | Talkoolainen siirtää merkkiä kartalla → "Seuraava merkki" -alapalkki (hero) katoaa eikä palaa; merkki muuttuu myös ei-raahattavaksi ∴ tilanne peruuttamaton ilman reloadia. Kenttähavainto (käyttäjä). **Juurisyy 1 (ei rollbackia):** `src/map/markers.ts:410` dragend mutatoi paikallisen `m`:n (`lat/lon/distanceFromStart/distanceByRoute/routeIds`) ENNEN `apiPut`:ia & kutsuu `onUpdate()` ehdoitta. `server/routes/markers.ts:217` hylkää 403:lla siirron oman pätkän rangen ulkopuolelle (V150) → client-tila jää eroon serveristä: merkki putoaa `resolveTaskMarkers`-jäsenyydestä (`segments.ts:323`) → `markers-wiring.ts:134` `segmentView.update(segMarkers)` saa kutistuneen setin → `segment-hero.ts` render tyhjenee, JA `setDraggablePredicate` (V150) tekee merkistä ei-raahattavan. **Juurisyy 2 (väärä reitti):** sama dragend valitsee `bestRouteIdx`:n = lähin piste yli KAIKKIEN reittien ja kirjoittaa sen `distanceFromStart`iin ∴ jaetulla osuudella (3 SMTB-reittiä samalla polulla) km mitataan väärältä reitiltä → merkki putoaa rangesta vaikka ei liikkuisi käytännössä mihinkään. Sama B114/B117/B120-suku: "km-väli ⊥ yksi kanoninen reitti". | V220,V221 → T309 |
| B122 | 2026-07-25 | Talkoolainen on umpikujassa karttanäkymässä: `/patkat`-hubiin ei pääse muuten kuin kirjoittamalla URL käsin. `src/ui/account-menu.ts:26` renderöi hub-linkin vain jos `role !== 'talkoolainen'` (T274/V189 järjestäjä-crossover) ∴ ainoa talkoolaiselle näkyvä "koti" on 🏠 `#btn-home-view` (`index.html:14`), joka vaihtaa VIEW-moodin saman pätkän sisällä — ei sivua. Ristiriita Model B:n kanssa (V188/T267): hubi on talkoolaisen koti josta pätkä valitaan, mutta sinne ei ole paluuta. IA-aukko, ei pelkkä puuttuva mukavuus — talkoolainen joka sulkee pätkän ei löydä toista. | V222 → T310 |
| B123 | 2026-07-25 | Pätkänäkymä `/s/<slug>` koti-moodissa ⊥ scrollaa alas kun pätkällä on paljon merkkejä — "Kaikki merkit" -listan alaosa leikkautuu tavoittamattomiin, MOLEMMILLA laitteilla (käyttäjä 2026-07-25). Talkoolaisen kenttätyö estyy: alimpia merkkejä ⊥ voi kuitata. Juurisyy = kolmen ERIKSEEN OIKEAN säännön ketju joka jättää sisällön ilman scrolleria: (1) `style.css:158` `html:has(#app),body:has(#app){overflow:hidden}` (B108/V187b) → sivukuori ⊥ scrollaa; (2) `style.css:3489` `#map-area{overflow:hidden;min-height:0}` → leikkaa ylijäämän; (3) `style.css:1646` `#app[data-view-mode="koti"] #segment-view{max-height:none}` (T254/V175 "koko korkeus") kumoaa kartta-moodin 38vh-rajan (r1466) ∴ elementin korkeus = SISÄLLÖN korkeus → sen oma `overflow-y:auto` ⊥ KOSKAAN aktivoidu, eikä yksikään esivanhempi tarjoa scrolleria (`#segment-view-container`, `index.html:65`, = tavallinen div ilman omaa mitoitusta). ⊥ mediakyselyä ketjussa ∴ oire identtinen mobiilissa & desktopissa. ⊥ B108-duplikaatti: B108 = scroll chaining (scrolleri ∃, liike valui väärään paikkaan), tämä = scrolleria ⊥ ole lainkaan. | V226 → T315 |
| B124 | 2026-07-25 | `marker_audit.segment_code` on NULL KAIKILLA 263 tuotantorivillä ∴ T227 on kuollutta koodia vaikka merkitty ✓: pätkämodaalin "Aktiviteetti" (`segment-details-modal.ts:581`) renderöi aina tyhjän, & `POST /api/audit/undo` (`audit.ts:39`) suodattaa `WHERE UPPER(segment_code)=?` ∴ ⊥ osu mihinkään. Juurisyy ⊥ KIRJAUSVIRHE: `logMarkerAudit` (`marker-audit.ts:15`) kirjaa `session.talkoolainen_code`n oikein — arvo vain puuttuu, koska yleissalasana-sessio (`/patkat`-hub T296, `auth.ts:105-110`) luodaan `talkoolainen_code=NULL, display_name='Talkoolainen'`. Tuotannossa 11 tällaista sessiota ∴ tekijää ⊥ voi tunnistaa EIKÄ palautusta tehdä UI:sta. Paljastui 2026-07-25: talkoo-sessio siirsi 14 merkkiä 1–7 km sivuun (07:30–07:45), palautus ! tehdä SSH:lla suoraan tuotanto-DB:hen — 2 backupia + `scripts/restore-marker-moves.ts` + `docs/RUNBOOK-merkkien-palautus.md`. Kaksi erillistä aukkoa: (1) pätkä ⊥ johdeta merkistä, (2) identiteetti ⊥ kysytä kirjautuessa. | V227,V228 → T316,T317 |
| B125 | 2026-07-25 | DESIGN.md sai T308:ssa säännön "aktiivi-kytkin ⊥ accent-täyttö valkoisella tekstillä" (mitattu 3.46:1 < 4.5:1 vaalealla teemalla) mutta olemassa oleva rikkoja jäi korjaamatta: `.inv-mode-toggle.active` (T251, inventaarion muokkaus/katselu-kytkin) käyttää juuri sitä kuviota. Löytyi T308-agentin kontrastianalyysissä; ⊥ korjattu koska tiedosto ⊥ ollut sen omistuksessa (rinnakkainen lane-työ). Sokea piste: DESIGN.md-sääntö ja koodi ovat nyt eri mieltä ∴ seuraava kytkin voi kopioida väärän esikuvan. | V234 → T326 |
| B126 | 2026-07-25 | Talkoolaisen "Seuraava merkki" ⊥ ehdota kulkusuunnassa seuraavaa merkkiä → talkoolainen ajaa väärään suuntaan (kenttähavainto, käyttäjä: "seuraava merkki tulee sen mukaan milloin se on laitettu"). Juurisyy: `src/logic/navigation.ts` järjestää KAIKKI 5 funktiota (`firstUnsetMarker:11`, `unsetMarkersOrdered:22`, `stepUnset`, `nearestUnsetMarker:52`, `nextMarkerAhead:71`) skalaarilla `m.distanceFromStart` = "nearest route at placement time" (`types.ts:18`) ⊥ pätkän reitti ∴ merkeillä on ERI km-akseli keskenään → `sort()` sekoittaa ne. `nearestUnsetMarker`/`nextMarkerAhead` ottavat jo `routeId`:n mutta käyttävät sitä VAIN `routeIds.includes`-jäsenyyteen, ⊥ km-lähteeksi. Kutsujat `src/ui/segment-hero.ts` (hero+◀▶) & `src/app/markers-wiring.ts:479-480` (drive, joka syöttää saman väärän `distanceFromStart`in `jumpToDistance`en). **MITATTU prod-datalla** (`fly ssh sftp get /data/karttamaster.db` → `scripts`-ulkopuolinen analyysi, 9 pätkää/190 merkkiä): 3/9 pätkää rikki, 8 merkkiä väärällä akselilla. Pahin Pätkä 4 (20.68–25.65 km, 13 merkkiä): 3 merkkiä (`30km only irtokyltti`, `oikea`, `vasen`) mitattu `smtb-30`:ltä (koko reitti 4.6 km) → km 0.00/0.03/0.57 vaikka todellinen sijainti pätkän LOPUSSA 25.18/25.21/25.65 (Δ25.18 km) ∴ nykyjärjestys nostaa pätkän VIIMEISET merkit ENSIMMÄISIKSI. Kendall tau nyt-vs-oikea: Pätkä 4 = 0.282, Pätkä 1 = 0.850, Pätkä 9 = 0.912; loput 5 pätkää 1.000 (yksireittinen osuus ⇒ ⊥ oiretta). Sama B114/B115/B117/B120/B121-suku. ⊥ korjaa B116:ta (4/13 Pätkä 4:n merkistä saa primary-reitiltä >1 km-ehdokkaan = lenkki-arpa, T302/T305) — tämä korjaa AKSELIN ⊥ geometrian. Sivulöytö: `primary_route_id` on NULL 9/9 prod-pätkällä ∴ `segmentPrimaryRouteId`in `routeIds[0]`-fallback on todellinen polku, ⊥ reunatapaus. | V235 → T327 |
| B127 | 2026-07-25 | `/patkat`-hub ja pätkänäkymä ovat ERI MIELTÄ siitä montako merkkiä pätkällä on. Juurisyy: `distanceByRoute` ⊥ ole persistoitu kenttä (`server`: 190/190 prod-merkkiä tyhjänä) vaan CLIENT-side lazy backfill geometriasta (`main.ts:66` `backfillDistanceByRoute`, V212/T300) ∴ jokainen sisääntulo joka lataa merkit ILMAN GPX:iä jää skalaari-fallbackiin (`distancesForRoute` → `[distanceFromStart]`) & jäsenyys (V140/V212 `[startDist,endDist]`) lasketaan väärältä akselilta. `src/patkat.ts:20-23` hakee `fetchMarkers()` mutta ⊥ lataa reittejä lainkaan (hub ⊥ ole karttasivu) ∴ hub laskee AINA skalaarilla. MITATTU prod-datalla 2026-07-25: 7/9 pätkää eroaa — Pätkä 4 hub 8 vs näkymä 13, Pätkä 6 9 vs 12, Pätkä 1 25 vs 23. Ristiinvahvistettu käyttäjän hub-kuvakaappauksella (25/18/12/8/19/9/4/12 = täsmälleen skalaari-sarake) ∴ ⊥ teoreettinen. Talkoolainen valitsee pätkän hubista väärällä tiedolla & luku muuttuu kun hän avaa sen. Sama juurisyyperhe kuin B115/B126 mutta ERI vika: B126 = järjestys, tämä = MÄÄRÄ/jäsenyys. Ratkaisuvaihtoehdot (PM päättää): (a) hub lataa GPX:t (hidas, 6 tiedostoa mobiilissa), (b) server persistoi `distance_by_route`n kirjoitushetkellä (kertaluontoinen migraatio + kirjoituspolku, ⊥ enää client-riippuvuutta), (c) hub laskee määrän serveriltä (`/api/segments` palauttaa count). | V236 → §T (avoin) |
| B128 | 2026-07-25 | `bunx playwright test e2e/` = 12 failausta T307–T315-erän jälkeen, joista **9 oli punaisia jo HEADissa** (todistettu: `git worktree add <tmp> HEAD` + sama ajo → area-interaction ×4, sprint-features ×2, critical-paths auth-tab, critical-paths T108 flyTo, segments "pätkän voi poistaa"). Aidot 3 regressiota (critical-paths drag + t309 ×2) hukkuivat kohinaan ja erottuivat vasta baseline-vertailulla. Juurisyy ⊥ ole yksi bugi vaan PROSESSI: punaisia e2e:itä on kertynyt kirjaamatta ∴ suite ⊥ enää kerro regressiosta mitään ilman erillistä baseline-ajoa. Ryhmät & epäilyt: A) area-interaction `.area-corner-handle` count 0 — `MapRectEditor.startEdit` ⊥ laukea polygonin dblclickista; D) T108 flyTo — sama `area-overlay.ts`-layer ∴ mahd. sama juurisyy; B) status-panel ×2 — `:46` käyttää headless-flakyä dblclick→picker-polkua (E2E-NOTES tunsi tämän jo 2026-07-10); C) auth talkoolainen-tab — voi olla AITO Model B -regressio (V188/T272), ! verifioida ensin; E) segments pätkän poisto. **RATKAISTU-DIAGNOOSI 2026-07-25 (PM-ajo, `bunx playwright test e2e/` = 124 passed / 9 failed / 6.4 min): 9/9 = HAPANTUNUTTA TESTIÄ, 0 sovellusbugia.** Epäilyt A+D osuivat oikeaan ("sama juurisyy") mutta juuri ⊥ ollut pointer-event-ketju vaan REITTIMÄÄRÄ: `route-defs.ts` 2→6 (T285) → `fitBounds` → alkuzoom romahti → alue 12×8 px & flyTo 4 s (T324). B ⊥ ollut dblclick-flaky vaan `.first()`-rivi 6 reitin joukosta. C ⊥ ollut Model B -regressio vaan T296/V208-navigointi `/patkat`-hubiin. E ⊥ ollut poistologiikka vaan T298/V209/B113:n automaattisesti avautuva details-modaali. OPPI (V233-linjaan): punaisen e2e:n juurisyy oli KOLME KERTAA "tuotemuutos jonka ennakkoehtoa testi ⊥ tunne" & KERRAN "testi olettaa kartan mittakaavan" — ⊥ kertaakaan headless-flaky. ∴ e2e joka koodaa kartan mittakaavan tai DOM-ennakkoehdon ! saada sen datasta/tilasta, ⊥ vakiona. | V233 → T324,T325 |
| B129 | 2026-07-25 | T327/V235 korjasi JÄRJESTYKSEN mutta ⊥ km-NÄYTTÖÄ ∴ oire jäi käyttäjälle päälle & näytti identtiseltä kuin B126 ("kun painan seuraava merkki niin se hyppii sinne tänne"): hero ◀▶ ETENEE oikeassa kulkusuunnassa mutta LUKEMA hyppää 18.9→…→25.1→**0.0→0.0**→25.6→**0.6→0.5** km. Todiste (ajettu prod `dev.db` + GPX 2026-07-25): Pätkä 4 = 20.68–25.65 km smtb-55, 15 merkkiä; `distanceByRoute`-backfill osuu **15/15** ∴ akseli ON saatavilla — sitä vain ⊥ käytetä näytössä; 6 merkkiä on smtb-30+smtb-55 -jaetulla osuudella & niiden `distance_from_start` on smtb-30:ltä (0.00–0.57) vaikka smtb-55-akselilla ne ovat 25.14–25.69. Sijainnit: `segment-hero.ts:131` (näyttö); `segment-marker-list.ts:31` (sort) + `:90`; `segment-view.ts:278` (sort); `segment-details-modal.ts:256` (sort) + `:267`. LISTOISSA vika rikkoo MYÖS järjestyksen ⇒ lista & hero eri mieltä samasta pätkästä. T327(d) käski greppaamaan nämä & kirjaamaan §B:hen ⊥ korjaamaan sokkona — tämä on se kirjaus. JUURISYY ⊥ ole yksittäinen unohdus vaan MALLI: km-akseli kulki erillisenä `routeId`-parametrina jonka kutsuja saattoi unohtaa → hiljainen fallback skalaariin. Sama juuri 9. kerran (B114,B115,B116,B117,B120,B121,B126,B127,B129) ∴ korjaus on mallitason (V237: pätkä omistaa akselin), ⊥ 6. paikkaus. | V237,V238 → T328 |
| B130 | 2026-07-25 | `/loki`-peruutuksen vahvistusviesti "✓ Muutos peruttu." katoaa käytännössä aina: `audit-log-page.ts:203-204` asettaa `status.textContent`in & kutsuu HETI `opts.onReload()`in, joka ajaa `loki.ts:36` → `renderAuditLogPage` → `container.innerHTML = ''` (`:47`) ∴ juuri asetettu `<p class="audit-status" aria-live="polite">` tuhoutuu ennen kuin kukaan ehtii lukea sen — myös ruudunlukija menettää aria-live-ilmoituksen. Järjestäjä näkee VAIN rivin katoamisen: peruiko se oikeasti vai kaatuiko jokin? Juuri se kysymys johon loki (T320/T321) rakennettiin vastaamaan 2026-07-25 incidentin takia. Löytyi T325-ajossa: `e2e/t321-audit-log.spec.ts:69` `.audit-status` toContainText('peruttu') FAILAA deterministisesti kun suite ajetaan kokonaan (`sprint-features.spec.ts` edellä) mutta MENEE LÄPI yksin ∴ testi oli vihreä vain koska uudelleenlatauksen fetch ehti joskus olla hitaampi kuin assertin polling — kilpajuoksu, ⊥ kattavuus. Sama sukulinja kuin B60/B61: wiring-tason totuus (kumpi kirjoittaa DOM:in viimeksi) ⊥ näy yksikkötestissä. | V241 → T332 |
| B131 | 2026-07-25 | Talkoolaisen hero-navigaatio katoaa "Näytä kartalla" -napista eikä palaa (kenttähavainto). `src/ui/segment-hero.ts:178` kutsuu `onShowOnMap(id)` JA `this.ctx.setCollapsed(true)`; kutistus piilottaa CSS:llä `.segment-view-next` (`style.css:1539`) = KOKO hero (◀▶ · Aseta · Näytä kartalla · ⋯). Ainoa paluu = otsikkorivin merkitsemätön 18px chevron `.segment-view-collapse` (`segment-view.ts:397`, pelkkä aria-label "Laajenna pätkänäkymä") ∴ napin NIMI lupaa panorointia, sivuvaikutus on ohjauksen romahdus & syy-seuraussuhdetta chevroniin ⊥ synny → umpikuja kentällä. Sama kuvio keräyslistan riviklikissä (`segment-view.ts:319`). Sukua B121 (hero katosi merkin siirrosta → T309) & B122 (paluupolku hubiin puuttui → T310) — perhe "talkoolaisen ohjaus katoaa eikä palaa" — mutta ERI liipaisin: ⊥ tilan/jäsenyyden rikkoutuminen vaan TARKOITUKSELLINEN kutistus ilman paluu-affordanssia ∴ ⊥ duplikaatti. Käyttäjäpäätös 2026-07-25: hero saa jäädä auki, panorointi ⊥ tarvitse tilaa. | V242 → T333 |
| B132 | 2026-07-25 | Merkkiin liitettyä valokuvaa ⊥ saa isommaksi (käyttäjähavainto 2026-07-25). `src/ui/marker-detail-modal.ts:187-199` rakentaa `.marker-detail-image-thumb` 72×72 (`style.css:3688`, `object-fit:cover` = RAJAA) & lisää VAIN `error`-kuuntelijan — ⊥ `click`-käsittelijää, ⊥ linkkiä, ⊥ lightboxia ∴ kuva on kannassa & ladattavissa (`GET /api/markers/:id/images/:imageId` sallii kaikki autentikoidut, `markers.ts:390`) mutta UI ⊥ tarjoa mitään tapaa nähdä sitä. Lightbox-kuvio ON JO olemassa (`marker-visual-row.ts:126-160` `createBackdrop` + `.marker-visual-lightbox`) mutta se palvelee KYLTTIVISUAALIA (`buildMarkerVisual` size 160), ⊥ valokuvia ∴ vika on puuttuva kytkentä, ⊥ puuttuva komponentti. Vaikutus: kentällä otettu todistekuva on käytännössä katsomiskelvoton — 72px rajattu neliö. Rinnakkaishavainto (⊥ sama bugi, kirjattu V246:een): kuvan LISÄYS on järjestäjä+ (`markers.ts:366` `requireRole`) ∴ talkoolainen ⊥ voi liittää kuvaa lainkaan. | V246 → T337 |
| B133 | 2026-07-26 | Talkoolaisen GPS ⊥ näytä omaa sijaintia tuotantomobiilissa (kenttähavainto, tapahtuma PÄÄLLÄ). Kaksi erillistä vikaa samassa polussa. **(1) Wiring-elinkaari:** talkoolaisen ainoa GPS-sisääntulo on ⋯-valikon `#btn-tk-gps` (hero-nappi `.segment-view-gps-btn` piilotettu `style.css:1727`, T257/R8) & sen click-listener kytketään `markers-wiring.ts:328` SISÄLLÄ `if (talkoolainenCode) { if (seg) {…} }` ∴ jos `getSegmentForCode` palauttaa undefined — pätkien lataus kaatui mobiiliverkossa (`main.ts:104` `wireSegments`), koodilla ⊥ pätkää, pätkä poistettu — nappi renderöityy KUUNTELIJATTA → klikkaus ⊥ tee mitään & mikään ⊥ kerro siitä. Oma sijainti on LAITTEEN tieto ⊥ pätkän ∴ riippuvuus on väärä kytkös, ⊥ vain reunatapaus. **(2) Hiljainen virhe:** `gps-navigator.ts:18` error-callback = `console.warn` ∴ lupa evätty / `timeout:15000` + `enableHighAccuracy:true` metsässä ⊥ tuota käyttäjälle mitään; label näyttää "📍 GPS päällä" ilman yhtäkään fixiä ∴ oire "ei tapahdu mitään" on IDENTTINEN molemmissa vioissa & kenttädiagnoosi mahdoton. Suljettu pois: `f909cdd` (T333-hero) ⊥ koske GPS-polkua, `gps-navigator.ts` muuttumaton T30:sta, ⊥ tuplakuuntelijoita (`wireMarkers` ajetaan kerran `main.ts:110`), ⊥ layer-siivousta joka poistaisi pisteen. Sukua B121/B122/B131 (talkoolaisen ohjaus katoaa) mutta liipaisin eri: kytkös puuttuu INIT-ajassa, ⊥ katoa käytössä. | V247 → T341 |
| B134 | 2026-07-26 | Järjestäjän pätkäVIIVAN klikkaus kartalla ⊥ avaa `SegmentDetailsModal`ia latauksen jälkeen — löytyi T347:n E2E:ssä (nimilappu ⊥ toiminut & syy osoittautui lapun sijaan koko klikkipolkuun: myös viivan klikkaus tuotti 0 modaalia). Syy on JÄRJESTYS ⊥ logiikka: `segments-wiring.ts:72` ajoi `renderSegmentOverlay()`n heti overlayn luonnin jälkeen, mutta `segmentOverlay.setOnSegmentClick(…)` on rivillä 144 — vasta `SegmentPanel`in rakentamisen jälkeen (kuuntelija TARVITSEE panelin `openDetailsModal`in) ∴ ensimmäisellä renderillä `this.onSegmentClick` on `undefined` & `if (this.onSegmentClick && style.interactive)` (`segment-overlay.ts:116`) ohittaa kuuntelijan kytkennän jokaiselta piirretyltä pätkältä. Kuuntelija ilmestyi vasta jos jokin MUU tapahtuma sattui renderöimään overlayn uudelleen (merkin status-muutos `main.ts` MarkerManager onUpdate, phase-vaihto, pätkän mutaatio) ∴ vika oli ajoittainen & näytti "joskus toimii" — kartta ilman merkkimuutoksia jäi kokonaan kuolleeksi. Sukua B133:lle: kuuntelija kytketään väärässä kohdassa INIT-elinkaarta, ⊥ katoa käytössä. Korjaus T347: ensimmäinen `renderSegmentOverlay()` siirretty wiringin LOPPUUN, kaikkien kuuntelijoiden kytkennän jälkeen. | V251 → T347 |
| B135 | 2026-07-26 | V96:n toinen puolisko ("phase-progress luettavissa kartalta visuaalisesti ilman sivupalkkia") ⊥ toteudu tuotannossa vaikka invariantti on kirjattu & T152 merkitty valmiiksi — PM-analyysi 2026-07-26. Kaksi vuotokohtaa. **(1)** `segment-overlay.ts:12-16` `LINE_STATE_STYLE` koodaa `kesken` & `ei_alkanut` `dashArray: '1 9'` (1px viiva, 9px aukko) ∴ kuvio hajoaa pistesarjaksi joka katoaa MML-taustakartan tekstuuriin — kolme tilaa erottuu käytännössä vain `valmis`in ehjyydestä, ⊥ kolmena luettavana ämpärinä. **(2)** `segment-overlay.ts:107` rajaa ✓-merkin `seg.phase === 'tarkastus'` -pätkiin ∴ asettamis- & purkuvaiheen valmis pätkä ⊥ saa mitään POSITIIVISTA signaalia — järjestäjä päättelee "valmis" viivan PUUTTUVASTA katkosta, & puuttumista ⊥ voi etsiä kartalta silmäyksellä. ⊥ regressio vaan T152:n vajaa toteutus: väri-kanava (tunniste) rakennettiin, status-kanava jäi liian hienoksi näkyäkseen ∴ invariantti oli voimassa mutta valvomatta (E2E tarkisti dashArrayn OLEMASSAOLON, ⊥ luettavuutta). Sukua B65:lle (sama VISION-UX-testi jäi täyttymättä, eri mekanismi: silloin väri koodasi väärää asiaa — lista-indeksiä — nyt oikea kanava on liian hiljainen). | V96-amend + V252 → T348 |
| B137 | 2026-07-27 | V244:n casing-ehto on YLIMÄÄRITELTY ⊥ täytettävissä millään paletilla — löytyi T304:ssä kun paletti kalibroitiin & ehto mitattiin. V244 vaatii `pätkäväri ≥3:1 sisusväriään (reittiväri) vasten`, mutta SAMAAN aikaan (a) reittiviivan ! erottua vaaleasta taustakartasta (`#F2F0EA`) ⇒ reittivärin luminanssi ≤0.304 & (b) reittipilleri (`route-bar.ts:96`) renderöi reittivärin TAUSTAKSI tummalla tekstillä (`--text-body`) ⇒ luminanssi ≥0.142 (AA large 3:1). Reitti on siis lukittu kaistaan 0.142–0.304; casing-ehto vaatisi pätkävärin luminanssiksi ≤(L_reitti+0.05)/3−0.05, mikä kirkkaimmalla sallitulla reitillä on ≤0.041 — & samalla tummimman sallitun reitin (0.142) kanssa sama pätkäväri antaa vain 2.2:1. Mitattu T304:n lopullisella paletilla: huonoin pari **2.19** (`#582F0F` vs `sgf-175`), paras 3.42 ∴ osa pareista täyttää ehdon & osa ⊥ — paletilla ⊥ voi ratkaista, koska ehtojoukko on ristiriitainen ⊥ paletti huono. Juurisyy: V244 kirjoitettiin olettaen että reunus & sisus ovat AINA vierekkäin ∴ niiden ! kontrastoitava keskenään. Ratkaisu ⊥ ole väri vaan GEOMETRIA: 1px valkoinen erotinviiva casingin & sisuksen väliin ⇒ kumpikin kontrastoi valkoista vasten (mitattu: reitit 3.40–5.23, pätkät 11.49–11.64 ∴ kaikki ≥3:1 reilulla marginaalilla) & vierekkäisyys-vaatimus katoaa. Sama kuvio kuin kartografian vakiokäytäntö (halo/casing). ⊥ havaittu aiemmin koska T336 (casing) ⊥ ole vielä rakennettu — ehto eli vain tekstinä. | V244-amend → T336 |
| B136 | 2026-07-27 | T348:n valmis-vihreä tulee KAHDESTA lähteestä jotka eroavat Kaamos-teemassa — löytyi testaaja-auditissa, MITATTU (`data-theme="dark"`, järjestäjän kartta): lapun reunus `rgb(47,163,91)` = `#2FA35B`, saman pätkän viiva `#1F8A50`. Kartan viiva lukee JS-vakion `SEGMENT_DONE_COLOR` (`segments.ts`, kiinteä hex koska Leaflet-polyline ⊥ lue CSS-muuttujaa), lapun reunus CSS-tokenin `var(--confirm)` (`.segment-label--done`). `:root`issa molemmat ovat `#1F8A50` ∴ vaaleassa teemassa täsmäävät & vika on NÄKYMÄTÖN; `[data-theme="dark"]` (`style.css:101`) määrittää `--confirm: #2FA35B` ∴ toggle (`theme.ts:28`, T203/V132, reachable tuotannossa) hajottaa parin & tummalle teemalle jää nimenomaan TUMMEMPI vihreä viivaan (heikompi kontrasti kuin kalibroitu Kaamos-arvo). Juurisyy on kategoriavirhe ⊥ kirjoitusvirhe: karttapinta on TARKOITUKSELLA teemariippumaton (`SEGMENT_COLORS` & `ROUTE_DEFS` kiinteitä hexejä valkoiselle kartalle V132, lapun tausta kiinteä navy B106) mutta `--confirm` on CHROME-token joka vaihtuu teeman mukana ∴ karttapinnan elementti ⊥ saa lainata sitä. T348 sitoi kaksi eri elinkaarta samaan "vihreään". ⊥ jäänyt kiinni automaattitestistä koska yksikään testi ⊥ aja Kaamos-teemassa & E2E tarkistaa lapun TAUSTAN (B106) muttei reunusväriä. | V253 → T349 |
| B140 | 2026-07-27 | Talkoolainen: kun pätkän kaikki merkit on asetettu, hero renderöi matalan done-rivin "✓ Kaikki asetettu 🎉" (`segment-hero.ts:80-88`) ILMAN toimintoa. Valmiiksi-merkintä elää kahdessa piilossa: **(1)** `SegmentView.completeSection` (T230) on "Kaikki merkit" -tabin pohjalla `markerListEl`:n alla (`segment-view.ts:157`) ∴ vaatii tabinvaihdon + skrollauksen, **(2)** duplikaatti `#btn-tk-complete` yläpalkin ⋯-valikossa (`index.html`, wiring `markers-wiring.ts:354-364` OMALLA `syncCompleteLabel`illaan). Kumpikaan ⊥ ole siinä näkymässä joka ilmoittaa valmiudesta ∴ talkoolaisen VIIMEINEN askel metsässä = tabinvaihto tai ⋯-valikko (max 2 nappia rikki). Toiminto `actions.onComplete` on jo olemassa & kytketty (`markers-wiring.ts:310`) — puuttuu vain sisääntulo oikeasta paikasta | V254 → T351 |
| B141 | 2026-07-27 | Järjestäjä ⊥ voi merkitä pätkää valmiiksi eikä keskeneräiseksi lainkaan. Kaksi porttia samassa polussa: **(1)** `index.html` `#tk-menu-actions` on `data-role-hide="järjestäjä"` ∴ ⋯-valikon `#btn-tk-complete` piilossa, **(2)** `SegmentView` (& sen `onComplete`/`completeSection`) kytketään VAIN talkoolaispolussa `markers-wiring.ts`:ssa (vaatii `talkoolainenCode` → `getSegmentForCode`) ∴ järjestäjän näkymässä koko osiota ⊥ ole olemassa — portin (1) avaaminen yksin ⊥ riitä. `SegmentDetailsModal` (järjestäjän ainoa pätkä-UI) ⊥ käsittele `completed`-kenttää lainkaan. Seuraus: järjestäjä ⊥ voi kuitata pätkää soittaneen talkoolaisen puolesta eikä perua virhekuittausta | V255 → T352 |
| B142 | 2026-07-27 | `Segment.completed` on **kirjoitus ilman lukijaa** — löytyi T352:n verifioinnissa (⊥ käyttäjäraportti). Koko `src/`:ssä lipun ainoat lukijat ovat nappien labelit (`segment-hero.ts`, `segment-view.ts:216`, `segment-details-modal.ts`) ∴ talkoolaisen eksplisiittinen "pätkä valmis" -signaali ⊥ näy MISSÄÄN järjestäjän tilannekuvassa: kartan viivan `valmis`-tila johdetaan MERKKIEN statuksista (`segmentLineState`+`getPhaseProgress`, `segments.ts:254-262`, T348/V96) & pätkälista näyttää saman status-laskurin. Seuraus: talkoolainen kuittaa pätkän valmiiksi & järjestäjä ⊥ voi erottaa sitä pätkästä jonka merkit ovat asetettu mutta jota ⊥ ole kuitattu — eli signaali jonka VUOKSI toiminto on olemassa (V93/T230) katoaa. ⊥ korjattu T351/T352:ssa (ne palauttivat vain sisääntulot); vaatii oman päätöksen: joko lippu näkyväksi (lista + kartta) TAI lippu poistetaan & valmius johdetaan pelkästään merkeistä. **PM-päätös 2026-07-27: (a) lippu näkyväksi** — kuittaus on kentältä tuleva tieto, laskuri on johdettu ∴ kuittaus ⊥ saa hävitä | V256 → T353 |
| B143 | 2026-07-27 | Sama merkki kuuluu KAHTEEN pätkään yhtä aikaa ∴ kaksi talkoolaista saa saman kyltin tehtäväkseen (& toinen löytää sen jo tehdyksi = turha ajo metsässä, WhatsApp-koordinointi ⊥ pelasta koska kumpikaan ⊥ tiedä päällekkäisyydestä). Mitattu TUOTANTODATASTA (`backups/karttamaster-20260727-050020.db`, ajettu appin omalla logiikalla + GPX-geometrialla, `computeDistanceByRoute` 100 m): **10 merkkiä 118:sta**. Pätkä3[15.76–20.68 km] + Pätkä4[20.68–25.65 km]: 3 merkkiä — sama merkki saa smtb-55:llä km 18.85 JA 20.69 (reitti käy samassa kohdassa kahdesti). Pätkä5a + Pätkä6: 4 merkkiä (km 33.01/36.98, 32.83/37.15, …). Pätkä8[smtb-55] + Pätkä9[smtb-30]: 2 merkkiä ("Soininsuo" smtb-30=4.59 \| smtb-55=52.10) — eri reittien jaettu korridori. **Juurisyy:** `resolveTaskMarkers` (`src/logic/task-markers.ts:40`) hyväksyy jos YKSI km-ehdokas osuu väliin (V214/T302) & tasapelinratkaisua ⊥ ole; `validateNoOverlap` (`src/logic/segments.ts:342`) vertaa vain samaa primarya ∴ eri reittien pätkät saavat laillisesti kattaa saman maaston & jäsenyys on silti reittipohjainen (`routeIds.some`). Sama B114/B115/B117/B120/B121-suku mutta juuri on SYVEMMÄLLÄ kuin niissä: **km→paikka ⊥ ole injektio** ∴ km-väli ⊥ voi yksilöidä maastoa, & jokainen korjaus joka pysyy km-avaruudessa siirtää vikaa ⊥ poista sitä. LISÄKSI oire jonka sama juuri tuottaa: pätkien rajat koskettavat (`endDist_A == startDist_B`, esim. 20676) & `RANGE_EPS_M=50` (`segment-order.ts:19`) levittää molempiin suuntiin ∴ rajamerkki näkyy kahden pätkän JÄRJESTYSLISTASSA vaikka jäsenyys olisi kunnossa — häviää V259:n eksklusiivisuudella. | V258,V259 → T358,T359 |
| B144 | 2026-07-27 | Luonnissa klikki→km on ARVAUS kahdesti ∴ pätkä "heittää minne sattuu" (kenttähavainto: pätkä joka alkaa smtb-reitiltä & päättyy päällekkäiselle osuudelle). **(a) Reitti arvataan hiljaa:** `segment-panel.ts:222 resolveClick` valitsee primaryn = lähin piste yli KAIKKIEN reittien & 3 SMTB-reittiä kulkee ≤100 m toisistaan (V25 jaettu osuus) ∴ metrien snap-ero GPX-pisteiden välillä ratkaisee reitin — & UI ⊥ kerro valintaa MISSÄÄN ennen tallennusta ∴ järjestäjä luulee klikanneensa smtb-65:ttä & saa smtb-55:n km-akselin. **(b) Kierros arvataan:** `distOnRoute:190` ottaa GLOBAALIN lähimmän pisteen (`nearestPointIndex`) ∴ edestakaisella osuudella km voi tulla väärältä kierrokselta (18.85 vs 20.69, ks. B143) → `Math.min/max` tuottaa intervallin joka ⊥ vastaa kumpaakaan klikkiä & pätkä venyy väärään maastoon. T299/B114 korjasi tästä puolet (klikki 2 luetaan primarysta ⊥ siitä mihin se snappasi) — loppu jäi, koska korjaus pysyi km-avaruudessa. Sama juuri kuin B143. | V258,V261 → T358,T362 |
| B145 | 2026-07-27 | T361:n backfill teki SIVULATAUKSESTA kirjoituksen ∴ jokainen jonka sessio ⊥ kelpaa saa koko ruudun kirjautumisoverlayn pelkästä sivun avaamisesta — taustamigraatiosta jota hän ⊥ pyytänyt. Ketju: `segments-wiring.ts` johtaa jäljen & kutsuu `updateSegmentRemote` ∀ pätkälle jolta se puuttui → outbox saa 401 → `main.ts:180 setOutboxReauthHandler` → `authScreen.promptReauth` peittää UI:n. Löytyi E2E:stä (`e2e/segments.spec.ts` 8 talkoolaistestiä jumiin: klikit odottivat elementtiä jonka overlay peitti; ilman T361:tä 23/23 vihreä) mutta vika ⊥ ole testimokissa: talkoolainen jonka sessio on vanhentunut metsässä saisi saman overlayn, & V18:n "ei pysty käyttämään sovellusta" on kriittisin luokka. **Juurisyy on VÄÄRÄ KIRJOITUSKANAVA, ⊥ pelkkä oikeusmalli.** Outbox (V116/T183) on tehty KÄYTTÄJÄN aloittamalle kirjoitukselle: sen 401-käsittely nostaa reauth-overlayn, mikä on oikein kun käyttäjä juuri kuittasi merkin & väärin kun kyse on taustamigraatiosta. Jälki on JOHDETTU arvo (rajat + GPX) joka lasketaan uudelleen joka latauksella ∴ se ⊥ tarvitse durabiliteettia ollenkaan — epäonnistunut push korjautuu itsestään seuraavalla kerralla. Korjaus kahdessa osassa: (a) push vain järjestäjältä (geometria on järjestäjän dataa, V13/V93), (b) push OUTBOXIN OHI hiljaisella fetchillä (`pushSegmentTrack`). Kumpikaan yksin ⊥ riitä: pelkkä (a) jätti järjestäjän saman overlayn armoille (3 critical-paths-testiä kaatui vielä sen jälkeen). V260 sanoo jo että jäljetön välitila on LAILLINEN ∴ push on optimointi ⊥ vaatimus. | V260-amend → T361 |
| B146 | 2026-07-27 | **Talkoolainen näkee merkin pätkälistallaan mutta saa siitä 403:n** — V260-siirtymäikkunassa (jälki clientin muistissa, ⊥ vielä kannassa). Mitattu tuotantodatalla ajamalla clientin `resolveSegmentMarkers` & serverin `ownerSegmentIds` SAMAA fixturea vasten: **6/26 merkkiä eri mieltä**, joista 5 muotoa "client: Pätkä 4, server: ei kenenkään" ∴ kirjoitus torjutaan (`routes/markers.ts` V150-portti). Kun jälki on kannassa: **0/26**. Juurisyy: serverin V260-legacy-haara lukee km:n `distsOnSegmentAxis`illa joka putoaa `marker.distFromStart`-SKALAARIIN kun `distance_by_route` puuttuu — & tuotannossa se puuttuu 190/194 merkiltä (lazy backfill V212; client ⊥ lähetä sitä statusmuutoksen mukana). Skalaari on mitattu sijoitushetken lähimmältä reitiltä ∴ toisen reitin pätkälle se ⊥ osu ollenkaan → "ei kenenkään". Client laskee jäljen geometriasta & löytää merkin. Sama B100/B115/V213-suku: kerrokset eri mieltä jäsenyydestä. Ikkuna sulkeutuu kun JÄRJESTÄJÄ avaa sovelluksen (T361-push on B145:n jälkeen järjestäjärajattu) ∴ jos talkoolainen saa linkin ennen sitä, kenttätyö estyy juuri niiltä merkeiltä joita koko sarja korjasi. **KORJATTU 2026-07-27, KOLME OSAA — yksikään ei riitä yksin:** (a) client liittää `distance_by_route`n JOKAISEEN kirjoitukseen yhdessä kuristuskohdassa (`markers.ts:withKmAxis`, eksplisiittinen patch voittaa); (b) serverin tarkistus (b) lukee akselin RUNGOSTA kun se on annettu — se luki `existing.distance_by_route`n KANNASTA ∴ pelkkä (a) ⊥ olisi korjannut mitään, koska juuri (b) on se portti joka 403:aa statuskirjoitukset; (c) `distance_by_route` POISTETTU audit-siirtopäättelystä (`AUDIT_MOVE_FIELDS` = lat/lon/distance_from_start/route_ids) — ilman tätä JOKA statusmuutos olisi kirjautunut `move`-riviksi & "Kumoa" olisi palauttanut koordinaatit statuksen sijaan (T319/V153). Sivuvaikutus jonka takia ikkuna sulkeutuu PYSYVÄSTI merkki kerrallaan: serveri kirjoittaa akselin kantaan (`routes/markers.ts:250`). **Mitattu tuotantodatalla korjauksen jälkeen: erimielisyyksiä 6 → 3, & kaikki 3 ovat SALLIVAA luokkaa** (server "Pätkä 3 JA 4", client valitsee toisen) ∴ 403-luokka poistui kokonaan — täsmälleen mitä V260-amend vaatii. Jäljen saapuminen kantaan (T361-push) vie loputkin nollaan (mitattu 0/26). Alkuperäinen suositus oli vain (a): client lähettää `distance_by_route`n merkkikirjoitusten mukana — arvo on jo muistissa (`backfillDistanceByRoute` T300) & kenttä on jo API:ssa (`routes/markers.ts` hyväksyy `body.distance_by_route`) ∴ serverin km-haara saa oikean akselin & "ei kenenkään" katoaa riippumatta jäljestä. | V260-amend → uusi §T |
| B147 | 2026-07-27 | **Klik-klik-luonnin ankkurimarkeri nielee seuraavat klikit hiljaa.** `segments-wiring.ts:130` luo ensimmäisen ankkurin merkiksi `L.circleMarker(radius: 9)` ILMAN `interactive: false` — Leafletin circleMarker on interaktiivinen oletuksena ∴ ~18px läpimittainen kiekko syö kartan `click`-eventin & luonti ⊥ saa klikkiä ollenkaan: ⊥ uutta ankkuria, ⊥ virhetekstiä, ⊥ mitään. Mitattu (Playwright 390×844, järjestäjän mobiili): 1. klikki OK, klikit 2–6 samalta alueelta ESTYIVÄT & `.segment-creation-error` pysyi tyhjänä. Ennen T362:ta oire oli piilossa: flow tarvitsi vain YHDEN lisäklikin joka oli tarkoitus tehdä kaukana. Klik-klik klikkaa reittiä pitkin ∴ peräkkäiset reittipisteet ovat pienellä zoomilla muutaman pikselin päässä toisistaan & kuollut klikki on todennäköinen. **KORJATTU 2026-07-27:** `interactive: false` (`segments-wiring.ts`) — markeri on PALAUTE ⊥ klikkikohde. Snap-markerit (`segment-overlay.ts:213`) JÄTETTIIN interaktiivisiksi: niillä on oma click-handler & ne ON tarkoitettu klikattaviksi. Regressiotesti `e2e/segments.spec.ts` "B147 — ankkurimarkeri ei syö seuraavaa klikkiä" mittaa SOPIMUKSEN (`leaflet-interactive`-luokan puuttuminen) ⊥ pelkkää "klikki näytti toimivan"; todistettu pureva — bugin palauttaminen antaa `"segment-creation-marker leaflet-interactive"` & testi kaatuu. **LISÄKSI havainto joka ⊥ ole bugi vaan aukko:** vain ENSIMMÄINEN ankkuri saa markerin kartalle — välipisteet & loppu näkyvät pelkkänä tekstilistana modaalissa, vaikka VISION §Järjestäjä sanoo "kartta on pääasia". Ankkuriketjun näkyminen kartalla kuuluu /karttamaster-ux:lle tai omaan §T:hen. | → uusi §T |
| B148 | 2026-07-27 | **Pätkä väitti koko reittiä omakseen aggregaattilukemissa** — löytyi `/ck:check`illä (⊥ testeillä: ne olivat vihreitä). `getSegmentStatusCounts` (`segments.ts:231`) & `getPhaseProgress` (`:269`) kutsuivat `getMarkersForSegment`ia ILMAN `peers`-argumenttia, & koska V259 on KYNNYKSETÖN, yksin kilpaileva pätkä voittaa jokaisen merkin reitillään. Mitattu tuotantodatalla: **9/10 pätkää raportoi 122 merkkiä** oikean 6–30 sijaan. Kolme pintaa luki näitä: `segment-overlay.ts:121` (kartan viivatyyli = T152/V96 tilannekuvakanava), `segment-panel.ts:404,409` (järjestäjän pätkärivi), `patkat-page.ts:119` (talkoolais-hub). `segment-view.ts:265` säilyi oikeana koska sille syötetään jo eksklusiivinen joukko. **Juurisyy on OLETUSARVON SEMANTIIKKA:** `peers = []` tarkoitti "kilpailijoita ⊥ ole", jolloin kynnyksettömyys räjäyttää joukon. Se perusteltiin serverin portin logiikalla ("degradoi sallivaan suuntaan", B145/B146-linja) — mutta LUKUfunktiolle salliva = numerot villisti väärin. Korjaus: tyhjä `peers` = "kilpailijoita ⊥ TIEDETÄ" → palataan legacy-km-sääntöön (`resolveTaskMarkers`, sama haara kuin V260-jäljetön) ∴ lukema on entinen & konservatiivinen; aggregaatit saivat `peers`-parametrin & kolme kutsupaikkaa antavat sen. SAMA vikaluokka löytyi `marker-focus.ts:24`:stä (V243): se osoitti yhä `resolveTaskMarkers`iin ∴ kartan korostus valaisi merkkejä joita pätkän oma lista ⊥ näytä — kaksi jäsenyystotuutta. Korjattu `markersForSegment`iin + `MarkerManager.setFocusSegment(seg, {peers})`. Miksi testit olivat vihreitä: fixturet syöttivät joukkoja joissa kaikki merkit kuuluivat pätkään joka tapauksessa, & E2E-pätkät olivat tyhjiä. Uusi regressio (`tests/segment-membership.test.ts`) käyttää fixturea jossa merkit ovat reitin ERI päissä ∴ liian iso joukko näkyy heti. | V259-amend → korjattu suoraan |
| B149 | 2026-07-27 | **Teemakierros-vahti pimeni hiljaa jälkigeometrian myötä.** `e2e/t349-map-surface-theme.spec.ts` (2/3 punaista) antaa neljä merkkiä SAMOISSA koordinaateissa (`65.62, 27.62`) & erottaa ne vain `distance_from_start`-kentällä — kelpasi kun jäsenyys oli km-väli, mutta T361:n jälki + V259:n eksklusiivinen sääntö ratkaisee omistajan KOHTISUORALLA etäisyydellä ∴ yksi pätkä vie kaikki neljä merkkiä & toinen jää nollaan → "valmis" ⊥ synny → `.segment-label--done` ⊥ renderöidy → `getComputedStyle(null)` kaataa kolmannen assertin perässä. Tuotantovikaa ⊥ seuraa (oikealla merkillä on oikeat koordinaatit) ∴ tämä on FIXTUREN vika — mutta hinta on tosi: T349/V253:n teemakierros on ainoa vahti B136-luokalle (valmis-vihreä kahdesta lähteestä, pääsi kerran tuotantoon) & se on nyt pimeänä. Yleisoppi: degeneroitunut fixture (∀ merkki samassa pisteessä) ⊥ kaadu kun sääntö on km-väli, mutta se ⊥ MYÖSKÄÄN testaa mitään geometrista — se oli velkaa jo ennen T361:tä. | T365 |
| B150 | 2026-07-27 | **Huomio putoaa kartan keskelle & sinne se jää.** `markers-wiring.ts` `btn-tk-add-note` → `commentModal.openCreate(map.getCenter())` ∴ sijainti on se jonka KARTTA arvasi, ⊥ se jonka käyttäjä tarkoitti — eikä pinniä voi siirtää luonnosvaiheessa eikä jälkikäteen. Havainto koskee KOHTAA maastossa ("tämä silta pitää korjata") ∴ metrit ovat koko sisältö: väärässä kohdassa oleva huomio lähettää järjestäjän väärään paikkaan & on pahempi kuin puuttuva huomio. Alkuperäinen T237(b) vaati sijoitusmoodin (klikkaus + `.map-mode-pill` + Esc); toteutus otti oikopolun kartan keskipisteeseen & se oikopolku ⊥ näkynyt testeissä koska yksikään testi ⊥ väittänyt mitään sijainnin VALINNASTA. | V264,T366 |
| B151 | 2026-07-27 | **Tekijä ⊥ voi korjata omaa huomiotaan.** `DELETE /api/comments/:id` on `requireRole('admin','järjestäjä')` (`comments.ts:239`) & siirtoa ⊥ ole lainkaan ∴ talkoolainen joka huomaa kirjoitusvirheen | väärän sijainnin sekunti lähetyksen jälkeen ⊥ voi tehdä mitään — hänen ainoa keinonsa on soittaa järjestäjälle. Kirjoitusoikeus ilman korjausoikeutta tuottaa kantaan roskaa jota kukaan ⊥ uskalla siivota. | V265,T367 |
| B152 | 2026-07-27 | **Omat kuvat katoavat näkyvistä lähetyksen jälkeen & niitä ⊥ voi poistaa.** Kaksi vikaa samassa polussa: (a) `comment-point-modal.ts` lataa kuvan & kutsuu `onChanged()`, mutta AVOINNA olevaa näkymää ⊥ renderöidä uudelleen ∴ käyttäjä ⊥ näe juuri lataamaansa kuvaa & lataa sen toistamiseen (kiintiö V262 täyttyy kahdesta samasta kuvasta); (b) `DELETE`-endpointia kuvalle ⊥ ole lainkaan ∴ väärä | epäonnistunut kuva jää kantaan ikuisesti. Kentällä otettu kuva on todiste — jos sitä ⊥ näe heti, ⊥ tiedä onnistuiko koko teko. | V264,V265,T366,T368 |
| B153 | 2026-07-27 | **Järjestäjän "missä tämä on" hautautuu modaalin alle.** `CommentPanel`in rivi kutsuu `onFocus` joka panoroi kartan JA avaa `commentModal.openView`in (`markers-wiring.ts`) ∴ kartta liikkuu oikeaan kohtaan & modaali peittää sen välittömästi. Rivin klikkaus on kysymys "missä?", ⊥ "mitä?" — sama virhe jota `segment-view.ts:317` (keräyslista) & T347 (nimilappu) välttävät tarkoituksella. | V266,T369 |
| B154 | 2026-07-27 | **Talkoolaisen oma huomio syntyy harmaana & luetaan poissuljetuksi.** T237(d):n fokus-integraatio (2026-07-27, tämä sessio) kytki `commentLayer.setFocusActive(true)` MYÖS talkoolaisen automaattifokukseen (`markers-wiring.ts`, T335: talkoolaisella korostus on automaatti ⊥ kytkin) ∴ ∀ huomio on hänen näkymässään pysyvästi himmennetty — ml. se jonka hän juuri jätti. V243 luettiin liian leveästi: "himmennä konteksti" ⊥ tarkoita "himmennä kaikki mikä ⊥ ole merkki". Testi (`t237-comment-focus-dim.test.ts`) väitti oikein että himmennys toimii, mutta ⊥ kysynyt KENELLE se saa tapahtua ∴ vihreä testi vahvisti väärän säännön. | V266,T370 |
| B155 | 2026-07-27 | **Huomion tallennus hajosi luonnos-refaktoroinnissa (T366) — POST ilman pituusastetta.** `markers-wiring.ts`:n `draftPosition`-fallback luki `map.getCenter()`:n suoraan `{lat,lon}`-muotoon castilla, mutta Leafletin `LatLng` kantaa pituusasteen nimellä `lng` ∴ `lon` oli `undefined` & serveri hylkäsi rivin (`missing_coordinates`, 400). Osui KAHTEEN kolmesta sisääntulosta (hero-⋯ & merkkipickerin alapalkki) koska vain yläpalkin nappi loi luonnospinnin — kolme rinnakkaista `openCreate`-kutsupaikkaa ajautuivat erilleen samassa committissa jossa ne syntyivät. **Miksi testit olivat vihreät:** Vitest-testit rakensivat modaalin SUORAAN & antoivat `draftPosition`in itse ∴ ne testasivat modaalia ⊥ wiringiä; e2e tarkisti että nappi on NÄKYVISSÄ ⊥ että klikkaus tallentaa. Oppi: `as unknown as T` -cast rajapinnan yli on tyyppitarkastuksen sammuttaminen juuri siinä kohdassa jossa kaksi nimeämiskäytäntöä (`lng` vs `lon`) kohtaa — ja käyttäjäpolun päästä-päähän-testi on se joka nappaa wiring-tason vian, ⊥ komponenttitesti. Korjaus: yksi `openCommentDraft`-sisääntulo ∀ polulle + fallback palauttaa `undefined`in (modaali käyttää avaushetken koordinaatteja) ⊥ castattua olio. | V264,T366 |
| B156 | 2026-07-28 | **Leafletin zoom-napit 30×30px — alle §A-kosketusminimin, & vahti ⊥ nähnyt sitä koskaan.** Löytyi kun T373 laajensi kosketusvahdin valitsimen `button` → `button, [role="button"]` (V268): Leaflet renderöi zoomin `a[role="button"]`-linkkeinä ∴ vahti joka assertoi `violations.toHaveLength(0)` mittasi vain osajoukon & lupasi silti §A:n pitävän. Rikkomus on ollut livenä koko ajan; V122 edellyttää zoomilta ≥44px hit-areaa (se puhuu vain sijainnista, ⊥ koosta ∴ koko jäi kirjaamatta). Sama juurisyy kuin section-headereilla (~28px): tagi-pohjainen mittari. Korjaus: `.leaflet-control-zoom a` 44×44px (`style.css`). Vahdin laajennus = pysyvä kate; V268 kieltää valitsimen kapeuttamisen vihreän saamiseksi. | V268,V122 → T373 |
| B157 | 2026-07-28 | **Reitin piilotus jättää pätkäviivat & nimilaput leijumaan.** `applyVisible` (`route-visibility-control.ts:55-63`) hoitaa polylinen (`addTo`/`remove`) + `markerManager.setVisibleRoutes(ids)`, mutta `SegmentOverlay` ⊥ kuule reittinäkyvyyttä lainkaan ∴ piilotetun reitin pätkäviivat (T336-kolmikko) & nimilaput jäävät kartalle ilman reittiä jonka päällä olisivat. Kartta väittää pätkän olevan siellä missä ⊥ ole reittiä. Löytyi järjestäjän suodatinpyynnöstä 2026-07-28 ("reittien päällä olevat pätkät ei häviä"). | V269 → T374 |
| B158 | 2026-07-28 | **Järjestäjän korostus himmentää vain merkit, ⊥ pätkäviivoja.** `setFocusSegment` → `focusState` (V243) koskee merkkejä; pätkäviivan himmennys `contextSegmentStyle` (`segment-overlay.ts:41`, `CONTEXT_DIM_OPACITY 0.22`) laukeaa VAIN `contextOwnId`:stä = talkoolaisen automaattifokus (V142) ∴ järjestäjän `◎ Korosta vain tämä pätkä` (T335/T356) jättää muut pätkäviivat täyteen kirkkauteen ("jos laittaa että korosta tätä pätkää niin muut pätkät näkyy myös vahvasti"). DESIGN §K MarkerFocus väittää jo "sama visuaalinen kieli kuin contextSegmentStyle tekee pätkäVIIVOILLE" — koodi ⊥ toteuta väitettään. Juurisolmu: himmennys & interaktiolukko samassa boolissa (V270). | V270 → T375 |
| B159 | 2026-07-28 | **Aukkonäkymä on ollut olemassa koodissa muttei koskaan silmässä.** `computeGapRanges` (`segment-overlay.ts:301`) laskee reitin kattamattomat osuudet & piirtää ne (`GAP_COLOR #94a3b8`, `weight 8`, `opacity 0.3`) — maastokartan päällä käytännössä näkymätön ∴ järjestäjä pyysi 2026-07-28 featurea joka oli jo rakennettu ("näyttää selkeästi missä ei ole vielä pätkää"). Sama perceptuaalinen virhe kuin §K SegmentOverlay alfa-alaraja 0.4→0.7 (UX-audit 2026-07-27): tila jonka pitäisi huutaa kuiskaa. Lisäksi puhdas laskenta asui `src/map/`ssä ∴ Leaflet roikkui Taso 1 -testeissä. PM-päätös: ⊥ kirkasteta vaan POISTETAAN — aukko luetaan paljaasta reitistä (V273), kolmas visuaalinen kanava reitin & pätkän päällä on kohinaa. | V273 → T378 |
| B160 | 2026-07-28 | **Suodatinbarin napit ⊥ tavoitettavissa mobiilissa** (käyttäjätesti heti T377:n jälkeen: "ei toimi mobiilissa napit"). Neljä dropdown-triggeriä ⊥ mahdu 375px-riviin: mitattu `Himmennys` alkoi `x=379` eli ruudun ULKOPUOLELTA, & `overflow-x:auto` ⊥ kanna mitään vihjettä vaakaskrollista ∴ kaksi neljästä akselista ⊥ ollut olemassa kosketuskäyttäjälle. Vika ⊥ ollut klikkauksissa vaan LAYOUTISSA — automaattitesti klikkasi ne läpi (Playwright skrollaa kohteen näkyviin) ∴ vihreä E2E antoi perusteettoman luottamuksen (V268:n yleisoppi toistui). | V274 → T377-jatko (mobiilin oma valikko: yksi "Suodata (N)" -nappi → bottom sheet jossa kaikki 4 osiota haitareina) |
| B161 | 2026-07-28 | **Suodatinpaneeli jäi kartan taakse desktopilla** (käyttäjä heti deployn jälkeen). Syy ⊥ ollut `z-index` — paneeli 1000, Leafletin panet 400 ∴ järjestys oli oikein. Barin `overflow-x:auto` (lisätty ahtauden varalta) LEIKKASI absoluuttisen lapsipaneelin barin alareunaan: `elementFromPoint` paneelin keskeltä palautti `leaflet-container`. Paneeli oli DOM:issa, `toBeVisible` vihreä & `boundingBox` oikea ∴ kaikki olemassa olevat vahdit läpäisivät. Vaakaskrolli oli väärä lääke ahtauteen jonka B160:n mobiilivalikko hoitaa oikein. | V274-amend → `overflow:visible` + sheet-tila ≤700px + `elementFromPoint`-vahti 5 leveydellä |
| B162 | 2026-07-29 | **Luonnin klikit 2..n ⊥ näy kartalla** (tuotanto, järjestäjä). `src/ui/segment-panel.ts:158` kutsuu `onFirstPoint`ia VAIN `vaihe1`-haarassa; `polku`-haara (:176) päivittää pelkän `creationModal.updatePhase(state)` ∴ `src/app/segments-wiring.ts:143` piirtää TASAN yhden `tempCreationMarker`in (ensiklikki) joka jää paikoilleen loppuflown ajaksi. Järjestäjä klikkaa reittiä pitkin karttaa johon ⊥ ilmesty mitään — ainoa palaute on modaalin ankkurilista jota hän ⊥ katso klikatessaan. Luonti näyttää kuolleelta vaikka tila päivittyy oikein. | V280→T388 |
| B163 | 2026-07-29 | **"Koko pätkä ⊥ tallennu" samassa istunnossa — JUURISYY ⊥ VARMISTETTU.** Fly-logi (`flyctl logs -a karttamaster-web`) sulkee serverin pois: `POST /api/segments -> 201 (järjestäjä)` 06:05:36, `POST /api/markers -> 201`, `PUT /api/markers/<id> -> 200`, EI yhtään 4xx/5xx sovellusreitillä. Ainoat ERROR-rivit = fly-infraa (autostop, health-check port 80 käynnistyksessä, `error umounting /data: EBUSY`) ∴ ⊥ liity. Epäily: vaiheeseen 3 (tiedot) ⊥ koskaan päästy — `nextAnchorIndex(route.routePoints, lat, lon, last.idx+1, …)` hakee vain ETEENPÄIN (V258/B144, oikein) & reitin suuntaa vastaan klikkaava saa `null`, jolloin ainoa palaute on `setError` modaalin pohjalla ankkurilistan alla ∴ "Valmis" pysyy disabloituna (`segment-creation-modal.ts:179`) & mikään ⊥ tallennu. Paikallinen repro on T388(c):n ensimmäinen askel; jos juurisyy osoittautuu muuksi, tämä rivi AMENDATAAN ⊥ suljeta hiljaa. | V280→T388 |
| B164 | 2026-07-29 | **Ensiklikki lukitsee reitin jota ⊥ voi vaihtaa** (järjestäjä, tuotanto, sama istunto kuin B162/B163). `segment-panel.ts resolveClick` iteroi KAIKKI reitit `nextAnchorIndex(…, 0, Infinity)`-kynnyksellä & valitsee globaalisti lähimmän kärkipisteen ∴ 3 SMTB-reittiä ≤100 m toisistaan → metrien ero ratkaisee. Lukituksen jälkeen ainoa ulospääsy on Peruuta → aloita alusta (⊥ "vaihda reitti"), & seuraava klikki toisen reitin varrella tuottaa "Ei osumaa reitillä 55 km eteenpäin" vaikka käyttäjä klikkaa täsmälleen näkemänsä reittiviivan päälle. T362 teki valinnan näkyväksi (B144(a)) muttei vaihdettavaksi — puolikas korjaus. HUOM ⊥ sama asia kuin reitiltä toiselle JATKUVA pätkä: se rikkoisi V258:n & vaatii oman specinsä. | V281→T389 |
| B165 | 2026-07-29 | **Reitin ainoa pätkä omistaa koko reitin merkit.** `segment-membership.ts:100-112` ratkaisee omistuksen kynnyksettömästi (`lähin jälki voittaa`, V259) & rajaa ehdokkaat vain reittijäsenyydellä (V25). Kun reitillä on VAIN yksi pätkä, sillä ⊥ ole kilpailijaa ∴ se voittaa jokaisen merkin koko reitin pituudelta. Mitattu tuotantokopiosta `backups/karttamaster-20260729-065755.db`: G62 (sgf-62, jälki 14.27 km) omisti **38** merkkiä joista **25 >100 m** & **21 >1 km** päässä jäljestä, kauimmainen **10 548 m**. Sama vika lievempänä Pätkä 8:lla (smtb-55: 14, pitäisi 10). Kynnysmallinnus samasta datasta: 300 m → G62 14, 200 m → 14, 100 m → 13; naapuripätkät menettävät 0–1 merkkiä ∴ kynnys ⊥ riko toimivaa jakoa. ⊥ regressio T362/T388/T389:stä — vika on ollut olemassa T359/V259:stä asti & paljastui vasta kun reitille tehtiin ensimmäinen pätkä. | V283→T391 |
| B167 | 2026-07-29 | **E2E-fixturet arpoivat koordinaatit ∴ V283:n kynnys pudotti ne pätkältä.** 6 testiä punaisena (`t309-marker-drag-rollback` ×2, `t312-hero-nav-edges` ×2, `t315-koti-scroll` ×2) — ⊥ flaky & ⊥ tuotebugi. Fixturet käyttivät keksittyä ruudukkoa (`lat: 65.6 + i*0.001`, `lat: 65.60/65.61/65.62`) joka on **1191 m** smtb-30:n jäljestä; kynnyksetön V259 hyväksyi sen ("lähin jälki voittaa" & kilpailijoita ⊥ ollut), T391/V283:n 200 m `MEMBERSHIP_THRESHOLD_M` ⊥ hyväksy. Seuraus: pätkä ⊥ omista merkkejä → t315:n lista 30 → **6** riviä, t312:n hero ⊥ renderöi ◀▶ lainkaan, t309:n merkki sai `marker-dimmed--locked` (fokus-dim, `markers.ts:212`) ∴ ⊥ ollut raahattava. Sama juurisyyluokka kuin E2E-NOTES §4 (kartan mittakaavaa ⊥ koodata testiin): fixture koodasi GEOMETRIAN. ⊥ regressio T397:stä — todennettu ajamalla sama joukko ilman `gps-navigator.ts`-muutosta (6 punaista myös silloin). | ✓ UUSI `e2e/helpers/route-points.ts` lukee koordinaatit oikeasta GPX:stä (`pointAtDistance`, `pointsAlongRoute`) ∴ reittipäivitys siirtää fixturet mukanaan; 3 spekkiä siirretty käyttämään sitä; E2E-NOTES §7 |
| B166 | 2026-07-29 | **GPS-sijaintipiste jää pätkä- & reittiviivojen alle** (talkoolainen, tuotanto, käyttäjän raportoima). `gps-navigator.ts:101` luo `L.circleMarker` ILMAN `pane`-optiota ∴ piste menee oletus-`overlayPaneen` (z-index 400) samaan SVG-rendereriin kuin `RouteLayer`- & `SegmentOverlay`-polylinet (`segment-overlay.ts:214,267` `addTo(this.map)`) & `AreaOverlay`-polygonit. Samassa panessa järjestyksen ratkaisee DOM-lisäysjärjestys ∴ jokainen `SegmentOverlay.update()` (pätkän luonti/muokkaus/refresh) piirtää pätkäviivan GPS-pisteen PÄÄLLE. Merkki-ikonit (`markerPane` 600) & pätkälaput (`tooltipPane` 650) ovat pysyvästi pisteen yllä ∴ käyttäjän sanoin "jää kaiken alle". Koko `src/`:ssä ⊥ yhtään `map.createPane()`-kutsua — mikään ⊥ nosta GPS-pistettä. ⊥ regressio: vika on ollut T341/GpsNavigatorin synnystä asti & piiloutui koska kehityskartalla on harvoin pätkä juuri sijainnin päällä. `.gps-dot`-luokalle ⊥ myöskään ole CSS:ää `style.css`:ssä. | V287→T397 |
